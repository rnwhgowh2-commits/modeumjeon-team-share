# 판매처(마켓) 연동 대시보드 — 카드 재디자인 구현 요청 프롬프트

> 다른 세션에 그대로 붙여넣어 사용. 아래 디자인 3종은 시안 비교 끝에 **확정된 안**임.
> 참고용 동작 목업: `바탕화면/모음전 판매처연동 재디자인 시안 v1.html` (열어서 M3·F10·H2 탭 확인 가능)

---

## 0. 배경/목표

"판매처 연동" 대시보드 — 모음전(여러 상품 묶음) 하나가 여러 판매 마켓(쿠팡·스마트스토어·G마켓·11번가·토스쇼핑·무신사 …)에 업로드되고, 각 마켓의 매칭·재고·가격·전송 현황을 카드로 보여주는 화면. **판매 마켓은 계속 늘어날 예정**이라, 마켓이 늘어도 위상이 한눈에 구분되고 레이아웃이 안 깨지는 구조가 핵심.

목록은 카드 리스트. 카드 1개 = 모음전 1개. 카드 안에 마켓별 현황을 표로 보여준다.

아래 3가지를 구현해줘.

---

## 1. 카드 내 마켓 현황 = **M3 매트릭스 표**

카드 안에 **고정폭 표** 하나. 칼럼 5개:

| 마켓 | 매칭 | 재고 (소/판) | 가격 (소/판) | 전송 |
|------|------|-------------|-------------|------|

- **마켓**: 마켓 로고(브랜드색 정사각 타일 + 2글자 약자) + 마켓명. 행 = 마켓 1개. (마켓 늘면 행만 추가 → 안 깨짐)
- **매칭**: `매칭수/전체수` (예 `76/102`). 색: 완료(=전체)=초록 `#0F8A4D`, 부분=주황 `#B45309`, 미연동(0)=회색 `–`
- **재고 (소/판)**: 상단 `있음`, 그 아래 **2줄 타임스탬프**
  - `소` = **소싱처 크롤 일자** (우리가 소싱처를 크롤한 시각)
  - `판` = **판매처 재고 API 일자** (판매처에서 재고를 API로 읽은 시각)
- **가격 (소/판)**: 상단 `최신`, 그 아래 2줄
  - `소` = 소싱처 크롤 일자 / `판` = 판매처 가격 API 일자
- **전송**: 전송됨 + 전송 일자 / 미전송
- 미연동 마켓 행은 재고·가격 `–`, 전송 `미전송`
- 일자 포맷 = `26.06.28` (위) + `09:30` (아래), 날짜·시간까지. 숫자는 `tabular-nums`로 자리폭 고정
- `소`/`판` 미니 라벨: 소=파랑 `#185FA5`, 판=갈색 `#854F0B` 배지
- 표 위에 범례 1줄(`소 소싱처 크롤 일자 · 판 판매처 API 일자`) + "재고·가격 셀 클릭 → 변동 이력" 안내

### ⭐ 정렬 필수 규칙 (반드시 지킬 것)
여러 카드의 표 세로선이 **카드끼리 정확히 일치**해야 함. 칼럼 폭이 내용 길이에 따라 카드마다 달라지면 안 됨.
→ `table { width:100%; table-layout:fixed }` + `<colgroup>` 고정폭(예 25 / 13 / 23 / 23 / 16 %)로 강제.

### 마켓 색 팔레트(확장 가능)
쿠팡 `#E03A3E` · 스마트스토어(네이버) `#03C75A` · G마켓 `#00A862` · 11번가 `#FF0038` · 토스쇼핑 `#3182F6` · 무신사 `#1a1a1a`. 새 마켓 추가 시 고유색 1개씩 배정.

### 클릭 동작
**재고 셀 / 가격 셀 클릭 → 해당 항목의 "변동 이력"(아래 3번 H2)** 패널을 연다.

---

## 2. 목록 필터 = **F10 2단(좌 필터 트리 + 우 결과)**

화면(또는 카드 리스트 영역)을 **좌우 2단**으로:

- **좌측 고정 필터 트리 패널**
  - `브랜드 (OR)` 체크박스 그룹 (르무통 / 나이키 / 커버낫 …)
  - `└ 등록된 마켓` 체크박스 그룹 (쿠팡 / 스마트스토어 / G마켓 / 11번가 / 토스쇼핑 / 무신사 …)
- **우측 결과 목록**: 필터 통과한 카드들 + 상단에 `결과 N건`

### ⭐ 필터 로직 (반드시 지킬 것)
- **브랜드 = OR 조건**: 여러 브랜드를 골라도, 모음전이 가진 브랜드 중 **하나라도** 선택 브랜드에 걸리면 포함.
  - 예) 브랜드가 "르무통+나이키" 둘 다 섞인 합동 모음전 → '르무통'만 골라도, '나이키'만 골라도, 둘 다 골라도 **포함**됨.
- **마켓 = OR 조건**: 모음전에 연결된 마켓 중 하나라도 선택 마켓에 걸리면 포함.
- **브랜드 그룹 ↔ 마켓 그룹 사이 = AND**: (브랜드 조건 만족) 그리고 (마켓 조건 만족). 각 그룹 내부는 OR.
- 아무것도 선택 안 하면 전체 표시.

---

## 3. 재고/가격 변동 이력 = **H2 2열 대조** (셀 클릭 시 패널)

재고 또는 가격 셀 클릭 시 뜨는 변동 이력 화면.

- 패널 헤더 예: `📦 메이트 · 쿠팡 — 재고 변동 이력` (상품 · 마켓 · 항목명)
- **상단 가로탭: [재고] [가격]** — 탭으로 어떤 항목의 변동을 볼지 전환
- 본문 = **2열 대조**
  - 좌측 열 = **소싱처 크롤** (헤더 파랑 `#185FA5`)
  - 우측 열 = **판매처 API** (헤더 갈색 `#854F0B`)
- 각 열 = 시간 역순 변동 리스트. 한 줄 = `일시` + `이전값 → 이후값` + 델타(▲/▼)
- 값이 바뀔 때마다 이벤트 누적: `{ 일시, 출처(소싱처/판매처), 이전값, 이후값 }`

### ⭐ 색상 규칙 (한국 증시 관례 — 반드시 지킬 것)
재고·가격 **공통**:
- **값이 떨어짐(감소) = 파란색** `#1d4ed8`, 화살표 ▼
- **값이 올라감(증가) = 빨간색** `#dc2626`, 화살표 ▲

> ⚠️ 흔한 실수: 일반 웹 관례(증가=초록/감소=빨강)와 **반대**다. 여기선 **하락=파랑, 상승=빨강**으로 한다.

---

## 4. 공통 규칙

### ⭐ 편집 / 삭제 버튼 동일 크기
카드의 `편집`·`삭제` 버튼은 **완전히 같은 크기**(같은 `min-width`·`height`·`padding`).
- 편집 = 파란 외곽선(`#3182f6`), 삭제 = 빨간 외곽선(`#e03a3e`), 둘 다 흰 배경.

### 데이터 모델 메모
- 재고·가격은 각각 **두 출처의 타임스탬프**를 가짐: 소싱처 크롤 일자 / 판매처 API 일자. (둘이 벌어져 있으면 "마켓이 아직 최신화 안 됨"을 뜻함)
- 변동 이력은 출처(소싱처/판매처)별로 값 변경 이벤트를 시계열로 누적.

### 톤
Pretendard 폰트, 토스 느낌(둥근 모서리·연한 회색 라인·블루 포인트). 색은 최소·일관되게.

---

## 5. 산출
M3 카드 표 + F10 2단 필터 + H2 변동이력 패널을 만들고, 위 ⭐ 규칙(고정폭 정렬 / 브랜드 OR / 하락=파랑·상승=빨강 / 버튼 동일크기) 4가지를 모두 충족시켜줘. 막히면 바탕화면 목업 HTML을 열어 동작을 참고.
