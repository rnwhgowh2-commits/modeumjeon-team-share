# 다른 세션 전달 — 정산조회 「계정 병렬 + 적응형」 전략을 전 마켓에 적용

> 작성: 2026-07-25 세션 (옥션·G마켓에서 먼저 구현·라이브 검증 완료)
> 목적: 방금 옥션·G마켓에 넣은 정산 스윕 전략을 **나머지 마켓(쿠팡·스마트스토어·롯데온·11번가)** 에도 같은 원리로 적용해 주세요. 그리고 **마켓별 조회기간을 최대로 잡는 게 유리한지** 반드시 확인해 주세요.

---

## 1. 배경 — 무슨 문제였나

사장님 신고: 마진계산기 정산금이 0으로 뜨거나 추정치로 남아 있었다.
두 원인 모두 **에러를 안 남겼다**(실패가 아니라 「안 본 것」이라 로그·경보 전무 → 43건이 3개월간 조용히 고착).

1. **금액과 근거 배지가 갈라짐** — 저장 병합(`order_store._merge_row`)이 빈 값은 안 덮는데 `"none"`은 빈 값이 아니라 덮어서 `_settle_source`가 소실. 마진계산기는 근거 없는 금액을 안 믿어 0으로 봄. (PR#460·462에서 수정)
2. **정산은 구매확정 「뒤에」 확정되는데 끝난 주문을 다시 안 봄** — 증분 수집은 최근 21일만 훑고, 그 창이 닫힌 뒤 마켓에 실정산이 들어와도 영영 못 받아옴. (PR#466에서 수정 — 정산조회만 다시 훑는 스윕 신설)

---

## 2. 옥션·G마켓에서 한 것 (참고 구현)

**핵심 파일**
- `lemouton/markets/order_ingest.py` → `refresh_settlement(market, ...)` : 정산조회만 다시 훑어 저장분에 실값을 얹음. **주문 조회는 안 함**(정산조회 API만).
- `shared/platforms/esm/settlements.py` → `settle_detail_map(...)` + `_request_with_rate_backoff(...)`
- `scheduler/main.py` → `_order_settle_sweep_tick()` : 30분마다 자동(주문 틱에서 분리).
- `webapp/routes/orders.py` → `POST /orders/settlement-sweep/run` (수동 넓은 스윕), `GET /orders/diag/esm-settlement` (기준일 비교 진단, 읽기 전용).
- `sell_source._stale_settle_notice` : 40일 지나도 실정산이 안 오면 마진 화면에 건수 안내(재발 감시).

**전략 3원칙 (이걸 그대로 이식)**

### ① 정산조회는 주문조회와 「다른 rate 버킷」이다 — 먼저 확인할 것
- 옥션·G마켓의 5초/1콜 제한은 `client._throttle_orders`가 `is_order=True`에만 건다. 정산조회(`request_settlement`)는 그 버킷을 안 씀 → 정산 스윕이 주문내역 화면 조회를 느리게 만들지 않는다.
- **각 마켓에서 정산조회 API가 주문조회와 같은 rate 한도를 공유하는지 반드시 확인**하세요. 공유하면 스윕이 주문 화면을 굶길 수 있으니 간격/버킷을 분리해야 합니다.

### ② 최대속도 = rate 버킷 단위로 병렬
- ESM 제한은 **seller 계정별**(`_throttle_key` = master·seller·site). 서로 다른 계정은 완전 독립 → **계정 수만큼 동시 실행**이 이론상 최대치.
- 같은 계정 안을 더 쪼개 병렬해 봤자 같은 버킷이라 3000만 유발(무의미).
- 구현: `refresh_settlement`에서 계정별 `ThreadPoolExecutor(max_workers=min(계정수, 8))`로 **조회는 병렬**, **DB 쓰기는 조회 뒤 단일 세션으로 직렬**(스레드 안전).
- **각 마켓의 rate 한도가 무슨 단위인지 확인**하세요(계정별? 앱키별? 전역?). 병렬 축은 그 단위와 같아야 합니다.

### ③ 한도에 걸리면(3000류) 조용히 실패하지 말고 「적응형 백오프」
- `_request_with_rate_backoff`: 호출제한 코드가 오면 즉시 실패로 굳히지 않고 지수 백오프+지터로 재시도. **기본 경로(안 걸림)는 대기 0**이라 최대속도 유지. 계속 걸리면 결국 예외로 올림(조용히 0건 삼키지 않음).
- 한 워커가 죽어도 나머지 계정은 저장하고, 실패는 `errors`에 담아 정직 보고.

**라이브 실측 (365일 × 3계정, 막힘 0)**
| 마켓 | 계정 | 정산 조회건수 | 소요 |
|---|---|---|---|
| G마켓 | 3 | 658건 | 17.3초 |
| 옥션 | 3 | 281건 | 6.5초 |

관련 PR: #460 #462 #466 #473. 메모리: `project_margin_settle_synced_to_orders_tab.md`, `project_margin_settle_sync_session_20260725.md`.

---

## 3. 다른 세션이 할 일 — 나머지 마켓에 이식

각 마켓엔 이미 정산 모듈이 있습니다(전부 다름 — 통째로 복붙 금지):
- 쿠팡 `shared/platforms/coupang/settlements.py` — ⚠️ **이미 정산 스윕이 있음**(인식일 창·`(주문번호,옵션ID)`키). 병렬·적응형이 들어갔는지 확인하고 없으면 보강.
- 스마트스토어 `shared/platforms/smartstore/settlements.py`
- 롯데온 `shared/platforms/lotteon/settle_orders.py`, `settlement.py`
- 11번가 `shared/platforms/eleven11/settlement.py`

**이식 체크리스트 (마켓마다)**
1. [ ] 정산조회 API가 **주문조회와 rate 버킷을 공유하는지** 실측(공유하면 스윕이 주문 화면을 굶긴다 → 분리 필요).
2. [ ] rate 한도의 **단위**(계정별/앱키별/전역) 확인 → 병렬 축을 그 단위로.
3. [ ] `refresh_settlement`류 함수에 **계정 병렬** 적용(단위가 계정이면).
4. [ ] 호출제한 응답에 **적응형 백오프**(기본 대기 0, 걸리면 물러섬, 지속되면 예외).
5. [ ] 정산은 **구매확정/정산확정 뒤에** 들어오므로, 그 시점에 다시 안 보던 주문이 있는지 확인(우리처럼 「끝난 상태」로 분류해 재조회를 건너뛰는 곳이 있나).
6. [ ] 30분 스케줄러 틱에 편입 + 수동 넓은 스윕 라우트 + 재발 감시(오래된 미정산 안내).
7. [ ] **금액과 근거 배지(`_settle_source`)가 갈라질 수 있는지** 확인 — 저장 병합에서 근거가 지워지면 마진계산기가 그 돈을 0으로 본다(우리 ①번 버그와 동형).

---

## 4. 🔴 반드시 확인할 것 — 「조회기간을 최대로 잡는 게 유리한가?」

직관적으로는 **창을 최대(마켓 상한)로 잡으면 호출 수가 줄어 빠를 것** 같습니다. 하지만 **항상 유리하지 않습니다.** 실측으로 확인하세요.

**마켓별 조회창 상한 (`order_ingest.CHUNK_DAYS`, 2026-07-20 실측)**
| 마켓 | 증분 상한 | 정산/백필 창 | 초과 시 |
|---|---|---|---|
| smartstore | 1일 | — | 2일부터 400 |
| lotteon | 1일 | 정산 API 29일 | 2일부터 returnCode 2003 |
| eleven11 | 7일 | — | 🔴 **초과 시 에러가 아니라 조용히 0건** |
| coupang | 31일 | 백필은 **7일**로 줄임 | 큰 창이 300초 초과로 통째 스킵 |
| gmarket | 31일 | 31일 | 181일부터 3000(auction) |
| auction | 180일 | 백필은 **90일**로 절반 | 워커 step 45초 예산 초과 시 스킵 |

**최대가 유리하지 **않은** 이유 (실측 근거)**
- **타임아웃 벽**: 창을 넓히면 한 요청이 오래 걸린다. 쿠팡은 상한이 31일이지만 데이터가 많은 과거 구간에서 30일 창이 **300초를 넘겨 통째로 스킵**됐다 → 백필은 7일로 줄여 각 창을 빠르게(호출 수는 늘지만 안 잃음). gunicorn step(45~60초)·Cloudflare(100초) 상한에 걸리면 넓은 창이 오히려 **데이터를 통째로 잃는다**.
- **조용한 상한**: 11번가는 7일을 넘기면 에러 없이 **0건**을 준다 → 넓게 잡으면 주문이 소리 없이 사라진다. 늘리기 전에 반드시 실측.
- **페이지네이션**: 창이 커도 한 응답은 `PageRowCnt`(ESM 500)에 묶여 여러 페이지가 된다 → 넓은 창이 콜 수를 항상 줄이는 것도 아니다.

**따라서 판단 기준** = 「한 창의 소요가 워커/게이트웨이 타임아웃 예산 안에 드는 최대 창」. 무작정 상한이 아니라 **실측한 안전 창**. 각 마켓에서:
1. [ ] 최대 창으로 한 요청 실측 소요 측정.
2. [ ] 그 소요가 gunicorn step / Cloudflare 100초 안인지 확인.
3. [ ] 초과하면 창을 줄여 각 요청을 빠르게(호출 수는 늘어도 **통째로 잃는 것보다 낫다**).
4. [ ] 11번가처럼 **초과를 0건으로 알리는 마켓**인지 먼저 확인(늘리기 전 필수).

---

## 5. 주의 (공통 함정)

- **워커가 3개다** — 프로세스 전역 변수는 저장이 아니다. 스로틀·커서는 DB 행으로(과거 `esm_order_throttle` 함정과 동형).
- **폴백 SQLite면 건너뛴다** — 정산 스윕도 `_is_sqlite`면 리턴(테스트 잔재 오염 방지).
- **없는 정산을 0으로 채우지 말 것** — 정산조회에 없는 주문 = 아직 미정산 = 그대로 둔다.
- **클레임 행(`_kind='change'`)·취소완료는 건드리지 말 것** — 취소·반품 정산은 zero_cancel·실정산 조인이 담당.
- **큰 스윕은 동기 HTTP 한 방으로 하지 말 것** — 30분 스케줄러(백그라운드)로. 수동 라우트는 병렬로 100초 안에 들게.
