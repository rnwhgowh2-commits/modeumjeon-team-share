# 다음 세션 이어받기 — 노션 「주문관리」 개선 (2026-08-12)

> 이 문서 하나로 이어받을 수 있게 썼다. **원 세션은 삭제됨** — 여기와 memory 가 전부다.
> 작업 워크트리 `_wt_ordmgmt` · 브랜치 `feature/orders-settle-fixes`

---

## 0. 30초 요약

| | |
|---|---|
| 무엇을 했나 | 노션 「주문관리」 칸 **a~d 14개 항목** + 대화 중 추가 요구 **11개** = **25개 전부** 코드 반영·머지 |
| 배포 | PR **#960·#965·#974 전부 머지 완료** (#974 = 2026-08-12 13:27) |
| 남은 일 | **6개** (§5) — ①은 다른 세션이 닫았고, 스스 대조가 새로 들어갔다 |
| 최우선 | ~~§5-①~~ → **2026-08-13 다른 세션이 닫음**(아래 참조). 다음 최우선 = §5-①-b 스스 대조 |
| 사장님 요구 수준 | *"완벽 완벽 완벽해야해"* — 「됐다」 전에 실측 증거를 붙일 것 |

## 1. 🔴 작업 폴더 (틀리면 하루를 버린다)

저장소 루트 `C:\dev\모음전 프로젝트` 는 `feature/crawl-worker-queue` 로 origin/main 대비
**1,600+ behind 인 stale 라인**이다. **읽는 것만으로도 오보가 난다**(과거 실측: 이 폴더 코드를
읽고 사장님께 틀린 보고 3건). memory `project_crawl_worker_queue_branch_stale`.

→ `git worktree add <새폴더> origin/main` 으로 새로 파거나 기존 `_wt_ordmgmt` 를 쓴다.
**어느 쪽이든 먼저 `git log --oneline -3 origin/main` 으로 #974 가 들어있는지 확인.**

🔴 **코드 경로는 전부 `프로그램/_시스템/` 기준 상대경로**다.
예: `_wt_ordmgmt\프로그램\_시스템\lemouton\margin\settle_recon.py`. 루트에서 찾으면 없다.

---

## 2. 노션 원문 ↔ 처리 결과 (한 줄도 빠짐없이 — 대조표)

노션: **무재고 모음전 솔루션 개발현황 > ■ 프로그램 개선사항 > 주문관리**

### a. 주문상태 보기 및 탭재배치

| 노션 원문 | 상태 | 결과 |
|---|---|---|
| **a-1** 실마켓 주문상태 : 전체/이행/미이행/클레임 **기능** 및 재배치 | ✅ | 재배치 완료. 「기능」은 **사장님이 되물으신 것**("이건 내가 오히려 물어본거야") → §7 에 답변 보관. 새로 만들 기능 없음 |
| **a-2** 사용자용 주문상태 : 주문관리별 재배치 | ✅ | `#ostTabs` 위치 이동. `tests/orders/test_orders_layout_order.py` 로 순서 잠금(뮤테이션으로 RED 실증) |
| **a-3** 상세 주문내역에 연속해서.. | ⚠️ | 노션 문장이 **끊겨 있다**. 「탭 재배치가 표까지 이어진다」로 해석해 b-1 로 처리. **사장님 확인 못 받음** |

### b. 상세 주문내역

| 노션 원문 | 상태 | 결과 |
|---|---|---|
| **b-1** 재배치 필요 | ✅ | 사장님이 **1~32 번호로 직접** 지정 → 그대로(§3) |
| **b-2** 롯데온 추정 맞음? 정산예정에서 확인하면 100% | ✅ | **금액은 이미 실측**(147건 중 146건 `real`), 추정인 건 **받는 날**뿐. 「금액 근거」 배지를 날짜 근거와 분리 + `seStdDt` 배선(§4-B) |
| **b-3** 실마진 열 + 추정 딱지(사유·근거 호버) + 「매입가 없음」 | ✅ | 라이브 손계산 일치 `56,565−50,596=5,969`. 366줄 중 77줄 계산·289줄 「매입가 없음」·25줄 「추정」 |

### c. 정산예정금액

| 노션 원문 | 상태 | 결과 |
|---|---|---|
| **c-1** 상세내역 추가: 마켓 계정이름/주문상태/주문일자/**수령자명** | ✅ | 서버는 수령자만 빠져 있었다(나머지 4개는 있는데 화면이 안 그림). 두 표 모두 |
| **c-2** 받는날 기준 : 과거 정산 받은 이력 | ✅ | `past_buckets` 신설 — **미래 본표와 다른 그릇**(합계·빠른정산 차감 안 건드림). 라이브 25칸 |
| **c-3** 주문일 기준 : 상세내역 | ✅ | `detail?axis=order`. 집계·목록이 **같은 함수**(`SP.order_axis_row`). 라이브 7칸·54건 |
| **c-4** 실마켓 정합성 검사 | 🟡 | 엔진·화면 완성 + **쿠팡 전수 대조 실시 → §5-① 발견**. 스스·로켓그로스 미실시 |
| **c-4-1** 쿠팡 기준일(로켓그로스·구매확정·미구매확정) | ✅ | 코드에 못 박고 화면에도 표시 |
| **c-4-2** 스스 기준일(정산예정일 1달·일반+빠른정산) | ✅ | 〃 |
| **c-4** 「실마켓 + **더망고** 활용」 | ✅ | **사장님이 제외 확정** — *"더망고는 제외하고 실마켓 기준으로. 실마켓은 100% 정답이야"* |

### d. 입금일 지남

| 노션 원문 | 상태 | 근본 원인 |
|---|---|---|
| **쿠팡** 배송완료인데 왜 입금일 지남? | ✅ | 구매확정 전 주문의 **추정** 예정일(주문일+29일)이 지나면 overdue 로 셌다 → `not_started`(정산 시작 전) 부류 신설 |
| **롯데온** 구매확정인데 왜 확정 전이라 나옴? | ✅ | `_CONFIRMED_WORDS` 에 롯데온 확정값 **「수취완료」(odPrgsStepCd=15)** 가 없었다 → 마켓별로 좁혀 인정 + 정산 실값 증거 병행 |

---

## 3. 사장님이 대화 중 추가로 주신 요구 (11개)

| 요구 | 상태 | 결과 |
|---|---|---|
| **A-1** 「주문 이행 가능 판단」 단추 → 실시간으로 열에 | 🟡 | 열·단추 완성. **①(저장값 재판정)만**. **②(변경분만 재크롤) 미완**(§5-⑤) |
| **A-1** 값 = 가능/불가능(S·P)/클레임/확인불가 | ✅ | `_ffcol`, 6종 |
| **A-1** 소싱처 URL 없으면 「소싱처url 없음」(확인불가 아님) | ✅ | `REASON_NO_SOURCE_URL`. 두 사유 호버 구분 — ①오프라인 사입 ②아직 미연동 |
| **A-1** 최근 크롤 시간 | ✅ | 칸마다 「N일 전」 + 호버에 정확한 시각 |
| **A-1** 한번 저장하면 삭제 말 것 | ✅ | 덮어쓰기만, 안 지움 |
| **B-1** 표 32열 순서 | ✅ | 아래 |
| **추가1** 열 너비 조절 | ✅ | 머리 경계 끌기 · 놓는 순간 저장 · 두 번 누르면 원복 |
| **추가2** 설정이 사라짐(재배포? 탭 이동?) | ✅ | **재배포 아님** — 브라우저 안(localStorage)에만 있었다. 서버(state_store·팀 공유)로 이전 |
| 주문상태 **정규화** + 다른 헤더도 확인 | ✅ | ▼필터가 마켓 원본값이었다 → 통일 상태로. 구매자·연락처 필터도 화면과 같은 값 |
| 구매자·연락처 = 수령자와 같으면 「-」 | ✅ | `sameAsRecv()` |
| 단가 vs 주문 판매가 통합? | ✅ | **라이브 373건 전부 동일·다른 건 0건** → 통합(주문 판매가 삭제) |

### 확정된 열 순서 (`PANEL_COLS`, index.html)

```
1 주문 관리(_ostatus)   2 주문 이행 가능(_ffcol)   3 주문전 예상 마진(_pdx_margin)
4 주문후 실마진(_margin)  5 매입가(_pp_purchase)★   6 공급방식  7 주문상태  8 송장입력
9 주문일  10 판매처  11 계정        [12 여백 _gap1]
13 상품명  14 옵션  15 수량  16 오픈마켓 주문번호
17 구매자  18 구매자 연락처  19 수령자  20 수령자 연락처  21 우편번호  22 주소   [23 여백 _gap2]
24 단가  25 실결제금액  26 고객배송비  27 정산예정금  28 정산예정금(배송비포함)
29 총주문금액  30 옵션추가금  31 마켓수수료  32 수수료율  33 바로가기(_links)
```

### ⚠️ 사장님 확인이 필요한 것 — **직전 세션이 임의로 정했다. 확정 아님.**

- ★ **5번 「매입가」 열** — 사장님 32열 목록에 **없는데** 남겼다.
  이유: 빼면 실매입가를 적을 곳이 사라지고, 그게 4번 「주문후 실마진」의 재료다.
- 같은 이유로 **뺀** 것: 「주문 판매가」(라이브 373건 전부 단가와 동일) ·
  「매입가 (올릴 때 / 지금)」(3번이 같은 재료로 이미 답을 말한다).
- **a-3** 끊긴 문장을 해석해서 진행했다.

---

## 4. 라이브 실측 증거 (놓치지 않았다는 증명)

### A. 「입금일 지남」 — 같은 API, 배포 전/후

| | 배포 전 (08-12 아침) | 배포 후 |
|---|---|---|
| 확정예정 | 919,260 (**롯데온 전 기간 0원**) | **11,399,452** |
| 「지남」 사유 | `not_confirmed_yet` **하나뿐**(9,334,590·160건) | **그 사유 0건** |
| 정산 시작 전 | (없던 부류) | 778,385 (쿠팡 12·롯데온 8·스스 1) |
| 과거 수령 이력 | 없음 | **25칸** |
| 주문일 축 상세 | 없음 | **7칸·54건**(수령자 포함) |

### B. 롯데온 — 앞선 판단이 틀렸다

「롯데온은 구매확정일을 안 준다 → 실입금일 조인 불가」가 **틀렸다.**
`/orders/diag/lotteon-itmd` 로 원본 필드를 눈으로 보니:

```
ajstDcAmt bizpRegNo bseCmsn crdayDpst dvCmsn dvDcOcoAmt dvDcSlrAmt dvDttm dvSeAmt
fnlDpst lrtrNm lrtrNo odNo odSeq pcsCmsn pdDcOcoAmt pdDcSlrAmt procSeq purDttm
pyDttm pymtAmt rtngDttm sbtAmt scatNm  ★seStdDt★  sitmNm sitmNo slAmt slQty
slrDcAmt spdNm spdNo tdfDvsCd trCtrtNo trNm trNo      ← 36개 중에 줄곧 있었다
```
→ `parse_itmd_line_dates()` 신설 · 스윕이 `_settle_confirmed_date` 저장 ·
`lotteon_paid.paid_date_map()` 조인해 **`_settle_paid_date`(실입금일)** 채움.
🔴 그 조인이 **여태 없었다**(호출자가 시험뿐) — 「입금일 지남 1,090만 전액 롯데온·확인 불가」의 원인.

### C. 쿠팡 — 사장님이 준 엑셀 9개(449행·153주문) 전수 대조

**쿠팡 화면 ↔ 쿠팡 엑셀** (내부 일관성) — 3회차 **원 단위 일치**:
```
08-14  Σ정산금액 434,926 × 70% = 304,448   (화면 304,448)
08-24  Σ정산금액 777,335 × 70% = 544,134   (화면 467,014 + 77,120)
08-31  Σ정산금액 590,219 × 70% = 413,153   (화면 413,153)
```
🔴 **쿠팡 상세 엑셀엔 「최종지급액」 열이 없다.** 「정산금액」(지급비율 적용 **전**)만 있고
화면 숫자는 `Σ정산금액 × 지급비율`이다. 노션 문구만 믿으면 영영 못 찾는 열이었다.

**우리 ↔ 쿠팡** (153주문 전수):

| | 결과 |
|---|---|
| 상품 정산분 | ✅ **원 단위 일치**(예: 113,924) |
| 배송비 정산분 | ⚠️ **건당 +132원 차이** — 100건 (§5-① 판정 필요) |
| 우리에 없는 주문 | ⚠️ **31건** 원인 미확인 |
| 완전 일치 | 22건(배송비 없는 주문) |

**대조 방법(재현)**
1. 라이브에서 `/orders/api/settle-plan/detail?category=<7부류>&market=coupang` 를
   **부류 7개 전부**(confirmed·unconfirmed·overdue·not_started·assumed_paid·paid·risk)
   돌려 `{주문번호: 총정산예정}` 을 만든다. **4.5초에 2,314주문**이 나온다.
   🔴 `preview.json` 은 45일이면 30초를 넘겨 멈춘다(10일=16초). 쓰지 말 것.
2. 엑셀은 `주문번호+옵션ID+정산예정일` 로 중복 제거 후 `주문번호` 별 `정산금액` 합.
3. 🔴 **`상품정산예정` 과 비교하면 안 된다** — 엑셀 정산금액은 **상품+배송비** 합이다.
   `총정산예정` 과 비교할 것(직전 세션이 이걸 헷갈려 한 번 오판했다).

---

## 5. 남은 일 — **6개** (①은 2026-08-13 다른 세션이 닫음)

### ① 배송비 수수료 — ✅ **2026-08-13 다른 세션이 닫았다. 손대지 말 것.**

origin/main 확인(2026-08-19):
```
order_export.py  _ship_for_n = ship if (ship_settle is None or zero_cancel) else ship_settle
                 r["정산예정금(배송비포함)"] = (settle + _ship_for_n)
order_export.py:1452  def cp_fee(base, rate_ex_vat) — 반올림(버림(기준×요율)×1.1), 상용 반올림
```
→ 배송비 정산 **실값**(`ship_settle`)이 있으면 그걸 쓰고 없으면 고객배송비로 폴백.
   인계한 방향 그대로다. memory `project_coupang_shipping_fee_not_deducted` 에
   *"2026-08-13 엑셀 전수 재대조로 **닫혔다**(예외 0)"* + 정확한 수수료 식이 기록돼 있다.
🔴 `cp_fee()` 가 정본이다 — **재구현 금지**. 파이썬 `round()` 는 은행가 반올림이라
   `.5` 를 절반 내려 정산이 1원씩 커진다(실측 28행 전부 쿠팡은 올림).

<details><summary>당시 판정 과정 (기록용)</summary>

**「버그」가 아니라 증거 둘이 충돌한 사안이었다.**

`order_export.py:1352-1354` 에 **반대 결정이 주석으로 박혀 있다**:
```
CP_FEE_FACTOR = 0.8845        # 1 - 0.1155 (쿠팡 상품 판매수수료 11.55%)
# (쿠팡 배송비 수수료 3% 상수는 M열=상품정산만 규약 전환(2026-07-23)으로 제거 —
#  N열 = M + 고객배송비 전액, 샵마인 45건 전수 실측.)
```

| 근거 | 결론 | 시점 |
|---|---|---|
| **샵마인 45건 전수 실측** | 배송비 **전액** 더함 | 2026-07-23 |
| **쿠팡 자기 정산 엑셀 153주문** | 배송비 − 수수료 3.3% | 2026-08-12 |

관측(주문 1100194049219):
```
상품 행   판매액 128,900 − 수수료 14,876(10.5%) = 113,924  ← 우리와 원 단위 일치
배송비 행 판매액   4,000 − 수수료    132( 3.3%) =   3,868  ← 우리는 4,000 을 더함
```
현재 코드: `order_export.py:3237` `정산예정금(배송비포함) = 정산예정금액 + 고객배송비`

→ 사장님은 2026-08-12 에 *"실마켓은 100% 정답이야"*(샵마인 제외 확정)라고 하셨다.
샵마인은 계산값이라 정의가 다를 수 있고(대조 엔진에 「정의차이」 부류가 따로 있다),
그래도 **45건 실측 결정을 근거 없이 되돌리지 말 것.** 실측 근거로 판정하고 그 판정을
**그 주석 자리에 남길 것.**

</details>

**당시 조사로 밝힌 것 — 실값은 이미 수집돼 있었다**
- 쿠팡은 배송비를 items 의 「행」으로 주지 않는다. **주문 레벨 형제 필드**
  `order["deliveryFee"]["settlementAmount"]`(REFUND 면 이미 음수)로 온다.
  `order_export.py:1546-1552` 가 이미 `deliv_map` 에 모은다.
- 그런데 **소비처가 없어 버린다** — `order_export.py:1116`, `order_ingest.py:1379·1396`.
  주석 `:429-430`: *"맵 자체는 계속 만든다(정산 진단·향후 소비처용). 쓰지 않는 게 의도다."*
- 🔴 items 에서 「배송비 행」을 찾지 말 것 — **없다**. 구분자는 `order.saleType` 뿐.

**함정 4가지**
1. N열(`정산예정금(배송비포함)`)은 **마진계산기·정산예정금액 탭·KPI·이행판정**이 전부 쓰는 **단일 원천**.
2. 🔴 **M열(`정산예정금액`)에 넣지 말 것** — 스스에서 그렇게 했다가 `_finalize` 가 또 더해
   **이중 계상** 사고(2,910원 과다·수수료율 1.32% 오표시, 2026-08-07).
3. 🔴 `ship` 은 **배송건당 1회만** 더해진다(`order_export.py:3148-3151` `_shipkey`).
   그 중복 방지 규약을 깨면 **다건 주문에서 배송비가 여러 번** 들어간다.
4. 🔴 열 설명 `order_export.py:50` 이 아직 *"상품정산 + 배송비정산(**수수료 차감**)"* 이라
   3237줄 실제 동작과 **모순**이다. 어느 쪽으로 결론내든 **같이** 고칠 것.

memory: `project_coupang_shipping_fee_not_deducted`

### ①-b 스마트스토어 정합성 검사 — **실행 자체를 안 했다**
c-4-2 는 **기준일 규칙만 코드에 넣었고**(`settle_recon.py:59-65`) **대조는 한 번도 안 돌렸다.**
노션 원문: *정산관리>정산 내역(일별/건별) > 일별 정산내역 > 정산예정일 (1달) /
일반정산금액 + 빠른정산금액 / **스스 기준, 집하일 하면 줌.***
🔴 「집하일 기준 지급」이 핵심 — 우리 규칙표는 `fast_cycle_days=1`(발송 관측 기준) 추정이라
어긋날 수 있다. 차이가 나면 「규칙 오차」로 분리 표기할 것(엔진에 그 부류가 있다).

### ② 쿠팡 「우리에 없는 31건」 원인
취소인지 · 조회창(180일) 밖인지 · **진짜 누락**인지 미확인.
🔴 **쿠팡용 `orders=` 진단 라우트가 없다 — 새로 만들어야 한다.**
`orders=` 를 받는 4곳은 전부 다른 마켓: `orders.py:1817`(ESM 정산)·`:1894`(옥션 주문)·
`:2236`(스스 정산 raw)·`:2302`(롯데온 raw).
`/orders/preview.json` 이 받는 인자는 `markets|market`·`days`·`from`+`to`·`fresh` **4종뿐**(확인됨).

### ③ 쿠팡 미구매확정 — 화면 합계 직접 입력 칸
엑셀(`UNCONFIRMED_SNAPSHOT_REPORT_DETAIL_LIST`)에 **수수료 열이 없다.**
판매금액 합 317,000 만 나오고 화면 금액 **268,840**(수수료 뺀 값)을 재현할 수 없다.
- 사람이 적는 칸의 **선례는 `settle_plan_rules.py:52-56` 의 `wallet_balance` 하나뿐.**
- 현재 `settle_recon.py:52-58` 이 `axis="unconfirmed"` 로 **자동 계산**한다.
- 저장소 전체에 "미구매확정" 문자열은 라벨·주석 3곳뿐 — **입력 UI·API 는 0건.**

### ④ 로켓그로스 대조 — **개발이 아니라 실행만 남았다**
배선은 끝나 있다: `settle_recon.py:222-231`(`rg_summary["회차수"]>0` 이면 대조) ·
`webapp/routes/orders.py:3008-3011·3022`(주입) · `api_margin.py:1016`(적재).
🔴 **엑셀 받지 말 것** — 회차마다 리포트가 **8종**이라 23회차면 184개 파일이다(사장님 화면 확인).
정산예정금액 탭 **「🚀 로켓그로스 가져오기」 단추 한 번**이면 확장이 Wing 화면 API 를 직접 읽는다.

### ⑤ 「주문 이행 가능 판단」 ② 변경분만 새로 긁기
사장님 확정: *"변경값 없는건 저장된 크롤값 그대로 + 변경값있는건 새로 긁고 판정.
해당 주문건에 소싱처 url 있는것만 긁으면 돼"*

🔴 **due 경로가 둘이다** (`lemouton/sources/crawl_schedule.py`):
- `:100-101` — `base_interval_seconds <= 0` 이면 `next_lap_products()` 로 빠져
  **`last_fetched_at` 을 아예 안 본다**(가중 라운드로빈 랩).
- `:109-111` — 벽시계 경로는 `last_fetched_at`·`crawl_weight`·`no_change_streak`·`slowdown`
  **4개**를 쓴다. `crawl_weight <= 0` 이면 영원히 due 가 아니다.
→ 「확인 요청」 표식을 **두 경로 모두에** 끼워야 한다.

🔴 `SourceProduct` 에 recheck/need_check/flag 류 컬럼은 **0건**.
`crawl_weight`(1~5)는 **빈도 가중치**라 재활용하면 크롤 주기가 통째로 흔들린다 — 새 컬럼을 만들 것.
🔴 랩 경로 `_lap_view:422` 는 `last_status=="error"` 를 건너뛴다 —
「일정 로직은 `last_status` 를 안 본다」는 due 경로에서만 참.

### ⑥ 「정산예정금」 27·28번 열 추정 딱지
`index.html:2868` 에서 `MONEY_COLS` 분기로 떨어져 **숫자만 찍는다 — 배지 자리 자체가 없다.**
`_settle_source` 소비처는 실마진 칸(2126)·마진 헤더필터(2232)·정산탭 드릴다운(6326) 3곳.
(preview.json 응답에 `_settle_source` 가 실려 온다는 것은 라이브에서 확인함.)

---

## 5-b. 노션에 적힌 기준일 세부 규칙 (①~④ 할 때 필요 — 원문 그대로)

**쿠팡 로켓그로스** (정산>로켓그로스 정산현황 · **빠른정산금 제외됨**)
- 기준일 선택지 = 정산일 / 매출인식일
- ▶ **매출인식일 2달** 설정 후, **오늘일 이후 정산일**에 대한 최종지급액 합산
- 🔴 *"일자 어떻게 선정하냐에 따라, **I(추가 상계금액) 빠른정산 이용액 상이함**"*

**쿠팡 일반정산** (정산>정산현황>정산예정) — 구매확정 / 미구매확정(빠른정산 해당없음)
- 기준일(구매확정) = 정산일(**ONLY 빠른정산 제외 나옴** · **공제금액(F) = 빠른정산 계좌인출액**)
  / 결제일 / 구매확정일 / 배송완료일 ◀ 🔴 **빠른정산금은 정산일 기준으로 해야함!**
- 기준일(미구매확정) = **결제일**
- ▶ 구매확정/미구매확정 나뉘어 기준일 **정산일 2달** 설정, 오늘일 이후 정산일의 최종지급액 합산

**스마트스토어**
- 정산관리>정산 내역(일별/건별) > 일별 정산내역 > **정산예정일(1달)**
- **일반정산금액 + 빠른정산금액**
- 🔴 **스스 기준, 집하일 하면 줌**

**더망고** — 노션엔 "■ 실마켓 + 더망고 활용" 이라 적혀 있지만 **사장님이 2026-08-12 에 제외
확정**했다: *"더망고는 제외하고 실마켓 기준으로 확인해줘. 실마켓은 100% 정답이야."*
→ **더망고를 정산 대조에 쓰지 말 것.** 더망고는 매입가 채우기에만 쓴다.

## 6. 사장님이 이미 주신 것 — **다시 요청하지 말 것**

`C:\Users\seung\Downloads` 에 그대로 있다:
- `MSF_PAYMENT_REVENUE_DETAIL-2026-08-12*.xlsx` **9개** (449행·153주문, 쿠팡 **일반정산** 상세)
- `UNCONFIRMED_SNAPSHOT_REPORT_DETAIL_LIST-2026-08-12*.xlsx` **3개** (미구매확정)

쿠팡 화면 값(사장님 캡처):
- 일반정산 구매확정(정산일 8/13~10/12) **1,841,413** (6회차)
- 일반정산 미구매확정(결제일 7/12~8/12) **268,840** (5건)
- 로켓그로스 지급예상금액 **7,818,202** · 매출인식일 축 23회차

---

## 7. a-1 「이 탭들이 무슨 기능이냐」 — 사장님 질문에 대한 답 (보관)

`lemouton/orders/fulfillment.py` · 2026-07-31 사장님 확정. 네 탭은 **마켓이 준 상태가 아니라
「지금 이 주문을 보낼 수 있나」를 프로그램이 스스로 판정**한 것:

| 탭 | 뜻 | 기준 |
|---|---|---|
| 이행 | 지금 사서 보내면 된다 | 소싱처 **재고 있음** + 정산예정금(배송비포함) − 최종매입가 **> 0** |
| 미이행 | 못 보낸다 | **S** 재고없음 / **P** 역마진 / 소싱처 URL 없음 / **확인 불가** |
| 클레임 | 취소·반품·교환 | |
| 전체 | 안 거름 | |

🔴 **모르면 「확인 불가」** — 미이행에 넣으면 팔 수 있는 주문을 버리고, 이행에 넣으면 손해 보는
주문을 내보낸다. 둘 다 돈이 샌다.
🔴 매출 기준은 마켓이 준 `정산예정금(배송비포함)` — 수수료율로 되계산하지 않는다.

---

## 8. 직전 세션이 **틀렸던 것** (같은 실수 반복 금지)

| 틀린 판단 | 실제 | 교훈 |
|---|---|---|
| 「롯데온은 구매확정일을 안 준다」 | `seStdDt` 가 36개 필드 중에 줄곧 있었다 | **원본 필드를 눈으로 보기 전에 「마켓이 안 준다」고 말하지 말 것** |
| 「배송비 수수료 미차감 = 명백한 버그」 | 45건 실측으로 **일부러 뺀 상수**였다(주석 :1352) | **「버그다」 전에 반대 결정이 주석에 없는지 grep 할 것** |
| 「로켓그로스 엑셀 주세요」 | 확장 단추 하나. 엑셀은 184개가 필요했다 | **사장님께 일 시키기 전에 이미 있는 자동 경로를 찾을 것** |
| `상품정산예정` 을 엑셀 `정산금액` 과 비교 | 엑셀은 **상품+배송비** 합 | **비교 전에 양쪽이 같은 종류의 숫자인지 확인** |
| `tests/orders·margin·platforms` 만 돌리고 올림 | `tests/markets` 에서 CI 실패 | **게이트와 같이 `pytest tests/` 전체(24분)** |
| 시안을 원본 감싸는 틀 없이 제작 | 배치가 무너져 「매우 보기 안좋다」 | 원본 화면 시안은 **감싸는 구조까지** 가져올 것 |

🔴 CI 실패의 **모양**이 중요했다: `TypeError` 를 위쪽 `except` 가 삼켜 **롯데온 정산 스윕이
통째로 안 도는** 상태가 됐다(「정산조회 실패」 한 줄만 남음). 프로덕션도 방어적으로 고침.
**가장 위험한 실패는 「에러 나는 것」이 아니라 「조용히 안 도는 것」이다.**

---

## 9. 배포·PR 현황

| PR | 내용 | 상태 |
|---|---|---|
| #960 | 입금일 지남 오분류 · 실마진 열 · 정산예정 상세/과거/주문일축 | ✅ 머지·배포·**라이브 검증 완료** |
| #965 | 마켓 정산 대조(엑셀) · 화면 순서 재배치 · 표 드롭 업로드 | ✅ 머지 |
| #974 | 표 32열 · 주문 이행 가능 열 · 판단 단추 · 헤더 필터 정규화 · 열 너비 · 설정 서버 저장 · 롯데온 실입금일 | ✅ **머지 완료 2026-08-12 13:27** (검사 통과 `a3b5bfa`) |

⚠️ **같은 파일을 건드리는 PR 이 열려 있다** — **#980**(옥션·G마켓 주문조회) · **#918**(롯데온
정산 창구). 작업 전 `gh pr list --state open` 으로 확인하고 머지 순서를 정할 것.

⚠️ **GitHub Actions 웹훅 유실이 잦다** — 푸시해도 **런이 아예 안 생긴다**. 「실패한 런」이
아니라 **「없는 런」**을 찾아야 한다(`gh run list --branch <br>`). 없으면 빈 커밋으로 재시도.
검사 평균 **18분**. 중간 커밋이 실패해도 HEAD 에서 복구됐을 수 있으니 **HEAD sha 기준**으로 볼 것.
memory: `project_deploy_watchdog_and_direct_path`

## 10. 이번에 만든/고친 파일 (전부 `프로그램/_시스템/` 기준)

**새로 만든 것** (실재 확인됨)
- `lemouton/margin/settle_recon.py` — 마켓 정산 대조 엔진(기준일 규칙·판정 5종·주문 단위 대조)
- `lemouton/margin/models_settle_recon.py` — 대조 이력 (app.py 에 import 추가함)
- `lemouton/markets/order_view_prefs.py` — 화면 설정 저장(state_store·팀 공유)
- `tests/orders/test_orders_layout_order.py` · `tests/orders/test_order_view_prefs.py`
- `tests/margin/test_settle_recon.py` · `tests/js/test_orders_margin_cell.mjs`

**주로 고친 것**
- `lemouton/margin/settle_plan.py` — 마켓별 확정 판정 · `not_started` 부류 · 과거 칸 · 주문일 축 단일 판정
- `lemouton/orders/fulfillment.py` — `no_source_url` 사유 · 크롤 시각
- `lemouton/markets/order_ingest.py` — 롯데온 확정일·실입금일 배선
- `shared/platforms/lotteon/settlement.py` — `parse_itmd_line_dates`
- `webapp/routes/orders.py` — settle-recon · view-prefs · `axis=order` detail
- `webapp/templates/orders/index.html` — 화면 전반

**관련 memory**
- `project_coupang_shipping_fee_not_deducted` (§5-① 상세)
- `project_crawl_worker_queue_branch_stale` (§1 작업 폴더)
- `project_deploy_watchdog_and_direct_path` (§9 웹훅 유실)
- `project_ss_settle_shipping_double_and_fee_rate` (§5-① 함정 2)
