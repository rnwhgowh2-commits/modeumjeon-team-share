# 상품관리(노션) 작업 이어받기 — 2026-08-13 · v2

> 이 파일 **하나로** 이어받는다. 앞 세션은 삭제된다.
> v1(`다른세션_전달_상품관리_2안_인수인계.md`)은 **틀린 곳이 4군데** 있다 — §8 에 다 적었다.
> 충돌하면 **이 문서가 이긴다.**

---

## 0. 시작 전 30초

| | |
|---|---|
| **작업 폴더** | `C:\dev\_wt_prodnotion` · 브랜치 `feature/prodmgmt-2an` |
| 🔴 **첫 명령** | `git -C "C:\dev\_wt_prodnotion" fetch origin && git -C "C:\dev\_wt_prodnotion" rebase origin/main` |
| 🔴 **절대 금지** | `C:\dev\모음전 프로젝트` 본체에서 코드 작업 (2,900커밋 뒤진 브랜치) |
| **현재 tip** | `ecc3c303` · 로컬 = 원격 (전부 push 됨) |
| **되돌릴 자리** | 원격 `backup/prodmgmt-2an-pre-rebase` = `2e2cc8a7` (rebase 전 상태) |
| **라이브 반영** | ❌ **아직 안 됨** — 13커밋 전부 브랜치에만 있다. PR 안 열었다 |
| 사장님 | 비개발자·평이한 한국어. **「1건도 이상 있으면 안 된다」**. 돈은 추측 금지 |

### 🔴 머지 전 반드시
1. `gh pr list` 로 **다른 세션이 같은 영역을 올렸는지** 먼저 본다(이번에 실제로 겹칠 뻔했다).
2. 전체 시험을 돌린다 — `cd "프로그램/_시스템" && python -m pytest tests -q -p no:cacheprovider`
   (약 28분. `python -m pytest -q` 처럼 경로 없이 돌리면 pytest 캡처가 터진다 — 반드시 `tests` 를 준다)
3. 아래 §7 「기존 실패 1건」이 그대로인지 확인(내 탓 아님을 증명해 둘 것).

---

## 1. 이번 세션 성과 — 커밋 13개 (전부 push 완료)

```
ecc3c303 feat(쿠팡쿠폰): 기본값 100원 + 「너무 작아 거부될 수 있다」 미리 알림
b185ebe3 fix(쿠팡정산): 사장님이 준 쿠팡 시험 결과 그대로 — 엑셀 455행 전수 일치 + 마진 두 줄
e10276cc fix(쿠팡정산): 반올림을 쿠팡과 같게 — 건당 1원 「받을 돈」 과대를 없앤다
b259c650 fix(쿠팡등록): 검색태그 2개 하드코딩 → 구매자가 검색할 말로, 한도까지
9de10982 feat(샵마인대조): 노랑(정의차이)이 **왜** 노란지를 화면이 말한다
696c385b fix(쿠팡등록): 바코드 — 우리 내부 번호를 「표준상품코드」라고 보내지 않는다
844e3559 fix(쿠팡등록): 정상가 = 할인가로 통일 — 우리가 준 적 없는 할인이 붙던 것
523d2ca7 fix(11번가): 마켓 실값이 저장분 보강에서 조용히 지워지던 것 — M열도 같이 채운다
9e8e57ae fix(정산): 스윕 2경로에 배송비 정산 배선 — 과거분이 안 고쳐지던 것
1b00091e fix(양식설정): 열 설명 4개가 코드와 딴 말을 하던 것 — M열은 금지된 정의를 광고 중이었다
d4eb9a13 fix(개명): 화면에 남은 「매트릭스 옵션」 5곳 + 404 이름표 — 깨진 채 통과하던 감시 시험도 같이
8e0854be fix(정산): 배송비 정산 실값을 pop 대신 0 명시 대입 (앞 세션분·rebase 됨)
15afedf2 fix(정산): 배송비도 수수료를 뗀다 (앞 세션분·rebase 됨)
```

**33파일 · +2,118 / −72줄 · 신규 시험 72건(9파일)**

---

## 2. 전체 체크리스트 — 원본 목록 전건 대조 (하나도 안 뺐다)

### ■ A. 지금 바로 (돈·거짓 초록불)

| # | 항목 | 상태 | 증거 |
|---|---|---|---|
| A0 | 워크트리 rebase | ✅ | 21커밋이 아니라 **103커밋** 뒤져 있었다. 충돌 0 |
| A1 | 옛 이름 5곳 + 감시 시험 구멍 | ✅ | `d4eb9a13` · 라이브 화면 확인 |
| A2 | 양식 설정 열 설명 | ✅ | `1b00091e` · **3개가 아니라 4개**였다 |
| A3 | 정산 스윕 2경로 배선 | ✅ | `9e8e57ae` · 시험 11건 |
| A4 | 샵마인 판정 이동 사장님 확인 | ✅ | 사장님 **「b」 확정** → `9de10982` 로 구현 완료 |
| A5 | 쿠팡 엑셀 153주문 재대조 | 🟡 **절반** | 엑셀 쪽 **예외 0 으로 닫힘**(§4). 저장분 대조는 라이브 필요 |
| A6 | 11번가 N열 덮임 | ✅ | `523d2ca7` · 시험 4건 추가 |

### ■ B. 쿠팡 가격

| # | 항목 | 상태 | 증거 |
|---|---|---|---|
| B1 | 정상가 = 할인가 통일 | ✅ | `844e3559` · 대량등록 경로는 **원래 맞았고** 쪽문만 갈려 있었다 |
| B2 | 마진 두 줄 (정가/할인가) | ✅ | `b185ebe3` · `policy/preview.margin_lines()` |
| B3 | 검색태그 쿠팡 20 · 스스 10 | ✅ **단**, 스스는 확인불가 | `b259c650` · 쿠팡 20개+개당 20자는 문서 확인. **스스 10개는 근거 없음**(§8-3) |
| B4 | GTIN 바코드 | ✅ | `696c385b` · 우리 바코드는 **내부 전용 대역**이라 안 보낸다 |
| B5 | 쿠폰 자동화 | 🟡 **부품·기본값 완료 / 배선 미완** | `ecc3c303` · **다음 할 일 1순위**(§5) |

### ■ C. 2안 구현 — ❌ 전부 미착수

| # | 항목 | 힌트(v1 §6·§5 그대로 유효) |
|---|---|---|
| C1 | 죽은 자료 5개 살리기 | `sources/models.py:29 product_name` · `:28 external_product_id` · `sourcing/models.py:110-111 naver/coupang_product_name_override` · `:113 Model.note` · `registration/models.py:111 search_filter_id` |
| C2 | 탭 6→4 | 한눈에 / 사올 때 / 팔 때 / 판매·정산. 🔴 표·그래프 **하나도 안 뺀다** |
| C3 | 목록 거르기 6종 | v1 §6 조사 완료본 그대로. 소싱처는 OR·배치쿼리 필수 |
| C4 | 글자수·옵션축 카운터 | 한도표 `registration/market_limits.py` **이미 있음**(이번에 태그 한도도 여기 추가) |

### ■ 노션 「주요 기능」 표 — ❌ 미착수 (v1 §1 그대로)
매트릭스 옵션 별도 탭 · 매출 증대 전략(판매처별 최저가 순위) · 카테고리 맵핑 보기 · 정책 항목 팝업

---

## 3. 사장님 확정 사항 — 이번 세션 (이대로 하면 된다)

| 물음 | 확정 | 반영 |
|---|---|---|
| 샵마인 2,072건 노랑 이동 | **b — 노랑에 사유를 붙여 올린다** | ✅ `9de10982` |
| 쿠팡 GTIN | **정의 확인 후 올바른 값** → 내부번호 안 보내고 「없음+사유」 | ✅ `696c385b` |
| 작업 순서 | **순서대로**: 쿠팡 가격 → 탭 재편 → 죽은 자료 5개 | 진행 중 |
| 할인가 정의 | **판매가 − 즉시할인 − 쿠폰적용**. 「정산되는 기준이 되는 금액이 할인가」 | ✅ `b185ebe3` |
| 즉시할인 vs 쿠폰 | **쿠팡에선 다른 것**(목적은 같음). 엑셀도 A/B로 따로 준다 | ✅ 문서화 |
| 쿠폰 기본값 | **100원** | ✅ `ecc3c303` |
| 🔴 100원이 거부되면 | **a — 최소금액에서 10원씩 올려 재시도** | ❌ **미구현 — 다음 할 일** |
| 쿠폰 연결 방식 | **c — 정책 자동 + 단추 둘 다** | ❌ **미구현 — 다음 할 일** |

### v1 에 있던 「사장님 대기 항목」 중 **아직 안 물어본 것**
- 「매출」 칸의 뜻 (지금 = 할인 전 상품금액)
- 배송비 과거분 되채움 (2,072건)
- 반품 딱지 747건 (롯데온 602·쿠팡 132·11번가 11·G마켓 2)
- 옵션 잘리는 순서 (96개 중 10개만 나감 — 재고순/판매순/직접)
- C등급 중 넣을 것 (필수구매옵션·저장 한도·별도 추가요금·상품 영상)
- 엑셀 한도값 6군데 정정 → **태그 부분만 확인함**(§8-3). 나머지 5군데 미확인

---

## 4. 🔴 이번에 **숫자로 확정**한 것 — 앞으로 이걸 기준으로

### 4-1. 쿠팡 수수료 정확한 식 (사장님이 준 쿠팡 시험 결과 · 엑셀 455행 전수 일치)

```
[상품 행]   요율 10.5% (부가세 별도)
   할인가   = 판매액 − 즉시할인(A) − 다운로드쿠폰(B)
   수수료   = 반올림( 버림(할인가 × 10.5%) × 1.1 )
   정산금액 = 할인가 − 수수료
[배송료 행] 요율 3.0% ← 따로 계산
   수수료   = 반올림( 버림(배송비 × 3.0%) × 1.1 )     (4,000 → 132 → 3,868)
주문 총정산 = 상품 행 합 + 배송료 행 합
```

- **정본 = `lemouton/markets/order_export.cp_fee()`. 재구현 금지.**
- 🔴🔴 **순서가 값을 바꾼다.** 11.55% = 10.5 × 1.1 이지만 **버림 자리가 달라** 1원씩 갈린다.
  `88,900`: 버림(9,334)×1.1 = 10,267.4 → **10,267**(쿠팡) / 한 번에 ×11.55% = **10,268**
- 🔴 반올림은 **`.5` 를 올린다**(상용). 파이썬 `round()` 는 은행가 반올림이라 `.5` 를 짝수로
  보내 절반을 내린다 → 정산이 1원씩 커진다. 실측으로 `.5` 자리 **28행 전부 올림** 확인.
- 요율은 엑셀 「서비스이용율(%,VAT별도)」 열이 행마다 알려 준다(10.5 / 3.0 / 0).

### 4-2. 쿠팡 정산 엑셀이 닫히는 항등식 (중복 119행 제거 후 455행)

| 부류 | 건수 | 식 | 안 맞는 행 |
|---|---|---|---|
| 상품 행 | 299 | `정산금액 = 판매액 − 판매자할인쿠폰(A+B) − 판매수수료` | **0** |
| 배송료 행 | 156 | `정산금액 = 판매액 − 판매수수료` (요율 3.30%) | **0** |

- 엑셀 9개는 **행 해시로 중복 제거** 필수 (574 → 455 · 겹친 119행)
- `<기본배송료>` 는 **「옵션 ID」 칸**에 있다 (「옵션명」 아님 — 거르기가 헛도는 지점)
- 🔴 **요율 분모는 「판매액」이다. 「판매가」는 단가.** 판매가로 나누면 8.25%·7.42% 같은
  **가짜 예외 14건**이 나온다. 실제로 이 함정에 빠져 「예외 발견」이라고 쓸 뻔했다.

### 4-3. 샵마인 판정 이동 (실증)

| | 값 | 판정 |
|---|---|---|
| 샵마인 | 117,924 | (기준) |
| 옛 우리 값 | 117,924 | **match**(초록) |
| 새 우리 값 | 117,792 | **def**(노랑 = 정의차이) |

차이 132원 > 허용오차 6원이라 초록이 안 된다. 재현식에 걸려 **빨강은 아니다**. 영향 2,072건.
→ `9de10982` 로 노랑에 「배송비 수수료 차이」 사유·금액·표본을 붙였다(라이브 화면 확인 완료).

---

## 5. 🔴 다음 할 일 — 이 순서로

### ① B5 쿠폰 자동화 배선 (사장님 확정 **c** + 거부 시 **10원씩 올려 재시도**)

**지금 있는 것**
- `shared/platforms/coupang/promotions.py` — 부품 **완성 + 라이브 검증됨**
  (`create_coupon` `add_items` `check_request` `expire_coupon` `tomorrow_midnight`)
  다른 세션이 2026-08-06 라이브로 돌려 3가지를 고쳤다(커밋 `6d4164d9`).
- `lemouton/policy/discount.py` — `discount_of` `exposed_price` `problem_for`
  **+ 이번에 추가**: `COUPANG_DEFAULT_WON=100` · `default_discount()` · `warn_for()`
- 🔴 **둘 다 부르는 곳이 하나도 없다.** 이게 전부 남은 일이다.

**해야 할 것**
1. **정책 자동** — 정책 적용 시 쿠팡이면 쿠폰을 만들어 옵션(vendorItemId)에 붙인다.
2. **단추** — 상품 화면에서 사장님이 직접 실행.
3. 🔴 **거부되면 10원씩 올려 재시도**(사장님 확정 a).
   시작 100원 → 실패하면 110, 120 … 성공할 때까지. **상한을 정해 두고 로그로 말할 것**
   (무한 재시도 금지 · 몇 번 만에 얼마로 성공했는지 남겨야 다음 상품에 배운다).
4. ⏰ **쿠폰은 다음날 0시부터 적용** — 오늘 못 켠다. 화면이 이 사실을 말해야 한다.

**🔴 반드시 지킬 것 (라이브에서 이미 데인 것들 · `6d4164d9`)**
- 내리기는 `PUT ... action=expire` — 빼면 500 이 난다.
- **FAIL/ERROR 도 「끝」으로 보고**, 안 붙었으면 만든 쿠폰을 **그 자리에서 도로 내린다**
  (빈 쿠폰이 윙에 쌓인다). 앞 세션이 `done` 만 보고 **실패를 성공이라고 보고**했다.
- 100원 거부 실측: 128,900원에 100원(0.07%) → `[CIE06] 할인이 너무 작거나 너무 큽니다`.
  같은 상품 1,400원(1.09%)은 통과. → 그래서 10원씩 올리기가 사장님 답이다.

### ② C2 탭 6개 → 4개 재편  → ③ C1 죽은 자료 5개 → ④ C3 거르기 → ⑤ C4 카운터

### ⑥ A5 나머지 — 저장분 대조
엑셀 쪽은 닫혔다. 남은 건 **주문번호별 정산금액 합 ↔ 우리 저장분** 대조인데 라이브가 필요하다.
`GET /orders/preview.json?markets=coupang&from=…&to=…` (91일 넘으면 저장분만).

---

## 6. 🔴 이번에 **새로 발견한 함정** (v1 에 없던 것)

1. **스윕은 「배송건당 1회」가 안 걸린다.** 행을 한 줄씩 `_finalize_rows([row])` 에 넣는데
   `_shipkey` 는 저장할 때 이미 pop 돼 있어 중복 제거가 **한 번도 안 돈다.**
   → `order_ingest._ship_settle_plan()` 이 담당 줄을 직접 정한다. **안정 정렬 필수** —
   DB 순서에 맡기면 조회마다 담당이 바뀌어 두 번 세어진다.
2. **`git add -A` 가 개발 서버 흔적을 커밋에 넣는다.** `data/settle_deep_backfill.json` 이
   두 번 딸려 들어갔다(정산 백필 진행 위치 — 라이브에 올라가면 되돌아간다).
   → 이 워크트리엔 `git update-index --skip-worktree` 를 걸어 뒀다. **풀지 말 것.**
3. **`round()` 는 은행가 반올림이다.** 돈 계산에서 `.5` 를 짝수로 보내 절반을 내린다.
   `math.floor(x + 0.5)` 를 쓸 것.
4. **구현식을 그대로 베낀 시험은 식이 틀려도 늘 통과한다.** 이번에 3건 발견
   (`round(103000*0.8845)` 꼴). **마켓 실측 숫자를 손으로 적을 것.**
5. **`{% set %}` 을 `extends` 바깥에 두면 블록 안에서 안 보인다**(Jinja). 자리마다 인라인으로.
6. **개명 감시 시험은 「개명 하나당 금지 낱말 하나」**. 템플릿만 훑으면 못 잡는다 —
   라우트가 글자를 만들어 넘긴다. `ast` 로 `render_template` 인자만 볼 것(도크스트링 제외).
7. **다른 세션이 같은 파일을 만진다.** 이번엔 PR #995 가 `orders/index.html` 을 만졌는데
   hunk 가 ~6850 까지, 샵마인 구역은 7039 부터라 겨우 안 겹쳤다. **`gh pr diff` 로 줄 확인.**

---

## 7. 시험 상태

- **전체: 8,533 passed / 1 failed** (`python -m pytest tests -q -p no:cacheprovider` · 약 28분)
- 🔴 그 1건 `tests/orders/test_settle_plan_api.py::test_집계_지급예정일축` 은
  **`origin/main` 에서도 실패한다** — 직접 체크아웃해 확인했다. **내 변경과 무관.**
  200원이 「미확정예정」→「정산 시작 전」으로 **분류만** 옮겨갔고 합계(300)는 그대로다.
  돈이 새는 건 아니지만 화면 카드 설명과 어긋날 수 있어 별건으로 남긴다.
- 신규 시험 **72건 / 9파일**
  ```
   9  tests/margin/test_coupang_settle_rounding.py      ← 엑셀 455행 전수 잠금
   5  tests/margin/test_export_column_desc.py
   7  tests/margin/test_shopmine_recon_reason.py
  11  tests/markets/test_sweep_shipping_settlement.py
   8  tests/policy/test_coupang_coupon_default.py
   8  tests/policy/test_preview_margin_two_lines.py
  10  tests/registration/test_coupang_barcode_gtin.py
   4  tests/registration/test_coupang_price_parity.py
  10  tests/registration/test_coupang_search_tags.py
  ```

### 라이브 화면으로 직접 확인한 것 (렌더된 글자로 판정)
- 옵션관리 0건 안내 「아직 **옵션 묶음**이 없어요」
- 404 4종 — `/matrix/…` 「옵션 묶음 번호」 · `/policies/…` 「정책을 찾을 수 없습니다 / 정책 번호」
  · `/bundles/…/option/…` 은 「모음전 코드 / 옵션 SKU」 **그대로 유지**(맞던 것 안 건드림)
- 양식 설정 열 설명 4개가 화면까지 도달
- 샵마인 대조 — 실제로 엑셀을 올려 「배송비 수수료 차이 · 1건 · 132원」 + 상세(117,924 → 117,792)

---

## 8. 🔴 v1 인수인계서에서 **틀린 것으로 밝혀진 것** (그대로 믿지 말 것)

1. **「기준 = 판매가 × 수량, 할인가 아님. 쿠폰 차액은 쿠팡이 메운다」(v1 §7)** → **틀렸다.**
   전수: `정산금액 = 할인가 − 수수료` 안 맞는 행 **0** / `= 판매액 − 수수료` 안 맞는 행 **109**.
   헷갈린 건 **누가 부담하느냐** — 판매자 부담 쿠폰(즉시·다운로드)은 정산에서 **빠지고**,
   쿠팡 부담 할인은 쿠팡이 메우므로 **안 빠진다**.
2. **「배송료 124건 전부 3.30%, 예외 0」** → 요율은 맞지만 **건수가 틀렸다.**
   중복 제거 후 배송료 행은 **156건**(금액 있는 것 126건).
3. **「양식 설명 3개」** → **4개**. 「마켓수수료」 설명도 틀렸다(롯데온처럼 마켓이 수수료 실값을
   주면 그 값을 그대로 쓰는데, 설명은 「실결제 − 정산예정금액」 한 갈래만 말했다).
4. **「태그 스스 10개」** → **근거 없음.** 네이버 등록 API `seoInfo.sellerTags` 는 `object[]`
   선언뿐이고 개수·길이 문장이 **어디에도 없다.** `market_limits.TAG_LIMIT_UNKNOWN` 에
   남기고 상한을 안 걸었다. 지어낸 상한으로 태그를 잘라내면 그게 더 큰 손해다.
   ⚠️ 스스 sellerTags 는 문자열 목록이 아니라 **객체 목록**(code+text)이다 — 실을 땐 모양부터.
5. **v1 §2-E 줄번호·파일 경로가 어긋난다.** `webapp/routes/order_export.py` 가 아니라
   **`lemouton/markets/order_export.py`** 다. rebase 로 줄번호도 다 밀렸다 — grep 으로 찾을 것.
6. **「21커밋 뒤짐」** → 실제 **103커밋**이었다. main 은 하루에도 수십 커밋 들어온다.

---

## 9. v1 에서 그대로 유효한 것 (다시 안 적음 — v1 을 참고)
- §3 배포 완료분 PR #963 상세 (사이드바 개명 4번 넘어진 이야기)
- §6 목록 거르기 조사 완료본 (C3 에 그대로 쓴다)
- §8 반복된 함정 10가지 (여전히 유효 — 특히 1·2·3·6·9·10)
- §10 참고 파일 목록 (시안·더망고·엑셀 경로)

---

## 10. 진행 방식
사장님은 비개발자다. **평이한 한국어**로. 「1건도 이상 있으면 안 된다」가 기준이다.
돈 관련은 **추측 금지** — 코드와 라이브 실데이터로 증명하고 나서 말할 것.
시험은 **RED 를 먼저 보고** 나서 고칠 것(TDD). 작업 끝나면 `/conversation-wrapup` 형식으로 마무리.
