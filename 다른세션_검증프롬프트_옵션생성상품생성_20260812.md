# 다른 세션 전달 — 「옵션생성 & 상품생성」 작업 독립 검증 프롬프트

> 아래 코드블록을 **통째로 복사해 새 세션에 붙여넣으면 됩니다.** 그것 말고는 할 일 없음.

---

```
당신은 다른 세션이 오늘(2026-08-12) 수행한 작업을 **독립적으로 검증**하는 감사자다.
칭찬이 목적이 아니다. **틀린 것을 찾아내는 것**이 목적이다. 「잘 됐다」는 결론은
근거를 다 대고 나서만 쓸 수 있다.

════════════════════════════════════════════════════════════
0. 시작 전 — 이것부터 지켜라 (안 지키면 결론이 통째로 틀린다)
════════════════════════════════════════════════════════════
· `C:\dev\모음전 프로젝트` 는 브랜치 feature/crawl-worker-queue 로 **origin/main 대비 수천 커밋
  뒤처진 폴더**다. 이 폴더는 **코드를 읽기만 해도 오보가 난다**(과거에 실제로 틀린 보고 3건 발생).
  → 반드시 새 워크트리를 떠서 거기서만 읽어라:
      git -C "C:\dev\모음전 프로젝트" fetch origin
      git -C "C:\dev\모음전 프로젝트" worktree add "C:\dev\모음전 프로젝트\_wt_audit" origin/main
    이후 모든 경로는 `_wt_audit\프로그램\_시스템\...`
· pytest 는 **인자 없이 돌리지 마라**(수천 개라 안 끝난다). 경로를 지정해라.
· 라이브는 https://mou-m.com 로 **로그인 없이** 열린다.
· 🔴 라이브에서 **데이터를 바꾸는 동작은 하지 마라**(상품 생성·재고 저장·축 맞춤 저장·삭제).
  조회만 해라. 쓰기 검증이 필요하면 로컬에서 해라.

════════════════════════════════════════════════════════════
1. 무엇을 검증하나 — 근거 문서
════════════════════════════════════════════════════════════
지시 원문 = 노션 「무재고 모음전 솔루션 개발현황 > ■ 프로그램 개선사항 > 옵션생성&상품생성」
(노션 페이지 id: 3a9cf482-7373-8014-b510-c3ef0b8a0418 — 노션 MCP 로 직접 읽어 대조할 것.
 요약본을 믿지 말고 원문을 읽어라.)

설계서 = `docs/superpowers/specs/2026-08-01-옵션생성-상품생성-탭-design.md`
(주의: 이 파일은 저장소 **루트 docs/** 에 있다. `프로그램/_시스템/docs/` 아님)

검증 대상 커밋 (origin/main, 전부 배포 완료됨):
  a11d5e36  없는 탭 안내·50개 상한·사이드 넘침·옵션축 열 폭·파생 카드
  68fe4179  색상·사이즈 칸을 「축 이름」 기준으로   ← 🔴 돈 걸린 것
  31c69932  만들기 순서 + 축 프리셋 + 브랜드 필수
  7b5b1198  옵션 표에 브랜드·모델명 열
  326e72e4  만든 상품 되채움 + 정책 바로가기
  1c2cbc88  초기 재고를 재고 원장에 연동            ← 🔴 돈 걸린 것
  32649ace  축 맞추기 「빼앗아 오기」
  ccb7b3f9  하위탭 2단 위상
  e3c91e68  격자 모델 탭 + 여러 묶음 골라 한 상품    ← 🔴 돈 걸린 것
  2e6274ad  표 가로 스크롤
  b3a874d1  단독 실행 시 깨지던 시험 1건
  dde4404f  파생 만들기 2개부터
  9f2aa247  「단독_」 화면에서 제거
  d3e9f5cf  만들기 창 라디오
  5ba2dfbe  여러 묶음 한 번에 지우기 + 재고 이력 정리
  083d6dbc  옛 재고 기록 치우는 창구

════════════════════════════════════════════════════════════
2. 🔴 최우선 — 돈이 틀어질 수 있는 3가지. 여기부터 깨뜨려 봐라
════════════════════════════════════════════════════════════
[M1] 축 이름 기준 배정 (68fe4179)
  주장 : 「모델·색상·사이즈」 3축으로 옵션을 만들어도 `Option.color_code` 에 모델명이
         안 들어가고 색상이 들어간다. 규칙은 `lemouton/sourcing/axis_slot.py` 한 곳뿐이다.
  깨뜨릴 것 :
   · `storage_slots` / `semantic_slots` 를 직접 호출해 축 이름 조합을 20가지 이상 넣어 봐라.
     (색상만 / 사이즈만 / 모델만 / 재질·용량 같은 모르는 이름 / 같은 이름 두 번 /
      빈 문자열 / None / 순서 뒤바꾼 것 / 4축 이상)
     **한 자리에 두 축이 들어가는 경우가 하나라도 있으면 그건 재고 이중계상이다.**
   · 축 이름을 못 알아보는 옛 매트릭스(예: 「단계1·단계2」)의 동작이 **바뀌지 않았는지**
     확인해라. 바뀌었다면 기존 데이터가 조용히 틀어진다.
   · `option_service.py` 의 옵션 생성과 이름 바꾸기(apply_renames) **두 곳 모두**
     같은 함수를 쓰는지 확인해라. 한 곳만 쓰면 언젠가 갈린다.
   · 라이브 주장 : 「184개 매트릭스 전수 대조 결과 자리가 달라지는 것 0건」.
     라이브에서 직접 다시 세어 이 숫자를 확인해라(GET /api/bundles/<code>/source-urls
     의 axis_steps 로 축 이름을 모을 수 있다). 숫자가 다르면 보고해라.

[M2] 초기 재고 (1c2cbc88)
  주장 : 초기 재고는 `InventoryTx` 원장에 남고, **두 번 넣어도 두 배가 안 된다**.
  깨뜨릴 것 :
   · 로컬에서 같은 SKU 에 초기 재고를 2회·3회 저장해 봐라. 늘어나면 이중 계상이다.
   · 재고가 이미 있는 옵션에 넣으면 건너뛴다는데, **건너뛴 사실을 화면이 말하는지** 봐라.
     조용히 넘어가면 「넣었다」가 거짓이 된다.
   · `options.boxhero_stock_total`(캐시)과 원장 합계가 **같은지** 확인해라.
     (주의: `recalc_stock_total` 은 **계산만 하고 저장은 안 한다** — 부르기만 하면 캐시가 안 맞는다)
   · 위치(Location)가 하나도 없을 때 거부하는지 확인해라.

[M3] 여러 묶음 합쳐 상품 만들기 (e3c91e68)
  주장 : 축은 이름으로 합치고 순번을 1부터 다시 매긴다 / 축 이름이 다르면 막는다 /
         (색상,사이즈) 겹치면 첫 것만 담고 `skipped` 로 알린다 / 출처는 묶음마다 한 줄.
  깨뜨릴 것 :
   · 축 순서가 다른 두 묶음(색상·사이즈 ↔ 사이즈·색상)을 합쳐 봐라. 막히나? 합쳐지나?
     합쳐진다면 값이 뒤섞이지 않는지 확인해라.
   · 원본 + 파생을 섞어 합쳐 봐라(파생은 model_code 가 없다). 축이 어디서 오나?
   · 3개 이상 합쳐 봐라. 겹침 보고(`skipped`)가 정확한가?
   · 옛 단수 호출(`matrix=`)이 여전히 그대로 도는지 확인해라(기존 화면이 그 길로 부른다).

════════════════════════════════════════════════════════════
3. 노션 13항목 — 라이브에서 하나씩 재라 (기대값 명시)
════════════════════════════════════════════════════════════
아래는 앞 세션이 2026-08-12 에 라이브에서 쟀다고 **주장한 값**이다. 직접 다시 재서 봐라.

⚠️ 단, **개수(93줄·88개·5줄 같은 숫자)는 그 뒤 사장님이나 다른 세션이 묶음을 만들거나
   지우면 달라진다.** 숫자가 다르다고 곧바로 「틀렸다」로 결론내지 말고,
   **규칙이 지켜지는지**(창고 물건은 숨는가 · 머리줄 숫자가 화면에 보이는 수와 같은가)로
   판정해라. 규칙이 깨졌으면 그건 진짜 결함이다.

 (1) 하위탭 a  /optgen?tab=direct
     기대 : 윗단 「옵션 생성」「상품 생성」 / 아랫단 「직접」「내마켓 불러오기」
            내마켓 탭에서도 윗단 「옵션 생성」이 켜진 채 유지
     확인 : 상단 메뉴 펼침에도 하위탭이 **3개 그대로**인가
            (원천 2곳: webapp/routes/optgen.py SUBTABS · webapp/routes/api_sidebar.py _STAGE_SPEC.
             tests/test_optgen_subtabs3.py 가 두 곳 일치를 지킨다)

 (2) 옵션 a  「+ 옵션 매트릭스 생성」 창
     기대 : 라벨이 「1 어떤 모음전인가요 / 2 옵션축 / 3 브랜드 / 4 매트릭스의 이름」
            종류·옵션축 **둘 다 세로 라디오** · 「이 매트릭스의 이름」 문구 없음
            브랜드 비우면 「브랜드를 적어주세요」로 막힘

 (3) 옵션 b  축 프리셋
     기대 : 색상 = 1축[색상] / 2축[색상,사이즈]
            모델 = 1축[모델] / 2축[모델,색상] / 3축[모델,색상,사이즈]
            프리셋 밖 조합을 API 로 보내면 400

 (4) 옵션 b★ 데이터 관리
     기대 : 옵션 표에 브랜드·모델명 칸이 있고 **모델명이 「—」인 줄이 0**
            새 DB 칸을 만들지 않았다(스키마 변경 0) — 확인해라
     확인 : 「모델명이 구조적으로 안 빈다」는 주장을 깨 봐라.
            매트릭스 이름이 빈 경우·모델 축 값이 빈 경우에 뭐가 나오나?

 (5) 옵션 c  축 맞추기 「빼앗아 오기」
     기대 : 충돌 시 409 + conflict{holder, holder_used_by, ghost}
            takeover:true 로 다시 부르면 놓기+잡기가 **한 트랜잭션**에서 일어나
            같은 표기를 쓰는 행이 **정확히 1줄**로 유지된다
     깨뜨릴 것 : 빼앗은 뒤 1:1 이 깨지는 경로가 있나? (동시 요청·연속 빼앗기)

 (6) 옵션 d  「재고관리 매핑」
     기대 : 그 문구가 화면에서 0건 · 표에 초기 재고 입력칸 + 「초기 재고 저장」
     확인 : 다른 화면(bundles/edit.html, _matrix_v3.html)에도 같은 거짓 안내가
            남아 있지 않은지 훑어라

 (7) 옵션 e  「단독_」
     기대 : /optgen?tab=direct 에서 화면에 「단독_」 글자 0건,
            전체 93줄 중 5줄만 보이고 「창고에만 있는 물건 보기」로 88개를 꺼낼 수 있음
     확인 : **「상품 생성」 탭에는 숨김이 안 들어갔다**고 앞 세션이 인정했다.
            그게 문제가 되는지 판단해라.

 (8) 상품 a-1  /optgen?tab=product
     기대 : document.documentElement.scrollWidth == clientWidth (가로 스크롤 없음)
            서랍 라벨이 칸 밖으로 안 나감(scrollWidth ≤ clientWidth)
     ⚠️ 이 항목은 앞 세션이 **한 번 틀렸다**(로컬 3줄만 보고 「해결」이라 보고 → 라이브에선
        1481 > 1265 로 그대로였다). 다른 화면·다른 폭에서도 같은 실수가 없는지 봐라.
        1280 / 1440 / 1920 폭에서 각각 재라.

 (9) 상품 a-2  /optgen/product/<id>
     기대 : 격자 첫 열이 글자 폭(약 68px). 전에는 394px(표의 절반)
     확인 : /matrix/<id> (보기 전용)도 같은 템플릿을 쓴다 — 양쪽 다 봐라

(10) 상품 b-1  이미 상품을 만든 묶음
     기대 : 만든 상품 이름·번호·브랜드·카테고리·담은 조합이 보이고,
            「또 만들기」는 접혀 있고 펼치면 경고
     ⚠️ 담은 옵션은 **(색상,사이즈) 대조로 되짚은 추정**이다.
        추정이 틀리는 경우(같은 조합이 여러 개·조합이 바뀐 경우)를 만들어 봐라.
        화면이 추정임을 밝히고 있나?

(11) 상품 c-1  정책 바로가기
     기대 : 정책이 붙었으면 /policies/product/<code>, 없으면 /policies/apply?model=<code>
     확인 : 딥링크로 들어갔을 때 그 상품이 **실제로 체크되어 있고 목록에 보이는지**
            (목록이 300줄 상한이라 검색칸 프리필이 없으면 「체크했다는데 안 보임」이 된다)

(12) 상품 c-2  여러 개 체크해 상품 만들기
     기대 : 체크칸 상시 노출 · 1개면 띠 없음 · 2개 이상이면 띠
            체크 클릭이 줄 이동(그 묶음으로 들어가기)을 **일으키지 않음**
     확인 : 키보드로만 조작해도 되나? 체크칸이 손끝 목표(44px)를 지키나(폰)?

(13) 상품 c-3  파생 만들기
     기대 : **2개 이상** 골랐을 때만 카드가 뜬다(1개·0개면 숨김)

════════════════════════════════════════════════════════════
4. 앞 세션이 스스로 인정한 실수·한계 — 진짜 고쳐졌는지 봐라
════════════════════════════════════════════════════════════
 · 「가로 스크롤 사라졌다」를 **데이터가 적은 로컬만 보고** 단정 → 라이브에선 그대로였음
 · 시안을 **구조만 재고** 「정상」이라 보고 → 실제로는 주석 글자가 노출되고 색·테두리가
   전부 빠져 있었음(색 변수가 `.ds` 클래스 안에만 있는 걸 놓침)
 · `recalc_stock_total` 이 계산만 하고 **저장은 안 한다**는 걸 처음에 몰라 캐시가 안 맞았음
 · `templates/matrix/detail.html` 에 NUL 문자 2개가 있었음(origin/main 부터) → 공백으로 교정
 · `tests/matrix/test_build_bundle.py` 가 **단독 실행 시** 깨지던 것 → 모델 import 한 줄로 해결
 → 같은 부류(적은 데이터로 단정 / 구조만 보고 판단 / 계산과 저장 혼동)가
    **다른 곳에도 남아 있는지** 찾아라.

════════════════════════════════════════════════════════════
5. 다른 세션과 겹칠 수 있는 것 (충돌 의심)
════════════════════════════════════════════════════════════
같은 날 다른 세션이 `92fa0129 feat(옵션 묶음): 화면에서 지울 수 있게 + 휴지통 메뉴 없앰` 를
올렸다. 이번 세션도 `5ba2dfbe 여러 묶음 한 번에 지우기` 를 올렸다.
→ **지우기 기능이 두 벌이 되었는지, 서로 다른 규칙으로 도는지** 확인해라.
  (한쪽만 재고 이력을 치우고 다른 쪽은 안 치우면 유령 재고가 생긴다)

════════════════════════════════════════════════════════════
6. 자동 시험
════════════════════════════════════════════════════════════
아래를 돌리고 **실패가 있으면 그대로 보고**해라. 통과했다고 다 된 게 아니다 —
「시험이 아무것도 안 보는」 경우도 찾아라(대상 데이터가 없어 항상 통과하는 시험).

  cd _wt_audit\프로그램\_시스템
  python -m pytest tests/matrix tests/sourcing tests/inventory tests/design tests/catalog tests/policy tests/test_optgen_subtabs3.py tests/test_bundle_detail_split.py -q

이번에 추가된 시험(진짜로 무언가를 지키는지 하나씩 읽어라):
  tests/sourcing/test_axis_slot.py            (19건 — 뒤 4건이 「읽기 경로」다)
  tests/sourcing/test_axis_takeover.py        (4건)
  tests/catalog/test_optgen_axis_preset.py    (5건)
  tests/inventory/test_optgen_initial_stock.py(4건)
  tests/inventory/test_optgen_reset_stock.py  (4건)
  tests/matrix/test_build_bundle_multi.py     (5건)
  tests/sourcing/test_guide_catalog_rich_js.py(2건 · 실체는 tests/js/test_guide_catalog_rich.js 11건)
→ 각 시험을 **일부러 깨 봐라**(구현을 되돌려 놓고 RED 가 뜨는지). RED 가 안 뜨면
  그 시험은 아무것도 안 지키고 있는 것이다.

════════════════════════════════════════════════════════════
6-B. 데이터 코드 지도 (같은 세션이 이어서 한 일 — 같이 검증해라)
════════════════════════════════════════════════════════════
앞 세션이 크롤링 가이드(`docs/크롤링-가이드.md`)를 갱신했다고 주장한다. 확인해라.

(1) **원문 ↔ 보기 데이터가 정말 일치하나** — 원문의 에러 id 를 전부 뽑고
    `webapp/static/error_catalog.json` 의 id 와 대조해라. 차이가 0 이어야 한다.
    §5 제목에 박힌 개수(재고 22 · 가격 34 · 기타 33)도 실제 항목 수와 맞는지 세어라.
(2) **§0 「축 맞추기」 층과 §5 O32·O33** 이 라이브에 뜨나 — `https://mou-m.com/sourcing-guide/map.md`
    (원문)과 `/sourcing-guide/map`(보기) **양쪽**. 🔴 보기 화면은 md 변환이 아니라
    **손으로 지은 HTML** 이다(근거 `routes/mobile_guide.py:11`) — md 만 맞고 화면은 안 바뀌는 함정이 있다.
(3) **글자가 깨져 보이지 않나** — 보기 화면 §6 에러이력에서 별표 두 개·백틱이
    **글자 그대로** 보이는 줄이 하나라도 있으면 실패다(고치기 전엔 16항목 30칸이 그랬다).
(4) **막기가 표기보다 먼저인가** — `map.html` 의 `rich()` 가 `esc()` 를 먼저 부르는지
    **눈으로 확인**해라. 순서가 바뀌면 카탈로그 글이 화면에서 실행된다(저장형 XSS).
(5) **판매처 지도는 정말 손댈 게 없었나** — 앞 세션은 「해당없음」이라 했다.
    `git log --since` 로 마켓 경로(`shared/platforms/` `lemouton/uploader/` `lemouton/markets/`
    `docs/markets/`) 커밋을 **직접 세어** 0 인지 확인해라. 말만 믿지 마라.

════════════════════════════════════════════════════════════
7. 보고 형식
════════════════════════════════════════════════════════════
· 항목마다 : 통과 / 실패 / 확인불가 중 하나 + **근거(실측값·파일:라인·명령 출력)**
· 「확인했다」는 말만으로 통과 처리 금지. 숫자나 화면 값을 붙여라.
· 발견한 결함은 **재현 절차**와 함께. 재현 못 하면 「의심」으로 표시.
· 마지막에 : 통과 N / 실패 N / 확인불가 N + **가장 위험한 것 3개**를 골라 이유와 함께.
· 앞 세션의 주장 중 **틀린 것을 찾았다면 그것을 맨 위에** 써라.
```

---

## 참고 — 이 프롬프트를 만든 세션이 남긴 것

- 노션 13항목 전부 반영 + 추가 8건, 자동 시험 2,248건 통과, 라이브 배포 완료
- 이어서 데이터 코드 지도 갱신 — 그 감사에서 **앞 세션이 낸 구멍 1건**(축 읽기 경로)과
  **지도 화면이 깨져 있던 것 1건**(별표 노출)을 더 잡았다. §6-B 로 같이 검증할 것
- 백업 파일 : `다운로드/지우기전_백업_옵션묶음88개_20260812.tsv`
- 시안 : 바탕화면 `모음전 시안 v13_…`(3항목×3안) · `모음전 시안 v14_…`(만들기 창 라디오 3안)
