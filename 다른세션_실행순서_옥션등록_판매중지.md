# 다른 세션 실행 순서 — 옥션 르무통 등록 → 판매중지 (2026-07-21)

> 이 문서를 연 세션이 **위에서 아래로 그대로 따라 하면** 끝난다.
> 목표: 르무통 메이트를 옥션에 재고 1개로 실등록 → 2~3분 뒤 판매중지 → 확인.
> 되면 G마켓은 같은 방식(자동), 그다음 11번가·롯데온.
> 배경·스펙 전체는 `다른세션_전달_4대마켓_상품등록_판매중지.md` 참고.

---

## 시작 프롬프트 (이 세션에 그대로 붙여넣기)

> 옥션에 르무통 메이트를 재고 1개로 실등록했다가 2~3분 뒤 판매중지하려 한다.
> 코드·재료·dry-run 검증은 이미 끝났고(origin/main 반영), 실행만 남았다.
> `다른세션_실행순서_옥션등록_판매중지.md` 를 열어 STEP 0부터 순서대로 진행해줘.

---

## 전제 확인 (STEP 0)

1. 라이브 정상: `curl -s -o /dev/null -w "%{http_code}" https://mou-m.com/health` == 200
2. origin/main 에 옥션 등록 함수 있는지: `git branch -r --contains 4cc8624c | grep origin/main`
   → 있으면 진행. 없으면 STOP(다른 세션이 아직 배포 안 함).
3. 라이브 브라우저(Claude-in-Chrome)로 `https://mou-m.com/accounts/upload` 접속해 둔다.
   (진단 엔드포인트를 브라우저 fetch 로 호출한다)

---

## STEP 1 — 등록 재료를 다시 읽는다 (읽기, 안전)

브라우저에서 fetch. 기존 옥션 상품에서 선행자원을, 쿠팡에서 콘텐츠를 읽는다.

```js
// ① 옥션 선행자원 (기존 상품 5806568636 에서 재사용)
const ref = await (await fetch('/api/live-send-test/product-detail?market=auction&product_id=5806568636')).json();
// ② 쿠팡 르무통 콘텐츠 (판매가)
const cp  = await (await fetch('/api/live-send-test/product-detail?market=coupang&product_id=16176862782&account=브랜드마켓쿠팡')).json();
const price = (cp.options && cp.options[0] && cp.options[0].sale_price) || 135820;
window.__REG = {
  market:'auction', account:'브랙드웍스',
  goods_name:'르무통 메이트 스니커즈 운동화 (테스트)',
  cat_code: ref.카테고리.esm.catCode,          // 00120005002000000000
  site_cat_code: ref.카테고리.site[0].catCode,  // 37500700
  price: price, stock: 1,
  place_no: ref.출하지placeNo,                  // 23590515
  dispatch_policy_no: ref.발송정책.iac,          // 1939006
  return_addr_no: ref.반품교환.addrNo,          // 46321571
  delivery_company_no: ref.택배사,              // 10008
  official_notice_no: ref.상품고시No,           // 25
  official_notice_details: ref.상품고시details, // 25-1~25-13
  image_url: ref.대표이미지, detail_html:'<p>르무통 메이트</p>',
  options: null                                 // 테스트 = 옵션 없이 단일상품
};
JSON.stringify({price, place_no:ref.출하지placeNo, cat:ref.카테고리, img:String(ref.대표이미지||'').slice(0,50)});
```
확인: price 는 십원단위·양수, place_no·cat·img 가 비어있지 않아야 함. 비면 STOP.

---

## STEP 2 — dry-run 으로 요청서 형식 검증 (마켓에 안 감)

```js
const dry = await (await fetch('/api/live-send-test/register-esm', {
  method:'POST', headers:{'Content-Type':'application/json'},
  body: JSON.stringify({...window.__REG, arm:'0'})   // arm=0 = dry-run
})).json();
JSON.stringify({mode:dry.mode, armed:dry.armed,
  price:dry.payload.itemAddtionalInfo.price, stock:dry.payload.itemAddtionalInfo.stock,
  name:dry.payload.itemBasicInfo.goodsName});
```
확인: `mode="dry-run(조립만)"`, `armed:false`, `price.Iac` 값 있고 `Gmkt:0`, `stock.Iac:1`.
어긋나면 STEP 1 로 돌아가 재료 다시 확인.

---

## STEP 3 — 서버 등록 잠금 켜기 (재배포 필요)

⚠️ 여기부터 실제 마켓에 상품이 생긴다. 사장님 "ㄱㄱ" 확인 후에만.

실등록은 `arm='1'` **그리고** 서버 환경변수 `LIVE_REGISTER_ARMED=1` 둘 다 필요.
서버 env 를 켜려면 배포 환경변수를 추가하고 재배포해야 한다.

- GitHub Actions / Fly.io / Lightsail 배포 설정에서 `LIVE_REGISTER_ARMED=1` 추가 후 재배포.
- 배포 완료 + health 200 확인.
- ★ 이 값은 등록 전용이다(가격전송 MOUM_LIVE_UPLOAD 와 별개). 다른 세션 전송은 안 열린다.

배포 방법을 모르면 여기서 사장님께 "서버에 LIVE_REGISTER_ARMED=1 을 켜고 재배포해달라"고
요청하고 대기. (코드가 아니라 배포 설정이라 사장님 손이 필요할 수 있음)

---

## STEP 4 — 실등록 (상품번호 수령)

```js
const reg = await (await fetch('/api/live-send-test/register-esm', {
  method:'POST', headers:{'Content-Type':'application/json'},
  body: JSON.stringify({...window.__REG, arm:'1'})   // arm=1 = 실등록
})).json();
window.__GOODSNO = reg.result && reg.result.goodsNo;
JSON.stringify({mode:reg.mode, armed:reg.armed, ok:reg.ok,
  goodsNo:window.__GOODSNO, siteDetail:reg.result&&reg.result.siteDetail,
  error:String(reg.error||'').slice(0,150)});
```
확인:
- `ok:true`, `goodsNo` 값이 있어야 성공. **goodsNo 없으면 등록 실패**(거짓성공 아님 — 함수가 막음).
- `armed:false` 로 나오면 STEP 3 의 서버 env 가 안 켜진 것 → STEP 3 재확인.
- 실패 시 `error` 를 읽고 원인 파악. 이미 만들어졌는데 응답만 실패했을 수 있으니
  STEP 6 의 sell-status 조회로 상품 존재 여부 먼저 확인(중복등록 방지).

★ **goodsNo 를 반드시 기록**(window.__GOODSNO + 채팅에 출력). 이걸 잃으면 못 내린다.

---

## STEP 5 — 2~3분 대기 (필수)

옥션은 등록 직후 수정(판매중지 포함) 호출 불가. 바로 부르면
"상품 정보가 부정확합니다. G마켓에 문의 바랍니다" 에러.
→ **3분 기다린다.** (`ScheduleWakeup` 이나 사장님께 3분 뒤 진행 알림)

---

## STEP 6 — 판매중지 + 확인

판매중지 전용 엔드포인트가 아직 없다면 아래 한 줄 진단을 추가 배포해야 한다.
(register-esm 처럼 set_sold_out 을 호출하는 POST 엔드포인트 신설 —
 `shared.platforms.esm.inventory.set_sold_out(goodsNo, 'auction', client=MF._esm_client('auction', env_prefix))`)

엔드포인트가 있으면:
```js
// 판매중지
const stop = await (await fetch('/api/live-send-test/suspend-esm', {
  method:'POST', headers:{'Content-Type':'application/json'},
  body: JSON.stringify({market:'auction', account:'브랙드웍스', goodsNo: window.__GOODSNO})
})).json();
// 확인 — isSell.iac == false 여야 판매중지 성공
JSON.stringify(stop);
```
확인: 옥션 셀러센터(ESM+)에서도 그 상품이 "판매중지"로 보이는지 눈으로 확인.

---

## STEP 7 — 서버 잠금 다시 끄기

`LIVE_REGISTER_ARMED` 환경변수를 제거하고 재배포. 실등록 문을 닫는다.

---

## STEP 8 — 결과 보고 + 다음 마켓

- 옥션 성공 → **G마켓은 같은 REG 에서 `market:'gmarket'` 로만 바꿔 STEP 1~7 반복**
  (같은 엔드포인트, siteType 2. 단 G마켓 선행자원은 G마켓 상품에서 다시 읽어야 함).
- 그다음 **11번가·롯데온** — 등록 함수를 새로 만들어야 함.
  스펙은 `다른세션_전달_4대마켓_상품등록_판매중지.md` §11번가·롯데온 참고.
  · 11번가: XML 필수 97개, 판매중지=전시중지(prdNo). 재고 0 불가.
  · 롯데온: 등록 전 출고지·거래처·배송비정책 3계약 선행. 판매중지=slStatCd END.

---

## 위험 지점 (반드시)

1. **등록~판매중지 사이 실판매.** 재고 1로 최소화. STEP 4→6 을 최단 시간에(단 2~3분 대기는 지킴).
2. **goodsNo 잃으면 못 내린다.** STEP 4 에서 반드시 기록.
3. **거짓 성공 금지.** ok:true 라도 goodsNo 없으면 실패. 셀러센터로 눈 확인까지.
4. **서버 게이트 끄기 잊지 말 것**(STEP 7). 켠 채로 두면 이후 실등록이 열린 상태로 남음.

---

## 코드 위치 (전부 origin/main)

- 등록: `shared/platforms/esm/products.py` register_goods, build_esm_register_payload
- 판매중지: `shared/platforms/esm/inventory.py` set_sold_out(isSell=false), get_sell_status
- 엔드포인트: `webapp/routes/live_send_test.py` register-esm(있음), product-detail(있음),
  suspend-esm(STEP 6 에서 신설 필요)
- 워크트리: `C:\dev\_wt_reg` (feat/4market-register)
