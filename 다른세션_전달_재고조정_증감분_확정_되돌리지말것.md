# 🔴 재고 「조정(adjust)」 = **증감분(차이값)** — 사장님 확정 (2026-08-13)

> **되돌리지 마세요.** 오늘 이 규칙이 **세 번** 뒤집혔고, 그때마다 재고가 조용히 틀어졌습니다.
> 사장님이 「이미 차이값으로 하기로 결정했다」고 못 박으셨습니다.

## 결정

**원장(`InventoryTx.qty`)에는 「차이(증감분)」를 남긴다.**

- 창구는 그대로 **「세어 보니 N개」(결과 수량)** 를 받는다 — 작업자에게 뺄셈을 시키지 않는다.
- 뺄셈은 **프로그램이** 한다. 화면 입력 방식은 안 바뀐다.
- 단일 진실 원천 = `프로그램/_시스템/shared/inventory_stock.py` 의 `fold_tx_rows()`
  → `adjust` 는 `total += q`

## 왜 절대값이 아닌가

| 이유 | 설명 |
|---|---|
| 합계로 못 센다 | 코드 스스로 「SUM 으로는 set 을 표현할 수 없다」고 적어 놨다. 절대값이면 조정 있는 SKU 만 느린 길로 하나씩 접어야 한다 |
| 창고가 둘 이상이면 뜻이 모호 | 「5개로 맞춤」이 **이 창고**를 5로 만들라는 건지 **전체**를 5로 만들라는 건지 정해지지 않는다 |
| 위치별 재고와 총합이 어긋난다 | 한 창고 실사가 다른 창고 재고까지 덮는다 |

## 오늘의 뒤집힘 이력 (같은 실수 반복 금지)

| 커밋 | 규칙 | 결과 |
|---|---|---|
| `1ff897f1` | 절대값 | 폰이 차이값이라 → **조정 5 → 재고 4** (적게) |
| `2b1132c5` | **증감분** | 폰을 절대값으로 바꿨던 것과 어긋나 → **조정 5 → 재고 6** (많게) |
| `b8459309` | 절대값 (Revert) | 「다른 세션과 정면 충돌」 — main 이 스스로 모순, 배포 막힘 |
| 지금 | **증감분** ← 확정 | `b8459309` 을 되돌려 복원 |

두 번 다 **에러 없이 숫자만 틀리는** 사고였습니다. 그래서 시험이 통과하는데도 뚫렸습니다.

## 손대기 전 반드시

1. **`fold_tx_rows()` 를 먼저 읽으세요.** 저장하는 뜻과 읽는 뜻이 갈리는 순간 재고가 조용히 틀어집니다.
2. 이 규칙을 바꾸려면 **네 곳을 한꺼번에** 바꿔야 합니다:

| 갈래 | 파일 |
|---|---|
| 읽는 쪽 | `shared/inventory_stock.py` (`fold_tx_rows` · `_stock_expr`) |
| 쓰는 쪽 | `lemouton/inventory/inbound.py` · `webapp/routes/api_inventory_link.py` · `webapp/routes/mobile.py` |
| 화면 | `webapp/templates/inventory/adjust/list.html` (`+N` 표기) |
| 시험 | `tests/inventory/test_adjust_semantics.py` · `tests/test_mobile_scan_lookup.py` |

3. 한 곳만 고치면 **반대쪽과 어긋나 재고가 틀어집니다.** 오늘 세 번 다 그래서 났습니다.

## 아직 안 고친 것 (별도 작업으로 띄워 둠)

감사에서 **실행으로 재현**한 것들입니다. 창고가 1곳이면 증상이 안 나서 기존 시험이 못 잡습니다.

| 어디 | 증상 |
|---|---|
| `webapp/routes/inventory/transactions.py:913,920` + `templates/inventory/tx_edit.html:66-67` | 조정 건 「수정」 화면이 옛 뜻(절대값). 안내대로 5 입력 → **재고 105**. 음수 조정은 `min="0"` 때문에 저장 자체가 막힘 |
| `lemouton/inventory/inbound.py:118` | 차이의 기준(`before`)을 **전 위치 합**으로 구하는데 기록은 특정 위치에 박힘 → A위치 **−2**, 합에서 **10개 증발** |
| `lemouton/inventory/inbound.py:121` + `transactions.py:565` | 금지된 스냅샷(`boxhero_stock_total`)을 절대값으로 덮고, 조정서 폼이 그걸 다시 기준으로 읽어 순환 → **없던 2개 생성** |

⚠️ 기존 `tests/inventory/test_adjust_semantics.py` 는 **전부 창고 1곳**(`loc=1`)이라 위 2·3번을 한 번도 안 봅니다. 고칠 때 **창고 2곳 이상** RED 부터 세우세요.

## 🔴 뒤집힘의 숨은 원인 — 「지킴이 시험」이 스스로를 속이고 있었다

`tests/inventory/test_adjust_writer_consistency.py` 는 「쓰는 곳 셋이 다 같은가」만 보는
좋은 설계인데, **규칙을 판별하는 방식이 틀려 있었다.**

```
옛 판별:  fold_tx_rows([("adjust", 999)]) == 999  →  절대값 규약
          절대값 → 999
          차이값 → 0 + 999 = 999      ← 똑같다! 구분이 안 된다
```

그래서 **차이값 규칙을 절대값으로 오인**했고, 정본·PC·상품관리가 다 차이값으로 맞아
있는데도 3건을 실패시켰다. 앞선 세션들이 이 잘못된 신호를 보고 「절대값이 맞나 보다」
하며 되돌린 것으로 보인다 — **뒤집힘을 막으려던 시험이 뒤집힘을 유발했다.**

```
고친 판별: fold_tx_rows([("in", 1), ("adjust", 999)])
          절대값 → 999 · 차이값 → 1000     ← 갈린다
```

**교훈**: 「어느 규칙인지」를 코드에 물어보는 검사를 만들 때는, **두 규칙이 다른 답을 내는
입력**으로 물어야 한다. 안 그러면 검사가 조용히 한쪽으로 단정한다.

## 📌 주석도 사실과 달랐다

폰(`webapp/routes/mobile.py`) 주석이 「쓰는 곳 셋이 모두 **절대값**」이라고 적혀 있었는데
실제로는 셋 중 둘이 차이값이었다. 코드를 안 보고 주석만 믿으면 또 뒤집는다.
**고칠 때는 세 곳의 실제 코드를 직접 열어 보라.**
