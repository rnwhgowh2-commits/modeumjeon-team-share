# 상품관리(노션) 작업 이어받기 — 2026-08-13 · v3

> **이 파일 하나로 이어받는다.** v1·v2 는 **낡았다** — 아래 §1 「v2 에서 틀린 것」을 먼저 읽어라.
> 충돌하면 **이 문서가 이긴다.**

---

## 0. 시작 전 30초

| | |
|---|---|
| **작업 폴더** | `C:\dev\_wt_prodnotion` · 브랜치 **`feature/prodmgmt-2an-2`** (v2 의 `-2an` 은 **병합 끝**) |
| 🔴 **첫 명령** | `git -C "C:\dev\_wt_prodnotion" fetch origin && git -C "C:\dev\_wt_prodnotion" rebase origin/main` |
| 🔴 **절대 금지** | `C:\dev\모음전 프로젝트` 본체에서 코드 작업 |
| **라이브 반영** | ✅ **PR #1018 병합 완료** (main `74cc5958` · 2026-08-13 11:08 UTC) |
| 사장님 | 비개발자·평이한 한국어. **「1건도 이상 있으면 안 된다」**. 돈은 추측 금지 |

### 🔴 머지 전 반드시
1. `gh pr list` + **`gh pr diff <번호> --name-only`** 로 겹침 확인.
   오늘 하루에 **두 번** 겹쳤다(#1001 · #1016). main 은 하루 **65커밋** 움직인다.
2. `cd "프로그램/_시스템" && python -m pytest tests -q -p no:cacheprovider` (**약 50분**)
   🔴 경로 없이 돌리면 pytest 캡처가 터진다 — 반드시 `tests` 를 준다.

---

## 1. 🔴 v2 에서 **틀린 것으로 밝혀진 것** (그대로 믿지 말 것)

오늘 **세 번** 「안 했다」고 잘못 보고했고, 사장님이 세 번 다 바로잡아 주셨다.
원인은 늘 같다 — **「인수인계서에 없다 = 안 했다」로 단정**했다.

| v2 가 말한 것 | 실제 |
|---|---|
| 하위탭 개명·순서 미착수 | ❌ **PR #963 으로 이미 라이브** (저장본까지 `옵션관리 → 상품관리`) |
| 옵션 클릭 → 조합 생성 미착수 | ❌ 이미 있음 (`matrix/detail.html:259`) |
| 판매이력 매입가 미착수 | ❌ 이미 있음 (실현마진 = 정산예정 − 실매입가) |
| 「상태에 따라 결과 다름」 미착수 | ❌ #963 「편집 목적지 통일」로 해결 |
| 탭 재편 = **정할 일** | ❌ **이미 확정본이 있었다** (v1 §6: 한눈에/사올 때/팔 때/판매·정산) |
| C1 죽은 자료 **5개** | ❌ **실제로 죽은 건 2개**뿐 (아래 §3) |

**교훈**: 항목을 「미착수」로 적기 전에 **코드로 확인**하라. `git log --grep` · `grep -rn` 30초면 된다.

---

## 2. ✅ 오늘 라이브에 들어간 것 (PR #1018 · 18커밋)

전체 시험 **9,094 passed · 실패 0** (49분 50초) 확인 후 병합.

- **쿠팡 쿠폰 자동화** — 없던 것을 새로 이었다. 정책에 즉시할인을 적어도 **쿠팡엔 한 원도 안
  나가던 것**이 실제로 깎인다. 만들기 → 붙이기 → 확인 → (실패 시) 회수 · 100원에서 10원씩
  **300원까지** · 기간은 **계약서 종료일**(=최대) · 자동연장 · 「정책/비정책」 토글
- **상품 상세 탭 6 → 4** (한눈에 / 사올 때 / 팔 때 / 판매·정산). **표·그래프 하나도 안 뺐다**
- **쿠팡 정산 정확한 식** — 엑셀 455행 전수 일치, 예외 0
- 11번가 N열 · 샵마인 노랑 사유 · 양식 설명 4개 · 개명 잔재 · 정산 스윕 2경로

---

## 3. 🚧 지금 브랜치에 있는 것 (`feature/prodmgmt-2an-2` · **PR 미생성**)

작업은 **끝났고** 시험도 통과했다. **PR 만 올리면 된다.**

| | 무엇 | 시험 |
|---|---|---|
| **C1** | 상품명 덮어쓰기 살림 — 5개 중 **진짜 죽은 2개** | 6건 |
| **B5** | 정책 저장 뒤 확인창 (몇 개 바뀌나 + 전체선택/해제) | 7건 + 뮤테이션 5종 |
| **C4** | 글자수 카운터 (쿠팡 100자, 스스는 「확인 불가」) | 화면 확인 |
| **C3 밑준비** | 목록 항목에 `category`·`policy_name` (추가 쿼리 0) | — |

### C1 실측 — 「죽은 자료 5개」의 진실
| 칸 | 읽는 곳 | 쓰는 곳 | 판정 |
|---|---|---|---|
| `product_name` | 48 | 34 | ✅ 살아 있음 |
| `external_product_id` | 5 | 4 | ✅ 살아 있음 |
| `naver_product_name_override` | 3 | **0** | 🔴 죽음 → **살렸다** |
| `coupang_product_name_override` | 3 | **0** | 🔴 죽음 → **살렸다** |
| `search_filter_id` | 1 | 3 | ✅ 살아 있음 |

---

## 4. ❌ 남은 일

### ① C3 목록 거르기 6종 — **조사 완료, 구현만 남음**
v1 §6 에 조사 완결본이 있다(그대로 쓰면 된다). 남은 자리 **네 곳**:
1. 목록 행에 `data-*` 싣기 (자료는 이미 붙였다 — `category`·`policy_name`)
2. 거르기 단추 6개
3. `applyFilter()` 조건 (`tower.html:400`)
4. **0건 안내문** (`zeroHint()` · `tower.html:418`) — 새 거르기로 0건이면 **왜 0인지 못 말한다**

🔴 **소싱처(OR) 하나가 위험하다.** `_url_union()` 은 상품 1개짜리라 목록에 쓰면 **N+1 로 276쿼리**
→ 목록이 즉사한다. 크롤실패 집계(`bundles_tower.py:806-832`)와 **같은 배치 모양**으로 짜라.
🔴 **쿼리 예산 시험**: `test_bundles_tower.py:542` 가 `second == first` + `≤60` 을 잰다.
   루프 안에서 세션을 쓰면 바로 깨진다.
🔴 **못 만드는 것 3가지**(v1 §6): 마켓그룹(개념 0건) · 마켓-계정별(등록에 account 없음) ·
   삭제상품(`Model.deleted_at` 없음). 만들려 들지 말고 **왜 못 하는지 화면이 말하게** 하라.

### ② 쿠폰 **라이브 실전송** — 아직 안 재 봤다
서버·화면은 다 됐지만 **라이브로 한 번도 안 돌렸다**(로컬에 자격증명 없음).
배포 뒤 **판매중지 상품 옵션 1개**로 확인할 것 — `scripts/live_test_coupang_coupon.py` 흐름 재사용.
🔴 확인할 것: 쿠폰이 실제로 만들어지나 · 옵션에 붙나 · **다음날 0시에 값이 깎이나** ·
  거부되면 10원씩 올라가나 · 실패 시 쿠폰이 도로 내려가나(윙에 빈 쿠폰이 안 쌓이나).

### ③ 이미지·상세페이지 「비정책」 — **담을 칸이 없다**
- 이미지는 지금도 **주소(URL) 목록**으로 다닌다(`images_json`) — 사장님 짐작이 맞았다.
  쿠팡엔 주소 그대로(`vendorPath`), 스스는 그 주소를 받아 CDN 에 올린다.
- 🔴 **상세페이지는 구성에 칸 자체가 없어** 어느 마켓에도 **빈 값**으로 나간다
  (`to_payload.py:252` 주석). 「비었다」가 아니라 **그릇이 없다**.
- → 새 칸 + 마이그레이션이 필요하다. 화면엔 그 사실을 이미 적어 뒀다.

### ④ 아직 사장님께 안 물어본 것
「매출」 칸의 뜻(지금은 할인 전) / 배송비 과거분 되채움 2,072건 / 반품 딱지 747건 /
옵션 잘리는 순서(96개 중 10개) / C등급 중 넣을 것 / 엑셀 한도값 나머지 5군데

### ⑤ 노션 「주요 기능」 표 — 미착수
매트릭스 옵션 별도 탭 · 판매처별 최저가 순위 · 카테고리 맵핑 보기 · 정책 항목 팝업

---

## 5. 🔴 오늘 새로 배운 함정 (v2 에 없던 것)

1. **시험이 통과해도 화면은 깨져 있을 수 있다 — 세 번 겪었다.**
   · 미리보기 칸을 `<button>` 으로 만들었더니 **단추 안 단추**라 브라우저가 바깥을 닫아 버렸다
   · 원본 잘라내기가 `a / b` 나눗셈을 **정규식으로 오인**해 함수 둘을 통째로 삼켰다
   · `renderT5` 만 인자 이름이 달라(`sum`) 치환에서 빠져 그 판만 조용히 죽었다
   → **조립한 JS 는 `node --check` 에 물리고, 화면은 브라우저로 직접 열어 봐라.**

2. **글자 세는 검사는 헛돈다.** `'0원' in '…깎을 금액이 **100원** 이상…'` 이 **참**이다.
   그 갈래에만 있는 말로 재라(예: '비정책').

3. **뮤테이션 없이 「시험 통과」를 믿지 마라.** 오늘 24종을 돌렸는데 처음엔 **3종이 안 잡혔다**:
   · 판을 짜 놓고 **안 쓰는** 코드 (`policy_targets` 만 재고 `request_for_policy` 는 안 쟀다)
   · 다른 방어(100원 하한)에 **우연히 걸려** 통과
   · 되돌리기 시험이 값 없이만 불러 **차이가 안 났다**

4. **main 이 하루 65커밋 움직인다.** rebase 는 **작업 직전에** 다시 하라.
   오늘 같은 파일에서 두 번 충돌했고, 한 번은 **`_to_int` → `_oe_to_int` 개명**을 놓쳤으면
   정산 스윕이 통째로 죽을 뻔했다(시험이 잡았다).

5. **`data/settle_deep_backfill.json` 이 rebase 를 막는다.** `skip-worktree` 가 걸려 있어
   체크아웃이 거부된다 → 잠깐 풀고 rebase 하고 **다시 걸어라**(풀린 채 두면 커밋에 딸려 간다).

---

## 6. 🔴 겹치는 열린 PR

**#1016** (`fix/checklist-discount-false-green`) 이 `lemouton/policy/discount.py` **같은 자리**에
`seller_share()` 를 더한다. **이름 충돌은 없다** — 「둘 다 남기기」로 끝난다.
그리고 **다른 세션이 「쿠폰 / 즉시할인쿠폰 칸 구분」을 만들고 있다**(사장님 말씀).
🔴 내 쿠폰 코드가 읽는 자리는 `rules['price']['discount_unit'|'discount_value']` 다 —
그쪽이 칸을 나누면 **여기를 같이 고쳐야 한다**(`lemouton/policy/discount.py:discount_of`).

---

## 7. 사장님 확정 사항 (이대로 하면 된다)

| 물음 | 확정 |
|---|---|
| 쿠폰 거부 시 재시도 상한 | **300원까지** |
| 쿠폰 기간 | **최대(=계약서 종료일) + 자동연장** |
| 쿠폰 연결 방식 | **c** — 정책 자동 + 단추 둘 다 |
| 쿠폰 기본값 | **100원** |
| 「정책/비정책」 모양 | **A2 미끄럼 스위치** (파랑=정책 · 주황=이 상품만) |
| 정책 값 바꿀 때 | **저장할 때 확인창** + **전체 선택/해제** |
| 「할인·쿠폰」 칸 | **C4** — 같으면 한 줄, 다를 때만 두 줄 |
| 상품 상세 탭 | **6→4** · 「사올 때」 배치는 **S5**(위 두 칸, 아래 매트릭스 전폭) |
| 샵마인 2,072건 노랑 | **b** — 노랑에 사유를 붙여 올린다 |

---

## 8. 진행 방식

사장님은 비개발자다. **평이한 한국어**로. 「1건도 이상 있으면 안 된다」가 기준이다.
돈 관련은 **추측 금지** — 코드와 라이브 실데이터로 증명하고 나서 말할 것.
시험은 **RED 를 먼저 보고** 고칠 것(TDD). **뮤테이션으로 시험이 무는지 확인**할 것.
화면 바꿀 땐 **브라우저로 직접 열어 볼 것**. 작업 끝나면 `/conversation-wrapup` 형식으로 마무리.
