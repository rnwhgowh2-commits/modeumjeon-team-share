# 옵션생성&상품생성 > 모음전 옵션 생성(직접) — 이어받기

작성 2026-08-14. **월 사용 한도에 걸려 중단**된 지점부터 이어서 하면 된다.

## 작업 자리

- 워크트리 **`C:/dev/모음전 프로젝트/_wt_optmtx`** (브랜치 `feature/optgen-matrix-panel`, `origin/main` 77eac5fb 기준)
- 로컬 서버: `.claude/launch.json` 의 **`optmtx-dev`** (포트 5302) → `/optgen/?tab=direct`
- 시연 자료 21줄이 이미 심겨 있다. 다시 심으려면
  `scratchpad/seed_optgen_demo.py "…/_wt_optmtx/프로그램/_시스템"`
- 시안 원본용 별도 워크트리 `_wt_optmtx_mock` (origin/main 그대로 — 손대지 말 것)

> scratchpad = `C:/Users/seung/AppData/Local/Temp/claude/C--dev---------/32880f94-c7b0-44af-b212-ac3866b3cda6/scratchpad`

---

## 사장님 확정 사항 (전부 반영 완료된 것 / 남은 것 구분)

| 항목 | 확정 | 상태 |
|---|---|---|
| 표 배치 | **4안** — 일곱 칸 + 오른쪽 상세 판 | ✅ 붙음 |
| 좁은 화면 | 넘치면 **가로 스크롤** | ✅ 그대로 |
| 매트릭스 번호 | 원본 `U…` · 파생 `P…` + 원본/파생 배지 | ✅ |
| 상품에 쓴 것 | 목록에서 **제외**, 서랍에 「상품으로 만든 것도 보기」(기본 꺼짐) | ✅ |
| 「미구성」 딱지 | **없앰** (축 0개라 늘 「준비 미완료」 + 사유 「축 없음」) | ✅ |
| 용어 | 「지우기」→「삭제」, 「덜 됨」→「미완료」 | ✅ 저장소 0건 |
| ① 삭제 자리 | **A안** — 표 바로 윗줄, 0개일 때도 자리 유지 | ✅ |
| ③ 전체 선택 | **A안** — 머리줄 마스터 체크, **보이는 줄만**, 중간 상태 | ✅ |
| ④ 모델명 칸 | **A안** + 단계 ①모음전→②축→③이름→④브랜드→⑤모델명 | ✅ |
| 모델 모음전 모델명 | **쉼표 나열** → 「모델」 축 값으로 저장 | ✅ |
| ㉮ 상태 칸 | **C안(막대) 바탕 + 필수/선택 구분** · 색은 초록/파랑 두 벌 보고 결정 | ❌ **시안부터 다시** |
| ㉯ SKU 정보 | **별도 열 「SKU 정보 상태」** + 품번·바코드·GTIN **셋 따로 호버**(D안) | ❌ 미착수 |
| ㉰ 입력 자리 | **「옵션 조합 생성 및 수정」 창**에 **격자**. 공란 = 미입력 | ❌ 미착수 |

### 필수/선택 (사장님 확정 — 준비 완료 판정은 **필수 4개만**)
- **필수 4**: SKU · 축 값 · 소싱처 URL · 맵핑 → 없으면 상품을 못 만들거나 못 판다
- **선택 2**: 모델명 · 초기재고 → 비어도 전송은 된다(모델명은 매트릭스 이름이 폴백)
- 색: **초록 = 됨** (이 프로그램 고정 규칙). 파랑 벌도 나란히 보여 드리고 고르시게 할 것

---

## 남은 일 (이 순서로)

### 1. ㉮ 상태 칸 시안 다시 (사장님 대기 중)
- 산출: `_시안_임시/optgen_direct_상태_필수선택_5안.html`
- 조립 스크립트를 **새로 쓰지 말고** `scratchpad/build_mockup3.py` 를 물려받을 것
  (껍데기·글꼴 처리·폭 재기·검사 방식이 다 들어 있다)
- 5안 = **C안(한 덩어리 막대) 바탕**으로 「필수 4 / 선택 2 를 어떻게 가르나」
  A 막대 둘로 끊기 / B 선택은 테두리만 / C 필수만 막대+선택은 잔글씨 /
  D 앞에 「4/4」 + 선택은 흐리게 / E 선택은 호버로만
- 🔴 **완료인 줄에 선택 2개가 빈 모습**을 반드시 보여라 — 이번 결정의 핵심이다
- 🔴 같은 5안을 **초록 벌 · 파랑 벌** 두 가지로 나란히

### 2. ㉯㉰ 품번·바코드·GTIN 구현
- 새 칸 2개: `options.article_no`(SKU별 품번) · `options.gtin`
  🔴 **NULL = 「아직 안 적음」.** 빈문자열 기본값 금지
  (`shared/db.py` 의 `_apply_lightweight_migrations()` 에 한 줄씩)
- 이미 있는 것: `options.barcode` · 자체 생성 함수 `shared/sku_format.gen_barcode()`
  없는 것: SKU별 품번 · GTIN (`models.article_no` 는 **모델 단위**라 다른 것)
- 입력: `webapp/static/option_url_modal.js` 큰 창에 **격자**(줄=SKU, 칸=품번·바코드·GTIN)
  - 공란 허용(미입력). 「자체 바코드 생성」은 **빈 것만** 채우고 든 값은 안 덮는다
  - 🔴 이 창은 CSS 를 `injectStyle()` 로 주입하고 `scheduleRerender` 가 `#oum-left` 를
    통째로 갈아끼운다 — **입력하던 값이 날아가지 않게** 할 것
  - 🔴 바코드·GTIN **중복 검사** 필요. 겹친 채 나가면 마켓에서 상품이 뒤섞인다
- 목록: 표에 열 하나 추가, 이름 정확히 **「SKU 정보 상태」**. 셋 진척(예 「품번 12/15」),
  🔴 못 세는 건 0 이 아니라 **「—」**
- 🔴 호버는 **6규칙** 필수 — 정본 `webapp/templates/bundles/_matrix_v3.html` 의
  `_pmFloat`/`_pmShowFloat`/`_pmScheduleHide` 를 쓰고, **열기 140ms 지연**을 반드시 더할 것
  (표 줄은 촘촘해 지연이 없으면 지나가는 줄이 카드를 가로챈다).
  동작하는 본보기: `_시안_임시/optgen_direct_상태_품번_5안.html` 의 ㉯-D

### 3. 상태 칸 실제 구현 (㉮ 확정 후)
- 열 이름 「맵핑·준비」 → **「상태」**
- 라벨·색은 🔴 `lemouton/matrix/readiness.py` 의 `PHASE_LABEL`·`PHASE_CLS` **한 곳에서만**
- 🔴 **맵핑 딱지는 사유가 아니라 진척으로 갈라라.** 사유만 보면 소싱처 주소 0개인
  **7줄이 「맵핑 됨」**으로 뜬다(실제 0/1)
- 모델명 칸 안내문(「비워 두면 매트릭스 이름을 씁니다」)이 선택 항목 표시와 안 어긋나게 손볼 것

### 4. 마무리
- W12 축 값 우클릭 「미구성 SKU 등록」(편입 API `optgen_sku.py` 는 이미 있음)
- 🔴 **`pytest tests/` 전체(약 8,500건) 완주** — 아직 전체를 한 번도 안 돌렸다
  (지금까지는 `tests/catalog/ design/ matrix/ sourcing/` = 1,792건까지만)
- 머지 + 라이브 확인

---

## 새로 생긴 것 (이미 검수 통과)

| 파일 | 무엇 |
|---|---|
| `lemouton/matrix/readiness.py` | 위상 판정 · `PHASE_LABEL`/`PHASE_CLS` 단일 원천 |
| `lemouton/matrix/unbuilt.py` | 미구성 SKU 판정(축 0 ∧ 옵션 1). 🔴 prefix 를 안 본다 |
| `lemouton/sourcing/axis_summary.py` | 축 이름·모음전 구성·모델명·빈 축 수 |
| `lemouton/sourcing/source_url_stats.py` | 소싱처별 URL 수 · 맵핑 진척 |
| `webapp/routes/optgen_sku.py` | 미구성 SKU 편입 API |
| 시험 11벌 | `tests/matrix/`·`tests/sourcing/`·`tests/catalog/`·`tests/design/` |

`Model.bundle_model_name` 컬럼도 새로 생겼다 — 색상 모음전의 모델명 자리.

---

## 🔴 다시 데이지 말아야 할 것 (이번에 실제로 겪은 것)

1. **시안에 글자·숫자를 손으로 적지 마라.** 세 번 연속 이걸 어겨 시안이 화면과 다른 말을 했다
   (「축을 아직 안 짬」 ↔ 실제 「축 없음」). 자료는 `scratchpad/extract_rows.py` 가
   **화면 함수 `optgen._boxes()` 를 그대로 불러** 뽑는다 — `rows.json`·`screen.json` 만 읽어 쓸 것.
2. **검사가 아무것도 안 보는 일이 잦다.** 만든 검사를 **일부러 깨서 FAIL 이 나는지** 반드시 확인.
   깨도 통과하면 그 검사는 없는 것이다.
3. **글꼴을 인라인할 때 상대 주소가 깨진다.** 원본 CSS 를 시안에 넣으면 글꼴 선언이
   시안 위치 기준으로 풀려 20건이 실패하고, 글꼴이 바뀌면 **칸 폭이 전부 달라진다.**
   `build_mockup2.py`/`3.py` 에 「원래 주소로 다시 선언」이 들어 있다 — 지우지 말 것.
4. **폭은 라이브 분량으로 재라.** 로컬 3줄로는 안 난다.
5. `settle_deep_backfill.json` 은 서버를 돌리면 바뀐다 — 커밋 전에 되돌릴 것.

## 실측해 둔 폭 (1870 창 기준, 표가 쓸 수 있는 자리 1053px)

- 지금 상태 칸 141px → 표 1053px, 안 밀림
- ㉮ 지난 5안: A 347 / B 203 / C **266** / D 261 / E 383 (C 는 표를 114px 민다)
- ㉯ 지난 5안: A 192 / B 141 / C 141 / D **179** / E 266
- 「SKU 정보 상태」를 **별도 열**로 빼면 더 밀린다 — 붙인 뒤 다시 잴 것
