# [다른 세션 전달용 프롬프트] 판매처 데이터코드지도에 「과거이력(문제 발생·해결)」 탭 신설

> 이 파일 전체를 새 세션에 그대로 붙여넣으면 됩니다. 자기충분 프롬프트입니다.

---

## 🎯 목표

**판매처관리 > 데이터코드지도**(`/marketplace-guide/map`)에 **「과거이력」 탭**을 신설한다.
- 목적: 마켓 연동에서 **발생했던 문제와 그 해결 방법**을 한곳에 축적 → 같은 원인이 다른 마켓/계정에서 재발할 때 즉시 참고.
- 소싱처 쪽에는 이미 **동일 성격의 탭(에러이력 카탈로그)** 이 있다 → 그 패턴을 판매처 지도에도 이식한다.
- 첫 콘텐츠 = **오늘(2026-07-17) 실제로 발생·해결한 4개 마켓 가격/재고 문제**(아래 §콘텐츠).

---

## 📐 참고할 기존 구조 (반드시 먼저 읽을 것)

**① 소싱처 데이터코드지도 (이식할 패턴)**
- 파일: `프로그램/_시스템/webapp/templates/sourcing_guide/map.html`
- 탭 구조: `.gtabs` / `.gtab[data-s]` — s1 전체흐름 … **s6 「⚠️ 에러이력」** … s8 동기화
- s6 섹션(`id="s6"`)이 참고 대상: `lead`(핵심 요약) + `🛑 확인 콜아웃` + `table.dt`(에러 카탈로그) + 원문(MD) 링크. 이 톤·레이아웃을 따른다.

**② 판매처 데이터코드지도 (탭을 추가할 대상)**
- 템플릿: `프로그램/_시스템/webapp/templates/marketplace_guide/map.html` (루트 클래스 `.dm2`)
  - 상위탭 컨테이너: `#dm-toptabs`(`.toptabs`), 마켓 가로탭: `#dm-mktabs`(`.mktabs`)
- 라우트: `프로그램/_시스템/webapp/routes/marketplace_guide.py`
  - 화면: `GET /marketplace-guide/map` (base + `?bare=1` SAMEORIGIN iframe 라우트 둘 다 존재 — 인앱 가이드 모달용, 그대로 유지)
  - 데이터: `GET /marketplace-guide/map-data.json` → `webapp.marketplace_api_map.load_map/validate_map`
- **단일 진실 원천(SOT)**: `프로그램/_시스템/webapp/data/marketplace_api_map.json`
  - top keys: `schema_version, markets, unifiedStatuses, transitions, codes, apis`
  - 검증기: `프로그램/_시스템/webapp/marketplace_api_map.py` (`load_map`, `validate_map`)

---

## 🧱 구현 방식 (SOT 원칙 준수)

판매처 지도는 **데이터 주도(JSON SOT → 검증기 → 탭 렌더)** 구조다. 과거이력도 같은 방식으로:

1. **SOT 확장**: `marketplace_api_map.json`에 `incidents`(또는 `history`) 배열 추가.
   각 항목 스키마(권장):
   ```json
   {
     "id": "2026-07-17-lotteon-multiaccount-trno",
     "date": "2026-07-17",
     "markets": ["lotteon"],          // 관련 마켓(복수 가능)
     "area": "가격/재고",              // 가격/재고/주문/정산/상품 등
     "title": "롯데온 다계정 trNo 버그",
     "symptom": "메인 외 모든 롯데온 계정 상품조회·가격·재고 8888",
     "cause": "trNo를 계정 클라이언트가 아니라 전역 config에서 가져옴",
     "fix": "get_product_detail·update_prices·update_stocks가 client._cfg 우선 사용",
     "commit": "2f7a400e",
     "severity": "high",              // high/med/low
     "status": "resolved",
     "lesson": "계정 식별자는 반드시 계정 클라이언트에서 (전역 config 금지)"
   }
   ```
2. **검증기 확장**: `webapp/marketplace_api_map.py`의 `validate_map`에 `incidents` 필수필드·타입 검증 추가(누락·오타 조용한 통과 금지).
3. **탭 렌더**: `map.html`의 `#dm-toptabs`에 「🗂️ 과거이력」 탭 추가 + 해당 뷰. 소싱처 s6 스타일(요약 lead + 콜아웃 + 표) 재현. 표 컬럼 권장: `날짜 · 마켓 · 영역 · 증상 · 원인 · 해결 · 교훈(·커밋)`. 마켓/영역 필터(칩) 있으면 좋음.
   - 마켓 배지·색은 기존 `.dm2` 마켓 탭 색 재사용(일관성).
4. **정렬**: 최신순(날짜 desc).

> 만약 SOT에 넣기 부담되면 별도 파일 `webapp/data/marketplace_incidents.json` + 전용 라우트도 허용. 단 **검증기·라우트·렌더 3종 세트**는 반드시 갖출 것(조용한 실패 금지).

---

## 📋 콘텐츠 — 오늘(2026-07-17) 발생·해결한 항목 (그대로 시드로 입력)

### 🟩 스마트스토어 (가격/재고 실전송)
1. **다계정 NOT_FOUND** — 증상: 전송이 상품 소유 계정이 아니라 전역 기본 어댑터로 나가 실패. / 원인: 어댑터가 계정별로 안 만들어짐. / 해결: `scoped_send.run_explicit`이 `env_prefix`로 계정 어댑터 생성(`SmartStoreAdapter(client=_smartstore_client(env_prefix))`).
2. **NOT_FOUND 지속(상품ID 종류)** — 증상: 계정 맞아도 수정 실패. / 원인: 수정 API는 `originProductNo` 필요한데 저장값이 `channelProductNo`. / 해결: 어댑터가 `resolve_product_ids`로 originProductNo 변환 후 `edit_options`.
3. **계정 이름 접미사 불일치** — 증상: 계정 매칭 실패로 키 공란. / 원인: `SetChannel.account_key`("브랜드마켓")와 `UploadAccount`("브랜드마켓쿠팡"/"브랜드마켓(쿠팡)") 불일치. / 해결: `_resolve_env_prefix`에 마켓명 접미사·괄호 제거 후 유일매칭.

### 🟦 쿠팡 (재고 조회)
- **옵션 재고 조회 불가** — 증상: 상품상세에 옵션 재고가 없어 화면 '조회 안됨'. / 원인: get_product 상세에 재고 미포함. / 해결: `get_inventory` 경로(`GET /v2/providers/seller_api/apis/api/v1/marketplace/vendor-items/{vendorItemId}/inventories`)+`get_quantity` 신설. 옵션 전량은 느려서 **화면에서 고른 옵션 1건만 온디맨드 조회**.

### 🟧 11번가 (미개통 → 정본 스펙으로 개통)
- **개통 자체 안 됨 + 재고 API 스펙 오류** — 증상: 어댑터 '전송 보류', 재고조회 415/0건. / 해결(셀러 오픈API센터 정본 2026-07-17):
  - Content-Type `application/xml` → **`text/xml; charset=euc-kr`** (415 해소)
  - **재고조회** `POST /rest/prodmarketservice/prodmarket/stocks`, 요청본문 `<ProductStocks><ProductStock><prdNo>N</prdNo></ProductStock></ProductStocks>` — **중첩 `<ProductStock>` 필수**(빠지면 "0 건")
  - **재고변경** `PUT /rest/prodservices/stockqty/{prdStckNo}` — **재고번호(prdStckNo) 1건씩 안전 변경**(full-replace 폐기, 옵션 소실 위험 제거)
  - **판매가 조회** `GET /rest/prodmarketservice/prodmarket/{prdNo}` → `<Product><selPrc>`
  - 어댑터 개통: `market_option_id=prdStckNo`, 재고조회로 optWght 확보 → PUT stockqty → 상품가(update_price)

### 🟥 롯데온 (다계정 trNo 버그 — 이번 세션의 핵심)
- **비메인 계정 전부 8888** — 증상: 메인(브랜드위시/LOTTEON_MAIN) 외 모든 롯데온 계정(브랜드마켓·브랜드박스 등) 상품조회·가격·재고 **전부 실패**(returnCode 8888 "인증정보와 요청정보가 일치하지 않습니다"). 주문 조회는 정상이라 가려져 있었음. / 원인: `get_product_detail`·`update_prices`·`update_stocks`가 판매자번호 `trNo`를 **계정 클라이언트(`client._cfg`)가 아니라 전역 `LOTTEON` config**에서 가져옴 → 토큰(계정) + trNo(전역=메인) 불일치. / 해결: 세 함수 모두 `cfg = getattr(client, "_cfg", None) or LOTTEON`로 계정 config 우선. **커밋 2f7a400e**. (참고 메모리: `project_lotteon_multiaccount_trno_bug_fix`)
- **부가 감사 결과**: 같은 "식별자를 전역에서 가져오는" 버그를 전 마켓 점검 → **롯데온만 해당, 쿠팡·옥션·G마켓·11번가·스마트스토어는 정상**(각각 vendor_id는 client._cfg 우선 / seller_id는 계정 JWT / openapikey 헤더·상품단위 경로 / 토큰 캐시 client_id별 분리).
- **부가**: 실전송 후 API값과 셀러오피스 화면이 달라 보였던 건 **변경 전 캐시 페이지**였고, 새로고침 시 일치 확인 — **코드 문제 아님**(과거이력엔 "화면 캐시 주의" 팁으로 기록 권장).

> 검증 방법도 함께 기록해두면 좋음: 오늘은 `webapp/routes/live_send_test.py`에 만든 진단 도구(`/api/live-send-test/pick-orders`·`direct-current`·`direct-send`(stock_only)·`accounts`·`current-stock`)로 **실제 판매 상품에 −100·−1(또는 −200·재고5) 전송→읽어서 반영확인→원복**하는 방식으로 왕복 검증했다.

---

## ✅ 준수 사항 (프로젝트 규칙)

1. **UI 변경 전 `/design-mockup` 게이트** — 탭/표 디자인은 시안 결정 게이트 먼저(항목별 결정 3줄). 기존 `.dm2`·소싱처 s6 톤과 일관되게.
2. **`/live-browser-verify`(또는 `/ui-verify`)** — 배포 후 `mou-m.com/marketplace-guide/map`에서 실제로 「과거이력」 탭이 뜨고 4개 마켓 항목이 보이는지 육안 검증. `?bare=1` 라우트도 깨지지 않는지 확인(SAMEORIGIN).
3. **데이터 무결성** — 검증기(`validate_map`) 통과 필수. incidents 필수필드 누락 시 조용히 통과 금지(예외로 표면화).
4. **워크트리 작업 시 master 머지 + 라이브 재시작까지 완료**해야 종료(Stop 훅 검사).
5. **마무리 시 7섹션 보고**(`/ui-verify` 형식).

## 🏁 완료 기준

- `marketplace_api_map.json`(또는 별도 incidents 파일)에 오늘 4개 마켓 항목이 스키마대로 들어감 + 검증기 통과.
- `/marketplace-guide/map`에 「과거이력」 탭이 소싱처 s6 톤으로 렌더되고, 마켓·영역 필터가 동작.
- 라이브 배포 + 브라우저 육안 검증 완료.
- (선택) 향후 항목 추가 방법을 탭 상단에 1줄 안내(“새 문제 해결 시 여기에 추가”).

---

*작성: 2026-07-17 세션. 이 프롬프트는 판매처 지도 과거이력 탭 신설 1건 전용.*
