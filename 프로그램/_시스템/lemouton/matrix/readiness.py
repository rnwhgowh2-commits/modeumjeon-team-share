# -*- coding: utf-8 -*-
"""옵션함 위상 판정 — 지금은 일괄조회 크기(`_CHUNK`)만 이 워크트리에 있다.

🔴 이 파일의 완전한 버전(`phase_batch` 등 목록 위상 판정)은 다른 워크트리
   (`feature/optgen-matrix-panel`)에서 같은 날 별도로 작업 중이다. 그 브랜치가
   합쳐지면 이 파일도 같이 합쳐 넣어야 한다 — 지금은 `lemouton/matrix/sku_info.py`
   가 이 상수 하나만 참조해서 이 최소 버전만 둔다(그 파일도 같은 상수를 쓰므로
   나중에 합칠 때 숫자가 갈릴 일이 없다).
"""

#: IN 절에 한 번에 넣어도 되는 크기 — 라이브에서 오래 잠그지 않게.
_CHUNK = 500
