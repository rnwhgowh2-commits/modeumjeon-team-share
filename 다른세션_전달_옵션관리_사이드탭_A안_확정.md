# 다른세션 전달 — 「매트릭스 옵션」(옵션관리) 재설계: A안(오른쪽 미끄럼판) 사장님 확정

> 2026-08-05 사장님 확정("a ㄱㄱ"). 카드형 → 목록 격자형 + 사이드탭 구조 변경을 진행 중인 세션이 이 문서대로 구현한다.
> 시안(렌더 검증 완료): 바탕화면 `모음전 옵션관리 시안 v1.html` — 상단 탭 A가 확정안. 시안 속 숫자는 예시.
> 원본 화면 코드: origin/main `프로그램/_시스템/webapp/routes/matrix.py` + `webapp/templates/matrix/index.html`(70줄)·`detail.html`(516줄). 시안은 이 두 파일의 `<style>`을 통째로 재사용했으므로 클래스(`mx-badge`·`mxsel`·`table.t`·`code` 등)를 그대로 쓰면 톤이 맞는다.

---

## 1. 확정 구조 — A안 (오른쪽 미끄럼판)

- 화면 = **목록 표(풀폭)** + 행 클릭 시 **오른쪽에서 미끄러져 나오는 상세 판(폭 600px, ✕ 닫기)**.
- 화면 성격 = **나열·살펴보기 전용**. 만들기(상품·파생·신규 묶음)는 전부 「옵션생성 & 상품생성」으로 **이동 링크만** 둔다. (사장님 방향: "생성/수정은 옵션생성&상품생성, 옵션관리는 정보·연결성")
- 이 화면에 남기는 조작: 옵션 켜고 끄기(is_active) · 옵션 이름 **정정(갈아끼우기)** · 소싱처 주소 붙이기/끊기. 이름·브랜드·카테고리 수정은 **원본에서만**(파생은 잠금 + "원본으로 가기").

## 2. 목록 표 — 열 8개와 정렬 (시안=코드: 같은 기법으로)

| 열 | 내용 | 정렬 |
|---|---|---|
| 묶음번호 | U번호, `code` 칩 | 좌 |
| 이름 | 이름 굵게 + 원본/파생 배지(`mx-badge o/d`) + 파생이면 "원본 ○○○" 회색 | 좌 |
| 축 구성 | 예 `색상 8 × 사이즈 12 · 켜짐 89/96` (켜짐 숫자 tabular-nums) | 좌 |
| 담긴 상품 | n개(단위 작게 분리). 0이면 회색 `미사용` 배지 | **우 + tabular-nums** |
| 마켓 등록 | 등록된 마켓 칩(스스·쿠팡…), 없으면 — | 가운데 |
| 소싱처 연결 | n곳 + 실패 있으면 빨간 `실패 n` 배지 | **우 + tabular-nums** |
| 살펴볼 신호 | `품절 n`(빨강) `손실가 n`(주황), 없으면 — | 가운데 |
| 최근 확인 | 상대시각, 오래되면(예: 3일+) 주황 | 좌(회색) |

- 필터 칩: `전체` `원본` `파생` `미사용` `살펴볼 것` + **`재고관리 전용(단독_) n개 숨김` 기본 ON**. `단독_%` 모델코드는 기본 목록에서 제외(카드 86개 중 다수가 단독이라 진짜 모음전이 묻히는 문제).
- 행 hover 배경, 선택 행 파랑 배경(`sel`).

## 3. 사이드판 — 상세 4탭

1. **요약**: 브랜드·카테고리·모델·옵션 n개(꺼짐 n) 한 줄 → 축 격자(`mxsel` 그대로, 칸 숫자=연결 소싱처 수, 조회용) → 안내문("여기서는 켜고 끄기·이름 정정만. 만들기는 옵션생성&상품생성으로 이동") → `이 묶음으로 상품 만들기 →` `파생 만들기 →` (이동 링크, ghost 버튼).
2. **연결 관계** (이 화면의 주인공): `원본 → 파생들 → 각각 담긴 상품(M번호) → 상품별 마켓 등록 상태(등록됨/전송 대기/미등록)` 트리. 지금 보는 묶음에 `지금 보는 중` 노랑 칩. 하단 안내 "파생에서는 이름·소싱처 주소를 못 고칩니다 — 원본에서만".
3. **소싱처**: 표 — 소싱처 | 옵션 매칭(성공/실패 n) | 표면가(우) | 최종매입가(우, 굵게) | 재고 | 확인 시각 | 바로가기↗. 최저 행 초록 배경 + `최저` 배지. **최저 판정 = 최종매입가 기준, 못 구했으면 「확인 불가」 그대로**(폴백 금지 — detail.html `popHTML` 규칙과 동일). 하단 `소싱처 주소 붙이기/끊기`.
4. **이력**: 가격 흐름(소싱처별 선그래프, `/api/matrix/price-history` 재사용) → 크롤 이력(성공/실패+사유) → 정정 이력(갈아끼움 기록, "옛 옵션 잔존 0" 표기).

## 4. 데이터 원천 (스키마는 main 기준 재확인 필요 — 아래는 조사 시점 기준)

- 묶음·축: `Model`(`lemouton/sourcing/models.py`) + N축 정본 `BundleOptionStep`. 옵션: `Option`(canonical_sku, axis_values_json, is_active).
- 담긴 상품: `ProductSet`/`SetProduct`/`SetOption`(`lemouton/sets/models.py`) — **한 SKU가 여러 상품에 들어갈 수 있음**(UNIQUE는 (set_product_id, sku)뿐). 역방향 조회가 이 화면의 존재 이유.
- 마켓 등록: `SetChannel`+`SetChannelOption` / `MarketRegistration`(sku×market) / `OptionAccountRegistration` **3벌 공존** → 🔴 아래 §5-1.
- 소싱처: `SourceProduct`/`SourceOption`+`OptionSourceLink`(크롤 정본) / `BundleSourceUrl`+`OptionSourceUrlLink`(URL 정본) / `OptionSourceUrl`(레거시 캐시) → 🔴 아래 §5-2. 가격 우선순위: `SourceProduct.last_price` > `OptionSourceUrl.price_cached`(api_pricing 주석).
- 이력: `PriceTrackHistory` · `BundleRun`/`CrawlJob`.
- U번호·원본/파생·`/matrix` 라우트는 main에만 있음(이 조사 브랜치 crawl-worker-queue는 stale — 재구현 금지 메모 참조).

## 5. 🔴 구현 전 결정해야 하는 함정 2개

1. **마켓 등록 배지의 단일 원천**: 등록 상태 기록 테이블이 3벌. 화면에 배지 달기 전에 어느 것을 정본으로 읽을지 결정하고, 다른 화면(상품 마켓 전송 탭 등)과 같은 원천을 쓸 것. 화면마다 다른 상태가 뜨면 "사실과 다른 화면" 사고 재발.
2. **소싱처 탭의 읽기 원천**: URL 저장소 3세대 공존(`URL 저장소 3중분열` 메모). `_option_matrix_data`(api_pricing.py)가 이미 3곳 병합 로직을 갖고 있으니 **그 빌더를 재사용**하고 새 병합 로직을 또 만들지 말 것.

## 6. 검증 기준 (끝나기 전)

- 단독_ 숨김 기본 ON 상태로 목록 수가 라이브 실데이터와 맞는지.
- 연결 관계 트리: 실제 파생 1개 이상 있는 묶음(예: 르무통 메이트 계열)에서 원본→파생→상품→마켓 4단이 실데이터로 그려지는지.
- 소싱처 탭 숫자가 기존 `/bundles/<code>` 매트릭스 화면과 같은 값인지(원천 불일치 검출).
- 파생 상세에서 수정 UI가 잠겨 있고 "원본으로 가기"가 뜨는지.
