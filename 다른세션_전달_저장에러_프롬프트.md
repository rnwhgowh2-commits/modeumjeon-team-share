# 다른 세션 전달용 — 저장(save) 단계 에러 → 크롤링 가이드 등재 프롬프트

> 사용법: 아래 `===` 사이 블록을 **그대로 복사**해서 다른 세션에 붙여넣으세요.
> (이번 세션 = 2026-06-26~27 옵션 조합 모달 데이터 저장 이슈 조사·수정. main 배포 v55~v57)

---

## 붙여넣기용 프롬프트 (이 줄 아래 ===부터 ===까지 복사)

===

크롤링 가이드(정본: `프로그램/_시스템/docs/크롤링-가이드.md` + 인앱 `/sourcing-guide/map` 데이터 지도, 보기/원문 토글)에 **"저장(save) 단계 에러"** 항목을 추가해줘.

**목적:** 신규 소싱처 추가나 크롤링/저장 코드 업데이트 시, 과거에 났던 동일 이슈가 재발하지 않도록 이 가이드를 먼저 참고하게 만들기 위함.

**분류 규칙 (각 에러마다 2축으로 태그):**
- 레이어: `백엔드 코드` / `프론트엔드 코드` / `그 외(인프라·데이터·설계·미구현)`
- 도메인: `재고` / `가격` / `기타`
- 크롤링 중 직접 발생한 재고·가격 에러가 아니라면 **'기타' 항목**으로 분류.

**추가할 에러 목록 (2026-06-26~27, 옵션 조합 모달 데이터 저장 이슈. main 배포 v55~v57 = commit c948d4ec·77127de8·c5406e92):**

1. [프론트 / 재고 / ✅수정] 재고관리 매핑을 입력·저장해도 새로고침하면 사라짐. 원인=`autoSave()`가 재고매핑 저장 단계를 호출 안 함(`invApplyMapping`이 삭제된 버튼에만 묶여 고아화). 재발 체크: 새 저장 단계 추가 시 autoSave에 배선됐는지 + 로딩완료(invLoaded) 가드.
2. [프론트 / 기타(정직성) / ✅수정] 저장 실패해도 "저장 완료"로 표시. 원인=에러를 `console.warn`으로 삼킴. 재발 체크: 저장 함수는 `{ok,errors[]}` 반환하고 버튼이 실패를 표면화.
3. [그 외(인프라 DB풀)+프론트 / 가격 / ✅수정] URL 저장 시 매 저장마다 임의 ~7건이 HTTP 500으로 조용히 누락(option_ids 유실). 원인=48개를 `Promise.all`로 동시 저장→Supabase 풀(15) 초과. 수정=동시성 5 제한+PUT 일시500 재시도. 재발 체크: 대량 저장은 반드시 동시성 제한.
4. [프론트(+백엔드 설계) / 가격 / ✅수정] 멀쩡한 URL↔옵션 매핑이 통째 삭제될 수 있음. 원인=옵션키가 canonical_sku로 해소 실패 시 빈 목록 전송, 백엔드는 빈 목록=전체삭제. 수정=빈 목록이면 omit(보존). 재발 체크: delete-then-insert 저장은 빈 페이로드 가드.
5. [프론트(유발=서버 일시 빈응답) / 기타(저장) / ✅수정] 저장 직전 옵션정보 재로딩이 빈 응답이면 `r.json()`이 터져 저장 전체 무산. 수정=응답 가드 후 명확 메시지로 중단. 재발 체크: 외부 응답 파싱 전 `res.ok`+본문 검증.
6. [프론트 / 기타(저장) / ✅수정] 옵션 구성 저장 실패해도 계속 진행→stale 기준으로 신규 매핑 유실. 수정=실패 시 즉시 중단(재시도 유도).
7. [프론트 / 기타(저장) / ✅수정] X·취소·배경 클릭으로 닫으면 저장 실패가 무음. 수정=닫혀도 실패를 알림으로 표면화.
8. [프론트 / 가격·재고 / ⏳미수정] 축 값 이름변경(예: 240→245) 시 그 옵션의 매핑이 조용히 유실. rename 시 키 마이그레이션 필요.
9. [백엔드 / 가격 / ⏳미수정] URL 저장 시 무효 SKU를 거부 건수 보고 없이 조용히 skip(`_sync_option_links`). 재고 매핑은 rejected 보고하나 URL은 무음.
10. [프론트 / 가격 / ⏳미수정] 매트릭스 '재계산▶' 버튼이 저장 0인 stub.
11. [백엔드 / 가격 / ⏳미수정] 가격설정 일괄저장이 허용목록 밖 필드를 조용히 무시(`api_pricing.py price-config/bulk`).

**공통 근본 패턴(가이드에 원칙으로 명시):** "조용한 실패(silent failure)" = ①실패를 삼키고 성공처럼 보임 ②delete-then-insert가 빈/부분 페이로드로 데이터 삭제 ③무제한 동시성이 인프라 한도 초과. → 재발 방지 3원칙: (a) 저장 실패는 항상 사용자에게 표면화, (b) 삭제형 저장은 빈 페이로드 가드, (c) 대량 저장은 동시성 제한+멱등 재시도.

작업 후 정본 문서와 인앱 데이터 지도(보기/원문) 양쪽 다 반영하고, CLAUDE.md 훅도 필요시 갱신해줘.

===

---

## 참고 — 레이어별 요약 (붙여넣기 불필요, 이해용)

- **백엔드 코드 문제 (2건):** #9 URL 무효SKU 무보고, #11 가격설정 허용목록 밖 필드 무시
- **그 외(인프라) (1건):** #3 DB 연결풀(15) 초과 (프론트 동시성 무제한이 유발)
- **프론트엔드 코드 문제 (나머지 전부):** #1·2·4·5·6·7·8·10
- **도메인:** 크롤 중 재고/가격 에러가 아니라 **저장(save) 단계** → 대부분 '기타'(저장), 일부 가격(URL→매입가)·재고(매핑)
