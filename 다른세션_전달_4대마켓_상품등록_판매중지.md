# 다른 세션 전달 — 4대 마켓 상품 등록 → 판매중지 (2026-07-21)

> 이 문서 하나로 이어받을 수 있게 씀. 목표: 르무통 메이트를 롯데온·11번가·옥션·G마켓에
> **실제로 등록했다가 바로 판매중지**. 옥션이 dry-run 검증까지 끝났고 실등록 직전 단계.

---

## 지금 어디까지 됐나 (한눈에)

| 단계 | 옥션 | G마켓 | 11번가 | 롯데온 |
|---|---|---|---|---|
| 상품 목록 조회 | ✅ | ✅ | ✅ | ✅ |
| 등록 스펙 확보(지도) | ✅ | ✅ | ✅ | ✅ |
| 등록 함수 구현 | ✅ | ✅(옥션과 동일) | ❌ | ❌ |
| 판매중지 함수 | ✅ set_sold_out | ✅ | ❌ | ❌ |
| **dry-run payload 조립** | ✅ 검증됨 | 미실행 | ❌ | ❌ |
| **실등록 → 판매중지** | ⏸ 사장님 확인 대기 | ⏸ | ❌ | ❌ |

**옥션이 실등록 직전.** payload 조립까지 라이브에서 검증 완료. 실행만 남음.
G마켓은 옥션과 **같은 엔드포인트·같은 함수**(siteType 만 2). 옥션 되면 자동.
11번가·롯데온은 등록 함수를 새로 만들어야 함(스펙은 지도에 다 있음).

---

## 옥션 실등록 — 바로 실행 가능 (재료 100% 확보)

### 확보된 재료

**콘텐츠(쿠팡에서 역읽기):** 르무통 메이트, 브랜드 르무통, 옵션 76개(색×사이즈).
쿠팡 상품ID `16176862782`, 계정 `브랜드마켓쿠팡`. 판매가 옵션0 = 135,820원.
⚠️ 쿠팡에 이미지가 0개 → 대표이미지는 기존 옥션 상품에서 빌림(필라 샌들 이미지).

**선행자원(기존 옥션 상품 `5806568636`에서 재사용):**
| 자원 | 값 |
|---|---|
| 출하지 placeNo | 23590515 |
| 발송정책 iac | 1939006 |
| 반품 addrNo | 46321571 |
| 택배사 companyNo | 10008 |
| 상품고시No | 25 (신발) + details 25-1~25-13 전부 "상품상세페이지 참조" |
| ESM 카테고리 | `00120005002000000000` / 옥션 site catCode `37500700` |

### 실행 절차 (사장님 "ㄱㄱ" 후)

1. **서버 게이트 켜기** — `LIVE_REGISTER_ARMED=1` 을 배포 환경변수로. 재배포 필요.
   (등록 전용 게이트. 가격전송 MOUM_LIVE_UPLOAD 와 별개 — 다른 세션 전송 안 열림)
2. **실등록** — `POST /api/live-send-test/register-esm` body 에 `arm:"1"` +
   아래 REGBODY. 응답에서 `goodsNo` 수령(없으면 실패 — 거짓성공 금지 배선됨).
3. **2~3분 대기** — ★ ESM 규칙: 등록 직후 수정(sell-status 포함) 불가.
   바로 부르면 "상품 정보가 부정확합니다" 에러.
4. **판매중지** — `set_sold_out(goodsNo, "auction", client=...)` → isSell.iac=false.
5. **확인** — `get_sell_status` 로 isSell.iac==false 검증.
6. **게이트 다시 끄기** — `LIVE_REGISTER_ARMED` 제거 재배포.

### REGBODY (dry-run 검증 완료된 것)
```json
{
  "market": "auction", "account": "브랙드웍스",
  "goods_name": "르무통 메이트 스니커즈 운동화 (테스트)",
  "cat_code": "00120005002000000000", "site_cat_code": "37500700",
  "price": 135820, "stock": 1,
  "place_no": 23590515, "dispatch_policy_no": 1939006,
  "return_addr_no": "46321571", "delivery_company_no": 10008,
  "official_notice_no": 25, "official_notice_details": [ /* 25-1~25-13 */ ],
  "image_url": "http://image.auction.co.kr/Itemimage/...",
  "detail_html": "<p>르무통 메이트</p>", "options": null,
  "arm": "1"
}
```
브라우저에 `window.__REGBODY` 로 남아 있음(세션 살아있으면). official_notice_details 는
`/api/live-send-test/product-detail?market=auction&product_id=5806568636` 로 다시 읽으면 됨.

---

## 구현된 코드 (전부 origin/main 반영됨)

| 파일 | 내용 |
|---|---|
| `shared/platforms/esm/products.py` | `register_goods()`, `build_esm_register_payload()`, `search_goods()` |
| `shared/platforms/esm/inventory.py` | `set_sold_out()`(판매중지), `get_sell_status()` — 기존 |
| `shared/platforms/__init__.py` | AUCTION/GMARKET paths 에 `search`, `register` |
| `shared/platforms/lotteon/products.py` | `list_products()` — 상품 목록(pageNo·rowsPerPage 필수) |
| `shared/platforms/eleven11/products.py` | `search_products()` — 다중 상품 조회 |
| `webapp/routes/live_send_test.py` | 진단 엔드포인트 4종(아래) |
| `webapp/routes/sets_api.py` | add_channel 6개 마켓으로 확장 |

**진단 엔드포인트(전부 읽기/게이트):**
- `GET /api/live-send-test/product-list?market=&q=&days=&all=1` — 4마켓 상품 검색
- `GET /api/live-send-test/product-detail?market=&product_id=&account=` — 쿠팡/옥션 상세 역읽기
- `POST /api/live-send-test/register-esm` — 옥션·G마켓 등록(dry-run/실등록 2중잠금)

---

## 11번가·롯데온 등록 — 새로 만들어야 함 (스펙은 지도에 있음)

### 11번가 (XML, 필수 97개)
- 등록: `POST http://api.11st.co.kr/rest/prodservices/product`, 루트 `<Product>`.
  신규상품은 `stdPrdYn=Y`. 응답 `<productNo>`+`<resultCode>`(200성공/210신규/500실패/400한도).
- 판매중지: `PUT /rest/prodstatservice/stat/stopdisplay/[prdNo]` — prdNo 하나. STAT:105=전시중지.
  ★ 11번가는 **재고 0 불가**, 판매중단은 전시중지가 유일.
- 필수 핵심: selMthdCd(01고정가), dispCtgrNo(최하위카테고리), prdNm(100자,CDATA),
  selPrc(10원단위), prdImage01(600x600), 원산지묶음, KC인증, 옵션(useYn/colCount),
  배송(dlvCstInstBasiCd), A/S안내(asDetail 공백불가). 재고 prdSelQty(0불가).
- client: `Eleven11Client.request(method, path, body:str) -> str` (XML).

### 롯데온 (필수 13 + 38테이블 미공개)
- 등록: `POST /v1/openapi/product/v1/product/registration/request`.
  필수: trGrpCd*, trNo*, scatNo*(표준카테고리), dcatLst*, mallCd*(LTON), lfDcatNo*,
  slTypCd*[GNRL], pdTypCd*, itmLst[](sitmNm/slPrc/stkQty). **body 상세는 38테이블 —
  product/detail 응답 스키마와 동일 구조를 떠야 함(지도에 안 펼쳐짐).**
- 판매중지: `POST /product/v1/product/status/change`, slStatCd*[SALE/SOUT/END].
  ★ 롯데온도 재고 0 아님. slStatCd=END/SOUT 로 내림.
- ★★ 등록 전 **3개 계약 선행 필수**: 출고지(registDvpSr)·하위거래처(registLrTraderSr)·
  배송비정책(registDvCstSr). 옥션처럼 기존 상품에서 재사용 가능한지 먼저 조회할 것.
- ★ 다계정 trNo 는 계정 client._cfg 에서. 전역 config 쓰면 8888 에러(과거 버그).

---

## 위험 지점 3개 (반드시 지킬 것)

1. **등록~판매중지 사이 실판매 노출.** 옥션·롯데온은 등록=바로 판매중(2~3분 수정금지).
   그 창에 주문 들어오면 오버셀. → 재고 1로 최소화, 등록 직후 최단 시간에 중지.
2. **상품ID 못 잡으면 못 내린다.** 등록 성공했는데 응답에서 goodsNo/productNo 를 못 받으면
   판매중 실상품을 못 내림. → register_goods 는 이미 "ID 없으면 실패" 배선됨. 4마켓 새 함수도 동일 규약.
3. **거짓 성공.** HTTP 200/code=SUCCESS 믿지 말고 **마켓 상품번호 수령으로만** 성공 판정.
   11번가 resultCode 210 도 정책위배면 200(일반)으로 강등되니 코드 확인 필수.

---

## 검증/배포 메모

- 배포는 GitHub Actions(push→main). 동시 실행 겹치면 헬스체크 실패로 뜰 수 있음 → `gh run rerun`.
- 워크트리 `C:\dev\_wt_reg` (feat/4market-register). 다른 세션이 쓸 수 있게 남겨둠.
- 앱 시작 필수 import 함정: 새 모델 만들면 app.py 에 `import ... # noqa: F401` 추가
  (안 하면 라이브 502 — 2026-07-20 실제 장애). ESM 등록은 새 모델 없어 해당 없음.
