# 다른 세션 전달 — 상품관리(노션) 작업 인수인계

> 2026-08-12. 앞 세션은 삭제된다. **이 문서 하나로 이어받는다.**
> 원문 = 노션 「무재고 모음전 솔루션 개발현황 > ■ 프로그램 개선사항 > **상품관리**」
> 🔴 이 문서의 「완료」는 전부 **독립 검증 에이전트 5개**로 재확인했고, **반증 전담 에이전트**가
> 뒤집은 것은 아래에 그대로 적었다. 앞 세션의 자기 보고를 그대로 옮기지 않았다.

---

## ⏱ 2026-08-13 진행 (이 줄 아래가 최신 — 나머지는 배경 설명으로 읽어라)

워크트리 `C:\dev\_wt_prodnotion` · 브랜치 `feature/prodmgmt-2an`.
🔴 **rebase 했다** — 21커밋이 아니라 **103커밋** 뒤져 있었다. 충돌 0.

| 항목 | 상태 | 커밋 |
|---|---|---|
| A0 rebase | ✅ | — |
| A1 옛 이름 5곳 + 404 이름표 + 감시 시험 | ✅ 라이브 화면 확인 | `d4eb9a13` |
| A2 양식 설명 (3개 → 실제로는 **4개**) | ✅ 화면까지 도달 확인 | `1b00091e` |
| A3 스윕 2경로 배송비 배선 | ✅ | `9e8e57ae` |
| A6 11번가 실값 덮임 | ✅ | `523d2ca7` |
| B1 쿠팡 정상가=할인가 | ✅ | `75c7204f` |
| A4 샵마인 판정 이동 | ⛔ **사장님 결정 대기** | — |
| A5 쿠팡 엑셀 재대조 | 🟡 **엑셀 쪽 끝(예외 0)** · 저장분 대조는 라이브 필요 | — |
| B2·B3·B4·B5 · C1~C4 | ⬜ 미착수 | — |

**시험**: 전체 **8,533 passed / 1 failed**.
그 1건(`tests/orders/test_settle_plan_api.py::test_집계_지급예정일축`)은
**origin/main 에서도 실패**한다(체크아웃해 확인). 내 변경과 무관 — 200원이
「미확정예정」→「정산 시작 전」으로 분류만 옮겨갔고 합계(300)는 그대로다.

### 🔴 이 문서에서 **틀린 것으로 밝혀진 것** (그대로 믿지 말 것)

1. **「배송료 124건 전부 3.30%, 예외 0」** → 건수가 틀렸다. 엑셀 9개를 행 해시로
   중복 제거(574 → 455)하면 배송료 행은 **156건**(금액 있는 것 126건)이다.
   요율이 전부 3.30% 인 것은 **맞다**(예외 0).
   🔴 단, **요율의 분모는 「판매액」이지 「판매가」가 아니다.** 「판매가」는 단가라
   그걸로 나누면 8.25%·7.42% 같은 **가짜 예외 14건**이 나온다. 실제로 한 번 빠졌다.
   전수 확정식 —
   · 상품 행 299건 : `정산금액 = 판매액 − 판매자할인쿠폰(A+B) − 판매수수료` (안 맞는 행 **0**)
   · 배송료 행 156건 : `정산금액 = 판매액 − 판매수수료`, 요율 **3.30%** (안 맞는 행 **0**)
2. **「양식 설명 3개」** → **4개**다. 「마켓수수료」 설명도 틀렸다(롯데온처럼 마켓이
   수수료 실값을 주면 그 값을 그대로 쓰는데, 설명은 「실결제 − 정산예정금액」 한 갈래만 말했다).
3. **A2 의 §2-E 줄번호**는 rebase 로 어긋난다. 파일도 `webapp/routes/order_export.py` 가
   아니라 **`lemouton/markets/order_export.py`** 다.

### A4 — 사장님께 물을 것 (숫자로 실증해 뒀다)

`shopmine_recon._judge_money` 를 실제로 돌려 확인:

| | 값 | 판정 |
|---|---|---|
| 샵마인 | 117,924 | (기준) |
| 옛 우리 값 | 117,924 | **match**(초록) |
| 새 우리 값 | 117,792 | **def**(노랑 = 정의차이) |

차이 132원 > 허용오차 6원이라 초록이 안 된다. 다만 샵마인 값이 재현식
(`실결제 − 수수료 + 고객배송비`)에 걸려 **빨강은 아니다**. 영향 2,072건.

### A3 에서 새로 알아낸 함정 (다음 사람이 반드시 알아야 함)

스윕은 행을 **한 줄씩** `_finalize_rows([row])` 에 넣는다. 배송건 열쇠(`_shipkey`)는
저장할 때 이미 pop 돼 있어서 **「배송건당 1회」 중복 제거가 한 번도 안 걸린다.**
그래서 `_ship_settle_plan()` 이 담당 줄을 직접 정한다(안정 정렬 필수 —
DB 순서에 맡기면 조회마다 담당이 바뀌어 두 번 세어진다).

---

## 0. 시작 전 30초

| | |
|---|---|
| **작업 폴더** | `C:\dev\_wt_prodnotion` · 브랜치 `feature/prodmgmt-2an` |
| 🔴 **먼저 할 것** | **`git fetch origin && git rebase origin/main`** — 이 워크트리는 origin/main 보다 **21커밋 뒤졌다**(67파일 ±6,673줄). 상품관리·옵션관리 화면 자체가 main 에서 더 바뀌었다. **여기서 grep 한 결과를 「지금 코드」로 믿으면 틀린다** |
| 🔴 **절대 금지** | `C:\dev\모음전 프로젝트` 본체에서 작업 — 2,967커밋 뒤진 브랜치라 상품관리 코드가 아예 없다 |
| 라이브 반영됨 | PR **#963** (노션 6건) |
| 🔴 **라이브에 없음** | `feature/prodmgmt-2an` 의 **`1952f6c4` + `2e2cc8a7`**(정산 배송비 수수료) — push 만 됐고 **머지 안 됨**. 충돌은 0 |
| 사장님 | 비개발자·평이한 한국어. **「1건도 이상 있으면 안 된다」**. 돈은 추측 금지 |

---

## 1. 노션 원문 → 상태 (원문 한 줄도 안 뺐다)

### ■ 하위탭

| 원문 | 상태 | 근거 |
|---|---|---|
| a. 이름변경 : 모음전 상품관리 / 모음전 옵션관리 → 모음전 삭제. 상품관리 / 옵션관리 | 🟡 **부분만** | 메뉴·주요 제목은 됨. **아래 §2-A 옛 이름 잔존 5곳** |
| b. 순서변경 | ✅ 완료 | `_CATALOG3=('i_matrix','i_bundles','i_catalog')` · 라이브 확인 |

「마켓 상품 현황」도 **「실마켓 상품 현황」**으로 개명(사장님 지시).

### ■ 옵션관리

| 원문 | 상태 | 근거 |
|---|---|---|
| 1) 옵션 클릭 시, 옵션 조합 생성 수정 할 수 있어야함 | 🟡 **부분만** | **원본만 된다. 파생(P)은 단추가 안 뜬다** — §2-B |
| 2) 사이드바 디자인 벗어남 | ✅ 완료 | `.gl-sb` 배경·테두리·여백 제거 · 250px · 14px (검증 확인) |

### ■ 상품관리

| 원문 | 상태 | 근거 |
|---|---|---|
| 1) 상태에 따라 눌러서 나오는 결과 다름 | ✅ 완료 | **상태 탓이 아니었다** — §3-2 |
| 2) 탭별 기능 확실히 + 필요 기능 (더망고 활용) | 🟡 조사·시안 끝 / **구현 대기** | 사장님 **2안 확정** |
| 3) 판매이력탭 : 매입가 필요 | ✅ 완료 | 4자리 · colspan 9/5/7 대조 확인 |

### ■ 같은 노션 「주요 기능」 표 — 🔴 **이번에 안 다룸**

| 원문 | 상태 |
|---|---|
| 하위탭 b. **매트릭스 옵션(원본/파생) 관리** 별도 탭 | ❌ 미착수 |
| **매출 증대 전략** — 판매처별 최저가 경쟁 순위 분석(배송비 포함) | ❌ 미착수 |
| 카테고리 맵핑 보기 (소싱처 ↔ 판매처별) | 🟡 t4 에 마켓별 등록 카테고리만 |
| 적용된 정책적용 보기 (정책명 > 항목 팝업) | 🟡 정책명만, 항목 팝업 없음 |

---

## 2. 🔴 반증으로 뒤집힌 것 — **여기부터 읽어라**

### A. 「매트릭스 옵션」 옛 이름이 5곳 남았는데 **감시 시험이 초록불로 통과한다**

```
matrix/detail.html:2      {{ mo.name }} — 매트릭스 옵션     (탭 제목)
matrix/detail.html:114    ← 매트릭스 옵션 목록              (되돌아가기 단추)
matrix/index.html:306,310 매트릭스 옵션이 없어요            (0건 안내)
bundles/new.html:17
```
404 화면은 **「모음전 코드: 매트릭스 옵션」**이라고 뜬다
(`matrix.py:499`·`optgen.py:330` → `errors/option_not_found.html:12`).

🔴 `tests/catalog/test_catalog_tabs_rename.py:209` 이 **'모음전 상품관리'·'모음전 옵션관리' 두 낱말만** grep 한다.
「매트릭스 옵션」은 안 본다 → **깨진 채로 64 passed**.
(메모리 `feedback_preservation_check_must_catch_broken` 의 함정 그대로)

**할 일**: 5곳 개명 + 감시 시험에 「매트릭스 옵션」 추가 + 404 라벨을 코드 자리에 맞게.

### B. 「옵션 클릭 시 조합 수정」이 **파생에서는 안 된다**

`matrix/index.html:569` 게이트 `s.editable && s.model_code`, `matrix.py:664` `editable = (kind == ORIGIN)`.
게이트에는 이유가 있다 — 파생은 `model_code` 가 비어 서버가 **원본 것을 대신 채워** 주므로,
그대로 링크하면 **남의 묶음을 고치게 된다**. 그런데 **그 사실이 화면에 안 적혀 있다.**
노션 원문은 「옵션 클릭 시」라 원본/파생을 안 가른다.

**할 일**: 파생에 「원본에서만 고칠 수 있어요 · 원본으로 가기 →」를 **보이게**.

### C. 샵마인 대조 탭이 **2,072건 판정을 바꾼다**

`shopmine_recon.py:185-192` 가 우리 N열을 샵마인 「정산예상금액(배송비포함)」과 **±6원**으로 댄다.
샵마인은 `M + 고객배송비 전액`(45건 전수 실측)인데 **우리만 −132** 가 된다.
→ 배송비 붙은 쿠팡 줄이 「일치(초록)」→「**정의차이(노랑)**」로 통째 이동.
`paid−fee+ship` 재현식에 걸려 빨강은 면하지만, **「1건=5만원 정합성」 목적 화면**이다.

**할 일**: 사장님께 알리고, `_settle_reproductions` 에 「샵마인값 − 배송비수수료」 재현식 추가 검토.

### D. 화면 산수가 안 맞게 됐다

배송비 열은 **4,000 그대로**, 마켓수수료는 배송비 수수료 132원을 **안 담는다**.
→ 화면에서 `정산예정금액 + 배송비 = 정산예정금(배송비포함)` 이 **132원 안 맞고 설명이 없다**.
사장님이 「줄을 다 더해도 합계가 안 맞는 화면」을 이미 한 번 지적한 적이 있다.

### E. 양식 설정 화면의 열 설명 **3개가 틀렸다**

```
order_export.py:51  "정산예정금액"        desc "상품정산 + 배송비정산(수수료 차감)"  ← 🔴 금지된 정의를 광고
order_export.py:57  "수수료율"            desc "마켓수수료 ÷ 총주문금액"            ← 분모는 실결제로 바뀜(:3252)
order_export.py:60  "정산예정금(배송비포함)" desc "정산예정금액 + 고객배송비"          ← 이제 배송비 정산 실값
```
🔴 특히 **M열 desc** 는 이번 커밋이 **금지한 정의**다. 다음 사람이 그걸 믿고 M 에 배송비정산을 넣으면
**2026-08-07 이중계상 사고가 그대로 재발**한다. 렌더 경로: `orders.py:121` → `orders/index.html:1119`.

### F. 11번가 실값이 **조용히 지워진다** (선행 결함 · 이번 변경이 위험 증가)

`eleven11_unconf.py:123` 이 N 에 `stlAmt` 를 **직접 쓰고** M 은 안 건드린다.
저장분 보강(`order_export.py:3679`)이 `_finalize_rows` 를 다시 돌려 **N 을 덮는다**.
M 이 비면 **N 이 빈칸이 된다**.

### G. 정산 스윕 2경로에 **미배선** — 과거분이 안 고쳐진다

```
order_ingest.py:1355          쿠팡 deliv 를 받아서 **버린다**
order_ingest.py:1528·1555     스스 deliv 를 만들어 놓고 **한 번도 안 읽는다**
```
스윕은 60~75일 묵은 주문을 추정→실값으로 바꾸는 **바로 그 경로**다.
거기서 M 만 실값이 되고 N 은 고객배송비 전액을 더한다 → **과거분 +132원 과대가 그대로 남는다.**

### H. 상품관리 판매이력 「실현 마진」도 같이 움직인다

`bundles_tower.py:230` `realized = Σ(N − 실매입가)` → 배송비 붙은 줄마다 **+132**.
게다가 `:127-136` 300초 캐시 도장은 **실매입가에만** 걸려 있어, 정산이 바뀌어도 **최대 5분 낡은 숫자**가 뜬다.

---

## 3. 배포 완료분 — PR #963 (17파일 · +821줄, 전건 검증 확인됨)

### 3-1. 하위탭 개명·재정렬

🔴 **`_ITEM_DEFS` 만 고치면 화면에 안 나온다.** 서버는 저장본(`data/sidebar_layout.json`)을 읽고,
`get_layout_for_template()` 은 저장본에 **이미 있는** 항목을 `_item()` 없이 통과시켜 `_FORCE_RENAME` 이 안 닿는다.
**같은 자리에서 네 번 넘어졌다.**

- `_migrate_catalog_rename`(`:452`) — 이름 비교, **늘 다시 건다**(자가치유)
- `_migrate_catalog_order`(`:482`) — **`_SCHEMA=9` 게이트로 딱 1회**. 안 그러면 드래그가 매 요청 되돌아간다
- 배선 `:651-652` `_mig12`·`_mig13`, or-체인 `:658`
- `api_put_layout:792` `payload.setdefault('schema', _SCHEMA)`
- 🔴 저장소의 `sidebar_layout.json` 은 **라이브에 안 쓰인다** — `fly.toml` 이 `/app/data` 볼륨으로 덮는다
- ⚠️ `_mig` 번호가 다른 세션과 **실제로 충돌했다**(`:653-656` 주석). 번호 말고 **이름 목록**으로 바꾸자는 제안 있음

### 3-2. 「상태에 따라 결과 다름」 — 범인은 상태가 아니었다

라이브 92개: 상태1 89·상태3 1 은 조립대로, **상태2 2개만** `/bundles/<code>` 로 샜다.
상태와 겹쳐 보였지만 **진짜 기준은 원본 매트릭스 유무**. 우연한 상관관계.

- **뿌리**: `ensure_origin` 이 `deleted_at` 을 안 봐 한 번 지운 원본이 **영영** 안 돌아옴
- **수정**: 되살린다(`service.py:39-46`). 🔴 **새로 만들면 유니크 제약에 터진다**(시험이 잡음)
- **문 하나로**: `/optgen/product/by-code/<code>`(`optgen.py:332`) — 원본 없으면 만들어 보냄

### 3-3. 판매이력 매입가

서버가 이미 주던 **미사용 필드**였다. 🔴 `purchase` 는 **실현 마진을 낸 줄만**의 합
→ 「(실현 기준)」 + 「N건 중 B건에 쓴 돈」. 값 없으면 **0 아니라 「—」**.
실매입가(`order_line_purchases`)만 — 사입가·소싱 예상가 폴백 **금지**.

---

## 4. 미머지 — `1952f6c4` + `2e2cc8a7` (정산 배송비)

**근거**: 사장님 쿠팡 정산 엑셀 실물, 배송료 **124건 전부** `4,000 → 132(3.30%) → 3,868`. 예외 0.
라이브 영향 **2,072건**.

- 마켓이 준 실값을 `_ship_settle` 로 실어 **N열에서만**. 없으면 종전대로(지어내지 않음)
- 🔴 요율을 **박지 않았다** · 🔴 **M열엔 안 넣는다**(2026-08-07 이중계상 사고)
- 🔴 안 맡는 줄은 **`= 0` 명시 대입**. pop 하면 `_merge_row` 가 못 지워 **저장분에서 이중가산**
  (반증 실측 재현: +3,868 과다) → `2e2cc8a7` 로 수정
- 쿠팡·스스 **인라인 빌더만** 배선. **스윕 2경로 미배선**(§2-G)
- 시험 9건. 전체 8,361 passed

🔴 **머지 전 필수**: §2-C 샵마인 영향 사장님 확인 · §7 153주문 재대조

---

## 5. 남은 작업 — 우선순위

### 🔴 A. 지금 바로 (돈·거짓 초록불)

| # | 할 일 |
|---|---|
| A0 | **워크트리 rebase** (21커밋 뒤짐) — 다른 일 하기 전에 |
| A1 | §2-A 옛 이름 5곳 + **감시 시험 구멍** |
| A2 | §2-E 양식 설명 3개 (금지된 정의를 광고 중) |
| A3 | §2-G 스윕 2경로 배선 (과거분) |
| A4 | §2-C 샵마인 영향 사장님 확인 → 머지 |
| A5 | §7 153주문 재대조 |
| A6 | §2-F 11번가 N 덮임 |

### 🔴 B. 쿠팡 가격 (사장님 확정, 미착수)

| # | 할 일 | 근거 |
|---|---|---|
| B1 | **정상가 = 할인가** 통일 | `coupang.py:52-53`. 🔴 이 경로 **라이브에서 살아 있음**(`/bundles/<code>` 「🚀 신규 등록」 단추 숨김 아님 확인) |
| B2 | **마진 두 줄** (정가 / 할인가) | 지금 예상 마진은 **전부 정가 기준**(`policy/preview.py:198`). 쿠폰 노출가를 같은 줄에 보여주면서 마진엔 안 쓴다 |
| B3 | **태그 20/10개** | `coupang.py:69` 2개 하드코딩. 규칙은 정책 §7-11(`process_rule_schema.py:311`)에 이미 있음 |
| B4 | **GTIN** | `coupang.py:37,65` `barcode=""`. 엑셀 「쿠팡만 입력(옵션별)」 |
| B5 | **쿠폰 자동화** | `promotions.py` 래퍼 완성·부르는 곳 없음. 🔴 **다음날 0시부터** 적용. 한 브랜드부터 |

### 🟡 C. 2안 구현 (사장님 확정)

| # | 할 일 | 힌트 |
|---|---|---|
| C1 | **죽은 자료 5개** | DB 칸 있고 화면만 없음 → 거의 공짜. `sources/models.py:29 product_name` · `:28 external_product_id` · `sourcing/models.py:110-111 naver/coupang_product_name_override` · `:113 Model.note` · `registration/models.py:111 search_filter_id` |
| C2 | **탭 6→4** | 한눈에 / 사올 때(소싱처+매트릭스+가격이력) / 팔 때(마켓·정책) / 판매·정산. 🔴 표·그래프 **하나도 안 뺀다** |
| C3 | **목록 거르기 6종** | §6 |
| C4 | **글자수·옵션축 카운터** | 「x/50자」. 🔴 롯데온만 byte → **구분 표기**. 한도표 `registration/market_limits.py` **이미 있음**(새 파일 금지). byte 칸 **따로** |

### ⚪ D. 나중
반품 섞인 주문 **747건**(롯데온 602·쿠팡 132·11번가 11·G마켓 2) 「일부 반품」 딱지 ·
과거 저장분 되채움 · 노션 「주요 기능」 미착수분 · §2-D 화면 산수 · §2-H 캐시 도장

---

## 6. 목록 거르기 — 조사 완료 (그대로 쓰면 된다)

거르기는 **100% 브라우저(JS)**. 서버가 전 상품을 그리고 `applyFilter()`(`tower.html:346`)가 숨긴다.
→ ① items 에 값 ② `data-*` ③ `state`+조건, **세 자리**만.

| 필터 | 자료 | 쿼리 |
|---|---|---|
| 브랜드 | 있음 | 0 |
| **카테고리** | `Model.category`. 라이브에 채워짐. 🔴 **「신발」과 「스니커즈」가 섞여** 같은 층 아님 | 0 |
| **모델명** | `data-blob` 이 이미 커버 | 0 |
| **정책명** | `price_index()` 가 이미 계산(`:614`) | 0~1 |
| **소싱처(OR)** | 🔴 `_url_union()`(`:655`)은 상품 1개짜리 → 목록에 쓰면 N+1(276쿼리) **즉사**. 크롤실패 집계(`:806-832`)와 같은 배치 모양으로 | +2 |
| **마켓 등록** | `items[].markets` 있음 | 0 |
| **품절** | `items[].soldout` + `Model.soldout_alerted_at` | 0 |

**🔴 못 만듦**: 마켓그룹(계정 묶음 개념 **0건**) · 마켓-계정별 완전판(`MarketRegistration` 에 account 없음 → 「판매중인데 회색」 재발) · 삭제상품·삭제보류(`Model.deleted_at` 없음)

**예산**: `test_bundles_tower.py:542` 가 `second == first` + `≤60`. 🔴 루프 안 세션 사용 금지.
**0건 안내문**(`tower.html:364-389`)도 같이 늘릴 것 — 새 필터로 0건이면 **왜 0인지 말을 못 한다**.

---

## 7. 쿠팡 정산 — 검증된 사실

### 기준 = **판매가 × 수량 − 수수료** (할인가 아님)
확정 2,952건 역산. 갈리는 195건 중 **상품금액 기준 36 / 실결제 기준 0**.
쿠폰으로 덜 내도 정산은 안 줄어든다 — **차액은 쿠팡이 메운다**. 요율 대부분 11.55%, 일부 12.43~12.51%.

🔴 **추정(estimated) 행으로 역산 금지** — 그 행은 `실결제×(1−11.55%)` 로 만든 값이라 **당연히 11.55%**(순환 논리).

### 이상치 298건 = **전부 반품 주문이었다**
정상 2,656 · 쿠팡부담 할인 34 · 실결제 빈칸 256 · **진짜 이상 6 → 전부 반품·부분반품. 쿠팡 정산은 정확.**

앞 세션이 「12.6만원 손해」로 **두 번 오판**했다. 원인: ① 엑셀 9개가 **겹치는데 합산**(119행 중복)
② `<기본배송료>` 가 **「옵션명」이 아니라 「옵션 ID」 칸**에 있어 거르기가 헛돌았다.

### 재대조 절차 (A5)
1. Wing 「정산>정산현황>정산예정」 회차별 엑셀 → `MSF_PAYMENT_REVENUE_DETAIL`
2. **중복 제거**(행 해시) 3. 배송료는 **옵션 ID** 기준
4. 주문번호별 `정산금액` 합 ↔ `/orders/api/settle-plan/detail?category=*&market=coupang` (부류 7개 전부)
5. 🔴 `상품정산예정` 과 비교 금지 — 엑셀 정산금액은 **상품+배송비 합**
6. 우리 저장분 읽기: `GET /orders/preview.json?markets=coupang&from=…&to=…` — **91일 넘으면 마켓에 안 묻고 저장분만**

★ 화면 최종지급액 = **Σ정산금액 × 지급비율(70%/30%)**. 상세 엑셀엔 「최종지급액」 열 **없음**.

---

## 8. 🔴 반복된 함정

1. **사이드바는 코드만 고치면 안 나온다** — 저장본이 이긴다. 마이그레이션 + `_load()` 배선. 네 번 반복
2. **「없는 줄 알았는데 있던 것」** — 휴지통(`i_trash`) · 수집제한(홈 「⚙ 자동화 토글」) · 주문내역 검색(「판매 이력」 탭) · 업데이트 잠금(대량등록 「📦 상품관리」). **없다 하기 전에 grep**
3. **자료는 있는데 화면이 안 읽는 것** — 판매이력 매입가가 그랬다. **응답부터 보라**
4. **낱말 몇 개만 세는 grep 시험은 깨진 채 통과한다** — §2-A 가 실증
5. **엑셀 여러 개 = 중복 의심** — 119행 겹쳐 오판
6. **pop 은 저장분 병합에서 안 지워진다** — 명시 대입(`=0`)
7. **0 으로 채우지 말 것** — 모르면 「확인 불가」
8. **`_mig` 번호·같은 줄 동시 편집 충돌** — 세 세션이 `s_etc` 를 번갈아 고치는 중
9. **템플릿 JS 는 `<script>` 뽑아 `node --check`**
10. **워크트리 머지 의무** — main 머지 + 배포까지가 끝

---

## 9. 사장님 대기 항목

| 항목 | 왜 |
|---|---|
| **샵마인 대조 2,072건 이동** (§2-C) | 「일치」→「정의차이」. 머지 전 알려야 함 |
| **엑셀 한도값 6군데 정정** | 쿠팡 「옵션축 2개 고정」 **틀림**(카테고리가 정함) · 옥션 100**byte**(검색+프로모션 **합계**) · 롯데온 150은 byte 아님 · 스스는 길이 제한 **없음** · 옥션/G마켓 이미지 **14장** 누락 · 롯데온 10장은 **단품당** |
| **「매출」 칸의 뜻** | 지금 = 상품금액(**할인 전**). 정산은 할인 후 → 나란히 두면 오해(앞 세션이 실제로 오판) |
| **배송비 되채움** | 과거 2,072건 |
| **반품 딱지 747건** | 붙일지 |
| **옵션 잘리는 순서** | 96개 중 10개만 나감. 재고순 / 판매순 / 직접 |
| **C등급 중 넣을 것** | 필수구매옵션 · 저장 한도 · 별도 추가요금 · 상품 영상 |

---

## 10. 참고

| | |
|---|---|
| 시안 (2안 확정) | `_시안_임시\상품관리_탭재편_더망고대조_v10_3안.html` — 「0. 대조표」가 근거 |
| 더망고 | `docs\benchmark\themango\screens\06-상품관리.md` · `16-상품관리-보조화면-4종.md` |
| 사장님 엑셀 | `바탕 화면\■ 마켓별 상품등록 정보.xlsx` (Sheet2 = 25항목 × 6마켓) |
| 쿠팡 정산 엑셀 | `Downloads\MSF_PAYMENT_REVENUE_DETAIL-2026-08-12*.xlsx` (9개 · **중복 119행**) |
| 옵션관리 A안 | `다른세션_전달_옵션관리_사이드탭_A안_확정.md` |
| 메모리 | `project_prodmgmt_notion_edit_one_door` · `project_sidebar_rename_needs_migration` · `project_coupang_shipping_fee_not_deducted` |
