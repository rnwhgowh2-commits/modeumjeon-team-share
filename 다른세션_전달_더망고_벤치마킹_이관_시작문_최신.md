# 【세션 시작문 — 최신본 2026-08-12】 통합 대량등록

> 다음 세션에 **이 문서를 그대로 붙여** 시작하면 된다.
> 🔴 **옛 시작문(2026-08-06)을 쓰지 말 것** — 거기 적힌 「할 일 4단계」는 **이미 다 끝났다.**
>   그대로 붙이면 다음 세션이 끝난 일을 다시 한다.

---

## ■ 목적 (변함 없음)

**무엇을 만드나** — 우리 모음전 프로그램의 「대량등록」을, 우리 탭별 기능 + 더망고솔루션의
좋은 기능을 합쳐 **어디서도 볼 수 없는 대량등록 프로그램**으로 만든다. **사업화 대상.**

**왜 더망고를 파헤쳤나** — 벤치마킹이다. 부족한 건 개선하고 좋은 건 따라한다.
단 **「없다고 다 가져오지 않는다」** — 구조적으로 안 좋은 것도 많다.

**모음전 ↔ 대량등록 근본 차이 (이게 핵심)**

| | 모음전 | 대량등록 |
|---|---|---|
| 넣는 것 | 옵션별 URL 을 **사람이 하나씩** | **검색형 URL 한 줄** |
| 하는 일 | 소싱처끼리 비교 → 재고·최종매입가 결정 | 수십~수천 상품 자동 수집 → 정책 붙여 등록 |
| 소싱처 비교 | 한다 | **안 한다** |

예) 검색형 `https://www.musinsa.com/search/goods?keyword=나이키…`
    상품페이지 `https://www.musinsa.com/products/3976350`

**대량등록 5단계** — ①소싱처 크롤 ②정책 만들기 ③정책 적용 ④마켓 업로드 ⑤가격·재고 갱신

---

## ■ 🔴 지금 어디까지 왔나 (옛 시작문과 가장 크게 달라진 곳)

**설계는 끝났고, 구현이 이미 라이브에 올라가 있다.**

| 단계 | 상태 |
|---|---|
| 1~4 조사·판정 | ✅ 완료 (🟢18 🟡8 ⚪17 🔴10 🔵12) |
| 6 **통합 설계 초안** | ✅ 완료 — `10-통합대량등록-설계초안.md` |
| **7 구현** | 🟡 **일부 라이브** — 검색필터(대량등록 입구)가 **실제로 돈다** |
| 5 한 바퀴 돌려보기 | ⬜ **미완** (아래 「남은 일」 참조) |
| 8 더망고 자산 이관 | ⬜ 다른 세션 |

**이미 라이브인 것** (PR #904 · #925 · #949 · #975 · #981)
- 검색 주소 한 줄 → 상품 주소 수십~수천 개 수집 → 「상품 만들기」 → 크롤 대기 → 초안
- 필터 성적표(수집 → 상품 → 등록 → 매출) · 「찾은 주소 보기」
- 「뺄 옵션」 실작동 · 옵션이 전부 걸리면 초안을 안 만들고 사유를 말함
- 훑기 규칙을 **서버가 내려준다**(소싱처 추가에 확장 수정 0)

---

## ■ 🔴 소싱처 숫자가 **세 가지**다 (여기서 오해가 난다)

| 무엇 | 몇 곳 | 어디 |
|---|---|---|
| 사장님 확정 범위 | **11곳** | 29CM·ABCmart·FashionPlus·GrandStage·GSShop·lotteimall·LOTTEON·MUSINSA·SSFShop·SSG·theHyundai |
| **크롤러가 있는 곳** | **8곳** | musinsa·lotteon·lotteimall·hmall·ssf·ssg·lemouton·ss_lemouton |
| **검색필터 규칙이 있는 곳** | **7곳** | 위 8곳 − `ss_lemouton`(로그인 필요) |

### 🔴 확정하신 「신규 소싱처 5곳 전부」는 **아직 하나도 착수 전이다**

GSShop · ABCmart · GrandStage · 29CM · FashionPlus — **다섯 곳 다 크롤러가 없다.**
크롤러가 없는 곳은 검색필터에 **일부러 안 넣었다**(넣으면 주소만 모으고 조용히 0건이 된다).

→ **다음 세션이 판단할 것**: 이 5곳 어댑터를 언제 만들 것인가. 지금 7곳 위에서 나머지를
  다지는 게 먼저인가, 5곳을 붙여 범위를 채우는 게 먼저인가.

---

## ■ 🔴 절대 잊으면 안 될 함정 (옛 시작문에 **없던 것** 3개 포함)

### ① 확장은 **세 곳**이 따로 논다 〔새로 추가〕
저장소 / 라이브 서버 / **크롬이 읽는 데스크톱 폴더** — 셋이 각각이다.
- 🔴 **화면 새로고침(F5)은 확장 본체를 안 바꾼다.** 화면 쪽만 새 판이라 표시는 새 판인데
  일하는 본체는 옛 판이다. 이걸로 두 번 헤맸다
- 폴더 이름이 `v0.7.63` 이라 **이름으로 판번호를 판단하면 안 된다**
- `chrome://extensions` 의 ↻ 는 **그 폴더를 다시 읽을 뿐** — 폴더가 옛 판이면 아무 일도 없다
- **처방**: 배포 후 `python scripts/sync_loaded_extension.py` 를 돌린 **다음에** ↻ 를 부탁한다.
  결과에 확장 판 번호가 실려 와 화면에 「확장 0.7.9x」로 보인다

### ② 「파라미터가 있다」 ≠ 「그게 먹는다」 〔새로 추가〕
무신사 `page=1` 과 `page=2` 의 응답이 **상품번호까지 완전히 같았다.**
→ **반드시 1쪽과 2쪽을 대조**한다. 안 하면 같은 쪽을 N번 긁고 「N장 봤다」고 거짓말한다.

### ③ 「결과 없는 검색어」로도 재라 〔새로 추가〕
0건 화면에도 추천 상품이 깔린다(**6곳 중 5곳**). 롯데아이몰은 **정상 검색어에서도 25건 전부 배너**였다.

### ④ 스크린샷 OCR 로 숫자·컬럼명 읽고 결론 내지 말 것
「휴지통」을 「유지율」로 오독해 *"78~100%가 죽는다"*는 **정반대 결론**을 내고 메모리에 저장했다.

### ⑤ 단방향 grep 은 **거짓 음성이 보장된다**
저쪽 단어로 우리 코드를 찾아 0건이면 「없음」 오판. **ORM 테이블 132개 명부에서 시작**해 양방향으로.

### ⑥ 모음전 탭 + 대량등록 탭 **둘 다** 본다 — 기능이 두 곳에 퍼져 있다

### ⑦ 「없다」 판정에는 **파일:줄 근거**를 단다 (`grep 0건`은 근거가 아니다)

---

## ■ 🔴 재구현 금지 — 이미 있는 것

옛 시작문의 명부(설계 §7)에 **아래 4건을 더한다.**

| 하려던 것 | 이미 있다 |
|---|---|
| 검색 주소 한 줄 → 상품 다건 수집 | `SearchFilter`·`SearchFilterItem` + `/api/crawl/due-listings` |
| 필터 성적표(수집→상품→등록→매출) | `bulk/search_filters.py::filter_scorecard` |
| 「찾은 주소 보기」(진행 상태 포함) | `bulk/search_filters.py::list_filter_items` |
| 훑기 규칙 | **서버가 내려준다** — `lemouton/sources/listing_discover.py` 가 **정본 한 곳** |

---

## ■ ⬜ 남은 일 (다음 세션이 이어받을 것)

1. 🔴 **「끝까지 한 바퀴」 미완** — 크롤 자동 실행이 꺼져 있어 롯데온 281건이 대기 중.
   **켜는 것은 사장님 결정**(PC·부하). 「자동화」 화면에서 켠다. 임의로 켜지 말 것
2. ⬜ **마켓에 진짜 1건 등록** — 허가받은 「테스트 1건」이 아직 안 쓰였다
3. ⬜ **신규 소싱처 5곳 어댑터** (위 참조)
4. ⬜ **스스르무통(`ss_lemouton`)** — 로그인 필요, 검색필터 규칙 없음
5. ⬜ 설계 §8 Phase 2·3 — 옵션 상한 규칙 / 재고 상한 캡 / 수기수정 잠금 배선 / `PolicyBinding`

---

## ■ 작업 환경

- 문서 워크트리 `C:\dev\_wt_mango` · 브랜치 `docs/themango-benchmark` (**머지 완료**)
- 코드 워크트리 `C:\dev\_wt_bulksafe`
- 설계 정본 `docs/benchmark/themango/10-통합대량등록-설계초안.md` (§12·§13 에 구현 실측)
- 입구 문서 `docs/benchmark/themango/README.md`

---

## ■ 지킬 규율

- 비교는 기능 **이름이 아니라 「그 기능이 푸는 문제(Job)」** 기준
- 게이트 5문 통과분만 채택. **「해법이 위험한 것」을 「안 함」에 넣으면 문제가 지워진다**
- **나는 비개발자다.** 평이한 한국어로, 화면 단어는 「」 안에 그대로
- 중간에 계속 묻지 말고 끝까지 진행. **결정이 꼭 필요한 것만 모아서** 물어볼 것
- 산출물은 파일로 쪼개 저장하고 커밋 (세션이 길어지면 잊는다)
- 더망고 실계정은 크롬에 로그인돼 있으니 붙어서 확인해도 된다.
  단 **저장·전송·삭제 단추는 누르기 전에 먼저 보여줄 것**
- **「검증됨」은 라이브 화면 숫자로 확인한 것만.** 코드만 보고 쓰지 않는다
