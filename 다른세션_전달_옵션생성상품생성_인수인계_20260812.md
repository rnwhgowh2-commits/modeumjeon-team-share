# 인수인계 — 「옵션생성 & 상품생성」 (2026-08-12)

> 이 문서만 읽으면 이어서 할 수 있게 썼습니다. 세션은 종료됐고 코드는 전부 라이브에 있습니다.
> 독립 검증이 필요하면 → `다른세션_검증프롬프트_옵션생성상품생성_20260812.md`

---

## 0. 지금 당장 알아야 할 것 3가지

1. **작업은 `origin/main` 기준 워크트리에서만.** `C:\dev\모음전 프로젝트` 본체는
   `feature/crawl-worker-queue` 라 수천 커밋 뒤처져 있고, **코드를 읽기만 해도 오보가 난다**
   (과거 실제 오보 3건). 이번에도 `_wt_optprod` 를 새로 떠서 작업했다.
2. **노션 13항목 전부 반영·배포 완료.** 근거 = 노션
   「무재고 모음전 솔루션 개발현황 > 프로그램 개선사항 > 옵션생성&상품생성」
   (page id `3a9cf482-7373-8014-b510-c3ef0b8a0418`). 설계서 = **저장소 루트**
   `docs/superpowers/specs/2026-08-01-옵션생성-상품생성-탭-design.md`
   (⚠️ `프로그램/_시스템/docs/` 아니다 — 여기서 한 번 헤맸다).
3. **미완 1건** — 데이터 코드 지도(크롤링 가이드) 갱신. 아래 §5 참조.

---

## 1. 사장님이 확정하신 것 (구현 중 바꾸지 말 것)

| 항목 | 확정 |
|---|---|
| 하위탭 위상 | **A1** — 윗단 「옵션 생성/상품 생성」, 아랫단 「직접/내마켓 불러오기」 |
| 만들기 창 | **A1** — ①종류 ②옵션축 **둘 다 세로 라디오**, 항목마다 쓰임새 한 줄 |
| 모델 축 순서 | **노션대로 모델·색상·사이즈** (내부 배선을 이름 기준으로 고침) |
| 격자에 모델 | **B2** — 격자 위 모델 탭 |
| 여러 묶음 고르기 | **C1** — 체크칸 상시, 2개 이상이면 아래 띠 |
| 축이 다른 묶음 | **막는다** |
| 여러 묶음 합칠 때 옵션 | **전부 담는다** (겹치면 첫 것만 + 알림) |
| 파생 만들기 | **2개 이상** 골랐을 때만 |
| 모델명 | **별도 칸 안 만듦** — 모델 축 있으면 그 값, 없으면 매트릭스 이름 |
| 정책 바로가기 | **만든 상품 줄**에. 정책 있으면 보기 / 없으면 붙이러 |
| 「파생」·「원본」 말 | **그대로 둔다** |
| 「단독_」 | 화면에서 **완전히 없앤다**(속 열쇠는 그대로) |

---

## 2. 🔴 다음 세션이 반드시 알아야 할 함정

### (1) 색상·사이즈 칸은 「축 이름」으로 채운다 — 위치 아님
`lemouton/sourcing/axis_slot.py` **한 곳**이 규칙의 집이다.
예전엔 `color_code=축값[0]`, `size_code=축값[1]` 이라, 노션대로 모델을 1축에 두면
**색상 칸에 모델명**이 들어갔다. 그 칸을 마켓 전송·재고·마진이 수백 곳에서 읽는다.
· `storage_slots` = 옛 칸 채우기(이름 먼저, 나머지는 남은 자리 — 비우면 격자가 뭉개진다)
· `semantic_slots` = 소싱처 짝 찾기(**모델은 언제나 None** — 소싱처가 안 준다)
· 이름을 못 알아보면 **오늘 그대로 위치 폴백**(기존 데이터 불변이 첫 의무)
· 쓰는 곳: `option_service.py`(생성·이름바꾸기 둘 다) · `webapp/routes/bundles.py`(축 맞추기)
배포 전 라이브 184개 전수 대조 → **자리가 달라지는 것 0건**.

### (2) 재고 이력은 옵션과 정식으로 안 묶여 있다
`inventory_txs.option_canonical_sku` 는 **FK 가 아니라 그냥 문자열 칸**이다.
옵션을 지우면 이력이 남고, `gen_sku` 가 **같은 SKU 를 다시 발급**하면
**없던 재고가 되살아난다.** → 묶음 삭제 시 `_purge_option_traces` 가 같이 치운다.

### (3) `recalc_stock_total` 은 계산만 하고 저장은 안 한다
부르기만 하면 캐시(`options.boxhero_stock_total`)가 안 맞는다. **돌려준 값을 직접 써야** 한다.

### (4) 색·여백 변수는 `.ds` 클래스 안에만 있다
`tokens.css` 가 `.ds` 로 묶어 놨다. 시안(순수 HTML)에 그 클래스가 없으면
**테두리·배경이 전부 0/투명**이 된다. 시안 만들 때 `<div class="ds">` 로 감싸라.

### (5) 시안에 Jinja 주석이 글자로 나온다
`{# … #}` 는 순수 HTML 에선 안 지워진다. 만들 때 정규식으로 제거하라.

### (6) 폭·넘침은 라이브 데이터 양으로 재라
로컬 3줄로 「가로 스크롤 없음」을 단정했다가 라이브(1481 > 1265)에서 그대로였다.
표가 넘칠 땐 화면을 넓히지 말고 **감싼 칸에 `min-width:0; overflow-x:auto`**.

### (7) heredoc 이 `\n` 을 깨뜨린다
Bash heredoc 으로 JS 문자열을 쓰면 `\n` 이 진짜 줄바꿈이 돼 문법 오류가 난다.
→ `String.fromCharCode(10)` 을 쓰거나 Write/Edit 도구로 파일을 직접 써라.
템플릿 JS 는 항상 `<script>` 만 뽑아 `node --check`.

### (8) `templates/matrix/detail.html` 에 NUL 문자가 있었다
`origin/main` 부터 2개. grep·편집 도구가 이 파일을 바이너리로 취급했다. 공백으로 교정함.

---

## 3. 무엇을 만들었나 (전부 라이브)

**노션 13항목** — 하위탭 2단 / 만들기 순서·라디오 / 축 프리셋 5종 / 브랜드 필수 /
옵션 표 브랜드·모델명 / 축 맞추기 빼앗아 오기 / 「재고관리 매핑」 제거 + 초기 재고 /
「단독_」 해소 / 사이드 넘침 / 옵션축 열 폭 / 만든 상품 되채움 / 정책 바로가기 /
여러 묶음 합치기 / 파생 2개부터

**추가 8건** — 격자 모델 탭 · 목록 50개 상한 해제 · 여러 묶음 한 번에 지우기 ·
옛 재고 치우는 창구 · 재고 유령 차단 · 거짓 안내 4곳 · NUL 교정 · 시험 격리 1건

**새 시험 37건** — `tests/sourcing/test_axis_slot.py`(15) `test_axis_takeover.py`(4)
`tests/catalog/test_optgen_axis_preset.py`(5) `tests/inventory/test_optgen_initial_stock.py`(4)
`test_optgen_reset_stock.py`(4) `tests/matrix/test_build_bundle_multi.py`(5)

**스키마 변경 0.** 이 저장소엔 Alembic 이 없고 경량 마이그레이션은 실패를 조용히 삼킨다.

---

## 4. 사장님 데이터에 한 일

- **아무것도 안 지웠다.** 옵션·묶음 88개 그대로.
- **옛 재고만 0으로** — 빔즈 「지갑」 6색 142개(화이트10·오렌지10·블랙100·브라운10·네이비10·그린2).
  옵션·묶음은 살아 있어 **재고만 새로 넣으면 된다**.
- 백업: `다운로드/지우기전_백업_옵션묶음88개_20260812.tsv` (묶음 88 · 옵션 85 · 재고 142)

---

## 5. 🔴 미완 — 데이터 코드 지도 갱신

사장님이 「데이터코드지도에 업데이트할 내용있으면 진행해줘」라고 하셨는데
검증 프롬프트 작성이 먼저라 **멈춘 상태**다. 절차는 `.claude/skills/update-data-code-map`.

**기준 시각 (조사 완료)**
- 판매처 지도 `webapp/data/marketplace_api_map.json` → 2026-08-12 12:35 (`c929db08`)
- 소싱처 정본 `docs/크롤링-가이드.md` → 2026-08-07 13:04 (`b57766a9`)

**이번 작업에서 지도에 반영할 후보 (소싱처 갈래 B)**
- `lemouton/sourcing/axis_slot.py` **신규** — 축 이름 기준 색상/사이즈 배정 (§0 4층 「저장」)
- `lemouton/sourcing/option_service.py` — `color_code`/`size_code` 채우는 규칙 변경
- `lemouton/sourcing/axis_alias.py` — `users_of()` 추가 · `AliasConflict.holder`
- `webapp/routes/bundles.py:2259·2344~` — 축 맞추기 슬롯 판정을 이름 기준으로
- **§5 에러 이력 필수 3건** (HARD-RULE 4):
  ① 축 자리를 위치로 정해 모델을 1축에 두면 색상 칸이 오염되던 것
  ② 축 맞춤 사전이 소싱처 전역이라 매트릭스를 지워도 유령이 남아 재매칭이 막히던 것
  ③ 재고 이력이 옵션과 FK 로 안 묶여 유령 재고가 되살아날 수 있던 것

**판매처 지도**는 이번 작업이 마켓 API 를 안 건드려 **반영할 것이 없을 가능성이 높다** —
다만 스킬 규칙대로 **전수 스캔 후** 판정할 것(안 보고 「없다」 하지 말 것).

---

## 6. 남은 판단거리 (사장님 결정 대기 아님 — 참고)

- 「상품 생성」 탭 목록에는 창고 물건 숨김이 **안 들어갔다**(그런 줄이 1개뿐이라 그냥 둠)
- 같은 날 다른 세션이 `92fa0129 옵션 묶음 지우기` 를 올렸다 — 이번 `5ba2dfbe` 와
  **두 벌이 됐는지** 확인 필요(한쪽만 재고 이력을 치우면 유령이 생긴다)
- 조립대의 「담은 옵션」은 **(색상,사이즈) 대조로 되짚은 추정**이다. 정확히 남기려면
  `BundleMatrixLink` 에 담은 SKU 를 기록하는 칸이 필요(스키마 변경이라 미룸)
