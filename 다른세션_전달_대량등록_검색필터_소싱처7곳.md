# 대량등록 검색필터 — 소싱처 7곳 인계문 (2026-08-13 갱신)

> **이 문서 하나로 이어받을 수 있게** 사실만 적었다.
> 모든 숫자는 **라이브 화면에서 읽은 값**이거나 실측이다. 추측은 「모름」이라 적었다.
> 🔴 앞 세션이 적은 **함정 3개는 이제 다 해결됐다** — §5 에 「해결됨」으로 표시했다.

---

## 0. 한 줄 요약

검색 주소 한 줄 → 상품 주소 수천 개를 캐는 「대량등록」 입구.
**7곳 전부 걷힌다.** 지금 라이브에 **26,127개**가 모여 있고, 3곳은 사장님 값과 **일치**했다.
남은 일은 **끝까지 채우기**(H몰·아이몰·SSG)와 **상품→마켓 한 바퀴**다.

---

## 1. 지금 당장 할 일 (순서대로)

### ① PR #1037 검사 통과 → 머지 → 배포 확인
```bash
gh pr view 1037 --json state,mergeStateStatus,statusCheckRollup
```
「훑기 잠금이 스스로 풀린다」 — 이게 없으면 **수집이 통째로 멈춘 채 아무 말도 없다**(실제로 겪음).

### ② 확장 폴더는 **이미 저장소와 연결**돼 있다 — 맞출 것이 없다
```bash
cd "C:/dev/_wt_bulksafe/프로그램/_시스템"
PYTHONIOENCODING=utf-8 python scripts/sync_loaded_extension.py
# → ✅ 연결돼 있습니다 — 저장소와 같은 폴더입니다
```
🎉 **2026-08-13 부터 복사 단계가 없다.** 코드를 고치면 `chrome://extensions` 의 **↻ 만** 누르면 반영된다.
자세한 것은 §5-① .

### ③ 라이브 수집을 이어 돌린다
```bash
# 지금 상태 보기 (인증 없이 됨)
curl -s "https://www.mou-m.com/bulk/api/search-filters" | python -c "
import sys,json
rows=[f for f in json.load(sys.stdin)['filters'] if f['name'].startswith('검증_')]
for f in sorted(rows,key=lambda x:-x['found_total']):
    print(f\"id={f['id']:<3} {f['found_total']:>7,}  {'더있음' if f['last_capped'] else '끝남':<6} next={f.get('next_page_from') or '-'}\")
print('합계', f\"{sum(f['found_total'] for f in rows):,}\")"

# 멈춰 있으면 다시 걸기
for id in 13 14 15 16 17 18 19 20 21; do
  curl -s -X POST "https://www.mou-m.com/bulk/api/search-filters/$id/run" >/dev/null
done
```
★ **원래는 서버가 스스로 이어 걷는다**(PR#1028). 손으로 걸 일은 확장이 잠겼다 풀린 직후뿐이다.

### ④ 남은 것 (§4 참조)
- H몰 13,947 → **16,668** 채우기(못 걸은 쪽 되찾기가 배포됐으니 회차를 돌리면 메워진다)
- 아이몰 「나이키 신발 화이트」(id=21) 첫 수집 — **6,427개 목표**
- SSG 1,011 → 사장님 값 26쪽과 대조
- **크롤 대기 → 상품(초안) → 마켓 1건 등록** 한 바퀴

---

## 2. 소싱처별 규칙 (코드 그대로 — 2026-08-13)

`MAX_PAGES = 60` (한 회차 상한. 소싱처 보호선이라 **없애지 않는다** — 대신 이어 걷는다)

| 소싱처 | 넘기는 방식 | 이어걷기 | 「결과 없음」 글귀 |
|---|---|---|---|
| musinsa | **다음쪽 주소**(`nextPageUrl`) | ❌ 한 회차에 끝까지 | 검색 결과가 없습니다 |
| lotteon | 「다음」 단추 `a.srchPaginationNext` | ✅ 눌러서 건너뜀 | 검색결과가 없습니다 |
| lotteimall | 「다음」 단추 **`.wrap_page a.on + a`** | ✅ 눌러서 건너뜀 | 검색된 상품이 없습니다 |
| hmall | 주소 `page` + **받은 글 읽기** | ✅ | 검색결과가 없습니다 |
| ssg | 주소 `page` + **받은 글 읽기** | ✅ | (모름 — 비워 둠) |
| lemouton | 주소 `page` | ✅ | 검색결과가 없습니다 |
| ssf | 주소 `currentPage` (**카테고리만**) | ✅ 카테고리 / ❌ 검색 | — |

- 정본 = `프로그램/_시스템/lemouton/sources/listing_discover.py`
- 확장 = `프로그램/_시스템/extension/moum-crawler/background.js`
- 규칙 전달 = `webapp/routes/api.py` 의 `/api/crawl/due-listings`
- 결과 접수 = `webapp/routes/api.py` 의 `/api/crawl/listing-result`
- 화면 = `webapp/templates/bulk/partials/_collect.html`

### 🔴 소싱처 숫자가 **세 가지**다 (오해 주의)
| 무엇 | 몇 곳 |
|---|---|
| 사장님 확정 범위 | **11곳** |
| 크롤러가 있는 곳 | **8곳** |
| 검색필터 규칙이 있는 곳 | **7곳** (8곳 − `ss_lemouton` 로그인 필요) |

**확정하신 신규 5곳(GSShop·ABCmart·GrandStage·29CM·FashionPlus)은 크롤러가 하나도 없어 아직 착수 전이다.**
크롤러 없는 곳은 일부러 안 넣었다 — 넣으면 주소만 모으고 **조용히 0건**이 된다.

---

## 3. 라이브 대조표 (2026-08-13 기준, 확장 0.8.0x)

| 소싱처 | 사장님 값 | 걷은 수 | 판정 |
|---|---|---|---|
| **무신사** | **1,218** | **1,218** | 🎯 **정확히 일치·끝남** |
| **롯데온** | 60개×35쪽 = 2,100 | **2,131** | 🎯 **넘김·끝남** |
| **SSG** | 26쪽 | 1,011 | ✅ 0 → 1,011 (진행 중) |
| **현대H몰** | **16,668**(463쪽×36) | 13,947 | 84% — 못 걸은 쪽 되찾기로 메우는 중 |
| 롯데아이몰 | 46,009 → **화이트 6,427 로 전환** | 7,228(옛 검색어) / 0(화이트) | 진행 중 |
| SSF 카테고리 | — | 453 | 8SECONDS 티셔츠 |
| SSF 검색 | 10,604 | 69 | ⚠️ 검색 주소는 **첫 쪽만**(규칙) |
| 르무통 | — | 70 | 견줄 값 없음 |
| **합계** | | **26,127** | |

### 🔴 사장님 값과 우리 값이 다를 때 — 어느 쪽이 맞나
- **무신사**: 우리가 맞았다. 처음 1,204 로 봤던 건 **내 브라우저 추적이 첫 쪽에서 13개를 놓친 것**이고, 확장은 1,218 을 다 걷고 있었다.
  ★ **내가 손으로 잰 값이 프로그램 값과 다르면 내 잼도 의심하라.**
- **H몰**: 우리가 부족했다. 표본 30쪽을 손으로 훑어 **겹침 0**을 확인 → 「그것뿐」이 아니라 **못 걷은 것**.

### 검증하는 법
```bash
# 소싱처가 스스로 말하는 총수 재기 (서버에서 바로)
curl -s -A "Mozilla/5.0 ... Chrome/120" "<검색주소>" | python -c "
import sys,re; h=sys.stdin.read()
for k in ['totalCount','totalPages']:
    m=re.search(r'\"'+k+r'\"\s*:\s*(\d+)',h)
    if m: print(k,'=',m.group(1))"
```
🔴 **끝나면 시험용 필터를 지운다** — `DELETE /bulk/api/search-filters/<id>` (지금은 **증거로 남겨 뒀다**)

---

## 4. 완료 목록 (전부 머지 — PR 번호로 추적)

### 앞 세션분 (2026-08-08)
- #925 소싱처 1곳→6곳 · 훑기 규칙을 **서버가 내려준다** · 「뺄 옵션」 실작동 · 필터 성적표
- #949 「결과 0건」 가짜 상품 차단(6곳 중 5곳) · 단추로 여러 장 걷기 · 실패 사유 보존
- #975 무신사 `page=` 무시 → `nextPageUrl` · SSF 주소 종류별 차이 · H몰은 주소로 넘어감 · MAX_PAGES 60

### 이번 세션 (2026-08-12~13) — **PR 17건, #990 만 중복으로 닫음**

| PR | 무엇을 고쳤나 |
|---|---|
| #981 | H몰은 **받은 글**에서 읽는다 · 확장이 `ext_version` 을 보낸다 · 0건 진단 |
| #984 | 🔴 **무신사 47 의 진범** — `/\\\\//g` 는 정규식이 아니라 `g` 로 나누기(ReferenceError) · 시간 초과인데 「끝남」 · 시킨 만큼만 보고 「끝남」 · 확장 맞추기 도구 2건 |
| #998 | 0건 진단이 **「받은 글에는 있나」**까지 답한다 |
| #1005 | **SSG 도 받은 글에서 읽는다**(화면 0개 / 받은 글 57개) |
| #1011 | **끝까지 걷는다** — 시간 초과 시 수확 손실 차단 + **이어서 걷기**(`next_page_from`) |
| #1023 | **아이몰이 아홉 쪽씩 건너뛰던 것**(「다음」이 「다음 10쪽 묶음」이었다) |
| #1025 | 단추 소싱처도 **눌러서 건너뛰어** 이어 걷는다(`click_skip`) |
| #1028 | **서버가 스스로 이어 걷는다** — 사람이 456번 안 눌러도 된다 |
| #1029 | **누른 뒤 바뀔 때까지 지켜본다** + **↻ 만으로 반영**(폴더를 저장소와 연결) |
| #1031 | 건너뛸 쪽도 **시간 예산에 넣는다** |
| #1034 | **못 걸은 쪽을 기억했다가 다시 건다**(H몰 16% 결손) |
| #1037 | **훑기 잠금이 스스로 풀린다** ⬅ **아직 열려 있음** |
| 문서 | #982 · #985 · #987 · #1006 · #1017 |

---

## 5. 🔴 함정 (앞 세션 3개는 **다 해결**, 새 것 6개)

### ① 확장 세 곳 따로 놀기 → 🎉 **해결됨**
크롬이 읽는 폴더를 **저장소를 가리키는 연결(junction)** 로 바꿨다. 사본이 아니라 **같은 폴더**다.
```
C:\Users\seung\Desktop\moum-crawler-v0.7.63   ──junction──▶  …\extension\moum-crawler
```
- 「배포 → 폴더 맞추기 → ↻」 삼단계가 **「↻」 한 단계**가 됐다
- 🔴 윈도우 junction 은 `os.path.islink` 가 **False** 다 → `st_file_attributes` 로 본다
- ⚠️ 연결 대상이 `_wt_bulksafe` 워크트리다 — **브랜치를 바꾸면 확장 내용도 바뀐다**
- 바꾸기 전 폴더는 `..._backup_20260813_171633` 로 백업

**일하는 본체 판을 바로 묻는 법** (화면 표시는 화면 쪽 판이라 못 믿는다):
```js
await new Promise((res)=>{
  const reqId='v'+Math.random().toString(36).slice(2);
  const on=(ev)=>{const d=ev.data; if(!d||d.__moum!=='ext'||d.reqId!==reqId)return;
    window.removeEventListener('message',on); res((d.resp&&d.resp.version)||'응답없음');};
  window.addEventListener('message',on);
  window.postMessage({__moum:'page',type:'ping',payload:{},reqId},'*');
  setTimeout(()=>res('응답없음(3초)'),3000);
});
```
★ 확장을 다시 불러오면 **열려 있던 화면의 연결이 끊긴다** — 화면을 새로 열고 물어야 한다.

### ② 「파라미터가 있다」 ≠ 「그게 먹는다」 → 여전히 유효
무신사 `page=1`·`page=2` 응답이 **상품번호까지 같았다.** 1쪽·2쪽을 **반드시 대조**한다.

### ③ 「결과 없는 검색어」로도 재라 → 여전히 유효
0건 화면에도 추천 상품이 깔린다(6곳 중 5곳). 아이몰은 **정상 검색어에서도 25건 전부 배너**였다.

### ④ 🔴🔴 **「끝남」이 네 가지로 거짓말한다**
| 겉으로 | 실제 | 고침 |
|---|---|---|
| 끝남 | 다음 쪽 주소를 **아예 못 찾음** | #984 |
| 끝남 | **시간 초과**로 멈춤 | #984 |
| 끝남 | **시킨 만큼만** 보고 멈춤 | #984 |
| **더 있음** | **다 걷었는데** 더 있다고 함 | #1011 |
★★ **「덜 걷고 끝났다」와 「다 걷고 더 있다」는 둘 다 거짓말이다.**

### ⑤ 🔴 **「다음」이라 적혀 있다고 「다음 쪽」이 아니다**
아이몰의 「다음」은 **「다음 10쪽 묶음」**이었다(1쪽 → 11쪽). 60번 눌러도 705개뿐이었다.
★ 단추 글자를 믿지 말고 **눌러서 쪽 번호가 몇으로 바뀌는지** 봐라.

### ⑥ 🔴 **「기다린다」와 「바뀐 것을 확인한다」는 다르다**
누른 뒤 고정 1.2초를 기다렸더니 아이몰이 아직 안 그려서 **같은 쪽을 읽고 첫 쪽에서 멈췄다.**
내가 손으로 잴 때 3.5초 기다린 것이 **우연히 넉넉했을 뿐**이었다. → 바뀔 때까지 지켜본다.

### ⑦ 🔴🔴 **「한 번에 하나」 잠금에는 반드시 시한이 있어야 한다**
죽은 회차가 `_listingBusy` 를 쥔 채 **수집이 통째로 멈췄다.** 확장은 살아서 다른 청에는 답하는데
훑기만 안 돌았다. **아무 말도 없이** 멈춘다. → 30분 시한(#1037).

### ⑧ 🔴 **시험이 내 PC 전제를 깔면 배포가 막힌다**
git 이력 깊이 2건을 요구했더니 **CI 얕은 클론(1건)**에서 실패 → 8,474건 통과 중 2건으로 **배포 skipped**.
★ **PR 검사 통과 ≠ main 통과**(체크아웃 방식이 다르다). 머지 뒤 main 런을 **끝까지** 본다.

### ⑨ 🔴 **내가 못 박은 시험이 다음 날 발목을 잡는다**
이번 세션에만 **네 번** 겪었다 — 「ssg 는 화면에서 읽는다」·「단추 소싱처는 이어걷기 불가」·
「실패 사유가 있으면 멈춘다」·「origin/main 글자가 나온다」.
★ 목록을 손으로 박을 때는 **「무엇을 근거로 넣었나」를 함께 적어라.**
★ **「구조상 안 된다」는 판정도 낡는다** — 「그 수단이 없다」와 「내가 아직 못 찾았다」는 다르다.

### ⑩ 병합 뒤 **저장소 전체**에서 충돌 표시를 훑어라
```bash
grep -rn "^<<<<<<< \|^>>>>>>> " 프로그램/_시스템 | grep -v "\.bak"
```
`git add -A` 가 `<<<<<<<` 를 **에러 없이 커밋**한다.

---

## 6. 아직 못 한 것 / 모르는 것 (정직하게)

- [ ] **H몰 13,947 → 16,668** — 못 걸은 쪽 되찾기가 배포됐으니 회차를 돌리면 메워진다
- [ ] **아이몰 화이트(id=21) 첫 수집** — 6,427개 목표. 아직 0
- [ ] **SSG 1,011 → 사장님 26쪽과 대조** — 26쪽×60 = 1,560 쯤이어야 맞다
- [ ] 🔴 **끝까지 한 바퀴 미완** — 크롤 자동 실행은 **켜 뒀다**(사장님 허락). 걷은 주소가
      상품(초안)이 되는지, **마켓에 진짜 1건 등록**까지는 아직 안 봤다
- [ ] **SSF 검색 10,604** — 검색 주소는 첫 쪽만이 규칙이다. 카테고리 주소로만 여러 쪽이 된다.
      8SECONDS **전체** 카테고리 주소를 받으면 10,604 에 맞출 수 있다
- [ ] **르무통** — 사장님 값이 없어 맞는지 모른다. 지금은 내가 임의로 고른 카테고리 주소다
- [ ] **신규 소싱처 5곳** — 크롤러가 하나도 없다(§2)
- [ ] **확장 카드의 「경고」 단추** — 무슨 경고인지 아직 못 봤다. 사장님께 여쭤볼 것
- [ ] 라이브 `검증_*` 필터 9개 — **증거로 남겨 뒀다**. 정리할지 사장님 결정

---

## 7. 작업 환경

- 코드 워크트리 `C:\dev\_wt_bulksafe` — **브랜치를 바꾸면 확장 내용도 바뀐다**(§5-①)
- 문서 워크트리 `C:\dev\_wt_mango` (`docs/themango-benchmark`) — 미머지 0
- 설계 정본 `docs/benchmark/themango/10-통합대량등록-설계초안.md` (§12·§13)
- 세션 시작문 `다른세션_전달_더망고_벤치마킹_이관_시작문_최신.md`

---

## 8. 사장님(사용자) 응대 규칙

- **비개발자다.** 평이한 한국어, 화면에 보이는 말은 「」 안에 **그대로**
- 마무리는 **`/conversation-wrapup` 형식** — ■대화 내용 정리 / ■사용자 결정 사항 2섹션,
  개조식·비유 금지, 맨 끝에 `※ 전체 체크리스트가 필요하신가요?` 한 줄
- **중간에 자꾸 묻지 말고 끝까지 진행**한다. 꼭 필요한 결정만 모아서 묻는다
- 「검증됨」은 **라이브 화면 숫자로 확인한 것만**. 코드만 보고 쓰지 않는다
- 🔴 **틀린 것은 틀렸다고 말한다** — 이번 세션에 내 진단 3건을 스스로 뒤집었다
