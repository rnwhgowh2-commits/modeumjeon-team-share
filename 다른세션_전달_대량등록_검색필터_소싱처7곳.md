# 대량등록 검색필터 — 소싱처 7곳 인계문 (2026-08-08)

> 앞 세션이 만든 인계문. **이 문서 하나로 이어받을 수 있게** 사실만 적었다.
> 모든 숫자는 실측이거나 라이브 확인값이다. 추측은 「모름」이라 적었다.

---

## 0. 지금 당장 할 일 (우선순위)

> ✅ 2026-08-12 갱신 — 1~5번은 **전부 끝났다.** 아래는 그 다음이다.

1. ~~PR #981 머지 → 배포~~ ✅ **완료** (머지·배포 성공)
2. ~~로드 확장 폴더 맞추기~~ ✅ **완료** (0.7.92 → 0.7.94)
3. ~~↻ 요청~~ ✅ **불필요했다** — 크롬이 새 파일을 이미 읽었다.
   **판정은 서비스워커에 직접 물어서** 했다(아래 §3-끝 「본체 판 확인법」)
4. ~~소싱처 7곳 라이브 수집~~ ✅ **7곳 전부 완료** — §3 대조표
5. ~~미검증 3곳 판정~~ ✅ **완료** (H몰 해결 / 무신사 원인 확정 / SSG 진단 확보)

### ⬜ 다음에 할 일

1. **PR #984 머지 → 배포 → 폴더 맞추기 → 확장 ↻**
   (🔴 이번엔 ↻ 가 **꼭 필요하다** — 「한 번에 하나」 잠금이 걸려 있어
    확장을 다시 불러와야 풀린다)
2. **무신사 재측정** — 47 → 1,200 대가 나와야 맞다
3. **SSG 링크 모양 확인** — 새 진단이 「많은 모양 …」을 함께 알려준다 →
   그 모양으로 `_PRODUCT_LINK['ssg']` 와 `_DOM_SELECT['ssg']` 를 고친다
4. **롯데온·아이몰** — 쪽 수를 나눠(예: 1~10, 11~20) 여러 번 돌려 시간 초과 회피
5. 르무통 70개가 맞는지 확인(10쪽에 70개 = 쪽당 7개, 적어 보인다)

---

## 1. 이 작업이 무엇인가

「대량등록」 화면의 **검색필터** — 소싱처 검색 결과 주소 한 줄을 넣으면 상품 주소
수십~수천 개를 캐 와서 상품(초안)으로 만드는 입구.

```
검색 주소 1줄 → [지금 수집] → 상품 주소 N개 → [상품 만들기] → 크롤 대기 → 초안 → 마켓
```

- 훑는 일은 **로컬 PC 크롬 확장**이 한다(「크롤 = 로컬 PC」 원칙). 서버는 **규칙만** 준다.
- 규칙 정본 = `프로그램/_시스템/lemouton/sources/listing_discover.py`
- 확장 = `프로그램/_시스템/extension/moum-crawler/background.js`
- 화면 = `프로그램/_시스템/webapp/templates/bulk/partials/_collect.html`
- 규칙 전달 = `webapp/routes/api.py` 의 `/api/crawl/due-listings`
- 결과 접수 = `webapp/routes/api.py` 의 `/api/crawl/listing-result`

---

## 2. 소싱처별 규칙 (현재 코드 그대로 — 2026-08-08)

`MAX_PAGES = 60`

| 소싱처 | 주소 쪽 | 「다음」 단추 | 다음쪽 주소 | 받은글 읽기 | 「결과 없음」 글귀 |
|---|---|---|---|---|---|
| musinsa | — | — | **O** | — | 검색 결과가 없습니다 |
| lotteon | — | `a.srchPaginationNext` | — | — | 검색결과가 없습니다 |
| lotteimall | — | `a.next.ico` | — | — | 검색된 상품이 없습니다 |
| hmall | `page` | — | — | **O** | 검색결과가 없습니다 |
| lemouton | `page` | — | — | — | 검색결과가 없습니다 |
| ssf | `currentPage` | — | — | — | — |
| ssg | `page` | — | — | — | — |

- 주소 예외 `_NO_PAGE_PATH` = `{'ssf': '/search/result'}`
  → **SSF 검색 주소에는 쪽을 안 붙인다**(카테고리 목록에서만 먹는다)
- 크롤러가 있는 8곳 안에서만 규칙을 넣는다
  (`sourcing/crawlers/__init__.py::build_crawlers`). **ABC마트·GS샵·29CM 는 크롤러가 없어 넣지 않았다.**
- 아직 규칙 없는 크롤 가능 소싱처: **`ss_lemouton`**(스마트스토어 르무통, 로그인 필요)

---

## 3. 라이브 검증 대조표 — ✅ **7곳 전부 실측 완료 (2026-08-12, 확장 0.7.94)**

사장님이 마켓마다 직접 검색해 알려 주신 숫자와, 우리 프로그램이 실제로 걷은 수.
**모든 숫자는 라이브 화면에서 읽은 값이다.**

| 소싱처 | 사장님 값 | 시킨 쪽 | 우리가 걷은 수 | 판정 |
|---|---|---|---|---|
| **현대H몰** | 16,413 | 1~60 | **2,159** | 🎉 **해결** — 36 → 2,159 (36개×60쪽=2,160) |
| 르무통 | — | 1~10 | **70** | 🆕 첫 측정 |
| SSF | 10,604 | 1~3 | **61** | ✅ 예상대로 (검색 주소는 첫 쪽만) |
| 롯데아이몰 | 46,009 | 1~60 | **300** | ⚠️ 「훑는 중 시간 초과」 — 5쪽까진 확인됨 |
| 롯데온 | 60개×35쪽 | 1~60 | **289** | ⚠️ 「훑는 중 시간 초과」 — 5쪽까진 확인됨 |
| 무신사 | 1,218 | 1~60 | **47** | ❌ **원인 확정·고침(PR#984)** → 재측정 필요 |
| SSG | 26쪽 | 1~3 | **0** | ❌ **링크 모양이 다르다** (진단 확보) |

### 🔴 앞 세션의 진단 2건이 **틀렸다**

1. **「무신사 47 은 확장이 옛 판(0.7.88)이라서」 → 오진.**
   확장을 **0.7.94 로 맞춘 뒤에도 47 그대로**였다. 진짜 원인은 다음 쪽 주소를
   푸는 식이 **정규식이 아니었던 것**이다 — `/\\\\//g` 는 JS 가 세 번째 `/` 에서
   정규식을 닫아 버려 뒤의 `/g` 가 **정의 안 된 변수 `g` 로 나누기**가 되고
   `ReferenceError` 를 던졌다. 그게 `catch (e) {}` 에 삼켜져 `url` 이 늘 `null`
   → **쪽 넘김이 한 번도 시작되지 않았다.**
   🔴 **문법은 맞아 `node --check` 로는 영영 안 잡힌다.**
   ★ 손으로 같은 검색을 따라가니 **47 → 227**(3쪽 추가·쪽당 60개·다음 쪽 계속 있음).

2. **「H몰 36」은 PR#981 의 「받은 글에서 읽기」로 진짜 풀렸다** — 36 → **2,159**.

### 🔴 새로 드러난 「조용한 거짓말」 3건 (전부 PR#984 에서 고침)

| 무엇 | 화면엔 | 실제 |
|---|---|---|
| 롯데온·아이몰 | 「끝남」 | **「훑는 중 시간 초과」** — 사유는 적히는데 「더 있음」이 꺼져 있었다 |
| 현대H몰 | 「끝남」 | 2,159 / **16,413** — 시킨 만큼만 보고 멈춘 것 |
| 무신사 | 「끝남」 | 다음 쪽 주소를 **아예 못 찾은 것** |

셋 다 **사장님이 「이 검색엔 이만큼뿐」이라고 믿게 만든다.**

### 🔴 SSG — 이제 「무엇을 봤는지」를 안다

```
0건(나이키 신발 - 추천•인기 상품, SSG.COM · 링크 62 · 선택자 0)
```

페이지는 **열린다**. 링크도 **62개 있다**. 그런데 우리 선택자
(`a[href*="itemView.ssg"]`)에 걸린 것이 **0개**다. 즉 상품 링크 모양이 다르다.

🔴 SSG 는 브라우저 도구로 **열 수 없다**(정책 차단 — 앞 세션과 동일하게 확인).
→ PR#984 에서 진단에 **그 화면의 링크 모양 상위 5개**를 함께 담게 했다.
  다음 수집 결과를 보면 어느 모양이 상품인지 고를 수 있다.

### ⚠️ 「모두(60쪽)」는 단추로 넘기는 곳엔 무리다

롯데온·아이몰은 「다음」을 60번 눌러야 해서 **시간 초과**가 났고, 그러면
**그 쪽에서 걷은 것이 통째로 사라진다**(이번 새로 0). 5쪽은 확인된 값이다.
→ 이 두 곳은 **쪽 수를 나눠 여러 번** 돌리는 편이 확실하다.

### 검증하는 법 (그대로 따라 하면 된다)

라이브 `https://www.mou-m.com/bulk?tab=collect` 화면에서 브라우저 콘솔로:

```js
// 필터 만들고 「지금 수집」까지
const r = await fetch('/bulk/api/search-filters', {method:'POST',
  headers:{'Content-Type':'application/json'},
  body: JSON.stringify({source_key:'musinsa', name:'검증_무신사',
    listing_url:'https://www.musinsa.com/search/goods?keyword=%EB%82%98%EC%9D%B4%ED%82%A4+%EC%8B%A0%EB%B0%9C&keywordType=keyword&gf=A',
    page_from:1, page_to:20})});
const j = await r.json();
await fetch(`/bulk/api/search-filters/${j.filter.id}/run`, {method:'POST'});
// 1~3분 뒤 결과 보기
(await (await fetch('/bulk/api/search-filters')).json()).filters
  .filter(f=>f.name.startsWith('검증_'))
  .map(f=>({이름:f.name, 찾음:f.found_total, 못봄:f.last_error, 더있음:f.last_capped, 확장:f.last_ext_version}));
```

🔴 **끝나면 시험용 필터를 지운다** — `fetch('/bulk/api/search-filters/<id>',{method:'DELETE'})`

---

## 4. 완료 목록 (전부 라이브 배포됨 — PR 번호로 추적 가능)

### PR #925 (머지·배포 완료)
- [x] 소싱처 **1곳 → 6곳**(SSF·롯데온·롯데아이몰·현대H몰·르무통 추가)
- [x] 🔴 **훑기 규칙을 서버가 내려준다** — 확장에 `a[href*="/products/"]` 가 박혀 있어
      서버만 고치면 **에러 없이 0건**이었다. 이제 소싱처 추가에 확장 수정 0
- [x] 🔴 크롤러 없는 곳은 안 넣는다(시험으로 못박음)
- [x] **「뺄 옵션」이 실제로 걸러진다** — 저장·표시만 되고 아무 데서도 안 읽히던 칸
- [x] 🔴 옵션이 **전부 걸리면 초안을 안 만들고 사유를 말한다**(옵션 0개 상품 차단)
- [x] **필터 성적표** — 수집 → 상품 → 등록 → 매출(정산예정금 기준)
- [x] 🔴 금액 못 읽은 주문을 **0원으로 안 센다**(`sales_unknown_lines`)

### PR #949 (머지·배포 완료)
- [x] 🔴🔴 **「결과 0건」 화면의 가짜 상품 차단** — 6곳 중 5곳이 그랬다
      (무신사 19 · 롯데온 25 · 아이몰 25 · H몰 12 · 르무통 1 / SSF만 0)
- [x] 🔴 **롯데아이몰 규칙 교정** — 링크 25건이 전부 메뉴 속 배너였다 → `data-goods-no`
- [x] 🔴 「무한 스크롤」 오진 정정 → **단추로 넘긴다**(롯데온·아이몰)
- [x] **단추로 여러 장 걷기** — 롯데온 47→283 · 아이몰 60→300(5쪽)
- [x] 🔴 확장이 보낸 **실패 사유를 서버가 버리던 것** → `last_error`
- [x] 「끝남 / 못 봄 / 더 있음」 세 상태를 색으로 구분
- [x] **「찾은 주소 보기」** — 숫자를 누르면 목록 + 진행 상태(크롤 전/크롤됨/상품됨)
- [x] 🔴 훑는 중 **20초 심장박동**(MV3 서비스워커 30초 사망 방지)

### PR #975 (머지·배포 완료)
- [x] 🔴🔴 **무신사 `page=` 는 서버가 아예 무시** → 응답이 주는 `nextPageUrl` 을 따라간다
      (🔴 `hmacId` 서명이 있어 **주소를 조립하면 403**)
- [x] 🔴 **SSF 는 주소 종류에 따라 다르다** — 카테고리는 먹고 **검색은 안 먹는다**
- [x] 🔴 **현대H몰은 주소 `page=` 로 넘어간다**(「스크롤」이 아니었다)
- [x] `chrome.debugger` 진짜 휠 통로를 만들었다가 **걷어냄**(답이 아니었다)
- [x] MAX_PAGES 20 → **60**
- [x] **SSG 규칙 추가**(🔴 화면을 못 봐서 `empty_text` 는 비워 둠)

### PR #981 (**열려 있음 — 아직 머지 전**)
- [ ] 🔴 **H몰은 화면이 아니라 받은 글에서 읽는다**(`_HTML_SCAN`)
      근거: `page=3` 에서 화면 36개 / 받은 글 36개 **겹침 0**
- [ ] 🔴🔴 확장이 **자기 판 번호(`ext_version`)를 결과에 실어 보낸다** + 화면 표시
- [ ] 0건이면 **무엇을 봤는지**(화면 제목·링크 수·규칙에 걸린 수) 함께 보냄
- [ ] `scripts/sync_loaded_extension.py` — 로드 폴더 맞추기 한 줄 도구
- [ ] 옛 H몰 시험 정정(`test_H몰은_받은_글에서_고른다`)

---

## 5. 🔴🔴 반드시 알아야 할 함정 (다시 겪지 말 것)

### ① 확장은 **세 곳**이 따로 논다
| 층 | 어디 | 어떻게 바꾸나 |
|---|---|---|
| 저장소 | `프로그램/_시스템/extension/moum-crawler/` | 코드 수정 → PR → 머지 |
| 라이브 서버 | Fly.io | 배포 |
| **크롬이 읽는 폴더** | `C:\Users\seung\Desktop\moum-crawler-v0.7.63` | **손으로 복사(스크립트)** |

- 🔴 폴더 **이름이 `v0.7.63`** 이라 이름으로 판단하면 안 된다
- 🔴 **`chrome://extensions` 의 ↻ 는 그 폴더를 다시 읽을 뿐**이다. 폴더가 옛 판이면 아무 일도 없다
- 🔴 **화면 새로고침(F5)은 확장 본체를 안 바꾼다** — 화면 쪽(`content_mou.js`)만 새로 붙어
  `data-moum-ext` 는 새 판을 보여 주는데 **일하는 본체(서비스워커)는 옛 판**이다.
  → 「확장 0.7.92 맞음」을 근거로 코드를 의심하며 한참 헤맸다.
  → 이제 확장이 `ext_version` 을 보내니 **화면의 「확장 0.7.9x」 표시로 판정**한다

#### ★ 본체 판을 **바로** 묻는 법 (2026-08-12 추가 — 수집 안 돌려도 된다)

화면 표시(`data-moum-ext`)는 **화면 쪽 판**이라 못 믿는다. 서비스워커에 직접 묻는다.
`mou-m.com` 아무 화면에서 브라우저 콘솔에 붙여 넣으면 된다.

```js
await new Promise((res)=>{
  const reqId='ver_'+Math.random().toString(36).slice(2);
  const on=(ev)=>{ const d=ev.data;
    if(!d||d.__moum!=='ext'||d.reqId!==reqId) return;
    window.removeEventListener('message',on);
    res('일하는 본체 판 = '+((d.resp&&d.resp.version)||'응답 없음'));
  };
  window.addEventListener('message',on);
  window.postMessage({__moum:'page', type:'ping', payload:{}, reqId}, '*');
  setTimeout(()=>res('응답 없음(3초)'),3000);
});
```

★ 이걸로 「↻ 를 눌러 주세요」를 부탁하기 **전에** 이미 새 판인지 알 수 있다.
  2026-08-12 에는 폴더만 맞췄는데 본체가 이미 0.7.94 였다 — ↻ 가 필요 없었다.

### ② 「파라미터가 있다」 ≠ 「그게 먹는다」
무신사 `page=1` 과 `page=2` 의 응답이 **상품번호까지 완전히 동일**했다.
→ **1쪽과 2쪽을 대조해야** 안다. 안 하면 같은 쪽을 N번 긁고 「N장 봤다」고 거짓말한다.

### ③ 「스크롤로 보인다」 ≠ 「스크롤로만 된다」
H몰은 화면상 스크롤인데 **주소 `page=`** 로 넘어갔다. 화면 동작을 흉내 내기 전에 **주소부터 눌러 볼 것.**

### ④ 검색 규칙은 **「결과 없는 검색어」로도** 재라
0건 화면에도 추천 상품이 깔린다(6곳 중 5곳). **아이몰은 정상 검색어에서도 25건 전부 배너**였는데
**26이라는 숫자가 그럴듯해서** 못 알아챘다.

### ⑤ 병합 뒤 **저장소 전체**에서 충돌 표시를 훑어라
```bash
grep -rn "^<<<<<<< \|^>>>>>>> " 프로그램/_시스템 | grep -v "\.bak"
python -c "import pathlib,ast; [ast.parse(f.read_text(encoding='utf-8')) for f in pathlib.Path('프로그램/_시스템').rglob('*.py')]"
```
`git add -A` 가 `<<<<<<<` 를 **에러 없이 커밋**한다. 「내가 고친 파일만」 검사해서 4번째 파일을 놓쳤고 `SyntaxError` 가 올라갔다.

### ⑥ 다른 세션이 확장을 자주 올린다
머지 전 `git show origin/main:...manifest.json` 으로 판을 확인하고,
충돌 시 **양쪽 내용을 눈으로 보고** 고른다. `sync_loaded_extension.py` 는
덮기 전에 **origin/main 에 없는 줄**이 있으면 멈춘다.

---

## 6. 아직 못 한 것 / 모르는 것 (정직하게)

- [ ] **SSG** — 앱 브라우저·claude-in-chrome **둘 다 정책 차단**, 서버로 받아도 **403**.
      상품 링크 모양을 **내 눈으로 못 봤다**. `empty_text` 도 모름(비워 둠).
      → 확장이 사장님 크롬에서 대신 재 보게 하고 **첫 결과를 사장님 화면 숫자와 대조**해야 한다
- [ ] **스스르무통(`ss_lemouton`)** — 로그인 필요. 규칙 없음
- [ ] **SSF 카테고리 목록 주소** — 사장님께 받아 「8SECONDS 티셔츠 1,579개」로 재측정
- [ ] **(a) 끝까지 한 바퀴 미완** — 롯데온 **281건이 크롤 대기**에 들어가 있는데
      **크롤 자동 실행이 꺼져 있어** 상품이 안 늘어난다.
      🔴 **켜는 것은 사장님 결정**(PC·부하) — 임의로 켜지 말 것. 「자동화」 화면에서 켠다
- [ ] **마켓에 진짜 1건 등록**까지는 아직 안 해 봤다
- [ ] 무신사·H몰·SSG **재측정**(§3)
- [ ] 라이브에 남은 **`검증_*` 시험 필터 6개 정리**(id 13~18)

### 별도 작업으로 띄워 둔 것 (칩)
- `task_c4f726b4` — **「구성별 자동화 예외」가 저장만 되고 안 먹는다**
  (`ProductSet.auto_mode`·`manual_crawl_hours` 를 크롤 일정이 **전혀 안 읽는다**)
- `task_6fe28ae2` — `test_process_tab.py::test_상세_페이지가_열린다` 가
  **혼자 돌리면 통과, 전체 시험에서 실패**(다른 세션 작업)

---

## 7. 검증 상태 (증거)

- 내가 만든 시험 **88건 전부 통과** (아래 14개 파일)
  ```
  tests/sources/test_listing_discover_sources.py     tests/registration/test_listing_rule_delivery.py
  tests/sources/test_click_paging.py                 tests/registration/test_listing_result_message.py
  tests/sources/test_musinsa_next_page.py            tests/registration/test_listing_diag.py
  tests/sources/test_ssf_search_vs_category.py       tests/registration/test_option_exclude_words.py
  tests/sources/test_ssg_rule.py                     tests/registration/test_build_excludes_options.py
  tests/sources/test_wheel_scroll_sources.py         tests/registration/test_search_filter_scorecard.py
  tests/sources/test_html_scan_sources.py            tests/registration/test_search_filter_items_view.py
  ```
- `tests/sources/` + 리스팅 관련 registration = **672건 통과**(2026-08-08 마지막 실행)
- 전체 시험 8,402건 중 **1건 실패** — `test_process_tab.py::test_상세_페이지가_열린다`,
  **다른 세션 작업**이고 혼자 돌리면 통과(위 칩으로 넘김)

---

## 8. 작업 환경

- 워크트리: `C:\dev\_wt_bulksafe` · 브랜치 `feat/lone-url-crawl`
- 문서 워크트리: `C:\dev\_wt_mango` · 브랜치 `docs/themango-benchmark`
  🔴 **46커밋 미머지** — 설계 문서 §13-7~11(무신사·SSF·H몰 실측)이 아직 안 올라갔다. PR 필요
- 설계 문서: `docs/benchmark/themango/10-통합대량등록-설계초안.md` (§12·§13)
- 로컬 확인 서버: `.claude/launch.json` 의 `bulksafe`(포트 5127)

---

## 9. 사장님(사용자) 응대 규칙

- **비개발자다.** 평이한 한국어로, 화면에 보이는 말은 「」 안에 **그대로**
- 마무리는 **`/conversation-wrapup` 형식** — ■대화 내용 정리 / ■사용자 결정 사항 2섹션,
  개조식·쌍괄식·비유 금지, 맨 끝에 `※ 전체 체크리스트가 필요하신가요?` 한 줄
- **중간에 자꾸 묻지 말고 끝까지 진행**한다. 꼭 필요한 결정만 모아서 묻는다
- 「검증됨」은 **라이브 화면 숫자로 확인한 것만**. 코드만 보고 쓰지 않는다
- 크롤 자동 실행처럼 **사장님 PC·비용이 걸린 스위치는 대신 켜지 않는다**
