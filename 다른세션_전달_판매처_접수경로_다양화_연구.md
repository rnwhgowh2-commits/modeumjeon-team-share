# 다른 세션 전달 프롬프트 — 판매처 API "접수 경로" 다양화·신뢰도 랭킹 연구 (v2)

> 아래 **《프롬프트 시작》 ~ 《프롬프트 끝》 사이 전체를 복사**해서 새 Claude Code 세션에 붙여넣으세요.
> v2 = 2026-07-17 실측 갱신(ESM 공식 블로그 fetch 관통 발견·네이버 SPA/11번가 robots 확정 포함).

---

## 왜 이 연구가 필요한가 (사람용 요약)

- 판매처(마켓) 공식 API 문서를 프로그램 「데이터 코드 지도」에 "접수"(구조화 저장)해서, 새 기능 만들 때 F12 반복 노가다 없이 이 지도만 보고 구현한다.
- 그런데 **마켓마다 문서 접근 경로가 다르다.** 자동 fetch가 뚫리는 곳(쿠팡·ESM), 실제 크롬으로만 되는 곳(롯데온), 둘 다 막혀 코드/붙여넣기뿐인 곳(11번가·스스).
- 마켓이 수십 개로 늘면 **"이 마켓은 어느 경로로, 어떤 순서로 시도하면 가장 잘·신뢰도 높게 뚫리는지"**를 미리 알아야 검증이 빠르다.
- 목표 = **마켓 × 접속경로 매트릭스 + 신뢰도 랭킹 + "새 마켓 접수 시 시도 순서" 플레이북**.

---

## 《프롬프트 시작》

**SUPERPOWERS SKILL 작동.** brainstorming 스킬로 시작해서 아래 연구를 끝까지 자동 진행해줘. 코드 구현이 아니라 **판매처 API 문서 "접수 경로"를 마켓별로 실측·분류하는 조사 연구**다. 사용자에겐 결정이 필요한 지점(산출물 저장 위치)에서만 물어봐.

### 목표
판매처(오픈마켓) 공식 API 문서 접근 **경로를 다양화**하고 마켓별로 **신뢰도 높은 순서**로 분류한 매트릭스와, 새 마켓 검증 시 쓸 **플레이북**을 만든다.

### 배경 — 이전 세션(2026-07-17) 실측 결과 (시드. 재확인·확장 환영)

**접수 경로 도구상자 A~H (실측 기반):**
- **A. `fetch` MCP** (`mcp__fetch__fetch`) — WebFetch류. ★claude-in-chrome 안전차단을 우회함(다른 경로). **SSR(서버렌더) 사이트 관통**. 기법: `raw=true`로 HTML·`href` 추출→링크 열거, `start_index`로 이어읽기(페이지 길면 반복), 기본 markdown 모드는 표·본문 깔끔 변환. **약점: robots.txt `Disallow`면 차단 / 클라이언트렌더 SPA(`<app-root>`만 있고 본문 JS 렌더)는 빈 껍데기.**
- **B. `WebFetch` 툴** — A와 유사, robots 준수. 인증·리다이렉트에 약함.
- **C. `claude-in-chrome` MCP** (사용자 실제 크롬, 로그인 세션) — **SPA도 navigate+JS추출로 구동**. 롯데온 성공법=`?apiNo=N` 이동+1초 대기+테이블 JS추출(1 API/1콜; 다중배치는 SPA 렌더 전 추출로 실패). **약점: 일부 도메인 안전정책상 구동 거부**(쿠팡·스스·11번가). 탭ID가 세션 중 바뀌니 `tabs_context`로 재확인.
- **D. 우리 어댑터 코드 + `docs/markets/*.yaml`** — 우리가 짜서 **라이브 왕복검증한 엔드포인트**. **신뢰도 최상**(실제 호출 성공). 단 "우리가 쓰는 API"만 커버(전체 카탈로그 아님).
- **E. SPA 백엔드 JSON API 발견** — Angular/React 문서앱이 내부적으로 부르는 JSON 데이터 엔드포인트를 찾아 직접 타격. (앱 번들 `main.*.js` 분석·네트워크 패턴 추정. **네이버 스스 뚫는 핵심 미개척**.)
- **F. 기계판독 스펙 발견** — OpenAPI/Swagger JSON, `sitemap.xml`, `robots.txt` 힌트, Zendesk help-center API(`/api/v2/help_center/...`). (체계적 미시도.)
- **G. 사용자 복붙** — 최후 수단(사람이 브라우저에서 보고 붙여넣기).
- **H. 공식 블로그/외부 문서 사이트** — ★신규 발견: 마켓이 공식 문서를 **별도 블로그/도메인**에 두는 경우가 있다(ESM=Tistory `etapi.gmarket.com`). 이런 곳은 SSR이라 A(fetch)로 관통되며 카테고리 나열(`/category/...`)로 글 열거 가능. **단 공지·안내 글이 API 문서와 섞여 있어 [METHOD]+URL 유무로 API 스펙만 선별해야 함.**

**마켓별 실측 상태 (2026-07-17):**

| 마켓 | 뚫린 경로 | 막힌 경로 | 문서 계층(상위>하위>가로) | 접수 수준 |
|---|---|---|---|---|
| **쿠팡** | **A**(developers.coupang.com, Next.js SSR + 가이드는 Zendesk-body SSR) | C(안전차단) | 홈·시작하기·API문서·FAQ·공지 > (API문서)12패밀리 > 100엔드포인트 > [요약·상세·경로·요청·응답·오류] / (시작하기)18가이드 | **완전**(100 API + 8가이드; VOC사유코드만 XLSX첨부라 미인라인) |
| **롯데온** | **C**(api.lotteon.com, Nuxt SPA를 실크롬 구동) | A/B(10MB SPA 실패) | 좌측 11카테고리 > API > 상세 가로탭 | 109/115(판촉6=사이트 죽은페이지) |
| **옥션·G마켓(ESM)** | **A+H**(etapi.gmarket.com=Tistory 공식블로그, `/category`·`/pages/API-가이드`) | — | 상단메뉴 상품·주문·배송·클레임·정산·CS·서비스 > 각 API글 > [Description·Request·Response·사유코드] | 공식블로그 접수 진행(코드기반 14→공식 승격 중) |
| **스마트스토어** | D(코드, 운영핵심 21) + A는 소개페이지만 프리렌더 | **A**(apicenter.commerce.naver.com=Angular `<app-root>` SPA, per-API 빈껍데기·sitemap 없음), C(안전차단) | 좌측 상품·주문·정산·문의… (SPA) | 운영핵심만·롱테일 미접수 → **E(SPA JSON) 개척 필요** |
| **11번가** | D(코드·라이브검증 10) | **A**(openapi.11st.co.kr robots `Disallow: /`), C(안전차단) | 셀러REST 상품·가격재고·주문·클레임·정산 | 운영핵심만 → 대체경로 필요 |

### 과제 (이 순서로)
1. **경로 다양화 연구(개척):** 위 A~H를 각 마켓에 전부 실측. 특히 미개척:
   - **E — 네이버 스스**: `apicenter.commerce.naver.com` Angular 앱이 부르는 **문서 데이터 JSON 엔드포인트**를 찾아라(`main.*.js` 번들 분석, 네트워크 패턴, 공식 OpenAPI/GitHub 배포 여부). 되면 스스 전체 카탈로그가 열린다.
   - **11번가 대체**: robots 우회는 **금지**. 셀러오피스 로그인 문서(사용자 크롬 C경로)·공식 OpenAPI 배포·H(외부 문서 블로그) 존재 여부 조사.
   - **H 일반화**: 다른 마켓도 ESM처럼 별도 공식 블로그/도메인을 두는지(위메프·티몬·카카오·인터파크 등 신규 마켓 대비).
2. **신뢰도 랭킹 기준 정의:** 예 ①우리 라이브 왕복검증(최상) → ②공식문서 완전(요청·응답·필드·예시·오류 4종) → ③공식문서 부분 → ④코드 근거만 → ⑤추정(금지). 각 경로(A~H)가 산출하는 등급 매핑.
3. **마켓 × 경로 매트릭스:** 칸마다 성공/실패/제약(robots·SPA·인증·안전차단) + 산출 신뢰도 + 실측 근거(URL·응답 스니펫).
4. **플레이북:** "새 마켓 접수 시작 → 판별 순서". 예: ①`fetch raw`로 SSR 여부 판별(본문 있으면 A로 크롤) → ②`<app-root>`만 있으면 SPA → E(백엔드 JSON) 탐색 → ③robots 막히면 H(외부 블로그)·C(실크롬) → ④전부 막히면 D(코드)·G(붙여넣기). 각 분기 판별법 명시.
5. **산출물 저장 위치 제안:** 매트릭스·플레이북을 `webapp/data/marketplace_api_map.json`에 마켓별 `ingest_paths` 필드로 넣을지, 별도 `docs/markets/_접수경로_매트릭스.md`로 둘지 장단점과 함께 추천. **결정은 사용자에게 물어라.**

### 제약 (절대)
- **날조 금지.** 실제로 뚫어본 것만 "가능". 추정은 "미확인"으로 분리. (쿠팡 VOC코드처럼 첨부파일에만 있으면 "첨부 위치"만 기록하고 값 창작 금지.)
- **robots.txt·이용약관 준수.** Disallow 사이트 우회·안전차단 도메인 강제 우회 금지.
- **개인정보·인증정보 URL 노출 금지.** 로그인 필요 페이지는 사용자 크롬(C) 경로만.
- 스킬 우선순위: brainstorming(경로 발산) → 실측 → 매트릭스.

### 참고 파일 (이 저장소)
- 지도 데이터(단일 진실 원천): `프로그램/_시스템/webapp/data/marketplace_api_map.json` (현재 386 apis: 쿠팡100+가이드8·스스139·롯데115·11번가10·옥션7·G마켓7)
- 검증기: `프로그램/_시스템/webapp/marketplace_api_map.py`
- 마켓 프로파일(코드 근거): `docs/markets/{coupang,smartstore,eleven11,auction,gmarket,lotteon}.yaml`
- 접수 경로 실측 이력(메모리): `project_marketplace_api_map_sot`
- 화면: `/marketplace-guide/map` (인앱 「데이터 코드 지도」)

**brainstorming 스킬부터 켜고 위 5과제를 끝까지 자동 진행해. 결정 지점(산출물 저장 위치)에서만 사용자에게 물어봐.**

## 《프롬프트 끝》
