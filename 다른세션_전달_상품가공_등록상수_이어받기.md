# 상품가공 — 등록 상수 배선 이어받기 (2026-08-13)

아래 블록을 **통째로 복사해서** 새 세션에 붙여 넣으세요.

---

당신은 「모음전」 프로젝트를 이어받습니다. 앞 세션이 노션 「상품가공」 지시를 끝내고,
그 과정에서 발견한 **「저장만 되고 마켓에 안 나가던 값들」**을 잇는 중입니다.

## 0. 먼저 할 일 (순서 고정 · 건너뛰지 말 것)

1. `C:\dev\모음전 프로젝트\_wt_fields` 로 간다. 브랜치 `feature/listing-fields`.
   커밋 6개가 이미 있고 **작업 폴더는 깨끗하다**(미커밋 0).
2. `git fetch origin && git log --oneline HEAD..origin/main | wc -l` 로 main 이 얼마나
   앞섰는지 본다. 하루 40커밋씩 움직이는 저장소다 — **뭘 하든 시작 전에 `gh pr list` 로
   같은 일 하는 PR 이 없는지 확인**한다(중복 작업으로 통째로 버린 전례가 두 번 있다).
3. 마켓 API 를 건드리기 전 **`consult-market-map` 스킬을 반드시 발동**한다(프로젝트 규칙).
   앞 세션은 이미 통과했다 — 6마켓 브리핑 전수 정독 + 원천 분열 검사(624 API, 불일치 0).
4. 디자인을 건드리기 전 **`design-mockup` 스킬을 발동**한다(변경당, 대화당 아님).

## 1. 사장님이 정하신 것 (전부 확정 · 되묻지 말 것)

| 항목 | 확정 내용 |
|---|---|
| 과세구분 | 기본 **과세**. 「영세」는 **선택지에서 뺐다**(수출·외화획득용이라 해당 없고, 쿠팡·옥션·G마켓엔 보낼 칸 자체가 없다) |
| 상품상태 | **무조건 새상품**. 고를 것이 아니라 정해진 값이라 정책 항목에서 빼고 「정해져 나가는 값」 표로 옮겼다 |
| 판매기간 | **마켓마다 가장 긴 것으로 알아서**. 같은 이유로 정책에서 뺐다 |
| 자동 가격 조정 | **쿠팡만 해당** (`only: ['coupang']`) |
| 바코드 | 브랜드 공식이 **대부분 있다** → 기본은 공식. 없으면 자체 생성하되 **자체 생성임이 구분**돼야 함 |
| 자체 바코드 | **숫자만**(영문 금지). **어느 마켓에도 안 보낸다** — 라벨·재고 스캔 전용 |
| 마켓 끄면 | 화면은 **빗금**으로 흐리게. 단 「쿠팡 정책설정」 체크 줄은 **빗금 밖**(다시 켤 수 있어야). 입력·수정은 되지만 **마켓 전송은 금지** |

**시안 확정**: 1-C(빗금·체크줄 제외) / 2번 라디오 + 마켓별 가능 조건 / 3-B(정하는 곳끼리 묶기) / 4-D(켜기 스위치 두 줄)
시안 파일: `바탕화면 / 모음전 정책상세 시안 v2 확정안.html`

## 2. 이미 끝난 것 — 라이브 배포 완료

노션 원문 지시는 **전부 라이브에 올라가 있고 화면에서 눈으로 확인**했다.

```
■ 하위탭
  a.   상품 정책 적용 → 정책 적용            ✅ PR #966 라이브
  b-1) 가격 정책 → 옵션 맵핑 템플릿          ✅ PR #966 라이브 (이모지 💲→🔗)
  b-2) 기타 상위탭 아래로 옮기기             ✅ PR #966 라이브 (마이그레이션 _mig14)
  b-3) 가로탭 3개 중 가격 템플릿 삭제         ✅ PR #966 라이브 (화면에서만)
■ 정책생성
  a.   마켓 활성화 체크한 것만 가공          ✅ PR #966·971 라이브 (흐림+자물쇠+체크박스)
  b.   마켓별 필수 정보 정리·논의            ✅ PR #971 라이브 (엑셀 25항목 대조 → 5항목 추가)
       후속: 정책값이 실제로 마켓까지        ✅ PR #991 라이브 (배송비·반품비·원산지·미성년자)
       후속2: 남은 7개 값                   🔄 지금 하는 일 (아래 3절)
■ 정책적용
       직렬(순서) 느낌으로                   ✅ PR #966·991 라이브 (①② + 상품 고르기 전 잠듦)
```

**PR #966(08-12) · #971(08-12) · #991(08-13) 전부 MERGED.**
라이브 확인 완료 — `mou-m.com/policies/5?m=coupang` 에서 A1 표 13행 · 알약 11개 ·
배선 배지 10개(전송됨 8 · 일부만 2) 눈으로 봄.

🔴 **배포 함정**: 머지가 몰리면 **옛 커밋이 나중에 배포돼 라이브가 되돌아간다.**
실측 — 내 머지 06:14:05 → 그보다 이전 커밋 06:15:12(둘 다 초록불인데 라이브는 옛 코드).
**배포 초록불 ≠ 내 코드가 라이브.** 머지 뒤엔 반드시 **이번에 새로 생긴 표식**을 화면에서 확인할 것.

## 3. 지금 브랜치에서 끝낸 것 (커밋 6개 · 회귀 0)

브랜치 `feature/listing-fields` @ `_wt_fields`. **PR 아직 안 열었다.**

```
f67406f6 wip(등록상수): 초안 칸 5개 + 마이그레이션 + 정책 선택지 정리
0200b6cd feat(재고): 자체 바코드 생성기 — 숫자 13자리, 쿠팡엔 안 나간다
f5263bc6 feat(등록): 쿠팡 등록 상수 6개 배선 + 자동 가격 조정(쿠팡 전용) 항목
2a2ce8ab feat(등록): 스마트스토어 등록 상수 배선
ca07de6f fix(바코드): 자체 생성분은 어느 마켓에도 안 보낸다 (사장님 확정)
cb390b0f feat(정책): 정책값이 초안까지 오는 다리 — 과세구분·제조사
```

### 3-1. 초안(ProductDraft) 새 칸 5개 — 라이브 마이그레이션까지 등록됨
`tax_type`(기본 '과세') · `manufacturer` · `model_no` · `barcode` · `search_tags`
→ `lemouton/registration/models.py` + `shared/db.py` 의 `migrations` 리스트
🔴 전부 **비워 둘 수 있는 칸**. NOT NULL 로 만들면 옛 초안이 마이그레이션에서 터진다.

### 3-2. 자체 바코드 생성기 — `lemouton/inventory/barcode.py` (전엔 빈 파일이었다)
```
make_internal(n)  →  '2' + 11자리 + 체크디지트 = 숫자 13자리
                     예) 1 → 2000000000015 · 7 → 2000000000077
is_internal(v)    →  값만 보고 판별(2로 시작). 출처 칸을 따로 안 만든다
for_market(v,mk)  →  마켓별로 보낼지 결정. _SELF_OK = () (자체는 어디에도 안 보냄)
```
🔴 왜 `2` — 국제표준이 200~299 를 **매장 내부용**으로 비워 뒀다. 한국 정식 상품은 880
   이라 절대 안 겹친다. 쿠팡이 「임의로 생성한 숫자」를 GTIN 으로 쓰는 걸 금지하기 때문.
🔴 **전엔 라벨에 SKU 를 대신 찍고 있었다** — 쿠팡이 금지한 「판매자 내부 관리용 SKU 코드」와
   정확히 겹쳤다. 그게 이 파일을 만든 진짜 이유다.

### 3-3. 쿠팡 배선 — `compile_coupang.py`
| 값 | 전 | 후 |
|---|---|---|
| 과세구분 | `'TAX'` 박힘 | 정책대로 (면세=FREE) |
| 상품상태 | **아예 안 보냄** | `offerCondition: 'NEW'` 명시 |
| 제조사 | 안 보냄 | 정책값, 비면 브랜드로 갈음 |
| 모델번호 | 안 보냄 | 있을 때만 |
| 바코드 | 안 보냄 | **공식만** + 없으면 `emptyBarcode=true`+사유 |
| 검색태그 | 안 보냄 | 20자·20개 상한 지켜 자름 |

### 3-4. 스마트스토어 배선 — `compile_smartstore.py`
`detailAttribute.taxType`(면세=DUTYFREE) · `naverShoppingSearchInfo.manufacturerName/.modelName`
· `sellerCodeInfo.sellerBarcode`(공식만) · `seoInfo.sellerTags`

🔴 **`saleType` 은 일부러 안 보낸다.** 기존 시험이 「추측한 미검증 필드는 넣지 않는다」로
   막고 있었고 확인해 보니 맞다 — 스스 등록 요청 예시에 그 필드가 없고 「미입력 시 NEW」다.
   반면 쿠팡은 **공식 요청 예시에 offerCondition 이 들어 있어** 보낸다.
   **같은 「새상품」이라도 마켓마다 근거가 다르다. 「같은 값이니까」로 끼워 넣지 말 것.**

### 3-5. 정책 → 초안 다리 — `process_apply._apply_listing` + `as_draft._POLICY_FIELDS`
끝까지 확인함:
```
정책 「면세·한국제화·19세 이상만」
  → 가공 사본  tax_type=면세 · manufacturer=한국제화 · minor_purchasable=False
  → 초안 행    policy_fields_from() 이 옮겨 담음   ← 모음전은 여기를 컴파일한다
  → 쿠팡       taxType=FREE · manufacture=한국제화 · adultOnly=ADULT_ONLY
```
🔴 정책이 말하지 않으면 **손대지 않는다**(사람이 상품에 넣은 값을 덮으면 안 됨).
🔴 제조사가 「브랜드와 동일」이면 **아무것도 안 넣는다** — 컴파일러가 브랜드로 갈음한다.
   여기서 복사하면 원천이 둘이 되어 브랜드를 고쳐도 제조사만 옛 값으로 남는다.
🔴 `_LISTING_NO_FIELD` 가 **비었다**(작업 전 4개 → 0). 「저장은 되는데 안 나간다」가 사라졌다.
   **새로 「못 보내는 칸」이 생기면 반드시 여기 적을 것** — 안 적으면 화면이 조용해진다.

### 3-6. 정책 화면 항목 정리 — `process_rule_schema.py` · `policy/fields.py`
- `listing.tax_type` 선택지 = 과세/면세 (**영세 제거**)
- `listing.product_condition` · `listing.sale_period` **제거** → `fixed_sends` 로 이동
- `_auto_pricing` 신규 (**쿠팡 전용**) — 안 씀 / 최저가를 마진율로 계산 / 직접 입력

## 4. 남은 일 — 이것만 하면 된다 (자가 점검으로 확인한 목록)

아래는 코드를 실제로 grep 해서 **「아직 없음」이 확인된** 것들이다.

### ⬜ 4-1. 11번가 배선 — `shared/platforms/eleven11/products.py`
지금 `<suplDtyfrPrdClfCd>01</suplDtyfrPrdClfCd>`(과세)·`<prdStatCd>01</prdStatCd>`(새상품)이 **박혀 있다**.
- 과세구분 → `suplDtyfrPrdClfCd` (01=과세 / 02=면세)
- 제조사 → `요청.company` · 모델번호 → `요청.modelCd`
- 🔴 **바코드 칸이 없다**(239칸 전수 확인) · 태그는 `ProductTag > tagName`(조건부)
- 🔴 판매기간 `aplBgnDy`/`aplEndDy` 는 문서에 [필수] 표기가 없지만 **누락 시 500**
  (지도 idTraps 에 실측 기록). 지금 오늘~+3년으로 나가고 3년이 상한 — **그대로 두면 된다**

### ⬜ 4-2. 옥션·G마켓 배선 — `shared/platforms/esm/products.py`
- 과세구분 → `itemAddtionalInfo > isVatFree` (Boolean: 면세=true) · 지금 인자 기본값 False
- 모델번호 → `itemBasicInfo > catalog > modelName` · 바코드 → `catalog > barCode`
- 🔴 **제조사·태그는 칸이 없다**(일반 상품). 제조사는 「예약설치 상품」 전용 칸만 있다
- 판매기간은 이미 `{-1,-1}`(무제한) = 가장 긴 값 — **그대로 두면 된다**

### ⬜ 4-3. 자동 가격 조정 payload 배선
정책 항목은 만들었지만 **payload 로 나가지 않는다**(`autoPricingInfo` 가 compile_coupang 에 없음).
- 승인 전: `items.autoPricingInfo { minSalePrice, active }`
- 승인 후: `PUT .../vendor-items/{vendorItemId}/prices/{price}?apMinSalePrice=&apActive=`
- 🔴 `minSalePrice` < 판매가 필수 · `apMinSalePrice`·`apActive` 는 **함께** 보내야 함(하나면 400)

### ⬜ 4-4. 화면 — `webapp/templates/policy/detail.html`
확정 시안(v2)대로. 지금 `hz-body`(빗금)·`scope pol`(세 묶음) 둘 다 **템플릿에 없다**.
- 1-C 빗금: 「쿠팡 정책설정」 줄은 빗금 **밖**, 그 아래만 빗금
- 2번: 과세구분을 **라디오**로 + 「마켓마다 어떤 값으로 나가는지 보기」 접힘표
- 3-B: 정책이 정함 / 상품마다 / 옵션마다 **세 묶음**
- 4-D: 즉시할인·쿠폰 **켜기 스위치 두 줄**
- 🔴 **마켓 끄면 전송 금지**가 실제로 되는지 확인(사장님 명시). 값 수정은 계속 가능해야 함

### ⬜ 4-5. 지도 되채움 — GTIN 필수화 공지
확인 결과 **브랜드 API 3종·`autoPricingInfo`·`isUIDRequired` 는 이미 지도에 있다**(내가
앞서 「통째로 빠졌다」고 한 건 오판이었다). 빠진 건 **공지 본문**뿐:
- 2026-05-27 공지 「상품 식별번호(GTIN/모델번호) 입력 정책」 — 지도에 「상품 식별번호」 0회
- 핵심: 브랜드 상품에 한해 의무 · **API 등록 상품은 2026-08-01 시행(이미 시행 중)**
- 🔴 금지: 판매자 내부 SKU · 재고관리 코드 · 옵션명 · **임의로 생성한 숫자** · 다른 상품 GTIN
- 예외: 자체제작 · 수공예 · 비브랜드 · 일반(Generic) · 제조사가 공식 번호 미제공
- 있는데 누락/부정확 → **등록 제한 또는 노출 제한**
→ `webapp/data/marketplace_api_map.json` 에 되채우고 `validate_map` 통과시킬 것

### ⬜ 4-6. 검증·머지·배포
- `pytest tests` **전체**(약 30분, 8,800건대). 인수인계 목록만 돌리면 CI 에서 깨진다
- 🔴 **검사 도는 중에 `git merge`·파일 수정 금지** — 결과를 두 번 버렸다
- PR 열고 CI 통과 → 머지 → **라이브 화면에서 이번에 새로 생긴 표식 확인**

## 5. 이 작업에서 나온 함정 (다시 밟지 말 것)

1. **「칸이 비었다」와 「칸이 아예 없다」는 다른 문제** — 필수값 빈 채 전송을 막는 게이트가
   모음전 전송을 6마켓 전부 멈출 뻔했다(`to_payload.set_view` 의 `detail_html` 은 늘 빈 값).
   앞은 막고, 뒤는 **막지 말고 말한다**(`blocking=False, gap=True`).
2. **모음전은 사본이 아니라 초안 행을 컴파일한다** (`send/runner.py:272`). 사본에만 실린
   값은 `_POLICY_FIELDS` 에 없으면 **조용히 사라진다**. 대량등록에선 되는데 모음전에서만
   안 되는 얼굴을 한다 — 실제로 미성년자 구매가 그랬다.
3. **배선을 고치면 그걸 설명하는 표도 같이 낡는다.** 배송비를 이었더니 알약이
   「정책 2,500 / 실제 3,000」이라고 **반대로** 거짓말했다 → `Fixed.policy_wins`.
4. **시험 도우미가 새 칸을 조용히 버린다.** `D(**kw)` 가 아는 칸만 읽어서, 시험이 계속
   기본값만 보고 있었다. 새 칸을 쓸 땐 `setattr` 로 직접 얹을 것.
5. **기존 시험이 막아설 때 그게 맞을 수 있다.** 스스 `saleType` 을 넣으려다 「미검증 필드는
   넣지 않는다」 시험에 걸렸고, 확인해 보니 그 판단이 옳았다.
6. **PowerShell 은 한글을 깨뜨린다.** Bash(Git Bash) + `PYTHONIOENCODING=utf-8` 를 쓸 것.
   heredoc 은 역슬래시를 먹으니 파일을 쓸 땐 **Write 도구**를 쓸 것.

## 6. 「놓치지 않았다」 증명 방법

새 세션에서 아래를 돌려 4절 목록이 **여전히 「남음」인지** 직접 확인하라. 하나라도
「됨」으로 바뀌어 있으면 다른 세션이 이미 했다는 뜻이니 중복 작업하지 말 것.

```bash
cd "C:/dev/모음전 프로젝트/_wt_fields/프로그램/_시스템" && python -c "
import io,sys; sys.stdout=io.TextIOWrapper(sys.stdout.buffer,encoding='utf-8')
r=lambda p: open(p,encoding='utf-8').read()
chk=[('11번가 과세구분 배선','tax_type' in r('shared/platforms/eleven11/products.py')),
     ('ESM 과세구분 배선','tax_type' in r('shared/platforms/esm/products.py')),
     ('ESM 모델명·바코드','model_no' in r('shared/platforms/esm/products.py')),
     ('자동가격 payload','autoPricingInfo' in r('lemouton/registration/compile_coupang.py')),
     ('화면 빗금','hz-body' in r('webapp/templates/policy/detail.html')),
     ('화면 세 묶음','scope pol' in r('webapp/templates/policy/detail.html')),
     ('지도 GTIN 공지','상품 식별번호' in r('webapp/data/marketplace_api_map.json'))]
[print(('  됨   ' if v else '  남음 ')+n) for n,v in chk]"
```

끝나면 사장님께 **7섹션 보고서**(`/ui-verify` 형식)로 보고하고, 대화 마무리는
`/conversation-wrapup` 형식(■대화 내용 정리 / ■사용자 결정 사항 · 색 칩 위젯)으로 할 것.
