모음전 프로젝트 「대량등록 검색필터 — 소싱처 7곳」 작업을 이어받아 진행해줘.

━━ 먼저 읽을 것 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
C:\dev\모음전 프로젝트\다른세션_전달_대량등록_검색필터_소싱처7곳.md

이 문서 하나에 전부 있다 — 규칙표 · 완료목록(PR 17건) · 함정 10가지 · 남은 일 · 증거.

━━ 지금 어디까지 왔나 (2026-08-13) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
7곳 전부 걷힌다. 라이브에 26,127개가 모여 있고 3곳은 사장님 값과 일치했다.

  무신사      1,218 / 1,218   🎯 정확히 일치 · 끝남
  롯데온      2,131 / 2,100   🎯 넘김 · 끝남
  SSG         1,011 / 26쪽    ✅ 0 → 1,011 (진행 중)
  현대H몰    13,947 / 16,668  84% (못 걸은 쪽 되찾기로 메우는 중)
  롯데아이몰  7,228 → 화이트 6,427 로 검색어 전환(id=21, 아직 0)
  SSF 카테고리  453           8SECONDS 티셔츠
  SSF 검색       69 / 10,604  ⚠️ 검색 주소는 첫 쪽만(규칙)
  르무통         70           견줄 값 없음

━━ 지금 당장 할 일 (순서대로) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. PR #1037 검사 통과 확인 → 머지 → 배포 완료까지 확인
   gh pr view 1037 --json state,mergeStateStatus,statusCheckRollup
   (「훑기 잠금이 스스로 풀린다」 — 없으면 수집이 통째로 멈춘 채 아무 말도 없다)

2. 확장 폴더는 이미 저장소와 연결(junction)돼 있다 — 맞출 것이 없다
   cd "C:/dev/_wt_bulksafe/프로그램/_시스템"
   PYTHONIOENCODING=utf-8 python scripts/sync_loaded_extension.py
   → 「✅ 연결돼 있습니다」가 나와야 정상

3. 라이브 수집을 이어 돌린다 (인증 없이 curl 로 된다)
   curl -s "https://www.mou-m.com/bulk/api/search-filters"
   멈춰 있으면 각 필터에 POST .../search-filters/<id>/run
   ★ 원래는 서버가 스스로 이어 걷는다(PR#1028). 손으로 걸 일은 확장이 잠겼다 풀린 직후뿐.

4. 남은 것을 끝까지 채운다
   · H몰 13,947 → 16,668
   · 아이몰 화이트(id=21) 첫 수집 — 6,427개 목표
   · SSG 1,011 → 사장님 26쪽(약 1,560)과 대조
   · 크롤 대기 → 상품(초안) → 마켓 진짜 1건 등록 (한 바퀴 미완)

5. 사장님께 여쭐 것 (모아서 한 번에)
   · 확장 카드의 「경고」 단추가 무슨 경고인지
   · SSF 8SECONDS 「전체」 카테고리 주소 (검색 주소로는 첫 쪽만 걷힌다)
   · 르무통 검색 주소와 그 화면의 상품 수 (지금은 내가 임의로 고른 주소다)
   · 라이브 「검증_*」 필터 9개 — 증거로 남겨 뒀다. 지울지

━━ 반드시 지킬 것 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
· 사장님은 비개발자다. 평이한 한국어, 화면 단어는 「」 안에 그대로.
· 마무리는 /conversation-wrapup 형식(■대화 내용 정리 / ■사용자 결정 사항),
  맨 끝에 「※ 전체 체크리스트가 필요하신가요?」 한 줄.
· 중간에 자꾸 묻지 말고 끝까지 진행한다. 꼭 필요한 결정만 모아서 묻는다.
· 「검증됨」은 라이브 화면 숫자로 확인한 것만 쓴다. 코드만 보고 쓰지 않는다.
· 크롤 자동 실행은 이미 켜져 있다(사장님이 허락하심). 끄지 말 것.
· 병합 뒤에는 저장소 전체에서 충돌 표시를 훑는다(git add -A 가 그냥 커밋해 버린다).
· 실브라우저 검증이 필요하면 /live-browser-verify 스킬을 쓴다.
· 틀린 것은 틀렸다고 말한다. 앞 세션은 제 진단 3건을 스스로 뒤집었다.

━━ 절대 잊으면 안 되는 함정 7개 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
① 확장 폴더는 이제 저장소와 연결(junction)돼 있다 — 복사 단계가 없다.
   코드를 고치면 chrome://extensions 의 ↻ 만 누르면 반영된다.
   ⚠️ 연결 대상이 _wt_bulksafe 워크트리다 — 브랜치를 바꾸면 확장 내용도 바뀐다.
   ⚠️ 화면 표시(data-moum-ext)는 화면 쪽 판이라 못 믿는다. 서비스워커에 직접 물어라
      (문서 §5-① 에 콘솔 코드 있음). 확장을 다시 불러오면 열린 화면의 연결이 끊긴다.

② 「파라미터가 있다」 ≠ 「그게 먹는다」 — 무신사 page=1 과 page=2 응답이 완전히
   같았다. 반드시 1쪽과 2쪽을 대조할 것.

③ 「결과 없는 검색어」로도 재라 — 0건 화면에도 추천 상품이 깔린다(6곳 중 5곳).
   아이몰은 정상 검색어에서도 25건 전부 배너였다.

④ 「끝남」이 네 가지로 거짓말한다 — 다음 쪽 주소를 못 찾음 / 시간 초과 / 시킨
   만큼만 봄 / 다 걷고도 「더 있음」. 넷 다 고쳤지만 판정 코드를 건드릴 때 조심.
   ★「덜 걷고 끝났다」와 「다 걷고 더 있다」는 둘 다 거짓말이다.

⑤ 「다음」이라 적혀 있다고 「다음 쪽」이 아니다 — 아이몰의 「다음」은 「다음 10쪽
   묶음」이었다(1쪽 → 11쪽). 단추 글자 말고 쪽 번호가 몇으로 바뀌는지 봐라.

⑥ 「기다린다」와 「바뀐 것을 확인한다」는 다르다 — 고정 1.2초를 기다렸더니 아이몰이
   아직 안 그려서 같은 쪽을 읽고 첫 쪽에서 멈췄다. 손으로 재서 되던 값을 코드에
   넣으면 그건 실측이 아니라 우연이다.

⑦ 「한 번에 하나」 잠금에는 반드시 시한이 있어야 한다 — 죽은 회차가 잠금을 쥔 채
   수집이 통째로, 아무 말도 없이 멈췄다. 확장은 살아서 다른 청에는 답하는데
   훑기만 안 돌았다.

━━ 시험 쓸 때 (이번 세션에 네 번 데임) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
· 내가 못 박은 시험이 다음 날 발목을 잡는다. 목록을 손으로 박을 때는
  「무엇을 근거로 넣었나」를 함께 적어라.
· 「구조상 안 된다」는 판정도 낡는다 — 「그 수단이 없다」와 「내가 아직 못 찾았다」는
  다른 사실이다.
· 시험이 내 PC 전제(git 이력 깊이 등)를 깔면 CI 얕은 클론에서 실패해 배포가 막힌다.
  PR 검사 통과 ≠ main 통과. 머지 뒤 main 런을 끝까지 확인할 것.
· 정규식·이스케이프가 든 식은 글자를 세지 말고 node 로 실제로 돌려 봐라.
  (`/\\\\//g` 는 정규식이 아니라 나누기였다 — 문법은 맞아 node --check 로 못 잡는다)
· 역슬래시가 든 파일은 heredoc 말고 Write 도구로 써라(heredoc 이 먹는다).
