# 다른 세션 전달 — 프로그램 전면 재설계 (이어서 진행)

> 작성 2026-07-30 · 갱신 2026-07-31 · 앞 세션에서 **1~5단계 배포 완료**, 6·7단계는 사장님 지시로 제외.
> 이 문서만 읽으면 이어서 할 수 있게 썼습니다.
>
> **2026-07-31 갱신** — 아래가 전부 배포·라이브 확인 완료입니다.
> · §3 🔴 「매입가=표면가」 수정 (PR#559)
> · **가공규칙 13항목 전량 실적용** (PR#560) — 47칸 중 13칸만 동작하던 것을 확대
> · **주문 3분류(이행/미이행/클레임) 자동 판정** (PR#561) — 6단계 완료
> · **정책 가격 미리보기** (PR#562) — 계산엔 아직 미배선(마진율 대기)
> · **주문 3분류 「우리 상품 아님」 분리** (PR#564·566) — 확인 불가 **281→0건**.
>   289건은 모음전으로 관리하지 않는 남의 상품(잔스포츠·마스마룰즈 등)이라
>   **고칠 것이 없습니다.** 연동 색인 자체는 정상(르무통 메이트 165쌍 전부 matched).
>   옥션·G마켓·11번가·롯데온은 **연동이 0건**이라 그 마켓 주문은 전부 남의 상품입니다.
> · **판정 근거 노출** (PR#567) — 🔴 역마진 판정은 「지금」 매입가 기준이라
>   소싱처 가격이 바뀌면 **어제 역마진이 오늘 이행으로 바뀝니다(버그 아님).**
>   줄에 마우스를 올리면 `정산예정금 − 최종매입가 = 이익`이 보입니다.
>
> 🔴🔴 **라이브에 저장된 규칙 값이 한 건도 없습니다.** 대량등록 가공정책 5개 전부
> `rule_count=0`, 모음전 정책 「TEST」 = 채움 0/83. 「기능이 안 된다」가 아니라
> **값을 안 넣은 상태**입니다. 자세한 것은 메모리
> `project_process_rules_all_items_and_order_3tabs.md`.

---

## 0. 지금 당장 알아야 할 것 3가지

1. **작업 폴더는 `C:\dev\_wt_redesign` (브랜치 `feature/restructure-stage1`)**
   `C:\dev\모음전 프로젝트` 는 `feature/crawl-worker-queue` 라 **마진계산기·webapp 상당수가 아예 없습니다**(stale).
   새로 시작하면 반드시 `origin/main` 기준 워크트리를 뜨세요.
2. **미커밋 변경 없음 · 전부 main 머지·배포 완료.** (마지막 커밋 `3de0d12e`)
3. **라이브는 `https://mou-m.com` 무인증 접속 가능** — 브라우저로 바로 확인됩니다.

---

## 0-1. 노션 원문 대조 결과 (2026-07-31)

노션 「상품 가공」은 정책을 **5갈래**로 적었는데, 대량등록 13항목을 그대로 쓰기로 하면서
(1)(3)(5)가 빠져 있었습니다. (2)(3) 은 PR#569 로 메웠고 **남은 것**:

- ~~(1) 마켓별 옵션 1/2/3축 구성~~ → **PR#570 완료** (one/two 실동작, three 는 모델명 칸 없어 불가)
- ~~(5) 마켓별 업로드 불가 카테고리 검사~~ → **PR#571 완료** (수집해 둔 사전과 대조, 사전 없으면 막지 않음)
- ~~④ 상품관리 가격·재고 변동 그래프~~ → **PR#572 완료**
  🔴 **그릴 숫자가 아예 없었습니다**(어디에도 시각별 가격이 안 쌓임) → 새 표
  `source_price_history` 를 만들어 크롤 저장 지점에서 남깁니다. **바뀌면 항상 ·
  안 바뀌면 하루 2회.** 표면가 기준(혜택 차감 전). 화면 = 매트릭스 정보창 「📈 가격 흐름」.
  ⚠️ **이제부터 쌓입니다** — 며칠 지나야 선이 보입니다.
- ~~④ 적용된 정책 보기 · 카테고리 맵핑 보기~~ → **PR#576 완료** (`/matrix/<id>` 카드)
- ✅ 주문 3분류 축은 **지금대로 유지 확정**(사장님 2026-07-31) — 노션의
  「모음전 vs 대량등록」 분류로 바꾸지 않습니다.

### 🔴 아직 남은 노션 항목 (막힌 이유 포함)
| 항목 | 왜 못 했나 |
|---|---|
| ① 「마켓 불러오기」로 상품 만들기 | 🔴 **옵션이 없습니다** — 아래 조사 결과 참고 |
| ④ 최저가 경쟁 순위 분석 | 다른 판매자 가격을 알아야 함 — **검색 API/크롤 필요** |
| ~~⑤ 주문 바로가기~~ | **PR#579·580 완료** — 소싱처·상품관리·가격이력. 🔴 판매처링크는 **주소 규칙 미확인이라 안 만듦** |
| ⑤ 주문건별 자세히보기 카드 | 미착수 (사장님 시안 `.lnk` 가 사장님 PC 에만 있음) |
| ⑤ 미이행 고객 SMS | **알리고 문자 계정 필요**(사장님) |
| ⑥ 소싱분석 | **네이버 API·아이템스카우트 키 필요**(사장님) |
| ③ 쿠팡 GTIN 전송 | 🔴 **보낼 값이 없습니다** — 아래 조사 결과 참고 |

### 남은 2건 — 착수 전 조사 결과 (2026-07-31, 코드 실측)

**① 「마켓 불러오기」로 모음전 상품 만들기**
- `lemouton/catalog/models.py` 에 `MarketProductGroup`(name·brand·**model_code**·set_id) +
  `MarketProduct`(market·account_key·market_product_id·name·brand·status·sale_price·group_id)
  가 이미 있고, **`MarketProductGroup.model_code` 로 모음전과 이을 자리도 이미 있습니다.**
- 🔴 그런데 **옵션(색상·사이즈) 행이 캐시에 없습니다** — `MarketProduct` 는 **상품 단위**뿐입니다.
  노션 STEP 2 는 「매트릭스 옵션 생성 + 옵션별 개별 옵션번호」인데, 만들 재료가 없습니다.
- **결정 필요**: ⓐ 마켓에서 옵션까지 새로 긁어올지 ⓑ 상품 껍데기만 만들고 옵션은
  기존 매트릭스에서 담게 할지(지금 `/matrix` 흐름과 이어짐).

**③ 쿠팡 GTIN**
- 데이터코드지도(`marketplace_api_map.json`)에 이미 근거가 있습니다 —
  쿠팡 브랜드 API 가 `isUIDRequired`(UID 필수 여부) · `allowedUIDTypes`(`["GTIN","MPN"]`)를 줍니다.
  **브랜드에 따라 GTIN 이 없으면 등록이 거부됩니다.**
- 🔴 그런데 **우리는 바코드(GTIN)를 어디에도 갖고 있지 않습니다.** `compile_coupang` 의
  `externalVendorSku` 는 빈 문자열이고, 상품·옵션 어디에도 바코드 칸이 없습니다.
- **결정 필요**: 바코드를 ⓐ 소싱처 크롤로 모을지 ⓑ 사장님이 넣을 칸을 만들지.
  값이 없는 채로 배선만 하면 「보낸다고 해 놓고 못 보내는」 거짓 기능이 됩니다.

### ✅ 초코바 맵핑 삭제 완료 (2026-07-31)
「스니커즈」가 쿠팡 **초코바** 카테고리로 제안돼 있던 **3건 삭제**(SSG·롯데온·스마트스토어
경로, 전부 쿠팡 코드 `72924`, `제안` 상태). 쿠팡 맵핑 6→3건.
⚠️ **같은 오탐이 다시 생길 수 있습니다** — 제안 생성은 이름 유사도로 돌아가고,
PR#571 검사도 못 막습니다(초코바는 쿠팡에 실재하는 코드라서). 확정 전에 눈으로 보세요.
맵핑 보기: `/matrix/62` 「📋 이 상품에 적용된 것」 · 삭제: `DELETE /bulk/api/catmap/<id>`

## 1. 스펙 원천

- 노션 「무재고 모음전 솔루션 개발현황」 `3a9cf482-7373-8014-b510-c3ef0b8a0418`
- 메모리 `project_program_restructure_8groups_numbering.md` — 결정사항·함정 전부

---

## 2. 끝난 것 (배포·라이브 확인 완료)

| 단계 | 내용 | PR |
|---|---|---|
| 1 | 사이드바 노션 8분류 재편 · 모드 2개 · 삭제 9종 · 이관 2종 | #529 |
| 2-1 | 표시번호 발급·소급(5,286건) · 화면 표시 | #532 #533 |
| 2-2 | 원본(U)·파생(P) 매트릭스 계층 신설 | #535 #536 |
| 4 | 매트릭스에서 새 상품 만들기(옵션 복제) | #545 |
| 5 | 마켓별 정책 — 만들기·채우기·붙이기·지우기 | #548 #549 #558 |
| — | 마무리(번호 검색·기본정책 자동적용·입구 안내) | #550 |

### 핵심 설계 결정 (건드리지 말 것)

- **번호**: `접두 + 날짜8 + '-' + 순번6`. `M`모상품 · `U`원본매트릭스 · `P`파생 · 소싱처 영문2(상품=앞자리 1, 옵션=0) · `D`로 시작하면 대량등록.
  개별 옵션번호는 **기존 `canonical_sku` 그대로** (252파일 1,715곳이 쓰는 열쇠).
  규칙 단일 원천 = `shared/display_no.py`. 순번은 `display_no_seq` 행잠금(워커 3개).
- **매트릭스**: 원본 = 지금의 모델 하나(멤버는 `Option.model_code` 가 이미 앎 → 중복 저장 안 함). 파생만 멤버 명시.
  `service.edit_target()` 이 「소싱처 URL·사입품번을 어디서 고쳐야 하나」의 단일 판정처.
- **상품 만들기**: 옵션을 **참조가 아니라 복제**. 프로그램 전체가 「옵션은 모델 하나 소유」를 전제로 돌아서, 참조로 두면 **새 상품이 조용히 전송에서 빠짐**. 소싱처 연결·축은 복제, **마켓 발급 옵션ID는 복제 안 함**.
- **정책 기본항목**: `lemouton/registration/process_rule_schema.py`(대량등록 가공 규칙 13항목)를 **베끼지 않고 그대로 불러 씀**. 마켓별 예외 4개만 `lemouton/policy/fields.py` 가 직접 정의.
  저장 = `field_key=item_key, value=JSON`. **저장 안 한 항목은 「안 정함」**(0 아님).

---

## 3. 이어서 할 일 — 「정책 → 가격 계산 연결」

앞 세션 마지막 작업. **조사만 하고 코드는 한 줄도 안 썼습니다.**

### 왜 아직 안 물렸나
정책의 수수료율·마진율이 **비어 있습니다**. 빈칸을 0으로 읽으면 그 가격이 그대로 마켓에 나갑니다(가격 오류 = 금전 손실). 그래서 화면이 마켓별로 「가격 아직 못 씀」을 표시하고, 계산에는 쓰지 않습니다.

### 조사해서 알아낸 계산 경로
```
최종매입가 = compute_final_price(
                sale_price = SourceOption.current_price(표면가),
                effective  = compute_breakdown(session, sku=, source_id=,
                                               sale_price=, source_product_id=)
             )['final_price']          # 백원 단위 버림
판매가     = 최종매입가 × (1 + 마진율/100)   ← 노션 「기준은 최종매입가(사장님 확정)」
```
- `compute_final_price` : `lemouton/pricing/final_price.py:365`
- `compute_breakdown` : `webapp/routes/api_benefits.py:496`
- 작동 예시 참고 : `lemouton/sourcing/price_verify_service.py:153`
- ⚠️ `source_id` 는 **`SourceRegistry.id`** — 화면용 `SourcingSource.id` 와 **다른 체계**.
  변환 단일 원천 = `lemouton/sourcing/source_ids.py` (그 파일에 실사고 기록 있음).

### 권장 순서 (안전 우선)
1. **미리보기부터.** 정책 상세에 「이 정책으로 계산하면」 패널 — 상품 하나 골라
   옵션별 `최종매입가 → 정책 판매가 → 지금 판매가 → 차이` 를 보여준다.
   **실제 전송 경로는 건드리지 않는다.**
2. 사장님이 숫자를 눈으로 확인한 뒤에 실제 계산 교체.
3. 기존 엔진 한계 확인 필요 — `lemouton/pricing/engine.py` 는 **전역 settings**를 받고
   **스마트스토어·쿠팡 2마켓만** 지원. 정책은 상품별·6마켓이라 구조가 다르다.

### ✅ 함께 고쳐야 했던 것 — 완료 (2026-07-31 · PR#559 배포)
**매트릭스 정보창의 「매입가」가 사실은 「표면가」였습니다.** 고쳤습니다.

- 정보창 표가 **표면가 · 최종매입가 두 칸**으로 분리 (헤더 툴팁에 뜻 설명)
- 「최저」 판정 기준 = **최종매입가** (`api_pricing._pick_cost_source` 와 같은 규칙)
- 계산 못 하면 「확인 불가」 — **표면가로 메우지 않음**
- 계산은 새로 짜지 않고 `api_pricing._attach_final_purchase`(→`compute_breakdown`) 호출만
- 테스트 `tests/matrix/test_rows_final_purchase_price.py` 4건

🔴🔴 **라이브 실측에서 버그가 실재했음이 확인됨** (메이트 라이트블루 240):
표면가 최저 = **롯데온 116,840** / 최종매입가 최저 = **르무통 공홈 107,700** — **다른 소싱처**입니다.
무신사는 표면가 129,890 으로 가장 비싼데 최종매입가는 108,000 으로 두 번째로 쌉니다.
→ **표면가로 소싱처를 고르면 틀립니다.** 앞으로 어디서든 「어디가 싼가」를 판단할 땐 최종매입가로.

---

## 4. 사장님 자료가 있어야 되는 것

| 무엇 | 어디에 쓰나 |
|---|---|
| 마켓별 수수료율·마진율·배송/반품비 실제 숫자 | 정책 → 가격 계산 연결 |
| 「더망고 캡처」 (노션에 「※ 보낼 것」이라 적힘) | 기본 정책 항목이 실제 양식과 맞는지 대조 |
| 알리고 문자 계정 | 6단계 미이행 고객 대응 |
| 네이버 API · 아이템스카우트 키 | 7단계 소싱분석 |

**지어내지 마세요.** 앞 세션은 값을 비운 채 그릇만 만들었습니다.

---

## 5. 제외된 6·7단계 — 재개 조건

- **6단계 주문 3탭(이행/미이행/클레임)** — `orders/index.html`(3,452줄)을 고쳐야 하는데
  다른 세션이 같은 날 송장작업으로 PR#531·534·544·546 연속 머지 중이었습니다.
  **2026-07-31 확인: 그 세션은 PR#546까지 머지하고 끝났습니다 → 지금은 착수 가능.**
  · 미이행 분류 = S(재고없음) · P(역마진) · 기타 (사장님 확정)
  · 참고 시안 `모음전 시안 2_대량등록 매입가마진카드.lnk` — 사장님 컴퓨터에만 있음
- **7단계 소싱분석** — 키가 없으면 빈 화면이 됩니다(거짓 기능). 키 받은 뒤 시작.

---

## 6. 테스트 주의

- `pytest` 는 **인자 없이 돌리지 말 것**. `tests/policy tests/matrix tests/catalog tests/sourcing` 식으로.
- **origin/main 기존 실패 5건** — `tests/registration/test_send_tab.py` 2 · `test_speed_settings.py` 3.
  기준선 `ac79c013` 에서도 동일하게 실패합니다. 새로 깨진 게 아닙니다.
- ⚠️ 앞 세션 실수: 1단계에서 모드를 3→2 로 줄인 뒤 `tests/registration` 를 안 돌려
  `test_bulk_mode` 2건이 깨진 채 PR 5개가 나갔습니다(#558에서 수정).
  **사이드바·모드·레이아웃을 건드리면 `tests/registration` 도 돌리세요.**

---

## 7. 화면 확인 빠른 경로

| 무엇 | 주소 |
|---|---|
| 매트릭스 옵션(메이트 원본) | `/matrix/62` |
| 옵션 상세(소싱처 관리번호) | `/bundles/르무통_메이트/option/SKU-HU61O8ZW` |
| 마켓별 정책 | `/policies` |
| 표시번호 현황·소급 | `GET /api/admin/display-no/status` · `POST …/backfill?all=1` |

메이트 실측값 — 모상품 `M20260730-000062` · 원본매트릭스 `U20260730-000062` · 옵션 126개 · 소싱처 5곳(LE·MU·SF·LO·SS)
