# 판매처 연동 카드 — 다른 세션 인계 프롬프트

> 그대로 붙여넣어 사용. 「판매처 연동」 대시보드 카드의 후속 작업(소싱처 바로가기 + 상세↔연동 데이터 공유) 인계.

## 무엇 (화면)
- 「판매처 연동」 연동 현황 대시보드. **URL: mou-m.com/api/sets/dashboard** (왼쪽 메뉴 「판매처 연동」).
- 파일: `프로그램/_시스템/webapp/templates/sets/dashboard.html` (extends base.html, 카드는 JS로 렌더). 라우트: `webapp/routes/sets_api.py` `sets_dashboard_page` → `render_template("sets/dashboard.html")`.
- 카드 1개 = 모음전 1개. 카드 안 = 마켓 행(쿠팡·스마트스토어…). 칼럼: **마켓 | 매칭 | 재고(소/판) | 가격(매입/예정/현재) | 상태(신호등) | 동작(이력·미리보기)**.

## 이미 배포·라이브 (재구현 금지)
- **백엔드 P1~P3·P2b** (이미 있었음): 데이터모델(`SetChannelOption.mkt_*`, `ChannelChangeEvent`)·수집(`lemouton/sets/collect_service.py`)·알림(`alert_service.py`)·자동수집(`scheduler/sets_collect.py`, 12h `LEMOUTON_SETS_COLLECT_HOURS`)·소싱변동 스냅샷.
- **P4 화면(PR#56)**: 확정 디자인 카드 — 상태 신호등(E2)·행펼침 상세(D3)·좌측 필터패널·헤더(업데이트▾·편집/삭제 글자). `set_service.list_linked_sets`가 소/판 요약·신호등·`src_summary`(주입 provider) 제공.
- **판매예정가(PR#58)**: 가격 3줄(소 매입[상세지연]/판 예정/현 현재), 현재값 연회색. `planned_price` = ss_price(스스)/cp_price(쿠팡).
- **가격 이력 3단계(PR#60)** + **하루 1줄(PR#61)**: 이력 = 일자 기준 1줄(그날 최신값), 전날 대비 ▼파랑/▲빨강. 가격(표면노출가·최종매입가·판매예정가)·재고(소싱·판매) 둘 다.
- **이력 데이터 출처·라벨 교정(PR#63)**: 수동 스냅샷(`snapshot-sources` 라우트)이 3단계 명시필드(surface/cost/planned)로 기록. 라벨 「표면 노출가」, 최종매입가=표면−혜택(fx).
- **카드 넘침 수정(PR#65)**: `row6 = 148px 56px 146px 154px 118px 172px` (space-between) + 필터 240→220 + 콘텐츠 `minmax(0,1fr)`. ⚠ **매우 타이트 — 새 칼럼 추가 여지 없음.**

## 단일 진실 원천 (⚠ 금전 정확성 = 타협 없음)
- **표면 노출가** = `crawled_price` (소싱 크롤 노출가)
- **최종매입가** = `compute_breakdown(...).final_price` (표면−fx혜택, `webapp/routes/api_benefits.py`)
- **판매예정가** = `compute_market_price` ss/cp (`api_pricing._option_matrix_data`의 `ss_price`/`cp_price`)
- 소싱 재고·소싱처명·위 값 = `_option_matrix_data`(옵션 단위) → `sets_api._card_src_provider`가 `list_linked_sets(src_provider=...)`로 주입.
- **대표 소싱처** = 상세 우선순위(`purchase_priority_resolved`: 사입 우선 or 소싱처 우선) → 소싱처면 제공 URL 중 **재고 있고 `crawled_price` 최저**인 것. (프론트 `pickSrc`·백 `_card_src_provider` 로직 동일)

## 해야 할 것 (논의 중 / 미완)

### A) 소싱처명 + 바로가기 버튼
- 재고/가격 출처 소싱처를 **이름 + ↗(그 소싱처 상품 URL 새 창)** 로 카드에 표시. 시안: 바탕화면 「모음전 판매처연동 소싱처 바로가기 시안.html」 (A 인라인 / B 칩). 사입이면 「사입」(바로가기 없음).
- **공간**: 소싱처명은 이미 재고(소/판) 셀 「소」 줄에 있음(`cStock`의 `srcName`). row6 타이트 → **새 칼럼 불가**. 현실안 = ① 「소」 줄 소싱처명을 ↗ 링크로, 또는 ② 행펼침(상태 클릭) 상세에 넣기. 실제 폭 라이브 확인.
- **데이터 배선 필요**: `_card_src_provider`가 source_name은 주지만 **대표 소싱처 URL은 아직 안 줌** → provider에 URL 추가(`_option_matrix_data` option의 `sources[]`에 상품 URL 있는지 확인 후). `src_summary`에 `source_url` 추가.

### B) 상세페이지 ↔ 판매처 연동 데이터 공유 (이력 현재값 시드)
- 현재값(카드)은 이미 상세(`_option_matrix_data`)와 공유됨 ✓.
- 「이력(📈)」이 배포 직후 비어 보임 = 시계열 기록이라 아직 안 쌓임(버그 아님). 표면 노출가가 「–」인 것도 배포 후 첫 「소싱처 업데이트」부터 채워지기 때문.
- **요청**: 이력 열 때 기록 없으면 **상세의 현재값(공유 크롤 데이터)을 '오늘' 첫 줄로 시드** → 즉시 상세와 일치. 크롤로 옵션 바뀌면 그 옵션만 이어 기록(크롤 완료 시 `snapshot-sources` 훅 이미 연결 — `dashboard.js recrawlSources`).

## 작업 규칙
- 워크트리: **origin/main 기준** 새 워크트리 → 커밋 → PR → **사용자 명시 승인("머지해") 후** 머지 → AWS 자동배포(GitHub Actions). force 금지.
- TDD: 백엔드 `cd 프로그램/_시스템 && python -m pytest tests/sets -q`(인메모리 SQLite). 프론트(dashboard.html)는 **라이브 검증(사용자 육안 — 자동화 브라우저 mou-m.com 로그인 미공유)**.
- 로컬 앱 기동 불가(dev=Supabase FK) → Jinja 렌더는 라이브에서.
- design-mockup 스킬: 디자인 변경 **매 턴** 게이트/시안 먼저(변경당). 5개+ 시안=썸네일 갤러리.
- `.sd-wrap`에 `zoom:1.4`(가독성). 레이아웃 폭은 CSS px 기준(시각만 1.4×).

## 추천 순서
1. A) 소싱처 바로가기: 위치 확정(「소」 줄 링크 vs 행펼침) → provider에 소싱처 URL 추가 → 렌더 → PR.
2. B) 이력 현재값 시드 → PR.
