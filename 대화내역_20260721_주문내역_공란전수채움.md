# 대화 내역 — 주문내역 공란 전수 채움 (2026-07-21)

> 세션 기록 내보내기. 작업 워크트리 = `C:/dev/_wt_om_claimfill` (origin/main 기반)
> 관련 파일: `프로그램/_시스템/lemouton/markets/order_export.py` ·
> `shared/platforms/esm/settlements.py` · `tests/markets/test_esm_claim_history_fill.py` ·
> `tests/markets/test_coupang_paid_amount.py` · `tests/markets/test_settle_backfill.py`

---

## 1. 사장님 요청 ①: 옥션 클레임 공란 채우기

> "주문내역 클레임건(취소/완료/반품)에서 상품명, 단가, 판매가 등 공란이 있어. 다 채워줘!
> 오픈마켓 주문번호 기준으로 주문 들어왔던 내역 다 뒤지면 되잖아!
> 1) 다른 마켓들 어떻게 했는지 참고할 것 2) 판매처관리 > 데이터코드지도 > 팝업창 전수 조사"

### 조사 결과
- **다른 마켓**: 스스·롯데온·쿠팡은 클레임 API가 상품명·단가를 직접 줌 → 안 빔.
  **옥션·G마켓(ESM)만** 클레임 응답이 주문번호+상태뿐 → 통째로 빔.
- **데이터코드지도 전수조사의 결정적 발견**: 이미 부르고 있던 **판매대금 정산조회
  (getsettleorder) 응답 안에 주문 시점 실값**이 들어 있었음 —
  `OrderUnitPrice`(단가)·`OrderQty`(수량)·`BuyerPayAmt`(실결제)·`SellOrderPrice`(주문금액).
  그래서 빈 행에도 정산예상금만 찍혔던 것. **추가 API 호출 0회**로 채움 가능.
- `fetch_by_order_no`(주문번호 재조회)는 마켓이 클레임에 0건 반환 → 죽은 경로(기존 확인).

### 구현 (커밋 1d412a30 → 778811f4 → fb0db037, main 배포)
1. **정산 실값 채움**: `settle_detail_map` 신설 — 정산액 합산 + 원주문(Kind=1) 단가·수량·실결제 추출.
   빈 칸만 채움(정상 주문 원본값 절대 안 덮음). 단가·수량이 채워지면 `_finalize_rows`가
   상품금액·주문금액·마켓수수료·수수료율까지 자동 계산.
2. **상품명 4단 폴백** (`fill_claim_blanks_from_history`):
   ①저장분 주문번호(market_order_lines) → ②같은조회 형제행(같은 사이트상품번호 정상주문)
   → ③저장분 상품번호 → ④등록DB(set_channels→product_sets 구성이름).
3. **라이브 검증**: 사장님 스크린샷의 취소완료 2건(2566096092·2565994395)이
   단가 63,400·수량 1·실결제 58,320·수수료 3,162(4.99%)로 채워짐 확인.
   상품명은 이 2건은 "삭제된 상품"이라 마켓이 안 주고 저장분에도 없어 공란 유지(정직).

---

## 2. 사장님 요청 ②: 6마켓 전체 · 최근 1주일 · 모든 주문 공란 채움

> "최근 1주일 기준으로 각 마켓(쿠팡·스스·11번가·롯데온·옥션·G마켓) 일반주문·클레임
> 요청/완료·정산 포함 모든 주문 공란 없이 꽉 채워줘. 안 채워지는 건 데이터코드지도
> 팝업창 전수 조사해서 해결해줘."

### 라이브 감사(7일 392행) → 공란 원인별 해소 (커밋 1b3806fe·5867156b, main 배포)
| 마켓 | 조치 | 전→후 |
|---|---|---|
| 쿠팡 | 실결제 = `orderPrice − discountPrice`(발주서 원본, 지도 확정) | 61건 → 0 |
| 롯데온 | 클레임 구매자·주소·전화·실결제 → 저장분 조인 배선 | 73건 → 39건 |
| 11번가 | 배송중 목록=상세 미제공 → `include_blank_orders`+line_uid 정확일치 조인 | 8건 → 4건 |
| ESM | 반품·교환 연락처 = `PickupInfo.SenderInfo`(수거지=구매자)·`ResendInfo.ReceiverInfo`(재발송 수령인) 매핑 (지도 esm:53·59) | 구현(창에 반품 없어 대기) |
| ESM | 옵션추가금 = `OptSelPrice+OptAddPrice` 매핑 누락 보수 | 14건 → 0 |
| 쿠팡·롯데온 | 옵션추가금 = 구조적 0 명시(옵션 단위=상품 단위) | 245건 → 0 |

- 🔴 **함정 발견·차단**: 이력채움이 .env 없는 테스트에서 폴백 SQLite 잔재 행을 읽으면
  골든테스트가 비결정 오염됨 → `shared.db._is_sqlite`면 건너뛰는 가드.
- 취소조회(esm:50)에는 연락처 필드 자체가 없음 — 지도 전수조사로 확정(저장분 경로가 유일).

---

## 3. 사장님 질문: 미정산 건의 정체와 우회 아이디어

### 분석(라이브 실측 64건)
- **취소완료·취소요청 클레임 44건** — 거래 무산이라 정산할 것 자체가 없음(0이 사실값)
- **11번가 배송중·배송완료 16건** — 구매확정 전. 단, 결제완료 조회 때 받은
  정산예정액(stlPlnAmt)이 저장분에 있음
- **쿠팡 반품완료 2건** — 환불 정산 인식 전(시간 지나면 자동 해소)
- **당일 주문 2건**(옥션·G마켓) — 정산 데이터 미생성

### 사장님 결정
> "1. 확정 / 2번 추가해줘 / 3번 역산으로 추정해줘. (다만 수수료·판매자부담할인·경유 등
> 고려할 것 반드시 확실히! 쿠팡은 무조건 상품 11.55%, 배송비 3% 기준)"

### 구현 (테스트 1006 passed, 커밋·배포는 아래 상태 참조)
1. **① 취소완료 = 정산 0 확정** (`_finalize_rows`, 전 마켓 공통)
   - 정산예정금액 0 · 마켓수수료 0 · `_settle_source="zero_cancel"`
   - 실정산이 이미 있으면(송금후취소 환불 마이너스 등) 절대 안 덮음. 취소요청은 미확정이라 제외.
   - 수수료 파생(실결제−0) 날조 방지 가드 포함.
2. **② 11번가 정산예정액 저장분 물려받기** (`settle_from_store_for_orders`)
   - 정상 행만. 클레임은 정산이 취소·차감되므로 물려받으면 날조 → 제외.
3. **③ 실효 수수료율 역산 추정** (`estimate_settle_from_history`)
   - 비율 = 과거 실정산 ÷ 실결제 (`_settle_source='real'` 행만 재료 — 추정의 복리오차 방지)
   - 실측 비율에 카테고리 수수료·판매자부담할인·경유(제휴·가격비교)가 전부 녹아 있음
   - 같은 상품 평균 우선 → 마켓 중앙값. 비정상 비율(0.5~1.0 밖) 제외. `estimated` 표식.
   - **쿠팡 전용 규칙(사장님 확정)**: 상품 11.55%(0.8845)·배송비 3%(0.97) 분리 적용 +
     **판매자부담할인(즉시+다운로드쿠폰) 차감**(쿠팡지원할인은 쿠팡 보전이라 차감 금지).

---

## 4. 최종 상태

- **배포 완료(main)**: 요청①·② 전체 (커밋 5건: 1d412a30·778811f4·fb0db037·1b3806fe·5867156b + 병합)
- **구현·테스트 완료, 배포 대기**: 요청③(①②③ 미정산 채움) — 회귀 1006 passed
- **남는 공란(정직 유지)**: 미정산 반품 정산(자동 해소 유형)·취소요청(미확정)·안심번호
  미생성 전화·11번가 옵션추가금(목록 API 필드 없음)·저장분에 없는 옛 주문의 상품명/연락처
  (증분적재가 쌓이면 자동 감소 — 옥션·G마켓은 이날 다른 세션에서 라이브검증 승인·공개되어
  스케줄러 적재 대상에 포함됨)
- **발행된 작업 칩**: 옥션·G마켓 주문 증분적재 스케줄 배선 확인

## 5. 이 세션의 교훈(메모리 저장됨)

- 정산조회 응답 = 클레임 빈칸의 보물창고(추가 호출 0회)
- "주문 들어왔던 내역 뒤지기"(저장분 조인)가 마켓이 안 주는 값의 마지막 실데이터 소스
- 폴백 SQLite에서 DB 보강 로직은 반드시 건너뛸 것(골든테스트 비결정 오염)
- 옥션 5초/1콜 — 검증 중 preview.json 직접 fetch 금지(화면 조회와 충돌 → 400/502)
- 본체 워킹트리는 대상 파일이 없음 — 반드시 origin/main 워크트리에서 작업
