# [다른 세션 전달] 자동화 실행 엔진 + 계층형 계수 + 옵션별 브랜드 — 이어받기

> 이 문서를 **그대로 새 세션에 붙여넣어** 이어가세요. 사용자는 비개발자입니다(평이한 한국어 필요).
> 아래 「지금 상태」·「완료」·「확정 결정」·「남은 일 체크리스트」·「안전 수칙」을 정확히 인지한 뒤 작업하세요.

---

## 0. 한 줄 요약
`/automation`(자동화 설정) 화면이 **저장만 되고 실제로 안 돌던 것**을 실제로 돌게 만드는 **자동화 실행 엔진(연속 배수 큐)** 워크스트림. 서버측은 거의 다 완성(TDD·리뷰), **화면·라이브·머지가 남음**. 여기에 **계층형 계수(소싱처>브랜드>모음전>URL)**와 **옵션별 브랜드 지정**이 설계 확정돼 붙었음.

## 1. 지금 상태 (위치)
- **작업 워크트리**: `C:\dev\모음전 프로젝트\_wt_auto_engine`  · **브랜치**: `feature/automation-engine` · **HEAD**: `7658540e` (워크트리 깨끗, 미커밋 없음)
- ⚠️ **origin/main 기반**. 메인 폴더(`C:\dev\모음전 프로젝트`)가 올라탄 `feature/crawl-worker-queue`는 stale(73커밋 뒤) — **폐기 권고, 폴더는 origin/main 최신으로 맞출 것**.
- **로컬 서버 가동 중**: `http://127.0.0.1:5052` (이 브랜치 코드, `DISABLE_SCHEDULER=1 DISABLE_AUTH=1 FLASK_PORT=5052 python app.py`). ⚠️ **템플릿 수정 시 debug off라 서버 재시작해야 반영**.
- **크롬 확장 실제 최신 = `C:\Users\seung\Desktop\moum-crawler-v0.7.14`** (0.7.13 비파괴 복사 + 자동화 폴링). 리포 `extension/moum-crawler`(0.7.5)는 8버전 stale — 폴더명↔manifest version 불일치 잦음(manifest 직접 확인). [[reference_loaded_extension_path]]
- **메모리**: `[[project_automation_engine_continuous_weighted_queue]]`, `[[project_option_level_brand_assignment]]`.
- **스펙/플랜**: `docs/superpowers/specs/2026-07-04-자동화-연속배수큐-실행엔진-design.md`, `.../2026-07-05-계층형-계수-...-design.md` + `docs/superpowers/plans/`의 P1~P5·P3b·P4b·계층형계수 플랜.
- **시안(Desktop)**: `모음전 시안 자동화-실행정지 v1/v2` · `자동화-화면(P5) v2~v4` · `계수 드릴다운 4단계/기준3종 v4` · `옵션별 브랜드지정 v1` · `일괄적용 체크조합 v2`.

## 2. 완료 (서버측, 전부 서브에이전트 TDD + 2중 리뷰 · 400+ pass)
- **P1** 데이터+URL별 변동기록: `CrawlDelta`·`crawl_weight`·`no_change_streak`·`detect_changes`·`changed_product_ids_since`. ★크롤 None재고=저장상태 반영(품절둔갑 방지).
- **P2** 연속 배수 큐: `crawl_schedule.py` `effective_interval_seconds`(기준÷계수×완화 min(1+streak*0.5,4))·`due_products`(연체순)·`select_due_products`.
- **P3a** 큐 API: `due_crawl_payload`+`GET /api/crawl/queue`(읽기전용, 실행/정지 게이트).
- **P3b 서버**: `due_bundle_codes`(마감URL→번들코드, normalize_url 양쪽)+`GET /api/crawl/due-bundles`. **확장 폴링루프는 0.7.14에 얹음(라이브 검증됨 — 토글→폴링→기존 크롤 실행 로그 확인).**
- **P4b** 계정별 속도: `AccountUploadPolicy`(seconds_per_item)·`get/set_account_policies`·`seconds_to_hourly`·`market_hourly_total`·`GET/POST /api/upload/account-speed`. (P4 마켓단위는 이걸로 대체·정리 완료 = `458bcb77`)
- **P5 계수 저장**: `set_crawl_weight`+`POST /api/sources/crawl-weight`.
- **실패 유형 분류**: `classify_crawl_failure`(차단/로그인/옵션/응답지연/유형외)+`list_crawl_failures`+`GET /api/crawl/failures`.
- **화면 묶음A(자동화 페이지, `3c6ba579`)**: ③계정 속도표·⑤실패 유형화·실행중 링 — **커밋됨, 렌더 스크린샷 확인 미완**(서버 재시작 후 `http://127.0.0.1:5052/automation` 새로고침해 확인 필요).
- **실행/정지 토글→확장 폴링 배선**(`ext_bridge.js` `MoumExt.autoPollStart/Stop`).
- **계층형 계수(서버, `f8870d6e`~`7658540e`)**: `CrawlWeightRule(scope_type source|brand|model|url, scope_key, weight1~5)`·`set_crawl_weight_rule`(해제=삭제)·`resolve_crawl_weight`(URL>모음전(최고)>브랜드(최고)>소싱처>기본1, 공유URL 최고, normalize_url)·`due_products`가 이걸 호출·`list_weight_rules`·`GET/POST /api/crawl/weight-rule(s)`. ★기존 tests/sources 29 FK오류(account_upload_policies)도 conftest에 multitenancy import로 해결.

## 3. 확정된 설계 결정 (사용자와 협의 완료 — 바꾸지 말 것)
- **엔진 = 연속 배수 큐**: URL단위(중복제거), 오래된순+URL별 계수(1~5), PC1대 최대치+워커확장(CrawlJob/CrawlWorker 뼈대), 크롤=로컬/업로드=서버, 무변동 자동완화.
- **실행중 표시 = 4번 시안**(카운트다운 링+실시간 로그, 연속이라 링=진행률). 실행=초록/정지=회색.
- **마켓 속도 = 계정(API)별** "1개당 몇 초"(초 클수록 천천히=안 막힘), 시간당개수=3600÷초, 마켓별 "총 스토어 업로드수"=켜진 계정 합. (시안 P5 ③1번)
- **실패 = 유형 컬럼+상세(옵션>URL)**, 유형 5종.
- **계수 계층 = 소싱처>브랜드>모음전>URL**, **가장 세부 우선**, 기본 ×1, 공유URL은 최고. **드릴다운 화면 = 파인더(열이동) + 진입 기준 3종 토글(소싱처 기준5열/브랜드 기준4열/모음전 기준3열)** + 옵션 단계 포함. (시안 「드릴다운 기준3종 v4」)
- **옵션별 브랜드**(★핵심 신규): 한 모음전에 **여러 브랜드 섞임** → `Option`에 brand 필드 추가(현재 `Model.brand` 상속·기본값 르무통). **르무통 기본 제거**(미지정 표기). UI확정: **지정모드=상단 토글(단일↔옵션별)** · **옵션별=배지클릭 검색 팝오버(있으면 검색·없으면 「+새 브랜드」)** · **일괄적용=스마트 바(체크N개 적용 / 안 고르면 전체·빈것만, 미지정 개수 표시)**. 모음전 등록 시 브랜드 필수화. [[project_option_level_brand_assignment]]

## 4. 라이브 검증 상태
- ✅ **확장 자동 크롤 배선 실증**: 로컬 서버 + 확장 0.7.14 로드 후 「크롤 자동 주기」 토글 ON → 확장이 `/api/crawl/due-bundles` 폴링 → 기존 크롤 흐름(crawl-reset/option-matrix/finalize) 자동 실행 로그 확인(단, dev DB 거의 비어 있어 0/0 — 테스트 찌꺼기 `DUEB001`도 있음).
- ❌ **화면 렌더 미확인**: 묶음A(속도표·실패·실행중)·계수/브랜드 화면은 스크린샷/라이브 확인 안 됨.
- ⚠️ **dev(로컬) DB 거의 비어 있음**(모음전 1개·브랜드 1종 르무통) → 브랜드 다양성 등 실데이터는 프로덕션에서만 확인 가능. 테스트가 SessionLocal로 dev DB에 찌꺼기(example.test·DUEB001) 남김 → **정리 필요**.

## 5. 남은 일 — 체크리스트 (해야 할 것 정확히)

### A. 옵션별 브랜드 (설계확정, 미구현 — 계수 브랜드기준의 전제라 먼저)
- [ ] `Option.brand` 컬럼 추가 + 마이그레이션 + `Model.brand` 르무통 기본값 **제거**(미지정 허용)
- [ ] 브랜드 검색·목록 함수(있는 브랜드 distinct + 「+새 브랜드」 추가), per-option 브랜드 저장
- [ ] 일괄적용 로직: **선택 N개 / 전체 / 빈 것만(미지정)** + 모음전 전체 ONE 브랜드
- [ ] 모음전 등록 시 브랜드 필수화(르무통 자동 채움 금지)
- [ ] API 라우트(옵션 브랜드 조회/저장/일괄/검색)
- [ ] (프론트·라이브) 모음전 구성 탭에 확정 3 UI(상단토글·검색팝오버·스마트 일괄바)

### B. 계수 계층 드릴다운 화면 (서버 done, 화면 미구현)
- [ ] 트리 데이터 백엔드(소싱처/브랜드/모음전 3기준 각 계층 + 유효계수) 조회
- [ ] 파인더 화면(진입 기준 3종 토글 + 옵션 단계 + 각 노드 「−×N+」·직접/상속 배지·「기본으로」)
- [ ] 기존 `SourceProduct.crawl_weight` 컬럼 → URL 규칙 일회성 마이그레이션(운영 데이터 확인 후)

### C. 화면 마무리 (묶음A·매트릭스·실행중)
- [ ] 서버 재시작 후 `/automation` 스크린샷으로 묶음A 렌더 확인·정렬 교정
- [ ] ① 계수 스텝퍼(「−×3+」+"1회당 N회전")를 **매트릭스** 화면에 — ⚠️ `_matrix_v3.html`은 7766줄·돈 걸린 화면·편집 함정([[reference_flow_html_encoding_traps]]) → 구조 읽고 라이브로 조심히. (계수 위치=계층 드릴다운으로 대체됐으니 매트릭스 스텝퍼는 재검토)
- [ ] 실행중 4번 시안(링+로그) 실제 크롤 데이터로 확인

### D. 라이브·정리 (사용자 승인 필요)
- [ ] dev DB 테스트 찌꺼기 정리(example.test·DUEB001)
- [ ] 확장 리포 동기화(리포 0.7.5 ↔ 실제 0.7.14) — 배포/버전관리 유실 방지
- [ ] **전체 main 머지 + 배포 + 라이브 검증** — ⚠️ 프로덕션=실제 돈·계정, **사용자 명시 승인 필요**. 자동 전송은 기본 「미리보기만」(안전).

## 6. 안전 수칙 (반드시)
1. **크롤=로컬 PC 원칙**(확장/Playwright), 업로드=서버. "서버 크롤 실패"=정상. 서버 크롤 부활 제안 금지.
2. **재고·가격 = 실브라우저 소싱처 URL 실데이터로만**. 폴백가 금지·확인 못하면 "확인 불가". (CLAUDE.md 3대 원칙)
3. **화면(특히 매트릭스)은 눈으로 검증 없이 단언 금지** — 사용자 스크린샷/라이브로 칸별 대조. 매트릭스는 돈 걸린 화면이라 blind 편집 위험.
4. **디자인 변경 = design-mockup 게이트 먼저**(항목별 결정). 새 기능·설계 = brainstorming.
5. **확장은 실제 최신(Desktop 버전폴더) 위에** 얹기 — 리포 사본 통째 복사 금지(회귀).
6. **한 워크트리 = 한 세션**(동시 편집 clobber 방지).

## 7. 다음 첫 행동 추천
사용자와 **(A) 옵션별 브랜드 백엔드**부터 TDD로 이어가는 것 추천(계수 브랜드기준의 전제, 서버측이라 안전). 시작 전 `_wt_auto_engine`에서 `git log/status`로 현재 상태 대조하고, 로컬 서버(5052)·확장(0.7.14)은 이미 떠 있음을 확인.
