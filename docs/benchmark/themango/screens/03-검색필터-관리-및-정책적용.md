# [실화면] 상품수집조건(검색필터) 관리 및 정책적용 — **더망고의 심장**

## 진입경로
`정책적용 > 상품수집조건(검색필터) 관리 및 정책적용`
**URL**: `/mall/admin/admin_group.php`
**L4 팝업 2종**: `admin_category_set.php?tm=F&ps_ftid=<필터ID>` (필터세부설정) · `admin_group_modify.php` (수집조건수정)
**실측일**: 2026-08-06

---

## ★★★ 화면이 직접 선언한 정책 우선순위 — **4단**

> **"정책은 [상품별 개별정책] → [상품수집조건(검색필터) 정책] → [카테고리 정책] 순으로 적용된 정책이 우선 적용됩니다."**
> "(즉, A라는 상품에 위의 3가지 정책이 모두 다르게 적용된 경우 **[상품별 개별정책]에 적용된 정책이 적용**됩니다.)"
> **"아무런 정책이 적용되지 않은 경우 환경설정을 통해 설정한 기본정책이 기본 적용됩니다."**

```
① 상품별 개별정책          (최우선)
② 검색필터 정책
③ 카테고리 정책            (상위→하위 상속)
④ 환경설정 기본정책        (최후 폴백)
```

---

## L3 — 화면 구조

### 가로탭 2개
`✔ 상품수집조건(검색필터) 관리 및 정책적용` | `✔ 카테고리 관리 및 정책적용`

### 검색 조건 (7종)
| 항목 | 형태 |
|---|---|
| 날짜 기준 | select `최근수집일` 외 |
| 기간 | 년·월·일 × 2 + ☐`날짜 조회 체크` |
| 상품수집사이트 | select |
| **매출발생 여부** | select — 🎯 *돈 번 필터만 골라보기* |
| 검색필터 범위 | select `전체 검색필터` |
| 검색 종류 | select `검색필터명 검색` |
| 키워드 | text + `[선택조건으로 검색하기]` |

### 목록 툴바
`[검색필터 삭제]` · `[일괄작업 ▾]` · `50개씩 보기` · `최근수집일순` · `[검색필터 수동등록]`

**일괄작업 메뉴 (JS 실측)**
| 항목 | 함수 |
|---|---|
| 검색필터 그룹핑 | `merge_filter('G')` |
| 검색필터 그룹핑 해제 | `bulk_group_restore()` |

---

## L4 — 목록 표 (8열)

| 열 | 내용 |
|---|---|
| **사이트(필터종류)** | 🎯 **한 행에 여러 소싱처가 들어간다** (MUSINSA.com + SSG.com) |
| **필터이름(수정가능)** | 인라인 텍스트박스 + `[그룹핑 해제]`(빨강) `[그룹핑 상품확인]` |
| **상품정책적용** | 정책 select + `[적용]` `[하기]` |
| **필터세부설정** | `[설정하기]` → `market_mapping_new(필터ID)` 팝업 |
| **검색필터(저장조건)** | 아래 상세 |
| **저장상품/유지율<br>(누적매출금액(원))** | 🎯 아래 상세 |
| **추가기능** | `[필터복사]` `copy_filter(id)` · `[필터라이브러리]` |
| **최근수집일자(필터생성일)** | |

### 「검색필터(저장조건)」 열 = **실제 URL 원문이 그대로 보인다**
```
URL 검색: https://www.musinsa.com/search/goods?keyword=언더아머&keywordType=keyword&gf=A&brand=u…
        | 수집개수: 3000개 | 전체저장 | [수집조건수정] | 그룹핑 전 필터명: 무_언더아머

URL 검색: https://www.ssg.com/search.ssg?query=…&shpp=department&…
        | 수집개수: 6000개 | 전체저장 | [수집조건수정] | 그룹핑 전 필터명: 언더아머
```
⇒ 필터 1개 = **(소싱처 URL + 수집개수 + 저장방식)** 한 벌. `[수집조건수정]` = `modify_filter(필터ID)`

### 🎯🎯 「그룹핑」 — 여러 소싱처 필터를 하나로 묶는다
- `group_restore('726','715|726')` → **그룹 멤버를 `715|726` 파이프 문자열로 보관**
- 묶이면 각 필터의 원래 이름은 `그룹핑 전 필터명`으로 보존
- 통합 이름(`언더아머`)으로 정책·세부설정·카테고리매핑을 **한 번만** 걸면 양쪽 소싱처에 적용

🟢 **채택**: 같은 브랜드를 무신사·SSG 양쪽에서 긁을 때 정책을 두 번 만들 필요가 없다.
🔵 **접합**: 우리 「구성(소싱처+브랜드)」 개념과 결이 같다. 다만 우리는 **소싱처 비교로 최저 매입가를 고르는** 데 쓰고, 더망고는 **정책을 공유하는** 데만 쓴다 — 우리가 한 단계 위.

### 🔴🔴🔴 정정 (8번째, 가장 심각) — 컬럼은 「유지율」이 아니라 **「휴지통」**이다

**틀린 판독**: 저해상도 스크린샷 OCR 로 컬럼 헤더를 `저장상품/유지율` 로 읽고
*"유지율 3.9% — 수집한 상품의 78~100%가 죽는다"* 라는 결론을 냈다. **정반대였다.**

**실제 헤더** (`get_page_text` 로 확인): **`저장상품/휴지통 (누적매출금액(원))`**

### 올바른 실측
| 필터 | 소싱처 | 저장상품 | **휴지통** | **생존율** | 누적매출 |
|---|---|---|---|---|---|
| 언더아머 | MUSINSA | 1,106개 | 43개 | **96.1%** | 0원 |
| 언더아머 | SSG | 3,120개 | 704개 | **77.4%** | 0원 |
| 피엘라벤 | MUSINSA | 571개 | 0개 | **100%** | 0원 |
| 피엘라벤 | SSG | 1,472개 | 1개 | **99.9%** | 0원 |
| 타미진스 | MUSINSA | 1,736개 | 0개 | **100%** | 0원 |
| 타미진스 | SSG | 372개 | 0개 | **100%** | 0원 |
| 나이키 rnwhgowh7 | MUSINSA | 1,491개 | 15개 | 99.0% | **207,060원** |
| 나이키 rnwhgowh7 | SSG | 2,307개 | 34개 | 98.5% | **221,500원** |
| 룰루레몬 | MUSINSA | 2,127개 | 4개 | 99.8% | **548,560원** |

⇒ **수집한 상품은 대부분 살아 있다.** 「78~100%가 죽는다」는 완전한 오독이었다.
⇒ 그렇다면 **저장 한도 20만이 병목인 이유가 달라진다** — 죽어서 쌓이는 게 아니라 **계속 새로 긁어서 차는 것**이다. 「연속 품절 일괄삭제」·「수집제한」은 *시체 치우기*가 아니라 **한도 관리 도구**다.

### 🎯 여전히 유효한 것 — **필터별 누적매출금액**
| 관찰 | 뜻 |
|---|---|
| 필터 9개 중 **매출이 난 것은 3개**(나이키×2, 룰루레몬) | **긁는다고 팔리지 않는다** |
| 룰루레몬 2,127개 → 548,560원 | 상품당 258원 |
| 매출 0원 필터가 다수 | **어떤 검색어가 돈이 되는지**를 이 숫자가 알려준다 |
| 정렬에 **`매출금액순`** 있음 · 필터에 **`매출이 발생한 필터만 보기`** 있음 | 더망고도 이걸 핵심 지표로 본다 |

⇒ 🟢 **채택 확정**: 우리에게 **「검색조건 단위 매출」 축이 없다.**
「몇 개 긁었나」가 아니라 **「이 검색어가 얼마를 벌었나」**가 대량등록의 성적표다.

### ★ 이 사고에서 배운 규율
🔴 **스크린샷 OCR 로 숫자·컬럼명을 읽고 결론 내지 말 것.**
작은 글씨·저대비 표는 반드시 **`get_page_text`(텍스트 추출)** 로 재확인한다.
이번 건은 판독 오류 하나가 **「대량등록은 78~100%가 죽는 사업」**이라는 완전히 틀린 세계관을 만들었고, 그걸 메모리에까지 저장했다.

🎯 **누적매출금액(원)** — 검색어(=검색필터) 단위 매출. **어떤 검색어가 돈을 벌었는지**가 보인다. 우리에게 없는 축.

---

## L5 — 「필터세부설정」 팝업 (`admin_category_set.php?tm=F&ps_ftid=726`)

### 🔴🔴 중대 정정 — 더망고에는 **베이스 + 오버라이드 상속이 있다**

> **"본 검색필터에 위와 같은 정책을 설정하시면, 해당 검색필터에 포함된 상품은 위 적용한 정책이 우선적용됩니다."**
> **"※ 설정하지 않은 값은 정책에 있는 값이 이용됩니다."**

⇒ 앞선 대조지도에서 *"더망고엔 상속 구조가 없어 복사본 138개가 남발된다"* 고 적었으나 **정확하지 않았다.**
**필터 레벨에서 항목 단위 부분 오버라이드가 가능하다.** 그런데도 복사본이 쌓인 이유는 **오버라이드 가능한 항목이 8개뿐**이고 마켓정책 21항목은 못 덮기 때문으로 보인다. → 우리는 **오버라이드 범위를 넓게** 잡으면 된다.

### ① 검색필터 세부설정 (8항목, `[설정저장]` 별도)
| 항목 | 형태 | 실측값 |
|---|---|---|
| 선택 검색필터명 | 표시 | `언더아머` |
| **검색필터 적용정책** | select + `[정책적용]` `[정책적용취소]` | `언더아머_타임` |
| 검색필터 국제운송료 | 원 + 배송무게 + LB/KG | 빈칸 |
| **검색필터 마진율/마진금액** | % / 원 | 빈칸(=정책값 사용) |
| 검색필터 상세페이지 템플릿 | select + `[상세페이지 템플릿 보기]` | `-- 미설정 --` |
| 검색필터 상품/옵션명 정책 | select + `[상품/옵션명 정책 보기]` | `-- 미설정 --` |
| 검색필터 번역정책 | select + `[번역정책 보기]` | `-- 미설정 --` |
| 검색필터 워터마크정책 | select + `[워터마크정책 보기]` | `-- 미설정 --` |
| **검색필터 금지어 설정** | select + `[금지어 템플릿 관리]` | `-- 미설정 --` |

> *"검색필터 금지어는 상품수집시 상품저장조건에서 적용한 금지어 설정입니다."* — 수집 시 고른 금지어 템플릿이 필터에 눌러붙는다.

### ② 마켓 검색태그 설정 (`[설정저장]` 별도)
- **가로탭 2개**: `마켓통합검색태그 설정` | **`마켓별 검색태그 설정`**
- 콤마(,) 구분 입력 + `(0개 태그)` 실시간 카운터
- 🎯 *"마켓마다 지원하는 태그개수가 다를 수 있으며, 다를 경우에는 **앞에서부터 허용하는 태그 개수만큼 전송**됩니다."*
- *"카테고리/브랜드/판매처명이 포함된 태그는 등록되지 않습니다."*
- *"판매상품과 직접 관련 없는 태그를 입력 시 **판매금지** 또는 마켓 관리자에 의해 삭제/변경될 수 있습니다."*

### ③ 전송마켓 카테고리 매핑 설정 (`[설정저장]` 별도)
| 항목 | 내용 |
|---|---|
| 🎯 **[AI] 마켓카테고리 자동 매핑** | `[AI 자동 매핑 시작하기]` — *"본 검색필터에 포함된 상품과 가장 유사한 마켓 카테고리를 **5개 추천**합니다"* |
| **더망고 카테고리 검색** | text + `[검색]` `[전체카테고리]` + `- 매핑 대상의 더망고 카테고리를 선택하세요 -` |
| **마켓카테고리 전체검색** | text + `[전체마켓검색]` — 한 번에 6마켓 검색 |

#### (오픈) 마켓 카테고리 매핑 — 6마켓 각각
행 구성: `검색어 입력` + `[검색]` `[전체카테고리]` `[상품고시보기]` + 🔴**`[카테고리 미매핑]` 빨간 배지** + 전송대상 카테고리 select

| 마켓 | 특수 규칙 |
|---|---|
| 옥션2.0 | — |
| G마켓2.0 | — |
| **11번가** | ◉해외카테고리 / ○국내카테고리 — *"11번가는 해외/국내카테고리가 다릅니다"* |
| 스마트스토어 | — |
| 쿠팡 | — |
| **롯데ON** | ○해외직구 카테고리 / ◉일반카테고리 — *"롯데온은 일반/해외직구 카테고리가 다릅니다"* |

하단: `[검색필터 설정저장 (Alt+S)]` · `[검색필터 설정삭제]`

> 🔴 **정정 2**: 앞서 *"카테고리 매핑 없으면 조용히 전송에서 빠진다"* 고 적었으나, 실화면은 **`[카테고리 미매핑]` 빨간 배지로 마켓마다 표시**하고 있다. 조용하지 않다.
> 다만 **정책관리 화면의 경고**(*"카테고리 매핑 사용을 체크했을 때 설정된 마켓카테고리가 없을 경우에는 마켓에 전송되지 않습니다"*)는 여전히 유효 — **전송 시점에 몇 건이 빠졌는지는 안 알려준다.** 우리는 preflight 집계까지 붙인다.

---

## 서버요청 (실측)
| 대상 | 방식 |
|---|---|
| `admin_group.php` | 목록 |
| `admin_category_set.php?tm=F&ps_ftid=<필터ID>` | 필터세부설정 팝업 (`tm=F` = 필터 모드로 추정, 카테고리 모드는 다른 값) |
| `admin_group_modify.php` | 수집조건 수정 팝업 |

### JS 함수 지도 (실측)
`merge_filter('G')` · `bulk_group_restore()` · `group_restore(필터ID, '멤버|멤버')` · `quick_create_filter()` · `market_mapping_new(필터ID)` · `modify_filter(필터ID)` · `copy_filter(필터ID, this)` · `toggle_bulk_menu(event)`

## 추론 데이터모델
```
검색필터(search_filter)
  ps_ftid PK · 소싱처 · 필터명 · 원본필터명(그룹핑 전) · 그룹키('715|726')
  · 수집URL · 수집개수 · 저장방식(전체저장/선택저장)
  · 정책FK
  · 오버라이드: 국제운송료·배송무게 · 마진율·마진금액 · 상세템플릿FK
                · 상품옵션명정책FK · 번역정책FK · 워터마크정책FK · 금지어템플릿FK
  · 검색태그(통합) · 검색태그(마켓별) 1:N
  · 마켓카테고리매핑 1:N (마켓 · 카테고리코드 · 국내/해외 구분)
  · 집계: 저장상품수 · 유지상품수 · 누적매출금액 · 최근수집일 · 생성일
```

## 모음전·대량등록 대응물
| 더망고 | 우리 실코드 | 판정 |
|---|---|---|
| 검색필터 = 수집 행위의 영구 객체 | ❌ 없음 (`draft_from_url` 은 URL 1건→초안 1건) | 🔴 **신설 최우선** |
| 정책 4단 상속 (개별>필터>카테고리>기본) | 평면 (`process` 정책↔소싱처URL) | 🔴 **상속 신설** |
| 필터 레벨 **부분 오버라이드 8항목** | ❌ | 🟢 **채택 — 범위를 더 넓게** |
| **그룹핑**(여러 소싱처 필터 병합) | 「구성(소싱처+브랜드)」 개념 존재 | 🔵 우리가 상위 개념 보유 |
| **저장상품/유지율** | ❌ | 🔴 **신설 — 대량등록의 성적표** |
| **검색필터별 누적매출금액** | ❌ | 🔴 **신설** |
| **매출발생 여부 필터** | ❌ | 🟢 채택 |
| 필터복사 · 필터라이브러리 | ❌ | 🟡 검토 |
| 마켓 검색태그(통합/마켓별, 앞에서부터 잘림) | ❌ | 🟢 채택 |
| AI 카테고리 5개 추천 | `category_map.py` 제안엔진(자동확정 금지) | 🟡 추천 UX 채택 |
| **더망고 카테고리 = 중간 표준층** | 소싱처↔마켓 2단 | 🟢 **채택** |
| 11번가 해외/국내 · 롯데ON 해외직구/일반 구분 | 🔴 **우리 사전엔 구분 컬럼이 없다** (아래) | ⚠️ **검증 과제** |

### ⚠️ 코드 확인 결과 — 11번가·롯데ON 「국내/해외」 이원 카테고리
`lemouton/registration/models.py:167 MarketCategory` 컬럼:
`market · code · name · full_path · parent_code · depth · is_leaf · extra_code(ESM 전용 sd코드) · raw_json · harvested_at · removed_at · child_count`

⇒ **국내/해외를 구분하는 컬럼이 없다.** 수집기도 단일 집합만 가져온다:
- `category_harvest.py:51 parse_eleven11` — *"11번가 **전체 카테고리** XML"* (한 벌)
- `category_harvest.py:352 harvest_lotteon` — *"롯데온 **표준카테고리**"* (한 벌)

**판정 = 「확인불가」** (일치도 불일치도 아님). 확인해야 할 것:
1. 우리가 수집한 11번가 `dispNo` 집합이 **국내용인지 해외용인지** — 마켓 API 원문으로 확인
2. 롯데온 `std_cat_id` 표준카테고리가 **일반/해외직구 중 무엇인지**
3. 우리는 **국내 소싱·국내배송**이므로 **국내(일반) 카테고리가 정답** — 해외 집합을 수집했다면 등록이 틀린 카테고리로 나간다

🔴 **틀린 카테고리로 등록되면 마켓에서 카테고리 변경이 불가능**하다(더망고 매뉴얼 `110`이 명시). **삭제 후 재등록만이 유일한 복구**다. 대량등록에선 수천 건이 한 번에 잘못 나갈 수 있으므로 **대량등록 실전송 전 반드시 확정할 것.**
| `[카테고리 미매핑]` 빨간 배지 | ❌ | 🟢 채택 + preflight 집계 추가 |
