# 10 — 통합 대량등록 설계 초안

> 채택 18개(§08)를 **우리 구조**(모음전 22화면 + 대량등록 9탭) 위에 어떻게 얹을지.
> 작성 2026-08-06 · 브랜치 `docs/themango-benchmark`
> 앞 문서: [08 채택목록](08-PhaseB-완료-전체채택목록.md) · [09 카테고리 실측](09-확인과제-카테고리-국내해외-실측.md)
> 🔴 **더망고는 건드리지 않는다.** 읽기만 한다. 이 문서의 변경은 전부 **우리 프로그램** 이야기다.

---

## 0. 이 문서가 정하는 것 / 안 정하는 것

| 정한다 | 안 정한다 (다음 문서) |
|---|---|
| 새로 만들 **개체 3개**와 **확장 2개** | 화면 그림·시안 (→ `/design-mockup`) |
| 정책 **상속 층**을 어떻게 세우나 | 함수 단위 구현 계획 (→ 구현 플랜) |
| 채택 18개가 **9탭 중 어디에** 앉나 | 마이그레이션 SQL |
| **재구현 금지** 명부 | 이관 실행 절차 (→ 다른 세션) |
| 단계(Phase) 순서와 이유 | |

---

## 1. 사장님 확정 — 이번 세션에서 새로 받은 7건

| # | 항목 | **확정** |
|---|---|---|
| 1 | 신규 소싱처 | 🟢 **5곳 전부** (GSShop · ABCmart · GrandStage · 29CM · FashionPlus) |
| 2 | 옵션 상한 대응 | 🟢 **잘라 보내되 「자를 순서」를 사람이 고른다** |
| 3 | 마켓 전송 재고 | 🟢 **10개** (⚠️ 상한이지 하한 아님 — 0은 0으로) |
| 4 | 랜덤키워드 | 🟢 **만들되 기본은 꺼 둔다** |
| 5 | 마켓분산등록 | 🟢 **넣는다** |
| 6 | 더망고 API 유료 | 🔴 **신청 안 함** — 마켓 API 로 우회 |
| 7 | 롯데온 카테고리 | 🟢 **둘 다** — 막이개(preflight) 먼저 + 「전시카테고리 직접지정」은 2차 |

앞 세션 확정 8건(§이관문서 3)은 그대로 유효하다.

---

## 2. 🔴 설계 전 정정 2건 — 실측이 앞선 진단을 뒤집었다

### 정정 ① 「정책 시스템이 두 벌」 — **항목 정의는 이미 한 벌이다**

§08-6 과 §README-⑤ 는 우리 진짜 숙제를 *"정책 시스템이 두 벌 → 배울 진짜는 두 벌 합치기"* 라고 적었다.
**절반만 맞다.** 실코드는 이렇다:

```python
# lemouton/policy/fields.py:18   ← 모음전 정책
from lemouton.registration.process_rule_schema import all_schemas
...
# :82~83
def base_items():
    """대량등록 가공 규칙 13항목 — 정의를 그대로 가져온다(베끼지 않는다)."""
    return [dict(s, only=None) for s in all_schemas()]
```

**모음전 정책 화면은 대량등록의 13항목 스키마를 직접 불러다 쓴다.** 베낀 게 아니라 그대로 쓴다.
`required.py` 의 마켓별 필수 표시도 같은 13개 키(`name`·`category`·`price`·`options`·`images`·`detail`·`shipping`·`notice`·`brand`·`origin`·`kc`·`tags`·`banned_words`)를 쓴다.

**저장 그릇의 모양도 사실상 같다:**

| | 모음전 | 대량등록 |
|---|---|---|
| 헤더 | `MarketPolicy`(name·brand·enabled_markets·is_default) | `ProcessPolicy`(name·description) |
| **값** | `MarketPolicyValue`(**policy_id · market · field_key · value** · `from_common_at`) | `ProcessRule`(**policy_id · market · item_key · config**) |
| 마켓 축 | `market` 칸 | `market=''` 공통 / `market='coupang'` 덮어씀 |

→ **같은 표를 이름만 다르게 두 벌 들고 있는 것.**
게다가 대량등록의 `ProcessRule` 은 이미 **「공통 < 마켓별」 2단 상속**을 한다.

> **그래서 진짜 숙제는 「두 벌 합치기」가 아니라 「붙이는 층 하나로 모으기」다.**
> 항목 어휘도 값 그릇 모양도 이미 같다. **다른 건 「무엇에 붙느냐」 뿐이다.**
> 합치는 비용이 겁냈던 것보다 훨씬 작다.

### 정정 ② 롯데온 — 「구분 컬럼이 없다」가 문제가 아니었다

§09 참조. 롯데온 등록은 카테고리를 **우리가 고르지 않는다.**
`compile_more.py:148 compile_lotteon` 이 「본보기 기존 상품번호(`LO…`)」를 받아 **그 상품의 카테고리·고시·배송·출하지를 통째로 상속**한다.
→ 컬럼을 채워도 그 값이 등록에 안 실린다. **막이개가 먼저다.**

---

## 3. 뼈대 — 새 개체 **3개** + 기존 확장 **2개**

> 새로 만드는 표는 **3개뿐**이다. 나머지는 이미 있는 것을 넓힌다.
> 🔴 재구현 금지 명부는 §7.

### 3-1. 🆕 **검색필터** (`SearchFilter`) — 대량등록의 중심축

**푸는 문제** — 「무신사에 나이키 검색한 결과 전부」라는 **수집 행위를 영구 개체로** 만든다.
지금 우리는 URL 1개 → 초안 1개(`draft_from_url.py`)뿐이라 **리스팅 URL → 다건이 통째로 없다.**

| 칸 | 뜻 |
|---|---|
| `name` | 자동 생성 (`무신사_나이키_001`). 사람이 고칠 수 있음 |
| `source_key` · `listing_url` | 어느 소싱처의 어느 검색 결과인가 |
| `max_items` | **저장상품수 상한** — 몇 개까지만 담을지 |
| `page_from` · `page_to` | **저장 페이지 범위** |
| `banned_template_id` | 수집 시점 **금지어 템플릿** |
| `option_exclude_words` | **옵션 수집 제외** (이 말이 든 옵션은 안 담음) |
| `apply_policy_id` | **수집 즉시 정책 적용** |
| `last_run_at` · `last_new_count` | 마지막 실행·새로 들어온 수 |
| `deleted_at` | 🔴 **필터를 지워도 상품은 안 지운다** (더망고 함정 §08-4) |

**상품과의 연결** — `ProductDraft.search_filter_id` 칸 하나 추가(비어 있어도 됨).
수기 등록 상품은 비어 있고, 검색필터로 들어온 상품만 채워진다.

**🔵 우리 강점과 접합** — `CrawlWeightRule.scope_type` 이 이미 `source｜brand｜model｜url` **4단**을 지원한다.
**여기에 `search_filter` 를 5번째로 얹으면** 가중 랩·계수 엔진이 **그대로 재사용된다.**
→ **「신규상품수집」은 새 기능이 아니라 「검색필터를 랩에 태우는 것」이다.**

### 🔴 실행 배선 정정 (2026-08-06 구현 중 실측)

이 문서는 앞서 *"실행은 `CrawlJob`(서버가 등록 → 로컬 워커가 선점·실행)을 그대로 탄다"*고 적었다.
**틀렸다. `CrawlJob` 큐는 소비하는 워커가 저장소에 없다.**
근거 — `webapp/routes/sourcing_guide.py:975~977` 원문:
*"종전 「검증」 버튼은 crawl_queue 에 잡을 넣었지만 그 큐를 소비하는 워커가 저장소에 없어 영원히 '대기'였다."*

**살아 있는 경로는 하나뿐** — 확장이 `/api/crawl/due-bundles` 를 1분 알람으로 폴링하고
(`extension/moum-crawler/background.js:2363`) 결과를 `/api/sources/crawl-result` 로 민다.
→ 검색필터도 **그 옆에** 붙였다: `GET /api/crawl/due-listings` · `POST /api/crawl/listing-result`.
🔴 **앞으로 크롤 성격의 신기능은 전부 이 자리에 붙여야 한다.** `CrawlJob` 은 쓰지 않는다.

### 🟢 찾은 주소 → 상품 (2026-08-07 구현 완료)

**빠진 고리는 한 칸뿐이었다** — 나머지는 이미 있었다.

```
찾은 주소(SearchFilterItem)
    │ ① 크롤 대상 등록  ← 이것만 새로 만듦 (`upsert_source_product` 재사용)
    ▼
SourceProduct ──(확장이 가중 랩으로 크롤 · 이미 있음)──▶ 가격·재고·옵션·이미지
    │ ② 초안 만들기 (`build_draft_from_source` · 이미 있음)
    ▼
ProductDraft (`search_filter_id` 새김 → 성적표의 연결고리)
```

- 통로 = `POST /bulk/api/search-filters/<id>/build` · 화면 = 「상품 만들기」 단추 한 개
- 한 단추가 **두 걸음을 겸한다** — 갈 수 있는 데까지 가고, 크롤이 돈 뒤 또 누르면 그만큼 는다
- 🔴 **자동으로 크롤 대기에 안 넣는다** — 검색 한 번에 수백~수천이 들어오는데 자동이면
  사장님 모르게 크롤 부하가 몇 배가 된다(수집 시점 필터가 아직 안 먹어 더 그렇다)
- 🔴 **「크롤됨」의 증거는 `last_status=='ok'` 하나뿐** — 행이 있다고 크롤된 게 아니다
  (방금 우리가 만든 빈 행일 수 있고, 그걸로 초안을 만들면 텅 빈 상품이 조용히 생긴다)
- 🔴 **판매가는 0으로 둔다** — 크롤은 매입가만 안다. 지어내지 않고 preflight 가 막게 한다
- 필터 「지우기」는 무엇이 남는지 먼저 말한다(만든 상품·찾은 주소 기록 둘 다 남음)

### 3-2. 🆕 **정책 붙임표** (`PolicyBinding`) — 두 벌 합치기의 유일한 신설

**푸는 문제** — 지금은 정책을 붙이는 표가 **대상마다 따로**다.
`BundlePolicyLink`(상품) · `SetPolicyLink`(구성) · `ProcessPolicySource`(소싱처×브랜드).
여기에 검색필터·카테고리·개별상품이 더 붙으면 **표가 6개**가 된다.

**하나로 모은다:**

| 칸 | 뜻 |
|---|---|
| `scope_type` | 무엇에 붙었나 — `default`·`category`·`search_filter`·`source_brand`·`model`·`set`·`draft` |
| `scope_key` | 그 대상의 열쇠 (`model_code`, `filter_id`, `source_key|brand` …) |
| `policy_id` | 어느 정책 |
| `priority` | 층 번호 (아래 §4) |

기존 3개 표는 이 표로 **옮기고 없앤다**(같은 내용이므로 데이터 이동만).

**✅ 「한 구성은 한 정책에만」(2026-07-19 확정) — 사장님 재확인 완료(2026-08-06, a안).**
같은 층 안에서는 여전히 **유일**(`UNIQUE(scope_type, scope_key)`)이다.
**다른 층끼리만** 겹칠 수 있고, 그때는 **위층이 이긴다.**
→ 확정을 지키면서 상속을 얻는다. `PolicyConflict` 는 **같은 층 검사로 남는다.**

### 3-3. 🆕 **마켓 아이디 그룹** (`MarketAccountGroup`)

**푸는 문제** — 계정이 33개인데 그룹이 없으면 **정책이 33배**가 된다(더망고 138개가 증거).

| 표 | 칸 |
|---|---|
| `MarketAccountGroup` | `name`(예: 「르무통 본점」) · `memo` |
| `MarketAccountGroupMember` | `group_id` · `market` · `account_key` |

우리는 이미 `account_key`(+`'default'` 센티넬)를 `ProductDraftMarket`·`ProcessPolicyMarket` 에 갖고 있다.
정책을 붙일 때 **계정 대신 그룹**을 고를 수 있게 하는 것뿐이다.
**여기에 「마켓아이디별 최대등록상품개수」와 「마켓분산등록」이 얹힌다**(확정 5).

### 3-4. 🔧 확장 — `MarketCategory` 에 `mall_scope` 한 칸

§09 결론. `domestic`／`overseas`／`unknown`.
🟢 **재수집 필요 없음** — 11번가 `gblDlvYn`, 롯데온 `disp_list[].mall_dvs_cd` 가
`raw_json` 에 **이미 저장돼 있다**(`category_harvest.py:436`).
유니크 키도 `(market, code)` → `(market, mall_scope, code)` 로 검토(지금은 같은 코드가 서로 덮어쓴다).

### 3-5. 🔧 확장 — `CrawlWeightRule.scope_type` 에 `search_filter`

§3-1 참조. **새 엔진을 만들지 않는다.** 값 하나를 늘린다.

---

## 4. 정책 상속 — 층을 세운다

더망고는 3단(개별 > 검색필터 > 카테고리)이다. 우리는 두 모드가 있으므로 **모드별 사슬**로 둔다.
같은 표(`PolicyBinding`)에 `priority` 숫자로 표현한다. **위가 이긴다.**

```
[대량등록 모드]                        [모음전 모드]
 6  개별 상품 (draft)                   6  구성 (set)
 5  구성 (소싱처 × 브랜드)               5  상품 (model)
 4  검색필터                    ← 신설
 3  소싱처 카테고리              ← 신설
 1  기본 정책 (is_default)               1  기본 정책
```

**항목 단위로 이긴다** — 정책 통째가 아니라 13항목 **하나하나** 위층 값이 있으면 그것을 쓴다.
🔵 우리는 이미 `from_common_at`(항목 단위 값 출처)을 갖고 있다(우위 12번). **더망고는 정책 단위뿐이다.**

**🔴 그래서 「정책 출처 표시」(채택 12)는 상속과 한 몸이다.**
상속을 켜는 순간 「이 값이 왜 이렇지?」를 못 풀면 사고가 난다.
→ 화면에 **「이 값은 〈검색필터: 무신사_나이키〉에서 왔습니다」**를 항목마다 띄운다.

**정책을 고치면 몇 개가 바뀌는지 먼저 보여준다** (§03:116) — 소급 적용 전 **영향 상품 수** 표시.

---

## 5. 화면 배치 — 🟢 **새 탭 0개**

> 게이트 G4(*화면이 늘면 배울 게 는다*)를 지키는 방법 = **기존 9탭 안에 전부 앉힌다.**
> 더망고는 31화면에 흩어놨다. 우리는 9탭에 넣는다. **이게 우리 차별점이다.**

| 탭 | 얹는 것 (🟢번호는 §08 채택 번호) |
|---|---|
| **📥 `collect` 데이터수집** | **주인공을 「검색필터」로 교체.** 🟢1 검색필터 · 🟢2 신규상품수집(랩에 자동) · 🟢3 수집 시점 필터(저장수·페이지범위·금지어·옵션제외·즉시정책) · 🟢18 새벽 3~6시 가중 |
| **🔧 `process` 데이터가공** | 🟢6 **[가격변동] 전후 가격 병기**(최종매입가 기준 + 역마진 경고) · 🟢7 배송비 총액 고정·분배 · 🟢9 판매가 절삭 단위 · 🟢11 마켓별 금지어(분기만) · 🟢12 **정책 출처 표시** · 정책 상속 층 |
| **📤 `send` 데이터전송** | 🟢8 재고 상한 캡(**10**) · 🟢17 옵션 잘림 표면화 + **자를 순서 고르기** · 🟢15 옥션·G마켓 옵션불가 preflight · 🔴**롯데온 본보기 preflight**(§09) · 중복등록 방어 `(소싱처URL, 마켓, 계정)` · 🟢16 최근 작업 자동 기록 |
| **✍️ `manual` 수기 등록** | 🟢14 엑셀 등록 **검토 테이블**(수천 건 들어가기 전) · 🟢10 **수기 수정 = 자동 잠금 배선** |
| **📦 `products` 상품관리** | 🟢13 원재고 병기(사람이 고친 값 ↔ 크롤 원본) · 🟢17 옵션개수 많은순 정렬 · **마켓분산등록**(확정 5) · 연속 품절 N일 일괄삭제 |
| **📊 `stats` 통계** | 🟢4 **검색필터 성적표** ← 아래 §5-1 |
| **⚙️ `settings` 설정** | 🟢5 **마켓 아이디 그룹** · 마켓아이디별 최대등록수 · **랜덤키워드(기본 꺼짐)**(확정 4) · 재고상한 기본값 10 |
| `orders` · `cs` | 모음전 화면 공유 — 변경 없음 |

### 5-1. 🟢4 검색필터 성적표 — **네 숫자를 한 줄에**

더망고는 이 숫자가 **네 화면에 흩어져** 있었다(§08-0). 우리는 **한 표**로 묶는다.

| 열 | 어디서 오나 | 지금 되나 |
|---|---|---|
| **수집량** | `ProductDraft` 중 그 `search_filter_id` 개수 | 🟢 Phase 1 이면 바로 |
| **생존** | 그중 안 지워지고 마켓에 올라간 것 | 🟢 `ProductDraftMarket.status` |
| **조회수** | 마켓 접속통계 | 🔴 **우리에게 없음** → 1차는 **「등록 성공 수」**로 대신하고 열 이름을 그렇게 쓴다 |
| **매출** | 주문 라인 → 마켓상품번호 → `ProductDraftMarket.market_product_id` → `draft_id` → `search_filter_id` | 🟢 **사슬이 이미 있다** |

🔴 **없는 숫자를 있는 척 채우지 않는다.** 조회수 열은 「조회수」라 쓰지 않는다.
더망고 실측: **필터 12개 중 3개만 돈이 됐다.** 이 표 없이는 **무엇을 늘릴지 모른다.**

---

## 6. 이관을 가능하게 하는 6가지 — 어디에 들어가나

> 🔴 이관은 다른 세션 일이지만, **지금 설계가 이관을 가능하게** 해야 한다(확정 2).
> 성패 = **「소싱처URL ↔ 마켓상품번호」 짝 보존.** 못 옮기면 마켓 중복등록 = 계정 위험.

| # | 미리 넣을 것 | **이 설계 어디에** |
|---|---|---|
| 1 | 소싱처 URL 을 **1급 식별자**로 | `ProductDraft.source_url` **이미 있음**(`models.py:106`). 🔴 **인덱스와 중복검사를 붙인다** |
| 2 | 마켓 상품번호를 **「우리가 등록」 vs 「받아옴」** 구분 | `ProductDraftMarket` 에 `origin_kind`(`ours`／`imported`) 한 칸 |
| 3 | **외부 유입 표시 + 출처** | 같은 곳에 `imported_from`(`themango` 등) |
| 4 | 등록 직전 **`(소싱처URL, 마켓, 계정)` 중복 방어** | §5 `send` 탭 preflight — **이관 전에 이미 돌고 있어야 한다** |
| 5 | 정책·필터를 **가져올 수 있는 모양**으로 | `MarketPolicyValue`(field_key·value **이름·값 쌍**) + `SearchFilter` — **이미 그 모양** |
| 6 | **이관 검증 도구** — 개수·짝 대조 | Phase 6. `MarketProduct`·`MarketProductCount` 재사용 |

**통로는 마켓 API 직접 조회**(확정 6 — 더망고 API 미신청).
받을 그릇 `MarketProduct` · `MarketProductGroup` · `BundleAccountRegistration` · `ProductDraftMarket(account_key)` 는 **이미 있다.**

🔴 **한 가지 못 가져오는 것** — 더망고 안에만 있고 마켓엔 안 올라간 데이터(정책 138개·검색필터 정의).
마켓 API 로는 안 나온다. **손으로 옮겨야 한다.** 그래서 §3-1 검색필터는 **「엑셀로 넣고 뺄 수 있게」** 만든다.

---

## 7. 🔴 재구현 금지 명부 — 이미 있는 것

> 이번 조사에서 **네 번 「우리에겐 없다」고 오판**했다(§이관문서 4-③). 만들기 전에 이 표를 본다.

| 하려던 것 | **이미 있다** |
|---|---|
| 상품/가격/재고 업데이트 ON·OFF | `ProductDraft.update_product/price/stock` (`models.py:91~93`) — 🔴 **읽는 곳이 없을 뿐**(채택 10) |
| 13항목 규칙 스키마 | `process_rule_schema.all_schemas()` — 모음전도 **이걸 쓴다**(`fields.py:18`) |
| 항목 단위 값 출처 | `MarketPolicyValue.from_common_at` |
| 마켓별 필수 표시 | `lemouton/policy/required.py` |
| 4단 상속 엔진 | `CrawlWeightRule.scope_type` (source·brand·model·url) |
| 카테고리 사전 | `MarketCategory` **59,048건 수집 완료** |
| 카테고리 응답 원문 | `MarketCategory.raw_json` — 🟢 **구분값이 여기 있다**(§09) |
| 업로드 우선순위 게이트 | `upload_gate.decide_upload` · `daily_cap` |
| 옵션 단위 부분 전송 | `uploader/adapters/*.update_price_and_stock` |
| 휴지통 2단계·변경 이력 | `/trash` · `AuditLog` · `CrawlDelta` · `PriceSnapshot` |
| 마켓 상품 그릇(이관 수신) | `MarketProduct` · `MarketProductGroup` · `BundleAccountRegistration` |
| 최종매입가 엔진 | `pricing/final_price.py` · `api_benefits.compute_breakdown` — **더망고엔 개념 자체가 없다** |

---

## 8. 단계 — 무엇부터

| Phase | 내용 | 왜 이 순서 |
|---|---|---|
| **1. 뼈대** | ✅🆕`SearchFilter`·`SearchFilterItem` · ✅리스팅 URL → 다건 수집(무신사, 확장 0.7.82) · ✅`ProductDraft.search_filter_id` · ✅「데이터수집」 화면 A안 · ✅**「상품 만들기」 — 찾은 주소 → 크롤 대기 → 초안** · ⬜수집 시점 필터 실적용 · ⬜`CrawlWeightRule` 에 `search_filter` scope | **없으면 대량등록이 성립 안 한다.** 나머지 전부가 이 위에 선다 |
| **2. 믿고 맡길 수 있게** | 🔴롯데온 본보기 preflight ✅ · 소싱처URL 중복등록 방어 ✅ · 옵션 상한 규칙 신설(자를 순서 + 잘림 표면화) · 재고 상한 캡(10) · 수기수정 잠금 **배선** · ⚠️옥션·G마켓 옵션불가 preflight → §8-2 참조 | 아래 §8-1 참조 — **되돌릴 수 없는 것부터** |
| **3. 정책 하나로** | 🆕`PolicyBinding` · 기존 링크 3표 흡수 · 상속 층 · **정책 출처 표시** · 영향 상품 수 사전 표시 | 정정 ①로 비용이 줄었다. 단 2를 먼저 — **사고 막는 게 먼저** |
| **4. 돈·성적표** | [가격변동] 전후 병기 · 배송비 총액고정 · 판매가 절삭 · **검색필터 성적표** · 최근작업 자동기록 · 새벽 3~6시 · 🆕`MarketAccountGroup` · 마켓분산등록 | 성적표는 1의 데이터가 쌓여야 뜻이 생긴다 |
| **5. 소싱처 5곳** | GSShop → ABCmart+GrandStage(`a-rt.com` **어댑터 한 벌**) → 29CM · FashionPlus | 뼈대가 있어야 새 소싱처가 값어치를 낸다 |
| **6. 이관 준비** | `origin_kind`·`imported_from` · 검증 도구 · 검색필터 엑셀 입출력 | 🔴 **다른 세션** |
| **곁가지** | `MarketCategory.mall_scope` 백필 · 랜덤키워드(기본 꺼짐) · 엑셀 검토 테이블 · 원재고 병기 · 마켓별 금지어 분기 | 값싸다. 아무 때나 |

### 8-1. 🔴 「지금 손실 중」의 정확한 위치 — **두 개를 섞지 말 것**

앞선 문서(§08-17)의 *"옵션 71개 상품이 10개만 나간다 — 지금 손실 중"* 은
**더망고에서** 나는 손실이다(마인드맵 원문: *「전송 옵션개수 = 10개」·「옵션명 오름차순 앞 N개」*).
**우리 코드에는 옵션 상한 규칙이 아예 없다** — `lemouton/` 전체에 옵션 개수를 자르는 코드 0건.
우리 원칙이 「실측값만 적용, 모르면 미적용」(🔵우위 8번)이기 때문이다.

| | 어디서 새나 | 우리에게 뜻하는 것 |
|---|---|---|
| **옵션 71→10 잘림** | 🟠 **더망고 쪽** | 우리로 옮겨올 때 **같은 손실을 물려받지 않게** 규칙을 새로 세운다 |
| **롯데온 본보기 복사** | 🔴 **우리 코드** (`compile_more.py:148`) | 대량으로 밀면 **되돌릴 수 없다.** 우리 유일한 실물 폭탄 |
| 재고 상한·중복 방어·옵션불가 카테고리 | ⚪ 아직 아무 규칙 없음 | 규칙이 없는 것 = 마켓이 대신 정하게 두는 것 |

**Phase 2 를 3보다 앞에 둔 이유** — 정책 합치기는 크고 오래 걸린다.
그 사이에 **되돌릴 수 없는 사고**(롯데온)가 한 번 나면 수천 건을 지우고 다시 올려야 한다.
**되돌릴 수 있는 미완성보다 되돌릴 수 없는 사고가 먼저다.**

### 8-2. ⚠️ 채택 15(옥션·G마켓 옵션등록 불가 카테고리) — **근거 데이터가 없다**

구현하려다 막혔다. **채택 판정을 정정해야 한다.**

**실측 — ESM 카테고리 API 응답에 「옵션 등록 가능 여부」 칸이 자체가 없다.**
`/item/v1/categories/site-cats` 가 주는 칸은 `catCode` · `catName` · `isLeaf` 셋뿐이다
(`marketplace_api_map.json` · 수집기 `category_harvest.py:279 harvest_esm_site`).
sd-cats·site-cats 매칭 API 도 같다.

**더망고도 API 로 아는 게 아니었다.** 그쪽 화면 이름이 **「단일상품등록설정」**이다 —
**사람이 넣는 목록**이다(마인드맵 L2 「상품관리관련·마켓 관리정보 설정」).

→ 목록을 우리가 지어내면 **없는 근거로 등록을 막는 것**이 된다.
「실측값만 적용, 모르면 미적용」(🔵우위 8번)의 정면 위반이다. 그래서 **지금은 안 만든다.**

**대신 지금 할 수 있는 것 (근거 100%)** — 우리는 옵션 부착 실패를 **이미 잡고 있다**
(`send_more.py:161~178` — 실패하면 `PartialRegisterError` + 즉시 판매중지 회수).
그런데 **그 실패가 어느 카테고리에서 났는지는 아무도 기억하지 않는다.**
→ 실패 메시지에 **카테고리 코드를 싣는다.** 그러면 목록의 씨앗이 실측으로 쌓인다.

**나중에 고를 두 갈래 (사장님 판단 필요)**

| 안 | 방식 | 값 | 비용 |
|---|---|---|---|
| **가** | **실패에서 자동으로 배운다** — 옵션 부착이 실패한 (마켓, 카테고리)를 표에 쌓고 다음부터 preflight 가 막는다 | 🔵 **더망고보다 낫다**(사람 입력 불필요·근거는 마켓 응답) | 새 표 1개. **첫 한 번은 못 막는다** |
| **나** | 사장님이 목록 입력 (더망고 방식) | 첫 번째부터 막는다 | 설정 화면 1개. 근거가 **사람 기억** |

권장 = **가 + 나 겸용.** 자동으로 쌓되 사람이 손으로 더할 수 있게.
단 **Phase 4 이후**로 미룬다 — 지금은 근거가 0건이라 만들어도 아무것도 막지 못한다.

---

## 8-3. 🔴 2묶음 착수 중 막힌 것 3건 (2026-08-06 실측)

Phase 2 의 나머지(옵션 상한·재고 상한·수기수정 잠금)를 짓다가 **세 건 다 전제가 틀렸음**을 발견했다.
셋 다 「없는 근거로 만들지 않는다」에 걸려 **멈췄다.**

### ① 재고 상한 — 🔴 같은 날 확정이 **두 개**다 (사장님 결정 필요)

| 어디 | 값 | 근거 |
|---|---|---|
| `lemouton/formatter/stock_policy.py:34~35` | **100** | 주석: *"마켓에 올리는 옵션당 최대 재고 (**사장님 확정 2026-08-06** — 주문 몰림 시 못 구할 위험 차단)"* |
| 이번 세션 판단 3 | **10** | 「더망고 운영값 10개. 우리도 10?」 → 「10개(더망고와 동일)」 |

🔴 내가 물을 때 **「우리는 이미 100으로 확정돼 있다」를 말하지 않았다.** 질문이 반쪽이었다.
→ **말없이 100→10 으로 바꾸지 않았다.** 사장님이 다시 정해야 한다.

🟡 참고 — **`STOCK_CAP` 이라는 이름이 두 곳에 있고 서로 다른 것이다.**
`sourcing/crawlers/musinsa_playwright.py:52 STOCK_CAP = cfg.stockCap || 10` 은
**소싱처에서 재고를 셀 때의 상한**(크롤 쪽)이다. 전송 상한(100)과 층이 다르다.
무신사는 크롤 상한이 10이라 전송 상한 100이 사실상 안 걸린다 —
**전송 상한을 10으로 낮춰도 무신사는 차이가 없고, 재고를 실수량으로 읽는 소싱처만 바뀐다.**

### ② 「수기 수정 = 자동 잠금」 — 🔴 훨씬 큰 결함이 밑에 있었다

애초 진단은 「`ProductDraft.update_product/price/stock` 토글을 읽는 곳이 없다」였다.
파 보니 **더 크다.**

| 칸 | 어디 | 상태 |
|---|---|---|
| `ProductDraft.update_product/price/stock` | `registration/models.py:91~93` | 화면이 보여주고 저장까지(`bulk/products.py:43~45,100`) · **읽는 곳 0곳** |
| `ProductSet.auto_stock_mode` / `auto_price_mode` | `sets/models.py:37~38` | 모델 주석에 **"(구) … 하위호환 유지(미사용)"** — 의도적 은퇴(버그 아님) |
| **`ProductSet.auto_mode`** (on/off/manual) | `sets/models.py:30` | 저장·표시만(`set_service.py:269,309,323`) · **랩 엔진·업로더가 안 읽음** |
| `manual_crawl_hours` · `manual_upload_hours` | `sets/models.py:31~34` | 상동 · **안 읽음** |

→ **「구성별 자동화 예외」 기능이 통째로 화면뿐이다.** 끄기를 눌러도 안 꺼진다.
→ 대량등록(`origin='bulk'`) 상품은 애초에 **자동 갱신 경로에 연결돼 있지도 않다**
  (`ProductDraft.model_code` = "모음전 상품에서 온 경우 연결" 인데 **채우는 코드가 없다**.
   `send/as_draft.py:129` 만 예외로 `origin='set'` 초안에 채운다).
🔴 이건 자동 크롤·자동 전송 거동을 바꾸는 일이라 **별건으로 크게 잡아야 한다.** 조용히 못 고친다.

### ③ 옵션 상한 — 실측값이 **쿠팡 200 하나뿐**이고, 우리 최대는 71이다

- 실측: 쿠팡 `요청.items` = *"최대 200개 옵션 등록 가능"*(마켓 API 원문). **나머지 5마켓은 모름.**
- 🔴 **우리 코드에는 옵션을 자르는 로직이 아예 없다**(`lemouton/` 전체 0건).
  「71개 중 10개만 나간다」는 **더망고 설정**이었다(§8-1).
- → 사장님 판단 2(「잘라 보내되 순서를 고른다」)는 **「우리가 자른다」는 전제** 위에서 답하셨다.
  실제로는 자를 일이 쿠팡 200 초과일 때뿐이고 지금 최대가 71이라 **한 번도 안 걸린다.**
- 값은 있다(대량등록으로 옵션 많은 상품이 들어오면 쿠팡이 등록을 통째로 거부한다).
  다만 **1순위는 아니다.**

| # | 무엇 | 상태 |
|---|---|---|
| 1 | **롯데온 본보기 상품 방식** — 검색필터 하나가 여러 카테고리 상품을 물어오는데 본보기는 하나다. 전부 같은 카테고리로 나간다 | 🟡 Phase 2 에서 **막이개**, 2차에 전시카테고리 직접지정(확정 7) |
| 2 | **조회수 열의 원천이 없다** | 🟡 1차는 「등록 성공 수」로 대체. 마켓 접속통계는 별도 검토(§03:147) |
| 3 | ~~`PolicyConflict` 의 뜻이 바뀐다~~ | ✅ **해소(2026-08-06 a안)** — 같은 층에서만 막고, 다른 층은 위가 이긴다 |
| 4 | **검색필터 ↔ 구성(소싱처×브랜드)** 두 그룹 개체가 공존한다 | 🟡 층으로 갈랐으나(§4) 화면에서 헷갈릴 수 있음 → 시안에서 검증 |
| 5 | 더망고 **정책 138개·검색필터**는 마켓 API 로 못 가져온다 | 🔴 손 이관 전제. 그래서 **엑셀 입출력**을 넣음(§6-5) |
| 6 | 실제로 **한 바퀴 돌려보기**(수집→정책→전송) 미착수 | ⬜ 체크리스트 5단계. 사장님 허가는 받아둠(테스트 1건) |

---

## 10. 한 장 요약

```
새로 만드는 표     3개 — 검색필터 · 정책붙임표 · 마켓아이디그룹
넓히는 것          2개 — MarketCategory.mall_scope · CrawlWeightRule.scope
새 탭              0개 — 기존 9탭 안에 전부
재구현 금지        12건 (§7)
정책 합치기 비용   생각보다 작음 — 항목 정의는 이미 한 벌 (fields.py:18)
가장 급한 것       Phase 2 — 지금 새고 있다 (옵션 61개 잘림 · 롯데온 본보기)
```

---

## §12. [2026-08-08] 소싱처 확대 · 「뺄 옵션」 · 성적표 구현 (PR#925)

### 12-1. 소싱처는 8곳이 천장이다 (🔴 설계 전제 정정)

「소싱처를 넓히자」에서 처음 적은 후보는 ABC마트·GS샵·29CM·패션플러스였다.
**틀렸다.** `sourcing/crawlers/__init__.py::build_crawlers` 가 아는 곳은 8곳뿐이고,
그 밖은 **크롤러가 아예 없다.** 리스팅 규칙만 넣으면 주소는 모이는데 아무도 못 긁어
조용한 0건이 된다. → 규칙은 **크롤 가능한 곳에만** 넣도록 시험으로 못박았다.

| 소싱처 | 리스팅 규칙 | 페이지 넘김 |
|---|---|---|
| 무신사 | ✅ (기존) | ✅ `page` |
| SSF | ✅ 브랜드칸 포함 | ✅ `currentPage` (카테고리 목록만) |
| 롯데온 | ✅ 묶음 제외 | ❌ 무한 스크롤 |
| 롯데아이몰 | ✅ | ❌ 무한 스크롤 |
| 현대H몰 | ✅ 속성에서 | ❌ |
| SSG | ❌ 미측정 (앱 브라우저 차단 + 서버 403) |
| 르무통 · 스스르무통 | ❌ 미측정 (로그인 필요) |

### 12-2. 확장에 규칙이 박혀 있으면 서버를 고쳐도 소용없다

`_listingCollectIds` 가 무신사 전용이었다. 「규칙을 넣었다」와 「그 규칙이 쓰인다」는
다른 사실 — 이 저장소에서 세 번째로 같은 모양이다(대기열 · SetChannel · 이번).
→ 규칙을 **서버가 내려준다**(`sel`·`attr`·`id_re`). 다음 소싱처는 확장 수정 0.

### 12-3. 남은 것

- SSG · 르무통 계열 리스팅 규칙 (실측 수단부터 필요)
- 무한 스크롤 소싱처는 **첫 화면분만** 걷힌다(롯데온 48 · 아이몰 26 · SSF 27).
  더 필요하면 확장이 스크롤해야 한다 — 별도 작업.
- 「구성별 자동화 예외」(`auto_mode`·`manual_crawl_hours` 등)는 여전히 **전혀 안 먹는다.**

---

## §13. [2026-08-08] 훑기 정직성 · 무한 스크롤 · 찾은 주소 보기

### 13-1. 🔴 보내는 쪽이 정직해도 받는 쪽이 버리면 소용없다

확장은 「한 장이 실패한 걸 0건과 구분해야 한다」며 실패 사유(`error`)를 늘 실어
보냈다. **서버가 그 값을 받기만 하고 버렸다** — 저장도 표시도 없었다.
그래서 「페이지가 안 열렸다」가 화면에선 「끝남 / 0건」과 **똑같아 보였다.**

같은 부류가 이 저장소에서 세 번째다 — 대기열(넣었는데 꺼내는 쪽이 다른 기준),
SetChannel(쓰는 곳과 읽는 곳이 다름), 이번(보내는데 받는 쪽이 버림).
**「A가 한다」와 「B가 그걸 쓴다」는 항상 따로 확인한다.**

### 13-2. 세 가지를 갈라 말한다

| 화면 | 뜻 |
|---|---|
| 끝남 | 다 봤고 그게 전부다 |
| **못 봄**(빨강) | 페이지를 못 열었다 — 0건이 아니다 |
| **더 있음**(주황) | 정해진 만큼만 내려 봤다 — 더 있을 수 있다 |

뭉치면 사장님이 **없는 상품을 없다고 믿는다.**

### 13-3. 🔴 「무한 스크롤」이 아니었다 — 내 진단이 틀렸다

처음엔 「페이지 넘김 파라미터가 없으니 무한 스크롤이겠다」고 보고 스크롤을 넣었다.
**실측하니 셋 다 안 통했다** — 롯데온 48→48 · 롯데아이몰 24→24 · 현대H몰 40→40.
(H몰은 몸통이 스크롤 대상이 아니라 **안쪽 상자**를 내려야 했는데, 그것까지 해도 그대로.)

셋 다 **단추를 눌러 넘기는** 방식이다. 롯데온에서 「2」를 눌러 보니 주소는 안 바뀌고
상품만 바뀌었다 — 그래서 주소 파라미터로도, 스크롤로도 못 넘긴다.

★ **파라미터가 없다 ≠ 무한 스크롤.** 「어떻게 넘기나」는 직접 눌러 봐야 안다.

지금 할 수 있는 정직한 일은 하나 — **「더 있다」를 말해 주는 것.**
「다음」 단추가 살아 있으면 우리가 첫 장만 가져온 것이므로 `capped` 로 알린다
(실측 확인: 롯데온 48건 + 더있음 · 아이몰 24건 + 더있음).
단추를 눌러 여러 장을 걷는 일은 **다음 걸음**이다.

### 13-4. 「찾은 주소 보기」

화면은 「찾음 31」이라고만 말했다. **무엇을** 찾았는지 볼 방법이 없어, 검색어가
엉뚱해 잡화가 딸려 와도 상품을 만들어 보기 전엔 몰랐다.
주소마다 **어디까지 왔는지**(크롤 전 / 크롤됨 / 상품됨)도 같이 말한다.

### 13-5. 🔴🔴 「결과 0건」 화면에도 상품이 깔려 있다 (가장 위험했던 것)

「검색 결과가 없습니다」가 떠 있는 화면에서 우리 규칙에 잡힌 수 — **6곳 중 5곳**:

| 소싱처 | 잡힌 수 | 정체 |
|---|---|---|
| 무신사 | 19 | 회원가입 이벤트 상품 |
| 롯데온 | 25 | 추천 |
| 롯데아이몰 | 25 | 메뉴 속 배너 |
| 현대H몰 | 12 | 실시간 베스트 |
| 르무통 | 1 | 상단 노출 |
| SSF | **0** | — 여기만 정직 |

처음엔 롯데 3사만인 줄 알았다. **「우리 첫 소싱처(무신사)는 괜찮겠지」가 틀렸다.**
그대로 뒀으면 **사장님이 오타 한 번 낼 때마다 엉뚱한 상품 수십 건이 크롤 대기에
들어가 초안까지 되고 마켓으로 갔다.**

더 나쁜 것 — **롯데아이몰은 정상 검색어에서도 전부 가짜였다.**
`a[href*=viewGoodsDetail]` 25건이 통째로 메뉴 속 추천 배너였고, 진짜 결과는
`data-goods-no`(나이키 60건)였다. **정상 검색어 하나만 봤을 땐 26건이 그럴듯해서
못 알아챘다** — 0건 검색어로 대조하고서야 드러났다.

★ 규칙을 넣기 전 **두 번 잰다** — ①흔한 검색어 ②없을 법한 검색어.
  ②에서 0이 아니면 규칙이 결과 영역 밖을 보고 있는 것이다.
★ 「결과 없음」 글귀를 가드로 두되, **글귀가 없다고 0건으로 만들지는 않는다**
  (UI 가 바뀌면 멀쩡한 수집이 통째로 죽는다). 있을 때만 「없다」고 확정한다.
