# .workflow-state.md

cycle_id: 20260521-212730
started_at: 2026-05-21T21:27:30
current_step: 7   # 코드 작성 — Phase 1 (가격 계산기 통합)
mode: C   # 특정 단계 — 디자인(21개 시안) 완료 상태에서 시작

# ── 사이클 정의 ────────────────────────────────
title: 모음전 시스템 개편 — 6개 기능 구현
features:
  - "① 가격 계산기 통합 (화면값 = 마켓 업로드값)"
  - "② 단계형 옵션 · 조합 추가 (1/2/3축, 쉼표/매트릭스 생성)"
  - "③ 소싱처 URL 옵션 단위 정리 + 다중 URL"
  - "④ 자동 업로드 파이프라인 복구 (수동→일괄→자동)"
  - "⑤ 마켓 옵션 매핑 (방법1 API자동/2 수동/3 엑셀 + 미매칭 후보·수기·직접입력)"
  - "⑥ 모음전 구성 4탭 재구성 (기본·템플릿·옵션·마켓)"
design_inputs:
  - "바탕화면 모음전 시안 v1~v21.html (21개)"

# ── 사전 결정 (pre-flight) — 확정 ──────────────
preflight:
  routing: 신규프로젝트   # 사용자 결정 A — 새집(C:\dev\모음전 프로젝트)에서 구현. 기존은 컷오버까지 보존 후 은퇴
  start_mode: 문서정리부터   # STEP 1~3(인터뷰·빼기) 건너뜀 — 21개 시안으로 완료
  caution: "04:00 기존→신규 자동 동기화 중단 필요 (안 하면 신규 작업 코드 덮어씀). CLAUDE.md 미러 규칙 갱신 필요."

artifacts:
  context_md: null
  prd_md: null
  office_hours_output: null
  light_spec_md: null
  plan_md: docs/PLAN.md
  plan_review_md: null
  code_paths: []
  codex_review: null
  architecture_review: null
  retro_md: null

step_log:
  - step: 1-3
    note: "21개 디자인 시안으로 도메인·기능·빼기 결정 사실상 완료 — 정식 문서(docs/*) 박제만 필요"
    status: design_done
  - step: 4-5
    skill_called: ai-workflow (conductor) — 21개 시안 → PLAN 통합
    status: completed
    artifact: docs/PLAN.md
    note: "6 Phase 구현 계획 작성 — Phase1 가격통합 → 2 단계형옵션 → 3 URL → 4 매핑 → 5 업로드 → 6 4탭"
  - step: 6
    status: completed
    note: "플랜·라우팅 확정 — 신규 프로젝트 / Phase 1부터"
  - step: 7
    phase: 1
    task: 1
    skill_called: tdd
    status: passed
    artifact: "lemouton/pricing/unified.py + tests/pricing/test_unified.py"
    evidence: "pytest 14/14 통과 — 통합 가격 함수 compute_sale_price_unified"
  - step: 7
    phase: 1
    task: 2
    status: passed
    artifact: "lemouton/sourcing/models_pricing.py — calc_auto_price → 통합 함수 위임"
    evidence: "pytest tests/pricing+inventory 49 passed. 실패 2(sku_mapping)는 사전존재 — git stash 검증 완료"
  - step: 7
    phase: 1
    task: 3
    status: completed
    note: "경로③(재고관리 /inventory/matrix) 분석 — boxhero_margin은 '사입가+마진'만(수수료·배송 없음)인 재고관리 내부 참고가. 마켓 판매가 아님 → 가격 통합 대상에서 제외 (별개 계산)"
  - step: 7
    phase: 1
    task: 4
    status: deferred
    note: "경로①(스케줄러 engine 분모형) → Phase 5(업로드 복구)로 이동. 현재 스케줄러 업로드 사실상 정지 → 라이브 가격 미생성. 업로드 복구 시 함께 곱셈형 통합·검증"
  - step: 7
    phase: 1
    status: substantially_done
    note: "Phase 1 핵심 완료 — 통합 함수 신설 + 라이브 가격 화면(옵션 매트릭스) 연결 + pytest 검증. 남은 검증: 브라우저 실시연. 경로① Phase 5로."
  - step: 7
    phase: 2
    task: 1
    skill_called: tdd
    status: passed
    artifact: "lemouton/sourcing/option_combo.py + tests/sourcing/test_option_combo.py"
    evidence: "pytest 13/13 통과 — parse_comma_values / generate_combinations(1·2·3축) / build_sku"
  - step: 7
    phase: 2
    task: 2
    skill_called: tdd
    status: passed
    artifact: "lemouton/sourcing/models.py(BundleOptionStep) + option_combo.py(steps_from_rows)"
    evidence: "모델 import OK (table=bundle_option_steps) / pytest 17/17. Alembic 없음 — create_all 이 신규 테이블 자동 생성"
  - step: 7
    phase: 2
    task: 3
    skill_called: tdd
    status: passed
    artifact: "Option.axis_values_json 컬럼 + shared/db.py 마이그레이션 + option_combo(option_axis_values, option_sku)"
    evidence: "Option 컬럼 등록 확인 / pytest 37/37 (option_combo 23 + pricing 14)"
  - step: 7
    phase: 2
    task: 4
    skill_called: tdd
    status: passed
    artifact: "option_combo.py(build_options_from_steps) + option_service.py(save_step_design, create_combination_options)"
    evidence: "pytest tests/sourcing 32/32 (순수 4 + in-memory SQLite 통합 5). HTTP 라우트 글루는 Task 5와 함께"
  - step: 7
    phase: 2
    task: 5
    status: partial
    artifact: "webapp/routes/api.py — POST /api/bundles/<code>/options/combo (조합 추가 라우트)"
    evidence: "py_compile OK / 백엔드 회귀 46 passed. 모달 UI(프론트 HTML·JS)는 서버 가동·브라우저 시연 필요 — 미완"
  - step: 7
    phase: 2
    task: 6
    status: pending
    note: "옵션 매트릭스 N축 대응 (UI) — 서버 가동·브라우저 시연 필요"
  - step: 7
    phase: 3
    task: 1
    skill_called: tdd
    status: passed
    artifact: "OptionSourceUrl UniqueConstraint 제거(다중 URL) + Option.offline_only 컬럼 + option_source_service.py + option_combo.option_is_offline"
    evidence: "pytest 51/51 — 한 소싱처 다중 URL·오프라인 옵션 검증 (in-memory SQLite)"
    note: "기존 SQLite DB는 option_source_urls 에 옛 UniqueConstraint 가 남음 → 배포 시 그 테이블 1회 재생성 필요. 신규(fresh) DB·테스트는 create_all 로 무제약."

# ── 세션 경계 (resume point) ───────────────────
resume:
  done: "Phase 1·2·3·6 완료 + Phase 4 완료(스스+쿠팡 옵션 매칭, 미매칭 3단 추천후보·검색·직접입력 — sync-cp-options/apply-cp-matching 라우트, openSsMatchingModal 마켓겸용, apply_matching option_id_field, mock 시연)"
  remaining: "Phase 4 잔여(엑셀 import — 우선순위 낮음) · Phase 5(자동 업로드 복구 — 사용자가 나중에 일괄테스트하기로 보류)"
  next_session: "커밋·배포 또는 Phase 5(사용자 일괄테스트 시점). Phase 5는 실마켓 전송 — 사용자 관여 필수"
  fixed: "라이브 서버 500(전 화면 다운) — 낡은 python 프로세스가 stale 코드 구동. 종료+재시작으로 해결. 코드 버그 아님."

deploy:
  target: "Fly.io (modeumjeon-team-share, region nrt) + Supabase Postgres"
  infra: "✅ 이미 완비 — Dockerfile · fly.toml · .dockerignore · config DATABASE_URL 지원 · requirements(gunicorn·psycopg2)"
  guide: docs/DEPLOY.md
  blockers: "① 6개 기능 구현 완료 ② Supabase 프로젝트 생성(사용자 계정) ③ fly auth login + secrets + deploy(사용자 계정). ②③은 Claude 대행 불가."

# ── 발견 사항 ──────────────────────────────────
findings:
  - "기존 실패: tests/inventory/test_sku_mapping.py 2건 (Korean 인코딩 추정) — Phase 1과 무관, 별도 처리 필요"
  - "작업 트리에 미커밋 변경 다수 (webapp/routes/bundles.py, 템플릿 5개, _demo_server.py 등) — 본 작업과 무관, 이전 세션 잔여"
