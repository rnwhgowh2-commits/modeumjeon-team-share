# .workflow-state.md

cycle_id: 20260820-000000
started_at: 2026-08-20T00:00:00Z
current_step: 7
mode: A   # 신규 기능 (samba-wave 수준 마켓 API 연동)
worktree: C:/dev/_wt_marketreg
worktree_branch: feature/policy-market-api-5

# ── 사이클 정의 ────────────────────────────────
title: 상품정책화 > 정책생성 → 5대 판매처 마켓 API 연동 (samba-wave 수준)
features:
  - "정책생성에서 입력한 13항목(상품명·카테고리·판매가·옵션·이미지·상세페이지·배송·고시정보·브랜드·원산지·KC인증·태그·모델번호·바코드·과세구분·미성년자구매·제조사)이 5개 마켓(스마트스토어·G마켓·옥션·11번가·롯데온)에 실제 API로 전송·등록되도록 함"

# ── 사전 조사 (Explore 에이전트, 867s) ──────────
preflight_findings:
  branch_divergence: "🔴🔴 CRITICAL — 현재 브랜치 feature/design-unify 에는 정책생성 기능 자체가 없음(2026-06-22 main 에서 분기 후 디자인 통일만 진행, 정책·마켓등록 코드 병합 안 됨). 실제 라이브 기능은 main(로컬 워크트리 C:/dev/moumjeon-backlog-wt)에 있음 — 배포 트리거도 main push. 사용자와 작업 브랜치 확정 필요."
  policy_feature_location: "webapp/routes/policy.py(main) — /policies 목록·상세·마켓탭. 항목정의 단일원천=lemouton/registration/process_rule_schema.py → lemouton/policy/fields.py 가 재사용. 값 저장=MarketPolicy/SetPolicyLink/BundlePolicyLink(JSON)"
  registration_engine: "lemouton/registration/service.py — 6마켓(정책화면 관리 기준, 사용자 요청 5개+쿠팡) 공통 등록 엔진. compile_*.py(스펙생성) → send_*.py(API전송) 구조"
  market_status:
    smartstore: "완성도 높음, 라이브 검증됨. 단 고시정보가 신발 카테고리로 하드코딩(smartstore.py:30-44) — 타 카테고리 오적용 위험"
    coupang: "완성도 높음(사용자 5개엔 없지만 정책화면엔 포함)"
    gmarket_auction_esm: "compile_more.py+send_more.py 전부 구현, 2026-07-21 라이브 실등록 검증 이력"
    eleven11: "구현됨. 고시정보 코드표를 공식문서에서 못 구해 임시 우회값 사용 중(send_more.py:366-370)"
    lotteon: "5마켓 중 가장 불확실 — required.py:337-338 에 '13항목 거의 전부 확인불가, 실등록으로만 검증가능'이라 명시됨"
  critical_blocker: "🔴🔴 LIVE_REGISTER_ARMED 환경변수(GitHub Actions 저장소 변수, service.py:120-121/:498-504/:345-351)가 꺼져있으면 정책값이 다 채워져도 전 마켓 호출이 blocked/LIVE_OFF 로 멈춤. 코드 내 토글 아니라 사장님이 화면에서 켤 방법 없음. 운영서버 실제값 미확인(리포지토리 설정 확인 필요) — '정책은 채웠는데 안 올라간다' 증상과 정확히 일치할 가능성"
  samba_wave_relation: "완전 별개 파이프라인. 정책등록은 lemouton/sourcing/models_v2.py UploadAccount(.env 기반 시크릿) 사용, samba-wave shared/market_accounts/client.py 는 이 테이블 전혀 참조 안 함 — 재사용 관계 없음(수집용 vs 등록용 분리)"
  existing_market_api_reuse: "shared/platforms/{smartstore,coupang,esm,eleven11,lotteon}/ 클라이언트는 재고·정산·주문조회와 신규상품등록(create_product/register_goods/register_product)에 동일 클라이언트 재사용 중 — 신규 스펙 필요 없음. 단 가격재고갱신 게이트(MOUM_LIVE_UPLOAD)와 신규등록 게이트(LIVE_REGISTER_ARMED)는 서로 다름"
  unresolved_RESOLVED:
    branch: "사용자 확정 A — main 기준 새 워크트리. C:/dev/_wt_marketreg 생성, branch feature/policy-market-api-5, origin/main(07c0cbf9) 기준"
    live_register_armed: "gh variable list 로 확인 — 0(꺼짐), 2026-07-21부터. 사용자 확정: 5마켓 다 테스트 끝나면 한번에 켠다(운영 GH변수는 보류)"
    stale_worktrees: "_wt_reg/_wt_multi/_wt_mktsend/policy-bar-ui/_wt_policy_ui/_wt_multipolicy/_wt_ppattach/_wt_proctab/_wt_optmkt/_wt_proc/_wt_proc_ui 11개 전수조사 완료 — 전부 죽음(머지완료 또는 상위호환 대체). 이어받을 것 없음. 참고할 머지PR: #440(6마켓동시등록) #691(정책13항목적용) #687~783(마켓전송탭) #1102(오늘아침, 배송비·반품비·원산지 기본값 버그수정)"
    per_market_test_without_prod_switch: "🟢 해결 — LIVE_REGISTER_ARMED 는 프로세스 환경변수라 로컬 개발서버에서만 켜서(운영 GH Actions 변수는 안 건드림) webapp/routes/live_send_test.py 의 arm=1 파라미터로 마켓별 실등록 테스트 가능. 운영 스위치는 그대로 0 유지"
  user_decisions:
    market_order: "완성도 순 — G마켓·옥션(ESM, 이미 라이브검증) → 스마트스토어(고시정보 신발하드코딩 수정 먼저) → 11번가(고시정보 임시값) → 롯데온(가장 불확실)"
    success_criteria: "마켓당 카테고리 1~2개로 실등록 성공(상품ID 수령)까지 확인되면 해당 마켓 종료"
    switch_timing: "5마켓 전부 테스트 끝나면 운영 LIVE_REGISTER_ARMED 를 한번에 켠다 (개별 마켓 검증은 로컬 arm=1 로 진행, 위 해결 참고)"

artifacts:
  context_md: docs/CONTEXT.md
  prd_md: null
  office_hours_output: null
  light_spec_md: null
  plan_md: null
  plan_review_md: null
  code_paths: []
  codex_review: null
  architecture_review: null
  retro_md: null

step_log:
  - step: 7
    skill_called: superpowers:subagent-driven-development
    status: in_progress
    note: "Task1~4 완료(각각 구현→spec리뷰→품질리뷰). 품질리뷰가 실결함 추가발견·즉시수정: ①ESM/11번가/롯데온 셋 다 안전장치 실패시 로그만 남던 것→_suspend_failed 플래그로 raw_json에 남게 수정 ②11번가·롯데온 둘 다 마켓응답이 거짓성공(200/returnCode성공이지만 실제상태 그대로)일 수 있어 get_product_detail 재조회검증 추가(기존 수동라우트 api_suspend_eleven11 방식 재사용, 롯데온은 구현자가 자체판단으로 필요성 확인 후 추가) ③스마트스토어(ESM)의 set_sold_out만 docstring에 재조회 권장 문구 없어 재조회 불필요로 판정(누락아님, 확인함). Task4(옥션 사이트할인 필드 노출)는 완료했으나 계획검토 중 더 큰 별개 공백 발견: EXTRA_ITEMS(마켓별 추가항목) 전체가 compile 단계에서 아예 안 읽혀 화면엔 저장돼도 API엔 하나도 안 실림(13항목과 무관한 배선공백) — 이번 사이클 범위 밖으로 판단해 task_9d4bf0ad로 별도세션 위임, Task6 계획에 '아직 안실림, 새버그 아님' 주석 추가. 커밋 5개(a3419fbb·8343b862·930e7a9b·8cffbf6b·e377f025·c8a7ae86). Task5 완료(tests/registration/+tests/policy/ 전체 2410 passed, 게이트테스트 79 passed 별도확인).

Task6(G마켓·옥션) 완료 — 2단계 실증:
①수기등록(경로A)으로 옥션 실제 등록 성공(상품번호 6478176871, 자동판매중지 코드 배선 확인) — 등록엔진(Task1 수정분) 자체 동작 증명
②/market-send(경로B, 정책기반)로 "메이트 TEST"(set_id=11, policy_id=5) 실전송 시도 — 컴파일단계에서 실패(카테고리·상세설명·고시정보·AS전화번호 미비)했으나 이는 이 테스트상품 데이터 미비 때문이지 코드결함 아님. 마켓 API 호출 자체는 안 나감(정상 — 컴파일 게이트가 먼저 막음). 정책→등록엔진 배선 자체는 컴파일단계 도달로 확인됨.
운영 자동화스위치(MOUM_LIVE_UPLOAD screen key)는 사용자가 짧게 켰다가(19:57 전송1건) 즉시 껐음(19:5x 재확인 완료, 미리보기 상태 복구).
🔴🔴 Task6 실행 중 중대발견 (아래 서술 계속)

── 사이클 일시중단 (2026-08-20 저녁) ──
Task 7~9(스마트스토어·11번가·롯데온 개별 실등록 재확인)는 사용자 라이브클릭 피로로 이번 세션에서 중단.
확보된 증거:
- 옥션·G마켓·11번가·롯데온: 전부 2026-07-21 사이클에 실제 등록 성공 이력 존재(진짜 마켓 상품번호 보유 — 옥션 6478176871·6477870979, G마켓 6478210710·6477875678, 11번가 9508477357·9508128090, 롯데온 LO2729209534·LO2729068316). 다만 이 세션에서 "다시 올리기"로 재확인 시도 중 테스트상품들의 선행자원(판매중 상태)이 만료된 것으로 보여 전부 "카테고리 필요"로 전환 — 재시도는 새 테스트상품 필요.
- 스마트스토어: 실등록 성공 이력 없음(한 번도 성공한 적 없음). 다만 PREFLIGHT 컴파일게이트가 카테고리·품질보증기준 누락을 정확히 감지해 마켓 호출을 막는 것은 실증(안전장치 정상).
- 정책→엔진 연결(/market-send): "메이트 TEST"(정책 실연결) 로 옥션 대상 실제 시도 → 컴파일단계에서 상세설명·고시정보·AS전화 누락으로 정상 차단됨(마켓 호출 0건) — 연결배선 자체는 증명, 완전 성공은 미확인.
남은 일(다음 세션): ①새 정상 테스트상품(선행자원 살아있는 것)으로 5마켓 각각 실등록 재확인 ②스마트스토어 최초 성공사례 확보 ③운영 LIVE_REGISTER_ARMED 최종 전환 여부 재논의. — 화면에 lemouton/send/(market_send.py, /market-send 탭)와 lemouton/registration/(이번에 고친 곳) 두 개의 별개 마켓전송 시스템이 있고, 후자를 부르는 UI가 templates 어디에도 없어(bulk/drafts.py register 엔드포인트 orphaned 가능성). 실제 라이브 상품("메이트" M20260730-000062)은 스마트스토어·쿠팡에 이미 등록됨 상태였는데 그게 어느 경로였는지 불명 — Explore 에이전트로 재조사 중(agentId 비공개). 운영 LIVE_REGISTER_ARMED=1 은 현재 켜진 채(조사 끝나면 방향 정해서 진행 또는 끔). 🔴 로컬 실등록 계획 변경 — _wt_marketreg 로 복사한 .env 의 DATABASE_URL 이 멈춰있는 옛 Supabase 프로젝트(faxhbkootagxnwqjakee) 를 가리켜 연결 자체가 실패(기존 memory project_supabase_egress_detail_html.md 와 동일 증상), 게다가 옥션/G마켓/11번가/롯데온 계정정보도 로컬 .env 에 없음(스마트스토어만 있음) → 사용자 결정: 로컬 대신 배포서버(AWS Lightsail)에서 검증. PR #1104 생성(feature/policy-market-api-5→main), CI(배포전 검사) 통과 대기중. 통과하면 병합→배포→운영 LIVE_REGISTER_ARMED 일시적으로 1로 전환→실등록 테스트→다시 0. 남은: Task6-9(마켓별 실등록검증, 매번 사용자확인 필요)·Task10(마무리)"
  - step: 1
    skill_called: grill-me
    status: completed
    artifact: docs/.workflow-state.md (본 파일 preflight_findings + user_decisions)
    note: "Explore 에이전트 2회 조사(정책/마켓현황, 기존 워크트리11개 전수) + AskUserQuestion 2회로 5개 결정 확정: 작업위치(main기준 새워크트리)·스위치상태(0)·죽은워크트리 없음 확인·순서(완성도순)·성공기준(카테고리1~2개 실등록)·스위치시점(로컬arm=1로 개별검증, 운영은 5개 끝나면 일괄)"
  - step: 2
    sub: 2a
    skill_called: grill-with-docs
    status: completed
    artifact: "C:/dev/_wt_marketreg/docs/CONTEXT.md"
    note: "코드에 이미 있던 자체 도메인 문서 발견·정리: required.py의 WIRING(배선여부 추적)·TO_VERIFY_BY_LIVE(문서로는 확인불가, 실등록만이 유일한 길 — 2026-08-02 사장님 확정 방법론) + docs/markets/lotteon.yaml 의 2026-07-21 라이브 등록 성공 실증(LO2729045338, 본보기복사 방식) 발견 — 롯데온은 '등록 자체'가 아니라 '13항목 필드매핑 확인'이 남은 일로 재정의됨. 옥션 사이트부담할인 필드 누락 버그(fields.py EXTRA_ITEMS only=['gmarket','lotteon'], 옥션 빠짐) 확인됨(required.py 주석과 fields.py 실코드 대조, 아직 안 고쳐져 있음)"
  - step: 2
    sub: 2b
    status: completed
    artifact: "C:/dev/_wt_marketreg/docs/ARCHITECTURE_FIRST_DRAFT.md"
    note: "새 폴더/모듈 불필요 — 기존 5개 파일(fields.py/required.py/smartstore.py/compile_more.py/send_more.py) 개보수로 충분. 사용자 승인(추천안 그대로)"
  - step: 2
    sub: 2c
    status: skipped
    note: "새 인터페이스·폴더 스켈레톤 불필요(기존 구조 재사용)라 생략 — ARCHITECTURE_FIRST_DRAFT.md 에 명시"
  - step: 3
    status: completed
    note: "흔들기4질문: 5마켓 전부 사용자 명시요청·핵심가치 직결·대체수단 없음 — 뺄 것 없음. 쿠팡 제외는 이미 STEP1에서 결정됨(중복 아님). Office Hours/CEO-ENG리뷰는 내부 운영기능 특성상 취지만 압축 적용 — 새로 발견된 리스크는 STEP4 스펙에 반영(실등록 테스트의 판매중지 자동화 여부, 옥션 사이트할인 버그 수정 범위 포함 여부)"
  - step: 4
    skill_called: superpowers:brainstorming (코드탐색으로 대체)
    status: completed
    artifact: "C:/dev/_wt_marketreg/docs/LIGHT_SPEC.md"
    note: "🔴 착수전 안전선결조건 발견: service.py:_send_live 의 등록직후 자동판매중지(mark_suspension)가 스마트스토어 전용 — G마켓·옥션·11번가·롯데온엔 없음. 각 마켓 작업에 change_status 자동전환 추가를 안전 선결과제로 포함. 마켓별 작업목록(버그+검증항목) 확정. 🟢 계획작성 중 재검증에서 스마트스토어 고시정보 하드코딩 버그는 오늘아침(08-20) 이미 merge·해결됨을 발견 — LIGHT_SPEC 정정(허위 버그 제거)"
  - step: 5
    skill_called: superpowers:writing-plans
    status: completed
    artifact: "C:/dev/_wt_marketreg/docs/superpowers/plans/2026-08-20-policy-market-api-5.md"
    note: "10태스크. 실제 코드 직접 읽고 작성(무플레이스홀더): ESM=shared/platforms/esm/inventory.py:set_sold_out, 11번가=products.py:stop_display, 롯데온=products.py:set_sale_status 3곳 다 이미 존재하는 함수를 등록직후 자동호출로 배선(Task1-3, 실제 mock 테스트 코드 포함). 옥션 사이트할인 버그 수정(Task4, fields.py:60 한 줄). Task6-9는 마켓별 실등록검증(TDD 아닌 절차형 — 실API라 사전 정답불가). Task10 마무리+운영게이트 전환은 사용자 명시승인 게이트로 분리(자동금지)"
  - step: 6
    skill_called: superpowers:code-reviewer (독립 에이전트)
    status: completed
    note: "🔴 실오류 3건 발견·수정: ①Task4 함수명 오류(items_for_market→실제는 item_keys_for) ②Task1 ESM 목테스트 spec dict 필드 누락으로 KeyError사망 위험 — 11개 필드 채움 ③Task1 안전장치 삽입위치 오류(옵션부착 블록 앞이 아니라 뒤, line 186 return 직전이어야 함, 안 그러면 옵션 붙이기 전에 판매중지되고 기존 롤백로직과 순서꼬임). Task2·3·나머지 patch경로·시그니처는 전부 실코드와 일치 확인됨. Task3 END/SOUT 선택근거를 코드주석엔 없어 직접 판단해 명시(SOUT는 재고연동이라 자동복구위험, END가 더 안전). 사용자가 코덱스 로그인 안 돼있음을 직접 지적(제가 --version만 보고 '설치됨'이라 잘못 안심시켰다가 login status 재확인해 정정 — Step8은 나중에 로그인하거나 대체수단 필요)"
