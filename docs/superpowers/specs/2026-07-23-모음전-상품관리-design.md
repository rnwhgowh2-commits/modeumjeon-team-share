# 모음전 상품관리 — 설계서

- 작성: 2026-07-23
- 범위: **1) 대시보드 + 2) 상품 상세·편집** (사장님 결정)
- 다음 사이클: 3) 신규 등록, 4) 타마켓 판매
- 관계: 상위 = 프로젝트 CLAUDE.md / 옆 = `2026-07-17-신규상품등록-가공템플릿-design.md`(대량등록)

---

## 1. 무엇을 만드나

모음전 모드에 **「상품관리」** 메뉴 하나를 새로 만든다. 여기서 사장님은

1. **전체 현황을 본다** — 마켓 > 계정 > 판매중/품절/판매중지/승인대기 건수
2. **모음전 상품을 고른다** — 28만 건에서 이름·브랜드·상품번호로 찾아 즐겨찾기
3. **고른 상품을 본다·고친다** — 마켓별 상품명·판매가·배송·옵션·이미지

### 왜 필요한가

지금은 **마켓에 뭐가 올라가 있는지 우리 프로그램이 모른다.** 아는 것은 두 가지뿐이다.

| 아는 것 | 어디에 | 한계 |
|---|---|---|
| 우리가 직접 연동한 모음전 구성 | `set_channels` | 사장님이 손으로 이은 것만 |
| 우리가 대량등록으로 올린 상품 | `product_drafts` | 프로그램으로 올린 것만 |

더망고도 이 한계는 같다. 더망고 원장은 **소싱처에서 수집한 상품**이고(화면의 「상품수집사이트」·「마켓등록/미등록」 필터가 근거), 마켓 등록 여부는 「내가 보냈다」는 자기 기록으로만 안다. **더망고를 쓰기 전에 올린 상품, 마켓에서 직접 올린 상품은 영영 안 보인다.**

이 설계는 **마켓에서 거꾸로 읽어와** 그 빈칸을 메운다.

---

## 2. 실측 근거

문서만 보고 설계하지 않았다. 라이브 서버에서 조회 API 만 불러 실측했다(2026-07-23).
재현: GitHub Actions → **「상품관리 실측(수동)」** → `script=1/2/3`.

### 2-1. 실제 상품 수

| 마켓 | 계정 | 상품 수 | 출처 |
|---|---:|---:|---|
| 롯데온 | 7 | **140,171** | `dataCount` 합 |
| 11번가 | 7 | 약 91,000 | 1계정 13,016 실측 × 7 (추정) |
| 쿠팡 | 7 | **29,244** | `inflow-status.registeredCount` 합 |
| 스마트스토어 | 5 | **9,555** | `totalElements` 합 |
| 옥션·G마켓 | 3+3 | **9,660** | 마스터 기준(옥션 4,8xx / 지마켓 4,8xx) |
| | | **약 28만** | |

계정별 최대는 롯데온 브랜드위시 **47,960**, 다음이 브랜드박스 33,870.

### 2-2. 마켓별 능력 (실측)

| 마켓 | 목록이 상품명을 주나 | 상태별 건수 즉답 | 상품명 검색 | 총건수 |
|---|:---:|:---:|:---:|---|
| 스마트스토어 | ✅ `name` | ✅ `productStatusTypes`+`totalElements` | ❌ | ✅ |
| 롯데온 | ✅ `spdNm` | ✅ `slStatCd`+`dataCount` | ❌ (파라미터 없음) | ✅ |
| 옥션·G마켓 | ✅ `goodsName` | ✅ `query.sellStatus`+`totalItems` | ✅ `query.keyword` | ✅ |
| 쿠팡 | ✅ | ❌ (페이징 필요) | ✅ `sellerProductName` | ✅ `inflow-status` |
| 11번가 | ✅ `prdNm` | ❌ (페이징 필요) | ✅ `prdNm` | ❌ (페이징으로 셈) |

**핵심: 6마켓 전부 목록에서 상품명·상태를 준다** → 통째로 캐시할 수 있다.

### 2-3. DB 여유

현재 **179MB / 500MB**(Supabase 무료). 상위 테이블 = `crawl_deltas` 62MB(19만 행), `market_categories` 31MB.
28만 건 머리글 캐시는 인덱스 포함 **약 170MB** 예상 → 합계 약 350MB, **들어간다.**

---

## 3. 이 조사에서 고친 것 — 옥션·G마켓 조용한 실패

상품관리와 별개로, 지도 정독 중 **살아있는 버그**를 찾아 먼저 고쳤다.

ESM `POST /item/v1/goods/search` 는 검색·상태 조건을 **`query` 객체 안**에 넣어야 한다. 최상위에 두면 **에러 없이 조건을 통째로 무시**하고 전체를 돌려준다.

| 요청 모양 | 「판매중만」 결과 |
|---|---|
| 최상위 `sellStatus=11` (고치기 전) | 3,260 = 전체 (무시) |
| `query.sellStatus=[11]` (지도 example) | **3,150** |

같은 방식으로 판매중지 109 · 품절 0 · `keyword="니트"` 46 · 없는 낱말 0 · 옥션만 1,605 · 지마켓만 1,655.

**낸 손해 2건**
- `registration/send_more.py:70` — 옥션 상품 등록 시 「판매중 기존 상품」에서 출하지·발송정책을 베끼는데, 필터가 무시되어 **판매중지 상품을 본보기로 집을 수 있었다.**
- `webapp/routes/live_send_test.py` — 「ESM keyword 는 무시됨」이라 결론짓고 5,000건을 10페이지 훑어 22초 걸려 거르는 우회로가 들어가 있었다. **원인은 우리 요청 모양이었다.**

수정: `shared/platforms/esm/products.py::search_goods` + 회귀 테스트 `tests/platforms/test_esm_goods_search_query.py` 9건.

---

## 4. 데이터 모델

### 4-1. 새 테이블 2개

**`market_products` — 마켓 상품 머리글 캐시** (약 28만 행)

| 열 | 뜻 | 비고 |
|---|---|---|
| `market`, `account_key` | 어느 마켓 어느 계정 | |
| `market_product_id` | 마켓 상품번호 | 스스=channelProductNo · 롯데온=spdNo · ESM=goodsNo |
| `site_product_id` | 사이트별 번호 | ESM 전용(옥션/지마켓이 갈림) |
| `name` | 상품명 | ★ 롯데온은 `&lt;` HTML 이스케이프로 온다 — **풀어서 저장** |
| `status` | 통일 4상태 | `sale` / `soldout` / `stopped` / `waiting` |
| `raw_status` | 마켓 원본 코드 | 거짓 통일 방지 — 원본을 반드시 남긴다 |
| `sale_price` | 판매가 | 마켓이 안 주면 `NULL`(0 금지) |
| `registered_at` | 마켓 등록일 | |
| `synced_at` | 마지막으로 확인한 시각 | 화면에 그대로 표시 |
| `set_id` | 모음전 상품으로 묶였으면 그 번호 | `NULL` = 아직 안 담음 |

- 유일 제약 `(market, account_key, market_product_id)`
- 인덱스 `(market, account_key, status)` · `(set_id)` · 상품명 부분검색용 GIN(pg_trgm)
- **지운 행은 지우지 않는다** — `deleted_at` 으로 표시(마켓에서 사라진 것도 이력)

**`market_product_counts` — 대시보드 건수 스냅샷** (수십 행)

`market` · `account_key` · `status` · `count` · `measured_at` · `source`(`api` \| `cache`)
→ 화면은 이 표만 읽는다. **대시보드는 항상 즉시 뜬다.**

### 4-2. 묶음은 새 테이블 — ProductSet 은 못 쓴다 (2026-07-24 정정)

애초에 「즐겨찾기 = `product_sets` 승격」으로 적었으나, 구현 계획을 쓰며 코드를 읽어보니 막힌다.

```
lemouton/sets/models.py:22
    model_code = Column(String(64), ForeignKey("models.model_code"),
                        nullable=False, index=True)
```

`ProductSet` 은 **소싱처 모델에 반드시 묶인다.** 마켓에서 거꾸로 긁어온 상품에는 소싱처 모델이 없다 — 더망고를 쓰기 전에 올린 상품, 마켓에서 직접 올린 상품이 그렇다. **그게 이 기능이 존재하는 이유다.**

그래서 묶음은 새 테이블로 만든다.

```
market_product_groups        내가 관리하는 상품 1건 (= 모음전 상품, 즐겨찾기의 실체)
  └ market_products.group_id 마켓 × 계정마다 1행        ← 1:N 이 여기서 갈림
```

- `model_code`(nullable) — 소싱처 모델이 있으면 채운다. FK 는 걸지 않는다
- `set_id`(nullable) — 나중에 `ProductSet` 과 이을 자리. 지금은 늘 비어 있다

**「모음전으로 담기」가 하는 일**
1. `market_product_groups` 에 1건 생성 (대표 상품 기준)
2. 고른 마켓 상품마다 `market_products.group_id` 기록

옵션 연동(`set_channel_options`)은 2단계에서 다시 본다 — 즐겨찾기한 상품만 옵션을 저장하므로 그때 붙인다. **기존 테이블은 하나도 건드리지 않는다.**

---

## 5. 동기화

### 5-1. 밤에 한 번 전체 훑기

새벽 3시. 마켓별 페이징으로 머리글만 받아 `market_products` 에 upsert.

| 마켓 | 페이지당 | 예상 호출 |
|---|---:|---:|
| 롯데온 | 100 | 1,402 |
| 11번가 | 100 | 910 |
| 쿠팡 | 100 | 292 |
| 스마트스토어 | 100 | 96 |
| 옥션·G마켓 | 500 | 20 |
| | | **약 2,720회** |

실측 속도(11번가 131콜 77초 = 0.59초/콜) 기준 **30~60분** 예상. 밤에 도는 일이라 문제 없다.

**계정 하나가 실패해도 나머지는 계속한다.** 실패한 계정은 화면에 「마지막 확인: 어제」로 정직하게 뜬다 — 조용히 옛 숫자를 최신인 척 보여주지 않는다.

### 5-2. 낮에는

- 계정 카드마다 **「지금 동기화」** 버튼 → 그 계정만 다시 훑는다
- 대시보드 건수는 **즉답 되는 마켓만 그때그때 새로** 받는다(스스·롯데온·ESM, 몇 초)
- 쿠팡은 `inflow-status` 로 총건수만 즉답(상태별은 캐시 기준)

### 5-3. 나중에 — 바뀐 것만 받기

지도 정독으로 확인한 델타 조회 수단(이번 범위 아님, 기록만):
- 11번가 `schDateType=4`(상품수정일) + 기간
- ESM `query.updatedDate.from/to`
- 쿠팡 `time-frame`(생성일만 — 수정은 못 잡음)
- 스마트스토어 `periodType`+`fromDate`/`toDate` (실측: 먹힘)
- 롯데온 — 등록일만, 수정일 없음

---

## 6. 화면 — 사장님 확정 (2026-07-24)

모음전 모드 사이드바 `판매` 그룹에 **「상품관리」** 한 줄 추가. 안에서 가로탭.

시안 65장을 항목별로 제시해 7가지를 확정받았다.
시안 원본 = `프로그램/_시스템/docs/mockups/product_mgmt_variants.html` (65장) ·
`product_mgmt_mix.html` (섞기 15장). 배포되면 `/mockup/product_mgmt_variants.html` 로도 열린다.

| 항목 | 확정 | 무엇인가 |
|---|---|---|
| ① 화면 오가기 | **가로 탭 + 그 자리에서 펼치기 (나머지가 얇아짐)** | 탭으로 갈아타되 상세는 목록 안에서 펼침. 펼치면 **다른 줄이 한 줄로 접혀** 화면 안에 다 들어온다 |
| ② 마켓·계정 숫자 | **모음전 / 대량등록 탭 2개** | 대시보드 안에서 갈라 본다 |
| ③ 상태 4가지 | **숫자 + 색 점** | 숫자를 그대로 크게, 앞에 색 점만 |
| ④ 찾아서 담기 | **치는 대로 결과가 바뀜** | 「찾기」 버튼 없음. 담은 것은 오른쪽에 쌓인다 |
| ⑤ 상품 묶기 | **대표를 정하고 붙이기** | 기준 상품 하나를 세우고 나머지를 거기에 붙인다 |
| ⑥ 마켓별 보기 | **마켓마다 카드 한 장** | 카드를 옆으로 나란히 놓고 훑는다 |
| ⑦ 마켓에 반영 | **고치기 → 확인 → 보내기 3단계** | 마지막에 전 → 후 대조를 보고 보낸다 |

### 6-1. 대시보드 (②③ 확정 반영)

- 상단 가로탭 5개: `현황 보기 / 상품 담기 / 상품 상세 / 신규 등록 / 타마켓 판매`
  (뒤 2개는 다음 사이클 — 자리만 잡아둔다)
- **탭 2개: 모음전(기본) / 대량등록** — 사장님 지시대로 대량등록 현황도 모음전 탭에서 본다
  - **모음전 탭** = `set_id` 가 있는 상품 · **대량등록 탭** = `product_drafts` 로 우리가 올린 것
  - 대량등록 모드의 기존 「📦 상품관리」 탭은 **이 화면으로 링크만** 남긴다(중복 제거)
- 마켓 > 계정 > 4상태를 **표**로. 상태는 **숫자 그대로 + 앞에 색 점**(③)
  — 판매중 초록 · 품절 노랑 · 판매중지 회색 · 승인대기 파랑
- 「마지막 확인: 오늘 새벽 3시」 항상 표시 · 계정마다 「지금 동기화」
- 성과 숫자(매출·마진)는 기존 주문내역·마진계산기에서 끌어온다

### 6-2. 모음전 상품 담기 (④⑤ 확정 반영)

- **치는 대로 결과가 바뀐다** — 「찾기」 버튼 없음. 상품명·브랜드·상품번호 모두 우리 캐시에서 → 즉시
- 화면 좌우 2단: 왼쪽 결과 목록 / **오른쪽에 담을 목록이 쌓인다**
- 묶기 = **대표를 정하고 붙이기**(⑤)
  1. 기준이 될 상품 하나를 「대표」로 세운다
  2. 오른쪽에서 찾아 **「선택한 N개 붙이기」**
  3. 붙인 마켓 상품이 대표 카드 아래 목록으로 쌓이고, × 로 뗄 수 있다
- 자동 확정 금지 — 프로그램은 후보만 보여주고 확정은 사장님이

### 6-3. 상품 상세·편집 (①⑥⑦ 확정 반영)

- **목록 안에서 펼친다**(①) — 펼치면 위아래 다른 줄이 **한 줄로 얇게 접혀** 화면 안에 다 들어온다
  - ⚠️ 접힌 줄에도 **상품명과 판매가는 남긴다** — 무슨 상품이 있었는지 사라지면 안 된다
- 펼친 칸 안에서 **마켓마다 카드 한 장**(⑥)을 옆으로 나란히
  - 카드 = 마켓 색띠 머리글 + 대표 사진 + 마켓 상품명 · 판매가 · 배송 · 옵션 수 + 「이 마켓만 고치기」
  - ⚠️ 이 안에는 **「값이 다른 곳」 표시가 없다** — 사장님이 눈으로 견준다(확정 시 안내함).
    나중에 필요하면 노란 표시만 얹으면 된다(구조 변경 없음)
- 바로가기 **2개**: 모음전 구성 화면 + 손님이 보는 마켓 상품 페이지
- 편집: 상품명·카테고리·판매가·옵션·이미지·상세설명·배송·고시정보·브랜드·원산지·KC인증·금지어·태그
- **저장 = 우리 DB에만.** 마켓 전송은 **3단계**(⑦): `고치기 → 확인하기 → 보내기`
  - 확인 단계에서 마켓별로 **전 → 후**를 나란히 보여주고, 옆에 사람 말로 된 메모
    (예: 「2,000원 내려갑니다」 「5만원 미만 주문에 붙습니다」)
  - 전 → 후는 **3칸 구조**(현재=우측 · 화살표=가운데 · 보낼값=좌측)로 화살표를 세로로 맞춘다

---

## 7. 함정 (실측으로 확인한 것)

1. **ESM 조건은 `query` 안에** — 밖에 두면 조용히 무시(§3)
2. **롯데온 `pageNo`·`rowsPerPage` 필수** — 빠지면 `returnCode 9000`("처리 중 오류"). 권한 문제로 오해하기 쉽다
3. **롯데온 상품명은 HTML 이스케이프**(`&lt;매장정품&gt;`) — 풀어서 저장하지 않으면 검색이 안 걸린다
4. **스마트스토어는 상품명 검색이 안 된다** — 후보 16종 전부 무시 또는 400. 캐시에서 찾는다
5. **쿠팡·11번가는 총건수 필드가 없다** — 세려면 끝까지 넘겨야 한다
6. **옥션·G마켓 마스터번호는 공용** — 사이트별 수는 `query.siteId` 로 갈라야 나온다
7. **지도 응답 필드 목록은 축약본일 수 있다** — 롯데온이 상품명을 안 주는 줄 알았으나 실제로는 25개 필드가 온다. 지도만 믿지 말고 한 번 열어볼 것

---

## 8. 안 하는 것 (이번 범위 밖)

- 3) 신규 상품 등록 · 4) 타마켓 판매 — 다음 사이클
- 옵션 전량 캐시 — 즐겨찾기한 상품만 옵션을 저장한다(150만 행은 DB 한도 초과)
- 델타 동기화 — 수단은 §5-3 에 기록. 전체 훑기가 30~60분이면 충분하므로 미룬다
- 소싱처 수집 상품과의 자동 짝짓기 — 규칙이 틀리면 중복이 생긴다. 별도 검증 필요

---

## 9. 남은 확인 (구현 중에 실측)

- 11번가 7계정 전체 건수 (1계정만 실측 — 91,000 은 추정치)
- 옥션·G마켓 목록 응답에 상태 필드(`sellStatus`)가 행마다 오는지
- 롯데온 14만 건 전체 훑기의 실제 소요 (1계정 표본만 쟀다)
- 마켓별 상품 상태 원본 코드 → 통일 4상태 매핑표 확정 (쿠팡 `PARTIAL_APPROVED` 등)
