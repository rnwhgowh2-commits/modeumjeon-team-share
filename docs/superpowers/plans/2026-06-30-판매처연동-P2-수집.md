# 판매처연동 P2 — 현재값 수집 + 변동 기록 구현 플랜

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development 로 task별 TDD 실행.

**Goal:** 판매처 채널의 현재 재고/가격을 API로 당겨와 SetChannelOption.mkt_* 에 저장하고, 바뀐 값은 ChannelChangeEvent(source=market)로 기록. 수동 새로고침 라우트 + 전체 일괄.

**Architecture:** 기존 fetch_market_options(P0)·record_change(P1) 재사용. collect_service.collect_channel 이 fetch→매칭(market_option_id)→diff→record_change→mkt_* 갱신. 쿠팡 재고는 null 유지. 마켓에 쓰지 않음(읽기+로컬 저장).

**Tech Stack:** SQLAlchemy, pytest. 재사용: market_fetch.fetch_market_options, set_link_service._resolve_env_prefix, change_service.record_change.

스펙: docs/superpowers/specs/2026-06-29-판매처연동-현재값수집-변동이력-알림-design.md (3)

범위: 판매처(source=market) 수집만. 소싱(source=source) 변동기록은 크롤 저장경로 훅이라 P2b 후속.

---

### Task 1: 채널 수집 서비스 collect_channel

**Files:**
- Create: 프로그램/_시스템/lemouton/sets/collect_service.py
- Test: 프로그램/_시스템/tests/sets/test_collect_service.py

핵심 로직:
- SetChannel 로드. market_product_id 없으면 {ok:False, error:상품번호 미입력}.
- env_prefix = _resolve_env_prefix(session, ch.market, ch.account_key)
- fr = fetcher(ch.market, ch.market_product_id, env_prefix=env_prefix). fr.success False면 {ok:False, error:fr.error} (mkt_* 덮어쓰지 않음, 폴백 금지).
- mo_map = {str(o.option_id): o for o in fr.options}
- 이 채널 SetChannelOption 중 status==matched 이고 market_option_id 있는 행마다:
    mo = mo_map.get(sco.market_option_id); if mo is None: continue
    new_stock = None if ch.market==coupang else mo.stock
    new_price = mo.price
    record_change(field=stock, source=market, prev=sco.mkt_stock, next=new_stock)
    record_change(field=price, source=market, prev=sco.mkt_price, next=new_price)
    sco.mkt_stock=new_stock; sco.mkt_price=new_price; sco.mkt_fetched_at=now
- return {ok:True, fetched:행수, changed:변동건수}

- [ ] Step 1: 실패 테스트

    import pytest
    from sqlalchemy import create_engine
    from sqlalchemy.orm import Session
    from shared.db import Base
    for _m in ("lemouton.sourcing.models","lemouton.sets.models"):
        try: __import__(_m)
        except ImportError: pass
    from lemouton.sets.models import SetChannel, SetChannelOption, ChannelChangeEvent
    from lemouton.sets import collect_service as col

    class _MO:
        def __init__(self, oid, stock, price): self.option_id=oid; self.stock=stock; self.price=price; self.color=""; self.size=""; self.usable=True
    class _FR:
        def __init__(self, options): self.success=True; self.product_name="P"; self.options=options; self.error=None

    @pytest.fixture
    def db():
        eng=create_engine("sqlite://"); Base.metadata.create_all(eng); s=Session(eng); yield s; s.close()

    def _seed(db, market):
        ch=SetChannel(set_id=1, market=market, account_key="a", market_product_id="999", status="linked")
        db.add(ch); db.flush()
        db.add(SetChannelOption(channel_id=ch.id, canonical_sku="SKU1", market_option_id="11", status="matched", mkt_stock=8, mkt_price=140000))
        db.commit(); return ch

    def test_collect_smartstore_records_change(db):
        ch=_seed(db,"smartstore")
        fetcher=lambda market,pid,env_prefix=None: _FR([_MO("11", 0, 133900)])
        r=col.collect_channel(db, ch.id, fetcher=fetcher); db.commit()
        sco=db.query(SetChannelOption).filter_by(channel_id=ch.id).first()
        evs=db.query(ChannelChangeEvent).all()
        assert r["ok"] and sco.mkt_stock==0 and sco.mkt_price==133900
        assert {(e.field,e.prev_value,e.next_value) for e in evs} == {("stock",8,0),("price",140000,133900)}

    def test_collect_coupang_stock_null(db):
        ch=_seed(db,"coupang")
        fetcher=lambda market,pid,env_prefix=None: _FR([_MO("11", 0, 133900)])
        col.collect_channel(db, ch.id, fetcher=fetcher); db.commit()
        sco=db.query(SetChannelOption).filter_by(channel_id=ch.id).first()
        assert sco.mkt_stock is None and sco.mkt_price==133900

- [ ] Step 2: 실패 확인 — cd 프로그램/_시스템 && python -m pytest tests/sets/test_collect_service.py -q → FAIL(ModuleNotFound)

- [ ] Step 3: collect_service.py 작성(위 로직). 임포트: SetChannel, SetChannelOption (models); record_change (change_service); _resolve_env_prefix (set_link_service); fetch_market_options (uploader.market_fetch); datetime,timezone. 기본 fetcher=fetch_market_options.

- [ ] Step 4: 통과 — python -m pytest tests/sets/test_collect_service.py -q → PASS(2)

- [ ] Step 5: 커밋 — feat(sets): 판매처 현재값 수집 collect_channel(diff 변동기록·쿠팡재고null)

---

### Task 2: 수동 새로고침 라우트 (채널 1개)

**Files:**
- Modify: 프로그램/_시스템/webapp/routes/sets_api.py (bp url_prefix=/api)
- Test: 프로그램/_시스템/tests/sets/test_sets_api_routes.py (등록 스모크)

라우트 (channels/<id>/product 근처 추가):

    @bp.post("/sets/channel/<int:channel_id>/collect")
    def collect_channel_route(channel_id):
        from lemouton.sets.collect_service import collect_channel
        s = SessionLocal()
        try:
            r = collect_channel(s, channel_id)
            if not r.get("ok"):
                return _err(r.get("error") or "수집 실패")
            s.commit()
            return jsonify(r)
        finally:
            s.close()

- [ ] Step 1: 기존 test_blueprint_registers_expected_routes 를 read 해서 동일 패턴으로 경로 추가: /api/sets/channel/<int:channel_id>/collect

- [ ] Step 2: 실패 확인 — cd 프로그램/_시스템 && python -m pytest tests/sets/test_sets_api_routes.py -q → FAIL

- [ ] Step 3: sets_api.py 에 위 라우트 추가

- [ ] Step 4: 통과 — python -m pytest tests/sets/test_sets_api_routes.py -q → PASS

- [ ] Step 5: 커밋 — feat(sets): 채널 현재값 수동 새로고침 라우트 collect

---

### Task 3: 구성 단위 일괄 수집

**Files:**
- Modify: 프로그램/_시스템/lemouton/sets/collect_service.py (collect_set)
- Modify: 프로그램/_시스템/webapp/routes/sets_api.py (라우트)
- Test: 프로그램/_시스템/tests/sets/test_collect_service.py (이어서)

collect_set(session, set_id, fetcher=fetch_market_options):
- SetChannel 중 set_id 일치·market_product_id 있는 채널 순회 → collect_channel
- return {ok:True, channels:[{channel_id, market, **r}]}

라우트:

    @bp.post("/sets/<int:set_id>/collect")
    def collect_set_route(set_id):
        from lemouton.sets.collect_service import collect_set
        s = SessionLocal()
        try:
            r = collect_set(s, set_id); s.commit(); return jsonify(r)
        finally:
            s.close()

- [ ] Step 1: 실패 테스트

    def test_collect_set_iterates_channels(db):
        ch=_seed(db,"smartstore")
        fetcher=lambda market,pid,env_prefix=None: _FR([_MO("11", 3, 133900)])
        r=col.collect_set(db, 1, fetcher=fetcher); db.commit()
        assert r["ok"] and len(r["channels"])==1 and r["channels"][0]["market"]=="smartstore"

- [ ] Step 2: 실패 확인 — python -m pytest tests/sets/test_collect_service.py::test_collect_set_iterates_channels -q → FAIL(collect_set 없음)

- [ ] Step 3: collect_set 추가 + 라우트 추가 + 스모크 경로 /api/sets/<int:set_id>/collect 등록 테스트 추가

- [ ] Step 4: 통과 — python -m pytest tests/sets -q → 전부 PASS

- [ ] Step 5: 커밋 — feat(sets): 구성 단위 일괄 수집 collect_set + 라우트

---

## 자가 점검 (스펙 3 대조)
- 3.2 fetch diff record_change(source=market) mkt_* 갱신 = Task1 ✅
- 쿠팡 stock=null = Task1 분기 ✅
- 3.1 수동 버튼 = Task2 / 일괄 = Task3 ✅
- 3.3 실패시 옛값유지·폴백금지 = Task1 fr.success False 미갱신 return ✅
- 타입 일관: collect_channel/collect_set {ok,...}, record_change 시그니처 P1 동일
- 라우트 단위테스트는 등록 스모크만(SessionLocal 내부=기존 패턴) → 라이브 검증 필수

## 비범위 (P2b 후속)
- 자동 주기 수집(스케줄러 배선 + 크롤 직후 훅) — 서버사이드·환경의존이라 분리
- 소싱(source=source) 변동기록 — 크롤 저장경로 훅, 별도

## 실행 핸드오프
배포: origin/main 격리 워크트리 PR 머지. 라이브 검증: 르무통 메이트 모음전 POST /api/sets/<id>/collect SetChannelOption.mkt_*·mkt_fetched_at 채워지고 변동옵션 channel_change_events 기록·쿠팡 mkt_stock=null 확인.
