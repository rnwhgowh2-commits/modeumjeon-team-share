# 주문내역 배송검사 탭 — 더망고 업로드·중복송장·배송흐름·배송방식 구현 플랜

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 더망고 주문내역 엑셀을 업로드해 DB에 누적 저장하고, 중복송장·배송흐름 미발생을 검사하며 배송방식(까대기/직배)을 관리하는 "배송검사" 탭을 주문내역 화면에 추가한다.

**Architecture:** 순수 SQLAlchemy `Base` 모델 2종(`MangoOrder`·`MangoStatusMap`)을 신규 도메인 `lemouton/delivery/`에 정의. 더망고 .xls(HTML 위장) 파서 → upsert 서비스 → 구분자 매핑·배송방식 판정·검사 2종. UI는 기존 `orders` 블루프린트에 `inspect` 하위탭 + POST 엔드포인트로 확장.

**Tech Stack:** Flask 3, SQLAlchemy 2.0(DeclarativeBase), BeautifulSoup4(html.parser), pytest(in-memory SQLite). 신규 의존성 없음.

**설계 원천:** `docs/superpowers/specs/2026-07-11-주문-배송검사-더망고업로드-design.md` · 확정 시안: 배송방식=토글칩 / 업로드=카드 드롭존 / 매핑=설정모달 / 일괄=라디오3택 / 검사결과=요약카드+필터+통합표 / 정상=초록.

**실행 위치:** 모든 `pytest`·`python` 명령은 cwd = `프로그램/_시스템` 에서 실행한다 (`from shared.db import Base` import 때문).

---

## 파일 구조

- **Create** `프로그램/_시스템/lemouton/delivery/__init__.py` — 빈 패키지 파일
- **Create** `프로그램/_시스템/lemouton/delivery/models.py` — `MangoOrder`, `MangoStatusMap`
- **Create** `프로그램/_시스템/lemouton/delivery/mango_parser.py` — `parse_mango_xls`, `MangoParseError`
- **Create** `프로그램/_시스템/lemouton/delivery/service.py` — 시드·upsert·배송방식·검사 로직
- **Modify** `프로그램/_시스템/app.py` — `lemouton.delivery.models` import 1줄 (create_all 등록)
- **Modify** `프로그램/_시스템/webapp/routes/orders.py` — `inspect` 서브탭 + 업로드/데이터/배송방식/매핑 엔드포인트
- **Modify** `프로그램/_시스템/webapp/templates/orders/index.html` — `{% elif tab == 'inspect' %}` UI 블록
- **Create** `프로그램/_시스템/tests/delivery/__init__.py` — 빈 파일
- **Create** `프로그램/_시스템/tests/delivery/test_mango_parser.py`
- **Create** `프로그램/_시스템/tests/delivery/test_delivery_service.py`

---

## Task 1: 신규 도메인 모델 (MangoOrder · MangoStatusMap)

**Files:**
- Create: `프로그램/_시스템/lemouton/delivery/__init__.py`
- Create: `프로그램/_시스템/lemouton/delivery/models.py`
- Modify: `프로그램/_시스템/app.py` (모델 import 구역, 61~93행 부근)
- Test: `프로그램/_시스템/tests/delivery/test_delivery_service.py`

- [ ] **Step 1: 빈 패키지 파일 생성**

Create `프로그램/_시스템/lemouton/delivery/__init__.py` (빈 파일) 과 `프로그램/_시스템/tests/delivery/__init__.py` (빈 파일).

- [ ] **Step 2: 실패 테스트 작성**

Create `프로그램/_시스템/tests/delivery/test_delivery_service.py`:

```python
import pytest
from sqlalchemy import create_engine
from sqlalchemy.orm import Session

from shared.db import Base
import lemouton.delivery.models as M


@pytest.fixture
def db():
    eng = create_engine("sqlite://")
    Base.metadata.create_all(eng)
    s = Session(eng)
    yield s
    s.close()


def test_models_create(db):
    o = M.MangoOrder(mango_uid="12039", recipient="이주연", market_name="쿠팡")
    db.add(o)
    db.commit()
    got = db.query(M.MangoOrder).filter_by(mango_uid="12039").one()
    assert got.recipient == "이주연"
    assert got.delivery_method == "미지정"
    assert got.delivery_method_source == "자동"

    sm = M.MangoStatusMap(status_value="해외현지배송중", meaning="해외배송중",
                          default_method="까대기", is_flow_check_target=False)
    db.add(sm)
    db.commit()
    assert db.query(M.MangoStatusMap).filter_by(status_value="해외현지배송중").one().default_method == "까대기"
```

- [ ] **Step 3: 테스트 실패 확인**

Run: `python -m pytest tests/delivery/test_delivery_service.py::test_models_create -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'lemouton.delivery.models'`

- [ ] **Step 4: 모델 구현**

Create `프로그램/_시스템/lemouton/delivery/models.py`:

```python
from datetime import datetime, timezone

from sqlalchemy import (Column, Integer, String, Text, Boolean, DateTime, JSON)

from shared.db import Base


def _now():
    return datetime.now(timezone.utc)


class MangoOrder(Base):
    """더망고 주문내역 엑셀 1행 = 1주문 (mango_uid 기준 upsert)."""
    __tablename__ = "mango_orders"

    id = Column(Integer, primary_key=True, autoincrement=True)
    mango_uid = Column(String(32), unique=True, nullable=False, index=True)  # P 더망고주문고유번호
    market_order_no = Column(String(64), index=True)   # C 마켓주문번호
    market_name = Column(String(32))                    # B 마켓명
    ordered_at = Column(String(32))                     # A 마켓주문일자 (원문 문자열)
    recipient = Column(String(64))                      # D 수령인명
    product_name = Column(Text)                         # E 마켓상품명
    option1 = Column(String(255))                       # F 옵션1
    phone = Column(String(32))                          # O 휴대폰번호
    invoice_no = Column(String(64))                     # K 국내송장번호 (없으면 None)
    courier = Column(String(32))                        # J 국내송장번호 택배사
    mango_status = Column(String(64), index=True)       # L 더망고주문상태 (구분자 원본값)
    market_status = Column(String(64))                  # M 마켓주문상태
    memo = Column(Text)                                 # N 간단메모

    delivery_method = Column(String(8), default="미지정", nullable=False)          # 까대기/직배/미지정
    delivery_method_source = Column(String(8), default="자동", nullable=False)     # 자동/일괄/수기

    invoice_history = Column(JSON, default=list)        # [{"invoice": "...", "at": iso}]
    is_duplicate_invoice = Column(Boolean, default=False, nullable=False)

    first_uploaded_at = Column(DateTime, default=_now)
    last_uploaded_at = Column(DateTime, default=_now, onupdate=_now)
    raw = Column(JSON)                                  # 원본 행 dict (감사용)


class MangoStatusMap(Base):
    """L(더망고주문상태) 원본값 → 의미·기본 배송방식·배송흐름 검사대상 매핑. 편집 가능."""
    __tablename__ = "mango_status_map"

    id = Column(Integer, primary_key=True, autoincrement=True)
    status_value = Column(String(64), unique=True, nullable=False)   # 예: "해외현지배송중"
    meaning = Column(String(16), default="기타")                     # 배송전/해외배송중/국내배송중/배송완료/취소반품교환/기타
    default_method = Column(String(8), default="미지정")             # 까대기/직배/미지정
    is_flow_check_target = Column(Boolean, default=False, nullable=False)
    sort_order = Column(Integer, default=100)
    editable = Column(Boolean, default=True, nullable=False)
```

- [ ] **Step 5: app.py 에 모델 import 등록**

Modify `프로그램/_시스템/app.py` — 다른 도메인 models import 들(61~93행 부근, `# noqa: F401` 붙은 줄들) 바로 아래에 추가:

```python
    import lemouton.delivery.models  # noqa: F401  # 배송검사 (MangoOrder, MangoStatusMap)
```

- [ ] **Step 6: 테스트 통과 확인**

Run: `python -m pytest tests/delivery/test_delivery_service.py::test_models_create -v`
Expected: PASS

- [ ] **Step 7: 커밋**

```bash
git add "프로그램/_시스템/lemouton/delivery/__init__.py" "프로그램/_시스템/lemouton/delivery/models.py" "프로그램/_시스템/app.py" "프로그램/_시스템/tests/delivery/__init__.py" "프로그램/_시스템/tests/delivery/test_delivery_service.py"
git commit -m "feat(delivery): 배송검사 모델 MangoOrder·MangoStatusMap 추가"
```

---

## Task 2: 더망고 .xls 파서 (HTML 위장 파일)

**Files:**
- Create: `프로그램/_시스템/lemouton/delivery/mango_parser.py`
- Test: `프로그램/_시스템/tests/delivery/test_mango_parser.py`

**배경:** 더망고 엑셀은 `.xls` 확장자지만 실체는 HTML(`<table>`) + 개행 `\r\n  ` 다수. 16열 고정(A~P). 프레임셋 껍데기 파일(실제 표 없음)은 명확히 실패시킨다(조용한 성공 금지).

- [ ] **Step 1: 실패 테스트 작성**

Create `프로그램/_시스템/tests/delivery/test_mango_parser.py`:

```python
import pytest
from lemouton.delivery.mango_parser import parse_mango_xls, MangoParseError

HEADERS = ["마켓주문일자", "마켓명", "마켓주문번호", "수령인명", "마켓상품명",
           "옵션1", "사이트주문번호", "구매가격", "국제운송료", "국내송장번호 택배사",
           "국내송장번호", "더망고주문상태 (사용자\r\n  연동)", "마켓주문상태 (오픈 마켓\r\n  연동)",
           "간단메모", "휴대폰번호", "더망고주문고유번호"]


def _sheet_html(data_rows):
    def tr(cells):
        return "<tr>" + "".join(f"<td>{c}</td>" for c in cells) + "</tr>"
    body = tr(HEADERS) + "".join(tr(r) for r in data_rows)
    return "<html><body><table>" + body + "</table></body></html>"


def test_parse_basic_rows():
    html = _sheet_html([
        ["2026-07-11 07:28:40", "쿠팡", "3101540255864", "이주연", "매장정품\r\n  살로몬 캡",
         "블랙", "", "", "", "", "", "결제완료", "특이사항없음", "", "0502-1736-4794", "12039"],
        ["2026-07-10 22:49:08", "롯데ON", "2026071015418571", "염수경", "잔스포츠 미니백팩",
         "ONESIZE", "1231231", "30000.00", "0.00", "", "6812345678", "해외현지배송중",
         "송장전송완료", "까대기", "01026584242", "12038"],
    ])
    rows = parse_mango_xls(html.encode("utf-8"))
    assert len(rows) == 2
    assert rows[0]["mango_uid"] == "12039"
    assert rows[0]["recipient"] == "이주연"
    assert rows[0]["mango_status"] == "결제완료"
    assert rows[0]["invoice_no"] == ""       # 빈 송장
    # 상품명 개행 정규화
    assert "살로몬 캡" in rows[0]["product_name"]
    assert "\r" not in rows[0]["product_name"]
    assert rows[1]["invoice_no"] == "6812345678"
    assert rows[1]["market_status"] == "송장전송완료"


def test_frameset_shell_raises():
    shell = ('<html xmlns:x="urn:schemas-microsoft-com:office:excel"><head>'
             '<meta name="Excel Workbook Frameset"></head>'
             '<frameset><frame src="20260711.files/sheet001.htm"></frameset></html>')
    with pytest.raises(MangoParseError):
        parse_mango_xls(shell.encode("utf-8"))


def test_no_table_raises():
    with pytest.raises(MangoParseError):
        parse_mango_xls(b"<html><body>hello</body></html>")
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest tests/delivery/test_mango_parser.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'lemouton.delivery.mango_parser'`

- [ ] **Step 3: 파서 구현**

Create `프로그램/_시스템/lemouton/delivery/mango_parser.py`:

```python
"""더망고 주문내역 .xls(HTML 위장) 파서.

더망고 엑셀은 확장자만 .xls 이고 실체는 HTML <table> 이다.
프레임셋 껍데기 파일(실제 표가 없는)은 조용히 통과시키지 않고 MangoParseError 를 던진다.
"""
import re
from bs4 import BeautifulSoup

# A~P 16열 고정. 헤더는 개행/공백이 섞이므로 위치(인덱스)로 매핑한다.
_COLS = [
    "ordered_at",       # A 마켓주문일자
    "market_name",      # B 마켓명
    "market_order_no",  # C 마켓주문번호
    "recipient",        # D 수령인명
    "product_name",     # E 마켓상품명
    "option1",          # F 옵션1
    "site_order_no",    # G 사이트주문번호
    "buy_price",        # H 구매가격
    "intl_ship",        # I 국제운송료
    "courier",          # J 국내송장번호 택배사
    "invoice_no",       # K 국내송장번호
    "mango_status",     # L 더망고주문상태
    "market_status",    # M 마켓주문상태
    "memo",             # N 간단메모
    "phone",            # O 휴대폰번호
    "mango_uid",        # P 더망고주문고유번호
]
_NCOLS = len(_COLS)


class MangoParseError(Exception):
    pass


def _norm(s: str) -> str:
    return re.sub(r"\s+", " ", (s or "").strip())


def parse_mango_xls(raw) -> list[dict]:
    """더망고 엑셀 바이트/문자열 → 주문 dict 리스트."""
    if isinstance(raw, bytes):
        text = raw.decode("utf-8", errors="replace")
    else:
        text = raw

    low = text.lower()
    # 프레임셋 껍데기 감지: 실제 데이터 <table> 없이 frameset 만 있으면 실패
    if "excel workbook frameset" in low or ("<frameset" in low and "<table" not in low):
        raise MangoParseError(
            "실제 주문 표가 없는 껍데기 파일입니다. 더망고에서 단일 파일로 내보내거나 "
            "'{이름}.files/sheet001.htm' 을 올려주세요.")

    soup = BeautifulSoup(text, "html.parser")
    rows = []
    for tr in soup.find_all("tr"):
        cells = [c.get_text(" ", strip=True) for c in tr.find_all(["td", "th"])]
        if len(cells) >= _NCOLS:
            rows.append(cells)

    if not rows:
        raise MangoParseError("표(행)를 찾지 못했습니다. 더망고 주문내역 엑셀이 맞는지 확인해주세요.")

    header = rows[0]
    if "마켓주문" not in _norm(header[0]) or "고유번호" not in _norm(header[_NCOLS - 1]):
        raise MangoParseError("헤더가 더망고 양식과 다릅니다. 첫 열=마켓주문일자, 끝 열=더망고주문고유번호 확인.")

    out = []
    for cells in rows[1:]:
        rec = {key: _norm(cells[i]) for i, key in enumerate(_COLS)}
        if not rec["mango_uid"]:
            continue  # 고유번호 없는 행(합계/빈행) 건너뜀
        out.append(rec)
    return out
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest tests/delivery/test_mango_parser.py -v`
Expected: PASS (3 passed)

- [ ] **Step 5: 실제 샘플로 스모크 검증 (선택, 파일 있으면)**

Run: `python -c "from lemouton.delivery.mango_parser import parse_mango_xls; import io; p=r'C:/Users/seung/Downloads/20260711.files/sheet001.htm'; print(len(parse_mango_xls(open(p,'rb').read())))"`
Expected: `425` (실제 샘플 데이터 행 수). 파일 없으면 이 스텝은 건너뛴다.

- [ ] **Step 6: 커밋**

```bash
git add "프로그램/_시스템/lemouton/delivery/mango_parser.py" "프로그램/_시스템/tests/delivery/test_mango_parser.py"
git commit -m "feat(delivery): 더망고 HTML위장 .xls 파서 + 껍데기 실패 처리"
```

---

## Task 3: 기본 구분자 매핑 시드

**Files:**
- Modify: `프로그램/_시스템/lemouton/delivery/service.py` (신규 생성)
- Test: `프로그램/_시스템/tests/delivery/test_delivery_service.py`

- [ ] **Step 1: 실패 테스트 추가**

`tests/delivery/test_delivery_service.py` 하단에 추가:

```python
from lemouton.delivery import service as svc


def test_seed_default_status_map(db):
    svc.seed_default_status_map(db)
    rows = {r.status_value: r for r in db.query(M.MangoStatusMap).all()}
    assert rows["해외현지배송중"].default_method == "까대기"
    assert rows["국내배송중"].is_flow_check_target is True
    assert rows["배송완료"].is_flow_check_target is True
    assert rows["결제완료"].is_flow_check_target is False
    # 재호출해도 중복 생성 안 함 (idempotent)
    svc.seed_default_status_map(db)
    assert db.query(M.MangoStatusMap).filter_by(status_value="해외현지배송중").count() == 1
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest tests/delivery/test_delivery_service.py::test_seed_default_status_map -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'lemouton.delivery.service'`

- [ ] **Step 3: 서비스 파일 생성 + 시드 함수**

Create `프로그램/_시스템/lemouton/delivery/service.py`:

```python
"""배송검사 도메인 서비스 — 매핑 시드, upsert, 배송방식 판정, 검사 2종."""
from datetime import datetime, timezone

from lemouton.delivery.models import MangoOrder, MangoStatusMap

# (status_value, meaning, default_method, is_flow_check_target, sort_order)
_DEFAULT_MAP = [
    ("결제완료",            "배송전",     "미지정", False, 10),
    ("배송대기중",          "배송전",     "미지정", False, 20),
    ("해외현지배송중",      "해외배송중", "까대기", False, 30),
    ("국내배송중",          "국내배송중", "미지정", True,  40),
    ("배송완료",            "배송완료",   "미지정", True,  50),
    ("반품/교환/취소진행중", "취소반품교환", "미지정", False, 60),
    ("반품/교환/취소완료",   "취소반품교환", "미지정", False, 70),
]


def seed_default_status_map(session):
    """기본 매핑을 없는 것만 삽입 (idempotent)."""
    existing = {r.status_value for r in session.query(MangoStatusMap.status_value).all()}
    for value, meaning, method, flow, order in _DEFAULT_MAP:
        if value in existing:
            continue
        session.add(MangoStatusMap(status_value=value, meaning=meaning,
                                   default_method=method, is_flow_check_target=flow,
                                   sort_order=order))
    session.commit()


def get_status_map(session) -> dict:
    """status_value -> MangoStatusMap."""
    return {r.status_value: r for r in session.query(MangoStatusMap).all()}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest tests/delivery/test_delivery_service.py::test_seed_default_status_map -v`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add "프로그램/_시스템/lemouton/delivery/service.py" "프로그램/_시스템/tests/delivery/test_delivery_service.py"
git commit -m "feat(delivery): 기본 구분자 매핑 시드 seed_default_status_map"
```

---

## Task 4: upsert + 배송방식 자동판정 (수기 보존)

**Files:**
- Modify: `프로그램/_시스템/lemouton/delivery/service.py`
- Test: `프로그램/_시스템/tests/delivery/test_delivery_service.py`

**규칙:** upsert 키 = `mango_uid`. 배송방식 우선순위 = 수기 > 일괄 > 자동(L매핑). 재업로드 시 `delivery_method_source == '수기'` 행의 배송방식은 덮지 않는다. `first_uploaded_at` 은 보존. 송장번호가 이전과 다른 새 값이면 `invoice_history` 에 누적하고 2개 이상 서로 다른 송장이면 `is_duplicate_invoice=True`.

- [ ] **Step 1: 실패 테스트 추가**

`tests/delivery/test_delivery_service.py` 하단에 추가:

```python
def _row(uid, **kw):
    base = dict(mango_uid=uid, market_name="롯데ON", market_order_no="C"+uid,
                ordered_at="2026-07-11", recipient="염수경", product_name="백팩",
                option1="ONESIZE", phone="010", invoice_no="", courier="",
                mango_status="해외현지배송중", market_status="특이사항없음", memo="")
    base.update(kw)
    return base


def test_upsert_auto_method_from_map(db):
    svc.seed_default_status_map(db)
    res = svc.upsert_orders(db, [_row("100")])
    assert res["inserted"] == 1
    o = db.query(M.MangoOrder).filter_by(mango_uid="100").one()
    # 해외현지배송중 -> 까대기 (자동)
    assert o.delivery_method == "까대기"
    assert o.delivery_method_source == "자동"


def test_upsert_preserves_manual(db):
    svc.seed_default_status_map(db)
    svc.upsert_orders(db, [_row("101")])
    o = db.query(M.MangoOrder).filter_by(mango_uid="101").one()
    o.delivery_method = "직배"
    o.delivery_method_source = "수기"
    first = o.first_uploaded_at
    db.commit()
    # 재업로드 — 수기값 유지, first_uploaded_at 보존
    svc.upsert_orders(db, [_row("101", mango_status="국내배송중")])
    o2 = db.query(M.MangoOrder).filter_by(mango_uid="101").one()
    assert o2.delivery_method == "직배"
    assert o2.delivery_method_source == "수기"
    assert o2.first_uploaded_at == first
    assert o2.mango_status == "국내배송중"  # 상태는 갱신됨


def test_upsert_invoice_history_and_duplicate(db):
    svc.seed_default_status_map(db)
    svc.upsert_orders(db, [_row("102", invoice_no="AAA")])
    svc.upsert_orders(db, [_row("102", invoice_no="BBB")])  # 다른 송장 재등장
    o = db.query(M.MangoOrder).filter_by(mango_uid="102").one()
    assert o.invoice_no == "BBB"
    assert len(o.invoice_history) == 2
    assert o.is_duplicate_invoice is True
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest tests/delivery/test_delivery_service.py -k upsert -v`
Expected: FAIL — `AttributeError: module 'lemouton.delivery.service' has no attribute 'upsert_orders'`

- [ ] **Step 3: upsert 구현**

`프로그램/_시스템/lemouton/delivery/service.py` 에 추가:

```python
def _now():
    return datetime.now(timezone.utc)


def _auto_method(status_value, status_map) -> str:
    m = status_map.get(status_value)
    return m.default_method if m else "미지정"


def upsert_orders(session, rows, bulk_method=None) -> dict:
    """파싱된 rows(dict 리스트)를 mango_uid 기준 upsert.

    bulk_method: '까대기'/'직배' 이면 이번 업로드 전체를 그 방식으로(source='일괄'),
                 None 또는 '자동판정' 이면 L매핑 자동(source='자동').
    수기(source=='수기') 행의 배송방식은 절대 덮지 않는다.
    """
    status_map = get_status_map(session)
    inserted = updated = 0
    for r in rows:
        uid = r["mango_uid"]
        o = session.query(MangoOrder).filter_by(mango_uid=uid).one_or_none()
        invoice = r.get("invoice_no") or ""
        if o is None:
            o = MangoOrder(mango_uid=uid, first_uploaded_at=_now(), invoice_history=[])
            session.add(o)
            inserted += 1
        else:
            updated += 1

        # 공통 필드 갱신
        o.market_name = r.get("market_name")
        o.market_order_no = r.get("market_order_no")
        o.ordered_at = r.get("ordered_at")
        o.recipient = r.get("recipient")
        o.product_name = r.get("product_name")
        o.option1 = r.get("option1")
        o.phone = r.get("phone")
        o.courier = r.get("courier")
        o.mango_status = r.get("mango_status")
        o.market_status = r.get("market_status")
        o.memo = r.get("memo")
        o.raw = r
        o.last_uploaded_at = _now()

        # 송장 이력 누적 (직전 값과 다른 새 송장이면 추가)
        hist = list(o.invoice_history or [])
        if invoice and (not hist or hist[-1].get("invoice") != invoice):
            hist.append({"invoice": invoice, "at": _now().isoformat()})
        o.invoice_history = hist
        o.invoice_no = invoice or o.invoice_no
        distinct = {h["invoice"] for h in hist if h.get("invoice")}
        o.is_duplicate_invoice = len(distinct) >= 2

        # 배송방식 판정 (수기는 보존)
        if o.delivery_method_source != "수기":
            if bulk_method in ("까대기", "직배"):
                o.delivery_method = bulk_method
                o.delivery_method_source = "일괄"
            else:
                o.delivery_method = _auto_method(o.mango_status, status_map)
                o.delivery_method_source = "자동"

    session.commit()
    return {"inserted": inserted, "updated": updated}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest tests/delivery/test_delivery_service.py -k upsert -v`
Expected: PASS (3 passed)

- [ ] **Step 5: 커밋**

```bash
git add "프로그램/_시스템/lemouton/delivery/service.py" "프로그램/_시스템/tests/delivery/test_delivery_service.py"
git commit -m "feat(delivery): upsert + 배송방식 자동판정(수기 보존)·송장이력·중복플래그"
```

---

## Task 5: 검사 2종 (중복송장 · 배송흐름 미발생) + 일괄 재지정

**Files:**
- Modify: `프로그램/_시스템/lemouton/delivery/service.py`
- Test: `프로그램/_시스템/tests/delivery/test_delivery_service.py`

- [ ] **Step 1: 실패 테스트 추가**

`tests/delivery/test_delivery_service.py` 하단에 추가:

```python
def test_find_duplicate_invoices(db):
    svc.seed_default_status_map(db)
    svc.upsert_orders(db, [_row("200", invoice_no="AAA")])
    svc.upsert_orders(db, [_row("200", invoice_no="BBB")])  # 중복
    svc.upsert_orders(db, [_row("201", invoice_no="CCC")])  # 단일 → 정상
    dups = svc.find_duplicate_invoices(db)
    uids = {o.mango_uid for o in dups}
    assert uids == {"200"}


def test_find_flow_missing(db):
    svc.seed_default_status_map(db)
    # 배송완료(검사대상) + 송장전송실패 → 배송흐름 없음
    svc.upsert_orders(db, [_row("300", mango_status="배송완료",
                                 market_status="송장전송실패", invoice_no="X")])
    # 배송완료 + 송장전송완료 + 송장있음 → 정상
    svc.upsert_orders(db, [_row("301", mango_status="배송완료",
                                 market_status="송장전송완료", invoice_no="Y")])
    # 결제완료(검사대상 아님) → 제외
    svc.upsert_orders(db, [_row("302", mango_status="결제완료", market_status="특이사항없음")])
    missing = svc.find_flow_missing(db)
    uids = {o.mango_uid for o in missing}
    assert uids == {"300"}


def test_apply_bulk_method_skips_manual(db):
    svc.seed_default_status_map(db)
    svc.upsert_orders(db, [_row("400"), _row("401")])
    o = db.query(M.MangoOrder).filter_by(mango_uid="401").one()
    o.delivery_method = "직배"; o.delivery_method_source = "수기"; db.commit()
    n = svc.apply_bulk_method(db, "까대기")
    assert n == 1  # 400만 바뀜, 401(수기)은 보존
    assert db.query(M.MangoOrder).filter_by(mango_uid="400").one().delivery_method == "까대기"
    assert db.query(M.MangoOrder).filter_by(mango_uid="401").one().delivery_method == "직배"
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest tests/delivery/test_delivery_service.py -k "duplicate or flow or bulk" -v`
Expected: FAIL — `AttributeError: ... has no attribute 'find_duplicate_invoices'`

- [ ] **Step 3: 검사·일괄 구현**

`프로그램/_시스템/lemouton/delivery/service.py` 에 추가:

```python
def find_duplicate_invoices(session):
    """같은 주문(mango_uid)에 서로 다른 송장이 2회 이상 = 중복송장."""
    return session.query(MangoOrder).filter(MangoOrder.is_duplicate_invoice.is_(True)).all()


def find_flow_missing(session):
    """구분자가 배송흐름 검사대상인데 배송흐름이 안 뜨는 주문.

    신호: 마켓상태 '송장전송실패' 또는 송장번호 없음.
    """
    status_map = get_status_map(session)
    targets = {v for v, m in status_map.items() if m.is_flow_check_target}
    out = []
    for o in session.query(MangoOrder).filter(MangoOrder.mango_status.in_(targets)).all():
        if (o.market_status == "송장전송실패") or not (o.invoice_no or ""):
            out.append(o)
    return out


def apply_bulk_method(session, method) -> int:
    """수기가 아닌 모든 주문의 배송방식을 method 로 일괄 변경. 변경 건수 반환."""
    assert method in ("까대기", "직배", "미지정")
    n = 0
    for o in session.query(MangoOrder).filter(MangoOrder.delivery_method_source != "수기").all():
        o.delivery_method = method
        o.delivery_method_source = "일괄"
        n += 1
    session.commit()
    return n


def set_method_manual(session, mango_uid, method) -> bool:
    """행별 수기 배송방식 지정 (최우선·재업로드로도 보존)."""
    assert method in ("까대기", "직배", "미지정")
    o = session.query(MangoOrder).filter_by(mango_uid=mango_uid).one_or_none()
    if not o:
        return False
    o.delivery_method = method
    o.delivery_method_source = "수기"
    session.commit()
    return True
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest tests/delivery/test_delivery_service.py -v`
Expected: PASS (전체 통과)

- [ ] **Step 5: 커밋**

```bash
git add "프로그램/_시스템/lemouton/delivery/service.py" "프로그램/_시스템/tests/delivery/test_delivery_service.py"
git commit -m "feat(delivery): 검사2종(중복송장·배송흐름)·일괄/수기 배송방식 지정"
```

---

## Task 6: 라우트 — inspect 서브탭 + 엔드포인트

**Files:**
- Modify: `프로그램/_시스템/webapp/routes/orders.py`

**참고:** `orders` 블루프린트(url_prefix `/orders`)에 라우트를 추가한다(새 블루프린트 불필요). DB 세션은 `from shared.db import SessionLocal` 로 요청마다 열고 닫는다(기존 라우트 관례 확인 후 동일 방식 사용).

- [ ] **Step 1: SUBTABS 에 inspect 추가 + 세션 헬퍼 import**

Modify `프로그램/_시스템/webapp/routes/orders.py` — 상단 import 에 추가:

```python
from flask import request, jsonify, render_template, Blueprint
from shared.db import SessionLocal
from lemouton.delivery import service as delivery_svc
from lemouton.delivery.mango_parser import parse_mango_xls, MangoParseError
```

`SUBTABS` 리스트에 항목 추가 (list 앞쪽, margin 앞에):

```python
    {'key': 'inspect', 'label': '🚚 배송검사', 'desc': '더망고 업로드 · 중복송장 · 배송흐름 · 배송방식'},
```

- [ ] **Step 2: 데이터 조회 API (검사결과 + 목록)**

`orders.py` 에 라우트 추가:

```python
def _order_to_dict(o):
    return {
        'uid': o.mango_uid, 'market': o.market_name, 'recipient': o.recipient,
        'product': o.product_name, 'option': o.option1, 'invoice': o.invoice_no or '',
        'mango_status': o.mango_status, 'market_status': o.market_status,
        'method': o.delivery_method, 'method_source': o.delivery_method_source,
        'dup': bool(o.is_duplicate_invoice),
    }


@bp.route('/inspect/data')
def inspect_data():
    s = SessionLocal()
    try:
        delivery_svc.seed_default_status_map(s)  # 최초 진입 시 기본 매핑 보장
        orders = s.query(delivery_svc.MangoOrder).order_by(
            delivery_svc.MangoOrder.last_uploaded_at.desc()).limit(1000).all()
        dup_uids = {o.mango_uid for o in delivery_svc.find_duplicate_invoices(s)}
        flow_uids = {o.mango_uid for o in delivery_svc.find_flow_missing(s)}
        rows = []
        for o in orders:
            d = _order_to_dict(o)
            d['flow_missing'] = o.mango_uid in flow_uids
            rows.append(d)
        status_map = [
            {'value': m.status_value, 'meaning': m.meaning,
             'default_method': m.default_method, 'flow': bool(m.is_flow_check_target)}
            for m in sorted(delivery_svc.get_status_map(s).values(), key=lambda x: x.sort_order)
        ]
        return jsonify({'orders': rows, 'status_map': status_map,
                        'summary': {'dup': len(dup_uids), 'flow': len(flow_uids),
                                    'total': len(orders)}})
    finally:
        s.close()
```

> 주: `delivery_svc.MangoOrder` 참조를 위해 `service.py` 는 이미 `from lemouton.delivery.models import MangoOrder` 를 import 하므로 `delivery_svc.MangoOrder` 로 접근 가능하다.

- [ ] **Step 3: 업로드 엔드포인트**

`orders.py` 에 추가:

```python
@bp.route('/inspect/upload', methods=['POST'])
def inspect_upload():
    f = request.files.get('file')
    if not f:
        return jsonify({'ok': False, 'error': '파일이 없습니다.'}), 400
    bulk = request.form.get('bulk_method') or None  # 까대기/직배/자동판정
    if bulk == '자동판정':
        bulk = None
    try:
        rows = parse_mango_xls(f.read())
    except MangoParseError as e:
        return jsonify({'ok': False, 'error': str(e)}), 422
    s = SessionLocal()
    try:
        delivery_svc.seed_default_status_map(s)
        res = delivery_svc.upsert_orders(s, rows, bulk_method=bulk)
        return jsonify({'ok': True, 'inserted': res['inserted'],
                        'updated': res['updated'], 'parsed': len(rows)})
    finally:
        s.close()
```

- [ ] **Step 4: 배송방식(수기)·일괄·매핑 저장 엔드포인트**

`orders.py` 에 추가:

```python
@bp.route('/inspect/method', methods=['POST'])
def inspect_method():
    uid = (request.json or {}).get('uid')
    method = (request.json or {}).get('method')
    if method not in ('까대기', '직배', '미지정'):
        return jsonify({'ok': False, 'error': '잘못된 배송방식'}), 400
    s = SessionLocal()
    try:
        ok = delivery_svc.set_method_manual(s, uid, method)
        return jsonify({'ok': ok})
    finally:
        s.close()


@bp.route('/inspect/bulk-method', methods=['POST'])
def inspect_bulk_method():
    method = (request.json or {}).get('method')
    if method not in ('까대기', '직배', '미지정'):
        return jsonify({'ok': False, 'error': '잘못된 배송방식'}), 400
    s = SessionLocal()
    try:
        n = delivery_svc.apply_bulk_method(s, method)
        return jsonify({'ok': True, 'changed': n})
    finally:
        s.close()


@bp.route('/inspect/mapping', methods=['POST'])
def inspect_mapping():
    """구분자 매핑 저장. body: {items:[{value, meaning, default_method, flow}]}"""
    items = (request.json or {}).get('items') or []
    s = SessionLocal()
    try:
        by_value = delivery_svc.get_status_map(s)
        for it in items:
            m = by_value.get(it.get('value'))
            if not m:
                continue
            m.meaning = it.get('meaning', m.meaning)
            m.default_method = it.get('default_method', m.default_method)
            m.is_flow_check_target = bool(it.get('flow', m.is_flow_check_target))
        s.commit()
        return jsonify({'ok': True})
    finally:
        s.close()
```

- [ ] **Step 5: 라우트 스모크 테스트 (수동)**

Run: `python -c "import app; c=app.app.test_client(); r=c.get('/orders/inspect/data'); print(r.status_code, r.get_json()['summary'])"`
Expected: `200 {'dup': 0, 'flow': 0, 'total': 0}` (빈 DB). 오류 없이 JSON 반환되면 성공. (`app.app` 이 아니라 팩토리면 `app.create_app()` 로 조정.)

- [ ] **Step 6: 커밋**

```bash
git add "프로그램/_시스템/webapp/routes/orders.py"
git commit -m "feat(delivery): 배송검사 라우트(inspect 탭·업로드·검사데이터·배송방식·매핑)"
```

---

## Task 7: 템플릿 UI — 배송검사 탭 (확정 시안)

**Files:**
- Modify: `프로그램/_시스템/webapp/templates/orders/index.html`

**확정 시안 반영:** 업로드=카드 드롭존 + 라디오 3택 일괄지정 · 검사결과=요약카드+필터+통합표 · 배송방식=토글칩 · 구분자 매핑=설정 모달 · 정상=초록.

- [ ] **Step 1: inspect 블록 추가**

Modify `프로그램/_시스템/webapp/templates/orders/index.html` — 기존 `{% if tab == 'list' %}` … 분기 안에 `{% elif tab == 'inspect' %}` 블록 추가 (placeholder `<ul>` 대신 실제 UI). 아래를 그대로 삽입:

```html
{% elif tab == 'inspect' %}
<div class="inspect-wrap">
  <!-- 업로드 카드 (2-A 카드형 드롭존 + 4-A 라디오 3택) -->
  <div class="card" style="margin-bottom:16px">
    <div class="card__title">더망고 주문내역 업로드</div>
    <label id="dropzone" style="display:block;border:2px dashed var(--color-border,#E5E8EB);border-radius:12px;padding:24px;text-align:center;cursor:pointer">
      <input type="file" id="mangoFile" accept=".xls,.htm,.html" style="display:none">
      <div style="font-size:32px">📥</div>
      <div style="font-weight:600;margin-top:6px">더망고 엑셀을 선택하거나 여기로 끌어놓기</div>
      <div id="fileName" style="font-size:12px;color:#8B95A1;margin-top:4px"></div>
    </label>
    <div style="margin-top:14px">
      <div style="font-size:12px;font-weight:600;color:#4E5968;margin-bottom:6px">이번 업로드 전체 배송방식</div>
      <div style="display:flex;gap:8px">
        <label class="rc on"><input type="radio" name="bulk" value="자동판정" checked hidden><b>자동판정</b><span>구분자 매핑대로</span></label>
        <label class="rc"><input type="radio" name="bulk" value="까대기" hidden><b>전부 까대기</b><span>📦 사무실 경유</span></label>
        <label class="rc"><input type="radio" name="bulk" value="직배" hidden><b>전부 직배</b><span>🚚 소싱처 직접</span></label>
      </div>
    </div>
    <div style="margin-top:14px;text-align:right">
      <button class="btn btn--secondary" id="btnMapping" type="button">⚙ 구분자 매핑</button>
      <button class="btn btn--primary" id="btnUpload" type="button">업로드</button>
    </div>
    <div id="uploadMsg" style="margin-top:10px;font-size:13px"></div>
  </div>

  <!-- 요약 카드 (5-A) -->
  <div id="summary" style="display:flex;gap:12px;margin-bottom:14px"></div>

  <!-- 통합표 -->
  <div class="card">
    <div class="table-wrap">
      <table class="table" id="inspectTable">
        <thead><tr>
          <th>마켓</th><th>수령인</th><th>상품</th><th style="text-align:center">송장번호</th>
          <th style="text-align:center">배송방식</th><th style="text-align:center">검사</th><th>근거</th>
        </tr></thead>
        <tbody id="inspectBody"></tbody>
      </table>
    </div>
  </div>
</div>

<!-- 구분자 매핑 모달 (3-B) -->
<div class="modal-backdrop" id="mappingModal" style="display:none">
  <div class="modal">
    <div class="modal__header"><span class="modal__title">⚙ 구분자 매핑 설정</span>
      <button class="modal__close" type="button" onclick="document.getElementById('mappingModal').style.display='none'">×</button></div>
    <div class="modal__body"><div class="table-wrap"><table class="table" id="mapTable">
      <thead><tr><th>더망고주문상태(원본)</th><th style="text-align:center">의미</th><th style="text-align:center">기본 배송방식</th><th style="text-align:center">배송흐름 검사</th></tr></thead>
      <tbody id="mapBody"></tbody></table></div></div>
    <div class="modal__footer">
      <button class="btn btn--secondary" type="button" onclick="document.getElementById('mappingModal').style.display='none'">취소</button>
      <button class="btn btn--primary" type="button" id="btnSaveMapping">저장</button>
    </div>
  </div>
</div>

<style>
.rc{flex:1;border:1px solid var(--color-border,#E5E8EB);border-radius:8px;padding:10px 12px;cursor:pointer;display:flex;flex-direction:column}
.rc.on{border-color:var(--color-primary,#3182F6);background:var(--color-primary-light,#E8F2FF)}
.rc b{font-size:13px}.rc span{font-size:11px;color:#8B95A1}
.chip-group{display:inline-flex;border:1px solid #E5E8EB;border-radius:9999px;overflow:hidden}
.chip-group button{border:none;background:#fff;padding:4px 11px;font-size:11px;font-weight:600;cursor:pointer;color:#8B95A1}
.chip-group button.on-k{background:#FEF3E2;color:#B4690E}
.chip-group button.on-j{background:#E8F2FF;color:#1B64DA}
.b-dup{background:rgba(239,68,68,.1);color:#EF4444}
.b-flow{background:rgba(245,158,11,.14);color:#B4690E}
.b-ok{background:rgba(34,197,94,.12);color:#16A34A}
.stat-c{background:#fff;border:1px solid #E5E8EB;border-radius:12px;padding:14px 18px;min-width:130px;cursor:pointer}
.stat-c .n{font-size:22px;font-weight:700;font-variant-numeric:tabular-nums}
.stat-c .l{font-size:11.5px;color:#8B95A1}
</style>
```

- [ ] **Step 2: JS 로직 추가 (같은 파일 `{% block scripts %}` 안, 또는 inspect 블록 하단 `<script>`)**

`inspect` 블록 하단에 `<script>` 추가:

```html
<script>
if (document.getElementById('inspectTable')) {
  const $ = s => document.querySelector(s);
  const methodBadge = (m) => {
    const on = x => m===x ? (x==='까대기'?'on-k':'on-j') : '';
    return `<span class="chip-group" data-uid="__UID__">
      <button class="${on('까대기')}" data-m="까대기">까대기</button>
      <button class="${on('직배')}" data-m="직배">직배</button></span>`;
  };
  const flagBadge = (r) => r.dup ? '<span class="badge b-dup">중복송장</span>'
    : r.flow_missing ? '<span class="badge b-flow">배송흐름 없음</span>'
    : '<span class="badge b-ok">정상</span>';
  let FILTER = null; // 'dup' | 'flow' | null
  let DATA = {orders:[], status_map:[], summary:{}};

  async function load() {
    const r = await fetch('/orders/inspect/data'); DATA = await r.json();
    renderSummary(); renderTable();
  }
  function renderSummary() {
    const s = DATA.summary;
    $('#summary').innerHTML = `
      <div class="stat-c" data-f="dup" style="border-color:#EF4444"><div class="n" style="color:#EF4444">${s.dup}</div><div class="l">중복송장</div></div>
      <div class="stat-c" data-f="flow" style="border-color:#F59E0B"><div class="n" style="color:#F59E0B">${s.flow}</div><div class="l">배송흐름 없음</div></div>
      <div class="stat-c" data-f="" style="border-color:#22C55E"><div class="n" style="color:#22C55E">${s.total - s.dup - s.flow}</div><div class="l">정상</div></div>`;
    document.querySelectorAll('.stat-c').forEach(c => c.onclick = () => { FILTER = c.dataset.f || null; renderTable(); });
  }
  function renderTable() {
    let rows = DATA.orders;
    if (FILTER === 'dup') rows = rows.filter(r => r.dup);
    else if (FILTER === 'flow') rows = rows.filter(r => r.flow_missing);
    $('#inspectBody').innerHTML = rows.map(r => {
      const why = r.dup ? '같은 주문에 송장 2회 이상'
        : r.flow_missing ? (r.market_status==='송장전송실패' ? '마켓상태=송장전송실패' : '배송완료 단계인데 배송흐름 없음') : '';
      return `<tr>
        <td>${r.market||''}</td><td>${r.recipient||''}</td><td style="font-size:12px">${r.product||''}</td>
        <td style="text-align:center;font-size:12px">${r.invoice||'—'}</td>
        <td style="text-align:center">${methodBadge(r.method).replace('__UID__', r.uid)}</td>
        <td style="text-align:center">${flagBadge(r)}</td>
        <td style="font-size:12px;color:#8B95A1">${why}</td></tr>`;
    }).join('') || '<tr><td colspan="7" style="text-align:center;color:#8B95A1;padding:24px">데이터가 없습니다 — 더망고 엑셀을 업로드하세요</td></tr>';
    // 배송방식 토글
    document.querySelectorAll('#inspectBody .chip-group button').forEach(b => b.onclick = async () => {
      const uid = b.parentElement.dataset.uid, method = b.dataset.m;
      await fetch('/orders/inspect/method', {method:'POST', headers:{'Content-Type':'application/json'},
        body: JSON.stringify({uid, method})});
      load();
    });
  }
  // 업로드
  const fileEl = $('#mangoFile');
  $('#dropzone').onclick = () => fileEl.click();
  fileEl.onchange = () => $('#fileName').textContent = fileEl.files[0] ? fileEl.files[0].name : '';
  document.querySelectorAll('input[name=bulk]').forEach(r => r.onchange = () => {
    document.querySelectorAll('.rc').forEach(l => l.classList.toggle('on', l.querySelector('input').checked));
  });
  $('#btnUpload').onclick = async () => {
    if (!fileEl.files[0]) { $('#uploadMsg').innerHTML = '<span style="color:#EF4444">파일을 선택하세요</span>'; return; }
    const fd = new FormData();
    fd.append('file', fileEl.files[0]);
    fd.append('bulk_method', document.querySelector('input[name=bulk]:checked').value);
    $('#uploadMsg').textContent = '업로드 중…';
    const r = await fetch('/orders/inspect/upload', {method:'POST', body: fd});
    const j = await r.json();
    if (j.ok) { $('#uploadMsg').innerHTML = `<span style="color:#16A34A">✔ ${j.parsed}건 처리 (신규 ${j.inserted} · 갱신 ${j.updated})</span>`; load(); }
    else $('#uploadMsg').innerHTML = `<span style="color:#EF4444">✖ ${j.error}</span>`;
  };
  // 매핑 모달
  $('#btnMapping').onclick = () => {
    $('#mapBody').innerHTML = DATA.status_map.map(m => `
      <tr data-value="${m.value}">
        <td style="font-weight:600">${m.value}</td>
        <td style="text-align:center"><select class="map-mean form-control" style="width:auto">
          ${['배송전','해외배송중','국내배송중','배송완료','취소반품교환','기타'].map(o=>`<option ${o===m.meaning?'selected':''}>${o}</option>`).join('')}</select></td>
        <td style="text-align:center"><select class="map-method form-control" style="width:auto">
          ${['미지정','까대기','직배'].map(o=>`<option ${o===m.default_method?'selected':''}>${o}</option>`).join('')}</select></td>
        <td style="text-align:center"><input type="checkbox" class="map-flow" ${m.flow?'checked':''}></td></tr>`).join('');
    $('#mappingModal').style.display = 'flex';
  };
  $('#btnSaveMapping').onclick = async () => {
    const items = [...document.querySelectorAll('#mapBody tr')].map(tr => ({
      value: tr.dataset.value,
      meaning: tr.querySelector('.map-mean').value,
      default_method: tr.querySelector('.map-method').value,
      flow: tr.querySelector('.map-flow').checked,
    }));
    await fetch('/orders/inspect/mapping', {method:'POST', headers:{'Content-Type':'application/json'},
      body: JSON.stringify({items})});
    $('#mappingModal').style.display = 'none'; load();
  };
  load();
}
</script>
```

> `modal-backdrop` 표시는 프로젝트 CSS 관례에 맞춘다. 기존 모달이 `display:flex`/`.show` 클래스 중 무엇으로 열리는지 base.html·toss.css 확인 후 `style.display='flex'` 부분을 관례에 맞게 조정한다.

- [ ] **Step 3: 렌더 확인 (수동)**

앱을 실행하고 `/orders?tab=inspect` 접속 → 업로드 카드·요약카드(정상=초록)·빈 표("데이터가 없습니다")가 뜨는지 확인.

- [ ] **Step 4: 커밋**

```bash
git add "프로그램/_시스템/webapp/templates/orders/index.html"
git commit -m "feat(delivery): 배송검사 탭 UI(카드 드롭존·라디오3택·요약카드+통합표·토글칩·매핑모달)"
```

---

## Task 8: 실브라우저 전수 검증 + 실데이터 업로드

**Files:** (검증만, 코드 수정은 발견 시)

- [ ] **Step 1: 앱 기동 + 탭 접속**

앱을 로컬 기동하고 `/orders?tab=inspect` 접속. 사이드바/서브탭에 「🚚 배송검사」가 뜨는지 확인.

- [ ] **Step 2: 실제 더망고 파일 업로드**

`C:/Users/seung/Downloads/20260711.files/sheet001.htm` 를 업로드 → "425건 처리" 메시지 확인. (껍데기 `20260711.xls` 를 올리면 "껍데기 파일입니다" 오류가 떠야 정상.)

- [ ] **Step 3: 검사결과 육안 대조**

- 중복송장 건수 · 배송흐름 없음(송장전송실패 M열) 건수가 요약카드에 뜨는지.
- 배송방식 토글칩 클릭 → 즉시 저장되고 새로고침해도 유지되는지(수기 우선).
- 구분자 매핑 모달에서 값 변경 → 저장 → 배송방식 자동판정 반영되는지.

- [ ] **Step 4: 전체 테스트 재실행**

Run: `python -m pytest tests/delivery/ -v`
Expected: 전부 PASS.

- [ ] **Step 5: 최종 커밋 (검증 중 수정분 있으면)**

```bash
git add -A "프로그램/_시스템"
git commit -m "fix(delivery): 배송검사 실브라우저 검증 반영"
```

---

## Self-Review 결과 (작성자 체크)

- **스펙 커버리지**: 업로드(Task2·6·7)·DB누적/upsert(Task4)·구분자 설정형 매핑(Task3·6·7)·배송방식 우선순위 수기>일괄>자동(Task4·5)·중복송장 K유무/uid이력(Task4·5)·배송흐름 M열(Task5)·신규 inspect 탭(Task6·7)·정합성 껍데기 실패(Task2) 모두 태스크 존재. ✔
- **플레이스홀더**: 없음(모든 코드 스텝에 실제 코드). "TBD"·"적절히 처리" 없음. ✔
- **타입 일관성**: `MangoOrder`/`MangoStatusMap` 필드명, `upsert_orders`·`find_duplicate_invoices`·`find_flow_missing`·`apply_bulk_method`·`set_method_manual`·`seed_default_status_map`·`get_status_map` 시그니처가 라우트(Task6)에서 쓰는 이름과 일치. ✔
- **미결(구현 중 확정)**: (1) 요청별 DB 세션 여는 방식은 기존 orders/타 라우트 관례 확인 후 `SessionLocal` 사용 — 관례가 `g` 스코프면 맞춘다. (2) 모달 오픈 CSS 클래스(`display:flex` vs `.show`)는 toss.css 확인 후 조정. (3) `app` 진입점(`app.app` vs `create_app()`)은 실제 파일 확인 후 스모크 명령 조정. 셋 다 코드로 확인 가능, 설계 미결 아님.
