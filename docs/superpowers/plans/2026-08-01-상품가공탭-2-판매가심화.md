# 상품 가공 ②「판매가」 항목 심화 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 가격 템플릿이 가진 것을 정책의 「판매가」 항목이 **빠짐없이** 담게 한다
(사장님 확정 — 「우리 정책이 가격 템플릿보다 큰 범위여야 한다」).

**Architecture:** 규칙 스키마(`process_rule_schema.py`)의 `price` 항목을 4칸 → 15칸으로 넓힌다.
소싱품·사입품을 가르고, 방식을 3가지(마진율·마진금액·지정가)로 늘리고,
가격 안전장치 6칸을 같은 항목 안에 둔다. **저장된 옛 값은 읽기 층에서 새 칸으로 번역**한다 —
DB 를 건드리지 않고 옛 정책이 계속 동작하게 한다.

**Tech Stack:** Python 3 · SQLAlchemy · pytest · Jinja2

**설계서:** `docs/superpowers/specs/2026-07-31-상품가공탭-1단계-design.md` §3-5·3-6·4
**시안:** 바탕화면 `모음전 시안 - 판매가 항목 심화 v1.html` (확정 I2 · J3 · K3)

---

## File Structure

| 파일 | 책임 |
|---|---|
| `lemouton/registration/process_rule_schema.py` (수정) | `price` 항목 15칸 — 화면·저장·문서의 단일 원천 |
| `lemouton/policy/price_cfg.py` (신규) | **옛 칸 ↔ 새 칸 번역**만 담당. 읽는 쪽은 전부 여기를 거친다 |
| `lemouton/policy/fields.py` (수정) | `_size_unify` 를 판매가 안으로 흡수(6마켓 공통) |
| `lemouton/policy/preview.py` (수정) | 미리보기가 새 칸을 읽게 |
| `lemouton/registration/process_apply.py` (수정) | 대량등록 점검이 새 칸을 읽게 |
| `webapp/templates/policy/detail.html` (수정) | 판매가만 전용 화면(소싱·사입 나란히 + 안전장치) |
| `tests/policy/test_price_fields.py` (신규) | 커버리지·번역·기본값 |

**왜 `price_cfg.py` 를 따로 두나** — 옛 칸을 읽는 곳이 3군데(미리보기·대량등록 점검·화면)다.
각자 번역하면 세 벌이 갈린다. 번역은 한 곳에만 둔다.

---

## Task 1: 옛 칸 ↔ 새 칸 번역기

**Files:**
- Create: `프로그램/_시스템/lemouton/policy/price_cfg.py`
- Test: `프로그램/_시스템/tests/policy/test_price_fields.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/policy/test_price_fields.py`:

```python
# -*- coding: utf-8 -*-
"""「판매가」 항목 — 옛 칸 번역 · 커버리지.

🔴 이미 저장된 정책이 있다. 칸 이름을 바꾸면서 옛 값을 못 읽으면
   사장님이 채워 둔 값이 조용히 「안 정함」이 된다.
"""
import pytest

from lemouton.policy.price_cfg import read_side, PriceSide


def test_새_칸을_그대로_읽는다():
    got = read_side({'sourcing_mode': 'margin_rate', 'sourcing_rate': 25}, 'sourcing')
    assert got == PriceSide(mode='margin_rate', rate=25.0, amount=None, fixed=None)


def test_사입품_칸을_읽는다():
    got = read_side({'purchase_mode': 'fixed_price', 'purchase_fixed': 115900}, 'purchase')
    assert got.mode == 'fixed_price'
    assert got.fixed == 115900


def test_옛_칸_마진율을_양쪽_모두에_준다():
    """옛 정책은 소싱·사입 구분이 없었다 — 양쪽 다 그 값으로 본다."""
    old = {'mode': 'margin_rate', 'margin_rate': 25}
    assert read_side(old, 'sourcing').rate == 25.0
    assert read_side(old, 'purchase').rate == 25.0


def test_옛_칸_고정금액은_지정가로_번역된다():
    old = {'mode': 'fixed_amount', 'fixed_amount': 128900}
    got = read_side(old, 'sourcing')
    assert got.mode == 'fixed_price'
    assert got.fixed == 128900


def test_새_칸이_있으면_옛_칸을_무시한다():
    """둘 다 있으면 새 것이 이긴다 — 안 그러면 고친 값이 안 먹는다."""
    mixed = {'mode': 'margin_rate', 'margin_rate': 25,
             'sourcing_mode': 'margin_rate', 'sourcing_rate': 32}
    assert read_side(mixed, 'sourcing').rate == 32.0


def test_안_정한_것은_None_이다():
    """0 과 「안 정함」은 다르다 — 0 으로 채우면 그 가격이 마켓에 나간다."""
    got = read_side({}, 'sourcing')
    assert got.rate is None and got.amount is None and got.fixed is None


def test_영은_값이다():
    assert read_side({'sourcing_mode': 'margin_rate', 'sourcing_rate': 0}, 'sourcing').rate == 0.0


def test_모르는_쪽은_거부한다():
    with pytest.raises(ValueError):
        read_side({}, '이상한쪽')
```

- [ ] **Step 2: 실패 확인**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy/test_price_fields.py -q`
Expected: FAIL — `ModuleNotFoundError: lemouton.policy.price_cfg`

- [ ] **Step 3: 구현**

`lemouton/policy/price_cfg.py`:

```python
# -*- coding: utf-8 -*-
"""「판매가」 항목 값 읽기 — 옛 칸 ↔ 새 칸 번역을 여기 한 곳에 둔다.

2026-08-01 이전 저장분은 소싱/사입 구분이 없고 방식이 둘(마진율·고정금액)뿐이었다.
읽는 쪽이 저마다 번역하면 세 벌이 갈린다 — 미리보기·대량등록 점검·화면이 모두 여기를 쓴다.

🔴 **안 정한 것은 None 이다.** 0 으로 채우면 그 가격이 그대로 마켓에 나간다.
"""
from __future__ import annotations

from dataclasses import dataclass

SIDES = ('sourcing', 'purchase')
MODES = ('margin_rate', 'margin_amount', 'fixed_price')


@dataclass(frozen=True)
class PriceSide:
    """한쪽(소싱품 또는 사입품)의 가격 정하는 법."""

    mode: str
    rate: float | None
    amount: int | None
    fixed: int | None


def _num(v):
    """숫자만 값으로 본다. True/False 는 숫자가 아니다(파이썬에선 int 로 통한다)."""
    if isinstance(v, bool) or not isinstance(v, (int, float)):
        return None
    return v


def read_side(cfg: dict, side: str) -> PriceSide:
    """정책 「판매가」 설정에서 한쪽을 읽는다. 옛 칸도 알아본다."""
    if side not in SIDES:
        raise ValueError(f"모르는 쪽입니다: {side!r} — 쓸 수 있는 값: {', '.join(SIDES)}")
    cfg = cfg or {}

    mode = cfg.get(f'{side}_mode')
    rate = _num(cfg.get(f'{side}_rate'))
    amount = _num(cfg.get(f'{side}_amount'))
    fixed = _num(cfg.get(f'{side}_fixed'))

    # ── 옛 칸 번역 (새 칸이 하나도 없을 때만) ──
    #   새 칸이 하나라도 있으면 사장님이 새 화면에서 고친 것이다 — 그게 이긴다.
    if mode is None and rate is None and amount is None and fixed is None:
        old_mode = cfg.get('mode')
        if old_mode == 'fixed_amount':
            mode = 'fixed_price'
            fixed = _num(cfg.get('fixed_amount'))
        elif old_mode == 'margin_rate':
            mode = 'margin_rate'
            rate = _num(cfg.get('margin_rate'))

    return PriceSide(mode=(mode or 'margin_rate'),
                     rate=None if rate is None else float(rate),
                     amount=None if amount is None else int(amount),
                     fixed=None if fixed is None else int(fixed))


def effective_value(cfg: dict, side: str):
    """그 쪽에서 **지금 실제로 쓰는 값** — 고른 방식의 칸 하나. 안 정했으면 None.

    화면이 「쓰는 값」 배지를 다는 자리와 같은 판정이어야 한다(I2 안).
    """
    s = read_side(cfg, side)
    return {'margin_rate': s.rate, 'margin_amount': s.amount,
            'fixed_price': s.fixed}.get(s.mode)
```

- [ ] **Step 4: 통과 확인**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy/test_price_fields.py -q`
Expected: PASS (8 passed)

- [ ] **Step 5: 커밋**

```bash
git add "프로그램/_시스템/lemouton/policy/price_cfg.py" "프로그램/_시스템/tests/policy/test_price_fields.py"
git commit -m "feat(판매가): 옛 칸 ↔ 새 칸 번역기 — 저장된 값이 조용히 사라지지 않게"
```

---

## Task 2: 「판매가」 항목 15칸으로 넓히기

**Files:**
- Modify: `프로그램/_시스템/lemouton/registration/process_rule_schema.py` (price 항목)
- Test: `프로그램/_시스템/tests/policy/test_price_fields.py`

- [ ] **Step 1: 실패하는 테스트를 붙인다**

```python
# ── 커버리지 — 가격 템플릿이 가진 것을 다 담았나 ──────────────────────────

def _price_field_keys():
    from lemouton.registration.process_rule_schema import schema_for
    return {f.key for f in schema_for('price').fields}


def test_소싱품_사입품이_따로_있다():
    keys = _price_field_keys()
    for side in ('sourcing', 'purchase'):
        for suffix in ('mode', 'rate', 'amount', 'fixed'):
            assert f'{side}_{suffix}' in keys, f'{side}_{suffix} 가 없다'


def test_가격_안전장치_여섯_칸():
    keys = _price_field_keys()
    for k in ('floor_price', 'cap_price', 'rounding_unit',
              'normal_price', 'source_pick', 'size_unify'):
        assert k in keys, f'{k} 가 없다'


def test_수수료율은_그대로_있다():
    assert 'fee_rate' in _price_field_keys()


def test_돈_칸은_기본값을_두지_않는다():
    """「안 정함」과 「0원으로 정함」은 다르다 — 임의 기본값은 그 가격을 마켓에 보낸다."""
    from lemouton.registration.process_rule_schema import schema_for
    money = {'sourcing_rate', 'sourcing_amount', 'sourcing_fixed',
             'purchase_rate', 'purchase_amount', 'purchase_fixed',
             'fee_rate', 'floor_price', 'cap_price', 'normal_price'}
    for f in schema_for('price').fields:
        if f.key in money:
            assert f.default is None, f'{f.key} 에 기본값 {f.default!r} 이 있다'


def test_방식은_세_가지():
    from lemouton.registration.process_rule_schema import schema_for
    for f in schema_for('price').fields:
        if f.key.endswith('_mode'):
            assert set(f.choices) == {'margin_rate', 'margin_amount', 'fixed_price'}
```

- [ ] **Step 2: 실패 확인**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy/test_price_fields.py -q`
Expected: FAIL — `sourcing_mode 가 없다`

- [ ] **Step 3: 구현**

`process_rule_schema.py` 의 `"price": ItemSchema(...)` 를 통째로 바꾼다:

```python
    "price": ItemSchema(
        "price", ITEM_LABELS["price"], "§7-2 판매가·마진 (§5 전체 적용)",
        note="★ 기준은 최종매입가입니다(사장님 확정). 소싱품과 사입품은 매입 구조가 "
             "달라 따로 정합니다 — 한 값으로 묶으면 사입품 마진이 틀어집니다.",
        fields=(
            # ── 소싱품 (소싱처에서 사서 바로 보내는 상품) ──
            _F("sourcing_mode", "소싱품 정하는 법", "choice", default="margin_rate",
               choices=("margin_rate", "margin_amount", "fixed_price"),
               hint="마진율 = 매입가 × (1+율) · 마진금액 = 매입가 + 금액 · 지정가 = 이 값으로 못 박음"),
            _F("sourcing_rate", "소싱품 마진율", "int", default=None, unit="%",
               hint="최종매입가 기준"),
            _F("sourcing_amount", "소싱품 마진금액", "int", default=None, unit="원"),
            _F("sourcing_fixed", "소싱품 지정가", "int", default=None, unit="원"),
            # ── 사입품 (우리가 미리 사둔 재고) ──
            _F("purchase_mode", "사입품 정하는 법", "choice", default="margin_rate",
               choices=("margin_rate", "margin_amount", "fixed_price")),
            _F("purchase_rate", "사입품 마진율", "int", default=None, unit="%"),
            _F("purchase_amount", "사입품 마진금액", "int", default=None, unit="원"),
            _F("purchase_fixed", "사입품 지정가", "int", default=None, unit="원"),
            # ── 마켓 공통 ──
            # [2026-07-31] 노션 「(3) 마켓별 가격 정책 — 마켓별수수료」.
            #   🔴 기본값을 두지 않는다 — 「안 정함」과 「0%로 정함」은 다른 뜻이다.
            #     안 정하면 마진 엔진의 마켓별 기본값(unified._DEFAULT_FEE)을 쓴다.
            _F("fee_rate", "수수료율", "int", default=None, unit="%",
               hint="비워두면 마켓 기본값을 씁니다 (스스 6 · 쿠팡 11.55 · 나머지 13)"),
            # ── 가격 안전장치 (확정 J3 — 접지 않고 항상 보인다) ──
            _F("floor_price", "안 내려갈 값", "int", default=None, unit="원",
               hint="이 밑으로는 안 나갑니다"),
            _F("cap_price", "안 올라갈 값", "int", default=None, unit="원"),
            _F("rounding_unit", "끝자리 맞춤", "int", default=100, unit="원",
               hint="이 단위로 버립니다 (100 = 100원 단위)"),
            _F("normal_price", "정상가", "int", default=None, unit="원",
               hint="할인 전 표시가. 안 정하면 마켓에 보내지 않습니다"),
            _F("source_pick", "여러 소싱처일 때", "choice", default="cheapest",
               choices=("cheapest", "priciest", "average"),
               hint="한 옵션에 소싱처가 여럿일 때 어느 매입가로 계산할지"),
            # [2026-08-01] 확정 K3 — 스스 전용 항목이던 「사이즈별 가격 통일」을
            #   판매가 안으로 들였다. 가격을 정하는 규칙이 판매가와 떨어져 있으면
            #   판매가만 채우고 지나친다.
            _F("size_unify", "사이즈별 가격 통일", "choice", default="",
               choices=("", "max", "min"),
               hint="사이즈마다 매입가가 다를 때 — 비우면 통일 안 함 · "
                    "max = 가장 비싼 값으로 · min = 가장 싼 값으로"),
        )),
```

- [ ] **Step 4: 통과 확인**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy/test_price_fields.py -q`
Expected: PASS (13 passed)

- [ ] **Step 5: 커밋**

```bash
git add "프로그램/_시스템/lemouton/registration/process_rule_schema.py" "프로그램/_시스템/tests/policy/test_price_fields.py"
git commit -m "feat(판매가): 4칸 → 15칸 — 소싱·사입 갈라짐, 방식 3가지, 안전장치 6칸"
```

---

## Task 3: 「사이즈별 가격 통일」을 판매가로 옮기기

**Files:**
- Modify: `프로그램/_시스템/lemouton/policy/fields.py` (`EXTRA_ITEMS` 에서 `_size_unify` 제거)
- Test: `프로그램/_시스템/tests/policy/test_price_fields.py`

- [ ] **Step 1: 실패하는 테스트를 붙인다**

```python
def test_사이즈_통일은_이제_판매가_안에_있다():
    """스스 전용 항목이 아니라 6마켓 모두의 판매가 안에 있다."""
    from lemouton.policy.fields import MARKET_KEYS, item_keys_for, items_for
    for mk in MARKET_KEYS:
        assert '_size_unify' not in item_keys_for(mk), f'{mk} 에 옛 항목이 남았다'
    for mk in MARKET_KEYS:
        price = [it for it in items_for(mk) if it['key'] == 'price'][0]
        assert any(f['key'] == 'size_unify' for f in price['fields']), \
            f'{mk} 판매가에 사이즈 통일이 없다'
```

- [ ] **Step 2: 실패 확인**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy/test_price_fields.py -q`
Expected: FAIL — `smartstore 에 옛 항목이 남았다`

- [ ] **Step 3: 구현**

`lemouton/policy/fields.py` 의 `EXTRA_ITEMS` 에서 `_size_unify` 묶음(`{...'key': '_size_unify'...}`)
을 통째로 지우고, 그 자리에 한 줄 주석을 남긴다:

```python
    # [2026-08-01] '_size_unify'(사이즈별 가격 통일)는 **판매가 항목 안으로 옮겼다**
    #   (확정 K3). 가격을 정하는 규칙이 판매가와 떨어져 있으면 지나친다.
    #   스스 전용도 아니게 됐다 — 가격 템플릿은 6마켓 모두 이 규칙을 갖고 있었다.
```

- [ ] **Step 4: 통과 확인**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy -q`
Expected: 실패 0건 (기존 정책 테스트 포함)

- [ ] **Step 5: 커밋**

```bash
git add "프로그램/_시스템/lemouton/policy/fields.py" "프로그램/_시스템/tests/policy/test_price_fields.py"
git commit -m "feat(판매가): 사이즈별 가격 통일을 판매가 안으로 — 6마켓 공통"
```

---

## Task 4: 미리보기·대량등록 점검이 새 칸을 읽게

**Files:**
- Modify: `프로그램/_시스템/lemouton/policy/preview.py:30-50`
- Modify: `프로그램/_시스템/lemouton/registration/process_apply.py:961-981`
- Test: `프로그램/_시스템/tests/policy/test_price_fields.py`

- [ ] **Step 1: 실패하는 테스트를 붙인다**

```python
# ── 읽는 쪽이 새 칸을 알아보나 ────────────────────────────────────────────

def test_미리보기가_새_칸을_읽는다():
    from lemouton.policy.preview import fixed_amount_of, margin_rate_of
    v = {'price': {'sourcing_mode': 'margin_rate', 'sourcing_rate': 25}}
    assert margin_rate_of(v) == 25.0
    assert fixed_amount_of(v) is None


def test_미리보기가_지정가를_읽는다():
    from lemouton.policy.preview import fixed_amount_of, margin_rate_of
    v = {'price': {'sourcing_mode': 'fixed_price', 'sourcing_fixed': 128900}}
    assert fixed_amount_of(v) == 128900
    assert margin_rate_of(v) is None


def test_미리보기가_옛_값도_읽는다():
    """옛 정책이 조용히 「안 정함」이 되면 안 된다."""
    from lemouton.policy.preview import margin_rate_of
    assert margin_rate_of({'price': {'mode': 'margin_rate', 'margin_rate': 9}}) == 9.0


def test_미리보기는_소싱품_기준이다():
    """미리보기의 매입가는 소싱처 값이라 소싱품 쪽을 봐야 한다."""
    from lemouton.policy.preview import margin_rate_of
    v = {'price': {'sourcing_mode': 'margin_rate', 'sourcing_rate': 9,
                   'purchase_mode': 'margin_rate', 'purchase_rate': 30}}
    assert margin_rate_of(v) == 9.0
```

- [ ] **Step 2: 실패 확인**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy/test_price_fields.py -q`
Expected: FAIL — `margin_rate_of` 가 None 을 돌려줌

- [ ] **Step 3: 구현**

`lemouton/policy/preview.py` 의 두 함수를 번역기 위로 올린다:

```python
def margin_rate_of(values: dict):
    """정책값에서 마진율(%) — 안 정했으면 None. 0 은 「0%로 정함」이라 값이다.

    ★ 미리보기의 매입가는 **소싱처 최종매입가**라 소싱품 쪽을 본다.
    """
    from lemouton.policy.price_cfg import read_side
    s = read_side((values or {}).get(PRICE_ITEM) or {}, 'sourcing')
    return s.rate if s.mode == 'margin_rate' else None


def fixed_amount_of(values: dict):
    """지정 판매가 — 「지정가」 방식일 때만. 아니면 None."""
    from lemouton.policy.price_cfg import read_side
    s = read_side((values or {}).get(PRICE_ITEM) or {}, 'sourcing')
    if s.mode != 'fixed_price' or s.fixed is None or s.fixed <= 0:
        return None
    return s.fixed
```

`lemouton/registration/process_apply.py` 의 `pr = rules.get('price')` 블록(961~981행)에서
`mode`·`fixed_amount`·`margin_rate` 를 직접 읽던 부분을 번역기로 바꾼다:

```python
    pr = rules.get('price')
    if pr is not None:
        from lemouton.policy.price_cfg import read_side
        side = read_side(pr, 'sourcing')
        if side.mode == 'fixed_price':
            want = side.fixed
            if not want:
                out.append(_skip('price', 'sourcing_fixed', 'NO_FIXED_PRICE',
                                 '판매가를 「지정가」로 정하셨는데 금액이 비어 '
                                 '있습니다 — 0원으로 올릴 수 없어 멈춥니다.', True))
            elif sale_price is not None and sale_price != want:
                out.append(_skip('price', 'sourcing_fixed', 'PRICE_NOT_APPLIED',
                                 f'규칙의 지정 판매가({want:,}원)가 이 상품의 판매가'
                                 f'({sale_price:,}원)와 다릅니다 — 판매가는 마진 엔진과 '
                                 f'상품 화면이 정합니다. 가공 규칙이 판매가를 덮으면 '
                                 f'같은 금액을 두 곳에서 만들게 돼 갈립니다.', False))
        else:
            out.append(_skip('price', 'sourcing_rate', 'PRICE_BY_MARGIN_ENGINE',
                             '판매가는 마진 엔진이 최종매입가에 마진율을 붙여 만듭니다 — '
                             '가공 규칙의 마진율은 계산에 쓰이지 않습니다. 마진율은 '
                             '「정책 생성」에서 정해 주세요(같은 숫자를 두 곳에 두면 '
                             '반드시 갈립니다).', False, gap=True))
```

- [ ] **Step 4: 통과 확인**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy tests/registration -q`
Expected: 실패는 작업 전과 같은 5건뿐 (`git stash` 로 대조)

- [ ] **Step 5: 커밋**

```bash
git add "프로그램/_시스템/lemouton/policy/preview.py" "프로그램/_시스템/lemouton/registration/process_apply.py" "프로그램/_시스템/tests/policy/test_price_fields.py"
git commit -m "fix(판매가): 미리보기·대량등록 점검이 새 칸을 읽게 — 번역기 한 곳만 거친다"
```

---

## Task 5: 판매가 전용 화면 (I2 + J3 + K3)

**Files:**
- Modify: `프로그램/_시스템/webapp/templates/policy/detail.html`
- Test: `프로그램/_시스템/tests/policy/test_policy_routes.py`

- [ ] **Step 1: 실패하는 테스트를 붙인다**

`tests/policy/test_policy_routes.py` 맨 아래:

```python
def test_판매가는_소싱품_사입품_두_칸으로_나온다(client):
    pid = _new_policy(client, '판매가화면')
    body = client.get(f'/policies/{pid}?m=smartstore').get_data(as_text=True)
    assert '소싱품' in body
    assert '사입품' in body


def test_판매가에_가격_안전장치_묶음이_있다(client):
    pid = _new_policy(client, '안전장치')
    body = client.get(f'/policies/{pid}?m=smartstore').get_data(as_text=True)
    assert '가격 안전장치' in body
    assert '안 내려갈 값' in body
    assert '사이즈별 가격 통일' in body


def test_안_고른_방식은_흐리게_나온다(client):
    """확정 I2 — 세 칸을 다 보여주되 지금 먹는 칸만 또렷하게."""
    pid = _new_policy(client, '흐리게')
    body = client.get(f'/policies/{pid}?m=smartstore').get_data(as_text=True)
    assert 'pf-dim' in body
    assert '쓰는 값' in body
```

- [ ] **Step 2: 실패 확인**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy/test_policy_routes.py -q -k 판매가 or 안전장치 or 흐리게`
Expected: FAIL

- [ ] **Step 3: 구현**

`detail.html` 의 `<style>` 끝(`.mk-grid .src{...}` 다음)에 붙인다:

```css
    /* ── 판매가 전용 (확정 I2·J3·K3) ── */
    .pf-two{display:grid;grid-template-columns:1fr 1fr;gap:12px}
    .pf-box{background:var(--bg,#F9FAFB);border:1px solid var(--line,#E5E8EB);
            border-radius:8px;padding:12px 12px}
    .pf-box.src2{border-top:3px solid var(--primary,#3182F6)}
    .pf-box.buy{border-top:3px solid var(--green,#15803D)}
    .pf-box .t{font-size:13px;font-weight:700;margin-bottom:2px}
    .pf-box .d{font-size:11px;color:var(--sub,#8B95A1);margin-bottom:8px}
    .pf-dim{opacity:.42}
    .pf-live{font-size:10.5px;font-weight:700;background:var(--연한-파랑,#E8F2FF);
             color:#1B64DA;border-radius:999px;padding:0 8px;margin-left:6px}
    .pf-guard{margin-top:12px;padding-top:10px;border-top:1px dashed var(--line,#E5E8EB)}
    .pf-guard .gt{font-size:12px;font-weight:700;color:var(--글자-기본,#4E5968);
                  margin-bottom:8px}
```

같은 파일 항목 반복문 안, `<div class="flds">` 를 그리기 **전에** 판매가만 갈라 그린다.
`{% for it in items %}` 블록의 `<div class="it-b">` 안을 이렇게 바꾼다:

```html
    <div class="it-b">
      {% if it.note %}<div class="it-note">{{ it.note }}</div>{% endif %}
      {% if it.key == 'price' %}
      {# 판매가 — 소싱품·사입품을 나란히(G1) · 방식 세 칸을 다 보이되 안 고른 건 흐리게(I2) #}
      {% set cfg = saved or {} %}
      <div class="pf-two">
        {% for side, label, desc, cls in [
             ('sourcing', '소싱품', '소싱처에서 사서 바로 보내는 상품', 'src2'),
             ('purchase', '사입품', '우리가 미리 사둔 재고', 'buy')] %}
        {% set cur = cfg.get(side ~ '_mode') or 'margin_rate' %}
        <div class="pf-box {{ cls }}">
          <div class="t">{{ label }}</div><div class="d">{{ desc }}</div>
          <div class="fld">
            <label>정하는 법</label>
            <select data-k="{{ side }}_mode" data-t="choice">
              <option value="margin_rate" {{ 'selected' if cur == 'margin_rate' }}>마진율</option>
              <option value="margin_amount" {{ 'selected' if cur == 'margin_amount' }}>마진금액</option>
              <option value="fixed_price" {{ 'selected' if cur == 'fixed_price' }}>지정가</option>
            </select>
          </div>
          <div class="flds" style="grid-template-columns:1fr 1fr 1fr;margin-top:8px">
            {% for k, nm, unit, mode in [
                 ('rate', '마진율', '%', 'margin_rate'),
                 ('amount', '마진금액', '원', 'margin_amount'),
                 ('fixed', '지정가', '원', 'fixed_price')] %}
            <div class="fld {{ '' if cur == mode else 'pf-dim' }}">
              <label>{{ nm }}<span class="unit">{{ unit }}</span>
                {% if cur == mode %}<span class="pf-live">쓰는 값</span>{% endif %}</label>
              <input type="number" step="1" data-k="{{ side }}_{{ k }}" data-t="int"
                     value="{{ cfg.get(side ~ '_' ~ k) if cfg.get(side ~ '_' ~ k) is not none else '' }}"
                     placeholder="비우면 안 정함">
            </div>
            {% endfor %}
          </div>
        </div>
        {% endfor %}
      </div>
      <div class="flds" style="margin-top:12px">
        <div class="fld"><label>수수료율<span class="unit">%</span></label>
          <input type="number" step="1" data-k="fee_rate" data-t="int"
                 value="{{ cfg.get('fee_rate') if cfg.get('fee_rate') is not none else '' }}"
                 placeholder="비우면 안 정함">
          <div class="hint">비워두면 마켓 기본값을 씁니다 (스스 6 · 쿠팡 11.55 · 나머지 13)</div></div>
      </div>
      {# 가격 안전장치 — 접지 않는다(J3). 사이즈 통일도 여기(K3). #}
      <div class="pf-guard">
        <div class="gt">가격 안전장치</div>
        <div class="flds">
          <div class="fld"><label>안 내려갈 값<span class="unit">원</span></label>
            <input type="number" step="1" data-k="floor_price" data-t="int"
                   value="{{ cfg.get('floor_price') if cfg.get('floor_price') is not none else '' }}"
                   placeholder="비우면 안 정함">
            <div class="hint">이 밑으로는 안 나갑니다</div></div>
          <div class="fld"><label>안 올라갈 값<span class="unit">원</span></label>
            <input type="number" step="1" data-k="cap_price" data-t="int"
                   value="{{ cfg.get('cap_price') if cfg.get('cap_price') is not none else '' }}"
                   placeholder="비우면 안 정함"></div>
          <div class="fld"><label>끝자리 맞춤<span class="unit">원</span></label>
            <input type="number" step="1" data-k="rounding_unit" data-t="int"
                   value="{{ cfg.get('rounding_unit') if cfg.get('rounding_unit') is not none else 100 }}">
            <div class="hint">이 단위로 버립니다</div></div>
          <div class="fld"><label>정상가<span class="unit">원</span></label>
            <input type="number" step="1" data-k="normal_price" data-t="int"
                   value="{{ cfg.get('normal_price') if cfg.get('normal_price') is not none else '' }}"
                   placeholder="비우면 안 정함">
            <div class="hint">할인 전 표시가</div></div>
          <div class="fld"><label>여러 소싱처일 때</label>
            <select data-k="source_pick" data-t="choice">
              {% set sp = cfg.get('source_pick') or 'cheapest' %}
              <option value="cheapest" {{ 'selected' if sp == 'cheapest' }}>가장 싼 곳</option>
              <option value="priciest" {{ 'selected' if sp == 'priciest' }}>가장 비싼 곳</option>
              <option value="average" {{ 'selected' if sp == 'average' }}>평균</option>
            </select></div>
          <div class="fld"><label>사이즈별 가격 통일</label>
            <select data-k="size_unify" data-t="choice">
              {% set su = cfg.get('size_unify') or '' %}
              <option value="" {{ 'selected' if su == '' }}>통일 안 함</option>
              <option value="max" {{ 'selected' if su == 'max' }}>가장 비싼 값으로</option>
              <option value="min" {{ 'selected' if su == 'min' }}>가장 싼 값으로</option>
            </select>
            <div class="hint">사이즈마다 매입가가 다를 때</div></div>
        </div>
      </div>
      {% else %}
      <div class="flds">
        ... (기존 반복문 그대로) ...
      </div>
      {% endif %}
    </div>
```

> **주의** — `{% else %}` 아래는 **지금 있는 `<div class="flds">…</div>` 를 그대로 둔다.**
> 옮겨 적지 말고 그 자리에 그대로 남긴다.

「정하는 법」을 바꾸면 흐림이 바로 따라오게 `<script>` 맨 아래에 붙인다:

```javascript
/* 판매가 — 「정하는 법」을 바꾸면 쓰는 칸만 또렷해진다(저장 전에도 눈에 보이게). */
document.querySelectorAll('select[data-k$="_mode"]').forEach(function (sel) {
  const side = sel.dataset.k.replace('_mode', '');
  const map = {margin_rate: 'rate', margin_amount: 'amount', fixed_price: 'fixed'};
  sel.addEventListener('change', function () {
    Object.entries(map).forEach(function ([mode, suffix]) {
      const box = document.querySelector('[data-k="' + side + '_' + suffix + '"]');
      if (!box) return;
      const fld = box.closest('.fld');
      fld.classList.toggle('pf-dim', sel.value !== mode);
      const old = fld.querySelector('.pf-live');
      if (old) old.remove();
      if (sel.value === mode) {
        const b = document.createElement('span');
        b.className = 'pf-live'; b.textContent = '쓰는 값';
        fld.querySelector('label').appendChild(b);
      }
    });
  });
});
```

- [ ] **Step 4: 통과 확인**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy -q`
Expected: 실패 0건

- [ ] **Step 5: 커밋**

```bash
git add "프로그램/_시스템/webapp/templates/policy/detail.html" "프로그램/_시스템/tests/policy/test_policy_routes.py"
git commit -m "feat(판매가): 화면 — 소싱·사입 나란히 · 안 고른 방식은 흐리게 · 안전장치 항상 펼침"
```

---

## Task 6: 라이브 실검증 · 배포

- [ ] **Step 1: 정책·대량등록 테스트**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy tests/registration -q`
Expected: 실패는 작업 전과 같은 5건뿐

- [ ] **Step 2: 격리한 서버로 끝까지 태워본다**

임시 SQLite 로 앱을 띄우고 확인:
1. 판매가 항목에 소싱품·사입품 두 칸이 나오는가
2. 「정하는 법」을 바꾸면 흐림과 「쓰는 값」이 따라오는가
3. 소싱품 마진율 9.45 · 사입품 지정가 115900 을 저장하고 다시 열면 그대로인가
4. 「가격 안전장치」 6칸이 접히지 않고 보이는가
5. 옛 값(`mode`/`margin_rate`)만 있는 정책을 열어도 안 깨지는가

- [ ] **Step 3: PR · 머지 · 배포**

```bash
git push -u origin feature/process-tab-stage2
gh pr create --title "feat(상품 가공): 「판매가」 항목 심화 — 소싱·사입 × 3가지 방식 + 안전장치" --body "..."
```

- [ ] **Step 4: 배포 뒤 라이브에서 눈으로 확인** — 「배포 success」만 믿지 않는다

---

## 이 계획이 다루지 않는 것

| 계획 | 내용 |
|---|---|
| 3 | 「상품 정책 적용」 하위탭 (상품 검색·체크 → 정책 붙이기) |
| 4 | 상품 상세 3탭 재편(F1) + 정책별 결과 보기(H1) |
| 5 | 가격 단일 원천 전환 + 전 모음전 전수 대조 |

---

## Self-Review

**설계서 대비** — §3-5(I2) Task 2·5 · §3-6(J3+K3) Task 2·3·5 · §4-3(11칸+안전장치) Task 2 ·
§4-2 커버리지 Task 2 테스트. §4-4(배송비 중복 금지)는 **판매가에 배송비 칸을 안 만드는 것**으로 지킨다.

**빈칸 검사** — 「TBD」·「적절히」 없음. Task 5 의 `{% else %}` 는 기존 코드를 **그대로 둔다**고
명시했다(옮겨 적지 않는다).

**이름 일관성** — `read_side`·`PriceSide`·`effective_value`·`sourcing_*`/`purchase_*`·
`floor_price`/`cap_price`/`rounding_unit`/`normal_price`/`source_pick`/`size_unify` 가
정의(Task 1·2)와 사용(Task 4·5)에서 같다.
