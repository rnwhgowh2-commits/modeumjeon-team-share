# 주문내역 #2 CS 「반품·교환·취소」 상태변경 탭 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 취소/교환/반품 클레임을 CS 탭에서 3단계(신규요청→대응필요→대응완료) 칸반으로 관리하고, 「확인」·메모만 저장하며 「완료/철회」는 마켓 상태에서 자동 파생한다.

**Architecture:** 데이터=`order_export.status_change_rows`(변경일 기준 `_kind='change'` 수집, #1 태그 재사용). 저장=신규 `lemouton/claims` 모듈(`ClaimHandling` 테이블 — 확인시각·메모, create_all 자동생성). 단계=저장 안 하고 매번 파생(종결→대응완료 최우선). 화면=`/orders/cs/claims.json`을 먹는 A1+B5 칸반(상단 로고+건수 마켓 탭 + 3열).

**Tech Stack:** Python 3, Flask, SQLAlchemy(DeclarativeBase `shared.db.Base`), pytest. cwd=`프로그램/_시스템`.

**스펙:** `docs/superpowers/specs/2026-07-16-CS-상태변경탭-반품교환취소-design.md`
**확정 시안:** `바탕화면/모음전_CS_칸반_마켓탭_시안_v2.html` (A1 상단 가로 마켓 탭 + B5 로고+건수 배지 + 3열 칸반)
**분기:** `main` 기준 새 워크트리(현재 `feature/crawl-worker-queue` stale).

---

## File Structure

- **Create** `프로그램/_시스템/lemouton/claims/__init__.py` — 빈 파일.
- **Create** `프로그램/_시스템/lemouton/claims/models.py` — `ClaimHandling` 모델.
- **Create** `프로그램/_시스템/lemouton/claims/service.py` — 파생 규칙(claim_key/type/terminal/stage) + acknowledge/save_memo/list_claims.
- **Modify** `프로그램/_시스템/lemouton/markets/order_export.py` — `status_change_rows` + `_change_date_of` 헬퍼.
- **Modify** `프로그램/_시스템/app.py` — `import lemouton.claims.models` 등록(create_all 대상).
- **Modify** `프로그램/_시스템/webapp/routes/orders.py` — `/orders/cs/claims.json`·`/ack`·`/memo` 라우트.
- **Modify** `프로그램/_시스템/webapp/templates/orders/index.html` — cs 탭 = A1+B5 칸반(claims.json fetch).
- **Create** tests: `tests/orders/test_status_change_rows.py`, `tests/claims/__init__.py`, `tests/claims/test_claim_service.py`, `tests/orders/test_cs_routes.py`.

---

## Task 1: `status_change_rows` (변경일 기준 클레임 수집)

**Files:**
- Modify: `프로그램/_시스템/lemouton/markets/order_export.py`
- Test: `프로그램/_시스템/tests/orders/test_status_change_rows.py`

- [ ] **Step 1: 실패 테스트 작성**

Create `프로그램/_시스템/tests/orders/test_status_change_rows.py`:

```python
# -*- coding: utf-8 -*-
"""status_change_rows — _kind='change' 를 변경일 기준으로 수집."""
import datetime as dt
import pytest
from lemouton.markets import order_export as oe

KST = dt.timezone(dt.timedelta(hours=9))
SINCE = dt.datetime(2026, 7, 15, tzinfo=KST)
UNTIL = dt.datetime(2026, 7, 15, 23, 59, tzinfo=KST)


def _fake_fetch(rows):
    def _f(markets, days, now, since=None, until=None, include_settlement=True, warnings=None):
        return list(rows)
    return _f


def test_keeps_change_rows_in_change_window(monkeypatch):
    rows = [
        {"_kind": "order", "주문일": "2026-07-15 09:00:00", "주문상태": "결제완료"},
        {"_kind": "change", "_change_date": "20260715120000", "주문상태": "취소요청", "오픈마켓주문번호": "A"},
        {"_kind": "change", "_change_date": "20260710120000", "주문상태": "반품완료", "오픈마켓주문번호": "B"},
        {"_kind": "change", "_change_date": "", "주문상태": "교환요청", "오픈마켓주문번호": "C"},
    ]
    monkeypatch.setattr(oe, "_fetch_combined", _fake_fetch(rows))
    out = oe.status_change_rows(["lotteon"], since=SINCE, until=UNTIL)
    assert {r["오픈마켓주문번호"] for r in out} == {"A"}   # 변경일 창내 change 만(order·창밖·공란 제외)


def test_parses_iso_change_date(monkeypatch):
    rows = [{"_kind": "change", "_change_date": "2026-07-15T12:00:00", "주문상태": "취소요청", "오픈마켓주문번호": "D"}]
    monkeypatch.setattr(oe, "_fetch_combined", _fake_fetch(rows))
    out = oe.status_change_rows(["coupang"], since=SINCE, until=UNTIL)
    assert [r["오픈마켓주문번호"] for r in out] == ["D"]
```

- [ ] **Step 2: 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/orders/test_status_change_rows.py -q`
Expected: FAIL — `module ... has no attribute 'status_change_rows'`

- [ ] **Step 3: 최소 구현** — `order_export.py`에서 `new_order_rows` 근처(또는 `_filter_by_order_date` 아래)에 추가:

```python
def _change_date_of(r):
    """행의 '_change_date'(변경일)에서 날짜(date) 추출. 컴팩트(YYYYMMDD…)·ISO 모두."""
    m = _re.search(r"(\d{4})[-./]?(\d{2})[-./]?(\d{2})", str(r.get("_change_date") or ""))
    if not m:
        return None
    try:
        return _dt.date(int(m.group(1)), int(m.group(2)), int(m.group(3)))
    except ValueError:
        return None


def status_change_rows(markets, days: int = 7, now=None,
                       since=None, until=None, warnings=None) -> list:
    """상태변경(취소/교환/반품) 이벤트만 — 변경일(_change_date) 기준 수집.

    #1의 _kind='change' 태그를 재사용. combined_order_rows는 주문일로 트리밍해 '옛 주문의
    이번 기간 변경'을 놓치므로, 여기선 _fetch_combined(트리밍 전)에서 change 행만 뽑아
    _change_date 로 트리밍한다. 클레임 이터레이터는 since~now 폭으로 조회하므로 창내 변경분 포함.
    """
    rows = _fetch_combined(markets, days, now, since=since, until=until,
                           include_settlement=False, warnings=warnings)
    if not (since and until):
        return [r for r in rows if r.get("_kind") == "change"]
    lo, hi = since.date(), until.date()
    out = []
    for r in rows:
        if r.get("_kind") != "change":
            continue
        d = _change_date_of(r)
        if d is None or not (lo <= d <= hi):
            continue
        out.append(r)
    return out
```
`_re`·`_dt` 는 모듈 상단에 이미 있음(`import re as _re`, `import datetime as _dt`). 없으면 실제 별칭에 맞춰 조정하고 보고.

- [ ] **Step 4: 통과 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/orders/test_status_change_rows.py -q`
Expected: PASS (2 passed)

- [ ] **Step 5: 커밋**

```bash
git add "프로그램/_시스템/lemouton/markets/order_export.py" "프로그램/_시스템/tests/orders/test_status_change_rows.py"
git commit -m "feat(cs): status_change_rows — 변경일 기준 클레임 수집(_kind='change')"
```

---

## Task 2: `ClaimHandling` 모델 + 등록

**Files:**
- Create: `프로그램/_시스템/lemouton/claims/__init__.py`, `프로그램/_시스템/lemouton/claims/models.py`
- Modify: `프로그램/_시스템/app.py` (모델 import 등록)
- Test: `프로그램/_시스템/tests/claims/__init__.py`, `프로그램/_시스템/tests/claims/test_claim_service.py`

- [ ] **Step 1: 실패 테스트 작성**

Create `프로그램/_시스템/tests/claims/__init__.py` (빈 파일) 그리고 `프로그램/_시스템/tests/claims/test_claim_service.py`:

```python
# -*- coding: utf-8 -*-
"""CS 클레임 처리상태 모델·서비스."""
import datetime as dt
import pytest
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from shared.db import Base


@pytest.fixture()
def session():
    import lemouton.claims.models  # 모델 등록
    eng = create_engine("sqlite:///:memory:")
    Base.metadata.create_all(eng)
    S = sessionmaker(bind=eng)
    s = S()
    yield s
    s.close()


def test_claim_handling_table_created(session):
    from lemouton.claims.models import ClaimHandling
    row = ClaimHandling(claim_key="롯데온:LO1:반품", market="롯데온", order_no="LO1", claim_type="반품")
    session.add(row); session.commit()
    got = session.query(ClaimHandling).filter_by(claim_key="롯데온:LO1:반품").one()
    assert got.acknowledged_at is None and got.memo is None
```

- [ ] **Step 2: 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/claims/test_claim_service.py::test_claim_handling_table_created -q`
Expected: FAIL — `No module named 'lemouton.claims'`

- [ ] **Step 3: 최소 구현**

Create `프로그램/_시스템/lemouton/claims/__init__.py` (빈 파일).
Create `프로그램/_시스템/lemouton/claims/models.py`:

```python
"""CS 클레임 처리상태(팀 공유). Alembic 불요 — shared/db.py:init_db()의 create_all이 생성.

단계(신규요청/대응필요/대응완료)는 저장하지 않고 매번 파생(service.derive_stage).
저장하는 것은 ①확인 시각 ②간단메모 둘뿐.
"""
from datetime import datetime, timezone
from sqlalchemy import Column, Integer, String, Text, DateTime
from shared.db import Base


def _now():
    return datetime.now(timezone.utc)


class ClaimHandling(Base):
    __tablename__ = "claim_handling"

    id = Column(Integer, primary_key=True, autoincrement=True)
    claim_key = Column(String(128), unique=True, nullable=False, index=True)  # market:order_no:type
    market = Column(String(32))
    order_no = Column(String(64), index=True)
    claim_type = Column(String(8))              # 취소/교환/반품
    acknowledged_at = Column(DateTime)          # 「확인」 누른 시각(있으면 대응필요)
    memo = Column(Text)                         # 간단메모
    created_at = Column(DateTime, default=_now)
    updated_at = Column(DateTime, default=_now, onupdate=_now)
```

In `프로그램/_시스템/app.py`, 다른 모델 import 옆(예: `import lemouton.delivery.models` 근처)에 추가:

```python
    import lemouton.claims.models  # noqa: F401  # CS 클레임 처리상태 (ClaimHandling)
```

- [ ] **Step 4: 통과 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/claims/test_claim_service.py::test_claim_handling_table_created -q`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add "프로그램/_시스템/lemouton/claims/__init__.py" "프로그램/_시스템/lemouton/claims/models.py" "프로그램/_시스템/app.py" "프로그램/_시스템/tests/claims/__init__.py" "프로그램/_시스템/tests/claims/test_claim_service.py"
git commit -m "feat(cs): ClaimHandling 모델 + app.py 등록(create_all)"
```

---

## Task 3: 파생 규칙 (claim_key·type·terminal·stage)

**Files:**
- Create: `프로그램/_시스템/lemouton/claims/service.py`
- Test: `프로그램/_시스템/tests/claims/test_claim_service.py` (추가)

- [ ] **Step 1: 실패 테스트 작성** — `test_claim_service.py`에 추가:

```python
def test_derive_helpers():
    from lemouton.claims import service as sv
    req = {"판매처": "롯데온", "오픈마켓주문번호": "LO1", "주문상태": "취소요청", "주문상태원본": "02"}
    done = {"판매처": "쿠팡", "오픈마켓주문번호": "CP1", "주문상태": "반품완료", "주문상태원본": "RETURNS_COMPLETED"}
    wd_lo = {"판매처": "롯데온", "오픈마켓주문번호": "LO2", "주문상태": "취소요청", "주문상태원본": "22"}  # 철회
    assert sv.claim_key_of(req) == "롯데온:LO1:취소"
    assert sv.claim_type_of(req) == "취소" and sv.claim_type_of(done) == "반품"
    assert sv.is_terminal(done) is True
    assert sv.is_terminal(wd_lo) is True          # 철회(raw 22)=종결
    assert sv.is_terminal(req) is False
    # stage 파생: 종결 최우선 > 확인 > 신규
    assert sv.derive_stage(done, acknowledged=False) == "대응완료"
    assert sv.derive_stage(wd_lo, acknowledged=False) == "대응완료"
    assert sv.derive_stage(req, acknowledged=True) == "대응필요"
    assert sv.derive_stage(req, acknowledged=False) == "신규요청"
```

- [ ] **Step 2: 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/claims/test_claim_service.py::test_derive_helpers -q`
Expected: FAIL — `No module named 'lemouton.claims.service'`

- [ ] **Step 3: 최소 구현** — Create `프로그램/_시스템/lemouton/claims/service.py`:

```python
"""CS 클레임 서비스 — 단계 파생 + 확인/메모 저장 + 목록 조인.

단계는 저장하지 않는다(스펙 §3). 종결(완료/철회)이면 항상 대응완료(확인 여부 무관 최우선).
"""
_TYPES = ("취소", "교환", "반품")


def claim_type_of(row) -> str:
    st = str(row.get("주문상태") or "")
    for t in _TYPES:
        if st.startswith(t):
            return t
    return ""


def claim_key_of(row) -> str:
    return f'{row.get("판매처","")}:{row.get("오픈마켓주문번호","")}:{claim_type_of(row)}'


def is_terminal(row) -> bool:
    """종결 = 완료 또는 철회. 철회는 마켓별 원본코드로 감지(라벨 미부여이므로)."""
    st = str(row.get("주문상태") or "")
    if st.endswith("완료"):
        return True
    raw = str(row.get("주문상태원본") or "")
    mk = row.get("판매처")
    if mk in ("롯데온", "11번가") and raw == "22":   # odPrgsStepCd/ordPrdStat 22=철회
        return True
    if mk == "쿠팡" and raw == "CANCEL":             # exchangeStatus CANCEL=철회
        return True
    return False


def claim_state_of(row) -> str:
    if str(row.get("주문상태") or "").endswith("완료"):
        return "완료"
    if is_terminal(row):
        return "철회"
    return "요청"


def derive_stage(row, acknowledged: bool) -> str:
    if is_terminal(row):
        return "대응완료"
    return "대응필요" if acknowledged else "신규요청"
```

- [ ] **Step 4: 통과 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/claims/test_claim_service.py::test_derive_helpers -q`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add "프로그램/_시스템/lemouton/claims/service.py" "프로그램/_시스템/tests/claims/test_claim_service.py"
git commit -m "feat(cs): 단계 파생 규칙(종결 최우선·철회 raw22·claim_key)"
```

---

## Task 4: 확인·메모 저장 (upsert)

**Files:**
- Modify: `프로그램/_시스템/lemouton/claims/service.py`
- Test: `프로그램/_시스템/tests/claims/test_claim_service.py` (추가)

- [ ] **Step 1: 실패 테스트 작성** — 추가:

```python
def test_acknowledge_and_memo_upsert(session):
    from lemouton.claims import service as sv
    from lemouton.claims.models import ClaimHandling
    sv.acknowledge("롯데온:LO1:반품", market="롯데온", order_no="LO1", claim_type="반품", session=session)
    got = session.query(ClaimHandling).filter_by(claim_key="롯데온:LO1:반품").one()
    assert got.acknowledged_at is not None
    sv.save_memo("롯데온:LO1:반품", "전화완료·수거대기", session=session)
    got2 = session.query(ClaimHandling).filter_by(claim_key="롯데온:LO1:반품").one()
    assert got2.memo == "전화완료·수거대기" and got2.acknowledged_at is not None   # 확인 유지
    # 같은 key 재확인 = 중복 생성 없음
    sv.acknowledge("롯데온:LO1:반품", market="롯데온", order_no="LO1", claim_type="반품", session=session)
    assert session.query(ClaimHandling).filter_by(claim_key="롯데온:LO1:반품").count() == 1
```

- [ ] **Step 2: 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/claims/test_claim_service.py::test_acknowledge_and_memo_upsert -q`
Expected: FAIL — `module 'lemouton.claims.service' has no attribute 'acknowledge'`

- [ ] **Step 3: 최소 구현** — `service.py`에 추가:

```python
import datetime as _dt

from shared.db import SessionLocal
from lemouton.claims.models import ClaimHandling


def _get_or_create(session, claim_key, **defaults):
    row = session.query(ClaimHandling).filter_by(claim_key=claim_key).one_or_none()
    if row is None:
        row = ClaimHandling(claim_key=claim_key, **defaults)
        session.add(row)
    return row


def acknowledge(claim_key, *, market="", order_no="", claim_type="", session=None):
    """「확인」 처리 — acknowledged_at 설정(upsert). 이미 있으면 유지."""
    own = session is None
    session = session or SessionLocal()
    try:
        row = _get_or_create(session, claim_key, market=market, order_no=order_no, claim_type=claim_type)
        if row.acknowledged_at is None:
            row.acknowledged_at = _dt.datetime.now(_dt.timezone.utc)
        session.commit()
    finally:
        if own:
            session.close()


def save_memo(claim_key, memo, *, market="", order_no="", claim_type="", session=None):
    own = session is None
    session = session or SessionLocal()
    try:
        row = _get_or_create(session, claim_key, market=market, order_no=order_no, claim_type=claim_type)
        row.memo = memo
        session.commit()
    finally:
        if own:
            session.close()
```

- [ ] **Step 4: 통과 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/claims/test_claim_service.py -q`
Expected: PASS (all)

- [ ] **Step 5: 커밋**

```bash
git add "프로그램/_시스템/lemouton/claims/service.py" "프로그램/_시스템/tests/claims/test_claim_service.py"
git commit -m "feat(cs): 확인·메모 upsert(중복 claim_key 갱신)"
```

---

## Task 5: `list_claims` (수집+조인+3그룹)

**Files:**
- Modify: `프로그램/_시스템/lemouton/claims/service.py`
- Test: `프로그램/_시스템/tests/claims/test_claim_service.py` (추가)

- [ ] **Step 1: 실패 테스트 작성** — 추가:

```python
def test_list_claims_groups_and_counts(session, monkeypatch):
    from lemouton.claims import service as sv
    import datetime as dt
    KST = dt.timezone(dt.timedelta(hours=9))
    change_rows = [
        {"판매처": "롯데온", "오픈마켓주문번호": "LO1", "주문상태": "취소요청", "주문상태원본": "02",
         "_change_date": "20260715120000", "상품명": "코트", "옵션": "블랙", "수량": 1, "배송메시지": "변심"},
        {"판매처": "쿠팡", "오픈마켓주문번호": "CP1", "주문상태": "반품완료", "주문상태원본": "RETURNS_COMPLETED",
         "_change_date": "20260715090000", "상품명": "니트", "옵션": "M", "수량": 1, "배송메시지": ""},
    ]
    monkeypatch.setattr(sv, "status_change_rows", lambda markets, **kw: change_rows)
    # LO1 은 확인함 → 대응필요
    sv.acknowledge("롯데온:LO1:취소", market="롯데온", order_no="LO1", claim_type="취소", session=session)
    since = dt.datetime(2026, 7, 15, tzinfo=KST); until = dt.datetime(2026, 7, 15, 23, tzinfo=KST)
    res = sv.list_claims(["lotteon", "coupang"], since=since, until=until, session=session)
    assert [c["오픈마켓주문번호"] for c in res["groups"]["대응필요"]] == ["LO1"]
    assert [c["오픈마켓주문번호"] for c in res["groups"]["대응완료"]] == ["CP1"]   # 완료=자동
    assert res["groups"]["신규요청"] == []
    assert res["market_counts"]["전체"] == 2
```

- [ ] **Step 2: 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/claims/test_claim_service.py::test_list_claims_groups_and_counts -q`
Expected: FAIL — `has no attribute 'list_claims'`

- [ ] **Step 3: 최소 구현** — `service.py` 상단 import에 `status_change_rows` 추가하고 함수 추가:

`service.py` 맨 위 import 블록에:
```python
from lemouton.markets.order_export import status_change_rows
```
그리고 함수:
```python
_STAGES = ("신규요청", "대응필요", "대응완료")


def _claim_view(row, ack):
    return {
        "판매처": row.get("판매처", ""),
        "오픈마켓주문번호": row.get("오픈마켓주문번호", ""),
        "유형": claim_type_of(row),
        "상태": claim_state_of(row),
        "상품명": row.get("상품명", ""),
        "옵션": row.get("옵션", ""),
        "수량": row.get("수량", ""),
        "사유": row.get("배송메시지", ""),
        "변경일": row.get("_change_date", ""),
        "claim_key": claim_key_of(row),
        "메모": (ack.memo if ack else "") or "",
        "단계": derive_stage(row, acknowledged=bool(ack and ack.acknowledged_at)),
    }


def list_claims(markets, *, since, until, session=None):
    """status_change_rows + ClaimHandling 조인 → {groups:3단계, market_counts}."""
    own = session is None
    session = session or SessionLocal()
    try:
        rows = status_change_rows(markets, since=since, until=until)
        keys = [claim_key_of(r) for r in rows]
        handled = {h.claim_key: h for h in
                   session.query(ClaimHandling).filter(ClaimHandling.claim_key.in_(keys or [""])).all()}
        groups = {s: [] for s in _STAGES}
        counts = {"전체": 0}
        for r in rows:
            v = _claim_view(r, handled.get(claim_key_of(r)))
            groups[v["단계"]].append(v)
            counts["전체"] += 1
            counts[v["판매처"]] = counts.get(v["판매처"], 0) + 1
        return {"groups": groups, "market_counts": counts}
    finally:
        if own:
            session.close()
```

- [ ] **Step 4: 통과 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/claims/test_claim_service.py -q`
Expected: PASS (all)

- [ ] **Step 5: 커밋**

```bash
git add "프로그램/_시스템/lemouton/claims/service.py" "프로그램/_시스템/tests/claims/test_claim_service.py"
git commit -m "feat(cs): list_claims — 수집+확인/메모 조인+3단계 그룹·마켓 건수"
```

---

## Task 6: 라우트 (`/orders/cs/claims.json`·`/ack`·`/memo`)

**Files:**
- Modify: `프로그램/_시스템/webapp/routes/orders.py`
- Test: `프로그램/_시스템/tests/orders/test_cs_routes.py`

- [ ] **Step 1: 실패 테스트 작성** — Create `프로그램/_시스템/tests/orders/test_cs_routes.py`:

```python
# -*- coding: utf-8 -*-
"""CS 클레임 라우트 — claims.json/ack/memo. (기존 orders 라우트 테스트 패턴 재사용)"""
import json
import pytest
import webapp.routes.orders as om
# NOTE: 앱/클라이언트 픽스처는 tests/orders/test_orders_tab.py 의 _client() 패턴을 그대로 재사용할 것.


def test_claims_json_shape(monkeypatch, client):
    def _fake(markets, **kw):
        return {"groups": {"신규요청": [{"오픈마켓주문번호": "A", "claim_key": "k"}], "대응필요": [], "대응완료": []},
                "market_counts": {"전체": 1}}
    monkeypatch.setattr("webapp.routes.orders._claim_svc.list_claims", _fake)
    r = client.get("/orders/cs/claims.json?markets=lotteon&range=today")
    assert r.status_code == 200
    data = json.loads(r.data)
    assert data["ok"] is True and data["groups"]["신규요청"][0]["오픈마켓주문번호"] == "A"


def test_ack_and_memo_post(monkeypatch, client):
    calls = {}
    monkeypatch.setattr("webapp.routes.orders._claim_svc.acknowledge",
                        lambda ck, **kw: calls.__setitem__("ack", ck))
    monkeypatch.setattr("webapp.routes.orders._claim_svc.save_memo",
                        lambda ck, memo, **kw: calls.__setitem__("memo", (ck, memo)))
    r1 = client.post("/orders/cs/claims/ack", json={"claim_key": "롯데온:LO1:반품", "market": "롯데온", "order_no": "LO1", "claim_type": "반품"})
    r2 = client.post("/orders/cs/claims/memo", json={"claim_key": "롯데온:LO1:반품", "memo": "메모"})
    assert r1.status_code == 200 and calls["ack"] == "롯데온:LO1:반품"
    assert r2.status_code == 200 and calls["memo"] == ("롯데온:LO1:반품", "메모")
```

> **주의:** `client` 픽스처는 `tests/orders/test_orders_tab.py` 의 `_client()`(Flask(__name__)+`om.bp` 등록) 그대로 재사용. 다르면 그 파일 패턴에 맞춰 이 테스트의 부트스트랩을 조정한다.

- [ ] **Step 2: 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/orders/test_cs_routes.py -q`
Expected: FAIL — 라우트 404 또는 `_claim_svc` 없음

- [ ] **Step 3: 최소 구현** — `webapp/routes/orders.py` 상단 import에 추가:

```python
from lemouton.claims import service as _claim_svc
```
그리고 라우트 3개 추가(파일 내 다른 `@bp.route` 옆):

```python
@bp.route('/cs/claims.json')
def cs_claims():
    markets = _parse_markets(request.args)
    since, until = _parse_range(request.args)
    try:
        res = _claim_svc.list_claims(markets, since=since, until=until)
        return jsonify(ok=True, **res)
    except Exception as e:   # noqa: BLE001
        import logging
        logging.getLogger(__name__).exception("cs claims failed markets=%s", markets)
        return jsonify(ok=False, error=str(e), groups={"신규요청": [], "대응필요": [], "대응완료": []},
                       market_counts={"전체": 0})


@bp.route('/cs/claims/ack', methods=['POST'])
def cs_claim_ack():
    d = request.get_json(silent=True) or {}
    ck = (d.get('claim_key') or '').strip()
    if not ck:
        return jsonify(ok=False, error='claim_key 필요'), 400
    _claim_svc.acknowledge(ck, market=d.get('market', ''), order_no=d.get('order_no', ''),
                           claim_type=d.get('claim_type', ''))
    return jsonify(ok=True)


@bp.route('/cs/claims/memo', methods=['POST'])
def cs_claim_memo():
    d = request.get_json(silent=True) or {}
    ck = (d.get('claim_key') or '').strip()
    if not ck:
        return jsonify(ok=False, error='claim_key 필요'), 400
    _claim_svc.save_memo(ck, d.get('memo', ''), market=d.get('market', ''),
                         order_no=d.get('order_no', ''), claim_type=d.get('claim_type', ''))
    return jsonify(ok=True)
```
`_parse_markets`·`_parse_range`·`request`·`jsonify` 는 이미 import/정의됨(확인 후 실제 이름 사용).

- [ ] **Step 4: 통과 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/orders/test_cs_routes.py -q`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add "프로그램/_시스템/webapp/routes/orders.py" "프로그램/_시스템/tests/orders/test_cs_routes.py"
git commit -m "feat(cs): /orders/cs/claims.json·ack·memo 라우트"
```

---

## Task 7: UI — cs 탭 A1+B5 칸반

**Files:**
- Modify: `프로그램/_시스템/webapp/templates/orders/index.html` (cs 탭 섹션)

TDD 어려운 프론트. 확정 시안(`바탕화면/모음전_CS_칸반_마켓탭_시안_v2.html`)의 **A1+B5** 구조를 그대로 옮긴다.

- [ ] **Step 1: cs 탭 마크업** — cs 서브탭 콘텐츠를 다음 구조로(샘플 TAB_CONFIG['cs'] 렌더 대체):
  - 상단 구분 토글 `[반품·교환·취소][고객문의]`(고객문의는 비활성/placeholder).
  - 상단 가로 마켓 탭(로고+건수 배지, B5): 전체·쿠팡·11번가·롯데온·스마트스토어. 시안 v2의 `.mt-logo` 스타일·`LOGO` 색 그대로.
  - 3열 칸반(`.kb` `.kcol`): 🆕 신규요청 / 👀 대응필요 / ✅ 대응완료. 시안 v2의 `.kcard` 구조 그대로(유형 배지·마켓·상품·옵션·수량·변경일·사유).
- [ ] **Step 2: 데이터 배선** — `fetch('/orders/cs/claims.json?markets=<선택>&range=<기간>')` → `groups`/`market_counts`로 칸반·탭 배지 렌더. 마켓 탭 클릭 시 재조회(또는 전체 조회 후 프론트 필터).
- [ ] **Step 3: 액션 배선**
  - 신규요청 카드 「확인 ✓」 → `POST /orders/cs/claims/ack {claim_key,market,order_no,claim_type}` → 성공 시 카드를 대응필요로 이동(재조회).
  - 메모 입력(신규요청/대응필요) blur/저장 → `POST /orders/cs/claims/memo {claim_key,memo}`.
  - 대응완료 카드 = 읽기전용(완료/철회 배지).
- [ ] **Step 4: 4상태** — 로딩("불러오는 중…")·빈("이 기간 요청 없음")·에러(ok:false 시 안내)·정상. CSS 변수만(하드코딩 금지), 색 의미 불변(취소=주황·반품=빨강·교환=보라 / 신규=주황·대응=파랑·완료=초록·철회=회색).
- [ ] **Step 5: 커밋**

```bash
git add "프로그램/_시스템/webapp/templates/orders/index.html"
git commit -m "feat(cs): 반품·교환·취소 칸반 UI(A1 마켓탭+B5 배지, claims.json 배선)"
```

---

## Task 8: 회귀·라이브 검증·머지·배포

- [ ] **Step 1: 회귀**

Run:
```bash
cd 프로그램/_시스템 && python -m pytest tests/orders/ tests/claims/ tests/platforms/test_order_export.py -q
```
Expected: 전부 PASS (#1 `new_order_rows`·`combined_order_rows` 불변 포함).

- [ ] **Step 2: main 머지 + 배포**

`main` 최신 pull → 워크트리 rebase(충돌 시 order_export.py 주의) → main ff-merge → push. 배포=push→`aws-lightsail-deploy.yml`. `gh run watch`로 success 확인.

- [ ] **Step 3: 라이브 검증** (`mou-m.com/orders/?tab=cs`, 인증 세션)
  - 실제 취소/반품/교환 요청이 **신규요청**에 뜨는가(마켓 탭 건수 일치).
  - 「확인 ✓」 → 대응필요로 이동·새로고침 후 유지, 메모 저장 왕복.
  - 마켓 **완료·철회**건이 **대응완료**에 자동으로 있는가(취소완료·반품완료·철회 배지).
  - preview.json 검증처럼 `/orders/cs/claims.json?days=7&markets=lotteon` 직접 조회로 `groups`·`market_counts` 실측.
  - 11번가 변경일 유무(스펙 §4.1 한계) 확인 — 없으면 별도 보고.
- [ ] **Step 4: 7섹션 보고서**(`/ui-verify` 형식) + 메모리 갱신.

---

## Self-Review (작성자 체크)

- **스펙 커버리지:** §2 화면=Task7(A1+B5) / §3 파생=Task3(종결·철회·확인·신규) / §4.1 status_change_rows=Task1 / §4.2 모델=Task2 / §4.3 서비스=Task3~5 / §5 라우트=Task6 / §6 create_all=Task2(app.py 등록) / §7 엣지=Task3(철회)·Task5(조인) / §8 테스트=Task1~6. ✅ 갭 없음.
- **플레이스홀더:** 코드 스텝 실제 코드·명령·기대출력 포함. Task6·7의 기존 픽스처/템플릿 재사용은 주의 노트로 명시(추측 아님). ✅
- **타입/이름 일관성:** `status_change_rows`·`_change_date_of`·`ClaimHandling`·`claim_key_of`·`claim_type_of`·`is_terminal`·`claim_state_of`·`derive_stage`·`acknowledge`·`save_memo`·`list_claims`·`_claim_svc` 전 태스크 동일. ✅
