# 배송검사 v2 — 마켓 API 연동 · 주문내역 통합 구현 플랜

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 더망고 엑셀의 오픈마켓주문번호로 마켓 실주문을 조회해 ①이중송장 위험 ②배송흐름 정체를 실데이터로 검사하고, 별도 배송검사 탭을 주문 내역 탭으로 통합한다.

**Architecture:** 백엔드(마켓 조회·매칭·검사 v2)를 먼저 TDD로 완성(order_export 목으로 테스트) → UI를 주문 내역 탭에 통합(확정 시안 부품). 기존 v1 배송검사 로직·라우트는 재활용, 화면만 이전.

**Tech Stack:** Flask 3, SQLAlchemy 2.0, pytest(in-memory SQLite + order_export monkeypatch). 신규 의존성 없음. 재활용: `order_export.combined_order_rows/_STATUS_KO/_SHIPPED_STATES`, `invoice_send.read_registered_invoice`.

**설계 원천:** `docs/superpowers/specs/2026-07-12-주문-배송검사-마켓API연동-design.md`

**전제 브랜치:** 최신 main + PR#247(구분자 매핑) 머지 후. `프로그램/_시스템` 에서 실행. 로컬 테스트는 마켓 API 목 필수(실 API=서버 전용).

---

## 파일 구조

- **Modify** `프로그램/_시스템/lemouton/delivery/models.py` — MangoOrder 필드 5개 추가
- **Modify** `프로그램/_시스템/shared/db.py` — `_apply_lightweight_migrations` 에 mango_orders 컬럼 5개 ALTER
- **Create** `프로그램/_시스템/lemouton/delivery/market_enrich.py` — 마켓 슬러그 판별 + order_export 조회 + 매칭 + MangoOrder 갱신
- **Modify** `프로그램/_시스템/lemouton/delivery/service.py` — 검사 v2 (find_double_invoice_risk, find_flow_stalled)
- **Modify** `프로그램/_시스템/webapp/routes/orders.py` — 업로드 시 enrich 자동 호출 + data 응답에 v2 검사 반영 + SUBTABS inspect 제거
- **Modify** `프로그램/_시스템/webapp/routes/api_sidebar.py` — i_inspect 제거 + 기존 저장메뉴 역마이그레이션
- **Modify** `프로그램/_시스템/webapp/templates/orders/index.html` — 주문 내역 list 블록에 더망고 카드·사이드바 막대요약·배송방식 토글칩·검사 배지 통합. inspect 블록 제거
- **Create** `프로그램/_시스템/tests/delivery/test_market_enrich.py`
- **Modify** `프로그램/_시스템/tests/delivery/test_delivery_service.py` — 검사 v2 테스트 추가

---

# PHASE 1 · 백엔드 (마켓 조회·매칭·검사 v2)

## Task 1: MangoOrder 마켓 API 필드 추가

**Files:**
- Modify: `프로그램/_시스템/lemouton/delivery/models.py`
- Modify: `프로그램/_시스템/shared/db.py` (migrations 리스트, 89행 부근)
- Test: `프로그램/_시스템/tests/delivery/test_delivery_service.py`

- [ ] **Step 1: 실패 테스트 추가**

`tests/delivery/test_delivery_service.py` 하단:

```python
def test_market_api_fields(db):
    o = M.MangoOrder(mango_uid="m1")
    o.market_api_status = "배송준비중"
    o.market_api_invoice = "INV1"
    o.market_check_error = None
    db.add(o); db.commit()
    got = db.query(M.MangoOrder).filter_by(mango_uid="m1").one()
    assert got.market_api_status == "배송준비중"
    assert got.market_api_invoice == "INV1"
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tests/delivery/test_delivery_service.py::test_market_api_fields -v`
Expected: FAIL — `AttributeError: 'MangoOrder' object has no attribute 'market_api_status'`

- [ ] **Step 3: 모델 필드 추가**

`lemouton/delivery/models.py` 의 MangoOrder 에 `raw` 컬럼 아래 추가:

```python
    # v2 마켓 API 연동 캐시
    market_api_status = Column(String(32))     # 마켓 통일 주문상태(배송준비중/배송중/배송완료…)
    market_api_invoice = Column(String(64))    # 마켓에 등록된 송장번호
    market_shipped_at = Column(String(32))     # 마켓 발송처리일시(제공 마켓만)
    market_checked_at = Column(DateTime)       # 마지막 조회 시각
    market_check_error = Column(String(200))   # 조회 실패/미매칭 사유(있으면 '확인 불가')
```

- [ ] **Step 4: 경량 마이그레이션 등록**

`shared/db.py` 의 `migrations` 리스트 끝(대략 172행 이후, 리스트 닫히기 전)에 추가:

```python
        # 2026-07-12: 배송검사 v2 — 마켓 API 조회 캐시
        ("mango_orders", "market_api_status", "VARCHAR(32)"),
        ("mango_orders", "market_api_invoice", "VARCHAR(64)"),
        ("mango_orders", "market_shipped_at", "VARCHAR(32)"),
        ("mango_orders", "market_checked_at", "DATETIME"),
        ("mango_orders", "market_check_error", "VARCHAR(200)"),
```

- [ ] **Step 5: 통과 확인**

Run: `python -m pytest tests/delivery/test_delivery_service.py::test_market_api_fields -v`
Expected: PASS

- [ ] **Step 6: 커밋**

```bash
git add "프로그램/_시스템/lemouton/delivery/models.py" "프로그램/_시스템/shared/db.py" "프로그램/_시스템/tests/delivery/test_delivery_service.py"
git commit -m "feat(delivery): MangoOrder 마켓 API 캐시 필드 5개 + 경량 마이그레이션"
```

---

## Task 2: 마켓 슬러그 판별 + 조회 대상 수집

**Files:**
- Create: `프로그램/_시스템/lemouton/delivery/market_enrich.py`
- Test: `프로그램/_시스템/tests/delivery/test_market_enrich.py`

- [ ] **Step 1: 실패 테스트**

Create `tests/delivery/test_market_enrich.py`:

```python
import pytest
from lemouton.delivery.market_enrich import market_slug, group_by_market

def test_market_slug():
    assert market_slug("쿠팡") == "coupang"
    assert market_slug("롯데ON") == "lotteon"
    assert market_slug("롯데온") == "lotteon"
    assert market_slug("스마트스토어") == "smartstore"
    assert market_slug("11번가") == "eleven11"
    assert market_slug("무신사") is None   # 마켓 API 미지원 → 스킵

def test_group_by_market():
    rows = [{"mango_uid":"1","market_name":"쿠팡","market_order_no":"A"},
            {"mango_uid":"2","market_name":"롯데ON","market_order_no":"B"},
            {"mango_uid":"3","market_name":"무신사","market_order_no":"C"}]
    grouped, skipped = group_by_market(rows)
    assert set(grouped.keys()) == {"coupang","lotteon"}
    assert grouped["coupang"] == ["A"]
    assert skipped == ["3"]   # 무신사 미지원 uid
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tests/delivery/test_market_enrich.py -v`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 3: 구현**

Create `프로그램/_시스템/lemouton/delivery/market_enrich.py`:

```python
"""배송검사 v2 — 더망고 주문을 마켓 API 실주문으로 보강(enrich).

오픈마켓주문번호로 order_export 실주문과 매칭 → 마켓 통일상태·등록송장을 MangoOrder 에 캐시.
마켓 API = 서버(AWS) 전용. 로컬/테스트는 order_export 를 monkeypatch.
"""
from datetime import datetime, timezone

from lemouton.markets import order_export as _oe

# 더망고 마켓명(B열) → order_export 슬러그. SUPPORTED 밖은 None(스킵).
_SLUG = {
    "쿠팡": "coupang",
    "롯데on": "lotteon", "롯데온": "lotteon", "롯데ON": "lotteon",
    "스마트스토어": "smartstore", "스스": "smartstore",
    "11번가": "eleven11",
}


def market_slug(market_name):
    if not market_name:
        return None
    slug = _SLUG.get(str(market_name).strip())
    return slug if slug in _oe.SUPPORTED else None


def group_by_market(rows):
    """rows(dict, market_name·market_order_no) → ({slug: [주문번호...]}, [스킵된 uid...])."""
    grouped, skipped = {}, []
    for r in rows:
        slug = market_slug(r.get("market_name"))
        if not slug:
            skipped.append(r.get("mango_uid"))
            continue
        no = (r.get("market_order_no") or "").strip()
        if no:
            grouped.setdefault(slug, []).append(no)
    return grouped, skipped
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest tests/delivery/test_market_enrich.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: 커밋**

```bash
git add "프로그램/_시스템/lemouton/delivery/market_enrich.py" "프로그램/_시스템/tests/delivery/test_market_enrich.py"
git commit -m "feat(delivery): 마켓 슬러그 판별 + 조회대상 그룹핑(미지원 마켓 스킵)"
```

---

## Task 3: enrich — order_export 조회 + 매칭 + MangoOrder 갱신

**Files:**
- Modify: `프로그램/_시스템/lemouton/delivery/market_enrich.py`
- Test: `프로그램/_시스템/tests/delivery/test_market_enrich.py`

**규칙**: 매칭키 = 오픈마켓주문번호. 매칭됨 → market_api_status(주문상태)·market_api_invoice(송장입력, '송장미입력'은 빈값)·checked_at, error=None. 미매칭 → error="마켓에서 못 찾음". 조회 실패 마켓 → 그 마켓 주문 전부 error=사유.

- [ ] **Step 1: 실패 테스트 추가**

`tests/delivery/test_market_enrich.py` 하단:

```python
from sqlalchemy import create_engine
from sqlalchemy.orm import Session
from shared.db import Base
import lemouton.delivery.models as M
from lemouton.delivery import market_enrich as me


@pytest.fixture
def db():
    eng = create_engine("sqlite://"); Base.metadata.create_all(eng)
    s = Session(eng); yield s; s.close()


def _seed(db, uid, market, no):
    db.add(M.MangoOrder(mango_uid=uid, market_name=market, market_order_no=no,
                        mango_status="해외현지배송중")); db.commit()


def test_enrich_matches_and_caches(db, monkeypatch):
    _seed(db, "1", "쿠팡", "A100")
    _seed(db, "2", "롯데ON", "B200")
    _seed(db, "3", "무신사", "C300")   # 미지원 → 확인불가

    def fake_rows(markets, **kw):
        return [
            {"판매처": "쿠팡", "오픈마켓주문번호": "A100", "주문상태": "배송중", "송장입력": "INV-A"},
            {"판매처": "롯데온", "오픈마켓주문번호": "B200", "주문상태": "배송준비중", "송장입력": "송장미입력"},
        ]
    monkeypatch.setattr(me._oe, "combined_order_rows", fake_rows)

    res = me.enrich_from_market_api(db, ["1", "2", "3"])
    o1 = db.query(M.MangoOrder).filter_by(mango_uid="1").one()
    o2 = db.query(M.MangoOrder).filter_by(mango_uid="2").one()
    o3 = db.query(M.MangoOrder).filter_by(mango_uid="3").one()
    assert o1.market_api_status == "배송중" and o1.market_api_invoice == "INV-A" and not o1.market_check_error
    assert o2.market_api_status == "배송준비중" and o2.market_api_invoice == ""   # 송장미입력→빈값
    assert o3.market_check_error and "지원" in o3.market_check_error            # 미지원 마켓
    assert res["checked"] == 2


def test_enrich_unmatched_and_fetch_fail(db, monkeypatch):
    _seed(db, "10", "쿠팡", "NOEXIST")      # 마켓 응답에 없음
    def fake_rows(markets, **kw): return []   # 매칭 0
    monkeypatch.setattr(me._oe, "combined_order_rows", fake_rows)
    me.enrich_from_market_api(db, ["10"])
    o = db.query(M.MangoOrder).filter_by(mango_uid="10").one()
    assert o.market_check_error and "못 찾" in o.market_check_error
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tests/delivery/test_market_enrich.py -k enrich -v`
Expected: FAIL — `AttributeError: ... has no attribute 'enrich_from_market_api'`

- [ ] **Step 3: 구현**

`market_enrich.py` 에 추가:

```python
from lemouton.delivery.models import MangoOrder


def _now():
    return datetime.now(timezone.utc)


def enrich_from_market_api(session, uploaded_uids, warnings=None) -> dict:
    """업로드된 주문을 마켓 API 실주문으로 보강. 반환 {checked, unmatched, skipped}."""
    if warnings is None:
        warnings = []
    orders = (session.query(MangoOrder)
              .filter(MangoOrder.mango_uid.in_(list(uploaded_uids))).all())
    rows = [{"mango_uid": o.mango_uid, "market_name": o.market_name,
             "market_order_no": o.market_order_no} for o in orders]
    grouped, skipped_uids = group_by_market(rows)

    # 미지원 마켓 주문 → 확인불가
    for o in orders:
        if o.mango_uid in skipped_uids:
            o.market_check_error = "마켓 API 미지원(주문 조회 불가)"
            o.market_checked_at = _now()

    # 마켓별 실주문 조회 → 오픈마켓주문번호 인덱스
    index = {}
    if grouped:
        try:
            fetched = _oe.combined_order_rows(list(grouped.keys()), use_cache=True,
                                              warnings=warnings)
        except Exception as e:   # noqa: BLE001 — 조회 전체 실패도 확인불가로 표면화
            fetched = []
            warnings.append(f"[배송검사] 마켓 조회 실패: {type(e).__name__}")
        for fr in fetched:
            no = str(fr.get("오픈마켓주문번호") or "").strip()
            if no:
                index[no] = fr

    checked = unmatched = 0
    for o in orders:
        if o.mango_uid in skipped_uids:
            continue
        no = (o.market_order_no or "").strip()
        fr = index.get(no)
        if fr is None:
            o.market_check_error = "마켓에서 못 찾음(조회 실패 또는 기간 밖)"
            o.market_checked_at = _now()
            unmatched += 1
            continue
        inv = str(fr.get("송장입력") or "").strip()
        if inv == "송장미입력":
            inv = ""
        o.market_api_status = str(fr.get("주문상태") or "").strip() or None
        o.market_api_invoice = inv
        o.market_shipped_at = str(fr.get("발송처리일") or "").strip() or None
        o.market_check_error = None
        o.market_checked_at = _now()
        checked += 1

    session.commit()
    return {"checked": checked, "unmatched": unmatched, "skipped": len(skipped_uids),
            "warnings": warnings}
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest tests/delivery/test_market_enrich.py -v`
Expected: PASS (4 passed)

- [ ] **Step 5: 커밋**

```bash
git add "프로그램/_시스템/lemouton/delivery/market_enrich.py" "프로그램/_시스템/tests/delivery/test_market_enrich.py"
git commit -m "feat(delivery): enrich — 마켓 실주문 조회·매칭·캐시(미매칭/실패=확인불가)"
```

---

## Task 4: 검사 v2 (이중송장 위험 · 배송흐름 정체)

**Files:**
- Modify: `프로그램/_시스템/lemouton/delivery/service.py`
- Test: `프로그램/_시스템/tests/delivery/test_delivery_service.py`

**규칙**:
- **이중송장 위험**: `mango_status == "해외현지배송중"`(재출력 대상) AND (`market_api_invoice` 있음 OR `market_api_status ∈ _SHIPPED_STATES`). `market_check_error` 있으면 제외(확인불가).
- **배송흐름 정체**: 송장 있음(market_api_invoice or 엑셀 invoice_no) AND `market_api_status ∉ _SHIPPED_STATES` AND 기준시각+24h < now. 기준시각 = market_shipped_at(있으면) 아니면 invoice_history[0].at. `market_check_error` 있으면 제외.

- [ ] **Step 1: 실패 테스트 추가**

`tests/delivery/test_delivery_service.py` 하단:

```python
from datetime import datetime, timedelta, timezone

def _mk(db, uid, **kw):
    o = M.MangoOrder(mango_uid=uid, invoice_history=[]); 
    for k, v in kw.items(): setattr(o, k, v)
    db.add(o); db.commit(); return o


def test_find_double_invoice_risk(db):
    # 해외현지배송중인데 마켓엔 이미 송장 → 위험
    _mk(db, "d1", mango_status="해외현지배송중", market_api_invoice="INV", market_check_error=None)
    # 해외현지배송중 + 마켓 배송중(송장 없어도 shipped) → 위험
    _mk(db, "d2", mango_status="해외현지배송중", market_api_status="배송중", market_check_error=None)
    # 해외현지배송중인데 마켓 송장 없음 → 정상
    _mk(db, "d3", mango_status="해외현지배송중", market_api_invoice="", market_api_status="배송준비중", market_check_error=None)
    # 확인불가 → 제외
    _mk(db, "d4", mango_status="해외현지배송중", market_api_invoice="INV", market_check_error="확인불가")
    risk = svc.find_double_invoice_risk(db)
    assert {o.mango_uid for o in risk} == {"d1", "d2"}


def test_find_flow_stalled(db):
    old = (datetime.now(timezone.utc) - timedelta(hours=30)).isoformat()
    recent = (datetime.now(timezone.utc) - timedelta(hours=3)).isoformat()
    # 송장 + 배송준비중 + 24h 경과 → 정체
    _mk(db, "s1", market_api_invoice="INV", market_api_status="배송준비중",
        invoice_history=[{"invoice":"INV","at":old}], market_check_error=None)
    # 배송중 → 정상(흐름 시작됨)
    _mk(db, "s2", market_api_invoice="INV", market_api_status="배송중",
        invoice_history=[{"invoice":"INV","at":old}], market_check_error=None)
    # 24h 미경과 → 제외
    _mk(db, "s3", market_api_invoice="INV", market_api_status="배송준비중",
        invoice_history=[{"invoice":"INV","at":recent}], market_check_error=None)
    # 확인불가 → 제외
    _mk(db, "s4", market_api_invoice="INV", market_api_status="배송준비중",
        invoice_history=[{"invoice":"INV","at":old}], market_check_error="확인불가")
    stalled = svc.find_flow_stalled(db)
    assert {o.mango_uid for o in stalled} == {"s1"}
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tests/delivery/test_delivery_service.py -k "double_invoice or flow_stalled" -v`
Expected: FAIL — `AttributeError: ... has no attribute 'find_double_invoice_risk'`

- [ ] **Step 3: 구현**

`service.py` 에 추가:

```python
from datetime import datetime, timezone
from lemouton.markets.order_export import _SHIPPED_STATES


def find_double_invoice_risk(session):
    """더망고는 아직 해외현지배송중(재출력 대상)인데 마켓엔 이미 송장/배송중 → 이중송장 위험."""
    out = []
    for o in (session.query(MangoOrder)
              .filter(MangoOrder.mango_status == "해외현지배송중").all()):
        if o.market_check_error:            # 확인불가 제외
            continue
        if (o.market_api_invoice or "") or (o.market_api_status in _SHIPPED_STATES):
            out.append(o)
    return out


def _stall_base_time(o):
    """정체 24h 기준시각 — 마켓 발송처리일 우선, 없으면 송장 첫 관측시각."""
    for cand in (o.market_shipped_at, (o.invoice_history or [{}])[0].get("at") if o.invoice_history else None):
        if cand:
            try:
                return datetime.fromisoformat(cand)
            except ValueError:
                continue
    return None


def find_flow_stalled(session, now=None):
    """송장 있음 + 마켓상태 배송중 미만 + 24h 경과 → 배송흐름 정체."""
    now = now or datetime.now(timezone.utc)
    out = []
    for o in session.query(MangoOrder).all():
        if o.market_check_error:
            continue
        has_inv = bool((o.market_api_invoice or "") or (o.invoice_no or ""))
        if not has_inv:
            continue
        if o.market_api_status in _SHIPPED_STATES:   # 이미 흐름 시작 → 정상
            continue
        base = _stall_base_time(o)
        if base is None:
            continue
        if base.tzinfo is None:
            base = base.replace(tzinfo=timezone.utc)
        if (now - base).total_seconds() > 24 * 3600:
            out.append(o)
    return out
```

> 주: `market_api_status` 가 None(미조회)인데 확인불가 아닌 경우는 정체 판정 안 함(정보 없음). 위 로직은 `None ∉ _SHIPPED_STATES` 라 통과되나, `market_check_error` 가 None이고 status가 None인 건 enrich 미실행 상태 → 실제로는 enrich 후 error 채워짐. 안전을 위해 status None 이면 skip 추가:

`find_flow_stalled` 의 `if o.market_api_status in _SHIPPED_STATES` 앞에:
```python
        if not o.market_api_status:   # 마켓 상태 미확인 → 정체 판정 보류
            continue
```

- [ ] **Step 4: 통과 확인**

Run: `python -m pytest tests/delivery/test_delivery_service.py -v`
Expected: 전부 PASS

- [ ] **Step 5: 커밋**

```bash
git add "프로그램/_시스템/lemouton/delivery/service.py" "프로그램/_시스템/tests/delivery/test_delivery_service.py"
git commit -m "feat(delivery): 검사 v2 — 이중송장 위험·배송흐름 정체(마켓 실데이터·24h·확인불가 제외)"
```

---

## Task 5: 라우트 — 업로드 시 enrich 자동 호출 + data 응답 v2

**Files:**
- Modify: `프로그램/_시스템/webapp/routes/orders.py`

- [ ] **Step 1: inspect_upload 에 enrich 연결**

`orders.py` 의 `inspect_upload` 안, `upsert_orders` 직후에 추가:

```python
        res = _dsvc.upsert_orders(s, rows, bulk_method=bulk)
        # 업로드 즉시 마켓 API 조회(오픈마켓주문번호 매칭 → 실상태·실송장 캐시)
        from lemouton.delivery import market_enrich as _me
        uids = [r["mango_uid"] for r in rows]
        warn = []
        try:
            enr = _me.enrich_from_market_api(s, uids, warnings=warn)
        except Exception as e:   # noqa: BLE001 — enrich 실패해도 업로드는 성공 처리
            enr = {"checked": 0}; warn.append(f"마켓 조회 실패: {type(e).__name__}")
        return jsonify(ok=True, inserted=res['inserted'], updated=res['updated'],
                       parsed=len(rows), market_checked=enr.get('checked', 0), warnings=warn)
```

- [ ] **Step 2: inspect_data 에 v2 검사 반영**

`inspect_data` 의 dup_uids/flow_uids 계산부를 v2로 교체:

```python
        dup_uids = {o.mango_uid for o in _dsvc.find_double_invoice_risk(s)}
        flow_uids = {o.mango_uid for o in _dsvc.find_flow_stalled(s)}
        unk_uids = {o.mango_uid for o in orders if o.market_check_error}
```

그리고 각 행 dict 에 `d['unknown'] = o.mango_uid in unk_uids` 추가, summary 에 `'unknown': len(unk_uids)` 추가. `_mango_to_dict` 에 `'market_status': o.market_api_status, 'why_error': o.market_check_error` 추가.

- [ ] **Step 3: 스모크 (목 없이 라우트 임포트만 확인)**

Run: `python -c "import webapp.routes.orders"`
Expected: 오류 없음(임포트 성공).

- [ ] **Step 4: 커밋**

```bash
git add "프로그램/_시스템/webapp/routes/orders.py"
git commit -m "feat(delivery): 업로드 시 마켓 enrich 자동호출 + data 응답 v2(이중송장·정체·확인불가)"
```

---

# PHASE 2 · UI 통합 (주문 내역 흡수 + 탭 제거)

> 확정 시안: 「모음전 시안_배송검사 통합 최종 v1.html」. 부품 = 더망고카드 1-A·3택 2-A·사이드바 막대 3-E·배송방식 토글칩 4-C·검사 배지 5-A. 기존 `.o7` 디자인 유지.

## Task 6: 주문 내역 list 블록에 배송검사 통합

**Files:**
- Modify: `프로그램/_시스템/webapp/templates/orders/index.html` (list 블록)

- [ ] **Step 1: 더망고 대조 카드 삽입** — list 블록의 `<main class="main">` 최상단(`<div class="kpis">` 앞)에 더망고 업로드 카드(드롭존 + 배송방식 3택 라디오 + 「구분자 매핑」 버튼 + 업로드) 추가. 마크업·CSS는 시안 최종본의 `.mango-card`/`.dz`/`.rcs`/`.rc` 를 `.o7` 스코프에 맞춰 이식(색 변수 `--blue/--amber/--kkad` 사용).

- [ ] **Step 2: 사이드바 검사 막대 요약** — `.side` 의 「검색」 위에 `.side-bars`(이중송장·정체·확인불가·정상 막대) 추가. 각 막대 클릭 → 그 검사 종류만 필터(JS `colFilter` 재활용 또는 별도 checkFilter).

- [ ] **Step 3: 표에 배송방식·배송검사 열** — `PANEL_COLS` 에 `{c:'배송방식',a:'ctr'}`, `{c:'배송검사',a:'ctr'}` 추가. `cellHTML` 에 분기 추가: 배송방식=토글칩(`.chip` 까대기↔직배, 클릭 시 `/orders/inspect/method` POST), 배송검사=이모지 배지(🔴이중송장/🟡정체/⚪확인불가/🟢정상). 값은 아래 Step 5의 병합 데이터에서.

- [ ] **Step 4: 「검사통과분 송장 전송」** — 기존 송장 툴바(ibar)의 전송 버튼 라벨을 「✓ 검사통과분 송장 전송」으로, 클릭 시 이중송장·정체 flag 걸린 행은 선택에서 제외(경고 후 진행 확인).

- [ ] **Step 5: 더망고 데이터 병합** — 「더망고 대조 업로드」 성공 후 `/orders/inspect/data` 를 불러와 오픈마켓주문번호로 현재 주문 rows 에 배송방식·검사 flag 를 병합(JS map). 업로드 안 했으면 배송방식/검사 열은 「—」.

- [ ] **Step 6: inspect 블록 제거** — 기존 `{% elif tab == 'inspect' %}` … 블록 통째 삭제(라우트·서비스는 유지, 화면만 list 로 이전).

- [ ] **Step 7: 렌더 확인** — 앱 기동(`ENVIRONMENT=team-share-dev DISABLE_AUTH=1 FLASK_PORT=5091`) → `/orders?tab=list` 접속 → 더망고 카드·사이드바 막대·표 열이 뜨는지. 더망고 샘플 업로드 → 배송방식·검사 배지 병합 확인.

- [ ] **Step 8: 커밋**

```bash
git add "프로그램/_시스템/webapp/templates/orders/index.html"
git commit -m "feat(delivery): 주문내역에 배송검사 통합(더망고카드·막대요약·토글칩·검사배지) + inspect 블록 제거"
```

---

## Task 7: 배송검사 탭·메뉴 제거

**Files:**
- Modify: `프로그램/_시스템/webapp/routes/orders.py` (SUBTABS)
- Modify: `프로그램/_시스템/webapp/routes/api_sidebar.py`

- [ ] **Step 1: SUBTABS 에서 inspect 제거** — `orders.py` SUBTABS 리스트에서 `{'key': 'inspect', ...}` 줄 삭제. (라우트 `/orders/inspect/*` 는 API로 유지 — 화면 병합이 호출.)

- [ ] **Step 2: 기본 사이드바에서 i_inspect 제거 + 역마이그레이션**

`api_sidebar.py`:
- `_default_layout` 의 `{'id': 'i_inspect', ...}` 줄 삭제.
- `_ensure_inspect` 를 **제거 마이그레이션**으로 교체(`_remove_inspect`): 저장된 레이아웃에 `i_inspect` 가 있으면 제거하고 저장(idempotent). `_load` 의 `_ensure_inspect` 호출을 `_remove_inspect` 로 교체.

```python
def _remove_inspect(layout: dict) -> bool:
    changed = False
    for st in layout.get('stages') or []:
        items = st.get('items') or []
        new = [it for it in items if it.get('id') != 'i_inspect']
        if len(new) != len(items):
            st['items'] = new; changed = True
    return changed
```

- [ ] **Step 3: 확인** — 앱 기동 → 사이드바에 「배송검사」 항목이 사라지고, `/orders?tab=inspect` 는 list 로 폴백(SUBTABS 검증 `tab not in ... → 'list'`).

- [ ] **Step 4: 커밋**

```bash
git add "프로그램/_시스템/webapp/routes/orders.py" "프로그램/_시스템/webapp/routes/api_sidebar.py"
git commit -m "feat(delivery): 배송검사 별도 탭·사이드바 항목 제거(주문내역 통합 완료)"
```

---

## Task 8: 실브라우저 검증 + 배포

- [ ] **Step 1: 전체 테스트** — Run: `python -m pytest tests/delivery/ -v` → 전부 PASS.
- [ ] **Step 2: 로컬 실브라우저** — `/orders?tab=list` 에서 더망고 대조 업로드 → 배송방식 토글칩·검사 배지·사이드바 막대·「검사통과분 송장 전송」 동작. 배송검사 탭·메뉴 없음 확인.
- [ ] **Step 3: 배포** — main 머지 → AWS 배포 → mou-m.com/orders?tab=list 실서버 확인. 실계정으로 이중송장·정체·확인불가 육안 검증(11번가 실검증 포함, 안 되면 명시 실패).
- [ ] **Step 4: 최종 커밋(검증 반영분)**

```bash
git add -A "프로그램/_시스템"
git commit -m "fix(delivery): 배송검사 주문내역 통합 실브라우저 검증 반영"
```

---

## Self-Review 결과

- **스펙 커버리지**: 마켓 조회·매칭(Task2·3)·필드캐시(Task1)·검사 v2 이중송장/정체/확인불가(Task4)·24h 유예·배송중 경계·업로드 즉시 조회(Task5)·주문내역 통합 UI 5부품(Task6)·탭 흡수·제거(Task6·7)·확인불가 정직표시(Task3·4) 모두 태스크 존재. ✔
- **플레이스홀더**: 백엔드(Task1~5) 전부 실제 코드. UI(Task6)는 확정 시안 마크업 이식이라 스텝 단위로 명시(시안 파일 = 참조 원본). "TBD" 없음.
- **타입 일관성**: `market_enrich.market_slug/group_by_market/enrich_from_market_api`, `service.find_double_invoice_risk/find_flow_stalled`, MangoOrder 필드명(`market_api_status/invoice/shipped_at/checked_at/check_error`)이 라우트(Task5)·검사(Task4)에서 쓰는 이름과 일치. ✔
- **미결(구현 중 확정)**: (1) `발송처리일` 키를 order_export 가 마켓별로 실제 주는지 — 주면 market_shipped_at 채움, 없으면 invoice_history 폴백(Task3·4 이미 폴백). (2) 11번가 상태/매칭 실계정 검증(Task8). (3) UI 검사필터 JS는 기존 colFilter 재활용 여부 구현 시 확정. 셋 다 코드로 확인 가능.
