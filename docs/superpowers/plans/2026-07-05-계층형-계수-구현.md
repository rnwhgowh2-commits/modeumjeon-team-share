# 계층형 계수 구현 플랜

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 크롤 계수를 소싱처 > 브랜드 > 모음전 > URL 4단계로 걸 수 있게 한다 — 상위=전체 적용, 하위=우선, 아무 데도 없으면 ×1.

**Architecture:** 범위별 규칙 테이블 `CrawlWeightRule(scope_type, scope_key, weight)` 하나에 4단계 계수를 저장. `resolve_crawl_weight(session, sp)` 가 URL→모음전→브랜드→소싱처→기본1 순으로 해석(공유 URL은 최고). P2 큐가 `sp.crawl_weight` 대신 이 해석 함수를 호출.

**Tech Stack:** SQLAlchemy, pytest(in-memory SQLite, `tests/sources/conftest.py`의 `db` 픽스처), 기존 `normalize_url`(정본).

**대상 워크트리:** `C:\dev\모음전 프로젝트\_wt_auto_engine` (feature/automation-engine)
**스펙:** [`docs/superpowers/specs/2026-07-05-계층형-계수-소싱처브랜드모음전URL-design.md`](../specs/2026-07-05-계층형-계수-소싱처브랜드모음전URL-design.md)
**경로 표기:** `프로그램/_시스템/...` 생략.

**전제(확인됨):** `crawl_schedule.py` `overdue_seconds`/`due_products`(계수 인수), `SourceProduct(site,url,crawl_weight)`(`sources/models.py`), `normalize_url`(`sources/service.py:73`, lru_cache·멱등·tracking 제거), `BundleSourceUrl(model_code,url)`(`sourcing/models.py:122`), `Model(model_code,brand)`(`sourcing/models.py:39,47`). 테스트 `db` 픽스처는 `sourcing.models`·`sources.models` 등록됨.

---

## File Structure
- **Modify** `lemouton/sources/models.py` — `CrawlWeightRule` 모델
- **Modify** `lemouton/sources/crawl_schedule.py` — `set_crawl_weight_rule`·`resolve_crawl_weight`·`list_weight_rules`, `due_products` 배선
- **Modify** `webapp/routes/api.py` — GET/POST 규칙 라우트
- **Modify** `shared/db.py` — 신규 테이블 주석
- **Create** `tests/sources/test_crawl_weight_rules.py`

---

## Task 1: CrawlWeightRule 모델 + 규칙 설정/해제

**Files:** `lemouton/sources/models.py` · `shared/db.py` · `lemouton/sources/crawl_schedule.py` · `tests/sources/test_crawl_weight_rules.py`(신규)

- [ ] **Step 1: 실패 테스트**

`tests/sources/test_crawl_weight_rules.py`:

```python
import pytest
from lemouton.sources.models import CrawlWeightRule
from lemouton.sources.crawl_schedule import set_crawl_weight_rule


def test_set_and_clamp(db):
    r = set_crawl_weight_rule(db, "source", "musinsa", 3); db.flush()
    assert r == 3
    assert db.query(CrawlWeightRule).filter_by(scope_type="source", scope_key="musinsa").first().weight == 3
    set_crawl_weight_rule(db, "source", "musinsa", 9); db.flush()   # 상한
    assert db.query(CrawlWeightRule).filter_by(scope_type="source", scope_key="musinsa").first().weight == 5
    set_crawl_weight_rule(db, "source", "musinsa", 0); db.flush()   # 하한
    assert db.query(CrawlWeightRule).filter_by(scope_type="source", scope_key="musinsa").first().weight == 1


def test_none_deletes_rule_inherit(db):
    set_crawl_weight_rule(db, "brand", "나이키", 4); db.flush()
    set_crawl_weight_rule(db, "brand", "나이키", None); db.flush()   # 해제
    assert db.query(CrawlWeightRule).filter_by(scope_type="brand", scope_key="나이키").first() is None


def test_bad_scope_type_raises(db):
    with pytest.raises(ValueError):
        set_crawl_weight_rule(db, "지역", "x", 3)
```

- [ ] **Step 2: 실패 확인** `cd 프로그램/_시스템 && python -m pytest tests/sources/test_crawl_weight_rules.py -v` → ImportError

- [ ] **Step 3: 모델** — `lemouton/sources/models.py` 맨 아래(다른 모델 뒤)에 추가. (상단에 `Column,Integer,String` import 확인)

```python
class CrawlWeightRule(Base):
    """계수 규칙 — 소싱처/브랜드/모음전/URL 범위별. 없으면 상속·기본 ×1."""
    __tablename__ = "crawl_weight_rules"

    id = Column(Integer, primary_key=True, autoincrement=True)
    scope_type = Column(String(8), nullable=False)   # source|brand|model|url
    scope_key = Column(String(512), nullable=False)  # site / brand / model_code / 정규화 url
    weight = Column(Integer, nullable=False)          # 1~5

    __table_args__ = (
        UniqueConstraint("scope_type", "scope_key", name="uq_crawl_weight_rule"),
    )
```
(파일 상단에 `UniqueConstraint` import 없으면 `from sqlalchemy import UniqueConstraint` 추가.)

- [ ] **Step 4: db.py 주석** — `shared/db.py` 마이그레이션 리스트 근처: `# 2026-07-05: crawl_weight_rules 신규 테이블 → create_all 생성`.

- [ ] **Step 5: set 함수** — `lemouton/sources/crawl_schedule.py` 에 추가

```python
_SCOPE_TYPES = ("source", "brand", "model", "url")


def set_crawl_weight_rule(session, scope_type: str, scope_key: str, weight):
    """범위 계수 규칙 설정. weight None = 해제(삭제→상속). 1~5 클램프. 호출자 commit."""
    from lemouton.sources.models import CrawlWeightRule
    if scope_type not in _SCOPE_TYPES:
        raise ValueError(f"scope_type: {scope_type}")
    r = (session.query(CrawlWeightRule)
         .filter_by(scope_type=scope_type, scope_key=scope_key).first())
    if weight is None:
        if r is not None:
            session.delete(r)
        session.flush()
        return None
    w = max(1, min(5, int(weight)))
    if r is not None:
        r.weight = w
    else:
        session.add(CrawlWeightRule(scope_type=scope_type, scope_key=scope_key, weight=w))
    session.flush()
    return w
```

- [ ] **Step 6: 통과 확인** (3 passed)

- [ ] **Step 7: 커밋**
```bash
git add 프로그램/_시스템/lemouton/sources/models.py 프로그램/_시스템/shared/db.py 프로그램/_시스템/lemouton/sources/crawl_schedule.py 프로그램/_시스템/tests/sources/test_crawl_weight_rules.py
git commit -m "feat(sources): 계수 규칙 CrawlWeightRule + set_crawl_weight_rule(해제=삭제)"
```

---

## Task 2: resolve_crawl_weight (4단계 해석 + 공유URL 최고 + 정규화)

**Files:** `lemouton/sources/crawl_schedule.py` · `tests/sources/test_crawl_weight_rules.py`

- [ ] **Step 1: 실패 테스트 추가**

```python
from lemouton.sources.crawl_schedule import resolve_crawl_weight
from lemouton.sources.models import SourceProduct
from lemouton.sourcing.models import Model, BundleSourceUrl


def _sp(db, site, url):
    sp = SourceProduct(site=site, url=url); db.add(sp); db.flush(); return sp


def _bundle(db, code, url, brand="나이키"):
    db.add(Model(model_code=code, model_name_raw=code, brand=brand)); db.flush()
    db.add(BundleSourceUrl(model_code=code, source_key="musinsa", url=url, url_type="단품")); db.flush()


def test_default_is_1(db):
    sp = _sp(db, "musinsa", "https://m/1")
    assert resolve_crawl_weight(db, sp) == 1


def test_priority_url_beats_all(db):
    url = "https://www.musinsa.com/products/1"
    _bundle(db, "M1", url, brand="나이키")
    sp = _sp(db, "musinsa", url)
    set_crawl_weight_rule(db, "source", "musinsa", 2)
    set_crawl_weight_rule(db, "brand", "나이키", 3)
    set_crawl_weight_rule(db, "model", "M1", 4)
    set_crawl_weight_rule(db, "url", url, 5); db.flush()
    assert resolve_crawl_weight(db, sp) == 5


def test_falls_through_source(db):
    sp = _sp(db, "ssf", "https://s/9")
    set_crawl_weight_rule(db, "source", "ssf", 2); db.flush()
    assert resolve_crawl_weight(db, sp) == 2


def test_model_then_brand_then_source(db):
    url = "https://www.musinsa.com/products/7"
    _bundle(db, "M7", url, brand="아디다스")
    sp = _sp(db, "musinsa", url)
    set_crawl_weight_rule(db, "source", "musinsa", 2)
    set_crawl_weight_rule(db, "brand", "아디다스", 3); db.flush()
    assert resolve_crawl_weight(db, sp) == 3        # 브랜드 > 소싱처
    set_crawl_weight_rule(db, "model", "M7", 4); db.flush()
    assert resolve_crawl_weight(db, sp) == 4        # 모음전 > 브랜드


def test_shared_url_takes_highest_model(db):
    url = "https://www.musinsa.com/products/8"
    _bundle(db, "A", url, brand="나이키")
    _bundle(db, "B", url, brand="나이키")           # 같은 URL 두 모음전
    sp = _sp(db, "musinsa", url)
    set_crawl_weight_rule(db, "model", "A", 2)
    set_crawl_weight_rule(db, "model", "B", 5); db.flush()
    assert resolve_crawl_weight(db, sp) == 5         # 최고


def test_normalized_match(db):
    # 등록 URL에 tracking 붙어도 매칭
    clean = "https://www.musinsa.com/products/9"
    _bundle(db, "N", clean + "?utm_source=x", brand="나이키")
    sp = _sp(db, "musinsa", clean)
    set_crawl_weight_rule(db, "model", "N", 4); db.flush()
    assert resolve_crawl_weight(db, sp) == 4
```

- [ ] **Step 2: 실패 확인** → ImportError `resolve_crawl_weight`

- [ ] **Step 3: 구현** — `crawl_schedule.py`

```python
def resolve_crawl_weight(session, source_product) -> int:
    """URL의 최종 계수: URL→모음전(최고)→브랜드(최고)→소싱처→기본1."""
    from lemouton.sources.models import CrawlWeightRule
    from lemouton.sources.service import normalize_url
    from lemouton.sourcing.models import BundleSourceUrl, Model

    def _rule(stype, skey):
        return (session.query(CrawlWeightRule)
                .filter_by(scope_type=stype, scope_key=skey).first())

    nurl = normalize_url(source_product.url)
    r = _rule("url", nurl)
    if r:
        return r.weight

    model_codes = {b.model_code for b in session.query(BundleSourceUrl).all()
                   if normalize_url(b.url) == nurl}
    if model_codes:
        mr = (session.query(CrawlWeightRule)
              .filter(CrawlWeightRule.scope_type == "model",
                      CrawlWeightRule.scope_key.in_(model_codes)).all())
        if mr:
            return max(x.weight for x in mr)
        brands = {m.brand for m in session.query(Model)
                  .filter(Model.model_code.in_(model_codes)).all() if m.brand}
        if brands:
            br = (session.query(CrawlWeightRule)
                  .filter(CrawlWeightRule.scope_type == "brand",
                          CrawlWeightRule.scope_key.in_(brands)).all())
            if br:
                return max(x.weight for x in br)

    sr = _rule("source", source_product.site)
    if sr:
        return sr.weight
    return 1
```

- [ ] **Step 4: 통과 확인** (초기 3 + 신규 6 = 9 passed)

- [ ] **Step 5: 커밋**
```bash
git add 프로그램/_시스템/lemouton/sources/crawl_schedule.py 프로그램/_시스템/tests/sources/test_crawl_weight_rules.py
git commit -m "feat(sources): resolve_crawl_weight 4단계 해석(공유URL 최고·정규화)"
```

---

## Task 3: P2 큐 배선 + 기존 crawl_weight 흡수

**Files:** `lemouton/sources/crawl_schedule.py` (`due_products`) · `tests/sources/test_crawl_weight_rules.py`

- [ ] **Step 1: 실패 테스트 추가** — 소싱처 규칙이 큐 선정에 반영

```python
from datetime import datetime, timedelta
from lemouton.sources.crawl_schedule import due_products

NOW = datetime(2026, 7, 5, 12, 0, 0)


def test_due_products_reflects_source_rule(db):
    base = 6 * 3600
    # 3시간 전 크롤. 소싱처 계수 규칙 ×2 → 유효간격 3h → 딱 due
    sp = SourceProduct(site="musinsa", url="https://m/due",
                       last_fetched_at=NOW - timedelta(hours=3), no_change_streak=0)
    db.add(sp); db.flush()
    # 규칙 없으면(계수1) 6h 미달이라 아직 아님
    assert sp.id not in [p.id for p in due_products(db, base_interval_seconds=base, now=NOW)]
    set_crawl_weight_rule(db, "source", "musinsa", 2); db.flush()
    assert sp.id in [p.id for p in due_products(db, base_interval_seconds=base, now=NOW)]
```

- [ ] **Step 2: 실패 확인** (규칙 걸어도 여전히 안 뽑힘 — due_products가 sp.crawl_weight 컬럼만 봄)

- [ ] **Step 3: 구현** — `due_products` 안에서 `p.crawl_weight` 를 `resolve_crawl_weight(session, p)` 로 교체. (현재 `overdue_seconds(now, p.last_fetched_at, base_interval_seconds, p.crawl_weight, p.no_change_streak)` → `... resolve_crawl_weight(session, p), p.no_change_streak)`)

- [ ] **Step 4: 통과 확인 + 회귀** `cd 프로그램/_시스템 && python -m pytest tests/sources -q` (기존 crawl_schedule 테스트 포함 전부 통과 — 기존은 규칙 없어 계수1 → sp.crawl_weight 기본1과 동일 결과라 안 깨짐)

- [ ] **Step 5: 커밋**
```bash
git add 프로그램/_시스템/lemouton/sources/crawl_schedule.py 프로그램/_시스템/tests/sources/test_crawl_weight_rules.py
git commit -m "feat(sources): 큐 선정이 계층 계수(resolve) 반영 — due_products 배선"
```

> 기존 `SourceProduct.crawl_weight` 컬럼: 이제 큐가 안 읽으므로 미사용. 값이 1이 아닌 URL이 있으면 URL 규칙으로 이전하는 일회성 마이그레이션은 **운영 데이터 확인 후 별도**(열린 항목). 컬럼 제거도 후속.

---

## Task 4: 브라우징 + 설정 API

**Files:** `lemouton/sources/crawl_schedule.py`(`list_weight_rules`) · `webapp/routes/api.py` · `tests/sourcing/test_crawl_weight_rule_endpoint.py`(신규)

- [ ] **Step 1: 실패 테스트** — 서비스 `list_weight_rules` + 라우트

`tests/sources/test_crawl_weight_rules.py` 에 추가:
```python
from lemouton.sources.crawl_schedule import list_weight_rules


def test_list_rules_grouped(db):
    set_crawl_weight_rule(db, "source", "musinsa", 2)
    set_crawl_weight_rule(db, "brand", "나이키", 3); db.flush()
    d = list_weight_rules(db)
    assert d["source"]["musinsa"] == 2 and d["brand"]["나이키"] == 3
```
`tests/sourcing/test_crawl_weight_rule_endpoint.py`(신규, client 픽스처 = `test_crawl_queue_endpoint.py` 패턴): `POST /api/crawl/weight-rule` {scope_type,scope_key,weight} → 200 {ok,weight}; weight 생략/None → 해제 200; 잘못된 scope_type → 400. `GET /api/crawl/weight-rules` → 200 규칙 dict.

- [ ] **Step 2: 실패 확인** → ImportError / 404

- [ ] **Step 3: 구현** — `crawl_schedule.py`
```python
def list_weight_rules(session) -> dict:
    """범위종류별 {scope_key: weight} (화면 트리가 유효계수 표시에 사용)."""
    from lemouton.sources.models import CrawlWeightRule
    out = {t: {} for t in _SCOPE_TYPES}
    for r in session.query(CrawlWeightRule).all():
        out.setdefault(r.scope_type, {})[r.scope_key] = r.weight
    return out
```
`webapp/routes/api.py`:
```python
@bp.get('/crawl/weight-rules')
def crawl_weight_rules():
    """[읽기] 범위별 계수 규칙(화면 트리)."""
    from lemouton.sources.crawl_schedule import list_weight_rules
    s = SessionLocal()
    try:
        return jsonify(list_weight_rules(s))
    finally:
        s.close()


@bp.post('/crawl/weight-rule')
def set_crawl_weight_rule_route():
    """계수 규칙 설정/해제. body {scope_type, scope_key, weight?(없으면 해제)}."""
    from lemouton.sources.crawl_schedule import set_crawl_weight_rule
    data = request.get_json(silent=True) or {}
    s = SessionLocal()
    try:
        w = set_crawl_weight_rule(s, data.get('scope_type'), data.get('scope_key'),
                                  data.get('weight'))
        s.commit()
        return jsonify({"ok": True, "weight": w})
    except ValueError as e:
        return jsonify({"ok": False, "error": str(e)}), 400
    finally:
        s.close()
```

- [ ] **Step 4: 통과 확인 + 회귀** `cd 프로그램/_시스템 && python -m pytest tests/sources tests/sourcing -q`

- [ ] **Step 5: 커밋**
```bash
git add 프로그램/_시스템/lemouton/sources/crawl_schedule.py 프로그램/_시스템/webapp/routes/api.py 프로그램/_시스템/tests/sources/test_crawl_weight_rules.py 프로그램/_시스템/tests/sourcing/test_crawl_weight_rule_endpoint.py
git commit -m "feat(api): 계수 규칙 조회/설정 GET weight-rules · POST weight-rule"
```

---

## 완료 기준
- [ ] 규칙 설정/해제·1~5 클램프·잘못된 scope 400
- [ ] resolve 4단계 우선순위 + 공유URL 최고 + 정규화 + 기본1
- [ ] due_products가 계층 계수 반영(회귀 없음)
- [ ] 조회/설정 API
- [ ] tests/sources·sourcing 회귀 통과

## 다음 (프론트 · design-mockup 필요)
- **소싱처 > 브랜드 > 모음전 > URL 드릴다운 화면** — 각 노드 유효계수 + 스텝퍼 + 직접/상속 표시 + 해제. 「소싱처-URL 2단계」 10종 시안을 계층 확장해 design-mockup 게이트로 확정 후 구현. 라이브 검증 필수(매트릭스/URL 화면).
- 기존 `crawl_weight` 컬럼 → URL 규칙 일회성 마이그레이션(운영 데이터 확인 후).
