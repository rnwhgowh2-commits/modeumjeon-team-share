# M2 카테고리 맵핑표(CategoryMap)+자동 제안 엔진+브랜드 지재권 제한표 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 소싱처 카테고리 경로 ↔ 6마켓 카테고리 맵핑표를 만들고(자동 제안 + 쓰일 때 확정 → 학습), 브랜드·지재권 제한표로 위험 마켓을 자동 제외한다. 이후 M3(빵부스러기 크롤)가 붙으면 "URL 크롤 → 업로드 카테고리 자동 결정"이 완성된다.

**Architecture:** M1의 `market_categories` 사전을 소비하는 층. 신규 테이블 3개(`source_categories`·`category_map`·`brand_restrictions`) + 순수함수 제안 엔진(이름 유사도, 쿠팡 추천 API 앵커) + 등록 흐름의 판정 게이트(confirmed=자동/suggested=확정 게이트/없음=검색). 제한표는 등록 마켓 선택 단계에서 잠금+사유.

**Tech Stack:** M1 과 동일 (Flask·SQLAlchemy create_all 규약·pytest cwd=`프로그램/_시스템`).

**스펙:** `docs/superpowers/specs/2026-07-22-더망고식-URL멀티마켓등록-카테고리맵핑-design.md` §B·§C·§D
**전제:** M1 배포 완료(6마켓 사전 적재). 워크트리 = `_wt_cat`(feature/market-category-harvest 계속 사용) 또는 origin/main 새 브랜치 — 착수 시점의 머지 상태에 따라 결정.
**게이트:** 쿠팡 추천 API = 마켓 API → consult-market-map 전수정독(coupang.categories.category-recommendation·check-auto-category-agreed). 발견 함정은 지도 되채움.

---

## 사전 지식 (M1 에서 검증된 사실 — 재조사 불필요)

1. DB 규약: 신규 테이블=models.py 클래스 추가(create_all 자동, app.py:76 이 import). **기존 테이블 신규 컬럼**=`shared/db.py::_apply_lightweight_migrations` 의 `migrations` 리스트에 `(table, column, dtype)` 추가(create_all 은 컬럼 추가 안 함).
2. `market_categories`: (market, code) 유니크, full_path 구분자 `>`, is_leaf, removed_at(None=현존). 검색은 `drafts.py::category_search` 가 LIKE(escape 처리) + 리프만.
3. 쿠팡 추천 API 래퍼가 이미 있음: `shared/platforms/coupang/categories.py::predict` (:25 부근 — **구현 전 시그니처 실코드 확인**). SUCCESS 외(FAILURE/INSUFFICIENT_INFORMATION)는 제안 생성 금지(추측 금지). WING 자동매칭 동의 확인 API 별도(`check-auto-category-agreed`).
4. 등록 흐름: `bulk_manual.js`(마켓 선택→카테고리 검색·번호선택→계정→POST register) / 서버 `drafts.py::register`→`service.py::register_draft`. `ProductDraftMarket.status` 에 'blocked' 값 이미 존재(models.py — Phase 2 계정선택 때 도입).
5. 설정 탭 카드 패턴: `_settings.html` — h2.card-title + 설명 p + `<div id="xx-root">` + IIFE fetch/render. `.st-tbl`·`.st-warn` 클래스 재사용, innerHTML 은 esc() 필수.
6. 테스트: `tests/registration/test_<기능>.py`, 한글 함수명, client fixture=`test_settings_tab.py:9-15` 패턴, 시드 자동정리 fixture 필수(공유 Supabase — 실존 마켓/실존 소싱처 이름으로 "비어있음" 가정 금지). pytest 인자 없이 금지. **git stash 금지.**
7. ESM 등록 카테고리 = `sd코드/site코드` 쌍. site-cats 목록엔 sd 없음(실측) → 이 플랜 Task 9 에서 서버측 실측으로 채움 전략 확정.

## 파일 구조

```
lemouton/registration/models.py              # (수정) SourceCategory·CategoryMapRow·BrandRestriction 추가
shared/db.py                                 # (수정) product_drafts 신규 컬럼 2개 migrations 등록
lemouton/registration/category_suggest.py    # (신규) 이름 유사도 후보 엔진(순수함수) + 제안 생성 오케스트레이션
lemouton/registration/brand_restrict.py      # (신규) 브랜드 정규화·제한 판정(순수함수)
webapp/routes/bulk/category_map.py           # (신규) 맵핑표 조회/확정/제안생성/제한표 CRUD 라우트
webapp/routes/bulk/__init__.py               # (수정) import 1줄
webapp/routes/bulk/drafts.py                 # (수정) register 에 제한표 적용 + 맵핑 판정 API
webapp/static/bulk_manual.js                 # (수정) 등록 흐름 — 맵핑 자동/확정 게이트/제한 잠금
webapp/templates/bulk/partials/_settings.html# (수정) 「브랜드 제한」 카드 + 「카테고리 맵핑」 현황 카드
tests/registration/test_category_suggest.py  # (신규)
tests/registration/test_brand_restrict.py    # (신규)
tests/registration/test_category_map_routes.py # (신규)
```

---

### Task 1: 테이블 3개 + ProductDraft 컬럼 2개

**Files:** Modify `lemouton/registration/models.py`, `shared/db.py` / Test `tests/registration/test_category_map_models.py`(신규)

- [ ] **Step 1: 실패하는 테스트**

```python
# tests/registration/test_category_map_models.py
"""M2 맵핑표·제한표 모델 테스트."""
import datetime

from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker

from shared.db import Base
from lemouton.registration.models import (
    SourceCategory, CategoryMapRow, BrandRestriction, ProductDraft)


def _mem():
    eng = create_engine('sqlite://')
    Base.metadata.create_all(eng)
    return sessionmaker(bind=eng)()


def test_소싱처카테고리_행을_넣고_읽는다():
    s = _mem()
    s.add(SourceCategory(source_id='musinsa', path='신발>스니커즈>여성운동화',
                         leaf_name='여성운동화', depth=3,
                         first_seen_at=datetime.datetime(2026, 7, 23)))
    s.commit()
    assert s.query(SourceCategory).filter_by(source_id='musinsa').one().product_count == 0


def test_맵핑행은_소싱처경로x마켓_유니크이고_기본상태는_suggested():
    s = _mem()
    row = CategoryMapRow(source_id='musinsa', source_path='신발>스니커즈>여성운동화',
                        market='smartstore', market_cat_code='50000167',
                        market_cat_path='패션잡화>여성신발>운동화', method='name_sim',
                        confidence=0.9)
    s.add(row); s.commit()
    got = s.query(CategoryMapRow).one()
    assert got.status == 'suggested' and got.confirmed_at is None


def test_브랜드제한_행을_넣고_읽는다():
    s = _mem()
    s.add(BrandRestriction(brand='나이키', market='coupang',
                           category_prefix='', reason='지재권 신고 이력', active=True))
    s.commit()
    assert s.query(BrandRestriction).filter_by(market='coupang').one().active is True


def test_드래프트에_소싱처카테고리_컬럼이_있다():
    s = _mem()
    d = ProductDraft(name='테스트', source_site='musinsa',
                     source_category_path='신발>스니커즈>여성운동화')
    s.add(d); s.commit()
    assert s.query(ProductDraft).one().source_category_path.endswith('여성운동화')
```

- [ ] **Step 2: 실패 확인** — Run: `python -m pytest tests/registration/test_category_map_models.py -q` / Expected: ImportError

- [ ] **Step 3: 모델 구현** (models.py 끝에 추가)

```python
class SourceCategory(Base):
    """소싱처 카테고리 사전 — 빵부스러기 축적(M3)과 트리 전수조사가 채운다 (스펙 §B)."""
    __tablename__ = 'source_categories'

    id = Column(Integer, primary_key=True)
    source_id = Column(String(40), nullable=False, index=True)   # source_registry 의 id (musinsa 등)
    path = Column(String(500), nullable=False)                   # '신발>스니커즈>여성운동화'
    leaf_name = Column(String(200), nullable=False)
    depth = Column(Integer, nullable=False, default=1)
    product_count = Column(Integer, nullable=False, default=0)   # 이 경로로 크롤된 상품 수(M3 증가)
    first_seen_at = Column(DateTime, nullable=False)
    last_seen_at = Column(DateTime)

    __table_args__ = (
        UniqueConstraint('source_id', 'path', name='uq_source_categories_source_path'),
    )


class CategoryMapRow(Base):
    """소싱처 카테고리 → 마켓 카테고리 맵핑 (스펙 §C — 하이브리드: 제안/확정/재확정).

    자동 확정 금지 — confirmed 는 사장님 클릭으로만 승격된다.
    """
    __tablename__ = 'category_map'

    id = Column(Integer, primary_key=True)
    source_id = Column(String(40), nullable=False)
    source_path = Column(String(500), nullable=False)
    market = Column(String(20), nullable=False)
    market_cat_code = Column(String(80), nullable=False)   # ESM 은 'sd코드/site코드' 조합 저장
    market_cat_path = Column(String(500))                  # 표시용
    status = Column(String(12), nullable=False, default='suggested')  # suggested|confirmed|re_confirm
    method = Column(String(20))                            # coupang_reco|name_sim|manual
    confidence = Column(Float)                             # 제안 근거 점수(0~1). 수기는 None
    candidates_json = Column(Text)                         # 상위 3 후보 [{code,path,score}] — 확정 게이트 표시용
    confirmed_at = Column(DateTime)
    updated_at = Column(DateTime)

    __table_args__ = (
        UniqueConstraint('source_id', 'source_path', 'market',
                         name='uq_category_map_source_market'),
    )


class BrandRestriction(Base):
    """브랜드·지재권 제한표 (스펙 §D) — 걸린 마켓만 자동 제외, 사유 표시. 사장님이 직접 관리."""
    __tablename__ = 'brand_restrictions'

    id = Column(Integer, primary_key=True)
    brand = Column(String(120), nullable=False)      # 비교는 brand_restrict.normalize() 로
    market = Column(String(20), nullable=False)      # 6마켓 중 하나 또는 '*'(전마켓)
    category_prefix = Column(String(500), default='')# 비우면 그 마켓 전체, 채우면 그 경로 이하만
    reason = Column(String(300), nullable=False)
    active = Column(Boolean, nullable=False, default=True)
    created_at = Column(DateTime)
```

`Float` import 확인(없으면 sqlalchemy import 줄에 추가). `ProductDraft` 클래스에 컬럼 2개 추가:

```python
    # M2: 소싱처 카테고리 (M3 크롤이 채움 — 수기 드래프트는 None)
    source_site = Column(String(40))              # source_registry id
    source_category_path = Column(String(500))    # '신발>스니커즈>여성운동화'
```

`shared/db.py` 의 `migrations` 리스트(기존 튜플들 뒤)에 추가 — **product_drafts 는 기존 테이블이라 create_all 이 컬럼을 안 만든다**:

```python
        ('product_drafts', 'source_site', 'VARCHAR(40)'),
        ('product_drafts', 'source_category_path', 'VARCHAR(500)'),
```

- [ ] **Step 4: 통과 확인** — Run: `python -m pytest tests/registration/test_category_map_models.py tests/registration/test_models.py -q` / Expected: PASS
- [ ] **Step 5: 커밋** — `feat(catmap): 맵핑표·소싱처사전·브랜드제한 테이블 + 드래프트 소싱처카테고리 컬럼`

---

### Task 2: 브랜드 정규화·제한 판정 (순수함수)

**Files:** Create `lemouton/registration/brand_restrict.py` / Test `tests/registration/test_brand_restrict.py`

- [ ] **Step 1: 실패하는 테스트**

```python
# tests/registration/test_brand_restrict.py
"""브랜드 제한 판정 — 정규화 비교·마켓/카테고리 범위."""
from lemouton.registration import brand_restrict as br


def _rule(brand='나이키', market='coupang', prefix='', active=True, reason='지재권'):
    return {'brand': brand, 'market': market, 'category_prefix': prefix,
            'active': active, 'reason': reason}


def test_브랜드는_대소문자_공백_무시하고_매칭한다():
    assert br.normalize('  Nike ') == br.normalize('NIKE') == br.normalize('nike')
    assert br.is_blocked([_rule(brand='NIKE')], brand='nike', market='coupang', cat_path='') is not None


def test_스타_마켓은_전마켓_차단이다():
    rules = [_rule(market='*')]
    for m in ('smartstore', 'coupang', 'eleven11'):
        assert br.is_blocked(rules, brand='나이키', market=m, cat_path='')


def test_카테고리_프리픽스가_있으면_그_경로_이하만_막는다():
    rules = [_rule(prefix='패션잡화>운동화')]
    assert br.is_blocked(rules, brand='나이키', market='coupang', cat_path='패션잡화>운동화>여성운동화')
    assert br.is_blocked(rules, brand='나이키', market='coupang', cat_path='가전>노트북') is None
    # 카테고리 미정(빈 경로)이면 보수적으로 막는다 — 지재권은 안전 우선
    assert br.is_blocked(rules, brand='나이키', market='coupang', cat_path='')


def test_비활성_규칙과_다른_브랜드는_안_막는다():
    assert br.is_blocked([_rule(active=False)], brand='나이키', market='coupang', cat_path='') is None
    assert br.is_blocked([_rule()], brand='아디다스', market='coupang', cat_path='') is None
```

- [ ] **Step 2: 실패 확인** — Run: `python -m pytest tests/registration/test_brand_restrict.py -q` / Expected: FAIL

- [ ] **Step 3: 구현**

```python
# lemouton/registration/brand_restrict.py
"""브랜드·지재권 제한 판정 (스펙 §D). 순수함수 — DB 는 라우트가 읽어 rules 로 넘긴다.

원칙: 걸리면 그 마켓만 제외(+사유). 카테고리 미정 상태에서 프리픽스 규칙이 있으면
보수적으로 막는다 — 지재권은 잘못 올리는 쪽이 잘못 막는 쪽보다 비싸다.
"""
from __future__ import annotations

import re


def normalize(brand):
    """대소문자·공백·중간점 차이를 무시하는 비교 키."""
    return re.sub(r'[\s·.\-_]+', '', str(brand or '')).lower()


def is_blocked(rules, brand, market, cat_path):
    """막히면 사유 문자열, 아니면 None. rules=[{brand,market,category_prefix,active,reason}]."""
    key = normalize(brand)
    if not key:
        return None
    for r in rules:
        if not r.get('active'):
            continue
        if normalize(r.get('brand')) != key:
            continue
        if r.get('market') not in ('*', market):
            continue
        prefix = (r.get('category_prefix') or '').strip()
        if prefix and cat_path and not str(cat_path).startswith(prefix):
            continue
        return f"{r.get('brand')} — {r.get('reason') or '지재권 제한'}"
    return None
```

- [ ] **Step 4: 통과 확인** / **Step 5: 커밋** — `feat(catmap): 브랜드 제한 판정 순수함수 — 정규화·전마켓·프리픽스·미정경로 보수 차단`

---

### Task 3: 이름 유사도 후보 엔진 (순수함수)

**Files:** Create `lemouton/registration/category_suggest.py` / Test `tests/registration/test_category_suggest.py`

- [ ] **Step 1: 실패하는 테스트**

```python
# tests/registration/test_category_suggest.py
"""이름 유사도 후보 — 정확일치 > 리프 부분일치 > 경로 토큰 겹침."""
from lemouton.registration import category_suggest as cs

_MARKET_LEAVES = [
    {'code': '1', 'name': '여성운동화', 'full_path': '패션잡화>여성신발>여성운동화'},
    {'code': '2', 'name': '운동화', 'full_path': '패션잡화>남성신발>운동화'},
    {'code': '3', 'name': '러닝화', 'full_path': '스포츠>운동화>러닝화'},
    {'code': '4', 'name': '노트북가방', 'full_path': '가방>노트북가방'},
]


def test_정확일치가_1등이고_부분일치가_그_다음이다():
    ranked = cs.rank_candidates('신발>스니커즈>여성운동화', _MARKET_LEAVES, top=3)
    assert [r['code'] for r in ranked][:2] == ['1', '2']
    assert ranked[0]['score'] > ranked[1]['score']


def test_리프명이_없으면_경로_토큰_겹침으로라도_찾는다():
    ranked = cs.rank_candidates('스포츠>운동화>트레일화', _MARKET_LEAVES, top=3)
    assert ranked and ranked[0]['code'] in ('3', '2')   # '운동화' 토큰 겹침


def test_아무것도_안_겹치면_빈_리스트다():
    assert cs.rank_candidates('식품>과일>사과', _MARKET_LEAVES, top=3) == []
```

- [ ] **Step 2: 실패 확인** — Run: `python -m pytest tests/registration/test_category_suggest.py -q`

- [ ] **Step 3: 구현**

```python
# lemouton/registration/category_suggest.py
"""맵핑 자동 제안 — 이름 유사도(순수함수) + 쿠팡 추천 앵커 오케스트레이션 (스펙 §C).

제안은 제안일 뿐이다: confidence 가 얼마든 자동 확정하지 않는다(정직성 원칙).
"""
from __future__ import annotations


def _tokens(path):
    out = set()
    for part in str(path or '').split('>'):
        part = part.strip()
        if part:
            out.add(part)
    return out


def rank_candidates(source_path, market_leaves, top=3):
    """source_path 의 리프명·경로 토큰으로 market_leaves 후보 상위 top 개.

    점수: 리프명 정확일치 1.0 / 리프명이 후보명에 포함(또는 역포함) 0.7
          / 경로 토큰 겹침 0.4×(겹친 토큰 비율). 0 은 제외.
    """
    parts = [p for p in str(source_path or '').split('>') if p.strip()]
    if not parts:
        return []
    leaf = parts[-1].strip()
    stoks = _tokens(source_path)
    ranked = []
    for cand in market_leaves:
        name = str(cand.get('name') or '').strip()
        score = 0.0
        if name == leaf:
            score = 1.0
        elif leaf and (leaf in name or name in leaf) and name:
            score = 0.7
        else:
            ctoks = _tokens(cand.get('full_path'))
            inter = stoks & ctoks
            if inter:
                score = 0.4 * (len(inter) / max(len(stoks), 1))
        if score > 0:
            ranked.append({'code': cand['code'], 'path': cand.get('full_path'),
                           'name': name, 'score': round(score, 3)})
    ranked.sort(key=lambda r: (-r['score'], r['path'] or ''))
    return ranked[:top]
```

- [ ] **Step 4: 통과 확인** / **Step 5: 커밋** — `feat(catmap): 이름 유사도 후보 엔진 — 정확>부분>토큰겹침, 0점 제외`

---

### Task 4: 제안 생성 오케스트레이션 (+쿠팡 추천 앵커)

**Files:** Modify `lemouton/registration/category_suggest.py` / Test `tests/registration/test_category_suggest.py` (추가)

- [ ] **Step 1: 실패하는 테스트** — DB 세션 넣고 source_categories × 6마켓 suggested 생성. 쿠팡은 predict 콜러블 주입(SUCCESS 만 채택).

```python
def test_제안생성은_confirmed_행을_건드리지_않고_suggested만_갱신한다():
    ...  # _mem 세션에 market_categories 몇 행 + source_categories 1행 시드
    # confirmed 행 미리 삽입 → generate_suggestions 후에도 status/코드 불변 확인
    # 쿠팡 predict 는 lambda name, brand: {'result': 'SUCCESS', 'predictedCategoryId': '777'} 주입


def test_쿠팡_predict_FAILURE면_쿠팡_제안을_만들지_않는다():
    ...
```

(구현자: 위 두 테스트는 시드 헬퍼 포함 완전한 코드로 작성 — `market_categories` 는 removed_at IS NULL·리프만 소비, 후보 상위 3 을 candidates_json 에 저장, 1등을 market_cat_code 로.)

- [ ] **Step 2: 실패 확인** → **Step 3: 구현** — `generate_suggestions(session, source_id, coupang_predict=None, now=None)`:
  1. `source_categories` 의 각 path 에 대해, 마켓별 `market_categories` 리프(현존만)로 `rank_candidates`.
  2. 쿠팡: `coupang_predict(name=리프명, brand=None)` 이 SUCCESS 면 predictedCategoryId 를 1등 후보로 삽입(method='coupang_reco', confidence 0.95) — 실패/미주입이면 이름 유사도만.
  3. `category_map` upsert: **status='confirmed' 행은 절대 불변.** suggested/re_confirm 행만 후보·코드 갱신. 후보 0개면 행을 만들지 않는다(없음=검색 유도).
  4. 반환 `{'sources': n, 'suggested': n, 'skipped_confirmed': n}`.
  실제 쿠팡 predict 래퍼는 `shared/platforms/coupang/categories.py::predict` — **시그니처를 실코드로 확인**해 라우트(Task 5)에서 주입.
- [ ] **Step 4: 통과** / **Step 5: 커밋** — `feat(catmap): 제안 생성 — 유사도+쿠팡추천 앵커, confirmed 불변, 후보0=행 없음`

---

### Task 5: 맵핑·제한 라우트

**Files:** Create `webapp/routes/bulk/category_map.py`, Modify `webapp/routes/bulk/__init__.py` / Test `tests/registration/test_category_map_routes.py`

- [ ] 라우트 5개 (TDD — 각각 실패 테스트 먼저, client fixture + 시드 자동정리):
  - `GET  /bulk/api/catmap/resolve?source=&path=&market=` → `{status: 'confirmed'|'suggested'|'none', code, path, candidates}` (등록 흐름의 판정 API)
  - `POST /bulk/api/catmap/confirm` body `{source, path, market, code}` → confirmed 승격(+market_cat_path 채움, confirmed_at). **없는 code(사전에 미존재·removed)면 400 거부**
  - `POST /bulk/api/catmap/suggest/<source_id>` → generate_suggestions 실행(동기 — 유사도는 인메모리 계산이라 빠름. 쿠팡 predict 는 source 당 리프 수만큼 호출이라 **호출 상한 200건/실행** 넘으면 쿠팡 앵커 생략하고 유사도만 + 응답에 명시)
  - `GET/POST/DELETE /bulk/api/brand-limits` → BrandRestriction CRUD (POST 는 upsert, 삭제는 id)
  - `GET  /bulk/api/catmap/status` → 소싱처별 suggested/confirmed/re_confirm 집계 (설정 카드용)
- [ ] `__init__.py` 에 `from . import category_map` 1줄.
- [ ] 커밋 — `feat(catmap): 맵핑 판정·확정·제안생성·브랜드제한 CRUD 라우트`

---

### Task 6: 등록 흐름에 판정·제한 배선

**Files:** Modify `webapp/routes/bulk/drafts.py`(register), `webapp/static/bulk_manual.js` / Test `tests/registration/test_category_map_routes.py` (추가)

- [ ] **서버**: `drafts.py::register` 에서 마켓 호출 전에:
  1. 드래프트 brand × market × (카테고리 경로 — 확정 시 market_cat_path) 로 `brand_restrict.is_blocked` → 막히면 **마켓 호출 없이** `ProductDraftMarket.status='blocked'`, `error_message=사유` 기록 후 `{'ok': False, 'blocked': True, 'reason': ...}` 반환(테스트: 마켓 클라이언트가 호출되지 않았음을 monkeypatch 로 증명).
- [ ] **프론트**(bulk_manual.js): 카테고리 단계 첫머리에 — 드래프트에 `source_site`+`source_category_path` 가 있으면 `catmap/resolve` 먼저:
  - `confirmed` → 그 코드 자동 사용(프롬프트 생략, "맵핑 자동: <경로>" 안내)
  - `suggested` → 후보 3개 번호 선택 → 선택 즉시 `catmap/confirm` 호출(학습) 후 등록 진행
  - `none` → 기존 검색 흐름, 선택 시 `catmap/confirm` 으로 저장(다음부터 자동)
  - 수기 드래프트(source 없음)는 기존 흐름 그대로.
- [ ] 커밋 — `feat(catmap): 등록 배선 — 제한 blocked 선차단·confirmed 자동·suggested 확정게이트·검색확정 학습`

---

### Task 7: 설정 탭 카드 2개

> 🎨 UI — design-mockup 게이트. 기존 카드 패턴 복제, 새 스타일 금지.

**Files:** Modify `_settings.html` / Test `test_category_map_routes.py` (마커 테스트)

- [ ] 「🚫 브랜드 제한」 카드: 목록 표(브랜드/마켓/카테고리범위/사유/삭제) + 추가 폼(브랜드·마켓 select(6+전마켓)·카테고리 prefix(선택)·사유). esc() 필수.
- [ ] 「🗺 카테고리 맵핑」 카드: 소싱처별 suggested/confirmed 집계 + 「제안 다시 생성」 버튼(POST suggest — 결과 요약 표시, 실패 사유 원문).
- [ ] 커밋 — `feat(catmap): 설정 탭 브랜드 제한·맵핑 현황 카드`

---

### Task 8: ESM sd짝 서버측 실측 + extra_code 전략 확정

> ⚠️ **제거 대기 임시 코드**: `webapp/routes/bulk/categories.py::esm_probe` (grep 마커 `TEMP-REMOVE-AFTER-M2T8`) — Step 2 완료 즉시 삭제. 인증 게이트 없이 ESM 실호출을 내므로 방치 금지.
> ⚠️ **Step 2 착수 시 필독**: `market_cat_code` 를 ESM 조합형(`sd/site`)으로 바꾸면 ①`catmap_confirm` 의 사전 존재 검증(400) ②`save_snapshot` 의 re_confirm 강등(코드 비교) 두 곳이 **조용히 안 맞게 된다** — 함께 고칠 것.

**Files:** Modify `webapp/routes/bulk/categories.py` (실측용 임시 확장) → 실측 후 확정 구현

- [ ] **Step 1(실측)**: `_run_harvest` 의 ESM fetch 로 리프 site-cat 1건 개별 조회(`GET /item/v1/categories/site-cats/{leafCode}`) 원문을 지도 되채움용으로 확보하는 **관리용 임시 라우트** `GET /bulk/api/categories/esm-probe?code=` 추가(원문 그대로 반환). 배포 후 실측:
  - ①개별 응답에 sd 코드 필드가 있으면 → 수집 시 리프별 개별 조회로 `extra_code` 채움(콜 수= 리프 수 — 백그라운드라 허용)
  - ②없으면 → `GET /item/v1/categories/sd-cats/{sd리프}/site-cats` 정방향 순회로 역인덱스 구축
- [ ] **Step 2(확정 구현)**: 실측 결과에 따라 `market_categories.extra_code` 채움 로직 + auction/gmarket 검색 응답에서 `code = extra_code + '/' + site_code` 완성형 반환(bulk_manual.js 의 보완 프롬프트 제거). 실측 결과·함정은 지도 idTraps 되채움 + 프로브 라우트 제거.
- [ ] 커밋 2개 — `feat(catmap): ESM sd짝 실측 프로브` / `feat(catmap): ESM extra_code 채움 — 검색이 등록 완성형 코드 반환`

---

### Task 10 (M3 착수 전 필수 — 2026-07-23 라이브 검증에서 드러남)

- [ ] **맵핑 행 ID 노출·삭제 동선** — `DELETE /bulk/api/catmap/<row_id>` 는 있는데 **화면에서 row_id 를 알 방법이 없다**(resolve·status 둘 다 id 미포함). 잘못 확정된 맵핑을 사장님이 스스로 지울 수 없다. → resolve 응답에 `map_id` 추가 + 설정 카드 「카테고리 맵핑」에 소싱처별 행 목록(경로·마켓·코드·삭제 버튼).
- [ ] **suggest 동기 라우트 타임아웃** — 유사도 O(경로×6마켓×리프) + 쿠팡 200콜 순차. `source_categories` 가 차는 M3 직후 첫 실행에서 Cloudflare 100초 상한에 걸릴 수 있다 → harvest 처럼 백그라운드+폴링으로 전환.
- [ ] **`source_id` 단일 원천 확정** — 모델 주석은 "source_registry 의 id(musinsa)" 인데 실제 `SourceRegistry.id` 는 정수·`name` 은 한글이고, `ProductDraft.pricing_source_id`(정수)와 표기가 갈린다. M3 가 무엇을 쓸지 못 박고 주석·컬럼을 맞출 것(안 맞으면 맵핑표가 영원히 조회 안 되는 값으로 채워진다).
- [ ] **수집 스테일 판정을 progress 기반으로** — 현재 `started_at + 3시간`. 쿠팡 실측 분당 ~180건이라 대형 트리는 3시간을 넘길 수 있어 살아있는 실행을 뺏길 수 있다 → `progress_at` 이 최근(예: 10분 이내)이면 살아있는 것으로 판정.

### Task 9: 라이브 검증 (배포 후)

- [ ] 머지·배포 → `/bulk/?tab=settings` 카드 2개 확인.
- [ ] 브랜드 제한 실검증: 규칙 1건 등록(예: 테스트브랜드×쿠팡) → 그 브랜드 드래프트 등록 시도 → 쿠팡만 blocked+사유, 다른 마켓 정상 진행 확인 → 규칙 삭제.
- [ ] 맵핑 시나리오: source_categories 에 표본 1행 수기 삽입(또는 M3 이후 크롤분) → suggest 실행 → resolve 가 suggested 반환 → confirm → resolve 가 confirmed 반환 확인.
- [ ] ESM 프로브 실측(Task 8 Step 1) 수행 → 전략 확정 → Task 8 Step 2 구현·재배포.
- [ ] `python -m pytest tests/registration/ -q` 기준선(기존실패 5건) 외 전부 그린.

---

## Self-Review

- 스펙 §C 커버: CategoryMap 테이블(T1)·자동제안 유사도+쿠팡앵커(T3·4)·등록순간 판정 3분기+학습(T6)·re_confirm 은 M1 재수집 diff 가 강등 주체 — **강등 배선(재수집 removed → 관련 맵핑 re_confirm)이 스펙에 있으므로 T5 의 status 라우트 구현 시 `save_snapshot` 의 removed 마킹에 훅 추가**: `category_harvest.save_snapshot` 에서 removed 처리 시 같은 세션으로 `category_map` 의 해당 (market, market_cat_code) 행 status='re_confirm' 갱신 + 테스트 1건(T5 에 포함).
- 스펙 §D 커버: 테이블·CRUD·등록 차단·설정 카드(T1·2·5·6·7).
- 스펙 §B 의 소싱처 "수집"(크롤 확장·트리 조사)은 M3 — 이 플랜은 source_categories 그릇과 소비 측만.
- 미확정 격리: 쿠팡 predict 시그니처(실코드 확인 지시)·ESM sd짝(T8 실측 후 구현)·유사도 세부(초기 규칙 명시, 형태소는 성능 보고 판단=스펙 그대로).
