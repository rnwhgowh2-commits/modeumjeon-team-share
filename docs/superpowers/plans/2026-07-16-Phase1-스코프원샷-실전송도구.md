# Phase 1 — 스코프 원샷 실전송 도구 구현 플랜

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans. Steps use checkbox (`- [ ]`).

**Goal:** 지정한 옵션(canonical_sku) 몇 개만, 명시적 확인 후 1회 실전송하고 마켓 응답을 정직히 보고하는 CLI 도구. 연속 스케줄러와 분리. 스스·쿠팡 통제 라이브 검증의 도구.

**Architecture:** 기존 오케스트레이터 `run_uploader`(price_guard·DLQ·detect_change·persist 내장)를 **스코프된 c_output**(지정 SKU만)과 함께 재사용. 실전송은 서버키 `MOUM_LIVE_UPLOAD` + 도구 자체 확인 플래그 이중 확인(연속모드 화면키 `autosend_mode`와 분리). 기본은 드라이런.

**Tech Stack:** Python, pytest. 재사용: `lemouton/uploader/orchestrator.py`(run_uploader·_extract_uploads), `uploader/runtime.py`(select_adapters·build_sku_by_option·live_upload_enabled), `scripts/verify_pipeline_dryrun.py`(build_a_output_from_stored·build_c_output).

**참조 스펙:** `docs/superpowers/specs/2026-07-16-4대마켓-가격재고-실전송-검증롤아웃-design.md` (Phase 1).

**작업 위치:** fresh `origin/main` 워크트리 `C:/dev/_wt_live_p1`, 브랜치 `feature/live-send-scoped-tool`.

---

## 파일 구조
- Create: `프로그램/_시스템/scripts/live_send_skus.py` — CLI 도구(안전 게이트 + 스코프 빌더 + 전송 + 보고).
- Modify: `프로그램/_시스템/scripts/verify_pipeline_dryrun.py` — `build_a_output_from_stored(session, only_skus=None)` 에 필터 파라미터 추가(기존 호출 무영향).
- Test: `프로그램/_시스템/tests/uploader/test_live_send_gate.py` — 안전 게이트 순수 로직.
- Test: `프로그램/_시스템/tests/uploader/test_live_send_scope.py` — SKU 스코프 필터.

---

## Task 1: fresh main 워크트리

- [ ] **Step 1: fetch + 워크트리 생성**
```bash
cd "C:/dev/모음전 프로젝트"
git fetch origin
git worktree add -b feature/live-send-scoped-tool "C:/dev/_wt_live_p1" origin/main
```
Expected: 워크트리 생성, `HEAD is now at <origin/main 최신>`.

- [ ] **Step 2: 확인**
```bash
cd "C:/dev/_wt_live_p1"
git log -1 --format="%h %s"
```
이후 모든 Task 는 이 워크트리에서.

---

## Task 2: 실전송 안전 게이트 (순수 로직, TDD)

**핵심:** 실제 전송(real 어댑터)은 세 조건 **모두** 참일 때만 — ①`--live` 요청 ②`--i-understand-live-send` 확인 ③서버키 `MOUM_LIVE_UPLOAD` ON. 하나라도 빠지면 드라이런 + 사유.

**Files:**
- Create: `프로그램/_시스템/scripts/live_send_skus.py` (게이트 함수만 먼저)
- Test: `프로그램/_시스템/tests/uploader/test_live_send_gate.py`

- [ ] **Step 1: 실패 테스트 작성** — `프로그램/_시스템/tests/uploader/test_live_send_gate.py`:
```python
# -*- coding: utf-8 -*-
"""실전송 안전 게이트 — real 어댑터 사용 조건(3중)."""
from scripts.live_send_skus import resolve_send_mode


def test_dryrun_when_not_requested():
    use_real, reason = resolve_send_mode(want_live=False, confirmed=False, server_key_on=False)
    assert use_real is False
    assert reason is None   # 드라이런은 정상, 사유 없음


def test_refuse_live_without_confirm():
    use_real, reason = resolve_send_mode(want_live=True, confirmed=False, server_key_on=True)
    assert use_real is False
    assert reason and "확인" in reason


def test_refuse_live_without_server_key():
    use_real, reason = resolve_send_mode(want_live=True, confirmed=True, server_key_on=False)
    assert use_real is False
    assert reason and "MOUM_LIVE_UPLOAD" in reason


def test_real_only_when_all_three():
    use_real, reason = resolve_send_mode(want_live=True, confirmed=True, server_key_on=True)
    assert use_real is True
    assert reason is None
```

- [ ] **Step 2: 실패 확인**
```bash
cd "C:/dev/_wt_live_p1/프로그램/_시스템"
python -m pytest tests/uploader/test_live_send_gate.py -v
```
Expected: FAIL — ModuleNotFoundError / ImportError (resolve_send_mode 없음).

- [ ] **Step 3: 게이트 구현** — `프로그램/_시스템/scripts/live_send_skus.py` 생성, 상단에:
```python
# -*- coding: utf-8 -*-
"""스코프 원샷 실전송 도구 — 지정 SKU만 1회 전송(연속 스케줄러와 분리).

실전송 조건(3중): --live + --i-understand-live-send + 서버키 MOUM_LIVE_UPLOAD ON.
기본 드라이런. price_guard·DLQ·정직한 성공판정은 run_uploader 재사용으로 보존.
"""
from __future__ import annotations


def resolve_send_mode(*, want_live: bool, confirmed: bool, server_key_on: bool):
    """(use_real: bool, refusal_reason: str|None). real 은 3조건 모두 참일 때만."""
    if not want_live:
        return False, None                      # 드라이런(정상)
    if not confirmed:
        return False, "실전송하려면 --i-understand-live-send 확인 플래그가 필요합니다(드라이런으로 실행)."
    if not server_key_on:
        return False, "서버키 MOUM_LIVE_UPLOAD 가 꺼져 있습니다. 배포 env 설정·재배포(사용자) 후 재시도(드라이런으로 실행)."
    return True, None
```

- [ ] **Step 4: 통과 확인** — Step 2 명령 재실행. Expected: PASS (4 passed).

- [ ] **Step 5: 커밋**
```bash
cd "C:/dev/_wt_live_p1"
git add "프로그램/_시스템/scripts/live_send_skus.py" "프로그램/_시스템/tests/uploader/test_live_send_gate.py"
git commit -m "feat(live-send): 스코프 원샷 실전송 안전 게이트(3중 조건)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: SKU 스코프 필터 (TDD)

**핵심:** 지정한 SKU 외에는 전송 후보에 **절대** 안 들어가야 한다. `build_a_output_from_stored` 에 `only_skus` 필터를 추가하고, 그 결과가 지정 SKU만 담는지 테스트.

**Files:**
- Modify: `프로그램/_시스템/scripts/verify_pipeline_dryrun.py`
- Test: `프로그램/_시스템/tests/uploader/test_live_send_scope.py`

- [ ] **Step 1: 실패 테스트 작성** — `프로그램/_시스템/tests/uploader/test_live_send_scope.py`:
```python
# -*- coding: utf-8 -*-
"""SKU 스코프 — only_skus 필터가 지정 SKU만 남기는지(가짜 세션으로 단위 검증)."""
from scripts.verify_pipeline_dryrun import filter_a_output


def test_filter_keeps_only_target_skus():
    a_output = {"SKU_A": {"x": 1}, "SKU_B": {"x": 2}, "SKU_C": {"x": 3}}
    out = filter_a_output(a_output, only_skus=["SKU_A", "SKU_C"])
    assert set(out.keys()) == {"SKU_A", "SKU_C"}


def test_filter_none_returns_all():
    a_output = {"SKU_A": {}, "SKU_B": {}}
    assert filter_a_output(a_output, only_skus=None) == a_output


def test_filter_empty_list_returns_empty():
    a_output = {"SKU_A": {}, "SKU_B": {}}
    assert filter_a_output(a_output, only_skus=[]) == {}


def test_filter_unknown_sku_ignored():
    a_output = {"SKU_A": {}}
    out = filter_a_output(a_output, only_skus=["SKU_A", "SKU_ZZZ"])
    assert set(out.keys()) == {"SKU_A"}
```

- [ ] **Step 2: 실패 확인**
```bash
cd "C:/dev/_wt_live_p1/프로그램/_시스템"
python -m pytest tests/uploader/test_live_send_scope.py -v
```
Expected: FAIL — ImportError (filter_a_output 없음).

- [ ] **Step 3: 필터 구현** — `프로그램/_시스템/scripts/verify_pipeline_dryrun.py` 에 순수 함수 추가(기존 코드 아래, import 근처):
```python
def filter_a_output(a_output: dict, only_skus=None) -> dict:
    """only_skus 가 주어지면 그 canonical_sku 만 남긴다. None 이면 전체."""
    if only_skus is None:
        return a_output
    keep = set(only_skus)
    return {sku: v for sku, v in a_output.items() if sku in keep}
```
그리고 `build_c_output(session, only_skus=None)` 시그니처에 `only_skus` 를 추가해, `a_output = build_a_output_from_stored(session)` 다음 줄에서 `a_output = filter_a_output(a_output, only_skus)` 로 필터를 적용하라(기존 호출부는 only_skus 기본 None 이라 무영향).

- [ ] **Step 4: 통과 확인** — Step 2 명령 재실행. Expected: PASS (4 passed).

- [ ] **Step 5: 회귀 확인** (verify 스크립트 순수함수 테스트 유지)
```bash
cd "C:/dev/_wt_live_p1/프로그램/_시스템"
python -m pytest tests/uploader/test_verify_dryrun_report.py tests/uploader/test_live_send_scope.py -q
```
Expected: 전부 PASS.

- [ ] **Step 6: 커밋**
```bash
cd "C:/dev/_wt_live_p1"
git add "프로그램/_시스템/scripts/verify_pipeline_dryrun.py" "프로그램/_시스템/tests/uploader/test_live_send_scope.py"
git commit -m "feat(live-send): a_output SKU 스코프 필터 + build_c_output only_skus

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: 도구 본체 (스코프 빌드 → run_uploader 재사용 → 보고)

**핵심:** `resolve_send_mode` + `build_c_output(session, only_skus)` + `run_uploader` 를 엮는다. 실전송 여부만 `select_adapters(live=use_real)` 로 갈린다. **run_uploader 를 반드시 재사용**(price_guard·DLQ·detect_change·persist 보존). 연속모드 토글 우회 위해 `automation=None` 전달.

run_uploader 정확한 시그니처(정본, orchestrator.py):
`run_uploader(session, c_output, *, sku_by_option, adapters, dlq_path, warnings_threshold=5, avg_price_change_pct=30.0, force=False, persist=False, pacer=None, automation=None)` → dict(uploaded/skipped/failed/filtered_out/preview/[held/hold_reason]).

**Files:**
- Modify: `프로그램/_시스템/scripts/live_send_skus.py` (main + argparse)

- [ ] **Step 1: main 구현** — `live_send_skus.py` 에 추가:
```python
import argparse
import os


def _server_key_on() -> bool:
    from lemouton.uploader.runtime import live_upload_enabled
    return live_upload_enabled()


def run(skus, *, want_live: bool, confirmed: bool, force: bool = False) -> dict:
    """스코프 원샷 전송. use_real 이면 실어댑터, 아니면 드라이런. 결과 dict 반환."""
    from shared.db import SessionLocal
    from lemouton.uploader.runtime import select_adapters, build_sku_by_option
    from lemouton.uploader.orchestrator import run_uploader
    from scripts.verify_pipeline_dryrun import build_c_output

    use_real, refusal = resolve_send_mode(
        want_live=want_live, confirmed=confirmed, server_key_on=_server_key_on())
    session = SessionLocal()
    try:
        c_output = build_c_output(session, only_skus=list(skus))
        sku_by_option = build_sku_by_option(session)
        adapters = select_adapters(live=use_real)   # use_real=False → 전부 DryRunAdapter
        dlq_path = os.path.join(os.path.dirname(__file__), "..", "data", "uploader_dlq.jsonl")
        result = run_uploader(
            session, c_output,
            sku_by_option=sku_by_option, adapters=adapters, dlq_path=dlq_path,
            force=force, persist=use_real, automation=None,   # automation=None → 연속모드 토글 우회
        )
    finally:
        session.close()
    return {"use_real": use_real, "refusal": refusal, "skus": list(skus), "result": result}


def main() -> None:
    ap = argparse.ArgumentParser(description="스코프 원샷 실전송(지정 SKU만).")
    ap.add_argument("--skus", required=True, help="쉼표구분 canonical_sku 목록")
    ap.add_argument("--live", action="store_true", help="실제 전송 시도(기본 드라이런)")
    ap.add_argument("--i-understand-live-send", dest="confirmed", action="store_true",
                    help="실전송 명시 확인(--live 와 함께여야 real)")
    ap.add_argument("--force", action="store_true", help="드라이런 보류(hold) 무시")
    args = ap.parse_args()
    skus = [s.strip() for s in args.skus.split(",") if s.strip()]

    out = run(skus, want_live=args.live, confirmed=args.confirmed, force=args.force)
    mode = "★실전송(LIVE)" if out["use_real"] else "드라이런(preview)"
    print(f"모드: {mode} · 대상 SKU {len(out['skus'])}개: {', '.join(out['skus'])}")
    if out["refusal"]:
        print(f"⚠️ 실전송 거부 → 드라이런으로 실행: {out['refusal']}")
    r = out["result"]
    if r.get("held"):
        print(f"⏸ 보류(hold): {r.get('hold_reason')} — 확인 후 --force 로 재시도")
    print(f"결과: 전송 {r.get('uploaded', 0)} · 스킵 {r.get('skipped', 0)} · 실패 {r.get('failed', 0)} · 토글제외 {r.get('filtered_out', 0)}")
    for mkt, pv in (r.get("preview") or {}).items():
        print(f"  · {mkt}: 건수 {pv['count']} (가격변동 {pv['price']}, 재고변동 {pv['stock']})")
    if r.get("failed", 0):
        print(f"⚠️ 실패 {r['failed']}건 — DLQ(data/uploader_dlq.jsonl) 확인. 거짓성공 아님(응답코드 판정).")


if __name__ == "__main__":
    main()
```
**주의(구현자 필독):** run_uploader 반환에 `held`/`hold_reason` 키는 보류 시에만 있다(정상 진행 시 없음). `.get()` 로 안전 접근(위 코드대로). run_uploader 실제 반환 키가 다르면 orchestrator.py 의 return 문들을 읽어 정확한 키명으로 맞춰라(정본=코드). select_adapters·build_sku_by_option·build_c_output·live_upload_enabled 시그니처도 실제와 다르면 실제에 맞춰라(추측 금지, 다르면 보고).

- [ ] **Step 2: 게이트+스코프 회귀** (Task 2·3 테스트가 여전히 통과)
```bash
cd "C:/dev/_wt_live_p1/프로그램/_시스템"
python -m pytest tests/uploader/test_live_send_gate.py tests/uploader/test_live_send_scope.py -q
```
Expected: PASS.

- [ ] **Step 3: 드라이런 스모크(실제 DB, --live 없이)**
```bash
cd "C:/dev/_wt_live_p1/프로그램/_시스템"
ENVIRONMENT=team-share-dev DISABLE_AUTH=1 python -m scripts.live_send_skus --skus TESTSKU1,TESTSKU2
```
Expected: "모드: 드라이런(preview)" 출력, 에러 없음. ★실어댑터·실크롤 호출 0(DryRunAdapter만). DB 비어 결과 0건이어도 정상(도구 배선 검증). 없는 SKU 라 0건이 정상.

- [ ] **Step 4: 실전송 거부 경로 스모크(서버키 OFF 상태에서 --live)**
```bash
cd "C:/dev/_wt_live_p1/프로그램/_시스템"
ENVIRONMENT=team-share-dev DISABLE_AUTH=1 python -m scripts.live_send_skus --skus X --live --i-understand-live-send
```
Expected: "⚠️ 실전송 거부 → 드라이런으로 실행: 서버키 MOUM_LIVE_UPLOAD ..." (서버키 OFF 라 real 거부, 안전측). 실제 마켓 호출 0.

- [ ] **Step 5: 커밋**
```bash
cd "C:/dev/_wt_live_p1"
git add "프로그램/_시스템/scripts/live_send_skus.py"
git commit -m "feat(live-send): 스코프 원샷 전송 본체(run_uploader 재사용·드라이런 기본)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: 사용 런북 + 머지 판단

- [ ] **Step 1: 런북 작성** — `docs/superpowers/plans/2026-07-16-Phase1-실전송-런북.md` 에 절차 기록:
  1. (제어) 드라이런으로 대상 SKU 값 확인: `python -m scripts.live_send_skus --skus <SKU>`
  2. (사용자) 서버키 켜기: 배포 env `MOUM_LIVE_UPLOAD=1` 설정 + 재배포(글로벌: 제가 못 켬).
  3. (제어+사용자 확인) 소량 실전송: `--skus <SKU> --live --i-understand-live-send`
  4. (사용자) 라이브 셀러 화면에서 값 대조 → 이상 없으면 SKU 늘려 확대.
  5. 끝나면 서버키 다시 OFF(재배포) — 연속 자동전송은 별도 결정 전까지 잠금.
- [ ] **Step 2: 회귀 전체** `python -m pytest tests/uploader/ -q` → 전부 PASS.
- [ ] **Step 3: main 머지 판단** — 도구는 드라이런 기본·서버키 없이는 실전송 불가라 회귀 위험 낮음. 사용자 승인 후 머지+배포. 워크트리 정리.

---

## 완료 기준 (Phase 1 도구)
- 지정 SKU만 대상으로 하고(스코프 필터 테스트), 3중 조건 없이는 실어댑터를 쓰지 않으며(게이트 테스트), 기본 드라이런.
- run_uploader 재사용으로 price_guard·DLQ·detect_change·정직한 실패보고 보존.
- 실제 라이브 소량 전송·셀러 화면 대조는 **런북에 따라 사용자와 함께**(서버키=사용자, 매 배치 확인). 도구 자체는 안전하게 미무장 상태로 머지.
