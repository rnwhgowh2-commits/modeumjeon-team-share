# 소싱처 크롤링 가이드 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 소싱처별 4단계(샘플 URL → 크롤 구조 → 매입가 로직 → 가격 검증) 진실원천 카드를 저장·표시·편집하는 신규 탭을 만들고, ④에서 신규 URL을 비동기 큐로 크롤해 가격을 검증한다.

**Architecture:** `SourceRegistry`에 `crawl_guide`(JSON 문자열, `Text`) 1컬럼 추가. 순수 검증/스켈레톤 로직은 `crawl_guide.py` 모듈로 분리(유닛 테스트 대상). Flask Blueprint `sourcing_guide`가 전체보기/상세 렌더 + JSON GET/PUT + ④ 검증 잡 등록(POST)·폴링(GET)을 제공. ④ 검증은 기존 `crawl_jobs` 큐에 `phase="verify"` 단건 URL 잡을 등록하고 로컬 PC 워커가 처리한 결과를 `crawl_guide.verification`에 병합.

**Tech Stack:** Flask + SQLAlchemy(Text 컬럼에 JSON 직렬화) + Jinja2 + 기존 `toss.css` `.cf-receipt` 재사용 + pytest. DB = 로컬 SQLite + Supabase Postgres(스키마 = `create_all()` + 신규 컬럼은 멱등 `ALTER TABLE`).

**Spec:** `docs/superpowers/specs/2026-06-06-소싱처-크롤링-가이드-design.md`
**시안 근거:** 바탕화면 `모음전 시안 v4`(4단계) · `v8`(③ A형) · `v5 ④가격 검증 3안`(시안3).

---

## 파일 구조 (생성/수정)

| 파일 | 책임 | 작업 |
|---|---|---|
| `프로그램\_시스템\lemouton\sourcing\crawl_guide.py` | 스켈레톤·검증·검증결과 병합 (순수 로직) | 생성 |
| `프로그램\_시스템\tests\sourcing\test_crawl_guide.py` | 위 모듈 유닛 테스트 | 생성 |
| `프로그램\_시스템\lemouton\sourcing\models_pricing.py` | `SourceRegistry.crawl_guide` 컬럼 | 수정 |
| `프로그램\_시스템\lemouton\sourcing\models.py` | `CrawlJob.verify_url` 컬럼 | 수정 |
| `프로그램\_시스템\shared\db.py` | 신규 컬럼 멱등 `ALTER TABLE` (init) | 수정 |
| `프로그램\_시스템\lemouton\sourcing\crawl_queue.py` | `enqueue_verify(url, ...)` + 결과 read helper | 수정 |
| `프로그램\_시스템\tests\sourcing\test_enqueue_verify.py` | enqueue_verify 테스트 | 생성 |
| `프로그램\_시스템\webapp\routes\sourcing_guide.py` | Blueprint: 렌더 + API | 생성 |
| `프로그램\_시스템\webapp\routes\__init__.py` | Blueprint 등록 | 수정 |
| `프로그램\_시스템\webapp\templates\sourcing_guide\overview.html` | 전체보기(보기1) | 생성 |
| `프로그램\_시스템\webapp\templates\sourcing_guide\detail.html` | 상세 4단계(보기2) | 생성 |
| `프로그램\_시스템\webapp\static\toss.css` | 가이드 전용 CSS(append) | 수정 |
| `프로그램\_시스템\webapp\templates\partials\sidebar.html` (또는 nav 정의처) | "크롤링 가이드" nav 항목 | 수정 |
| `docs\crawl-worker-system.md` | verify 잡 워커 계약 문서화 | 수정 |

---

## Task 1: crawl_guide 순수 로직 모듈 (스켈레톤 + 검증)

**Files:**
- Create: `프로그램\_시스템\lemouton\sourcing\crawl_guide.py`
- Test: `프로그램\_시스템\tests\sourcing\test_crawl_guide.py`

- [ ] **Step 1: Write the failing test**

```python
# 프로그램/_시스템/tests/sourcing/test_crawl_guide.py
import pytest
from lemouton.sourcing.crawl_guide import empty_skeleton, validate_guide

def test_empty_skeleton_shape():
    sk = empty_skeleton()
    assert sk["version"] == 2
    assert sk["sample_urls"] == []
    assert set(sk["fields"]) == {
        "thumbnail", "title", "price", "benefit", "option_stock", "detail_image"}
    assert sk["pricing"]["benefits"] == []
    assert sk["pricing"]["benefit_collection"] == "per_product"

def test_validate_accepts_minimal_valid():
    data = {
        "version": 2,
        "sample_urls": [{"url": "https://www.musinsa.com/products/1", "is_lead": True}],
        "fields": empty_skeleton()["fields"],
        "pricing": {
            "base_label": "표면 노출가",
            "benefit_collection": "per_product",
            "benefits": [
                {"name": "등급 할인", "method": "rate", "rule": "잔액 × 등급 %", "status": "always"}
            ],
            "note": "",
        },
    }
    out = validate_guide(data)
    assert out["pricing"]["benefits"][0]["name"] == "등급 할인"
    assert out["version"] == 2

def test_validate_rejects_bad_benefit_method():
    data = empty_skeleton()
    data["pricing"]["benefits"] = [
        {"name": "x", "method": "WRONG", "rule": "r", "status": "always"}]
    with pytest.raises(ValueError):
        validate_guide(data)

def test_validate_rejects_bad_status():
    data = empty_skeleton()
    data["pricing"]["benefits"] = [
        {"name": "x", "method": "rate", "rule": "r", "status": "WRONG"}]
    with pytest.raises(ValueError):
        validate_guide(data)

def test_validate_rejects_non_http_url():
    data = empty_skeleton()
    data["sample_urls"] = [{"url": "javascript:alert(1)", "is_lead": True}]
    with pytest.raises(ValueError):
        validate_guide(data)

def test_validate_rejects_empty_benefit_name():
    data = empty_skeleton()
    data["pricing"]["benefits"] = [
        {"name": "  ", "method": "rate", "rule": "r", "status": "always"}]
    with pytest.raises(ValueError):
        validate_guide(data)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sourcing/test_crawl_guide.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'lemouton.sourcing.crawl_guide'`

- [ ] **Step 3: Write minimal implementation**

```python
# 프로그램/_시스템/lemouton/sourcing/crawl_guide.py
"""소싱처 크롤링 가이드 카드 = SourceRegistry.crawl_guide(JSON 문자열) 의
스켈레톤·검증·검증결과 병합. 순수 로직(DB 의존 없음) — 유닛 테스트 대상.

스펙: docs/superpowers/specs/2026-06-06-소싱처-크롤링-가이드-design.md (스키마 v2)
"""
from __future__ import annotations

import json
from typing import Any

SCHEMA_VERSION = 2

FIELD_KEYS = ("thumbnail", "title", "price", "benefit", "option_stock", "detail_image")
FIELD_METHODS = {"crawl", "manual", "none", "crawl_per_product", "uniform"}
FIELD_STATUSES = {"ok", "warn", "none"}

BENEFIT_METHODS = {"rate", "accrue", "amount", "amount_or_rate", "payment"}
BENEFIT_STATUSES = {"always", "conditional", "optional", "planned"}
BENEFIT_COLLECTION = {"per_product", "uniform"}

FLAG_VALUES = {"ok", "warn"}
VERIFY_STATUSES = {"pending", "claimed", "running", "done", "failed"}


def empty_skeleton() -> dict:
    """미작성 카드의 빈 스켈레톤(v2)."""
    return {
        "version": SCHEMA_VERSION,
        "sample_urls": [],
        "fields": {k: {"method": "none", "locator": "", "status": "none", "note": ""}
                   for k in FIELD_KEYS},
        "pricing": {
            "base_label": "표면 노출가",
            "benefit_collection": "per_product",
            "benefits": [],
            "note": "",
        },
        "verification": {"lead_cache": None, "last_new_check": None},
        "updated_at": None,
    }


def _is_http_url(v: Any) -> bool:
    return isinstance(v, str) and (v.startswith("http://") or v.startswith("https://"))


def validate_guide(data: dict) -> dict:
    """입력 JSON(dict)을 화이트리스트 검증하고 정제본을 반환. 위반 시 ValueError.

    저장 전 서버에서 호출. 알 수 없는 키는 버리고(화이트리스트), enum 위반은 거부.
    """
    if not isinstance(data, dict):
        raise ValueError("crawl_guide must be an object")

    out = empty_skeleton()
    out["version"] = SCHEMA_VERSION

    # sample_urls
    urls = data.get("sample_urls", [])
    if not isinstance(urls, list):
        raise ValueError("sample_urls must be a list")
    clean_urls = []
    for u in urls:
        if not isinstance(u, dict) or not _is_http_url(u.get("url")):
            raise ValueError(f"invalid sample url: {u!r}")
        clean_urls.append({"url": u["url"], "is_lead": bool(u.get("is_lead", False))})
    out["sample_urls"] = clean_urls

    # fields
    fields = data.get("fields", {})
    if not isinstance(fields, dict):
        raise ValueError("fields must be an object")
    for k in FIELD_KEYS:
        f = fields.get(k, {}) or {}
        method = f.get("method", "none")
        status = f.get("status", "none")
        if method not in FIELD_METHODS:
            raise ValueError(f"fields.{k}.method invalid: {method}")
        if status not in FIELD_STATUSES:
            raise ValueError(f"fields.{k}.status invalid: {status}")
        out["fields"][k] = {
            "method": method,
            "locator": str(f.get("locator", "")),
            "status": status,
            "note": str(f.get("note", "")),
        }

    # pricing
    pricing = data.get("pricing", {}) or {}
    collection = pricing.get("benefit_collection", "per_product")
    if collection not in BENEFIT_COLLECTION:
        raise ValueError(f"benefit_collection invalid: {collection}")
    benefits_in = pricing.get("benefits", [])
    if not isinstance(benefits_in, list):
        raise ValueError("pricing.benefits must be a list")
    clean_benefits = []
    for b in benefits_in:
        if not isinstance(b, dict):
            raise ValueError("each benefit must be an object")
        name = str(b.get("name", "")).strip()
        method = b.get("method")
        status = b.get("status")
        if not name:
            raise ValueError("benefit.name must be non-empty")
        if method not in BENEFIT_METHODS:
            raise ValueError(f"benefit.method invalid: {method}")
        if status not in BENEFIT_STATUSES:
            raise ValueError(f"benefit.status invalid: {status}")
        clean_benefits.append({
            "name": name, "method": method,
            "rule": str(b.get("rule", "")), "status": status,
        })
    out["pricing"] = {
        "base_label": str(pricing.get("base_label", "표면 노출가")),
        "benefit_collection": collection,
        "benefits": clean_benefits,
        "note": str(pricing.get("note", "")),
    }

    # verification (있으면 보존, 없으면 빈 채로)
    ver = data.get("verification", {}) or {}
    out["verification"] = {
        "lead_cache": _clean_check(ver.get("lead_cache")),
        "last_new_check": _clean_check(ver.get("last_new_check")),
    }

    out["updated_at"] = data.get("updated_at")
    return out


def _clean_check(c: Any) -> dict | None:
    if not isinstance(c, dict):
        return None
    flags = c.get("flags", {}) or {}
    clean_flags = {k: v for k, v in flags.items() if v in FLAG_VALUES}
    return {
        "url": c.get("url") if _is_http_url(c.get("url")) else None,
        "surface_price": _int_or_none(c.get("surface_price")),
        "benefit_total": _int_or_none(c.get("benefit_total")),
        "final_price": _int_or_none(c.get("final_price")),
        "option_stock": str(c.get("option_stock", "")),
        "flags": clean_flags,
        "job_id": _int_or_none(c.get("job_id")),
        "status": c.get("status") if c.get("status") in VERIFY_STATUSES else None,
        "crawled_at": c.get("crawled_at"),
    }


def _int_or_none(v: Any) -> int | None:
    try:
        return int(v)
    except (TypeError, ValueError):
        return None


def loads(raw: str | None) -> dict:
    """DB Text → dict. None/빈/파싱실패 시 빈 스켈레톤."""
    if not raw:
        return empty_skeleton()
    try:
        data = json.loads(raw)
        return validate_guide(data)
    except (ValueError, json.JSONDecodeError):
        return empty_skeleton()


def dumps(guide: dict) -> str:
    """dict → DB Text(JSON 문자열). 검증 후 직렬화."""
    return json.dumps(validate_guide(guide), ensure_ascii=False)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sourcing/test_crawl_guide.py -v`
Expected: PASS (6 passed)

- [ ] **Step 5: Commit**

```bash
git add "프로그램/_시스템/lemouton/sourcing/crawl_guide.py" "프로그램/_시스템/tests/sourcing/test_crawl_guide.py"
git commit -m "feat(guide): crawl_guide 스켈레톤·검증 순수 로직 모듈 + 유닛 테스트"
```

---

## Task 2: 검증 결과 병합 로직 (merge_verification)

**Files:**
- Modify: `프로그램\_시스템\lemouton\sourcing\crawl_guide.py`
- Test: `프로그램\_시스템\tests\sourcing\test_crawl_guide.py`

- [ ] **Step 1: Write the failing test (append)**

```python
from lemouton.sourcing.crawl_guide import merge_verification

def test_merge_verification_last_new_check():
    guide = empty_skeleton()
    result = {
        "url": "https://www.musinsa.com/products/4112020",
        "surface_price": 42000, "benefit_total": -2100, "final_price": 39900,
        "option_stock": "그레이/250 재고○",
        "flags": {"benefit": "warn", "surface_price": "ok"},
        "job_id": 1234, "status": "done", "crawled_at": "2026-06-06T00:00:00Z",
    }
    out = merge_verification(guide, "last_new_check", result)
    assert out["verification"]["last_new_check"]["final_price"] == 39900
    assert out["verification"]["last_new_check"]["flags"]["benefit"] == "warn"
    # 기존 lead_cache 는 건드리지 않음
    assert out["verification"]["lead_cache"] is None

def test_merge_verification_rejects_bad_kind():
    with pytest.raises(ValueError):
        merge_verification(empty_skeleton(), "WRONG", {})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sourcing/test_crawl_guide.py -k merge -v`
Expected: FAIL — `ImportError: cannot import name 'merge_verification'`

- [ ] **Step 3: Write minimal implementation (append to crawl_guide.py)**

```python
def merge_verification(guide: dict, kind: str, result: dict) -> dict:
    """검증 크롤 결과를 guide.verification[kind] 에 병합. kind in {lead_cache,last_new_check}."""
    if kind not in ("lead_cache", "last_new_check"):
        raise ValueError(f"invalid verification kind: {kind}")
    out = validate_guide(guide)
    out["verification"][kind] = _clean_check(result)
    return out
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sourcing/test_crawl_guide.py -v`
Expected: PASS (8 passed)

- [ ] **Step 5: Commit**

```bash
git add "프로그램/_시스템/lemouton/sourcing/crawl_guide.py" "프로그램/_시스템/tests/sourcing/test_crawl_guide.py"
git commit -m "feat(guide): 검증 결과 병합 merge_verification"
```

---

## Task 3: 모델 컬럼 추가 (SourceRegistry.crawl_guide, CrawlJob.verify_url)

**Files:**
- Modify: `프로그램\_시스템\lemouton\sourcing\models_pricing.py` (SourceRegistry)
- Modify: `프로그램\_시스템\lemouton\sourcing\models.py` (CrawlJob)

> 코드베이스는 JSON 을 `Column(Text)` 로 저장(예: `CrawlWorker.logins_json`). Alembic 없음 — 신규 테이블은 `create_all()`, 신규 컬럼은 Task 4 의 멱등 ALTER 로 추가.

- [ ] **Step 1: Add `crawl_guide` to SourceRegistry**

`models_pricing.py` 의 `SourceRegistry` 클래스에 `updated_at` 컬럼 다음 줄에 추가:

```python
    # 소싱처 크롤링 가이드 카드(JSON 문자열). NULL = 미작성(빈 스켈레톤).
    # 스키마 v2: lemouton/sourcing/crawl_guide.py
    crawl_guide = Column(Text, nullable=True)
```

`Text` 가 import 되어 있는지 확인. 안 되어 있으면 상단 import 에 `Text` 추가:
```python
from sqlalchemy import Column, Integer, String, Text, DateTime  # 기존 import 에 Text 보장
```

- [ ] **Step 2: Add `verify_url` to CrawlJob**

`models.py` 의 `CrawlJob` 클래스에서 `model_code` 컬럼 다음 줄에 추가:

```python
    verify_url = Column(String(512))   # phase="verify" 단건 검증 잡의 대상 URL (NULL=일반 크롤)
```

- [ ] **Step 3: Verify import compiles**

Run: `cd 프로그램/_시스템 && python -c "from lemouton.sourcing.models_pricing import SourceRegistry; from lemouton.sourcing.models import CrawlJob; print(SourceRegistry.crawl_guide, CrawlJob.verify_url)"`
Expected: 두 컬럼 객체가 출력되고 에러 없음

- [ ] **Step 4: Commit**

```bash
git add "프로그램/_시스템/lemouton/sourcing/models_pricing.py" "프로그램/_시스템/lemouton/sourcing/models.py"
git commit -m "feat(guide): SourceRegistry.crawl_guide + CrawlJob.verify_url 컬럼"
```

---

## Task 4: 기존 DB 멱등 컬럼 추가 (SQLite + Supabase)

**Files:**
- Modify: `프로그램\_시스템\shared\db.py` (init_db / create_all 근처)

> `create_all()` 은 **기존 테이블에 신규 컬럼을 추가하지 않는다**. 멱등 `ALTER TABLE`로 보강. SQLite·Postgres 모두 `ADD COLUMN IF NOT EXISTS` 미지원 버전 대비 try/except.

- [ ] **Step 1: Locate create_all call**

Run: `cd 프로그램/_시스템 && python -c "import shared.db, inspect; print(inspect.getsourcefile(shared.db))"`
그리고 `shared/db.py` 에서 `create_all` 호출 함수(`init_db` 등)를 찾는다.

- [ ] **Step 2: Add ensure-columns helper**

`shared/db.py` 에 함수 추가하고 `create_all()` 직후 호출:

```python
def ensure_guide_columns() -> None:
    """신규 컬럼을 기존 테이블에 멱등 추가(create_all 은 신규 컬럼 못 만듦)."""
    from sqlalchemy import text
    stmts = [
        "ALTER TABLE source_registry ADD COLUMN crawl_guide TEXT",
        "ALTER TABLE crawl_jobs ADD COLUMN verify_url VARCHAR(512)",
    ]
    eng = engine  # shared.db 의 SQLAlchemy engine
    for sql in stmts:
        try:
            with eng.begin() as conn:
                conn.execute(text(sql))
        except Exception:
            # 이미 존재(SQLite: "duplicate column", PG: "already exists") → 무시
            pass
```

`init_db()`(create_all 호출하는 함수) 끝에 `ensure_guide_columns()` 추가. `engine` 변수명이 다르면 해당 모듈의 실제 engine 객체로 교체.

- [ ] **Step 3: Run app once to apply locally**

Run: `cd 프로그램/_시스템 && set DISABLE_SCHEDULER=1 && python -c "import shared.db; shared.db.init_db(); print('ok')"`
Expected: `ok` (두 번째 실행해도 에러 없이 ok = 멱등)

- [ ] **Step 4: Verify column exists (SQLite local)**

Run: `cd 프로그램/_시스템 && python -c "from shared.db import engine; from sqlalchemy import text; print([r for r in engine.connect().execute(text('PRAGMA table_info(source_registry)'))])"`
Expected: 출력에 `crawl_guide` 포함 (Postgres면 information_schema 로 확인)

- [ ] **Step 5: Commit**

```bash
git add "프로그램/_시스템/shared/db.py"
git commit -m "feat(guide): 기존 DB 멱등 컬럼 추가(crawl_guide, verify_url)"
```

---

## Task 5: enqueue_verify — 단건 URL 검증 잡 등록

**Files:**
- Modify: `프로그램\_시스템\lemouton\sourcing\crawl_queue.py`
- Test: `프로그램\_시스템\tests\sourcing\test_enqueue_verify.py`

- [ ] **Step 1: Write the failing test**

```python
# 프로그램/_시스템/tests/sourcing/test_enqueue_verify.py
from lemouton.sourcing.crawl_queue import enqueue_verify
from lemouton.sourcing.models import CrawlJob
from shared.db import SessionLocal

def test_enqueue_verify_creates_job():
    out = enqueue_verify("https://www.musinsa.com/products/4112020",
                         required_login="musinsa", triggered_by="guide_verify")
    assert out["created"] in (True, False)
    assert out["status"] == "pending"
    s = SessionLocal()
    try:
        job = s.query(CrawlJob).get(out["id"])
        assert job.phase == "verify"
        assert job.verify_url == "https://www.musinsa.com/products/4112020"
        assert job.triggered_by == "guide_verify"
    finally:
        s.close()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sourcing/test_enqueue_verify.py -v`
Expected: FAIL — `ImportError: cannot import name 'enqueue_verify'`

- [ ] **Step 3: Write minimal implementation (append to crawl_queue.py)**

```python
def enqueue_verify(
    verify_url: str,
    *,
    required_login: Optional[str] = None,
    triggered_by: str = "guide_verify",
    priority: int = 50,
) -> dict:
    """가이드 ④ 검증용 단건 URL 크롤 잡 등록. phase='verify'.

    같은 URL 의 미완 verify 잡이 있으면 재사용(dedup). priority 50 = 일반 크롤(100)보다 우선.
    """
    if not (verify_url.startswith("http://") or verify_url.startswith("https://")):
        raise ValueError("verify_url must be http(s)")
    s = SessionLocal()
    try:
        existing = (s.query(CrawlJob)
                    .filter(CrawlJob.status.in_(LIVE_STATUSES))
                    .filter(CrawlJob.phase == "verify")
                    .filter(CrawlJob.verify_url == verify_url)
                    .order_by(CrawlJob.id.desc()).first())
        if existing is not None:
            return {"id": existing.id, "created": False, "status": existing.status}
        job = CrawlJob(
            model_code=None, phase="verify", status="pending", routing="queue",
            required_login=required_login, priority=priority,
            verify_url=verify_url, triggered_by=triggered_by, created_at=_now(),
        )
        s.add(job)
        s.commit()
        return {"id": job.id, "created": True, "status": "pending"}
    finally:
        s.close()


def get_job(job_id: int) -> Optional[dict]:
    """잡 1건 상태/결과 조회(폴링용)."""
    import json as _json
    s = SessionLocal()
    try:
        job = s.query(CrawlJob).get(job_id)
        if job is None:
            return None
        result = None
        if job.result_json:
            try:
                result = _json.loads(job.result_json)
            except Exception:
                result = None
        return {"id": job.id, "status": job.status, "phase": job.phase,
                "worker_name": job.worker_name, "verify_url": job.verify_url,
                "result": result, "error": job.error}
    finally:
        s.close()
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sourcing/test_enqueue_verify.py -v`
Expected: PASS (1 passed)

- [ ] **Step 5: Commit**

```bash
git add "프로그램/_시스템/lemouton/sourcing/crawl_queue.py" "프로그램/_시스템/tests/sourcing/test_enqueue_verify.py"
git commit -m "feat(guide): enqueue_verify 단건 URL 검증 잡 + get_job 폴링"
```

---

## Task 6: 라우트 모듈 (Blueprint) + 등록

**Files:**
- Create: `프로그램\_시스템\webapp\routes\sourcing_guide.py`
- Modify: `프로그램\_시스템\webapp\routes\__init__.py`

- [ ] **Step 1: Write the blueprint**

```python
# 프로그램/_시스템/webapp/routes/sourcing_guide.py
"""소싱처 크롤링 가이드 — 전체보기/상세 렌더 + crawl_guide JSON GET/PUT + ④ 검증."""
from __future__ import annotations

import os
from datetime import datetime, timezone

from flask import Blueprint, jsonify, render_template, request

from shared.db import SessionLocal
from lemouton.sourcing.models_pricing import SourceRegistry
from lemouton.sourcing import crawl_guide as cg
from lemouton.sourcing.crawl_queue import enqueue_verify, get_job

bp = Blueprint("sourcing_guide", __name__, url_prefix="/sourcing-guide")


@bp.before_request
def _admin_only():
    if os.environ.get("ENVIRONMENT") != "team-share-dev":
        return None
    from webapp.auth.permissions import enforce_admin
    return enforce_admin()


def _now_iso() -> str:
    return datetime.now(timezone.utc).isoformat()


def _sources():
    s = SessionLocal()
    try:
        return s.query(SourceRegistry).order_by(SourceRegistry.sort_order, SourceRegistry.id).all()
    finally:
        s.close()


def _source(sid: int):
    s = SessionLocal()
    try:
        return s.query(SourceRegistry).get(sid)
    finally:
        s.close()


@bp.route("/")
def overview():
    rows = []
    for src in _sources():
        guide = cg.loads(src.crawl_guide)
        rows.append({"id": src.id, "name": src.name, "guide": guide})
    return render_template("sourcing_guide/overview.html", rows=rows, active="sourcing_guide")


@bp.route("/<int:sid>")
def detail(sid: int):
    src = _source(sid)
    if src is None:
        return "not found", 404
    guide = cg.loads(src.crawl_guide)
    sources = [{"id": x.id, "name": x.name} for x in _sources()]
    return render_template("sourcing_guide/detail.html",
                           src={"id": src.id, "name": src.name},
                           guide=guide, sources=sources, active="sourcing_guide")


@bp.route("/api/<int:sid>", methods=["GET"])
def api_get(sid: int):
    src = _source(sid)
    if src is None:
        return jsonify(ok=False, error="not_found"), 404
    return jsonify(ok=True, guide=cg.loads(src.crawl_guide))


@bp.route("/api/<int:sid>", methods=["PUT"])
def api_put(sid: int):
    s = SessionLocal()
    try:
        src = s.query(SourceRegistry).get(sid)
        if src is None:
            return jsonify(ok=False, error="not_found"), 404
        try:
            guide = cg.validate_guide(request.get_json(force=True) or {})
        except ValueError as e:
            # 기존 값 보존(덮어쓰기 금지)
            return jsonify(ok=False, error="invalid", message=str(e)), 400
        guide["updated_at"] = _now_iso()
        src.crawl_guide = cg.dumps(guide)
        s.commit()
        return jsonify(ok=True, guide=guide)
    finally:
        s.close()


@bp.route("/api/<int:sid>/verify", methods=["POST"])
def api_verify(sid: int):
    src = _source(sid)
    if src is None:
        return jsonify(ok=False, error="not_found"), 404
    url = (request.get_json(force=True) or {}).get("url", "")
    try:
        job = enqueue_verify(url, required_login=(src.name or "").lower(),
                             triggered_by="guide_verify")
    except ValueError as e:
        return jsonify(ok=False, error="invalid_url", message=str(e)), 400
    return jsonify(ok=True, job_id=job["id"], status=job["status"])


@bp.route("/api/<int:sid>/verify/<int:job_id>", methods=["GET"])
def api_verify_status(sid: int, job_id: int):
    job = get_job(job_id)
    if job is None:
        return jsonify(ok=False, error="not_found"), 404
    # 완료 시 결과를 guide.verification.last_new_check 에 병합 저장
    if job["status"] == "done" and job.get("result"):
        s = SessionLocal()
        try:
            src = s.query(SourceRegistry).get(sid)
            if src is not None:
                merged = cg.merge_verification(cg.loads(src.crawl_guide),
                                               "last_new_check",
                                               {**job["result"], "job_id": job_id,
                                                "status": "done"})
                src.crawl_guide = cg.dumps(merged)
                s.commit()
        finally:
            s.close()
    return jsonify(ok=True, job=job)
```

- [ ] **Step 2: Register the blueprint**

`webapp/routes/__init__.py` 의 `register_routes` 안, 기존 import/등록 패턴과 동일하게 추가:

```python
    from webapp.routes.sourcing_guide import bp as sourcing_guide_bp
    app.register_blueprint(sourcing_guide_bp)
```

(기존 다른 blueprint 가 `app.register_blueprint(...)` 로 등록되는 위치에 한 줄 추가. 등록 방식이 리스트/루프면 그 리스트에 추가.)

- [ ] **Step 3: Verify routes load**

Run: `cd 프로그램/_시스템 && set ENVIRONMENT= && python -c "from app import create_app; app=create_app(); print([r.rule for r in app.url_map.iter_rules() if 'sourcing-guide' in r.rule])"`
Expected: `/sourcing-guide/`, `/sourcing-guide/<int:sid>`, `/sourcing-guide/api/<int:sid>`, `.../verify`, `.../verify/<int:job_id>` 출력

- [ ] **Step 4: Commit**

```bash
git add "프로그램/_시스템/webapp/routes/sourcing_guide.py" "프로그램/_시스템/webapp/routes/__init__.py"
git commit -m "feat(guide): sourcing_guide Blueprint(렌더+GET/PUT+검증 API) 등록"
```

---

## Task 7: 가이드 전용 CSS (toss.css append)

**Files:**
- Modify: `프로그램\_시스템\webapp\static\toss.css` (파일 끝에 append)

> `.cf-receipt` 등 FX 영수증 클래스는 이미 toss.css 에 존재 → 재사용. 가이드 전용 클래스만 추가.

- [ ] **Step 1: Append guide CSS**

`toss.css` 끝에 추가(시안 v8 ③ A형 + v5 ④ 시안3 기준). 핵심 클래스:

```css
/* ===== 소싱처 크롤링 가이드 ===== */
.sg-card{background:#fff;border:1px solid #E5E8EB;border-radius:14px;padding:18px 20px;margin-bottom:14px;}
.sg-ct{font-size:14.5px;margin-bottom:14px;display:flex;align-items:center;gap:9px;}
.sg-ct .n{width:24px;height:24px;background:#191F28;color:#fff;border-radius:7px;font-size:12.5px;font-weight:800;display:inline-flex;align-items:center;justify-content:center;}
.sg-ct .tt{font-weight:800;} .sg-ct .src{font-weight:600;color:#6B7684;font-size:13px;}
/* ③ A형 표 */
.sg-atbl{width:100%;border-collapse:collapse;}
.sg-atbl th{text-align:left;font-size:11px;color:#6B7684;font-weight:700;padding:7px 8px;border-bottom:1px solid #E5E8EB;}
.sg-atbl td{padding:13px 8px;border-bottom:1px solid #F1F3F5;vertical-align:middle;}
.sg-atbl tr:last-child td{border-bottom:none;}
.sg-name{font-size:14.5px;font-weight:800;}
.sg-rule{font-size:12.5px;color:#475059;font-family:ui-monospace,monospace;}
.sg-ty{font-size:10.5px;font-weight:800;border-radius:6px;padding:2px 8px;white-space:nowrap;}
.sg-ty.rate{background:#E8F3FF;color:#3182F6;} .sg-ty.accrue{background:#E7F7F0;color:#0E9F6E;}
.sg-ty.amount{background:#FEF3E2;color:#C2730A;} .sg-ty.amount_or_rate{background:#FEF3E2;color:#C2730A;}
.sg-ty.payment{background:#F3EEFF;color:#7C3AED;}
.sg-st{display:inline-flex;align-items:center;gap:8px;font-size:12.5px;font-weight:700;}
.sg-st .d{flex:0 0 auto;width:8px;height:8px;border-radius:50%;}
.sg-st.always{color:#10B981;} .sg-st.always .d{background:#10B981;}
.sg-st.conditional{color:#F59E0B;} .sg-st.conditional .d{background:#F59E0B;}
.sg-st.optional{color:#3182F6;} .sg-st.optional .d{background:#3182F6;}
.sg-st.planned{color:#ADB5BD;} .sg-st.planned .d{background:#ADB5BD;}
/* ④ 서브탭(언더라인) + 2열 */
.sg-sub{display:flex;gap:22px;margin-bottom:16px;border-bottom:1px solid #E5E8EB;}
.sg-sub button{background:none;border:none;border-bottom:2.5px solid transparent;padding:8px 2px 11px;font-size:14px;font-weight:700;cursor:pointer;color:#6B7684;margin-bottom:-1px;}
.sg-sub button.on{color:#3182F6;border-bottom-color:#3182F6;}
.sg-subpane{display:none;} .sg-subpane.on{display:block;}
.sg-grid2{display:grid;grid-template-columns:1fr 1fr;gap:14px;}
.sg-new{max-width:560px;margin:0 auto;}
.sg-nvin{display:flex;gap:8px;margin-bottom:12px;}
.sg-nvin input{flex:1;border:1px solid #D1D6DB;border-radius:9px;padding:11px 14px;font-family:ui-monospace,monospace;font-size:12.5px;color:#3A4A5A;}
.sg-status{display:inline-flex;align-items:center;gap:8px;font-size:12px;color:#C2730A;background:#FEF3E2;border-radius:999px;padding:6px 13px;font-weight:600;}
.sg-status .d{width:8px;height:8px;border-radius:50%;background:#F59E0B;}
.sg-vchk.ok{color:#10B981;font-size:11px;font-weight:700;} .sg-vchk.warn{color:#F59E0B;font-size:11px;font-weight:700;}
```

- [ ] **Step 2: Commit**

```bash
git add "프로그램/_시스템/webapp/static/toss.css"
git commit -m "feat(guide): 크롤링 가이드 전용 CSS(③ A형 표 + ④ 서브탭) append"
```

---

## Task 8: 상세 템플릿 (detail.html) — 4단계

**Files:**
- Create: `프로그램\_시스템\webapp\templates\sourcing_guide\detail.html`

> 마크업 전체 기준 = 바탕화면 `모음전 시안 v4`(4단계) + `v8`(③) + `v5 ④시안3`. 아래는 Jinja 바인딩 골격. 저장 JS 는 PUT `/sourcing-guide/api/<id>`, 검증 JS 는 POST/폴링.

- [ ] **Step 1: Write detail.html**

```html
{% extends "base.html" %}
{% block title %}크롤링 가이드 · {{ src.name }}{% endblock %}
{% block content %}
<div class="sg-wrap" data-sid="{{ src.id }}" style="padding:20px 24px;">
  <a href="/sourcing-guide/" class="sg-back">← 전체보기</a>
  <div class="page-head" style="display:flex;align-items:center;gap:12px;margin:12px 0;">
    <h2 style="font-size:19px;font-weight:800;">{{ src.name }}</h2>
    <button class="btn btn-primary btn-sm" id="sg-save" style="margin-left:auto;">저장</button>
  </div>

  <!-- ① 샘플 URL -->
  <div class="sg-card">
    <div class="sg-ct"><span class="n">1</span><span class="tt">기준 샘플 URL</span></div>
    <div id="sg-urls">
      {% for u in guide.sample_urls %}
      <div class="sg-urlrow" data-url="{{ u.url }}">
        {% if u.is_lead %}<span class="tagk">대표</span>{% endif %}
        <span class="u">{{ u.url }}</span>
        <a href="{{ u.url }}" target="_blank" class="btn btn-ghost btn-sm">↗ 열기</a>
      </div>
      {% endfor %}
    </div>
    <button class="btn btn-ghost btn-sm" id="sg-add-url">+ URL 추가</button>
  </div>

  <!-- ② 크롤 구조 분석 -->
  <div class="sg-card">
    <div class="sg-ct"><span class="n">2</span><span class="tt">크롤 구조 분석</span></div>
    <table class="sg-atbl" id="sg-fields">
      <thead><tr><th style="width:110px;">항목</th><th style="width:150px;">수집 방법</th><th>위치(셀렉터·설명)</th><th style="width:80px;">상태</th></tr></thead>
      <tbody>
        {% set labels = {'thumbnail':'썸네일','title':'상품명','price':'가격','benefit':'혜택','option_stock':'옵션·재고','detail_image':'상세 이미지'} %}
        {% for key, lab in labels.items() %}
        {% set f = guide.fields[key] %}
        <tr data-field="{{ key }}">
          <td class="sg-name" style="font-size:13px;">{{ lab }}</td>
          <td><input class="sg-method" value="{{ f.method }}"></td>
          <td><input class="sg-loc" value="{{ f.locator }}" style="width:100%;"></td>
          <td><input class="sg-fstatus" value="{{ f.status }}" style="width:60px;"></td>
        </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>

  <!-- ③ 최종 매입가 로직 (A형 표) -->
  <div class="sg-card">
    <div class="sg-ct"><span class="n">3</span><span class="tt">최종 매입가 로직</span><span class="src">{{ src.name }}</span></div>
    <table class="sg-atbl" id="sg-benefits">
      <thead><tr><th style="width:170px;">혜택명</th><th style="width:120px;">혜택 방식</th><th>계산 규칙</th><th style="width:150px;">적용</th></tr></thead>
      <tbody>
        {% set tylab = {'rate':'정률 %','accrue':'적립 %','amount':'정액 원','amount_or_rate':'정액·정률','payment':'결제'} %}
        {% set stlab = {'always':'상시','conditional':'조건부','optional':'선택','planned':'업데이트 예정'} %}
        {% for b in guide.pricing.benefits %}
        <tr data-method="{{ b.method }}" data-status="{{ b.status }}">
          <td class="sg-name">{{ b.name }}</td>
          <td><span class="sg-ty {{ b.method }}">{{ tylab[b.method] }}</span></td>
          <td class="sg-rule">{{ b.rule }}</td>
          <td><span class="sg-st {{ b.status }}"><span class="d"></span>{{ stlab[b.status] }}</span></td>
        </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>

  <!-- ④ 가격 검증 (서브탭) -->
  <div class="sg-card">
    <div class="sg-ct"><span class="n">4</span><span class="tt">가격 검증</span></div>
    <div class="sg-sub">
      <button class="on" data-sub="sample">기존 샘플</button>
      <button data-sub="new">신규 검증</button>
    </div>
    <!-- 기존 샘플: 2열 grid -->
    <div class="sg-subpane on" id="sub-sample">
      <div class="sg-grid2" id="sg-leadcards">
        {% set lc = guide.verification.lead_cache %}
        {% if lc %}
        <div class="fxpop"><div class="body"><div class="cf-receipt">
          <div class="cf-rc-ln"><span class="lbl">표면 노출가</span><span class="num">{{ lc.surface_price }}원</span></div>
          <div class="cf-rc-ln disc"><span class="lbl">적용 혜택</span><span class="num">{{ lc.benefit_total }}원</span></div>
          <div class="cf-rc-div"></div>
          <div class="cf-rc-ln fin"><span class="lbl">최종 매입가</span><span class="num">{{ lc.final_price }}원</span></div>
        </div></div></div>
        {% else %}<div class="muted">아직 기존 샘플 캐시값이 없습니다.</div>{% endif %}
      </div>
    </div>
    <!-- 신규 검증: 중앙 입력 + 영수증 -->
    <div class="sg-subpane" id="sub-new">
      <div class="sg-new">
        <div class="sg-nvin"><input id="sg-verify-url" placeholder="검증할 새 URL 붙여넣기"><button class="btn btn-primary" id="sg-verify-btn">검증</button></div>
        <div style="text-align:center;"><span class="sg-status" id="sg-verify-status" style="display:none;"><span class="d"></span><span id="sg-verify-msg"></span></span></div>
        <div id="sg-verify-result"></div>
      </div>
    </div>
  </div>
</div>

<script>
{% include "sourcing_guide/detail.js" %}
</script>
{% endblock %}
```

- [ ] **Step 2: Commit (template skeleton)**

```bash
git add "프로그램/_시스템/webapp/templates/sourcing_guide/detail.html"
git commit -m "feat(guide): 상세 4단계 템플릿(detail.html)"
```

---

## Task 9: 상세 JS — 저장(PUT) + 검증(POST·폴링)

**Files:**
- Create: `프로그램\_시스템\webapp\templates\sourcing_guide\detail.js`

> 별도 partial 로 분리해 detail.html `{% include %}`. (정적 JS 라우팅 없이 인라인 — 기존 매트릭스 패턴과 동일)

- [ ] **Step 1: Write detail.js**

```javascript
(function(){
  const root = document.querySelector('.sg-wrap');
  const sid = root.dataset.sid;
  // 서브탭
  document.querySelectorAll('.sg-sub button').forEach(b=>b.addEventListener('click',()=>{
    document.querySelectorAll('.sg-sub button').forEach(x=>x.classList.toggle('on',x===b));
    document.getElementById('sub-sample').classList.toggle('on', b.dataset.sub==='sample');
    document.getElementById('sub-new').classList.toggle('on', b.dataset.sub==='new');
  }));

  // 현재 화면 → guide JSON 수집
  function collect(){
    const urls=[...document.querySelectorAll('#sg-urls .sg-urlrow')].map((r,i)=>(
      {url:r.dataset.url, is_lead:!!r.querySelector('.tagk')}));
    const fields={};
    document.querySelectorAll('#sg-fields tr[data-field]').forEach(tr=>{
      fields[tr.dataset.field]={method:tr.querySelector('.sg-method').value,
        locator:tr.querySelector('.sg-loc').value, status:tr.querySelector('.sg-fstatus').value, note:''};
    });
    const benefits=[...document.querySelectorAll('#sg-benefits tr[data-method]')].map(tr=>(
      {name:tr.querySelector('.sg-name').textContent.trim(), method:tr.dataset.method,
       rule:tr.querySelector('.sg-rule').textContent.trim(), status:tr.dataset.status}));
    return {version:2, sample_urls:urls, fields:fields,
      pricing:{base_label:'표면 노출가', benefit_collection:'per_product', benefits:benefits, note:''}};
  }

  // 저장
  document.getElementById('sg-save').addEventListener('click', async ()=>{
    const res=await fetch(`/sourcing-guide/api/${sid}`,{method:'PUT',
      headers:{'Content-Type':'application/json'}, body:JSON.stringify(collect())});
    const j=await res.json();
    alert(j.ok? '저장됨':('저장 실패: '+(j.message||j.error)));
  });

  // ④ 검증: POST → 폴링
  const stEl=document.getElementById('sg-verify-status');
  const msgEl=document.getElementById('sg-verify-msg');
  const resEl=document.getElementById('sg-verify-result');
  function receipt(c){
    const f=(c.flags||{});
    const chk=(k)=> f[k]==='warn'? '<span class="sg-vchk warn">⚠️</span>':'<span class="sg-vchk ok">✅</span>';
    return `<div class="fxpop" style="margin-top:14px;"><div class="body"><div class="cf-receipt">
      <div class="cf-rc-ln"><span class="lbl">표면 노출가</span><span class="num">${c.surface_price}원 ${chk('surface_price')}</span></div>
      <div class="cf-rc-ln disc"><span class="lbl">적용 혜택</span><span class="num">${c.benefit_total}원 ${chk('benefit')}</span></div>
      <div class="cf-rc-div"></div>
      <div class="cf-rc-ln fin"><span class="lbl">최종 매입가</span><span class="num">${c.final_price}원</span></div>
      <div class="cf-rc-div"></div>
      <div class="cf-rc-ln"><span class="lbl">옵션·재고</span><span class="num">${c.option_stock||''} ${chk('option_stock')}</span></div>
    </div></div></div>`;
  }
  let pollTimer=null;
  async function poll(jobId){
    const res=await fetch(`/sourcing-guide/api/${sid}/verify/${jobId}`);
    const j=await res.json();
    const job=j.job||{};
    msgEl.textContent = ({pending:'대기 중(온라인 워커 대기)',claimed:'워커 선점됨',
      running:`크롤 중 · 워커 ${job.worker_name||''}`,done:'완료',failed:'실패'})[job.status]||job.status;
    if(job.status==='done' && job.result){ clearInterval(pollTimer); resEl.innerHTML=receipt(job.result); }
    else if(job.status==='failed'){ clearInterval(pollTimer); resEl.innerHTML=`<div class="muted">검증 실패: ${job.error||''}</div>`; }
  }
  document.getElementById('sg-verify-btn').addEventListener('click', async ()=>{
    const url=document.getElementById('sg-verify-url').value.trim();
    if(!url) return;
    const res=await fetch(`/sourcing-guide/api/${sid}/verify`,{method:'POST',
      headers:{'Content-Type':'application/json'}, body:JSON.stringify({url})});
    const j=await res.json();
    if(!j.ok){ alert('검증 요청 실패: '+(j.message||j.error)); return; }
    stEl.style.display='inline-flex'; resEl.innerHTML='';
    if(pollTimer) clearInterval(pollTimer);
    poll(j.job_id); pollTimer=setInterval(()=>poll(j.job_id), 2500);
  });
})();
```

- [ ] **Step 2: Commit**

```bash
git add "프로그램/_시스템/webapp/templates/sourcing_guide/detail.js"
git commit -m "feat(guide): 상세 저장(PUT)+검증(POST·폴링) JS"
```

---

## Task 10: 전체보기 템플릿 (overview.html)

**Files:**
- Create: `프로그램\_시스템\webapp\templates\sourcing_guide\overview.html`

- [ ] **Step 1: Write overview.html**

```html
{% extends "base.html" %}
{% block title %}소싱처별 크롤링 가이드{% endblock %}
{% block content %}
<div style="padding:22px 26px;">
  <div class="page-head" style="display:flex;align-items:center;gap:12px;margin-bottom:14px;">
    <h2 style="font-size:19px;font-weight:800;">소싱처별 크롤링 가이드</h2>
  </div>
  <div class="sg-card" style="padding:0;overflow:hidden;">
    <table class="sg-atbl">
      <thead><tr><th>소싱처</th><th>샘플 URL</th><th>구조</th><th>매입가 로직</th><th>가격 검증</th><th>혜택 방식</th></tr></thead>
      <tbody>
        {% for r in rows %}
        {% set g = r.guide %}
        <tr style="cursor:pointer;" onclick="location.href='/sourcing-guide/{{ r.id }}'">
          <td class="sg-name" style="font-size:13px;">{{ r.name }}</td>
          <td>{{ g.sample_urls|length }}개</td>
          <td>{% set okf = g.fields.values()|selectattr('status','equalto','ok')|list|length %}{{ okf }}/6</td>
          <td>{{ '정의됨' if g.pricing.benefits else '미정의' }}</td>
          <td>{% if g.verification.last_new_check %}검증함{% else %}—{% endif %}</td>
          <td>{{ '상품별 크롤' if g.pricing.benefit_collection=='per_product' else '일괄' }}</td>
        </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>
  <div class="muted" style="margin-top:12px;">행 클릭 → 상세 4단계.</div>
</div>
{% endblock %}
```

- [ ] **Step 2: Commit**

```bash
git add "프로그램/_시스템/webapp/templates/sourcing_guide/overview.html"
git commit -m "feat(guide): 전체보기 템플릿(overview.html)"
```

---

## Task 11: 사이드바 nav 항목 추가

**Files:**
- Modify: 사이드바 nav 정의처 (sidebar.html 또는 nav 데이터 소스)

- [ ] **Step 1: Locate the 소싱 group nav definition**

Run: `cd 프로그램/_시스템 && grep -rn "소싱처 사전\|소싱처 운영센터\|sourcing" webapp/templates/partials/sidebar.html webapp/ | grep -i "nav\|sidebar\|소싱" | head -40`
사이드바가 DB/설정 기반이면 nav 항목 소스(예: `home.py` 의 컨텍스트, 설정 테이블, seed)에서 "소싱" 그룹을 찾는다.

- [ ] **Step 2: Add the nav item**

"소싱" 그룹에 항목 추가 (정적 markup 이면):
```html
<div data-url="/sourcing-guide/" class="sb3-item{% if (active|default('')) == 'sourcing_guide' %} on{% endif %}">
  <span class="emo pi"><i class="ph-light ph-book-open"></i></span>
  <span class="nm">크롤링 가이드</span>
</div>
```
DB 기반이면 해당 seed/설정에 `{name:'크롤링 가이드', url:'/sourcing-guide/', active_key:'sourcing_guide', icon:'book-open'}` 항목 추가.

- [ ] **Step 3: Verify (브라우저)** — Task 12 의 실검증에서 사이드바 항목 노출·활성 확인.

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "feat(guide): 사이드바 '크롤링 가이드' nav 항목"
```

---

## Task 12: 워커 계약 문서화 (verify 잡)

**Files:**
- Modify: `docs\crawl-worker-system.md`

> ④ 검증의 실제 크롤은 로컬 PC 워커(이 레포 밖/별도 Phase 2)가 수행. 워커가 `phase="verify"` 잡을 처리하는 계약을 명문화. **이 레포의 M1 범위 = 서버 enqueue/폴링/병합 + UI** 까지. 워커가 verify 미지원이면 잡은 `pending` 유지(UI 가 안내).

- [ ] **Step 1: Append worker contract section**

`docs/crawl-worker-system.md` 에 추가:

```markdown
## verify 잡 계약 (가이드 ④ 가격 검증)

- 잡: `crawl_jobs` 에 `phase="verify"`, `verify_url=<대상 URL>`, `required_login=<소싱처 소문자>`, `priority=50`.
- 워커는 verify 잡 선점 시 단건 URL 을 크롤해 아래 result_json 을 기록하고 `status="done"`:
  ```json
  {"url":"...","surface_price":42000,"benefit_total":-2100,"final_price":39900,
   "option_stock":"그레이/250 재고○",
   "flags":{"surface_price":"ok","benefit":"warn","final_price":"warn","option_stock":"ok"}}
  ```
- flags 는 사니티 가드(`lemouton/sourcing/crawlers/pricing_policy.py::sanity_check`) 결과 기반: 의심 항목 `warn`, 정상 `ok`.
- 실패 시 `status="failed"`, `error=<사유>`. 서버 폴링이 done 결과를 `crawl_guide.verification.last_new_check` 에 병합.
```

- [ ] **Step 2: Commit**

```bash
git add "docs/crawl-worker-system.md"
git commit -m "docs(crawl): verify 잡 워커 계약(가이드 ④ 검증)"
```

---

## Task 13: 전체 테스트 + 라이브 브라우저 실검증 + 7섹션 보고서

**Files:** (없음 — 검증)

- [ ] **Step 1: Run full test suite**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sourcing/test_crawl_guide.py tests/sourcing/test_enqueue_verify.py -v`
Expected: 전부 PASS

- [ ] **Step 2: Run app locally**

Run: `cd 프로그램/_시스템 && set DISABLE_SCHEDULER=1 && set ENVIRONMENT=team-share-dev && python app.py`
열기: `http://127.0.0.1:5052/sourcing-guide/`

- [ ] **Step 3: `/live-browser-verify` 게이트로 실검증**

`/live-browser-verify` 스킬 실행. 시나리오:
1. 사이드바 "소싱 > 크롤링 가이드" 클릭 → 전체보기 표 노출(소싱처 행).
2. 무신사 행 클릭 → 상세 4단계.
3. ① URL 추가, ② 구조 입력, ③ 혜택 행 확인(A형 표·적용 점 정렬), 저장 → 토스트/alert.
4. 새로고침 → 값 유지(저장 영속 확인).
5. ④ 신규 검증 탭 → URL 입력 → 검증 → 상태 뱃지(대기→…) 확인. (워커 미가동이면 `pending` 유지가 정상 — UI 안내 확인.)
6. 잘못된 입력(비-http URL) → 400 거부 확인(저장 실패 alert, 기존값 보존).

- [ ] **Step 4: 7섹션 보고서 작성** (`/ui-verify` 형식)

- [ ] **Step 5: 워크트리 작업 시 master 머지 + 라이브 서버 재시작** (글로벌 6원칙)

---

## Self-Review (작성자 점검)

- **Spec coverage:** ①~④ 4단계(Task 8/10) · ③ A형(Task 8 템플릿+Task7 CSS) · ④ 비동기 검증(Task 5/6/9) · 저장 PUT(Task 6/9) · 모델+멱등 컬럼(Task 3/4) · 검증 enum(Task 1) · 사이드바(Task 11) · 워커 계약(Task 12) · 실검증(Task 13). ✅ 누락 없음.
- **No placeholders:** 코드 스텝마다 실제 코드 포함. 단, Task 4(engine 변수명)·Task 6(blueprint 등록 위치)·Task 11(nav 소스)은 코드베이스 실제 위치 확인 후 적용 — 명령 포함.
- **Type consistency:** `loads/dumps/validate_guide/empty_skeleton/merge_verification` 시그니처 Task 1·2·6·9 일관. `enqueue_verify`/`get_job` Task 5·6 일관. JSON 키(`sample_urls/fields/pricing.benefits/verification.last_new_check`)가 모델 없이 전 Task 동일.
- **주의(스펙 정정):** Alembic 없음 → Task 4 멱등 ALTER 로 대체. JSON 은 `Text` 컬럼(코드베이스 패턴) — 스펙의 "Alembic 마이그레이션" 표현은 본 plan 의 Task 4 로 치환.
