# 구성 레이어 Phase 3a — 4단계 흐름 JSON 라우트 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 검색우선 4단계 흐름(§14·§15)을 프런트가 호출할 JSON API로 노출한다 — 기존 검증된 서비스(set_service·channel_service·set_link_service) 위 얇은 라우트.

**Architecture:** 새 블루프린트 `webapp/routes/sets_api.py`(`url_prefix=/api`)에 9개 라우트를 정의하고 `webapp/routes/__init__.py::register_routes`에 등록한다(app.py 미수정 — WIP 회피). 각 라우트는 `SessionLocal()` → 서비스 호출 → commit → jsonify의 기존 패턴(api.py와 동일). 밑단 로직은 이미 sets 19/19 테스트 통과. 본 라우트층은 **오프라인 라우트-등록 스모크 테스트**로 배선을 검증하고, 행위 검증은 라이브(curl/브라우저)로 한다(이 프로젝트 라우트는 내부 SessionLocal 사용이라 오프라인 행위 단위테스트 부재 — 기존 P0 연결 라우트와 동일).

**Tech Stack:** Flask · SQLAlchemy · pytest. 재사용: `lemouton/sets/{set_service,channel_service,set_link_service}.py`, `lemouton/sourcing/models.py`(Model/Option).

**Scope:** §15의 4단계 흐름 백엔드 API. 실제 화면(템플릿)·라이브 브라우저 검증은 Phase 3b. 라이브 마켓 push는 Phase 4.

**작업 위치:** `cd "C:\dev\모음전 프로젝트\프로그램\_시스템"`.

---

## File Structure

| 파일 | 책임 | 신규/수정 |
|---|---|---|
| `webapp/routes/sets_api.py` | 4단계 흐름 9개 JSON 라우트(검색·구성CRUD·채널CRUD·연동실행) | 신규 |
| `webapp/routes/__init__.py` | 블루프린트 등록 2줄 | 수정 |
| `tests/sets/test_sets_api_routes.py` | 라우트 등록 스모크(오프라인) | 신규 |

---

## Task 1: sets_api 블루프린트 + 등록 + 스모크

**Files:**
- Create: `webapp/routes/sets_api.py`, `tests/sets/test_sets_api_routes.py`
- Modify: `webapp/routes/__init__.py` (등록)

- [ ] **Step 1: 실패하는 스모크 테스트 작성** — `tests/sets/test_sets_api_routes.py`:

```python
"""sets_api 라우트 등록 스모크 — Flask url_map 검증(오프라인, DB 호출 없음)."""
from flask import Flask

from webapp.routes.sets_api import bp


def _rules():
    app = Flask(__name__)
    app.register_blueprint(bp)
    return {r.rule for r in app.url_map.iter_rules()}


def test_blueprint_registers_expected_routes():
    rules = _rules()
    expected = {
        "/api/sets/search-bundles",
        "/api/sets",
        "/api/sets/<int:set_id>",
        "/api/sets/<int:set_id>/options",
        "/api/sets/<int:set_id>/channels",
        "/api/channels/<int:channel_id>/product",
        "/api/channels/<int:channel_id>",
        "/api/channels/<int:channel_id>/link",
    }
    missing = expected - rules
    assert not missing, f"라우트 누락: {missing}"


def test_blueprint_name():
    assert bp.name == "sets_api"
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd "C:\dev\모음전 프로젝트\프로그램\_시스템" && python -m pytest tests/sets/test_sets_api_routes.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'webapp.routes.sets_api'`

- [ ] **Step 3: 블루프린트 구현** — `webapp/routes/sets_api.py`:

```python
"""[구성 레이어 Phase 3a] 검색우선 4단계 흐름 JSON API.

기존 검증된 서비스(set_service·channel_service·set_link_service) 위 얇은 라우트.
패턴은 webapp/routes/api.py 와 동일(SessionLocal→서비스→commit→jsonify).
"""
from flask import Blueprint, jsonify, request

from shared.db import SessionLocal
from lemouton.sourcing.models import Model, Option
from lemouton.sets import set_service as svc
from lemouton.sets import channel_service as ch
from lemouton.sets import set_link_service as link

bp = Blueprint("sets_api", __name__, url_prefix="/api")


def _err(msg, code=400):
    return jsonify({"ok": False, "error": msg}), code


# ── 단계 ① 모음전 검색 ──
@bp.get("/sets/search-bundles")
def search_bundles():
    q = (request.args.get("q") or "").strip()
    s = SessionLocal()
    try:
        query = s.query(Model)
        if q:
            like = f"%{q}%"
            query = query.filter(
                Model.model_code.ilike(like)
                | Model.model_name_raw.ilike(like)
                | Model.model_name_display.ilike(like)
            )
        models = query.order_by(Model.model_code).limit(20).all()
        out = []
        for m in models:
            cnt = s.query(Option).filter_by(model_code=m.model_code).count()
            out.append({
                "code": m.model_code,
                "name": m.model_name_display or m.model_name_raw,
                "option_count": cnt,
            })
        return jsonify({"ok": True, "results": out})
    finally:
        s.close()


# ── 단계 ② 구성 생성·옵션 ──
@bp.post("/sets")
def create_set():
    p = request.get_json(silent=True) or {}
    model_code = (p.get("model_code") or "").strip()
    name = (p.get("name") or "").strip()
    if not model_code or not name:
        return _err("model_code 와 name 이 필요해요.")
    quantity = int(p.get("quantity") or 1)
    s = SessionLocal()
    try:
        ps = svc.create_set(s, model_code=model_code, name=name)
        sp = svc.add_product(s, set_id=ps.id, model_code=model_code, quantity=quantity)
        s.commit()
        return jsonify({"ok": True, "set_id": ps.id, "product_id": sp.id})
    finally:
        s.close()


@bp.get("/sets")
def list_sets():
    model_code = (request.args.get("model_code") or "").strip()
    if not model_code:
        return _err("model_code 가 필요해요.")
    s = SessionLocal()
    try:
        rows = svc.list_sets(s, model_code)
        return jsonify({"ok": True, "sets": [
            {"id": r.id, "name": r.name} for r in rows]})
    finally:
        s.close()


@bp.get("/sets/<int:set_id>")
def get_set(set_id):
    s = SessionLocal()
    try:
        detail = svc.get_set_detail(s, set_id)
        if not detail:
            return _err("구성을 찾을 수 없어요.", 404)
        return jsonify({"ok": True, "set": detail})
    finally:
        s.close()


@bp.post("/sets/<int:set_id>/options")
def set_set_options(set_id):
    p = request.get_json(silent=True) or {}
    set_product_id = p.get("set_product_id")
    skus = p.get("canonical_skus") or []
    if not set_product_id:
        return _err("set_product_id 가 필요해요.")
    s = SessionLocal()
    try:
        svc.set_options(s, set_product_id=int(set_product_id), canonical_skus=list(skus))
        s.commit()
        return jsonify({"ok": True, "count": len(skus)})
    finally:
        s.close()


@bp.delete("/sets/<int:set_id>")
def delete_set(set_id):
    s = SessionLocal()
    try:
        ok = svc.delete_set(s, set_id)
        s.commit()
        return (jsonify({"ok": True}) if ok else _err("구성을 찾을 수 없어요.", 404))
    finally:
        s.close()


# ── 단계 ③ 판매처 채널 ──
@bp.post("/sets/<int:set_id>/channels")
def add_channel(set_id):
    p = request.get_json(silent=True) or {}
    market = (p.get("market") or "").strip()
    if market not in ("smartstore", "coupang"):
        return _err("market 은 smartstore/coupang 중 하나여야 해요.")
    s = SessionLocal()
    try:
        c = ch.add_channel(s, set_id=set_id, market=market,
                           account_key=(p.get("account_key") or None))
        s.commit()
        return jsonify({"ok": True, "channel_id": c.id})
    finally:
        s.close()


@bp.delete("/channels/<int:channel_id>")
def remove_channel(channel_id):
    s = SessionLocal()
    try:
        ok = ch.remove_channel(s, channel_id)
        s.commit()
        return (jsonify({"ok": True}) if ok else _err("채널을 찾을 수 없어요.", 404))
    finally:
        s.close()


# ── 단계 ④ 마켓별 API 정보 + 연동 실행 ──
@bp.post("/channels/<int:channel_id>/product")
def set_channel_product(channel_id):
    p = request.get_json(silent=True) or {}
    product_id = str(p.get("market_product_id") or "").strip()
    if not product_id:
        return _err("market_product_id 가 필요해요.")
    s = SessionLocal()
    try:
        c = ch.set_channel_product(s, channel_id=channel_id,
                                   market_product_id=product_id,
                                   api_fields=p.get("api_fields"))
        if c is None:
            return _err("채널을 찾을 수 없어요.", 404)
        s.commit()
        return jsonify({"ok": True, "status": c.status})
    finally:
        s.close()


@bp.post("/channels/<int:channel_id>/link")
def link_channel(channel_id):
    s = SessionLocal()
    try:
        result = link.link_set_channel(s, channel_id)
        if not result.get("ok"):
            s.rollback()
            return _err(result.get("error") or "연동 실패", 400)
        s.commit()
        return jsonify(result)
    finally:
        s.close()
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd "C:\dev\모음전 프로젝트\프로그램\_시스템" && python -m pytest tests/sets/test_sets_api_routes.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: 블루프린트 등록** — `webapp/routes/__init__.py` 의 `register_routes()` 안에서:

(a) import 블록(`from webapp.routes.sourcing_guide import bp as sourcing_guide_bp` 줄 다음)에 추가:
```python
    from webapp.routes.sets_api import bp as sets_api_bp  # 구성(세트) 4단계 흐름 API
```
(b) register 블록(`app.register_blueprint(sourcing_guide_bp)` 줄 다음)에 추가:
```python
    app.register_blueprint(sets_api_bp)  # 구성(세트) 4단계 흐름 API
```

- [ ] **Step 6: register_routes import 스모크 + 전체 sets 스위트**

Run: `cd "C:\dev\모음전 프로젝트\프로그램\_시스템" && python -c "import ast; ast.parse(open('webapp/routes/__init__.py', encoding='utf-8').read()); print('__init__.py parses OK')"`
Expected: `__init__.py parses OK`

Run: `cd "C:\dev\모음전 프로젝트\프로그램\_시스템" && python -m pytest tests/sets/ -q`
Expected: 기존 19 + 신규 2 = 21 passed

- [ ] **Step 7: 커밋 (3 파일)**

```bash
cd "C:\dev\모음전 프로젝트"
git add "프로그램/_시스템/webapp/routes/sets_api.py" "프로그램/_시스템/webapp/routes/__init__.py" "프로그램/_시스템/tests/sets/test_sets_api_routes.py"
git commit -m "feat(sets): Phase3a 4단계 흐름 JSON 라우트(sets_api 블루프린트) + 등록

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 완료 후 — 다음 단계 (본 플랜 범위 밖)

- **Phase 3b (화면)**: 4단계 페이지 템플릿(검색바+행리스트 / 구성헤더+조합매트릭스 축선택 재사용 / 마켓별 계정그룹 / 마켓별 탭 폼). 라이브 브라우저 검증(`/ui-verify`). 단계④ 폼 필드 = `docs/markets/{market}.yaml` step4_form.
- **라이브 스모크**: 앱 실행 후 `curl /api/sets/search-bundles?q=...` 등으로 각 라우트 200 확인.
- **Phase 4 (P2 push)**: 매칭된 SetChannelOption(market_option_id)으로 재고/판매가 마켓 push.

---

## Self-Review

- **Spec 커버리지**: §15 단계①검색=search_bundles, 단계②구성=create_set/set_set_options/get_set/list_sets/delete_set, 단계③판매처=add_channel/remove_channel, 단계④API필드+연동=set_channel_product/link_channel. 9개 라우트 전부 4단계 매핑. ✅
- **Placeholder 스캔**: TBD/TODO 없음. 모든 라우트 실제 코드. 단, 라우트 행위 단위테스트는 이 프로젝트 구조상 부재(내부 SessionLocal) → 스모크(등록)+서비스테스트(밑단 19개)+라이브 검증으로 명시 대체. ✅
- **타입 일관성**: 서비스 시그니처와 호출 일치 — `create_set(model_code,name)`·`add_product(set_id,model_code,quantity)`·`set_options(set_product_id,canonical_skus)`·`list_sets(model_code)`·`get_set_detail(set_id)`·`delete_set(set_id)`·`add_channel(set_id,market,account_key)`·`set_channel_product(channel_id,market_product_id,api_fields)`·`remove_channel(channel_id)`·`link_set_channel(session,channel_id)`. `_err` 반환 `(jsonify, code)` 일관. 블루프린트명 sets_api, url_prefix=/api (api.py와 경로 비충돌: /sets·/channels). ✅
