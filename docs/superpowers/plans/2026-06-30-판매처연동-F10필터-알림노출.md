# F10 필터 + 알림 노출 + 소싱대조 알림 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans.

**Goal:** (5a) F10 필터(브랜드 OR + 마켓 OR, 그룹간 AND) · (5b) 알림 노출(카드 배지·M3 행끝·대시보드 요약) · (5c) 소싱대조 알림 3종(둘다0·미동기화·소싱변동).

**Architecture:** 백엔드 — `alert_service.alerts_for_set`에 3종 추가(src_stock_map 주입 시 both_zero 판정, 없으면 market_soldout으로만=개수동일). `set_service.list_linked_sets`에 per-set `alerts`(싼 호출=sets 테이블만)+per-product `brand` 추가. 신규 `GET /sets/<id>/alerts`(=_option_matrix_data로 src_stock_map 계산→both_zero 정밀). 프론트 — 대시보드 카드 배지(4)·M3 행끝 아이콘(9)·상단 요약(2 대용)·브랜드/마켓 칩 필터(F1형).

**Tech Stack:** Python/SQLAlchemy/Flask, pytest(in-memory SQLite), Vanilla JS.

**스코프 노트:** 위치 2(글로벌 사이드바 하단 카운트)는 모든 페이지 렌더에 비용 → 본 PR은 대시보드 페이지 상단 요약 카운트로 대체. 진짜 사이드바 글로벌 배지는 후속(api_sidebar 비용 검토). 소싱변동 알림(source_changed)은 작업6(source 이벤트 훅) 후 실제 점등.

---

### Task 1: alert_service 3종 추가 (TDD)

**Files:** Modify `lemouton/sets/alert_service.py` · Test `tests/sets/test_alert_service.py`(추가)

- [ ] Step 1: 실패 테스트 추가

```python
def _chan_fa(db, market, stock, fetched=None):
    from datetime import datetime, timezone
    ch = SetChannel(set_id=2, market=market, account_key="a", market_product_id="9", status="linked")
    db.add(ch); db.flush()
    db.add(SetChannelOption(channel_id=ch.id, canonical_sku="K", market_option_id="1",
        status="matched", mkt_stock=stock, mkt_price=1000, mkt_fetched_at=fetched))
    db.commit(); return ch


def test_both_zero_when_src_zero(db):
    from datetime import datetime, timezone
    _chan_fa(db, "smartstore", 0, datetime(2026,6,30,tzinfo=timezone.utc))
    al_list = al.alerts_for_set(db, 2, {"K": 0})
    assert any(a["type"]=="both_zero" and a["severity"]=="critical" for a in al_list)
    assert not any(a["type"]=="market_soldout" for a in al_list)


def test_market_soldout_when_src_nonzero(db):
    from datetime import datetime, timezone
    _chan_fa(db, "smartstore", 0, datetime(2026,6,30,tzinfo=timezone.utc))
    al_list = al.alerts_for_set(db, 2, {"K": 5})
    assert any(a["type"]=="market_soldout" for a in al_list)
    assert not any(a["type"]=="both_zero" for a in al_list)


def test_not_synced_when_never_fetched(db):
    _chan_fa(db, "smartstore", 3, None)
    assert any(a["type"]=="not_synced" and a["severity"]=="info" for a in al.alerts_for_set(db, 2))


def test_source_changed_after_last_fetch(db):
    from datetime import datetime, timezone
    _chan_fa(db, "smartstore", 3, datetime(2026,6,30,1,0,tzinfo=timezone.utc))
    db.add(ChannelChangeEvent(set_id=2, market="smartstore", canonical_sku="K",
        field="stock", source="source", prev_value=10, next_value=8,
        at=datetime(2026,6,30,2,0,tzinfo=timezone.utc)))
    db.commit()
    assert any(a["type"]=="source_changed" and a["severity"]=="warning" for a in al.alerts_for_set(db, 2))
```

- [ ] Step 2: 실패 확인 — `python -m pytest tests/sets/test_alert_service.py -v` → 4 FAIL
- [ ] Step 3: alert_service.py 전체 교체 (구현 — 아래 코드)
- [ ] Step 4: 통과 확인 — 기존 3 + 신규 4 = 7 PASS
- [ ] Step 5: 커밋 `feat(sets): 알림 3종(both_zero·not_synced·source_changed) 추가`

---

### Task 2: list_linked_sets brand+alerts (TDD)

**Files:** Modify `lemouton/sets/set_service.py` · Test `tests/sets/test_set_service.py`(추가)

- [ ] Step 1: 실패 테스트 — 기존 단품 구성에 `r["alerts"]` is list, `r["products"][0]["brand"]` 존재 단언
- [ ] Step 2: 실패 확인
- [ ] Step 3: products dict에 `"brand": getattr(m,"brand",None)`, set dict에 `"alerts": _alerts(session, ps.id)` 추가(상단 `from lemouton.sets.alert_service import alerts_for_set as _alerts`)
- [ ] Step 4: 통과 확인
- [ ] Step 5: 커밋 `feat(sets): 목록에 brand·alerts 요약 추가(F10필터·배지용)`

---

### Task 3: GET /sets/<id>/alerts 라우트

**Files:** Modify `webapp/routes/sets_api.py` · Test `tests/sets/test_sets_api_routes.py`(expected 추가)

- [ ] Step 1: expected에 `"/api/sets/<int:set_id>/alerts"` 추가 → FAIL
- [ ] Step 2: 라우트 구현(set_history_route 옆): _option_matrix_data로 src_stock_map(구성 옵션만, purchase면 purchase_stock) → alerts_for_set
- [ ] Step 3: 통과 확인
- [ ] Step 4: 커밋 `feat(sets): GET /sets/<id>/alerts(소싱대조 src_stock 주입)`

---

### Task 4: 프론트 — 알림 노출(4·9·2대용) + F10 필터

**Files:** Modify `webapp/templates/sets/dashboard.html` (JS, node --check)

- [ ] Step 1: 알림 헬퍼 — `alSev(alerts)`=최고 severity(critical>danger>warning>info), `alColor(sev)` 색맵(critical#b91c1c·danger#e03a3e·warning#B45309·info#185FA5), `alIcon`.
- [ ] Step 2: 카드 우상단 배지(4) — render에서 r.alerts 있으면 `<span class="sd-alert">⚠ N</span>`(색=최고severity) 카드 top에.
- [ ] Step 3: M3 행끝 아이콘(9) — m3Table 각 마켓행 끝 칸에 해당 market 알림 있으면 점 아이콘.
- [ ] Step 4: 상단 요약(2 대용) — 목록 렌더 후 전체 알림합을 sd-head 아래 요약줄로(`총 N건 주의`). 0이면 숨김.
- [ ] Step 5: F10 필터 — 검색바 아래 칩 바(브랜드 칩=목록 brand들 distinct, 마켓 칩=coupang/smartstore). data-fb/data-fm 토글. `applyFilter(sets)`=브랜드 OR(미선택=전체) AND 마켓 OR. render는 필터 통과분만. 칩 active 스타일.
- [ ] Step 6: node --check → 커밋 `feat(sets): 알림 노출(배지·행끝·요약)+F10 브랜드/마켓 필터`

---

## alert_service.py 전체 구현 (Task1 Step3)

```python
"""[구성 레이어] 알림 판정 — 저장 안 하고 현재 데이터에서 파생 계산.

유형 5종: market_soldout(danger 위험·판매재고0,쿠팡제외) / both_zero(critical 심각·소싱판매 둘다0)
 / not_synced(info 정보·미동기화) / source_changed(warning 주의·소싱변동→판매갱신) / price_spike(info 정보).
src_stock_map={canonical_sku:src_stock} 주입 시 both_zero 판정. 없으면 market_soldout 으로만(개수동일).
"""
from __future__ import annotations

from lemouton.sets.models import SetChannel, SetChannelOption, ChannelChangeEvent

PRICE_SPIKE_RATE = 0.2


def alerts_for_set(session, set_id, src_stock_map=None):
    """구성 알림 — [{type, severity, market, canonical_sku, message}]."""
    src = src_stock_map or {}
    out = []
    chans = session.query(SetChannel).filter_by(set_id=set_id).all()
    for ch in chans:
        scos = (session.query(SetChannelOption)
                .filter_by(channel_id=ch.id, status="matched").all())
        if ch.status == "linked" and ch.market_product_id and scos:
            if all(s.mkt_fetched_at is None for s in scos):
                out.append({"type": "not_synced", "severity": "info",
                            "market": ch.market, "canonical_sku": None,
                            "message": "마켓 현재값 미수집(미동기화)"})
        for sco in scos:
            if ch.market == "coupang" or sco.mkt_stock != 0:
                continue
            if src.get(sco.canonical_sku) == 0:
                out.append({"type": "both_zero", "severity": "critical",
                            "market": ch.market, "canonical_sku": sco.canonical_sku,
                            "message": "소싱·판매 둘 다 재고 0"})
            else:
                out.append({"type": "market_soldout", "severity": "danger",
                            "market": ch.market, "canonical_sku": sco.canonical_sku,
                            "message": "판매처 재고 0(품절)"})
        last_fetch = max((s.mkt_fetched_at for s in scos if s.mkt_fetched_at is not None),
                         default=None)
        if last_fetch is not None:
            recent_src = (session.query(ChannelChangeEvent)
                          .filter_by(set_id=set_id, market=ch.market, source="source")
                          .filter(ChannelChangeEvent.at > last_fetch).first())
            if recent_src is not None:
                out.append({"type": "source_changed", "severity": "warning",
                            "market": ch.market, "canonical_sku": None,
                            "message": "소싱 변동 — 판매처 갱신 필요"})
        last = (session.query(ChannelChangeEvent)
                .filter_by(set_id=set_id, market=ch.market, field="price")
                .order_by(ChannelChangeEvent.at.desc()).first())
        if last and last.prev_value and last.next_value is not None:
            rate = abs(last.next_value - last.prev_value) / last.prev_value
            if rate >= PRICE_SPIKE_RATE:
                out.append({"type": "price_spike", "severity": "info",
                            "market": ch.market, "canonical_sku": last.canonical_sku,
                            "message": "가격 급변 %d%%" % round(rate * 100)})
    return out
```
