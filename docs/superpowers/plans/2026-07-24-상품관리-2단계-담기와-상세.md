# 상품관리 2단계 — 상품 담기 · 묶기 · 상세 조회

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 캐시된 28만 건에서 찾아 「모음전 상품」으로 담고, 흩어진 마켓 상품을 하나로 묶고, 담은 상품을 마켓별로 나란히 본다.

**Architecture:** 1단계에서 만든 `market_products` 캐시를 읽기만 한다. 담기 = `market_product_groups` 1행 + `market_products.group_id` 설정. 화면은 확정 시안 ④(치는 대로 검색)·⑤(대표 정하고 붙이기)·①(그 자리 펼침)·⑥(마켓마다 카드 한 장).

**Tech Stack:** Flask · SQLAlchemy · PostgreSQL(ILIKE) · pytest · node(화면 렌더)

**설계서:** `docs/superpowers/specs/2026-07-23-모음전-상품관리-design.md` §6
**1단계:** `docs/superpowers/plans/2026-07-24-상품관리-1단계-캐시와-대시보드.md`

---

## 이 계획에 들어가지 않는 것 — ⑦ 편집·마켓 전송

확정 시안 ⑦(고치기 → 확인 → 보내기)은 **3단계로 뺀다.** 이유:

- 마켓에 **쓰는** 일이라 6마켓 수정 API 를 각각 배선해야 한다(1단계 조회와 규모가 다르다)
- 잘못 보내면 손님 화면이 바로 바뀐다 — `LIVE_*` 2중잠금 정책을 새로 얹어야 한다
- 2단계(읽기만)를 먼저 써보시면 3단계에서 무엇을 고칠지가 분명해진다

**2단계는 조회 전용이다.** 마켓에 한 글자도 쓰지 않는다. 상세 화면의 「고치기」 자리는 **비활성 버튼 + 「3단계에서 열립니다」** 로 둔다 — 눌러도 아무 일이 안 일어나는 버튼(거짓 기능)은 만들지 않는다.

---

## 파일 구조

| 파일 | 책임 |
|---|---|
| `lemouton/catalog/search.py` | 캐시 검색 — 이름·브랜드·상품번호 |
| `lemouton/catalog/groups.py` | 묶음 만들기·붙이기·떼기 |
| `webapp/routes/catalog/pick.py` | 검색·담기 API |
| `webapp/routes/catalog/detail.py` | 담은 상품 목록·상세 API |
| `webapp/templates/catalog/partials/_pick.html` | 상품 담기 화면 (④⑤) |
| `webapp/templates/catalog/partials/_detail.html` | 상품 상세 화면 (①⑥) |

---

## Task 1: 캐시 검색

**Files:**
- Create: `프로그램/_시스템/lemouton/catalog/search.py`
- Test: `프로그램/_시스템/tests/catalog/test_search.py`

- [ ] **Step 1: 실패하는 테스트**

```python
# tests/catalog/test_search.py
# -*- coding: utf-8 -*-
"""캐시 검색 — 이름·브랜드·상품번호. 마켓에 묻지 않고 우리 DB 에서만."""
import sqlalchemy as sa
from sqlalchemy.orm import sessionmaker

from lemouton.catalog import search as S
from lemouton.catalog.models import (
    MarketProduct, MarketProductCount, MarketProductGroup,
)
from shared.db import Base


def _session():
    engine = sa.create_engine('sqlite://')
    Base.metadata.create_all(engine, tables=[
        MarketProduct.__table__, MarketProductCount.__table__,
        MarketProductGroup.__table__,
    ])
    return sessionmaker(bind=engine)()


def _add(s, **kw):
    base = dict(market='lotteon', account_key='브랜드위시',
                market_product_id='LO1', name='아디다스골프 썬 햇 모자',
                brand='아디다스', status='sale', sale_price=31900)
    base.update(kw)
    s.add(MarketProduct(**base))
    s.commit()


def test_상품명_일부로_찾는다():
    s = _session()
    _add(s, market_product_id='LO1', name='아디다스골프 썬 햇 모자')
    _add(s, market_product_id='LO2', name='나이키 조던 팬츠', brand='나이키')
    out = S.search(s, '썬 햇')
    assert [r['market_product_id'] for r in out['rows']] == ['LO1']
    assert out['total'] == 1


def test_대소문자_상관없이_찾는다():
    s = _session()
    _add(s, market_product_id='LO1', name='FILA 페이토 샌들')
    assert S.search(s, 'fila')['total'] == 1


def test_브랜드로도_찾는다():
    s = _session()
    _add(s, market_product_id='LO1', name='썬 햇', brand='아디다스')
    assert S.search(s, '아디다스')['total'] == 1


def test_상품번호로_정확히_찾는다():
    s = _session()
    _add(s, market_product_id='LO2727575855', name='썬 햇')
    assert S.search(s, 'LO2727575855')['total'] == 1


def test_지운_상품은_안_나온다():
    """마켓에서 사라진 것을 담으라고 보여주면 안 된다."""
    from datetime import datetime, timezone
    s = _session()
    _add(s, market_product_id='LO1', name='썬 햇')
    r = s.query(MarketProduct).one()
    r.deleted_at = datetime.now(timezone.utc)
    s.commit()
    assert S.search(s, '썬 햇')['total'] == 0


def test_마켓으로_좁힐_수_있다():
    s = _session()
    _add(s, market='lotteon', market_product_id='LO1', name='썬 햇')
    _add(s, market='coupang', market_product_id='CP1', name='썬 햇')
    assert S.search(s, '썬 햇', market='coupang')['total'] == 1


def test_상태로_좁힐_수_있다():
    s = _session()
    _add(s, market_product_id='LO1', name='썬 햇', status='sale')
    _add(s, market_product_id='LO2', name='썬 햇 둘', status='soldout')
    assert S.search(s, '썬 햇', status='soldout')['total'] == 1


def test_이미_담은_것만_또는_아직_안_담은_것만():
    s = _session()
    g = MarketProductGroup(name='묶음'); s.add(g); s.commit()
    _add(s, market_product_id='LO1', name='썬 햇', group_id=g.id)
    _add(s, market_product_id='LO2', name='썬 햇 둘')
    assert S.search(s, '썬 햇', picked=True)['total'] == 1
    assert S.search(s, '썬 햇', picked=False)['total'] == 1


def test_검색어가_없으면_최근_것을_보여준다():
    """빈 화면 대신 뭐라도 보여야 사장님이 시작할 수 있다."""
    s = _session()
    _add(s, market_product_id='LO1', name='가')
    _add(s, market_product_id='LO2', name='나')
    out = S.search(s, '')
    assert out['total'] == 2


def test_한_번에_너무_많이_돌려주지_않는다():
    """28만 건을 통째로 보내면 화면이 멈춘다."""
    s = _session()
    for i in range(120):
        _add(s, market_product_id=f'LO{i}', name=f'상품 {i}')
    out = S.search(s, '상품', limit=50)
    assert len(out['rows']) == 50
    assert out['total'] == 120          # 총 몇 건인지는 알려준다


def test_결과에_화면이_필요한_것이_다_들어있다():
    s = _session()
    _add(s, market_product_id='LO1', name='썬 햇', brand='아디다스',
         status='sale', sale_price=31900)
    r = S.search(s, '썬 햇')['rows'][0]
    for k in ('id', 'market', 'account_key', 'market_product_id', 'name',
              'brand', 'status', 'sale_price', 'group_id'):
        assert k in r, k
```

- [ ] **Step 2: 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/catalog/test_search.py -q`
Expected: FAIL — `ModuleNotFoundError: lemouton.catalog.search`

- [ ] **Step 3: 구현**

```python
# lemouton/catalog/search.py
# -*- coding: utf-8 -*-
"""캐시 검색 — 마켓에 묻지 않고 우리 DB 에서만 찾는다.

★ 6마켓 중 4곳이 상품명 검색을 못 한다(스마트스토어·롯데온은 아예 불가,
  옥션·G마켓은 조건을 무시). 그래서 캐시에서 찾는 이 길이 유일하다.
  1단계 실측 근거: docs/superpowers/specs/2026-07-23-모음전-상품관리-design.md §2-2
"""
from __future__ import annotations

from typing import Optional

from sqlalchemy import or_

from .models import MarketProduct

#: 한 번에 돌려줄 최대 건수. 28만 건을 통째로 보내면 화면이 멈춘다.
DEFAULT_LIMIT = 50
MAX_LIMIT = 200


def search(session, q: str = '', *, market: Optional[str] = None,
           account_key: Optional[str] = None, status: Optional[str] = None,
           picked: Optional[bool] = None,
           limit: int = DEFAULT_LIMIT, offset: int = 0) -> dict:
    """캐시에서 상품을 찾는다.

    Args:
        q: 상품명·브랜드 일부, 또는 상품번호 전체. 비면 최근 것을 보여준다.
        picked: True=이미 담은 것만 · False=아직 안 담은 것만 · None=전부
    """
    limit = max(1, min(int(limit or DEFAULT_LIMIT), MAX_LIMIT))
    qq = (q or '').strip()

    base = session.query(MarketProduct).filter(
        MarketProduct.deleted_at.is_(None))
    if market:
        base = base.filter(MarketProduct.market == market)
    if account_key:
        base = base.filter(MarketProduct.account_key == account_key)
    if status:
        base = base.filter(MarketProduct.status == status)
    if picked is True:
        base = base.filter(MarketProduct.group_id.isnot(None))
    elif picked is False:
        base = base.filter(MarketProduct.group_id.is_(None))
    if qq:
        like = f'%{qq}%'
        base = base.filter(or_(
            MarketProduct.name.ilike(like),
            MarketProduct.brand.ilike(like),
            MarketProduct.market_product_id == qq,
            MarketProduct.site_product_id == qq,
        ))

    total = base.count()
    rows = (base.order_by(MarketProduct.id.desc())
            .offset(max(0, int(offset or 0))).limit(limit).all())
    return {'total': total, 'rows': [_row(r) for r in rows],
            'limit': limit, 'offset': int(offset or 0)}


def _row(m: MarketProduct) -> dict:
    return {
        'id': m.id, 'market': m.market, 'account_key': m.account_key,
        'market_product_id': m.market_product_id,
        'site_product_id': m.site_product_id,
        'name': m.name, 'brand': m.brand, 'status': m.status,
        'sale_price': m.sale_price, 'group_id': m.group_id,
    }
```

- [ ] **Step 4: 통과 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/catalog/test_search.py -q`
Expected: PASS (실패 0)

- [ ] **Step 5: 커밋**

```bash
git add 프로그램/_시스템/lemouton/catalog/search.py 프로그램/_시스템/tests/catalog/test_search.py
git commit -m "feat(상품관리): 캐시 검색 — 마켓 4곳이 검색을 못 해 이 길이 유일하다"
```

---

## Task 2: 묶기

**Files:**
- Create: `프로그램/_시스템/lemouton/catalog/groups.py`
- Test: `프로그램/_시스템/tests/catalog/test_groups.py`

- [ ] **Step 1: 실패하는 테스트**

```python
# tests/catalog/test_groups.py
# -*- coding: utf-8 -*-
"""묶기 — 흩어진 마켓 상품을 한 상품으로.

확정 시안 ⑤ 「대표를 정하고 붙이기」: 기준 상품 하나를 세우고 나머지를 붙인다.
"""
import pytest
import sqlalchemy as sa
from sqlalchemy.orm import sessionmaker

from lemouton.catalog import groups as G
from lemouton.catalog.models import (
    MarketProduct, MarketProductCount, MarketProductGroup,
)
from shared.db import Base


def _session():
    engine = sa.create_engine('sqlite://')
    Base.metadata.create_all(engine, tables=[
        MarketProduct.__table__, MarketProductCount.__table__,
        MarketProductGroup.__table__,
    ])
    return sessionmaker(bind=engine)()


def _add(s, pid, **kw):
    base = dict(market='lotteon', account_key='브랜드위시',
                market_product_id=pid, name=f'상품 {pid}',
                brand='아디다스', status='sale')
    base.update(kw)
    m = MarketProduct(**base)
    s.add(m); s.commit()
    return m


def test_대표를_정하면_묶음이_생기고_그_상품이_붙는다():
    s = _session()
    a = _add(s, 'LO1', name='아디다스골프 썬 햇 모자')
    g = G.create_group(s, leader_id=a.id)
    assert g['name'] == '아디다스골프 썬 햇 모자'
    assert g['brand'] == '아디다스'
    assert s.query(MarketProduct).get(a.id).group_id == g['id']


def test_이름을_직접_줄_수도_있다():
    s = _session()
    a = _add(s, 'LO1', name='&lt;매장정품&gt; 어쩌고')
    g = G.create_group(s, leader_id=a.id, name='아디다스 썬 햇')
    assert g['name'] == '아디다스 썬 햇'


def test_나머지를_붙인다():
    s = _session()
    a = _add(s, 'LO1'); b = _add(s, 'CP1', market='coupang')
    c = _add(s, 'E1', market='eleven11')
    g = G.create_group(s, leader_id=a.id)
    n = G.attach(s, g['id'], [b.id, c.id])
    assert n == 2
    assert G.get_group(s, g['id'])['member_count'] == 3


def test_뗄_수도_있다():
    s = _session()
    a = _add(s, 'LO1'); b = _add(s, 'CP1', market='coupang')
    g = G.create_group(s, leader_id=a.id)
    G.attach(s, g['id'], [b.id])
    assert G.detach(s, [b.id]) == 1
    assert s.query(MarketProduct).get(b.id).group_id is None
    assert G.get_group(s, g['id'])['member_count'] == 1


def test_이미_다른_묶음에_있으면_옮기고_알려준다():
    """★ 조용히 두 곳에 속하면 어느 쪽이 진짜인지 알 수 없다."""
    s = _session()
    a = _add(s, 'LO1'); b = _add(s, 'CP1', market='coupang')
    c = _add(s, 'E1', market='eleven11')
    g1 = G.create_group(s, leader_id=a.id)
    G.attach(s, g1['id'], [b.id])
    g2 = G.create_group(s, leader_id=c.id)
    r = G.attach(s, g2['id'], [b.id], detail=True)
    assert r['moved'] == [{'market_product_id': 'CP1', 'from_group_id': g1['id']}]
    assert s.query(MarketProduct).get(b.id).group_id == g2['id']


def test_없는_상품을_붙이려_하면_알려준다():
    s = _session()
    a = _add(s, 'LO1')
    g = G.create_group(s, leader_id=a.id)
    with pytest.raises(ValueError, match='없는 상품'):
        G.attach(s, g['id'], [99999])


def test_없는_묶음에_붙이려_하면_알려준다():
    s = _session()
    a = _add(s, 'LO1')
    with pytest.raises(ValueError, match='없는 묶음'):
        G.attach(s, 99999, [a.id])


def test_지운_상품은_대표로_못_세운다():
    """마켓에서 사라진 것을 대표로 삼으면 빈 묶음이 된다."""
    from datetime import datetime, timezone
    s = _session()
    a = _add(s, 'LO1')
    a.deleted_at = datetime.now(timezone.utc); s.commit()
    with pytest.raises(ValueError, match='사라진 상품'):
        G.create_group(s, leader_id=a.id)


def test_묶음_목록은_붙은_상품과_마켓을_같이_준다():
    s = _session()
    a = _add(s, 'LO1'); b = _add(s, 'CP1', market='coupang')
    g = G.create_group(s, leader_id=a.id)
    G.attach(s, g['id'], [b.id])
    lst = G.list_groups(s)
    assert lst['total'] == 1
    row = lst['rows'][0]
    assert row['member_count'] == 2
    assert sorted(row['markets']) == ['coupang', 'lotteon']


def test_묶음_상세는_마켓별_카드에_필요한_것을_준다():
    """확정 시안 ⑥ — 마켓마다 카드 한 장."""
    s = _session()
    a = _add(s, 'LO1', sale_price=31900)
    b = _add(s, 'CP1', market='coupang', sale_price=32900, status='stopped')
    g = G.create_group(s, leader_id=a.id)
    G.attach(s, g['id'], [b.id])
    d = G.get_group(s, g['id'])
    got = {m['market']: m for m in d['members']}
    assert got['lotteon']['sale_price'] == 31900
    assert got['coupang']['status'] == 'stopped'
    assert got['coupang']['market_product_id'] == 'CP1'


def test_묶음을_지우면_붙었던_상품이_풀린다():
    """★ 상품까지 지우면 안 된다 — 마켓엔 그대로 있다."""
    s = _session()
    a = _add(s, 'LO1')
    g = G.create_group(s, leader_id=a.id)
    G.delete_group(s, g['id'])
    assert s.query(MarketProduct).get(a.id).group_id is None
    assert s.query(MarketProduct).count() == 1
```

- [ ] **Step 2: 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/catalog/test_groups.py -q`
Expected: FAIL — `ModuleNotFoundError: lemouton.catalog.groups`

- [ ] **Step 3: 구현**

```python
# lemouton/catalog/groups.py
# -*- coding: utf-8 -*-
"""묶기 — 흩어진 마켓 상품을 한 상품으로.

확정 시안 ⑤ 「대표를 정하고 붙이기」. 사장님이 기준 상품 하나를 세우고
나머지를 거기에 붙인다. 프로그램은 후보만 보여주고 확정은 사장님이 한다.
"""
from __future__ import annotations

from datetime import datetime, timezone
from typing import Optional

from .models import MarketProduct, MarketProductGroup


def _now():
    return datetime.now(timezone.utc)


def _alive(session, ids: list):
    rows = session.query(MarketProduct).filter(
        MarketProduct.id.in_(list(ids)),
        MarketProduct.deleted_at.is_(None)).all()
    found = {r.id for r in rows}
    missing = [i for i in ids if i not in found]
    if missing:
        raise ValueError(f"없는 상품이 섞여 있습니다: {missing}")
    return rows


def create_group(session, *, leader_id: int, name: Optional[str] = None,
                 brand: Optional[str] = None) -> dict:
    """대표 상품 하나로 묶음을 만든다. 이름·브랜드는 대표에서 가져온다."""
    m = session.query(MarketProduct).get(leader_id)
    if m is None:
        raise ValueError(f"없는 상품입니다: {leader_id}")
    if m.deleted_at is not None:
        raise ValueError("마켓에서 사라진 상품은 대표로 세울 수 없습니다.")
    g = MarketProductGroup(name=(name or m.name or f'상품 {m.market_product_id}'),
                           brand=(brand or m.brand))
    session.add(g)
    session.flush()
    m.group_id = g.id
    session.commit()
    return get_group(session, g.id)


def attach(session, group_id: int, ids: list, *, detail: bool = False):
    """묶음에 마켓 상품을 붙인다.

    ★ 이미 다른 묶음에 있으면 **옮기고 알려준다.** 조용히 두 곳에 속하면
      어느 쪽이 진짜인지 알 수 없다.
    """
    g = session.query(MarketProductGroup).get(group_id)
    if g is None or g.deleted_at is not None:
        raise ValueError(f"없는 묶음입니다: {group_id}")
    rows = _alive(session, ids)
    moved = []
    for m in rows:
        if m.group_id is not None and m.group_id != group_id:
            moved.append({'market_product_id': m.market_product_id,
                          'from_group_id': m.group_id})
        m.group_id = group_id
    g.updated_at = _now()
    session.commit()
    if detail:
        return {'attached': len(rows), 'moved': moved}
    return len(rows)


def detach(session, ids: list) -> int:
    """묶음에서 뗀다. 상품 자체는 남는다 — 마켓엔 그대로 있다."""
    rows = session.query(MarketProduct).filter(
        MarketProduct.id.in_(list(ids))).all()
    n = 0
    for m in rows:
        if m.group_id is not None:
            m.group_id = None
            n += 1
    session.commit()
    return n


def delete_group(session, group_id: int) -> bool:
    """묶음을 지운다. ★ 붙었던 상품은 풀어줄 뿐 지우지 않는다."""
    g = session.query(MarketProductGroup).get(group_id)
    if g is None:
        return False
    for m in session.query(MarketProduct).filter_by(group_id=group_id).all():
        m.group_id = None
    g.deleted_at = _now()
    session.commit()
    return True


def get_group(session, group_id: int) -> Optional[dict]:
    """묶음 1건 — 마켓별 카드에 필요한 것까지(확정 시안 ⑥)."""
    g = session.query(MarketProductGroup).get(group_id)
    if g is None or g.deleted_at is not None:
        return None
    members = session.query(MarketProduct).filter(
        MarketProduct.group_id == group_id,
        MarketProduct.deleted_at.is_(None)).order_by(MarketProduct.market).all()
    return {
        'id': g.id, 'name': g.name, 'brand': g.brand,
        'member_count': len(members),
        'markets': sorted({m.market for m in members}),
        'members': [{
            'id': m.id, 'market': m.market, 'account_key': m.account_key,
            'market_product_id': m.market_product_id,
            'site_product_id': m.site_product_id,
            'name': m.name, 'status': m.status, 'sale_price': m.sale_price,
            'synced_at': m.synced_at.isoformat() if m.synced_at else None,
        } for m in members],
    }


def list_groups(session, *, q: str = '', limit: int = 50,
                offset: int = 0) -> dict:
    """담아둔 모음전 상품 목록."""
    base = session.query(MarketProductGroup).filter(
        MarketProductGroup.deleted_at.is_(None))
    qq = (q or '').strip()
    if qq:
        like = f'%{qq}%'
        base = base.filter(MarketProductGroup.name.ilike(like))
    total = base.count()
    gs = (base.order_by(MarketProductGroup.id.desc())
          .offset(max(0, int(offset or 0))).limit(max(1, int(limit or 50))).all())
    ids = [g.id for g in gs]
    members = {}
    if ids:
        for m in session.query(MarketProduct).filter(
                MarketProduct.group_id.in_(ids),
                MarketProduct.deleted_at.is_(None)).all():
            members.setdefault(m.group_id, []).append(m)
    rows = []
    for g in gs:
        ms = members.get(g.id, [])
        prices = [m.sale_price for m in ms if m.sale_price is not None]
        rows.append({
            'id': g.id, 'name': g.name, 'brand': g.brand,
            'member_count': len(ms),
            'markets': sorted({m.market for m in ms}),
            # 마켓마다 값이 다를 수 있어 한 값으로 뭉개지 않는다 — 범위로 보여준다
            'price_min': min(prices) if prices else None,
            'price_max': max(prices) if prices else None,
            'has_soldout': any(m.status == 'soldout' for m in ms),
            'has_stopped': any(m.status == 'stopped' for m in ms),
        })
    return {'total': total, 'rows': rows}
```

- [ ] **Step 4: 통과 확인 + 커밋**

Run: `cd 프로그램/_시스템 && python -m pytest tests/catalog/test_groups.py -q`
Expected: PASS (실패 0)

```bash
git add 프로그램/_시스템/lemouton/catalog/groups.py 프로그램/_시스템/tests/catalog/test_groups.py
git commit -m "feat(상품관리): 묶기 — 대표를 정하고 붙이기, 두 곳에 속하면 옮기고 알린다"
```

---

## Task 3: 검색·담기·상세 API

**Files:**
- Create: `프로그램/_시스템/webapp/routes/catalog/pick.py`
- Modify: `프로그램/_시스템/webapp/routes/catalog/__init__.py`
- Test: `프로그램/_시스템/tests/catalog/test_pick_api.py`

- [ ] **Step 1: 실패하는 테스트**

```python
# tests/catalog/test_pick_api.py
# -*- coding: utf-8 -*-
"""검색·담기 API 의 입력 검증 — 잘못된 입력을 조용히 넘기지 않는다."""
import pytest

from webapp.routes.catalog.pick import parse_search_args, parse_ids


def test_검색어와_거르개를_읽는다():
    a = parse_search_args({'q': ' 아디다스 ', 'market': 'lotteon',
                           'status': 'sale', 'picked': 'false', 'limit': '30'})
    assert a['q'] == '아디다스'
    assert a['market'] == 'lotteon'
    assert a['status'] == 'sale'
    assert a['picked'] is False
    assert a['limit'] == 30


def test_picked_는_세_가지다():
    assert parse_search_args({'picked': 'true'})['picked'] is True
    assert parse_search_args({'picked': 'false'})['picked'] is False
    assert parse_search_args({})['picked'] is None


def test_모르는_마켓은_거절한다():
    """★ 조용히 무시하면 전체가 나와 '검색이 됐다'고 착각한다."""
    with pytest.raises(ValueError, match='모르는 마켓'):
        parse_search_args({'market': '없는마켓'})


def test_모르는_상태는_거절한다():
    with pytest.raises(ValueError, match='모르는 상태'):
        parse_search_args({'status': '팔림'})


def test_개수는_상한을_넘지_않는다():
    assert parse_search_args({'limit': '99999'})['limit'] == 200
    assert parse_search_args({'limit': '0'})['limit'] == 1
    assert parse_search_args({'limit': '가나다'})['limit'] == 50


def test_상품번호_목록을_읽는다():
    assert parse_ids({'ids': [1, 2, 3]}) == [1, 2, 3]
    assert parse_ids({'ids': ['1', '2']}) == [1, 2]


def test_빈_목록은_거절한다():
    """0개를 붙이라고 하면 사장님이 뭔가 잘못 누른 것이다."""
    with pytest.raises(ValueError, match='고른 상품이 없습니다'):
        parse_ids({'ids': []})
    with pytest.raises(ValueError, match='고른 상품이 없습니다'):
        parse_ids({})


def test_숫자가_아니면_거절한다():
    with pytest.raises(ValueError, match='숫자'):
        parse_ids({'ids': ['가나다']})


def test_한_번에_너무_많이_붙이지_않는다():
    with pytest.raises(ValueError, match='한 번에'):
        parse_ids({'ids': list(range(300))})
```

- [ ] **Step 2: 실패 확인 → Step 3: 구현**

```python
# webapp/routes/catalog/pick.py
# -*- coding: utf-8 -*-
"""검색·담기 API.

★ 잘못된 입력을 조용히 무시하지 않는다. 모르는 마켓을 넘겼는데 전체가 나오면
  사장님은 「검색이 됐다」고 믿는다(ESM 이 정확히 그래서 사고가 났다).
"""
from flask import jsonify, request

from shared.db import SessionLocal

from . import bp

MARKETS = ('smartstore', 'coupang', 'lotteon', 'eleven11', 'auction', 'gmarket')
STATUSES = ('sale', 'soldout', 'stopped', 'waiting', 'unknown')
MAX_IDS = 200


def parse_search_args(args: dict) -> dict:
    """조회 파라미터 검증. 모르는 값은 거절한다."""
    from lemouton.catalog.search import DEFAULT_LIMIT, MAX_LIMIT

    market = (args.get('market') or '').strip() or None
    if market and market not in MARKETS:
        raise ValueError(f'모르는 마켓입니다: {market}')
    status = (args.get('status') or '').strip() or None
    if status and status not in STATUSES:
        raise ValueError(f'모르는 상태입니다: {status}')

    raw_picked = (args.get('picked') or '').strip().lower()
    picked = True if raw_picked == 'true' else (
        False if raw_picked == 'false' else None)

    try:
        limit = int(args.get('limit') or DEFAULT_LIMIT)
    except (TypeError, ValueError):
        limit = DEFAULT_LIMIT
    limit = max(1, min(limit, MAX_LIMIT))
    try:
        offset = max(0, int(args.get('offset') or 0))
    except (TypeError, ValueError):
        offset = 0

    return {'q': (args.get('q') or '').strip(), 'market': market,
            'account_key': (args.get('account_key') or '').strip() or None,
            'status': status, 'picked': picked,
            'limit': limit, 'offset': offset}


def parse_ids(body: dict) -> list:
    """고른 상품 번호 목록. 비었거나 숫자가 아니면 거절한다."""
    raw = (body or {}).get('ids') or []
    if not isinstance(raw, list) or not raw:
        raise ValueError('고른 상품이 없습니다.')
    if len(raw) > MAX_IDS:
        raise ValueError(f'한 번에 {MAX_IDS}개까지만 담을 수 있습니다.')
    out = []
    for v in raw:
        try:
            out.append(int(v))
        except (TypeError, ValueError):
            raise ValueError(f'상품 번호는 숫자여야 합니다: {v!r}')
    return out


@bp.get('/api/search')
def api_search():
    from lemouton.catalog.search import search
    try:
        a = parse_search_args(request.args)
    except ValueError as e:
        return jsonify({'error': str(e)}), 400
    s = SessionLocal()
    try:
        return jsonify(search(s, a['q'], market=a['market'],
                              account_key=a['account_key'], status=a['status'],
                              picked=a['picked'], limit=a['limit'],
                              offset=a['offset']))
    except Exception as e:      # noqa: BLE001
        return jsonify({'error': 'search_failed', 'detail': str(e)[:300]}), 500
    finally:
        s.close()


@bp.post('/api/groups')
def api_create_group():
    """대표를 정해 묶음을 만들고, 함께 고른 것이 있으면 같이 붙인다."""
    from lemouton.catalog import groups as G

    body = request.get_json(silent=True) or {}
    try:
        leader_id = int(body.get('leader_id'))
    except (TypeError, ValueError):
        return jsonify({'ok': False, 'error': '대표 상품을 골라주세요.'}), 400
    s = SessionLocal()
    try:
        g = G.create_group(s, leader_id=leader_id,
                           name=(body.get('name') or '').strip() or None)
        others = [i for i in (body.get('ids') or []) if int(i) != leader_id]
        moved = []
        if others:
            r = G.attach(s, g['id'], [int(i) for i in others], detail=True)
            moved = r['moved']
            g = G.get_group(s, g['id'])
        return jsonify({'ok': True, 'group': g, 'moved': moved})
    except ValueError as e:
        return jsonify({'ok': False, 'error': str(e)}), 400
    except Exception as e:      # noqa: BLE001
        return jsonify({'ok': False, 'error': str(e)[:300]}), 500
    finally:
        s.close()


@bp.post('/api/groups/<int:group_id>/attach')
def api_attach(group_id: int):
    from lemouton.catalog import groups as G
    try:
        ids = parse_ids(request.get_json(silent=True) or {})
    except ValueError as e:
        return jsonify({'ok': False, 'error': str(e)}), 400
    s = SessionLocal()
    try:
        r = G.attach(s, group_id, ids, detail=True)
        return jsonify({'ok': True, 'attached': r['attached'],
                        'moved': r['moved'], 'group': G.get_group(s, group_id)})
    except ValueError as e:
        return jsonify({'ok': False, 'error': str(e)}), 400
    finally:
        s.close()


@bp.post('/api/groups/<int:group_id>/detach')
def api_detach(group_id: int):
    from lemouton.catalog import groups as G
    try:
        ids = parse_ids(request.get_json(silent=True) or {})
    except ValueError as e:
        return jsonify({'ok': False, 'error': str(e)}), 400
    s = SessionLocal()
    try:
        n = G.detach(s, ids)
        return jsonify({'ok': True, 'detached': n,
                        'group': G.get_group(s, group_id)})
    finally:
        s.close()


@bp.get('/api/groups')
def api_list_groups():
    from lemouton.catalog import groups as G
    s = SessionLocal()
    try:
        return jsonify(G.list_groups(
            s, q=(request.args.get('q') or '').strip(),
            limit=int(request.args.get('limit') or 50),
            offset=int(request.args.get('offset') or 0)))
    except Exception as e:      # noqa: BLE001
        return jsonify({'error': 'list_failed', 'detail': str(e)[:300]}), 500
    finally:
        s.close()


@bp.get('/api/groups/<int:group_id>')
def api_get_group(group_id: int):
    from lemouton.catalog import groups as G
    s = SessionLocal()
    try:
        g = G.get_group(s, group_id)
        if g is None:
            return jsonify({'error': '없는 묶음입니다.'}), 404
        return jsonify(g)
    finally:
        s.close()


@bp.delete('/api/groups/<int:group_id>')
def api_delete_group(group_id: int):
    from lemouton.catalog import groups as G
    s = SessionLocal()
    try:
        return jsonify({'ok': G.delete_group(s, group_id)})
    finally:
        s.close()
```

`webapp/routes/catalog/__init__.py` 맨 아래에 추가:

```python
from . import pick  # noqa: E402,F401  (검색·담기·묶기 API)
```

- [ ] **Step 4: 통과 확인 + 커밋**

Run: `cd 프로그램/_시스템 && python -m pytest tests/catalog/ -q`

```bash
git add 프로그램/_시스템/webapp/routes/catalog/ 프로그램/_시스템/tests/catalog/test_pick_api.py
git commit -m "feat(상품관리): 검색·담기·묶기 API — 모르는 값은 거절(조용한 전체반환 금지)"
```

---

## Task 4: 상품 담기 화면 (④⑤)

**Files:**
- Create: `프로그램/_시스템/webapp/templates/catalog/partials/_pick.html`
- Modify: `프로그램/_시스템/webapp/templates/catalog/index.html`
- Test: `프로그램/_시스템/tests/catalog/test_pick_render.js`

확정 시안: **④ 치는 대로 결과가 바뀜 + 오른쪽에 담을 목록 / ⑤ 대표를 정하고 붙이기**

- [ ] **Step 1: 화면**

좌우 2단. 왼쪽 = 검색 결과, 오른쪽 = 담을 목록.
- 검색창에 글자를 치면 250ms 뒤 자동 조회(「찾기」 버튼 없음)
- 결과 줄을 누르면 오른쪽으로 담긴다
- 오른쪽 맨 위 = **대표**(첫 번째로 담은 것). 「대표로」 버튼으로 바꾼다
- 「이대로 담기」 → `POST /api/groups {leader_id, ids}`
- 이미 다른 묶음에 있던 것은 담긴 뒤 **「N개를 여기로 옮겼습니다」** 로 알린다

- [ ] **Step 2: node 로 렌더 확인** (1단계 `test_dashboard_render.js` 와 같은 방식)

세 경우: 결과 있음 / 검색 결과 0건 / 담을 목록 비어 있음

- [ ] **Step 3: 커밋**

---

## Task 5: 상품 상세 화면 (①⑥)

**Files:**
- Create: `프로그램/_시스템/webapp/templates/catalog/partials/_detail.html`
- Test: `프로그램/_시스템/tests/catalog/test_detail_render.js`

확정 시안: **① 그 자리 펼침(나머지가 얇아짐) + ⑥ 마켓마다 카드 한 장**

- [ ] **Step 1: 화면**

- 담아둔 모음전 상품이 목록으로. 줄을 누르면 **그 자리에서 펼쳐지고 나머지 줄은 한 줄로 얇아진다**
  - ⚠️ 접힌 줄에도 **상품명과 가격 범위는 남긴다** — 뭐가 있었는지 사라지면 안 된다
- 펼친 칸 안에 **마켓마다 카드 한 장**을 옆으로 나란히
  - 카드 = 마켓 색띠 머리글 + 마켓 상품명 · 판매가 · 상태 · 상품번호 · 마지막 확인
  - 「마켓 상품 페이지 ↗」 바로가기
  - **「이 마켓만 고치기」는 비활성 + 「3단계에서 열립니다」** — 거짓 버튼 금지
- 「묶음에서 떼기」 는 동작한다(읽기 데이터만 바꾸므로 안전)

- [ ] **Step 2: node 로 렌더 확인** — 펼침/접힘 · 담은 게 없을 때 · 마켓 3개 카드

- [ ] **Step 3: 커밋**

---

## Task 6: 검증 · 배포 · 라이브 확인

- [ ] **Step 1: 전체 테스트**

Run: `cd 프로그램/_시스템 && python -m pytest tests/catalog/ -q`

- [ ] **Step 2: 화면 렌더 3종**

```bash
cd 프로그램/_시스템
node tests/catalog/test_dashboard_render.js
node tests/catalog/test_pick_render.js
node tests/catalog/test_detail_render.js
```

- [ ] **Step 3: 라우트 확인**

```bash
cd 프로그램/_시스템 && mv .env .env.bak && \
DATABASE_URL="sqlite:///C:/Users/seung/AppData/Local/Temp/claude/p2check.db" python -c "
from app import create_app
app = create_app()
rules = sorted(str(r) for r in app.url_map.iter_rules() if str(r).startswith('/catalog'))
print('\n'.join(rules))
for need in ('/catalog/api/search', '/catalog/api/groups'):
    assert any(need in r for r in rules), need
print('OK')
"; mv .env.bak .env
```

- [ ] **Step 4: 회귀 확인** — `tests/platforms tests/uploader` 를 돌려 1단계와 같은 실패만 남는지 대조

- [ ] **Step 5: PR · 머지 · 배포**

- [ ] **Step 6: 라이브 확인**

1. `/catalog/?tab=pick` 에서 「아디다스」 검색 → 결과가 뜨는지
2. 두세 개 골라 담기 → `/catalog/?tab=detail` 에 뜨는지
3. 줄을 눌러 펼치면 마켓 카드가 나란히 뜨는지
4. 「떼기」가 되는지

⚠️ 지금 캐시에는 옥션 1,605 · 쿠팡 19 · 스마트스토어 12 건만 들어 있다.
검색이 비면 **버그가 아니라 아직 안 훑어서**다 — 먼저 「지금 동기화」를 눌러야 한다.

---

## 3단계로 넘기는 것

- ⑦ 고치기 → 확인 → 보내기 (마켓 쓰기 · `LIVE_*` 2중잠금)
- 옵션 연동(`set_channel_options`) — 담은 상품만 옵션을 받아온다
- 「이것들도 같은 상품인가요?」 자동 제안 (이름 비슷한 것 찾기)
