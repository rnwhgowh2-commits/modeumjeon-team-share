# M1 판매처 6마켓 카테고리 전수 수집기 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 6대마켓(스마트스토어·쿠팡·옥션·G마켓·11번가·롯데온) 카테고리 전체를 공식 API 로 수집해 `market_categories` 사전을 만들고, 대량등록의 카테고리 prompt 수기입력을 사전 기반 검색·선택 UI 로 대체한다.

**Architecture:** 마켓별 파서는 순수함수(응답→행 리스트, fetch 콜러블 주입)로 두고 fixture 로 TDD. 저장은 스냅샷 upsert + `removed_at` 마킹(diff). 실호출은 서버(마켓 API=서버 단일IP 원칙)에서 설정 탭 「재수집」 버튼으로. 검색 라우트는 실시간 트리 순회 → 사전 테이블 조회로 재작성.

**Tech Stack:** Flask + SQLAlchemy(`shared/db.py` create_all 규약) + 기존 A계층 마켓 클라이언트(`shared/platforms/*/client.py`) + pytest (cwd=`프로그램/_시스템`).

**스펙:** `docs/superpowers/specs/2026-07-22-더망고식-URL멀티마켓등록-카테고리맵핑-design.md` §A
**게이트:** 마켓 API 작업 — 구현 세션은 착수 전 `consult-market-map` 스킬로 6마켓 `/map-brief` 전수정독. 발견한 함정은 `webapp/data/marketplace_api_map.json` idTraps 에 되채움(Task 12).

---

## 사전 지식 (구현자가 알아야 할 프로젝트 사실)

1. **베이스 = origin/main.** feat/4market-register 는 origin/main 에 완전 병합됨(diff 0). `_wt_reg` 워크트리는 다른 세션 소유 — 절대 거기서 커밋하지 말 것.
2. **DB 마이그레이션 규약**: Alembic 없음. 신규 테이블 = `shared/db.py:init_db()` 의 `Base.metadata.create_all` 이 생성(모델 클래스를 `lemouton/registration/models.py` 에 추가하면 `app.py:76` 이 이미 import 하므로 추가 배선 불필요). create_all 은 기존 테이블에 컬럼을 추가하지 않는다.
3. **마켓 API 인증**: A계층 클라이언트가 전부 처리(쿠팡 HMAC·스스 OAuth2·롯데온 Bearer·11번가 openapikey·ESM JWT). 계정은 `UploadAccount(market, is_active).order_by(id).first()` 의 `env_prefix` → `lemouton/uploader/market_fetch.py` 의 `_<market>_client(env_prefix)` 헬퍼 재사용(기존 `drafts.py:291-299` 패턴).
4. **마켓 API 는 서버 IP 에서만** 통과(롯데온·11번가·ESM 은 IP 등록 게이트 403). 로컬에선 fixture 테스트만, 실수집은 배포 후 설정 탭 버튼으로.
5. **pytest**: cwd=`프로그램/_시스템` 에서 **반드시 경로 지정** 실행(`pytest tests/registration/...`). 인자 없이 실행 금지(라이브 접속 테스트 존재).
6. **테스트 규약**: `tests/registration/test_<기능>.py`, 함수명 한글 서술형, 라우트 테스트 client fixture 는 `tests/registration/test_settings_tab.py:9-15` 패턴(DISABLE_AUTH=1 + create_app).
7. **마켓별 카테고리 API 요약** (지도 `webapp/data/marketplace_api_map.json` 발췌):
   - 스마트스토어: `GET /v1/categories` 1콜 평면 리스트 `{id, name, wholeCategoryName, last}` (last=리프). ⚠️ 실제 path 는 `/external/v1/categories` 프리픽스일 가능성 — 다른 스스 path 전부 `/external/v...`(`shared/platforms/__init__.py:113`). 실호출로 확정.
   - 11번가: `GET /rest/cateservice/category` 1콜 XML 전체 트리 `{depth, dispNm, dispNo, parentDispNo(0=최상위), leafYn}`. 키 불필요. 기존 파싱 코드 `webapp/routes/bulk/drafts.py:303-317` 승격.
   - 쿠팡: `GET /v2/providers/seller_api/apis/api/v1/marketplace/meta/display-categories/{code}` — code=0 루트부터, **child 는 1depth 하위만** → 노드마다 재귀 호출(BFS) 필요. 응답 노드 `{displayItemCategoryCode, name, status(ACTIVE/READY/DISABLED), child[]}`.
   - ESM(옥션·G마켓): `GET /item/v1/categories/site-cats`(대분류 전체) / `.../site-cats/{code}`(하위 1depth) — `{catCode, catName, isLeaf, subCats[]}` 재귀. 옥션 JWT ssi=`A:...`, G마켓 `G:...` 각각(사이트별 개별 조회는 그 사이트만 든 토큰). ESM표준: `GET /item/v1/categories/sd-cats/{code}` `{sdCategoryTree:[{SDCategoryCode, SDCategoryName, IsLeafCategory}]}`. 등록엔 `sd코드/site코드` 쌍이 필요(`compile_more.py:111`이 '/' 분리).
   - 롯데온: `GET https://onpick-api.lotteon.com/cheetah/econCheetah.ecn?job=cheetahStandardCategory&skip=N&limit=100` — ⚠️ **host 가 일반 롯데온 API 와 다름** → `LotteonClient` base_url 재사용 불가, requests 직접 호출(Bearer 인증키는 credentials 재사용). 필드 `{std_cat_id, std_cat_nm, upr_std_cat_id, depth_no}`. skip/limit 페이징. 단, **롯데온 등록은 카테고리 코드가 아니라 본보기 상품(spdNo)** — 표준카테고리는 사전·맵핑 표시용, 선택 UI 의 롯데온은 기존 본보기 검색 유지.

## 파일 구조

```
lemouton/registration/models.py                  # (수정) MarketCategory 모델 추가
lemouton/registration/category_harvest.py       # (신규) 마켓별 파서 순수함수 + 수집 워커 + 저장/diff 엔진
webapp/routes/bulk/categories.py                # (신규) harvest/status API 라우트
webapp/routes/bulk/__init__.py                  # (수정) categories 모듈 import 1줄
webapp/routes/bulk/drafts.py                    # (수정) category_search 를 사전 조회로 재작성
webapp/templates/bulk/partials/_settings.html   # (수정) 「카테고리 사전」 카드 추가
webapp/static/bulk_manual.js                    # (수정) prompt 검색 루프 → 사전 검색·선택
tests/registration/test_category_harvest.py     # (신규) 파서·diff 테스트
tests/registration/test_category_routes.py      # (신규) 라우트 테스트
```

---

### Task 0: 워크트리 준비

- [ ] **Step 1: origin/main 기준 새 워크트리 생성**

```bash
cd "C:\dev\모음전 프로젝트"
git fetch origin
git worktree add _wt_cat -b feature/market-category-harvest origin/main
```

- [ ] **Step 2: 기준 테스트 그린 확인 (registration 만)**

Run: `cd "C:\dev\모음전 프로젝트\_wt_cat\프로그램\_시스템" && pytest tests/registration/ -q`
Expected: 전부 PASS (origin/main 기존 실패 테스트는 registration 밖에 있음 — 실패가 나오면 내 탓인지 `git stash` 대조로 판별)

---

### Task 1: MarketCategory 모델

**Files:**
- Modify: `lemouton/registration/models.py` (파일 끝에 클래스 추가)
- Test: `tests/registration/test_category_harvest.py` (신규)

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# tests/registration/test_category_harvest.py
"""market_categories 사전 — 모델·파서·저장 diff 테스트."""
import datetime

from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker

from shared.db import Base
from lemouton.registration.models import MarketCategory


def _mem_session():
    eng = create_engine('sqlite://')
    Base.metadata.create_all(eng)
    return sessionmaker(bind=eng)()


def test_market_categories_테이블에_행을_넣고_읽는다():
    s = _mem_session()
    s.add(MarketCategory(
        market='eleven11', code='1011634', name='여성운동화',
        full_path='패션잡화>운동화>여성운동화', parent_code='1011630',
        depth=3, is_leaf=True, raw_json='{}',
        harvested_at=datetime.datetime(2026, 7, 22, 12, 0, 0)))
    s.commit()
    row = s.query(MarketCategory).filter_by(market='eleven11', code='1011634').one()
    assert row.is_leaf is True
    assert row.removed_at is None
```

- [ ] **Step 2: 실패 확인**

Run: `pytest tests/registration/test_category_harvest.py -q`
Expected: FAIL — `ImportError: cannot import name 'MarketCategory'`

- [ ] **Step 3: 모델 구현** (`lemouton/registration/models.py` 끝에 추가 — 파일 상단에 이미 있는 `Column, Integer, String, Text, DateTime, Boolean, UniqueConstraint` import 를 확인하고 없는 것만 추가)

```python
class MarketCategory(Base):
    """판매처 마켓 카테고리 사전 — 6마켓 전수 수집 스냅샷 (스펙 2026-07-22 §A).

    재수집 시 사라진 코드는 지우지 않고 removed_at 마킹(맵핑 재확정 강등의 근거).
    """
    __tablename__ = 'market_categories'

    id = Column(Integer, primary_key=True)
    market = Column(String(20), nullable=False, index=True)   # smartstore|coupang|auction|gmarket|eleven11|lotteon
    code = Column(String(40), nullable=False)                 # 마켓 카테고리 코드 (ESM 은 site-cat 코드)
    name = Column(String(200), nullable=False)
    full_path = Column(String(500), nullable=False)           # '패션잡화>운동화>여성운동화' (구분자 '>')
    parent_code = Column(String(40))                          # 루트는 None
    depth = Column(Integer, nullable=False, default=1)
    is_leaf = Column(Boolean, nullable=False, default=False)
    extra_code = Column(String(40))                           # ESM: 짝 ESM표준(sd) 코드. 그 외 None
    raw_json = Column(Text)                                   # 마켓 응답 원문 조각 (버리지 않는다)
    harvested_at = Column(DateTime, nullable=False)           # 마지막으로 존재 확인된 수집 시각
    removed_at = Column(DateTime)                             # 재수집에서 사라진 시각 (None=현존)

    __table_args__ = (
        UniqueConstraint('market', 'code', name='uq_market_categories_market_code'),
    )
```

- [ ] **Step 4: 통과 확인**

Run: `pytest tests/registration/test_category_harvest.py -q`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add lemouton/registration/models.py tests/registration/test_category_harvest.py
git commit -m "feat(category): market_categories 사전 테이블 — 6마켓 카테고리 스냅샷+removed_at diff"
```

---

### Task 2: 11번가 XML 파서 (기존 로직 승격)

**Files:**
- Create: `lemouton/registration/category_harvest.py`
- Test: `tests/registration/test_category_harvest.py` (추가)

- [ ] **Step 1: 실패하는 테스트 작성** (test_category_harvest.py 에 추가)

```python
from lemouton.registration import category_harvest as ch

_XML_11ST = """<?xml version="1.0" encoding="euc-kr"?>
<ns2:categorys xmlns:ns2="http://skt.tmall.business.openapi.spring.service.client.domain/">
  <ns2:category><depth>1</depth><dispNm>패션잡화</dispNm><dispNo>1001</dispNo><parentDispNo>0</parentDispNo><leafYn>N</leafYn></ns2:category>
  <ns2:category><depth>2</depth><dispNm>운동화</dispNm><dispNo>1002</dispNo><parentDispNo>1001</parentDispNo><leafYn>N</leafYn></ns2:category>
  <ns2:category><depth>3</depth><dispNm>여성운동화</dispNm><dispNo>1003</dispNo><parentDispNo>1002</parentDispNo><leafYn>Y</leafYn></ns2:category>
</ns2:categorys>"""


def test_11번가_XML을_행으로_파싱하고_경로를_조립한다():
    rows = ch.parse_eleven11(_XML_11ST)
    assert len(rows) == 3
    leaf = [r for r in rows if r['code'] == '1003'][0]
    assert leaf['name'] == '여성운동화'
    assert leaf['parent_code'] == '1002'
    assert leaf['is_leaf'] is True
    assert leaf['full_path'] == '패션잡화>운동화>여성운동화'


def test_11번가_필수태그_누락이면_HarvestError():
    import pytest
    bad = '<category><dispNm>이름만</dispNm></category>'
    with pytest.raises(ch.HarvestError):
        ch.parse_eleven11(bad)
```

- [ ] **Step 2: 실패 확인**

Run: `pytest tests/registration/test_category_harvest.py -q`
Expected: FAIL — `ModuleNotFoundError` (category_harvest 없음)

- [ ] **Step 3: 구현** (`lemouton/registration/category_harvest.py` 신규)

```python
"""판매처 카테고리 전수 수집 — 마켓별 파서(순수함수) + 저장/diff 엔진.

원칙 (스펙 2026-07-22 §A·§5):
- 파서는 순수함수(응답 텍스트/JSON → 행 리스트). 네트워크는 fetch 콜러블 주입 — 테스트는 fixture.
- 실패는 HarvestError 로 표면화한다. 조용히 빈 리스트를 돌려주지 않는다(조용한 실패 금지).
- 행 스키마: {code, name, parent_code, depth, is_leaf, full_path, raw}  (전부 str/int/bool, raw=원문 조각)
"""
from __future__ import annotations

import re


class HarvestError(Exception):
    """카테고리 수집 실패 — 사유를 그대로 담는다."""


def build_paths(rows):
    """parent_code 사슬로 full_path('A>B>C')를 조립해 각 행에 넣는다. 고아 부모는 HarvestError."""
    by_code = {r['code']: r for r in rows}
    def _path(r, guard=0):
        if guard > 10:
            raise HarvestError(f"카테고리 경로 순환 의심: {r['code']}")
        p = r.get('parent_code')
        if not p or p not in by_code:
            return r['name']
        return _path(by_code[p], guard + 1) + '>' + r['name']
    for r in rows:
        r['full_path'] = _path(r)
    return rows


# ── 11번가 ──────────────────────────────────────────────
_CAT_BLOCK = re.compile(r'<(?:\w+:)?category>(.*?)</(?:\w+:)?category>', re.S)


def _tag(block, t):
    m = re.search(r'<(?:\w+:)?%s>(.*?)</(?:\w+:)?%s>' % (t, t), block, re.S)
    return m.group(1).strip() if m else ''


def parse_eleven11(xml_text):
    """11번가 전체 카테고리 XML → 행 리스트. leafYn=='Y' 를 리프로 본다(기존 검색 코드와 동일 기준)."""
    rows = []
    for block in _CAT_BLOCK.findall(xml_text or ''):
        code, name = _tag(block, 'dispNo'), _tag(block, 'dispNm')
        if not code or not name:
            raise HarvestError('11번가 카테고리 블록에 dispNo/dispNm 누락: ' + block[:120])
        parent = _tag(block, 'parentDispNo')
        rows.append({
            'code': code, 'name': name,
            'parent_code': (parent if parent not in ('', '0') else None),
            'depth': int(_tag(block, 'depth') or 0),
            'is_leaf': _tag(block, 'leafYn') == 'Y',
            'raw': block,
        })
    if not rows:
        raise HarvestError('11번가 카테고리 응답에서 category 블록을 하나도 못 찾음')
    return build_paths(rows)
```

- [ ] **Step 4: 통과 확인**

Run: `pytest tests/registration/test_category_harvest.py -q`
Expected: PASS (Task 1 테스트 포함 전부)

- [ ] **Step 5: 커밋**

```bash
git add lemouton/registration/category_harvest.py tests/registration/test_category_harvest.py
git commit -m "feat(category): 11번가 전체 카테고리 XML 파서 — drafts.py 검색 로직을 전수 수집으로 승격"
```

---

### Task 3: 스마트스토어 평면 리스트 파서

**Files:**
- Modify: `lemouton/registration/category_harvest.py`
- Test: `tests/registration/test_category_harvest.py` (추가)

- [ ] **Step 1: 실패하는 테스트 작성**

```python
def test_스마트스토어_평면리스트를_행으로_파싱한다():
    payload = [
        {'id': 50000000, 'name': '패션잡화', 'wholeCategoryName': '패션잡화', 'last': False},
        {'id': 50000167, 'name': '여성운동화', 'wholeCategoryName': '패션잡화>운동화>여성운동화', 'last': True},
    ]
    rows = ch.parse_smartstore(payload)
    leaf = [r for r in rows if r['code'] == '50000167'][0]
    assert leaf['is_leaf'] is True
    assert leaf['full_path'] == '패션잡화>운동화>여성운동화'
    assert leaf['depth'] == 3


def test_스마트스토어_빈_응답이면_HarvestError():
    import pytest
    with pytest.raises(ch.HarvestError):
        ch.parse_smartstore([])
```

- [ ] **Step 2: 실패 확인**

Run: `pytest tests/registration/test_category_harvest.py -q`
Expected: FAIL — `AttributeError: parse_smartstore`

- [ ] **Step 3: 구현** (category_harvest.py 에 추가)

```python
# ── 스마트스토어 ─────────────────────────────────────────
def parse_smartstore(payload):
    """GET /v1/categories 평면 리스트 → 행. wholeCategoryName 이 경로 그 자체(재조립 불필요)."""
    import json as _json
    if not payload:
        raise HarvestError('스마트스토어 카테고리 응답이 비었음')
    rows = []
    for c in payload:
        code = str(c.get('id') or '')
        name = str(c.get('name') or '')
        path = str(c.get('wholeCategoryName') or '')
        if not code or not name or not path:
            raise HarvestError('스마트스토어 카테고리에 id/name/wholeCategoryName 누락: %r' % (c,))
        parts = path.split('>')
        rows.append({
            'code': code, 'name': name, 'parent_code': None,
            'depth': len(parts), 'is_leaf': bool(c.get('last')),
            'full_path': path, 'raw': _json.dumps(c, ensure_ascii=False),
        })
    return rows
```

- [ ] **Step 4: 통과 확인** — Run: `pytest tests/registration/test_category_harvest.py -q` / Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add lemouton/registration/category_harvest.py tests/registration/test_category_harvest.py
git commit -m "feat(category): 스마트스토어 카테고리 평면 리스트 파서 (wholeCategoryName=경로, last=리프)"
```

---

### Task 4: 쿠팡 재귀(BFS) 수집 워커

**Files:**
- Modify: `lemouton/registration/category_harvest.py`
- Test: `tests/registration/test_category_harvest.py` (추가)

- [ ] **Step 1: 실패하는 테스트 작성** — fetch 를 dict 로 흉내내 재귀·리프 판정을 검증

```python
def test_쿠팡_BFS가_전_노드를_수집하고_자식없음을_리프로_판정한다():
    tree = {
        '0':   {'displayItemCategoryCode': 0, 'name': 'ROOT', 'status': 'ACTIVE',
                'child': [{'displayItemCategoryCode': 10, 'name': '패션잡화', 'status': 'ACTIVE'}]},
        '10':  {'displayItemCategoryCode': 10, 'name': '패션잡화', 'status': 'ACTIVE',
                'child': [{'displayItemCategoryCode': 101, 'name': '여성운동화', 'status': 'ACTIVE'}]},
        '101': {'displayItemCategoryCode': 101, 'name': '여성운동화', 'status': 'ACTIVE', 'child': []},
    }
    calls = []
    def fetch(code):
        calls.append(code)
        return tree[code]
    rows = ch.harvest_coupang(fetch, sleep=lambda s: None)
    assert [r['code'] for r in rows] == ['10', '101']       # 루트(0)는 행 제외
    leaf = rows[-1]
    assert leaf['is_leaf'] is True and leaf['full_path'] == '패션잡화>여성운동화'
    assert calls == ['0', '10', '101']                       # 전 노드 1회씩


def test_쿠팡_DISABLED_노드는_행에서_제외하고_하위도_안내려간다():
    tree = {
        '0':  {'displayItemCategoryCode': 0, 'name': 'ROOT', 'status': 'ACTIVE',
               'child': [{'displayItemCategoryCode': 10, 'name': '중단분류', 'status': 'DISABLED'}]},
    }
    rows = ch.harvest_coupang(lambda c: tree[c], sleep=lambda s: None)
    assert rows == []
```

- [ ] **Step 2: 실패 확인** — Run: `pytest tests/registration/test_category_harvest.py -q` / Expected: FAIL (`harvest_coupang` 없음)

- [ ] **Step 3: 구현** (category_harvest.py 에 추가)

```python
# ── 쿠팡 ────────────────────────────────────────────────
def harvest_coupang(fetch, sleep):
    """code='0' 루트부터 BFS. fetch(code:str)->data 노드 dict. child 는 1depth 하위만이라 노드마다 호출.

    리프 판정 = 그 노드를 fetch 했을 때 child 가 빔. DISABLED 는 행 제외 + 하위 미탐색.
    호출량이 크므로(노드 수 = 콜 수) sleep 콜러블로 마켓 예의를 지킨다(운영은 0.2s, 테스트는 no-op).
    """
    import json as _json
    rows, queue, seen = [], ['0'], set()
    parents = {}    # code -> parent_code
    names = {}      # code -> full_path 조립용
    while queue:
        code = queue.pop(0)
        if code in seen:
            continue
        seen.add(code)
        data = fetch(code)
        if not isinstance(data, dict):
            raise HarvestError(f'쿠팡 카테고리 {code} 응답이 dict 아님: {data!r}')
        children = data.get('child') or []
        if code != '0':
            path = (names.get(parents.get(code), '') + '>' if parents.get(code) in names else '')
            full = path + str(data.get('name') or '')
            names[code] = full
            rows.append({
                'code': code, 'name': str(data.get('name') or ''),
                'parent_code': parents.get(code),
                'depth': full.count('>') + 1,
                'is_leaf': len(children) == 0,
                'full_path': full,
                'raw': _json.dumps({k: data[k] for k in data if k != 'child'}, ensure_ascii=False),
            })
        for ch_node in children:
            c_code = str(ch_node.get('displayItemCategoryCode') or '')
            if not c_code:
                raise HarvestError(f'쿠팡 카테고리 {code} 의 child 에 코드 누락: {ch_node!r}')
            if str(ch_node.get('status') or '') == 'DISABLED':
                continue
            parents[c_code] = code if code != '0' else None
            queue.append(c_code)
        sleep(0.2)
    return rows
```

- [ ] **Step 4: 통과 확인** — Run: `pytest tests/registration/test_category_harvest.py -q` / Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add lemouton/registration/category_harvest.py tests/registration/test_category_harvest.py
git commit -m "feat(category): 쿠팡 display-categories BFS 수집 — child 1depth 함정 대응, DISABLED 가지치기"
```

---

### Task 5: ESM(옥션·G마켓) site-cats 재귀 수집

**Files:**
- Modify: `lemouton/registration/category_harvest.py`
- Test: `tests/registration/test_category_harvest.py` (추가)

- [ ] **Step 1: 실패하는 테스트 작성**

```python
def test_ESM_site_cats를_재귀수집하고_isLeaf를_그대로_쓴다():
    tree = {
        None:        {'subCats': [{'catCode': '100000002', 'catName': '패션의류', 'isLeaf': False}]},
        '100000002': {'catCode': '100000002', 'catName': '패션의류', 'isLeaf': False,
                      'subCats': [{'catCode': '200001091', 'catName': '여성운동화', 'isLeaf': True}]},
    }
    def fetch(code):
        return tree[code]
    rows = ch.harvest_esm_site(fetch, sleep=lambda s: None)
    assert [r['code'] for r in rows] == ['100000002', '200001091']
    assert rows[1]['is_leaf'] is True
    assert rows[1]['full_path'] == '패션의류>여성운동화'
    # 리프는 재호출하지 않는다 (isLeaf 를 믿는다 — 콜 수 절약)


def test_ESM_resultCode_실패응답이면_HarvestError():
    import pytest
    def fetch(code):
        return {'resultCode': 9001, 'message': '인증 오류'}
    with pytest.raises(ch.HarvestError):
        ch.harvest_esm_site(fetch, sleep=lambda s: None)
```

- [ ] **Step 2: 실패 확인** — Run: `pytest tests/registration/test_category_harvest.py -q` / Expected: FAIL

- [ ] **Step 3: 구현** (category_harvest.py 에 추가)

```python
# ── ESM (옥션·G마켓 공용 — 사이트별 client 로 각각 호출) ──
def harvest_esm_site(fetch, sleep):
    """fetch(code|None)->응답 dict. None=대분류 전체(/site-cats), code=하위(/site-cats/{code}).

    subCats 는 1depth 하위만 → isLeaf=False 인 노드만 재귀(리프는 재호출 안 함).
    실패 응답({resultCode!=0})은 HarvestError 로 표면화.
    """
    import json as _json
    rows = []
    queue = [(None, None, '')]          # (code, parent_code, parent_path)
    while queue:
        code, parent, ppath = queue.pop(0)
        data = fetch(code)
        if not isinstance(data, dict):
            raise HarvestError(f'ESM site-cats {code} 응답이 dict 아님: {data!r}')
        if data.get('resultCode') not in (None, 0):
            raise HarvestError(f"ESM site-cats {code} 실패: {data.get('resultCode')} {data.get('message')}")
        for sub in (data.get('subCats') or []):
            c_code, c_name = str(sub.get('catCode') or ''), str(sub.get('catName') or '')
            if not c_code or not c_name:
                raise HarvestError(f'ESM subCats 에 코드/이름 누락: {sub!r}')
            full = (ppath + '>' if ppath else '') + c_name
            is_leaf = bool(sub.get('isLeaf'))
            rows.append({
                'code': c_code, 'name': c_name, 'parent_code': (code if code else None),
                'depth': full.count('>') + 1, 'is_leaf': is_leaf, 'full_path': full,
                'raw': _json.dumps(sub, ensure_ascii=False),
            })
            if not is_leaf:
                queue.append((c_code, code, full))
        sleep(0.3)
    if not rows:
        raise HarvestError('ESM site-cats 수집 결과 0건 — 응답 구조 확인 필요')
    return rows
```

- [ ] **Step 4: 통과 확인** — Run: `pytest tests/registration/test_category_harvest.py -q` / Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add lemouton/registration/category_harvest.py tests/registration/test_category_harvest.py
git commit -m "feat(category): ESM site-cats 재귀 수집 — isLeaf 신뢰로 리프 재호출 절약, 실패 resultCode 표면화"
```

> ⚠️ **ESM sd코드 짝(extra_code)은 이 플랜에서 확정하지 않는다.** 등록에 `sd코드/site코드` 쌍이 필요하지만 site→sd 역방향 API 존재 여부가 미실측. Task 12(라이브 검증)에서 ①`/site-cats/{leafCode}` 개별 응답에 sd 코드 필드가 있는지 ②`/sd-cats/{sd}/site-cats` 정방향 매핑으로 채울 수 있는지 실측 후 결정한다. 그 전까지 옥션·G마켓 선택 UI 는 site 경로 검색만 제공하고, 등록 코드 조립 불가 시 "ESM표준코드 확인 필요" 를 정직하게 표시(추측 금지).

---

### Task 6: 롯데온 cheetah 표준카테고리 페이징 수집

**Files:**
- Modify: `lemouton/registration/category_harvest.py`
- Test: `tests/registration/test_category_harvest.py` (추가)

- [ ] **Step 1: 실패하는 테스트 작성**

```python
def test_롯데온_표준카테고리를_페이징으로_전수수집한다():
    page1 = [{'std_cat_id': 'C1', 'std_cat_nm': '패션잡화', 'upr_std_cat_id': None, 'depth_no': 1}] * 1
    page1 += [{'std_cat_id': f'C1{i}', 'std_cat_nm': f'하위{i}', 'upr_std_cat_id': 'C1', 'depth_no': 2}
              for i in range(99)]
    page2 = [{'std_cat_id': 'C2', 'std_cat_nm': '여성운동화', 'upr_std_cat_id': 'C1', 'depth_no': 2}]
    pages = {0: page1, 100: page2}
    rows = ch.harvest_lotteon(lambda skip, limit: pages.get(skip, []))
    assert len(rows) == 101
    last = [r for r in rows if r['code'] == 'C2'][0]
    assert last['full_path'] == '패션잡화>여성운동화'
    # 리프 판정 = 아무도 나를 부모로 안 가리킴
    assert last['is_leaf'] is True
    assert [r for r in rows if r['code'] == 'C1'][0]['is_leaf'] is False
```

- [ ] **Step 2: 실패 확인** — Run: `pytest tests/registration/test_category_harvest.py -q` / Expected: FAIL

- [ ] **Step 3: 구현** (category_harvest.py 에 추가)

```python
# ── 롯데온 (표준카테고리 — cheetah host) ─────────────────
def harvest_lotteon(fetch):
    """fetch(skip:int, limit:int)->data 배열. 빈 배열이면 종료. 리프=자식 없는 노드(응답에 리프 플래그 없음)."""
    import json as _json
    raw_rows, skip, LIMIT = [], 0, 100
    while True:
        batch = fetch(skip, LIMIT)
        if not isinstance(batch, list):
            raise HarvestError(f'롯데온 표준카테고리 skip={skip} 응답이 배열 아님: {type(batch)}')
        if not batch:
            break
        raw_rows.extend(batch)
        if len(batch) < LIMIT:
            break
        skip += LIMIT
    if not raw_rows:
        raise HarvestError('롯데온 표준카테고리 수집 결과 0건')
    rows = []
    for c in raw_rows:
        code, name = str(c.get('std_cat_id') or ''), str(c.get('std_cat_nm') or '')
        if not code or not name:
            raise HarvestError(f'롯데온 표준카테고리에 std_cat_id/std_cat_nm 누락: {c!r}')
        parent = c.get('upr_std_cat_id')
        rows.append({
            'code': code, 'name': name,
            'parent_code': (str(parent) if parent else None),
            'depth': int(c.get('depth_no') or 0), 'is_leaf': False,
            'raw': _json.dumps(c, ensure_ascii=False),
        })
    has_child = {r['parent_code'] for r in rows if r['parent_code']}
    for r in rows:
        r['is_leaf'] = r['code'] not in has_child
    return build_paths(rows)
```

- [ ] **Step 4: 통과 확인** — Run: `pytest tests/registration/test_category_harvest.py -q` / Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add lemouton/registration/category_harvest.py tests/registration/test_category_harvest.py
git commit -m "feat(category): 롯데온 cheetah 표준카테고리 페이징 수집 — 리프=자식부재 도출"
```

---

### Task 7: 저장·diff 엔진 (save_snapshot)

**Files:**
- Modify: `lemouton/registration/category_harvest.py`
- Test: `tests/registration/test_category_harvest.py` (추가)

- [ ] **Step 1: 실패하는 테스트 작성**

```python
def _rows(*specs):
    return [{'code': c, 'name': n, 'parent_code': None, 'depth': 1,
             'is_leaf': True, 'full_path': n, 'raw': '{}'} for c, n in specs]


def test_save_snapshot_추가_갱신_삭제마킹_부활을_구분한다():
    s = _mem_session()
    t1 = datetime.datetime(2026, 7, 22, 10, 0, 0)
    r1 = ch.save_snapshot(s, 'eleven11', _rows(('1', '가'), ('2', '나')), now=t1)
    assert (r1['added'], r1['removed'], r1['total']) == (2, 0, 2)

    # 2차: '2' 사라지고 '3' 추가, '1' 이름 변경
    t2 = datetime.datetime(2026, 7, 22, 11, 0, 0)
    r2 = ch.save_snapshot(s, 'eleven11', _rows(('1', '가나'), ('3', '다')), now=t2)
    assert (r2['added'], r2['updated'], r2['removed']) == (1, 1, 1)
    gone = s.query(MarketCategory).filter_by(market='eleven11', code='2').one()
    assert gone.removed_at == t2                      # 지우지 않고 마킹

    # 3차: '2' 부활
    t3 = datetime.datetime(2026, 7, 22, 12, 0, 0)
    ch.save_snapshot(s, 'eleven11', _rows(('1', '가나'), ('2', '나'), ('3', '다')), now=t3)
    back = s.query(MarketCategory).filter_by(market='eleven11', code='2').one()
    assert back.removed_at is None


def test_save_snapshot_빈_rows는_거부한다():
    import pytest
    s = _mem_session()
    with pytest.raises(ch.HarvestError):
        ch.save_snapshot(s, 'eleven11', [], now=datetime.datetime(2026, 7, 22))
    # 이유: 수집 실패를 "전부 삭제됨" 으로 오기록하는 조용한 대참사 방지
```

- [ ] **Step 2: 실패 확인** — Run: `pytest tests/registration/test_category_harvest.py -q` / Expected: FAIL

- [ ] **Step 3: 구현** (category_harvest.py 에 추가)

```python
# ── 저장·diff ────────────────────────────────────────────
def save_snapshot(session, market, rows, now):
    """수집 rows 를 market_categories 에 반영. 반환 {'added','updated','removed','total'}.

    빈 rows 거부 — 수집 실패가 '전부 삭제' 로 둔갑하는 조용한 실패 방지.
    사라진 코드는 삭제하지 않고 removed_at 마킹(M2 CategoryMap 재확정 강등의 근거).
    """
    from lemouton.registration.models import MarketCategory
    if not rows:
        raise HarvestError(f'{market}: 수집 결과 0건 — 스냅샷 저장 거부(전부삭제 오기록 방지)')
    existing = {r.code: r for r in session.query(MarketCategory).filter_by(market=market).all()}
    added = updated = removed = 0
    seen = set()
    for row in rows:
        seen.add(row['code'])
        cur = existing.get(row['code'])
        if cur is None:
            session.add(MarketCategory(
                market=market, code=row['code'], name=row['name'],
                full_path=row['full_path'], parent_code=row.get('parent_code'),
                depth=row.get('depth') or 1, is_leaf=bool(row.get('is_leaf')),
                raw_json=row.get('raw'), harvested_at=now))
            added += 1
        else:
            changed = (cur.name != row['name'] or cur.full_path != row['full_path']
                       or cur.is_leaf != bool(row.get('is_leaf')) or cur.removed_at is not None)
            cur.name, cur.full_path = row['name'], row['full_path']
            cur.parent_code = row.get('parent_code')
            cur.depth = row.get('depth') or 1
            cur.is_leaf = bool(row.get('is_leaf'))
            cur.raw_json = row.get('raw')
            cur.harvested_at = now
            cur.removed_at = None
            if changed:
                updated += 1
    for code, cur in existing.items():
        if code not in seen and cur.removed_at is None:
            cur.removed_at = now
            removed += 1
    session.commit()
    return {'added': added, 'updated': updated, 'removed': removed, 'total': len(seen)}
```

- [ ] **Step 4: 통과 확인** — Run: `pytest tests/registration/test_category_harvest.py -q` / Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add lemouton/registration/category_harvest.py tests/registration/test_category_harvest.py
git commit -m "feat(category): 스냅샷 저장·diff — removed_at 마킹, 빈수집 거부(전부삭제 오기록 방지)"
```

---

### Task 8: 수집 라우트 (harvest / status)

**Files:**
- Create: `webapp/routes/bulk/categories.py`
- Modify: `webapp/routes/bulk/__init__.py` (import 1줄)
- Test: `tests/registration/test_category_routes.py` (신규)

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# tests/registration/test_category_routes.py
"""카테고리 사전 라우트 — status·harvest·검색."""
import datetime

import pytest


@pytest.fixture()
def client(monkeypatch):
    monkeypatch.setenv('DISABLE_AUTH', '1')
    import app as appmod
    application = appmod.create_app()
    application.config['TESTING'] = True
    with application.test_client() as c:
        yield c


def _seed(code='1003', market='eleven11', name='여성운동화',
          path='패션잡화>운동화>여성운동화', leaf=True):
    from shared.db import SessionLocal
    from lemouton.registration.models import MarketCategory
    s = SessionLocal()
    s.add(MarketCategory(market=market, code=code, name=name, full_path=path,
                         depth=3, is_leaf=leaf, harvested_at=datetime.datetime(2026, 7, 22)))
    s.commit(); s.close()


def test_status가_마켓별_건수와_수집시각을_준다(client):
    _seed()
    r = client.get('/bulk/api/categories/status')
    assert r.status_code == 200
    data = r.get_json()
    m = {row['market']: row for row in data['rows']}
    assert m['eleven11']['total'] >= 1
    assert m['eleven11']['last_harvested'] is not None
    assert set(m) == {'smartstore', 'coupang', 'auction', 'gmarket', 'eleven11', 'lotteon'}


def test_harvest_모르는_마켓은_400(client):
    r = client.post('/bulk/api/categories/harvest/nosuch')
    assert r.status_code == 400


def test_harvest_실패는_502와_사유_원문(client, monkeypatch):
    from lemouton.registration import category_harvest as ch
    from webapp.routes.bulk import categories as cat_routes
    def boom(market):
        raise ch.HarvestError('IP 미등록 403: 원문사유')
    monkeypatch.setattr(cat_routes, '_run_harvest', boom)
    r = client.post('/bulk/api/categories/harvest/eleven11')
    assert r.status_code == 502
    assert '원문사유' in r.get_json()['error']
```

- [ ] **Step 2: 실패 확인** — Run: `pytest tests/registration/test_category_routes.py -q` / Expected: FAIL (라우트 404)

- [ ] **Step 3: 구현** (`webapp/routes/bulk/categories.py` 신규)

```python
"""카테고리 사전 라우트 — 전수 수집(harvest)·현황(status).

실수집은 서버에서만 의미가 있다(마켓 API=서버 단일IP·IP 등록 게이트).
수집 실패는 502 + 사유 원문 — 실패를 성공으로 칠하지 않는다.
"""
from __future__ import annotations

import datetime
import time

from flask import jsonify

from shared.db import SessionLocal
from lemouton.registration import category_harvest as ch
from lemouton.registration.models import MarketCategory
from . import bp

MARKETS = ('smartstore', 'coupang', 'auction', 'gmarket', 'eleven11', 'lotteon')


def _first_env_prefix(session, market):
    from lemouton.markets.models import UploadAccount
    acct = (session.query(UploadAccount)
            .filter_by(market=market, is_active=True).order_by(UploadAccount.id).first())
    if acct is None:
        raise ch.HarvestError(f'{market}: 활성 계정이 없음 — 판매처 계정 관리에서 먼저 등록')
    return acct.env_prefix


def _run_harvest(market):
    """마켓별 실호출 → 행 리스트. (테스트에서 monkeypatch 되는 경계)"""
    import lemouton.uploader.market_fetch as MF
    s = SessionLocal()
    try:
        if market == 'eleven11':
            client = MF._eleven11_client(_first_env_prefix(s, market))
            xml = client.request('GET', '/rest/cateservice/category')
            return ch.parse_eleven11(xml)
        if market == 'smartstore':
            client = MF._smartstore_client(_first_env_prefix(s, market))
            # ⚠️ path 프리픽스(/external) 실호출로 확정 — 404 면 '/v1/categories' 재시도 후
            #    확정값을 marketplace_api_map.json 에 되채움 (Task 12)
            payload = client.request('GET', '/external/v1/categories')
            return ch.parse_smartstore(payload)
        if market == 'coupang':
            client = MF._coupang_client(_first_env_prefix(s, market))
            base = '/v2/providers/seller_api/apis/api/v1/marketplace/meta/display-categories/'
            def fetch(code):
                res = client.request('GET', base + code)
                return (res or {}).get('data') or {}
            return ch.harvest_coupang(fetch, sleep=time.sleep)
        if market in ('auction', 'gmarket'):
            client = MF._esm_client(market, _first_env_prefix(s, market))
            def fetch(code):
                path = '/item/v1/categories/site-cats' + (f'/{code}' if code else '')
                return client.request('GET', path)
            return ch.harvest_esm_site(fetch, sleep=time.sleep)
        if market == 'lotteon':
            import requests as _rq
            from lemouton.auth.secrets import load_credentials
            creds = load_credentials('lotteon', _first_env_prefix(s, market))
            def fetch(skip, limit):
                r = _rq.get(
                    'https://onpick-api.lotteon.com/cheetah/econCheetah.ecn',
                    params={'job': 'cheetahStandardCategory', 'skip': skip, 'limit': limit},
                    headers={'Authorization': f'Bearer {creds.api_key}'}, timeout=30)
                if r.status_code != 200:
                    raise ch.HarvestError(f'롯데온 표준카테고리 HTTP {r.status_code}: {r.text[:300]}')
                return (r.json() or {}).get('data') or []
            return ch.harvest_lotteon(fetch)
        raise ch.HarvestError(f'모르는 마켓: {market}')
    finally:
        s.close()


@bp.post('/api/categories/harvest/<market>')
def harvest(market):
    if market not in MARKETS:
        return jsonify({'ok': False, 'error': f'모르는 마켓: {market}'}), 400
    try:
        rows = _run_harvest(market)
    except ch.HarvestError as e:
        return jsonify({'ok': False, 'error': str(e)}), 502
    s = SessionLocal()
    try:
        summary = ch.save_snapshot(s, market, rows, now=datetime.datetime.utcnow())
        return jsonify({'ok': True, 'market': market, **summary})
    finally:
        s.close()


@bp.get('/api/categories/status')
def status():
    s = SessionLocal()
    try:
        out = []
        for m in MARKETS:
            q = s.query(MarketCategory).filter_by(market=m)
            alive = q.filter(MarketCategory.removed_at.is_(None))
            last = (q.order_by(MarketCategory.harvested_at.desc()).first())
            out.append({
                'market': m,
                'total': alive.count(),
                'leaves': alive.filter(MarketCategory.is_leaf.is_(True)).count(),
                'removed': q.filter(MarketCategory.removed_at.isnot(None)).count(),
                'last_harvested': (last.harvested_at.isoformat(sep=' ') if last else None),
            })
        return jsonify({'ok': True, 'rows': out})
    finally:
        s.close()
```

`webapp/routes/bulk/__init__.py` 하위 모듈 import 블록(`:44-50` 부근, `from . import settings_tab` 옆)에 추가:

```python
from . import categories        # noqa: E402,F401  # 카테고리 사전 (M1)
```

⚠️ `market_fetch` 에 `_smartstore_client`/`_coupang_client`/`_esm_client` 헬퍼가 없으면(`_eleven11_client`·`_lotteon_client` 만 있을 수 있음) — 있는 것 재사용, 없는 것은 `lemouton/uploader/market_fetch.py` 의 기존 헬퍼와 같은 모양으로 추가한다(각 A계층 client 생성자에 env_prefix 로 로딩한 credentials 전달, 기존 헬퍼 코드 5줄 내외 복제). ESM 은 옥션/G마켓 별개 config(`shared/platforms/__init__.py:240,248`)를 쓰므로 market 인자로 분기.

- [ ] **Step 4: 통과 확인** — Run: `pytest tests/registration/test_category_routes.py -q` / Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add webapp/routes/bulk/categories.py webapp/routes/bulk/__init__.py tests/registration/test_category_routes.py
git commit -m "feat(category): 수집·현황 라우트 — POST harvest/<market> (502+원문), GET status"
```

---

### Task 9: category-search 를 사전 조회로 재작성

**Files:**
- Modify: `webapp/routes/bulk/drafts.py:272-343` (category_search)
- Test: `tests/registration/test_category_routes.py` (추가)

- [ ] **Step 1: 실패하는 테스트 작성**

```python
def test_검색이_사전에서_리프만_경로포함으로_돌려준다(client):
    _seed()   # eleven11 여성운동화 리프
    _seed(code='1002', name='운동화', path='패션잡화>운동화', leaf=False)
    r = client.get('/bulk/api/category-search?market=eleven11&q=운동화')
    data = r.get_json()
    assert data['ok'] is True
    assert [row['code'] for row in data['rows']] == ['1003']       # 리프만
    assert data['rows'][0]['path'] == '패션잡화>운동화>여성운동화'  # 경로 동봉


def test_검색은_removed_행을_빼고_사전이_비면_수집안내를_준다(client):
    r = client.get('/bulk/api/category-search?market=coupang&q=운동화')
    data = r.get_json()
    assert data['ok'] is False
    assert '수집' in data['error']       # "설정 탭에서 카테고리 수집 먼저" 안내
```

- [ ] **Step 2: 실패 확인** — Run: `pytest tests/registration/test_category_routes.py -q` / Expected: FAIL

- [ ] **Step 3: 구현** — `drafts.py::category_search` 본문 교체. **롯데온 분기(본보기 상품 검색, `drafts.py:323-340`)는 그대로 두고**, 그 외 5마켓을 사전 조회로:

```python
@bp.get('/api/category-search')
def category_search():
    """카테고리 이름 검색 — market_categories 사전 조회 (롯데온만 본보기 상품 검색 유지)."""
    market = (request.args.get('market') or '').strip()
    q = (request.args.get('q') or '').strip()
    if not market or not q:
        return jsonify({'ok': False, 'error': 'market 과 q 가 필요합니다'}), 400
    if market == 'lotteon':
        return _lotteon_sample_search(q)     # 기존 본보기 검색 로직을 헬퍼로 추출(코드 이동, 수정 없음)
    from lemouton.registration.models import MarketCategory
    s = SessionLocal()
    try:
        base = (s.query(MarketCategory)
                .filter_by(market=market)
                .filter(MarketCategory.removed_at.is_(None)))
        if base.count() == 0:
            return jsonify({'ok': False,
                            'error': f'{market} 카테고리 사전이 비어 있습니다 — 설정 탭에서 「카테고리 수집」을 먼저 실행하세요'})
        like = f'%{q}%'
        rows = (base.filter(MarketCategory.is_leaf.is_(True))
                .filter(MarketCategory.full_path.like(like))
                .order_by(MarketCategory.full_path).limit(30).all())
        return jsonify({'ok': True, 'market': market, 'count': len(rows),
                        'rows': [{'code': r.code, 'name': r.name, 'path': r.full_path} for r in rows]})
    finally:
        s.close()
```

기존 11번가 실시간 XML 조회 블록(`drafts.py:285-322`)은 삭제한다(파서는 Task 2 에서 `category_harvest.py` 로 승격 완료 — 죽은 코드 이중화 금지).

- [ ] **Step 4: 통과 확인** — Run: `pytest tests/registration/test_category_routes.py -q && pytest tests/registration/test_drafts_route.py -q` / Expected: PASS (기존 drafts 테스트 포함)

- [ ] **Step 5: 커밋**

```bash
git add webapp/routes/bulk/drafts.py tests/registration/test_category_routes.py
git commit -m "feat(category): category-search 사전 조회로 재작성 — 5마켓 리프+경로, 빈사전 정직 안내, 롯데온 본보기 유지"
```

---

### Task 10: 설정 탭 「카테고리 사전」 카드

> 🎨 UI 작업 — 구현 세션은 이 태스크 시작 전 `design-mockup` 스킬의 항목별 결정 게이트를 통과할 것(최소 UI 변경 원칙 — 기존 카드 패턴 복제라 결정 항목은 적다).

**Files:**
- Modify: `webapp/templates/bulk/partials/_settings.html` (업로드 속도 카드 `:37-43` 아래에 카드 추가)
- Test: `tests/registration/test_category_routes.py` (추가)

- [ ] **Step 1: 실패하는 테스트 작성**

```python
def test_설정탭에_카테고리사전_카드가_뜬다(client):
    r = client.get('/bulk/?tab=settings')
    assert r.status_code == 200
    assert 'cat-dict-root' in r.get_data(as_text=True)
```

- [ ] **Step 2: 실패 확인** — Run: `pytest tests/registration/test_category_routes.py -q` / Expected: FAIL

- [ ] **Step 3: 구현** — `_settings.html` 의 업로드 속도 카드 다음에 추가(기존 카드 마크업·IIFE 패턴 복제):

```html
<h2 class="card-title">📂 카테고리 사전 (6마켓)</h2>
<p style="color:var(--tx3);font-size:12px;margin:4px 0 8px">
  마켓별 카테고리 전체를 공식 API로 수집해 둡니다. 등록 화면의 카테고리 검색이 이 사전을 씁니다.
  마켓이 카테고리를 개편할 수 있으니 가끔 「재수집」을 눌러 주세요.</p>
<div id="cat-dict-root">불러오는 중…</div>
<script>
(function(){
  const root = document.getElementById('cat-dict-root');
  const NAMES = {smartstore:'스마트스토어', coupang:'쿠팡', auction:'옥션',
                 gmarket:'G마켓', eleven11:'11번가', lotteon:'롯데온'};
  function load(){
    fetch('/bulk/api/categories/status').then(r=>r.json()).then(d=>{
      if(!d.ok){ root.textContent = '현황 조회 실패: ' + (d.error||''); return; }
      root.innerHTML = '<table class="tbl"><tr><th>마켓</th><th style="text-align:right">전체</th>'
        + '<th style="text-align:right">리프</th><th style="text-align:right">삭제됨</th>'
        + '<th>마지막 수집</th><th></th></tr>'
        + d.rows.map(r=>'<tr><td>'+NAMES[r.market]+'</td>'
          + '<td style="text-align:right">'+r.total+'</td>'
          + '<td style="text-align:right">'+r.leaves+'</td>'
          + '<td style="text-align:right">'+(r.removed||0)+'</td>'
          + '<td>'+(r.last_harvested||'수집 전')+'</td>'
          + '<td><button class="btn-sm" data-m="'+r.market+'">재수집</button></td></tr>').join('')
        + '</table><div id="cat-dict-msg" style="font-size:12px;margin-top:6px"></div>';
      root.querySelectorAll('button[data-m]').forEach(b=>b.onclick=()=>run(b.dataset.m, b));
    });
  }
  function run(market, btn){
    const msg = document.getElementById('cat-dict-msg');
    btn.disabled = true; msg.textContent = NAMES[market] + ' 수집 중… (쿠팡·ESM은 수 분 걸립니다)';
    fetch('/bulk/api/categories/harvest/' + market, {method:'POST'})
      .then(r=>r.json().then(d=>({s:r.status, d})))
      .then(({s, d})=>{
        msg.textContent = d.ok
          ? NAMES[market]+' 완료 — 추가 '+d.added+' · 갱신 '+d.updated+' · 삭제됨 '+d.removed+' · 전체 '+d.total
          : NAMES[market]+' 실패('+s+'): '+d.error;   // 사유 원문 그대로
        load();
      })
      .catch(e=>{ msg.textContent = NAMES[market]+' 실패: '+e; btn.disabled = false; });
  }
  load();
})();
</script>
```

- [ ] **Step 4: 통과 확인** — Run: `pytest tests/registration/test_category_routes.py -q` / Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add webapp/templates/bulk/partials/_settings.html tests/registration/test_category_routes.py
git commit -m "feat(category): 설정 탭 카테고리 사전 카드 — 마켓별 현황표+재수집, 실패 사유 원문 표시"
```

---

### Task 11: 등록 화면 카테고리 선택 — prompt 검색 루프 교체

> 🎨 UI 작업 — design-mockup 게이트 대상. 단, prompt→선택리스트 전환은 기존 UX 결함 수정 성격이라 최소 변경으로.

**Files:**
- Modify: `webapp/static/bulk_manual.js:207-221` (카테고리 prompt + `?검색` 루프 부분만)

- [ ] **Step 1: 현재 흐름 파악** — `bulk_manual.js:185-243` 을 읽는다. 마켓 선택(:192)·계정 선택(:222)·등록 POST(:230)는 그대로 두고, **카테고리 입력(:207-221)만** 교체한다.

- [ ] **Step 2: 구현** — `?검색` 루프를 "검색어 입력 → 후보 목록에서 번호 선택" 으로:

```javascript
// 기존 :207-221 (cat prompt + ?검색 루프) 를 아래로 교체
let cat = '';
while (!cat) {
  const q = prompt(CAT_HINT[mkt] + '\n\n카테고리 검색어를 입력하세요 (예: 여성운동화)\n' +
                   '(코드를 이미 알면 #코드 로 직접 입력)');
  if (q === null) return;                       // 취소
  if (q.startsWith('#')) { cat = q.slice(1).trim(); break; }   // 직접 입력 탈출구
  const r = await fetch('/bulk/api/category-search?market=' + mkt + '&q=' + encodeURIComponent(q))
                  .then(r => r.json());
  if (!r.ok) { alert(r.error); continue; }      // 빈 사전 안내 = "설정 탭에서 수집 먼저"
  if (!r.count) { alert('검색 결과 없음: ' + q); continue; }
  const menu = r.rows.map((row, i) => (i + 1) + ') ' + (row.path || row.name) + '  [' + row.code + ']').join('\n');
  const pick = prompt('번호를 고르세요 (1~' + r.count + ')\n\n' + menu);
  if (pick === null) return;
  const idx = parseInt(pick, 10) - 1;
  if (idx >= 0 && idx < r.rows.length) cat = r.rows[idx].code;
}
```

주의: 옥션·G마켓은 사전의 site 코드만으로는 등록 코드(`sd코드/site코드`)가 완성되지 않는다 — Task 12 실측 전까지 `CAT_HINT` 의 안내문에 "옥션·G마켓은 선택한 코드 앞에 ESM표준코드/ 를 붙여야 합니다(기존 상품 상세 참조)" 문구를 유지한다(거짓 완성 금지).

- [ ] **Step 3: 라이브 아닌 로컬 검증** — 서버 기동 없이 JS 문법만: `node --check webapp/static/bulk_manual.js`
Expected: 오류 없음

- [ ] **Step 4: 커밋**

```bash
git add webapp/static/bulk_manual.js
git commit -m "feat(category): 등록 카테고리 입력 — 사전 검색·번호 선택으로 교체 (#코드 직접입력 탈출구)"
```

---

### Task 12: 라이브 검증 + 지도 되채움 (배포 후)

**Files:**
- Modify: `webapp/data/marketplace_api_map.json` (idTraps 되채움)
- Modify: `docs/superpowers/specs/2026-07-22-더망고식-URL멀티마켓등록-카테고리맵핑-design.md` (실측 반영)

- [ ] **Step 1: 머지·배포** — PR 생성 → main 머지 → GitHub Actions 배포 완료 확인(무중단 배포, 배포발 502 은 concurrency 잠금 있음).

- [ ] **Step 2: 6마켓 순차 수집 실행** — 라이브 `/bulk/?tab=settings` 카드에서 마켓별 「재수집」. 순서: 11번가(1콜) → 스마트스토어(1콜, `/external` 프리픽스 실측) → 롯데온 → 옥션 → G마켓 → 쿠팡(콜 수 최대·마지막).
Expected: 마켓별 `총 N건·리프 M건` — 상식 범위(수백~수만) 확인. 실패 시 사유 원문으로 진단(403=IP·401=키).

- [ ] **Step 3: 표본 대조 (정합성 기준=실제 화면)** — 마켓 3곳(11번가·스마트스토어·쿠팡)에서 리프 3개씩 골라 각 마켓 판매자센터 카테고리 트리와 경로 문자열이 일치하는지 눈으로 대조.

- [ ] **Step 4: ESM sd코드 짝 실측** — 라이브에서 ①`GET /item/v1/categories/site-cats/{리프코드}` 응답 원문에 sd 코드 필드가 있는지 ②없으면 `GET /item/v1/categories/sd-cats/{sd리프}/site-cats` 정방향 매핑 표본 확인. 결과에 따라 `extra_code` 채움 방식을 결정해 **후속 태스크로 스펙에 기록**(이번 플랜 범위 밖 — 추측으로 미리 만들지 않는다).

- [ ] **Step 5: 지도 되채움 커밋** — 실측에서 확인된 함정을 `marketplace_api_map.json` idTraps 에 추가(최소: 쿠팡 child=1depth 재귀 필수·롯데온 cheetah host 상이·스마트스토어 실제 path 프리픽스·11번가 leafYn 실측 의미·ESM sd짝 실측 결과). `validate_map` 통과 확인 후 커밋.

```bash
git add webapp/data/marketplace_api_map.json docs/superpowers/specs/2026-07-22-더망고식-URL멀티마켓등록-카테고리맵핑-design.md
git commit -m "docs(map): 카테고리 API 함정 되채움 — 실측(호스트·재귀·프리픽스·leafYn·ESM sd짝)"
```

- [ ] **Step 6: 전체 registration 테스트 최종 확인**

Run: `pytest tests/registration/ -q`
Expected: 전부 PASS

---

## Self-Review 결과 (플랜 작성 시 수행)

- 스펙 §A 커버리지: 수집 6마켓(T2~6)·market_categories(T1)·재수집 diff(T7·T10)·검색 UI(T9·T11)·지도 되채움(T12) — 전부 태스크 존재. §A "삭제 코드→CategoryMap re_confirm 강등"은 M2 범위(CategoryMap 자체가 M2)라 이월 명시.
- 스펙 정정: 스마트스토어 카테고리 API 는 지도에 **있음**(`smartstore.get-category-list-product`) — 스펙 §A·§8 의 "지도에 없음" 서술은 이번 커밋에서 함께 수정.
- 미확정 실측 항목(스스 path 프리픽스·ESM sd짝·11번가 leafYn 의미)은 전부 Task 12 실측으로 격리 — 코드에 추측값 박지 않음.
