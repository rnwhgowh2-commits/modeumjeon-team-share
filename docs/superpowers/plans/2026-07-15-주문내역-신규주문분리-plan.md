# 주문내역 #1 「주문일=신규주문」 분리 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 주문일 목록을 "실주문일이 이 기간 안인 주문"만으로 정리하고, 취소/교환/반품 이벤트에 `_kind`/`_change_date` 태그를 심어 기능 #2가 소비할 수 있게 한다.

**Architecture:** `order_export.py`의 각 마켓 빌더가 만드는 행에 `_kind`('order'/'change')를 태깅하고, 클레임 행의 `주문일`을 실주문일로만(추정 금지) 정리한다. 그 위에 `new_order_rows()`(=combined 결과에서 "실주문일이 기간 안인 행"만 남기는 얇은 후처리)를 신설하고, `/orders/preview.json`만 이걸 쓰게 바꾼다. `combined_order_rows`의 계약(엑셀·배송검사·마진 공유)은 태그 필드만 추가될 뿐 불변.

**Tech Stack:** Python 3, Flask, pytest (cwd = `프로그램/_시스템`). 대상 파일 = `lemouton/markets/order_export.py`, `webapp/routes/orders.py`.

**스펙:** `docs/superpowers/specs/2026-07-15-주문내역-신규주문분리-design.md`

**분기:** `main` 기준 새 워크트리(현재 `feature/crawl-worker-queue`는 stale). 모든 `git`·`pytest` 경로는 `프로그램/_시스템` 기준.

---

## File Structure

- **Modify** `프로그램/_시스템/lemouton/markets/order_export.py`
  - `_fetch_combined`: 모든 행에 `_kind` 기본값 `'order'` 부여(`setdefault`).
  - `lotteon_order_rows._claim_row`: `_kind='change'`, `_change_date=clmReqDttm`, `주문일=odAccpDttm`만(폴백 제거).
  - `coupang_order_rows._cp_claim_row`: `_kind='change'`, `_change_date=createdAt`, `주문일=""`.
  - `eleven11_order_rows._claim_row`/`_return_row`: `_kind='change'`, `_change_date=clmDt(best-effort)`.
  - 신설 `_window(since, until, days, now=None)` + `new_order_rows(...)`.
- **Modify** `프로그램/_시스템/webapp/routes/orders.py`
  - `/orders/preview.json`: `_oe.combined_order_rows` → `_oe.new_order_rows`.
- **Create** `프로그램/_시스템/tests/orders/test_new_order_rows.py` — 필터링·라우트 단위테스트.
- **Modify** `프로그램/_시스템/tests/platforms/test_order_export.py` — 마켓별 태깅·위생 테스트 추가.

---

## Task 1: `new_order_rows` 필터 + `_kind='order'` 기본 태그

**Files:**
- Modify: `프로그램/_시스템/lemouton/markets/order_export.py` (`_fetch_combined`, 신설 `_window`/`new_order_rows`)
- Test: `프로그램/_시스템/tests/orders/test_new_order_rows.py`

- [ ] **Step 1: 실패 테스트 작성**

Create `프로그램/_시스템/tests/orders/test_new_order_rows.py`:

```python
# -*- coding: utf-8 -*-
"""new_order_rows — 실주문일이 기간 안인 행만 남기는 필터."""
import datetime as dt
import pytest
from lemouton.markets import order_export as oe

KST = dt.timezone(dt.timedelta(hours=9))
SINCE = dt.datetime(2026, 7, 15, tzinfo=KST)
UNTIL = dt.datetime(2026, 7, 15, 23, 59, tzinfo=KST)


def _fake_combined(rows):
    def _f(markets, **kw):
        return list(rows)
    return _f


def test_keeps_order_rows_regardless_of_status(monkeypatch):
    rows = [
        {"오픈마켓주문번호": "A", "주문일": "2026-07-15 09:00:00", "주문상태": "결제완료", "_kind": "order"},
        {"오픈마켓주문번호": "B", "주문일": "2026-07-15 10:00:00", "주문상태": "취소완료", "_kind": "order"},
    ]
    monkeypatch.setattr(oe, "combined_order_rows", _fake_combined(rows))
    out = oe.new_order_rows(["smartstore"], since=SINCE, until=UNTIL)
    assert {r["오픈마켓주문번호"] for r in out} == {"A", "B"}   # 취소여도 order면 남김


def test_change_in_window_kept_out_of_window_dropped(monkeypatch):
    rows = [
        {"오픈마켓주문번호": "C", "주문일": "2026-07-15 08:00:00", "주문상태": "취소요청", "_kind": "change"},
        {"오픈마켓주문번호": "D", "주문일": "2026-07-10 08:00:00", "주문상태": "취소요청", "_kind": "change"},
    ]
    monkeypatch.setattr(oe, "combined_order_rows", _fake_combined(rows))
    out = oe.new_order_rows(["lotteon"], since=SINCE, until=UNTIL)
    assert {r["오픈마켓주문번호"] for r in out} == {"C"}   # 기간 안 change만


def test_change_blank_orderdate_dropped(monkeypatch):
    rows = [
        {"오픈마켓주문번호": "E", "주문일": "", "주문상태": "취소완료", "_kind": "change"},
        {"오픈마켓주문번호": "F", "주문일": "2026-07-15 08:00:00", "주문상태": "결제완료", "_kind": "order"},
    ]
    monkeypatch.setattr(oe, "combined_order_rows", _fake_combined(rows))
    out = oe.new_order_rows(["coupang"], since=SINCE, until=UNTIL)
    assert {r["오픈마켓주문번호"] for r in out} == {"F"}   # 공란 change(쿠팡·11번가) 제외
```

- [ ] **Step 2: 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/orders/test_new_order_rows.py -q`
Expected: FAIL — `AttributeError: module 'lemouton.markets.order_export' has no attribute 'new_order_rows'`

- [ ] **Step 3: 최소 구현**

In `order_export.py`, `_fetch_combined` 반환 직전에 기본 태그 부여. `_fetch_combined` 끝의 `return rows` 앞에 추가:

```python
    for _r in rows:
        _r.setdefault("_kind", "order")   # 클레임 빌더가 'change'로 override(Task 2~4)
    return rows
```

`combined_order_rows` 정의 아래(또는 `_filter_by_order_date` 근처)에 신설:

```python
def _window(since, until, days, now=None):
    """필터용 [lo, hi] date 튜플. since/until 우선, 없으면 최근 days일."""
    if since and until:
        return since.date(), until.date()
    now = now or _dt.datetime.now(KST)
    return (now - _dt.timedelta(days=days)).date(), now.date()


def new_order_rows(markets, days: int = 7, now=None, use_cache: bool = False,
                   since=None, until=None, include_settlement: bool = True,
                   warnings=None) -> list:
    """주문일 탭 전용 — 실주문일이 기간 안인 주문만.

    order 행은 항상 유지(취소완료여도 그날 들어온 주문이면 남김 = '상태 무관').
    change 행(취소/교환/반품 이벤트)은 실주문일이 기간 안일 때만 유지(롯데온 등),
    실주문일 공란/기간밖(쿠팡·11번가·옛주문)은 제외 → 기능 #2가 변경일 기준으로 잡는다.
    """
    rows = combined_order_rows(markets, days=days, now=now, use_cache=use_cache,
                               since=since, until=until,
                               include_settlement=include_settlement, warnings=warnings)
    lo, hi = _window(since, until, days, now)
    out = []
    for r in rows:
        if r.get("_kind") == "change":
            d = _row_order_date(r)               # 실주문일 파싱(공란/실패→None)
            if d is None or not (lo <= d <= hi):
                continue
        out.append(r)
    return out
```

- [ ] **Step 4: 통과 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/orders/test_new_order_rows.py -q`
Expected: PASS (3 passed)

- [ ] **Step 5: 커밋**

```bash
git add 프로그램/_시스템/lemouton/markets/order_export.py 프로그램/_시스템/tests/orders/test_new_order_rows.py
git commit -m "feat(orders): new_order_rows — 실주문일 기간내 행만(_kind 태그 기반)"
```

---

## Task 2: 롯데온 클레임 태깅 + 주문일 위생

**Files:**
- Modify: `프로그램/_시스템/lemouton/markets/order_export.py` (`lotteon_order_rows._claim_row`, 약 316–338행)
- Test: `프로그램/_시스템/tests/platforms/test_order_export.py`

- [ ] **Step 1: 실패 테스트 작성** — `test_order_export.py` 끝에 추가:

```python
def test_lotteon_claim_tagged_and_orderdate_hygiene(monkeypatch):
    """롯데온 클레임 행: _kind='change', _change_date=clmReqDttm, 주문일=odAccpDttm(폴백 없음)."""
    since = dt.datetime(2026, 7, 15, tzinfo=KST)
    until = dt.datetime(2026, 7, 15, 23, tzinfo=KST)
    claim_item = {"odNo": "LO1", "spdNm": "코트", "sitmNm": "블랙/95", "cnclQty": 1,
                  "odAccpDttm": "20260715090000", "clmReqDttm": "20260715120000",
                  "odPrgsStepCd": "21", "itmSlPrc": 189000}
    monkeypatch.setattr("shared.platforms.lotteon.orders.iter_delivery_orders",
                        lambda *a, **k: iter([]))
    monkeypatch.setattr("shared.platforms.lotteon.orders.iter_progress_states",
                        lambda *a, **k: iter([]))
    monkeypatch.setattr("shared.platforms.lotteon.claims.iter_cancel",
                        lambda *a, **k: iter([claim_item]))
    monkeypatch.setattr("shared.platforms.lotteon.claims.iter_return", lambda *a, **k: iter([]))
    monkeypatch.setattr("shared.platforms.lotteon.claims.iter_exchange", lambda *a, **k: iter([]))
    monkeypatch.setattr("shared.platforms.lotteon.claims.commission_map", lambda *a, **k: {})
    rows = oe.lotteon_order_rows(since, until, client=object(), include_settlement=False)
    claim = [r for r in rows if r["오픈마켓주문번호"] == "LO1"][0]
    assert claim["_kind"] == "change"
    assert claim["_change_date"] == "20260715120000"     # clmReqDttm
    assert claim["주문일"] == "20260715090000"            # odAccpDttm (clmReqDttm 폴백 아님)
```

- [ ] **Step 2: 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/platforms/test_order_export.py::test_lotteon_claim_tagged_and_orderdate_hygiene -q`
Expected: FAIL — `KeyError: '_kind'` (또는 주문일 불일치)

- [ ] **Step 3: 최소 구현** — `_claim_row`(롯데온) 반환 dict 수정:

`"주문일": str(_g(it, "odAccpDttm", "clmReqDttm")),` → 다음으로 교체:

```python
            "주문일": str(_g(it, "odAccpDttm", default="")),   # 실주문접수일만(클레임일 폴백 금지)
```

그리고 같은 dict 마지막 필드들 뒤(`"실결제금액": "", "송장입력": "",` 다음)에 추가:

```python
            "_kind": "change",
            "_change_date": str(_g(it, "clmReqDttm", default="")),   # 변경일(#2용)
```

- [ ] **Step 4: 통과 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/platforms/test_order_export.py::test_lotteon_claim_tagged_and_orderdate_hygiene -q`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add 프로그램/_시스템/lemouton/markets/order_export.py 프로그램/_시스템/tests/platforms/test_order_export.py
git commit -m "feat(orders): 롯데온 클레임 _kind='change'+_change_date, 주문일=odAccpDttm만"
```

---

## Task 3: 쿠팡 클레임 태깅 + 주문일 공란

**Files:**
- Modify: `프로그램/_시스템/lemouton/markets/order_export.py` (`coupang_order_rows._cp_claim_row`, 약 541–556행)
- Test: `프로그램/_시스템/tests/platforms/test_order_export.py`

- [ ] **Step 1: 실패 테스트 작성** — `test_order_export.py` 끝에 추가:

```python
def test_coupang_claim_tagged_and_orderdate_blank(monkeypatch):
    """쿠팡 클레임 행: _kind='change', _change_date=createdAt, 주문일=''(실주문일 미제공)."""
    since = dt.datetime(2026, 7, 15, tzinfo=KST)
    until = dt.datetime(2026, 7, 15, 23, tzinfo=KST)
    ret = {"orderId": "CP1", "receiptType": "CANCEL", "receiptStatus": "RETURNS_UNCHECKED",
           "reasonCodeText": "변심", "requesterName": "구매자", "createdAt": "2026-07-15T12:00:00",
           "returnItems": [{"sellerProductName": "코트", "vendorItemName": "블랙/95", "cancelCount": 1}]}
    monkeypatch.setattr("shared.platforms.coupang.orders.fetch_orders",
                        lambda *a, **k: {"data": []})
    monkeypatch.setattr("shared.platforms.coupang.claims.iter_returns",
                        lambda *a, **k: iter([ret]))
    monkeypatch.setattr("shared.platforms.coupang.claims.iter_exchanges", lambda *a, **k: iter([]))
    rows = oe.coupang_order_rows(since, until, client=object(), include_settlement=False)
    claim = [r for r in rows if r["오픈마켓주문번호"] == "CP1"][0]
    assert claim["_kind"] == "change"
    assert claim["주문일"] == ""                          # 실주문일 미제공 → 공란
    assert claim["_change_date"] == "2026-07-15T12:00:00"  # createdAt
```

- [ ] **Step 2: 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/platforms/test_order_export.py::test_coupang_claim_tagged_and_orderdate_blank -q`
Expected: FAIL — `KeyError: '_kind'` (또는 주문일이 createdAt)

- [ ] **Step 3: 최소 구현** — `_cp_claim_row` 수정:

`"주문일": str(cdt or ""), "판매처": "쿠팡",` → 다음으로 교체:

```python
            "주문일": "", "판매처": "쿠팡",   # 쿠팡 클레임은 실주문일 미제공(§5 한계) → 공란
```

그리고 dict 마지막(`"실결제금액": "", "송장입력": "",` 다음)에 추가:

```python
            "_kind": "change",
            "_change_date": str(cdt or ""),   # createdAt=변경일(#2용)
```

- [ ] **Step 4: 통과 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/platforms/test_order_export.py::test_coupang_claim_tagged_and_orderdate_blank -q`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add 프로그램/_시스템/lemouton/markets/order_export.py 프로그램/_시스템/tests/platforms/test_order_export.py
git commit -m "feat(orders): 쿠팡 클레임 _kind='change'+_change_date, 주문일 공란(실주문일 미제공)"
```

---

## Task 4: 11번가 클레임 태깅

**Files:**
- Modify: `프로그램/_시스템/lemouton/markets/order_export.py` (`eleven11_order_rows._claim_row`, 약 827–856행 — `주문일:""` 유지)
- Test: `프로그램/_시스템/tests/platforms/test_order_export.py`

- [ ] **Step 1: 실패 테스트 작성** — `test_order_export.py` 끝에 추가:

```python
def test_eleven11_claim_tagged(monkeypatch):
    """11번가 클레임 행: _kind='change' (주문일은 공란 유지)."""
    since = dt.datetime(2026, 7, 15, tzinfo=KST)
    until = dt.datetime(2026, 7, 15, 23, tzinfo=KST)
    cancel = {"ordNo": "E1", "slctPrdOptNm": "블랙/95", "ordCnQty": 1,
              "ordPrdStat": "701", "clmDt": "20260715120000"}
    mod = "shared.platforms.eleven11.orders."
    for name in ("iter_orders", "iter_delivered", "iter_completed", "iter_preparing",
                 "iter_shipping", "iter_canceled", "iter_return", "iter_exchange"):
        monkeypatch.setattr(mod + name, lambda *a, **k: iter([]))
    monkeypatch.setattr(mod + "iter_cancel", lambda *a, **k: iter([cancel]))
    rows = oe.eleven11_order_rows(since, until, client=object())
    claim = [r for r in rows if r["오픈마켓주문번호"] == "E1"][0]
    assert claim["_kind"] == "change"
    assert claim["주문일"] == ""
```

- [ ] **Step 2: 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/platforms/test_order_export.py::test_eleven11_claim_tagged -q`
Expected: FAIL — `KeyError: '_kind'`

- [ ] **Step 3: 최소 구현** — 11번가 `_claim_row`와 `_return_row`가 공유하는 `_claim_row` dict 마지막(`"송장입력": _g11(od, "twPrdInvcNo"),` 다음)에 추가:

```python
            "_kind": "change",
            "_change_date": str(_g11(od, "clmDt", default="") or ""),   # 변경일 best-effort(#2용)
```

(`_return_row`는 `_claim_row`를 호출하므로 자동 적용.)

- [ ] **Step 4: 통과 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/platforms/test_order_export.py::test_eleven11_claim_tagged -q`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add 프로그램/_시스템/lemouton/markets/order_export.py 프로그램/_시스템/tests/platforms/test_order_export.py
git commit -m "feat(orders): 11번가 클레임 _kind='change'+_change_date(best-effort)"
```

---

## Task 5: `/orders/preview.json` 배선

**Files:**
- Modify: `프로그램/_시스템/webapp/routes/orders.py` (약 237행, `orders_preview`)
- Test: `프로그램/_시스템/tests/orders/test_new_order_rows.py`

- [ ] **Step 1: 실패 테스트 작성** — `test_new_order_rows.py`에 추가:

```python
def test_preview_json_uses_new_order_rows(monkeypatch):
    """preview.json 이 new_order_rows(=change 제외)를 쓰는지."""
    from webapp import create_app  # 앱 팩토리(기존 테스트 관례 따름)
    called = {"fn": None}

    def _fake_new(markets, **kw):
        called["fn"] = "new_order_rows"
        return [{"오픈마켓주문번호": "A", "주문일": "2026-07-15 09:00:00", "_kind": "order"}]

    monkeypatch.setattr("webapp.routes.orders._oe.new_order_rows", _fake_new)
    app = create_app()
    app.config["TESTING"] = True
    c = app.test_client()
    resp = c.get("/orders/preview.json?markets=smartstore&range=today")
    assert resp.status_code == 200
    assert called["fn"] == "new_order_rows"
```

> **주의:** 앱 팩토리 import 경로(`from webapp import create_app`)와 인증 우회는 `tests/conftest.py`·기존 라우트 테스트(`tests/orders/test_orders_tab.py`)의 관례를 그대로 따를 것. 다르면 그 파일의 픽스처(app/client)를 재사용하도록 이 테스트를 맞춘다.

- [ ] **Step 2: 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/orders/test_new_order_rows.py::test_preview_json_uses_new_order_rows -q`
Expected: FAIL — `called["fn"]` 이 None (아직 combined_order_rows 사용)

- [ ] **Step 3: 최소 구현** — `orders_preview`(약 237행)에서 호출 교체:

`rows = _oe.combined_order_rows(markets, days=days, use_cache=True,` → `rows = _oe.new_order_rows(markets, days=days, use_cache=True,` (인자 목록·이어지는 줄 동일 유지).

- [ ] **Step 4: 통과 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/orders/test_new_order_rows.py -q`
Expected: PASS (4 passed)

- [ ] **Step 5: 커밋**

```bash
git add 프로그램/_시스템/webapp/routes/orders.py 프로그램/_시스템/tests/orders/test_new_order_rows.py
git commit -m "feat(orders): preview.json → new_order_rows(주문일=신규주문만)"
```

---

## Task 6: 회귀·라이브 검증·머지·배포

**Files:** 없음(검증·배포)

- [ ] **Step 1: 관련 회귀 스위트 실행**

Run:
```bash
cd 프로그램/_시스템 && python -m pytest tests/platforms/test_order_export.py tests/orders/ tests/delivery/test_market_enrich.py tests/margin/test_order_export_settle_source.py -q
```
Expected: 전부 PASS (태그 필드 추가만이라 `combined_order_rows` 계약 불변).

- [ ] **Step 2: 로컬 앱 기동 + preview 육안**

`ENVIRONMENT=team-share-dev DISABLE_AUTH=1 FLASK_PORT=8899` 로컬 기동(메모리 관례). 브라우저 MCP로 `/orders/?tab=list` 접속 → 「오늘/최근7일」에서 (a) 옛주문 취소/반품 churn 행이 사라졌는지 (b) 이 기간 유입 후 취소된 스스·롯데온 주문은 남는지 확인. `/live-browser-verify` 게이트 따름.

- [ ] **Step 3: main 머지 + 배포**

워크트리 → `main` 머지 → push. 배포 = `push main → aws-lightsail-deploy.yml`(Fly 아님). 서버 `54.116.196.90` 서빙 파일 반영 확인(`mou-m.com`).

- [ ] **Step 4: 라이브 최종 검증**

`mou-m.com/orders/?tab=list` 실접속으로 Step 2 항목 재확인. 완료 기준(스펙 §9) 충족 시 종료. 7섹션 보고서 작성(`/ui-verify` 형식).

---

## Self-Review (작성자 체크)

- **스펙 커버리지:** §4.1 태그=Task2~4 + `_fetch_combined` 기본값(Task1) / §4.1(b) 주문일 위생=Task2(롯데온)·Task3(쿠팡)·Task4(11번가) / §4.2 `new_order_rows`=Task1 / §4.3 배선=Task5 / §5 한계=Task3·4 테스트로 고정(쿠팡·11번가 공란→제외) / §7 테스트=Task1~5 / §6 엣지=Task1 테스트(order 취소완료 유지·change 기간밖 제외). ✅ 갭 없음.
- **플레이스홀더 스캔:** 모든 코드 스텝에 실제 코드·명령·기대출력 포함. Task5 앱 팩토리 관례는 주의 노트로 명시(추측 아님, 기존 픽스처 재사용 지시). ✅
- **타입/이름 일관성:** `_kind`('order'/'change'), `_change_date`, `new_order_rows`, `_window`, `_row_order_date`(기존 함수 재사용) 전 태스크 동일. ✅
