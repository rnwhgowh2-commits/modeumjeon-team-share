# 개발 체크리스트 탭 1단계 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 「데이터 코드 지도」에 판매처·소싱처 **개발 체크리스트** 탭을 추가해, 마켓/소싱처별로 무엇이 남았는지 한 표로 보이게 한다.

**Architecture:** 판정 로직은 순수 함수 하나(`lemouton/policy/checklist.py`)에 모으고, 근거는 이미 있는 `policy/required.py`(마켓이 요구하는가 + 등록 API 원문) · `registration/market_limits.py`(마켓별 상한) · `required.wiring_of`(실제로 나가는가)를 **읽기만** 한다. 사람이 다는 값(실계정 검증일·불가 사유)은 `webapp/data/dev_checklist_marks.json` 한 장에만 둔다. 화면은 JSON 을 fetch 해 그리므로 판매처·소싱처가 **렌더러 한 벌**을 공용한다.

**Tech Stack:** Python 3 / Flask (Blueprint) / 순수 함수 + pytest / 바닐라 JS (템플릿 인라인, 기존 `map.html` 방식 그대로)

**Spec:** `docs/superpowers/specs/2026-08-12-개발체크리스트-탭-design.md`

---

## 파일 구조

| 파일 | 책임 |
|---|---|
| `scripts/gen_dev_checklist_columns.py` (신규) | 사장님 엑셀 → 열 정의 JSON 생성. 엑셀이 바뀌면 다시 돌린다 |
| `webapp/data/dev_checklist_columns.json` (신규) | **판매처 25열** 정의 — 묶음·이름·우리 기준·프로그램 항목 매핑·마켓별 엑셀 메모 |
| `webapp/data/dev_checklist_sourcing.json` (신규) | **소싱처 8묶음** 열 정의 |
| `webapp/data/dev_checklist_marks.json` (신규) | 손보정 — 실계정 검증일 · 불가 사유 |
| `lemouton/policy/checklist.py` (신규) | 순수 함수. 열 + 근거 + 손보정 → 셀 상태. **Flask 의존 없음** |
| `webapp/routes/marketplace_guide.py` (수정) | `GET /marketplace-guide/checklist.json` 추가 |
| `webapp/routes/sourcing_guide.py` (수정) | `GET /sourcing-guide/checklist.json` 추가 |
| `webapp/templates/_checklist_matrix.html` (신규) | 표 + 호버 정보창 — **두 지도가 공용** |
| `webapp/templates/marketplace_guide/map.html` (수정) | 탭 추가 |
| `webapp/templates/sourcing_guide/map.html` (수정) | 9번째 탭 추가 |
| `tests/policy/test_checklist.py` (신규) | 판정 순수 함수 |
| `tests/marketplace_guide/test_checklist_route.py` (신규) | 라우트 + 스키마 |
| `tests/sourcing/test_guide_tabs.py` (수정) | 탭 수 잠금 갱신 |

---

## Task 1: 열 정의 데이터 — 엑셀 25열을 저장소로

**Files:**
- Create: `프로그램/_시스템/scripts/gen_dev_checklist_columns.py`
- Create: `프로그램/_시스템/webapp/data/dev_checklist_columns.json`
- Test: `프로그램/_시스템/tests/policy/test_checklist.py`

사장님 엑셀은 바탕화면에 있어 서버가 읽을 수 없다. **한 번 뽑아 저장소에 넣고**, 엑셀이 바뀌면 스크립트를 다시 돌린다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
# 프로그램/_시스템/tests/policy/test_checklist.py
# -*- coding: utf-8 -*-
"""개발 체크리스트 — 열 정의와 셀 판정."""
import json
import pathlib

import pytest

from lemouton.policy import checklist as C

DATA = pathlib.Path(C.__file__).parents[2] / "webapp" / "data"


def _cols():
    return json.loads((DATA / "dev_checklist_columns.json").read_text(encoding="utf-8"))


def test_columns_are_25():
    cols = _cols()["columns"]
    assert len(cols) == 25, f"엑셀 Sheet2 는 B~Z 25열이다 (지금 {len(cols)})"


def test_every_column_has_required_keys():
    for c in _cols()["columns"]:
        for k in ("col", "group", "name", "rule", "item", "specs"):
            assert k in c, f"{c.get('name')} 에 {k} 없음"


def test_specs_cover_six_markets():
    markets = {"coupang", "smartstore", "lotteon", "eleven11", "auction", "gmarket"}
    for c in _cols()["columns"]:
        assert set(c["specs"]) == markets, f"{c['name']} 의 마켓 목록이 다름"


def test_item_maps_to_real_policy_item_or_none():
    from lemouton.registration.process_policy import ITEM_KEYS
    for c in _cols()["columns"]:
        if c["item"] is not None:
            assert c["item"] in ITEM_KEYS, f"{c['name']} → 없는 항목 {c['item']}"


def test_price_column_carries_owner_rule():
    """사장님이 엑셀에 적어 두신 「▶」 기준이 살아 있어야 한다."""
    price = [c for c in _cols()["columns"] if c["item"] == "price"][0]
    assert "할인가" in price["rule"] and "마진율" in price["rule"]
```

- [ ] **Step 2: 실패를 확인한다**

Run: `cd 프로그램/_시스템 && python -m pytest tests/policy/test_checklist.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'lemouton.policy.checklist'`

- [ ] **Step 3: 생성 스크립트를 쓴다**

```python
# 프로그램/_시스템/scripts/gen_dev_checklist_columns.py
# -*- coding: utf-8 -*-
"""사장님 엑셀 「■ 마켓별 상품등록 정보」 Sheet2 → 판매처 열 정의 JSON.

엑셀은 사장님 바탕화면에 있어 서버가 못 읽는다. **한 번 뽑아 저장소에 넣는다.**
엑셀이 바뀌면 이 스크립트를 다시 돌리고 결과를 커밋한다.

  python scripts/gen_dev_checklist_columns.py "C:/Users/seung/OneDrive/바탕 화면/■ 마켓별 상품등록 정보.xlsx"

🔴 열 순서·묶음은 엑셀 그대로 둔다. 우리가 재배열하면 사장님이 보던 표와 어긋난다.
"""
import json
import pathlib
import sys

import openpyxl

# 엑셀 열(B~Z) → 가공정책 항목 키. None = 아직 담을 칸이 프로그램에 없음.
ITEM_BY_COL = {
    2: "name", 3: "brand", 4: "category", 5: "price", 6: "price",
    7: "images", 8: "images", 9: "options", 10: "detail",
    11: "shipping", 12: "shipping", 13: "price_compare",
    14: "listing", 15: "listing", 16: "origin", 17: "listing", 18: "listing",
    19: "kc", 20: "notice", 21: "tags",
    22: "ids", 23: "ids", 24: None, 25: "shipping", 26: None,
}
ROWS = [(3, "lotteon"), (4, "coupang"), (5, "smartstore"),
        (6, "eleven11"), (7, "auction"), (8, "gmarket")]


def _split_rule(text):
    """「이름 ▶ 사장님 기준」이 한 칸에 붙어 있다. 둘로 나눈다."""
    t = (text or "").replace("\r", "")
    i = t.find("▶")
    return (t.strip(), "") if i < 0 else (t[:i].strip(), t[i + 1:].strip())


def build(xlsx_path):
    ws = openpyxl.load_workbook(xlsx_path, data_only=True)["Sheet2"]
    merged = {}
    for rng in ws.merged_cells.ranges:
        top_left = ws.cell(rng.min_row, rng.min_col).value
        for r in range(rng.min_row, rng.max_row + 1):
            for c in range(rng.min_col, rng.max_col + 1):
                merged[(r, c)] = top_left

    def cell(r, c):
        v = merged[(r, c)] if (r, c) in merged else ws.cell(r, c).value
        return "" if v is None else str(v).strip()

    columns = []
    for col in range(2, 27):
        group, _ = _split_rule(cell(1, col))
        name, rule = _split_rule(cell(2, col) or cell(1, col))
        columns.append({
            "col": col,
            "group": group,
            "name": name or group,
            "rule": rule or _split_rule(cell(1, col))[1],
            "item": ITEM_BY_COL[col],
            "specs": {mk: cell(row, col) for row, mk in ROWS},
        })
    return {"source": "■ 마켓별 상품등록 정보.xlsx / Sheet2", "columns": columns}


if __name__ == "__main__":
    out = pathlib.Path(__file__).parents[1] / "webapp" / "data" / "dev_checklist_columns.json"
    data = build(sys.argv[1])
    out.write_text(json.dumps(data, ensure_ascii=False, indent=1), encoding="utf-8")
    print(f"열 {len(data['columns'])}개 → {out}")
```

- [ ] **Step 4: 스크립트를 돌려 JSON 을 만든다**

Run:
```bash
cd 프로그램/_시스템 && python scripts/gen_dev_checklist_columns.py "C:/Users/seung/OneDrive/바탕 화면/■ 마켓별 상품등록 정보.xlsx"
```
Expected: `열 25개 → .../webapp/data/dev_checklist_columns.json`

- [ ] **Step 5: 최소 모듈을 만들어 테스트를 통과시킨다**

```python
# 프로그램/_시스템/lemouton/policy/checklist.py
# -*- coding: utf-8 -*-
"""개발 체크리스트 — 셀 판정 (순수 함수, Flask 의존 없음)."""
from __future__ import annotations

import json
import os

_DATA = os.path.join(os.path.dirname(os.path.dirname(os.path.dirname(__file__))),
                     "webapp", "data")


def load_columns(name: str = "dev_checklist_columns.json") -> list[dict]:
    with open(os.path.join(_DATA, name), encoding="utf-8") as f:
        return json.load(f)["columns"]
```

- [ ] **Step 6: 테스트가 통과하는지 확인한다**

Run: `cd 프로그램/_시스템 && python -m pytest tests/policy/test_checklist.py -v`
Expected: 5 passed

- [ ] **Step 7: 커밋**

```bash
git add 프로그램/_시스템/scripts/gen_dev_checklist_columns.py 프로그램/_시스템/webapp/data/dev_checklist_columns.json 프로그램/_시스템/lemouton/policy/checklist.py 프로그램/_시스템/tests/policy/test_checklist.py
git commit -m "feat(체크리스트): 사장님 엑셀 25열을 저장소 데이터로 (생성 스크립트 포함)"
```

---

## Task 2: 셀 판정 — 5단계

**Files:**
- Modify: `프로그램/_시스템/lemouton/policy/checklist.py`
- Test: `프로그램/_시스템/tests/policy/test_checklist.py`

- [ ] **Step 1: 실패하는 테스트를 덧붙인다**

```python
# tests/policy/test_checklist.py 에 이어서

def test_cell_na_when_excel_blank():
    """엑셀이 비었거나 「-」면 그 마켓엔 해당 없음."""
    col = {"col": 22, "name": "모델번호", "rule": "", "item": "ids",
           "specs": {"eleven11": "-"}}
    assert C.cell_state("eleven11", col) == "na"


def test_cell_impossible_when_excel_x():
    col = {"col": 13, "name": "가격비교 노출", "rule": "", "item": "price_compare",
           "specs": {"coupang": "X"}}
    assert C.cell_state("coupang", col) == "impossible"


def test_cell_todo_when_no_program_item():
    col = {"col": 24, "name": "사이즈", "rule": "", "item": None,
           "specs": {"coupang": "-입력X"}}
    assert C.cell_state("coupang", col) == "todo"


def test_cell_todo_when_market_evidence_unknown():
    """롯데온은 등록 문서가 요약본이라 근거를 못 찾는다 = 미착수(불가 아님)."""
    col = {"col": 21, "name": "태그", "rule": "", "item": "tags",
           "specs": {"lotteon": "값 있음"}}
    assert C.cell_state("lotteon", col) == "todo"


def test_cell_stored_only_when_not_wired():
    """칸도 있고 마켓도 받는데 보내는 코드가 없으면 「저장만 됨」."""
    col = {"col": 2, "name": "상품명", "rule": "", "item": "name",
           "specs": {"smartstore": "100글자"}}
    assert C.cell_state("smartstore", col) == "stored"


def test_cell_done_needs_both_wired_and_verified():
    """판매가는 나가지만(WIRED), 실계정 확인 표시가 있어야 검증완료."""
    col = {"col": 5, "name": "판매가", "rule": "", "item": "price",
           "specs": {"smartstore": "판매가"}}
    assert C.cell_state("smartstore", col) == "wired"
    assert C.cell_state("smartstore", col, marks={"smartstore:5": {"verified": "2026-08-12"}}) == "done"


def test_conflict_when_market_requires_but_we_skip():
    """11번가는 「등록 기본값」을 [필수]로 요구하는데 엑셀 제조사 칸은 「-」다.

    ⚠️ 조합을 바꾸지 마라 — required.py 의 실제 값에 맞춰 고른 것이다.
      (모델번호/11번가는 required 가 아니라 conditional 이라 여기 쓰면 안 된다.)
    """
    col = {"col": 17, "name": "제조사", "rule": "", "item": "listing",
           "specs": {"eleven11": "-"}}
    assert "필수" in C.conflict_of("eleven11", col)


def test_no_conflict_when_both_agree():
    col = {"col": 5, "name": "판매가", "rule": "", "item": "price",
           "specs": {"smartstore": "판매가"}}
    assert C.conflict_of("smartstore", col) == ""
```

- [ ] **Step 2: 실패를 확인한다**

Run: `cd 프로그램/_시스템 && python -m pytest tests/policy/test_checklist.py -v`
Expected: FAIL — `AttributeError: module 'lemouton.policy.checklist' has no attribute 'cell_state'`

- [ ] **Step 3: 판정 함수를 쓴다**

```python
# lemouton/policy/checklist.py 에 이어서

from lemouton.policy import required as _req

#: 칸 상태 — 화면 표시와 뜻
NA = "na"                # ➖ 해당없음   : 엑셀이 비었거나 「-」
IMPOSSIBLE = "impossible"  # ⚫ 불가      : 엑셀이 「X」 (마켓에 기능 자체가 없음)
TODO = "todo"            # ⬜ 미착수     : 우리 칸이 없거나 마켓 근거를 못 찾음
STORED = "stored"        # 🟡 저장만 됨  : 칸은 있는데 보내는 코드가 없음
WIRED = "wired"          # 🟢 나감(미검증): 보내지만 실계정 확인 전
DONE = "done"            # 🟢 검증완료   : 보내고 실계정으로 확인함

_BLANK = ("", "-")


def _spec(market: str, col: dict) -> str:
    return (col.get("specs") or {}).get(market, "").strip()


def cell_state(market: str, col: dict, marks: dict | None = None) -> str:
    """그 칸의 상태 하나. 판정 순서를 바꾸지 말 것 — 앞 조건이 뒤를 가린다."""
    spec = _spec(market, col)
    if spec in _BLANK:
        return NA
    if spec.upper() == "X":
        return IMPOSSIBLE
    item = col.get("item")
    if not item:
        return TODO                       # 프로그램에 담을 칸이 아직 없다
    status, _evidence, _note = _req.status_of(market, item)
    if status == _req.UNKNOWN:
        return TODO                       # 「없다」가 아니라 「모른다」 — 지어내지 않는다
    if _req.wiring_of(item)[0] != _req.WIRED:
        return STORED                     # 채워도 안 나간다
    key = f"{market}:{col['col']}"
    if ((marks or {}).get(key) or {}).get("verified"):
        return DONE
    return WIRED


def conflict_of(market: str, col: dict) -> str:
    """우리 기준과 마켓 문서가 어긋나는가. 기계 판정만 — 추측하지 않는다."""
    item = col.get("item")
    if not item:
        return ""
    spec = _spec(market, col)
    status, _e, _n = _req.status_of(market, item)
    if spec in _BLANK or spec.upper() == "X":
        if status == _req.REQUIRED:
            return "마켓 문서는 [필수]라는데 우리는 안 쓰기로 돼 있습니다"
    return ""
```

- [ ] **Step 4: 테스트가 통과하는지 확인한다**

Run: `cd 프로그램/_시스템 && python -m pytest tests/policy/test_checklist.py -v`
Expected: 13 passed

- [ ] **Step 5: 커밋**

```bash
git add 프로그램/_시스템/lemouton/policy/checklist.py 프로그램/_시스템/tests/policy/test_checklist.py
git commit -m "feat(체크리스트): 칸 5단계 판정 — 「저장만 됨」을 별도 상태로"
```

---

## Task 3: 손보정 파일 + 한 마켓 요약

**Files:**
- Create: `프로그램/_시스템/webapp/data/dev_checklist_marks.json`
- Modify: `프로그램/_시스템/lemouton/policy/checklist.py`
- Test: `프로그램/_시스템/tests/policy/test_checklist.py`

- [ ] **Step 1: 실패하는 테스트를 덧붙인다**

```python
# tests/policy/test_checklist.py 에 이어서

def test_marks_file_loads_and_is_a_dict():
    marks = C.load_marks()
    assert isinstance(marks, dict)


def test_build_returns_row_per_market():
    data = C.build()
    assert [r["market"] for r in data["rows"]] == \
        ["coupang", "smartstore", "lotteon", "eleven11", "auction", "gmarket"]


def test_build_cell_count_is_columns_times_markets():
    data = C.build()
    assert len(data["cells"]) == len(data["columns"]) * len(data["rows"])


def test_build_counts_exclude_na_from_denominator():
    """「해당없음」은 채울 대상이 아니므로 분모에서 뺀다 — 100%가 영영 안 차면 안 된다."""
    data = C.build()
    for row in data["rows"]:
        c = row["counts"]
        assert c["total"] == len(data["columns"]) - c["na"]


def test_build_marks_lotteon_as_evidence_missing():
    """롯데온은 등록 문서가 요약본이라 대부분 미착수로 떠야 한다."""
    data = C.build()
    lotteon = [r for r in data["rows"] if r["market"] == "lotteon"][0]
    assert lotteon["counts"]["todo"] >= 10


def test_drift_flags_verified_on_something_that_never_goes_out():
    """🔴 조용한 통과 금지 — 나가지도 않는 값에 「검증완료」가 달려 있으면 알린다."""
    problems = C.drift({"smartstore:2": {"verified": "2026-08-12"}})   # 2 = 상품명(저장만 됨)
    assert problems and "상품명" in problems[0]


def test_drift_silent_when_marks_are_sane():
    assert C.drift({"smartstore:5": {"verified": "2026-08-12"}}) == []   # 5 = 판매가(나감)


def test_drift_flags_unknown_column():
    problems = C.drift({"smartstore:99": {"verified": "2026-08-12"}})
    assert problems and "99" in problems[0]
```

- [ ] **Step 2: 실패를 확인한다**

Run: `cd 프로그램/_시스템 && python -m pytest tests/policy/test_checklist.py -v`
Expected: FAIL — `AttributeError: ... has no attribute 'load_marks'`

- [ ] **Step 3: 손보정 파일을 만든다**

```json
{
  "_설명": "사람이 다는 값만 둔다. 코드로 알 수 없는 것 — 실계정 왕복 검증일과 불가 사유.",
  "_형식": "\"<마켓>:<엑셀 열번호>\": {\"verified\": \"YYYY-MM-DD\", \"note\": \"…\"}",
  "marks": {}
}
```
(파일: `프로그램/_시스템/webapp/data/dev_checklist_marks.json`)

- [ ] **Step 4: 조립 함수를 쓴다**

```python
# lemouton/policy/checklist.py 에 이어서

MARKETS = [("coupang", "쿠팡"), ("smartstore", "스마트스토어"), ("lotteon", "롯데온"),
           ("eleven11", "11번가"), ("auction", "옥션"), ("gmarket", "G마켓")]


def load_marks() -> dict:
    path = os.path.join(_DATA, "dev_checklist_marks.json")
    if not os.path.exists(path):
        return {}
    with open(path, encoding="utf-8") as f:
        return json.load(f).get("marks") or {}


def build(columns_file: str = "dev_checklist_columns.json") -> dict:
    """화면이 그대로 그릴 수 있는 한 벌."""
    cols = load_columns(columns_file)
    marks = load_marks()
    cells, rows = {}, []
    for market, label in MARKETS:
        counts = {k: 0 for k in (NA, IMPOSSIBLE, TODO, STORED, WIRED, DONE)}
        for col in cols:
            state = cell_state(market, col, marks)
            counts[state] += 1
            item = col.get("item")
            status, evidence, note = (_req.status_of(market, item) if item
                                      else (_req.UNKNOWN, "", "프로그램에 담을 칸이 아직 없습니다"))
            cells[f"{market}:{col['col']}"] = {
                "state": state,
                "spec": _spec(market, col),
                "required": status,
                "evidence": evidence,
                "note": note,
                "wiring": (_req.wiring_of(item) if item else ("", ""))[0],
                "wiring_note": (_req.wiring_of(item) if item else ("", ""))[1],
                "api": _req.SOURCE_API.get(market, ""),
                "conflict": conflict_of(market, col),
                "verified": ((marks.get(f"{market}:{col['col']}") or {}).get("verified") or ""),
            }
        counts["total"] = len(cols) - counts[NA]
        rows.append({"market": market, "label": label, "counts": counts})
    return {"columns": cols, "rows": rows, "cells": cells,
            "drift": drift(marks)}


def drift(marks: dict, columns_file: str = "dev_checklist_columns.json") -> list[str]:
    """손으로 단 값이 코드와 어긋나면 사람 말로 돌려준다. 없으면 빈 목록.

    🔴 조용한 통과 금지 — 「나가지도 않는 값에 검증완료가 달린 것」이 가장 위험하다.
      그 칸은 화면에서 초록으로 보이지만 마켓에는 아무것도 안 갔다.
    """
    by_col = {c["col"]: c for c in load_columns(columns_file)}
    out = []
    for key, mark in (marks or {}).items():
        if not (mark or {}).get("verified"):
            continue
        market, _, raw = key.partition(":")
        try:
            col = by_col[int(raw)]
        except (ValueError, KeyError):
            out.append(f"손보정에 없는 열 번호가 있습니다: {key}")
            continue
        item = col.get("item")
        if not item:
            out.append(f"「{col['name']}」({market}) 는 프로그램에 담을 칸이 아직 없는데 "
                       f"검증완료로 표시돼 있습니다")
        elif _req.wiring_of(item)[0] != _req.WIRED:
            out.append(f"「{col['name']}」({market}) 는 저장만 되고 마켓으로 나가지 않는데 "
                       f"검증완료로 표시돼 있습니다")
    return out
```

- [ ] **Step 5: 테스트가 통과하는지 확인한다**

Run: `cd 프로그램/_시스템 && python -m pytest tests/policy/test_checklist.py -v`
Expected: 21 passed

- [ ] **Step 6: 커밋**

```bash
git add 프로그램/_시스템/webapp/data/dev_checklist_marks.json 프로그램/_시스템/lemouton/policy/checklist.py 프로그램/_시스템/tests/policy/test_checklist.py
git commit -m "feat(체크리스트): 손보정 파일 + 마켓별 요약 조립"
```

---

## Task 4: 판매처 라우트

**Files:**
- Modify: `프로그램/_시스템/webapp/routes/marketplace_guide.py` (파일 끝에 추가)
- Test: `프로그램/_시스템/tests/marketplace_guide/test_checklist_route.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
# 프로그램/_시스템/tests/marketplace_guide/test_checklist_route.py
# -*- coding: utf-8 -*-
"""개발 체크리스트 데이터 라우트."""
import pytest
from flask import Flask

from webapp.routes import marketplace_guide as mg


@pytest.fixture
def client(monkeypatch):
    monkeypatch.setenv("ENVIRONMENT", "test")   # _admin_only 우회
    app = Flask(__name__)
    app.register_blueprint(mg.bp)
    return app.test_client()


def test_checklist_json_ok(client):
    r = client.get("/marketplace-guide/checklist.json")
    assert r.status_code == 200
    data = r.get_json()
    assert len(data["columns"]) == 25
    assert len(data["rows"]) == 6
    assert len(data["cells"]) == 150


def test_every_cell_has_state_and_evidence_keys(client):
    data = client.get("/marketplace-guide/checklist.json").get_json()
    for key, cell in data["cells"].items():
        for k in ("state", "spec", "required", "evidence", "wiring", "api", "conflict"):
            assert k in cell, f"{key} 에 {k} 없음"


def test_stored_only_is_the_majority(client):
    """정책값 대부분이 저장만 되는 현실이 표에 그대로 드러나야 한다."""
    data = client.get("/marketplace-guide/checklist.json").get_json()
    stored = sum(1 for c in data["cells"].values() if c["state"] == "stored")
    assert stored > 0
```

- [ ] **Step 2: 실패를 확인한다**

Run: `cd 프로그램/_시스템 && python -m pytest tests/marketplace_guide/test_checklist_route.py -v`
Expected: FAIL — 404

- [ ] **Step 3: 라우트를 추가한다**

```python
# webapp/routes/marketplace_guide.py 파일 끝에 추가

@bp.route("/checklist.json")
def checklist_data():
    """개발 체크리스트 데이터. 판정 근거는 policy/required.py 하나뿐이다."""
    from lemouton.policy.checklist import build
    return jsonify(build())
```

- [ ] **Step 4: 테스트가 통과하는지 확인한다**

Run: `cd 프로그램/_시스템 && python -m pytest tests/marketplace_guide/test_checklist_route.py -v`
Expected: 3 passed

- [ ] **Step 5: 커밋**

```bash
git add 프로그램/_시스템/webapp/routes/marketplace_guide.py 프로그램/_시스템/tests/marketplace_guide/test_checklist_route.py
git commit -m "feat(체크리스트): 판매처 데이터 라우트"
```

---

## Task 5: 표 화면 — 공용 조각

**Files:**
- Create: `프로그램/_시스템/webapp/templates/_checklist_matrix.html`
- Test: `프로그램/_시스템/tests/marketplace_guide/test_checklist_route.py` (이어서)

시안 정본: 바탕화면 `모음전 시안 v4 - 개발 체크리스트 확정.html`

🔴 **아래 시험들의 한계를 먼저 못 박는다.** 이 시험들은 「그 글자가 파일에 있나」만 센다 —
**깨진 채로도 통과할 수 있다.** (Task 1 에서 실제로 그 함정을 밟았다: 시험이 정작
`load_columns()` 를 안 불러, 함수 안에 오타를 심어도 5개가 전부 초록불이었다.)

그래서 이 조각의 **진짜 검증은 Task 7 의 라이브 실화면 측정**이다. 여기 시험은
「빠뜨리지 않았나」를 지키는 그물일 뿐이고, 「제대로 동작하나」는 재지 못한다.
**Task 7 을 건너뛰면 이 작업은 검증되지 않은 것이다.**

- [ ] **Step 1: 실패하는 테스트를 덧붙인다**

```python
# tests/marketplace_guide/test_checklist_route.py 에 이어서
import pathlib
from webapp.routes import marketplace_guide as mg

TPL = pathlib.Path(mg.__file__).parents[1] / "templates"


def _matrix():
    return (TPL / "_checklist_matrix.html").read_text(encoding="utf-8")


def test_first_column_is_sticky():
    """마켓 이름은 옆으로 밀어도 계속 보여야 한다."""
    html = _matrix()
    assert "position:sticky" in html and "left:0" in html


def test_numbers_use_tabular_nums():
    assert "tabular-nums" in _matrix()


def test_hover_card_lives_on_body_and_is_fixed():
    """표 안에 두면 스크롤에서 잘린다 (hover-info-card 규칙 1)."""
    html = _matrix()
    assert "document.body.appendChild" in html
    assert "position:fixed" in html


def test_hover_delays_match_standard():
    """열기 140ms · 닫기 250ms (hover-info-card 규칙 2·6)."""
    html = _matrix()
    assert "OPEN_DELAY=140" in html.replace(" ", "")
    assert "CLOSE_DELAY=250" in html.replace(" ", "")


def test_hover_closes_on_scroll():
    html = _matrix()
    assert "'scroll'" in html or '"scroll"' in html


def test_five_states_are_labelled_in_korean():
    html = _matrix()
    for word in ["미착수", "저장만", "검증완료", "불가", "해당없음"]:
        assert word in html


def test_contract_functions_are_exposed():
    """Task 6·8 이 이 두 개만 부른다 — 이름이 바뀌면 탭이 조용히 백지가 된다."""
    html = _matrix()
    assert "window.CHECKLIST_HTML" in html
    assert "window.ckInit" in html


def test_pop_and_panel_have_different_outer_names():
    """같은 이름이 두 물건을 가리키면 한쪽을 고칠 때 다른 쪽이 조용히 바뀐다."""
    html = _matrix()
    assert "ckpop" in html and "ckpanel" in html
    assert "class=\"ckpop ckpanel" not in html
```

- [ ] **Step 2: 실패를 확인한다**

Run: `cd 프로그램/_시스템 && python -m pytest tests/marketplace_guide/test_checklist_route.py -v`
Expected: FAIL — `FileNotFoundError: _checklist_matrix.html`

- [ ] **Step 3: 표 조각을 만든다**

**원본을 손으로 옮겨 적지 않는다.** 확정 시안 파일에서 잘라 온다.

```
원본 : C:/Users/seung/OneDrive/바탕 화면/모음전 시안 v4 - 개발 체크리스트 확정.html
      (같은 내용 사본: C:/dev/모음전 프로젝트/_시안_임시/checklist_v4.html)
가져올 것 : <style> 안의 .ckpop / .ckcard / .ck* 규칙  +  마지막 <script> 의 정보창 코드
버릴 것   : 시안 전용 껍데기 (.mock-win · .variant-tabs · .v-tab · .cmp · .chk · PAGE_CSS)
          + 시안에 박아 둔 const CELLS / COLS / ROWL (실제로는 fetch 로 받는다)
```

**이 조각이 바깥에 내놓는 계약** — Task 6·8 이 이것만 부른다:

```js
// 조각 안에 이 두 개를 반드시 정의한다.
window.CHECKLIST_HTML = '…표와 오른쪽 고정판의 빈 껍데기 HTML…';
window.ckInit = function (url) { /* url 을 fetch → CELLS/COLS/ROWS 채우고 표를 그린다 */ };
```

🔴 **Task 3 재작업으로 바뀐 계약 — 화면이 이걸 모르면 거짓말을 그린다:**

| 값 | 무엇이 바뀌었나 |
|---|---|
| `wiring` | 2값이 아니라 **3값** — `wired`(나감) · `stored`(저장만) · **`none`(프로그램에 칸조차 없음, 12칸)**. `!= 'wired'` 로 뭉뚱그리면 칸조차 없는 12칸이 「저장은 된다」는 거짓말로 뜬다 |
| `verified` | **🟢검증완료 칸에만 실린다.** 나머지 다섯 상태에서는 늘 `""` 다 — 노란 칸에 날짜가 찍히면 「반쯤 검증됨」으로 읽히기 때문이다. 화면은 「날짜가 있으면 검증완료」로 그려도 안전하고, 왜 검증완료가 아닌지는 배너가 말한다 (2차 재작업으로 바뀜 — 예전 「TODO·STORED 에는 실려 올 수 있다」는 이제 틀리다) |
| `drift` | 배너 차례가 정해져 있다 — ① 손보정 파일을 못 읽은 사유 ② 열 정의에 마켓이 빠진 칸 ③ 손보정 어긋남. 표는 어느 경우에도 그대로 그리되 배너를 반드시 띄운다 |
| `counts.total` | 분모에서 ➖해당없음과 ⚫불가를 **둘 다** 뺀 값이다. 진행률은 이 값으로 나눈다 |

지켜야 할 것:

| 규칙 | 값 |
|---|---|
| 마켓 이름 열 | `position:sticky; left:0` |
| 숫자 | `font-variant-numeric:tabular-nums` |
| 정보창 | `document.body.appendChild` + `position:fixed` |
| 열기 지연 / 닫기 지연 | `OPEN_DELAY = 140` / `CLOSE_DELAY = 250` |
| 간격 / 화면 여백 | `GAP = 7` / `EDGE = 8` |
| 정보창 mouseenter | 닫기 타이머 취소 |
| 정보창 mouseleave | 닫기 재예약 |
| 스크롤(창·표 둘 다) | 정보창 닫기 |
| 아래쪽 칸 | 위로 뒤집기 |
| 「더 보기」로 커지면 | 자리 다시 잡기 |
| 정보창 / 고정판 | 바깥틀 이름을 **다르게** (`ckpop` / `ckpanel`), 속 부품만 공용(`ckcard`) |
| 글자 | 11 / 12.5 / 14 **세 가지만** · 줄간격 1.65 |
| 섹션 이름 | 12.5px · 굵기 800 · `#191F28` |
| 표 머리글 | 11px · 굵기 800 · `#4E5968` · **바탕 없음** |
| 「우리 기준」 | `#1B64DA` 파란 칸 |

데이터는 `{{ checklist_url }}` 에서 fetch 한다(판매처·소싱처가 같은 조각을 쓰기 위함).

- [ ] **Step 4: 테스트가 통과하는지 확인한다**

Run: `cd 프로그램/_시스템 && python -m pytest tests/marketplace_guide/test_checklist_route.py -v`
Expected: 11 passed

- [ ] **Step 5: JS 문법을 검사한다**

Run: `cd 프로그램/_시스템 && python -c "import re,pathlib,subprocess,sys; p=pathlib.Path('webapp/templates/_checklist_matrix.html'); js='\n'.join(re.findall(r'<script>(.*?)</script>', p.read_text(encoding='utf-8'), re.S)); pathlib.Path('_tmp.js').write_text(js, encoding='utf-8')" && node --check _tmp.js && rm _tmp.js`
Expected: 아무 출력 없음 (문법 오류 없음)

- [ ] **Step 6: 커밋**

```bash
git add 프로그램/_시스템/webapp/templates/_checklist_matrix.html 프로그램/_시스템/tests/marketplace_guide/test_checklist_route.py
git commit -m "feat(체크리스트): 표+정보창 공용 조각 (호버 6규칙 준수)"
```

---

## Task 6: 판매처 지도에 탭 달기

**Files:**
- Modify: `프로그램/_시스템/webapp/templates/marketplace_guide/map.html` (`TOP_DEFS` · `TAB_GROUPS` · `dmRender` 분기)
- Test: `프로그램/_시스템/tests/marketplace_guide/test_map_template.py`

🔴 **이 시험도 「글자가 있나」만 센다** — 탭이 실제로 열리는지는 못 잰다.
탭이 눌리고 표가 그려지는 것은 **Task 7 에서 눈으로** 확인한다.

- [ ] **Step 1: 실패하는 테스트를 덧붙인다**

```python
# tests/marketplace_guide/test_map_template.py 에 이어서

def test_checklist_tab_exists():
    html = _map()
    assert "개발 체크리스트" in html
    assert "data-top=\"checklist\"" in html or "'checklist'" in html


def test_checklist_tab_is_in_program_group():
    """「프로그램 적용」 묶음에 든다 — 마켓 문서 참고가 아니라 우리 할 일이다."""
    html = _map()
    i_group = html.index("프로그램 적용")
    i_tab = html.index("checklist")
    assert abs(i_tab - i_group) < 2000
```

- [ ] **Step 2: 실패를 확인한다**

Run: `cd 프로그램/_시스템 && python -m pytest tests/marketplace_guide/test_map_template.py -v`
Expected: FAIL — `ValueError: substring not found`

- [ ] **Step 3: 탭을 추가한다**

`map.html` 의 `dmRender()` 안 세 곳을 고친다.

```js
// ① TOP_DEFS 목록 맨 앞에 추가
{k:'checklist', lb:'개발 체크리스트'},

// ② TAB_GROUPS 의 「프로그램 적용」 줄
{lab:'프로그램 적용', acc:true, keys:['checklist','proglookup','speed']},

// ③ 본문 분기 — 다른 DM_TOP 분기들과 같은 자리에 추가
if(DM_TOP==='checklist'){
  document.getElementById('dm-mktabs').style.display='none';
  body.innerHTML=CHECKLIST_HTML;          // _checklist_matrix.html 이 심어 둔 조각
  ckInit('/marketplace-guide/checklist.json');
  return;
}
```

- [ ] **Step 4: 테스트가 통과하는지 확인한다**

Run: `cd 프로그램/_시스템 && python -m pytest tests/marketplace_guide/ -v`
Expected: 전부 passed

- [ ] **Step 5: 커밋**

```bash
git add 프로그램/_시스템/webapp/templates/marketplace_guide/map.html 프로그램/_시스템/tests/marketplace_guide/test_map_template.py
git commit -m "feat(체크리스트): 판매처 지도에 「개발 체크리스트」 탭"
```

---

## Task 7: 라이브 실화면 검증 (판매처)

**Files:** 없음 (검증만)

🔴 **코드만 보고 「됐다」고 하지 않는다.** 실제 브라우저에서 눈으로 본다.

- [ ] **Step 1: 개발 서버를 띄운다**

`.claude/launch.json` 에 아래를 추가하고 `preview_start` 로 띄운다.

```json
{"name":"devchk","runtimeExecutable":"python","runtimeArgs":["app.py"],
 "cwd":"C:/dev/_wt_devchk/프로그램/_시스템","port":5261,
 "env":{"DISABLE_AUTH":"1","ENVIRONMENT":"team-share-dev","PORT":"5261",
        "FLASK_PORT":"5261","DISABLE_SCHEDULER":"1","PYTHONIOENCODING":"utf-8"}}
```

- [ ] **Step 2: 화면을 열고 아래를 **하나씩 눈으로** 확인한다**

`/marketplace-guide/map` → 「개발 체크리스트」 탭

| 확인 | 통과 기준 |
|---|---|
| 표가 그려지나 | 25열 × 6행 = 150칸 |
| 마켓 이름 고정 | 표를 오른쪽 끝까지 밀어도 안 움직임 |
| 정보창 | 칸에 올려 잠깐 멈추면 뜸 |
| **정보창으로 마우스 건너가기** | 내용 안 바뀌고 안 꺼짐 ← **가장 중요** |
| 빠르게 훑기 | 깜빡이며 열리지 **않음** |
| 맨 아랫줄(G마켓) | 정보창이 **위로 뒤집혀** 뜸 |
| 표를 옆으로 밀기 | 정보창 닫힘 |
| 「저장만 됨」 | 14항목이 노란색으로 뜸 |
| 롯데온 줄 | 대부분 「미착수」 (「불가」가 아님) |

- [ ] **Step 3: 어긋난 것이 있으면 고치고 Step 2 를 다시 한다**

- [ ] **Step 4: 커밋**

```bash
git commit --allow-empty -m "chore(체크리스트): 판매처 탭 라이브 실화면 검증 통과"
```

---

## Task 8: 소싱처판

**Files:**
- Create: `프로그램/_시스템/webapp/data/dev_checklist_sourcing.json`
- Modify: `프로그램/_시스템/webapp/routes/sourcing_guide.py`
- Modify: `프로그램/_시스템/webapp/templates/sourcing_guide/map.html`
- Test: `프로그램/_시스템/tests/sourcing/test_guide_tabs.py`

소싱처는 `required.py` 같은 근거표가 **없다.** 그래서 1단계에서는 **열 정의와 손보정만** 두고,
자동 판정은 「크롤러가 등록돼 있나」 하나만 쓴다. 나머지는 손보정으로 채운다.

- [ ] **Step 1: 실패하는 테스트를 덧붙인다**

```python
# tests/sourcing/test_guide_tabs.py 에 이어서

def test_map_has_ninth_tab():
    html = (TPL / "map.html").read_text(encoding="utf-8")
    assert 'data-s="s9"' in html
    assert "개발 체크리스트" in html


def test_sourcing_checklist_json(client):
    r = client.get("/sourcing-guide/checklist.json")
    assert r.status_code == 200
    data = r.get_json()
    assert len(data["rows"]) == 8          # 소싱처 8곳
    assert len(data["columns"]) >= 20
```

- [ ] **Step 2: 실패를 확인한다**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sourcing/test_guide_tabs.py -v`
Expected: FAIL — 404 / substring not found

- [ ] **Step 3: 소싱처 열 정의를 만든다**

`webapp/data/dev_checklist_sourcing.json` — 설계서 §2-2 의 8묶음을 그대로. 판매처와 **같은 스키마**:
`{col, group, name, rule, item, specs}`. 소싱처 8곳:
`lemouton · ss_lemouton · musinsa · ssf · lotteon · ssg · hmall · lotteimall`

🔴 **모든 열이 8소싱처 키를 빠짐없이 갖고 있어야 한다.** 빠진 칸은 조용히 「해당없음」이 되지
않는다 — `build` 가 그 칸을 **⬜미착수**로 그리고 `note` 에 「열 정의에 이 마켓이 빠져 있습니다」를
넣은 뒤, 무엇이 빠졌는지를 **배너로** 모아 올린다(2차 재작업으로 바뀜 — 예전엔 표 전체가 `KeyError`
로 안 떴다. 손으로 쓰는 소싱처 열 정의의 오타 한 칸이 지도 탭을 죽이면 안 되기 때문).
`cell_state`·`_spec` 을 **직접** 부르면 여전히 `KeyError` 로 크게 터진다 — 그 계약은 그대로다.
배너는 「빠져도 된다」는 뜻이 아니므로, 판매처의 `test_specs_cover_six_markets` 에 해당하는 시험을
소싱처판에도 **반드시** 넣어라:

```python
def test_sourcing_specs_cover_eight_sources():
    cols = C.load_columns("dev_checklist_sourcing.json")
    keys = {k for k, _ in C.SOURCES}
    for c in cols:
        assert set(c["specs"]) == keys, f"{c['name']} 의 소싱처 목록이 다름"


def test_sourcing_column_numbers_are_unique():
    C.load_columns("dev_checklist_sourcing.json")   # 겹치면 ValueError 로 터진다
```

🔴 **열 번호는 판매처(2~26)와 겹쳐도 된다** — 손보정 파일을 판마다 따로 두기 때문이다.
`build_sourcing` 은 `marks_file="dev_checklist_sourcing_marks.json"` 을 쓴다(판매처 것과 섞이면
있는 경고를 놓치거나 없는 경고를 띄운다 — 검토에서 실제로 재현됐다).

- [ ] **Step 4: 소싱처 조립 함수를 쓴다**

판매처와 **같은 스키마**를 돌려준다. 다만 근거표(`required.py`)가 없으므로
「마켓이 요구하는가」 축은 쓰지 않고, 크롤러 등록 여부만 자동으로 본다.

```python
# lemouton/policy/checklist.py 에 이어서

SOURCES = [("lemouton", "르무통 공홈"), ("ss_lemouton", "스마트스토어 르무통"),
           ("musinsa", "무신사"), ("ssf", "SSF"), ("lotteon", "롯데온"),
           ("ssg", "SSG"), ("hmall", "현대H몰"), ("lotteimall", "롯데아이몰")]


def _crawler_registered(source_key: str) -> bool:
    """그 소싱처의 크롤러가 실제로 등록돼 있나. 없으면 무엇도 시작 안 된 것."""
    try:
        from lemouton.sourcing.crawlers import build_crawlers
    except Exception:
        return False
    try:
        return source_key in (build_crawlers() or {})
    except Exception:
        return False


def build_sourcing() -> dict:
    """소싱처판. 자동으로 아는 것은 「크롤러가 있나」 하나뿐 — 나머지는 손보정이다."""
    cols = load_columns("dev_checklist_sourcing.json")
    marks, marks_problem = load_marks("dev_checklist_sourcing_marks.json")   # 판마다 따로
    cells, rows = {}, []
    for key, label in SOURCES:
        has_crawler = _crawler_registered(key)
        counts = {k: 0 for k in (NA, IMPOSSIBLE, TODO, STORED, WIRED, DONE)}
        for col in cols:
            mark = (marks.get(f"{key}:{col['col']}") or {})
            if mark.get("impossible"):
                state = IMPOSSIBLE
            elif mark.get("verified"):
                state = DONE
            elif has_crawler:
                state = STORED
            else:
                state = TODO
            counts[state] += 1
            cells[f"{key}:{col['col']}"] = {
                "state": state,
                "spec": (col.get("specs") or {}).get(key, ""),
                "required": "", "evidence": "", "wiring": "",
                "wiring_note": ("" if has_crawler else "이 소싱처의 크롤러가 아직 없습니다"),
                "api": "", "conflict": "",
                "verified": mark.get("verified", ""),
                "note": mark.get("note", ""),
            }
        counts["total"] = len(cols) - counts[NA]
        rows.append({"market": key, "label": label, "counts": counts})
    return {"columns": cols, "rows": rows, "cells": cells,
            "drift": ([marks_problem] if marks_problem else [])}
```

- [ ] **Step 5: 라우트와 탭을 추가한다**

```python
# webapp/routes/sourcing_guide.py 파일 끝에 추가

@bp.route("/checklist.json")
def checklist_data():
    """소싱처 개발 체크리스트. 근거표가 아직 없어 손보정 비중이 크다."""
    from lemouton.policy.checklist import build_sourcing
    return jsonify(build_sourcing())
```

```html
<!-- templates/sourcing_guide/map.html — 8번 탭 다음 -->
<div class="gtab" data-s="s9"><span class="k">9</span>✅ 개발 체크리스트</div>
```

- [ ] **Step 5: 테스트가 통과하는지 확인한다**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sourcing/ tests/policy/ tests/marketplace_guide/ -q`
Expected: 전부 passed

- [ ] **Step 6: 커밋**

```bash
git add -A 프로그램/_시스템
git commit -m "feat(체크리스트): 소싱처판 탭 + 데이터"
```

---

## Task 9: 전체 시험 + 병합

- [ ] **Step 1: 저장소 전체 시험을 돌린다**

Run: `cd 프로그램/_시스템 && python -m pytest -q 2>&1 | tail -20`
Expected: 새로 깨진 것 없음 (기존 실패는 `reference_pytest_preexisting_failures` 참고)

- [ ] **Step 2: 병합 표시(`<<<<<<<`)가 남아 있지 않은지 저장소 전체를 훑는다**

Run: `cd "C:/dev/_wt_devchk" && git grep -n "<<<<<<<" -- "프로그램/_시스템" || echo "없음"`
Expected: `없음`

- [ ] **Step 3: PR 을 올린다**

```bash
cd "C:/dev/_wt_devchk"
git push -u origin feat/dev-checklist-tab
gh pr create --title "feat(체크리스트): 개발 체크리스트 탭 1단계 — 판매처·소싱처 매트릭스" --body "설계서: docs/superpowers/specs/2026-08-12-개발체크리스트-탭-design.md"
```

- [ ] **Step 4: 병합·배포를 확인하고 라이브에서 다시 본다**

배포 성공 ≠ 라이브 반영이다. 컨테이너 교체에 ~1분 더 걸린다 — **라이브 화면을 직접 열어** Task 7 의 표를 다시 확인한다.

---

## 다음 단계 (이 계획서 밖)

| 단계 | 내용 |
|---|---|
| 2 | 할인 기준 마진 수정 — 마진 기준가 = 판매가 − 판매자부담 할인액 |
| 3 | 전송 배선 14항목 — 저장만 되는 값을 실제로 마켓에 보냄 |
| 4 | 실등록 검증 6칸 — ⚠️ 실제 상품이 올라감, 사장님 승인 필요 |
