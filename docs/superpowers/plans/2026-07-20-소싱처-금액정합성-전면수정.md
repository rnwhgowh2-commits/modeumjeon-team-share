# 소싱처 금액 정합성 전면 수정 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 소싱처별 최종 매입가가 어느 화면에서 보든 같은 값이 되게 하고, 잘못된 소싱처에 혜택이 붙는 구조를 없앤다.

**Architecture:** 소싱처 번호 체계가 두 개(화면=`SourcingSource.id`, 계산=`SourceRegistry.id`)로 갈라져 서로 어긋난 것이 최상위 원인이다. ①번호 변환을 단일 지점으로 모으고 ②계산 중 공유 객체를 변형하지 않게 바꾼 뒤 ③소싱처별 크롤 결함을 고치고 ④마지막에 계산 조립부를 공용화해 지도 「▶ 크롤」이 같은 엔진을 쓰게 한다. 금액이 바뀌는 수정이므로 **모든 단계는 박제(characterization) 테스트를 통과해야** 하고, 실제 금액이 바뀌는 단계는 **전/후 대조표를 사장님께 확인받은 뒤** 배포한다.

**Tech Stack:** Python 3.14 · Flask · SQLAlchemy · pytest · 크롬 확장(MV3, JS)

**근거 문서:** [2026-07-19 소싱처상세-지도흡수 design §7-2](../specs/2026-07-19-소싱처상세-지도흡수-design.md)

---

## 확정된 사실 (조사 완료 · 2026-07-20 라이브 실측)

| # | 사실 | 증거 |
|---|---|---|
| F1 | 번호 체계 2개가 **전부 어긋남** | 라이브 `SG_SOURCES`(화면) vs option-matrix `source_id`(계산) |
| F2 | 화면 번호로 템플릿을 씀 | `webapp/routes/sourcing_guide.py:779` `sync_templates_from_crawl_guide(s, sid, guide)` |
| F3 | 무신사 가이드 「후기 적립 500」이 **롯데온 계산 템플릿**(source_id=5)에 존재 | 라이브 `/api/source-benefits/templates/5` |
| F4 | 롯데온 가이드(화면 8)엔 후기 적립 **없음** | 라이브 `/sourcing-guide/api/8` |
| F5 | 2026-06-30 이관은 **내용만** 복사, id 정렬 안 함 | `lemouton/sourcing/roster.py:153-192` |
| F6 | 계산 중 공유 ORM 객체를 5곳에서 직접 변형 | `api_benefits.py:850,858,879,1013,1096` |
| F7 | 게이트는 raw 비교, 경고는 공백제거 비교 → 불일치가 **조용히** 통과 | `api_benefits.py:1097` vs `:1101` |

**번호 대응표 (라이브 확정)**

| 소싱처 | 화면(`SourcingSource.id`) | 계산(`SourceRegistry.id`) |
|---|---|---|
| lemouton | 4 | 1 |
| ss_lemouton | 9 | 2 |
| musinsa | 5 | 3 |
| ssf | 6 | 4 |
| lotteon | 8 | 5 |
| ssg | 7 | 6 |
| hmall | 2 | `key:hmall` (정수 없음) |
| lotteimall | 3 | `key:lotteimall` (정수 없음) |

---

## File Structure

| 파일 | 책임 | 신규/수정 |
|---|---|---|
| `lemouton/sourcing/source_ids.py` | **번호 변환 단일 원천.** source_key ↔ 계산 source_id | 신규 |
| `tests/sourcing/test_source_ids.py` | 변환 규칙 + 드리프트 감시 | 신규 |
| `webapp/routes/api_benefits.py` | `_SITE_BY_SRC` 제거 → 공용 helper 사용 / 공유객체 변형 제거 / 게이트 이름 정규화 / 조립부 추출 | 수정 |
| `lemouton/pricing/benefit_assembly.py` | `compute_breakdown` 조립부(순수 함수) | 신규 |
| `webapp/routes/sourcing_guide.py` | 템플릿 동기화 시 계산 번호로 변환 | 수정 |
| `scripts/audit_benefit_template_owner.py` | 잘못 붙은 템플릿 전수 대조표 생성 | 신규 |
| `lemouton/sourcing/guide_url_result.py` | 공용 조립부를 태워 실제 최종매입가 산출 | 수정 |
| `extension/moum-crawler/background.js` | 롯데온 오너스율 폴백 가드 | 수정 |
| `lemouton/sourcing/crawlers/ssf.py` 소비측 | 기프트포인트 실금액 사용 | 수정(`api_benefits.py`) |
| `webapp/routes/api_sources_parse.py` | 빈 혜택이 기존 혜택을 덮지 않게 | 수정 |
| `tests/pricing/test_breakdown_characterization.py` | 박제(이미 작성됨 · 12건) | 수정(기대값 확정) |

---

## Phase 0 — 안전망 완성 (금액 수정 전 필수)

### Task 0: 박제 테스트 기대값을 실제 동작으로 확정

**Files:**
- Modify: `프로그램/_시스템/tests/pricing/test_breakdown_characterization.py`

현재 12건 중 2건이 내 추정값과 달라 실패한다. **추정이 아니라 현재 동작이 기준**이므로 실측값으로 맞춘다.

- [ ] **Step 1: 실제값 확인**

Run:
```bash
cd "프로그램/_시스템" && python -m pytest tests/pricing/test_breakdown_characterization.py -q 2>&1 | grep "^E.*assert"
```
Expected: `assert 94000 == 94100` · `assert 92400 == 100000`

- [ ] **Step 2: `test_ssf_gift_point_inactive_without_crawl` 기대값 수정**

```python
    # 100,000 −1,000(기본적립) = 99,000 → ×5%(멤버십) = 4,950 → 94,050 → 백원버림 94,000
    assert got['final'] == 94000
```

- [ ] **Step 3: `test_benefits_unavailable_keeps_full_price` 를 실제로 가드가 걸리게 고친다**

가드는 `SourceRegistry.crawl_guide` 를 읽는다(`api_benefits.py:472-474`). 픽스처가 `SourcingSource` 에
심어 안 걸렸다. **두 저장소를 다 심어** 가드가 실제로 동작하는 것을 박제한다.

```python
def test_benefits_unavailable_keeps_full_price(sess):
    """★ 혜택을 크롤로 가져오는 소싱처인데 못 가져왔으면 템플릿도 안 쓴다.

    싸게 잡으면 손해 매입 — 더 비싼 쪽으로 둔다(2026-07-19 사용자 확정).
    ⚠ 이 가드가 읽는 곳은 SourceRegistry.crawl_guide 다(api_benefits.py:472).
      화면이 쓰는 SourcingSource.crawl_guide 와 다른 저장소 — Task 2 에서 통일한다.
    """
    from lemouton.sourcing.models_pricing import SourceRegistry
    from lemouton.sourcing import crawl_guide as cg
    sku, sid = PREFIX + 'unavail', SRC['musinsa']
    _seed(sess, sku=sku, source_id=sid, site='musinsa',
          templates=[{'name': '등급적립', 'type': 'amount', 'value': 5000}],
          dynamic={})
    g = cg.empty_skeleton()
    g['fields']['benefit'].update({'method': 'crawl_per_product',
                                   'mechanism': 'html', 'status': 'ok'})
    reg = sess.get(SourceRegistry, sid)
    if reg is None:
        reg = SourceRegistry(id=sid, name=f'박제-{sid}')
        sess.add(reg)
    reg.crawl_guide = cg.dumps(cg.validate_guide(g))
    sess.commit()
    got = _run(sku, sid, 100000.0)
    assert got['final'] == 100000        # 템플릿 5,000 이 안 빠진다
```

- [ ] **Step 4: 전건 통과 확인**

Run: `cd "프로그램/_시스템" && python -m pytest tests/pricing/test_breakdown_characterization.py -q`
Expected: `12 passed`

- [ ] **Step 5: Commit**

```bash
git add "프로그램/_시스템/tests/pricing/test_breakdown_characterization.py"
git commit -m "test(가격): compute_breakdown 박제 12건 — 금액 리팩터링 안전망"
```

---

## Phase 1 — 번호 체계 통일 (최우선 · F1~F5)

### Task 1: 번호 변환 단일 원천 + 드리프트 감시 테스트

**Files:**
- Create: `프로그램/_시스템/lemouton/sourcing/source_ids.py`
- Test: `프로그램/_시스템/tests/sourcing/test_source_ids.py`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# -*- coding: utf-8 -*-
"""소싱처 번호 변환 — 화면 번호와 계산 번호가 다르다는 사실을 한 곳에 가둔다.

배경(2026-07-20): 두 체계가 8개 소싱처 전부에서 어긋나 있었고, 화면에서 저장한
혜택이 **다른 소싱처** 계산에 들어갔다(무신사 후기적립 500 → 롯데온).
"""
import pytest

from lemouton.sourcing import source_ids as si


def test_pricing_id_for_builtin_keys():
    assert si.pricing_source_id('lemouton') == 1
    assert si.pricing_source_id('ss_lemouton') == 2
    assert si.pricing_source_id('musinsa') == 3
    assert si.pricing_source_id('ssf') == 4
    assert si.pricing_source_id('lotteon') == 5
    assert si.pricing_source_id('ssg') == 6


def test_catalog_keys_get_string_id():
    """정수 자리가 없는 카탈로그 소싱처는 'key:' 합성 id."""
    assert si.pricing_source_id('hmall') == 'key:hmall'
    assert si.pricing_source_id('lotteimall') == 'key:lotteimall'


def test_unknown_key_is_none():
    assert si.pricing_source_id('없는소싱처') is None


def test_site_key_round_trip():
    for k in ('lemouton', 'ss_lemouton', 'musinsa', 'ssf', 'lotteon', 'ssg'):
        assert si.site_key(si.pricing_source_id(k)) == k
    assert si.site_key('key:hmall') == 'hmall'


def test_site_key_bad_input_is_none():
    assert si.site_key(None) is None
    assert si.site_key(999) is None
    assert si.site_key('key:') is None


def test_templates_are_integer_only():
    """혜택 템플릿은 Integer 컬럼이라 카탈로그 소싱처엔 못 붙는다 — 호출자가 알 수 있게."""
    assert si.supports_benefit_templates('musinsa') is True
    assert si.supports_benefit_templates('hmall') is False
```

- [ ] **Step 2: 실패 확인**

Run: `cd "프로그램/_시스템" && python -m pytest tests/sourcing/test_source_ids.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'lemouton.sourcing.source_ids'`

- [ ] **Step 3: 구현**

```python
# -*- coding: utf-8 -*-
"""소싱처 번호 변환 — 단일 원천.

소싱처를 가리키는 번호가 이 저장소에 **두 가지** 있다.

  화면 번호  `SourcingSource.id`   — 소싱처 관리·크롤 가이드 화면이 쓴다
  계산 번호  `SourceRegistry.id`   — 최종 매입가 계산(혜택 템플릿·override)이 쓴다

2026-06-30 단일명부 이관은 가이드 **내용만** 복사했고 번호는 정렬하지 않았다
(roster.py:153). 그 결과 8개 소싱처 전부에서 두 번호가 어긋났고, 화면에서 저장한
혜택이 다른 소싱처 계산에 들어갔다(무신사 후기적립 500원 → 롯데온, 2026-07-20 실측).

두 체계를 합치는 마이그레이션은 범위가 크므로, **변환을 여기 한 곳에만 둔다.**
번호를 직접 하드코딩하는 코드는 이 모듈을 쓰도록 바꾼다.
"""
from __future__ import annotations

# 계산 번호 → 소싱처 키. api_benefits._SITE_BY_SRC 가 갖고 있던 표를 여기로 옮긴 것.
#   ⚠ SourceRegistry 행의 실제 id 와 일치해야 한다. 어긋나면 전 소싱처 금액이 틀어지므로
#     tests/sourcing/test_source_ids.py 의 드리프트 감시 테스트가 지킨다.
_SITE_BY_PRICING_ID: dict[int, str] = {
    1: 'lemouton', 2: 'ss_lemouton', 3: 'musinsa',
    4: 'ssf', 5: 'lotteon', 6: 'ssg',
}
_PRICING_ID_BY_SITE: dict[str, int] = {v: k for k, v in _SITE_BY_PRICING_ID.items()}

# SourceRegistry 에 행이 없는 카탈로그 소싱처 — 'key:<source_key>' 합성 id 를 쓴다
# (api_pricing.py:728). 혜택 템플릿은 Integer 컬럼이라 이 소싱처엔 붙일 수 없다.
_CATALOG_KEYS = ('hmall', 'lotteimall')


def pricing_source_id(source_key: str):
    """소싱처 키 → 계산이 쓰는 source_id. 모르는 키면 None.

    반환형이 int 또는 'key:...' 문자열 두 가지다 — compute_breakdown 이 둘 다 받는다.
    """
    k = (source_key or '').strip()
    if k in _PRICING_ID_BY_SITE:
        return _PRICING_ID_BY_SITE[k]
    if k in _CATALOG_KEYS:
        return 'key:' + k
    return None


def site_key(pricing_id) -> "str | None":
    """계산 source_id → 소싱처 키. 모르면 None(추측 금지)."""
    if isinstance(pricing_id, str) and pricing_id.startswith('key:'):
        return pricing_id[4:].strip() or None
    try:
        return _SITE_BY_PRICING_ID.get(int(pricing_id))
    except (TypeError, ValueError):
        return None


def supports_benefit_templates(source_key: str) -> bool:
    """혜택 템플릿(SourceBenefitTemplate.source_id = Integer)을 붙일 수 있는 소싱처인가.

    False 인 소싱처(현대H몰·롯데아이몰)는 템플릿을 저장해도 계산이 읽지 못한다 —
    호출자가 조용히 버리지 말고 사용자에게 알려야 한다.
    """
    return isinstance(pricing_source_id(source_key), int)
```

- [ ] **Step 4: 통과 확인**

Run: `cd "프로그램/_시스템" && python -m pytest tests/sourcing/test_source_ids.py -q`
Expected: `6 passed`

- [ ] **Step 5: Commit**

```bash
git add "프로그램/_시스템/lemouton/sourcing/source_ids.py" "프로그램/_시스템/tests/sourcing/test_source_ids.py"
git commit -m "feat(가격): 소싱처 번호 변환 단일 원천 — 화면 번호와 계산 번호 분리 명시"
```

---

### Task 2: `api_benefits` 가 공용 변환을 쓰게 (동작 무변경)

**Files:**
- Modify: `프로그램/_시스템/webapp/routes/api_benefits.py:555-572`

- [ ] **Step 1: 박제가 통과하는 상태인지 먼저 확인 (기준선)**

Run: `cd "프로그램/_시스템" && python -m pytest tests/pricing/test_breakdown_characterization.py -q`
Expected: `12 passed`

- [ ] **Step 2: `_SITE_BY_SRC` 하드코딩 제거**

`api_benefits.py:555` 의 `_SITE_BY_SRC = {...}` 한 줄과 `_resolve_site_key` 정의(566-572)를 아래로 교체:

```python
    # [2026-07-20] 번호 변환은 lemouton/sourcing/source_ids.py 단일 원천을 쓴다.
    #   종전엔 이 표가 여기에만 있어, 화면 쪽(SourcingSource.id)과 어긋난 것을
    #   아무도 못 잡았다(무신사 혜택이 롯데온 계산에 들어감 — 2026-07-20 실측).
    from lemouton.sourcing.source_ids import site_key as _resolve_site_key
    _site_for = _resolve_site_key(source_id)
```

- [ ] **Step 3: 동작 무변경 확인**

Run: `cd "프로그램/_시스템" && python -m pytest tests/pricing/ tests/sourcing/ -q`
Expected: `12 passed` 포함 전건 통과 (금액 한 원도 안 바뀜)

- [ ] **Step 4: Commit**

```bash
git add "프로그램/_시스템/webapp/routes/api_benefits.py"
git commit -m "refactor(가격): 번호 변환을 source_ids 로 일원화 (동작 무변경·박제 통과)"
```

---

### Task 3: 잘못 붙은 혜택 전수 대조표 (읽기 전용 · 배포 없음)

**Files:**
- Create: `프로그램/_시스템/scripts/audit_benefit_template_owner.py`

⚠️ **이 스크립트는 아무것도 고치지 않는다.** 무엇이 잘못됐는지 사장님께 보여드리는 표만 만든다.

- [ ] **Step 1: 스크립트 작성**

```python
# -*- coding: utf-8 -*-
"""[읽기 전용] 혜택 템플릿이 올바른 소싱처에 붙어 있는지 전수 대조.

배경: 크롤 가이드 화면은 화면 번호(SourcingSource.id)로 템플릿을 저장했는데
      계산은 계산 번호(SourceRegistry.id)로 읽는다. 두 번호가 어긋나 혜택이
      다른 소싱처에 붙었다(2026-07-20 확인).

출력: 소싱처별로 ①가이드가 말하는 혜택 ②계산이 실제로 쓰는 혜택 ③불일치
사용:  python scripts/audit_benefit_template_owner.py
"""
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from shared.db import SessionLocal
from lemouton.sourcing import crawl_guide as cg
from lemouton.sourcing.models import SourcingSource, SourceBenefitTemplate
from lemouton.sourcing.source_ids import pricing_source_id, supports_benefit_templates


def main():
    s = SessionLocal()
    try:
        print('소싱처         화면번호  계산번호  가이드 혜택(값 있는 것)      계산이 쓰는 템플릿')
        print('-' * 110)
        for src in s.query(SourcingSource).order_by(SourcingSource.id).all():
            key = src.source_key
            pid = pricing_source_id(key)
            guide = cg.loads(src.crawl_guide) or {}
            gb = [(b.get('name'), b.get('value'))
                  for b in ((guide.get('pricing') or {}).get('benefits') or [])
                  if b.get('value') is not None]
            tpl = []
            if isinstance(pid, int):
                tpl = [(t.benefit_name, t.value) for t in
                       s.query(SourceBenefitTemplate).filter_by(source_id=pid)
                        .order_by(SourceBenefitTemplate.sort_order).all()]
            # 화면 번호로 잘못 저장된 템플릿(= 다른 소싱처 자리에 앉은 것)
            wrong = [(t.benefit_name, t.value) for t in
                     s.query(SourceBenefitTemplate).filter_by(source_id=src.id).all()]
            print(f'{(src.label or key)[:12]:<14}{src.id:^9}{str(pid):^10}'
                  f'{str(gb)[:28]:<30}{str(tpl)[:30]}')
            if not supports_benefit_templates(key):
                print(f'    ⚠ {key}: 계산이 혜택 템플릿을 못 읽는 소싱처(문자 번호)')
            if wrong and src.id != pid:
                print(f'    🔴 화면번호 {src.id} 자리에 저장된 템플릿 {len(wrong)}건 '
                      f'= 계산에선 「{__import__("lemouton.sourcing.source_ids", fromlist=["x"]).site_key(src.id)}」 것으로 읽힘: {wrong}')
    finally:
        s.close()


if __name__ == '__main__':
    main()
```

- [ ] **Step 2: 실행해 표 확인**

Run: `cd "프로그램/_시스템" && python scripts/audit_benefit_template_owner.py`
Expected: 소싱처별 한 줄 + 🔴 표시된 잘못된 템플릿 목록

- [ ] **Step 3: Commit**

```bash
git add "프로그램/_시스템/scripts/audit_benefit_template_owner.py"
git commit -m "chore(가격): 혜택 템플릿 소유자 전수 대조 스크립트(읽기 전용)"
```

- [ ] **Step 4: 🛑 사장님 확인 게이트**

대조표를 사장님께 보여드리고 **어느 혜택을 어느 소싱처로 옮길지** 확인받는다.
확인 전에는 Task 4 로 넘어가지 않는다. (매입가가 실제로 바뀌는 지점)

---

### Task 4: 화면 저장이 계산 번호로 가게 (🛑 Task 3 확인 후)

**Files:**
- Modify: `프로그램/_시스템/webapp/routes/sourcing_guide.py:775-790`
- Test: `프로그램/_시스템/tests/sourcing/test_guide_template_sync_owner.py`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# -*- coding: utf-8 -*-
"""가이드에서 저장한 혜택이 **그 소싱처** 계산에 붙는지.

2026-07-20 실측: 무신사 가이드(화면 5)의 「후기 적립 500」이 롯데온(계산 5)
템플릿에 앉아 있었다. 화면 번호를 그대로 계산 번호로 쓴 탓이다.
"""
import json
import pytest

from lemouton.sourcing import crawl_guide as cg
from lemouton.sourcing.models import SourcingSource, SourceBenefitTemplate
from lemouton.sourcing.source_ids import pricing_source_id
from shared.db import SessionLocal

LABEL = '동기화테스트무신사'


@pytest.fixture
def sid():
    s = SessionLocal()
    try:
        s.query(SourcingSource).filter_by(label=LABEL).delete(synchronize_session=False)
        s.commit()
        g = cg.empty_skeleton()
        g['pricing'] = {'benefits': [{'name': '후기 적립', 'apply': 'deduct',
                                      'status': 'always', 'method': '정액(원)',
                                      'value': 500, 'value_source': 'fixed',
                                      'triggers': [], 'match': 'any'}]}
        src = SourcingSource(source_key='musinsa_syncttest', label=LABEL,
                             domain='musinsa.com',
                             crawl_guide=cg.dumps(cg.validate_guide(g)))
        s.add(src); s.commit()
        out = src.id
    finally:
        s.close()
    yield out
    s = SessionLocal()
    try:
        s.query(SourcingSource).filter_by(label=LABEL).delete(synchronize_session=False)
        s.commit()
    finally:
        s.close()


def test_sync_writes_to_pricing_id_not_screen_id(sid):
    """★ 저장은 계산 번호로 간다 — 화면 번호 자리에 앉으면 안 된다."""
    from webapp.routes.api_benefits import sync_templates_from_crawl_guide
    s = SessionLocal()
    try:
        src = s.get(SourcingSource, sid)
        guide = cg.loads(src.crawl_guide)
        # 실제 소싱처 키로 바꿔 계산 번호가 나오게
        src.source_key = 'musinsa'; s.commit()
        pid = pricing_source_id('musinsa')
        assert pid == 3 and pid != sid          # 두 번호가 다른 상황을 재현
        s.query(SourceBenefitTemplate).filter(
            SourceBenefitTemplate.source_id.in_([pid, sid])).delete(
            synchronize_session=False)
        s.commit()
        sync_templates_from_crawl_guide(s, pid, guide)
        s.commit()
        on_pricing = s.query(SourceBenefitTemplate).filter_by(source_id=pid).all()
        on_screen = s.query(SourceBenefitTemplate).filter_by(source_id=sid).all()
        assert [t.benefit_name for t in on_pricing] == ['후기 적립']
        assert on_screen == []                  # 화면 번호 자리엔 아무것도 없어야
    finally:
        s.rollback(); s.close()
```

- [ ] **Step 2: 실패 확인**

Run: `cd "프로그램/_시스템" && python -m pytest tests/sourcing/test_guide_template_sync_owner.py -q`
Expected: 현재 라우트가 sid 를 그대로 넘기므로 라우트 경유 시 실패 (본 테스트는 함수 직접 호출이라 통과 → Step 3 에서 라우트를 고친 뒤 라우트 테스트 추가)

- [ ] **Step 3: 라우트가 변환하도록 수정**

`webapp/routes/sourcing_guide.py` 의 `api_put` 안, `sync_templates_from_crawl_guide` 호출부를 교체:

```python
        # [2026-07-20] 혜택 템플릿은 **계산 번호**로 저장한다.
        #   화면 번호(SourcingSource.id)를 그대로 넘기던 탓에 무신사 가이드의
        #   「후기 적립 500」이 롯데온 계산에 붙어 있었다(실측). 번호 변환은
        #   lemouton/sourcing/source_ids.py 단일 원천을 쓴다.
        from lemouton.sourcing.source_ids import (
            pricing_source_id as _pid, supports_benefit_templates as _supports,
        )
        _pricing_id = _pid(src.source_key)
        benefits_synced = 0
        template_warning = None
        if _pricing_id is None:
            template_warning = (f'「{src.label}」는 계산 쪽에 등록되지 않은 소싱처라 '
                                f'혜택 값이 매입가 계산에 반영되지 않습니다.')
        elif not _supports(src.source_key):
            # 문자 번호 소싱처(현대H몰·롯데아이몰) — Integer 컬럼이라 못 붙는다.
            template_warning = (f'「{src.label}」는 아직 혜택 템플릿을 저장할 수 없는 '
                                f'소싱처입니다(계산 쪽 번호 없음). 값은 가이드에만 남습니다.')
        else:
            benefits_synced = sync_templates_from_crawl_guide(s, _pricing_id, guide)
```

그리고 같은 함수의 `snapshot_bundle_from_templates(..., source_ids=[sid])` 호출도
`source_ids=[_pricing_id]` 로 바꾸고, 응답에 `template_warning=template_warning` 을 추가한다.

- [ ] **Step 4: 통과 확인 + 박제 무변동**

Run: `cd "프로그램/_시스템" && python -m pytest tests/sourcing/ tests/pricing/ -q`
Expected: 전건 통과

- [ ] **Step 5: Commit**

```bash
git add "프로그램/_시스템/webapp/routes/sourcing_guide.py" "프로그램/_시스템/tests/sourcing/test_guide_template_sync_owner.py"
git commit -m "fix(가격): 가이드 혜택이 다른 소싱처 계산에 붙던 것 수정 — 계산 번호로 저장"
```

---

## Phase 2 — 계산 엔진 안전성 (F6·F7)

### Task 5: 계산이 공유 객체를 변형하지 않게

**Files:**
- Modify: `프로그램/_시스템/webapp/routes/api_benefits.py:850,858,879,1013,1096`
- Test: `프로그램/_시스템/tests/pricing/test_breakdown_no_shared_mutation.py`

문제: 계산 중 `it.enabled = False` 로 **DB에서 읽어온 객체를 직접** 끈다. 그 객체는
여러 상품이 공유하는 캐시에 있어, 먼저 계산한 상품이 나중 상품의 혜택을 지운다.
코드 주석에 같은 사고 전력이 두 번 적혀 있다(`final_price.py:228`).

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# -*- coding: utf-8 -*-
"""같은 캐시로 여러 상품을 계산해도 서로 오염되지 않아야 한다.

계산 중 혜택을 끄는 동작이 공유 ORM 객체를 직접 변형하면, 먼저 계산한 상품이
나중 상품의 혜택을 통째로 지운다. 한 건만 볼 땐 안 보이고 여러 건일 때만 틀린다.
"""
import json
import pytest

from shared.db import SessionLocal
from lemouton.sourcing.models import SourceBenefitTemplate, OptionBenefitOverride
from lemouton.sourcing.models_pricing import OptionSourceUrl
from lemouton.sources.models import SourceProduct
from webapp.routes.api_benefits import compute_breakdown

PFX = 'SHAREDTEST-'
LOTTEIMALL = 'key:lotteimall'


@pytest.fixture
def two_skus():
    s = SessionLocal()
    try:
        for sku in (PFX + 'a', PFX + 'b'):
            url = f'https://example.test/shared/{sku}'
            s.add(OptionSourceUrl(canonical_sku=sku, source_id=LOTTEIMALL,
                                  product_url=url))
            s.add(SourceProduct(site='lotteimall', url=url,
                                dynamic_benefits_json=json.dumps(
                                    {'point_rewards': {'default_point': 1000}})))
            s.add(OptionBenefitOverride(canonical_sku=sku, source_id=LOTTEIMALL,
                                        benefit_name='구매적립금', benefit_type='amount',
                                        value=3000, enabled=True, sort_order=0))
        s.commit()
    finally:
        s.close()
    yield [PFX + 'a', PFX + 'b']
    s = SessionLocal()
    try:
        s.query(OptionBenefitOverride).filter(
            OptionBenefitOverride.canonical_sku.like(PFX + '%')).delete(
            synchronize_session=False)
        s.query(OptionSourceUrl).filter(
            OptionSourceUrl.canonical_sku.like(PFX + '%')).delete(
            synchronize_session=False)
        s.query(SourceProduct).filter(
            SourceProduct.url.like('https://example.test/shared/%')).delete(
            synchronize_session=False)
        s.commit()
    finally:
        s.close()


def test_second_sku_not_polluted_by_first(two_skus):
    """★ 두 번 계산해도 같은 값. 첫 계산이 둘째를 오염시키면 안 된다."""
    a, b = two_skus
    s = SessionLocal()
    try:
        first_a = compute_breakdown(s, sku=a, source_id=LOTTEIMALL, sale_price=100000.0)
        _ = compute_breakdown(s, sku=b, source_id=LOTTEIMALL, sale_price=100000.0)
        again_a = compute_breakdown(s, sku=a, source_id=LOTTEIMALL, sale_price=100000.0)
        assert again_a['final_price'] == first_a['final_price']
    finally:
        s.rollback(); s.close()


def test_orm_rows_keep_enabled_after_compute(two_skus):
    """★ 계산은 DB 행의 enabled 를 건드리지 않는다(세션 flush 시 영구 변형 방지)."""
    a = two_skus[0]
    s = SessionLocal()
    try:
        compute_breakdown(s, sku=a, source_id=LOTTEIMALL, sale_price=100000.0)
        rows = s.query(OptionBenefitOverride).filter_by(canonical_sku=a).all()
        assert all(r.enabled for r in rows)
    finally:
        s.rollback(); s.close()
```

- [ ] **Step 2: 실패 확인**

Run: `cd "프로그램/_시스템" && python -m pytest tests/pricing/test_breakdown_no_shared_mutation.py -q`
Expected: FAIL — `test_orm_rows_keep_enabled_after_compute` 에서 `enabled` 가 False 로 꺼져 있음

- [ ] **Step 3: 5곳을 지역 집합으로 교체**

`api_benefits.py` 의 `effective` 생성 직후(`_ovr_names` 정의 부근)에 추가:

```python
    # [2026-07-20] 계산 중 "이 혜택 끔"은 **지역 집합**으로만 기록한다.
    #   종전엔 it.enabled = False 로 DB 행을 직접 껐다. 그 행은 여러 SKU 가 공유하는
    #   캐시에 있어, 먼저 계산한 상품이 나중 상품의 혜택을 지웠다(순서 의존 = 재현 난해).
    #   final_price._compute_legacy 가 같은 이유로 이미 _pay_losers 지역집합을 쓴다.
    _disabled = set()          # id(item) 모음 — 원본은 건드리지 않는다
    def _turn_off(_it):
        _disabled.add(id(_it))
```

그리고 다섯 곳의 `_it.enabled = False` / `it.enabled = False` 를 각각 `_turn_off(_it)` /
`_turn_off(it)` 로 바꾼다 (850·858·879·1013·1096 부근).

마지막으로 `compute_final_price` 호출 직전에 지역 집합을 반영한 사본을 만든다:

```python
    # 끈 항목을 원본 변형 없이 반영 — 읽기 전용 프록시로 감싼다.
    if _disabled:
        class _Off:
            __slots__ = ('_it',)
            def __init__(self, it): object.__setattr__(self, '_it', it)
            def __getattr__(self, k):
                return False if k == 'enabled' else getattr(
                    object.__getattribute__(self, '_it'), k)
        effective = [(k, _Off(it) if id(it) in _disabled else it)
                     for k, it in effective]
```

- [ ] **Step 4: 통과 + 박제 무변동 확인**

Run: `cd "프로그램/_시스템" && python -m pytest tests/pricing/ -q`
Expected: 전건 통과 (박제 12건 포함 — 단일 계산 금액은 안 바뀐다)

- [ ] **Step 5: Commit**

```bash
git add "프로그램/_시스템/webapp/routes/api_benefits.py" "프로그램/_시스템/tests/pricing/test_breakdown_no_shared_mutation.py"
git commit -m "fix(가격): 계산이 공유 혜택 행을 끄던 것 제거 — 여러 상품 계산 시 혜택 누락 차단"
```

---

### Task 6: 게이트 이름 비교를 공백 무시로 통일

**Files:**
- Modify: `프로그램/_시스템/webapp/routes/api_benefits.py:1093-1108`
- Test: `프로그램/_시스템/tests/pricing/test_benefit_gate_name_normalize.py`

문제: 끄는 쪽은 `_bname in _off`(원문 그대로), 경고 쪽은 공백을 지우고 비교한다.
가이드 「상품 쿠폰」 vs 주입 「상품쿠폰」 — **띄어쓰기 하나** 때문에 못 끄고, 경고도 안 뜬다.

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# -*- coding: utf-8 -*-
"""혜택 이름의 띄어쓰기 차이로 게이트가 조용히 통과하면 안 된다.

가이드는 「상품 쿠폰」, 계산 주입 이름은 「상품쿠폰」이다. 끄는 비교는 원문,
경고 비교는 공백제거라 '못 끄는데 경고도 없는' 조합이 나왔다(2026-07-20).
"""
from webapp.routes.api_benefits import _norm_benefit_name


def test_space_is_ignored():
    assert _norm_benefit_name('상품 쿠폰') == _norm_benefit_name('상품쿠폰')


def test_surrounding_space_ignored():
    assert _norm_benefit_name('  등급 적립 ') == _norm_benefit_name('등급적립')


def test_different_names_stay_different():
    assert _norm_benefit_name('구매적립') != _norm_benefit_name('등급적립')


def test_none_is_empty():
    assert _norm_benefit_name(None) == ''
```

- [ ] **Step 2: 실패 확인**

Run: `cd "프로그램/_시스템" && python -m pytest tests/pricing/test_benefit_gate_name_normalize.py -q`
Expected: FAIL — `ImportError: cannot import name '_norm_benefit_name'`

- [ ] **Step 3: 정규화 함수 추가 + 양쪽 비교 통일**

`api_benefits.py` 상단(다른 helper 근처)에 추가:

```python
def _norm_benefit_name(name) -> str:
    """혜택 이름 비교용 정규화 — 공백만 무시한다.

    가이드는 「상품 쿠폰」, 계산 주입은 「상품쿠폰」처럼 띄어쓰기가 다르다.
    끄는 비교와 경고 비교가 서로 다른 기준을 쓰면 '못 끄는데 경고도 없는' 조합이 난다.
    """
    return (name or '').replace(' ', '').strip()
```

게이트 블록(1093-1099)을 교체:

```python
                _off_norm = {_norm_benefit_name(n) for n in _off}
                for _k2, _it2 in effective:
                    if _norm_benefit_name(_it2.benefit_name) in _off_norm:
                        _turn_off(_it2)
                _eff_names = {_norm_benefit_name(_it2.benefit_name)
                              for _k2, _it2 in effective}
                for _cname in _gated_names:
                    if _norm_benefit_name(_cname) not in _eff_names:
                        _gate_logger.warning(...)   # 기존 그대로
```

- [ ] **Step 4: 통과 확인**

Run: `cd "프로그램/_시스템" && python -m pytest tests/pricing/ -q`
Expected: 전건 통과

⚠️ 이 수정으로 **무신사 상품쿠폰이 실제로 꺼진다** = 매입가가 올라갈 수 있다.
박제 테스트가 바뀌면 그것이 의도한 변화인지 확인하고, 전/후 금액을 Task 12 대조표에 싣는다.

- [ ] **Step 5: Commit**

```bash
git add "프로그램/_시스템/webapp/routes/api_benefits.py" "프로그램/_시스템/tests/pricing/test_benefit_gate_name_normalize.py"
git commit -m "fix(가격): 혜택 이름 띄어쓰기로 게이트가 조용히 통과하던 것 수정"
```

---

## Phase 3 — 소싱처별 크롤 결함

### Task 7: 롯데온 오너스 할인 이중차감 차단

**Files:**
- Modify: `C:/Users/seung/Desktop/moum-crawler-v0.7.35/background.js:1408-1421` (로드본)
- Modify: `프로그램/_시스템/extension/moum-crawler/background.js` (리포 동기)

문제: 표면가를 못 읽으면 「나의 혜택가」를 표면가로 쓰는 폴백이 있는데, 그 상태에서
오너스율을 `(표면−혜택)/표면` 으로 **역산**한다. 이미 혜택이 반영된 값을 기준가로 삼고
그 위에 오너스를 또 빼므로 이중차감이다.

- [ ] **Step 1: 폴백이 걸렸는지 기록하는 플래그 추가**

`lotteonExtractor` 안, 표면가 폴백 지점:

```js
      // [2026-07-20] 표면가를 못 읽어 혜택가로 대체했는가. 대체했다면 그 값엔 이미
      //   혜택이 반영돼 있으므로 오너스율 역산을 하면 안 된다(이중차감).
      var _surfaceIsBenefit = false;
      if (sale == null && benefit != null) { sale = benefit; _surfaceIsBenefit = true; }
```

- [ ] **Step 2: 역산을 그 경우에만 막는다**

```js
      // 명시된 「롯데오너스 N%」가 있으면 그대로 쓴다.
      if (_m) ownusRate = parseFloat(_m[1]) / 100;
      // 없으면 가격 차이로 역산하되, 표면가가 혜택가로 대체된 경우엔 하지 않는다.
      //   그 차이는 이미 0 이거나 다른 혜택까지 섞인 값이라 오너스율이 아니다.
      else if (!_surfaceIsBenefit && sale != null && benefit != null && benefit < sale)
        ownusRate = Math.round((sale - benefit) / sale * 1000) / 1000;
```

- [ ] **Step 3: 문법 검사**

Run: `node --check "C:/Users/seung/Desktop/moum-crawler-v0.7.35/background.js"`
Expected: 출력 없음(통과)

- [ ] **Step 4: 버전 3곳 올리기 (0.7.55)**

`manifest.json` · `background.js` · `content_mou.js` 의 버전을 `0.7.55` 로. 한 곳만 올리면
크롬이 새 코드를 안 읽거나 화면 표시가 어긋난다.

- [ ] **Step 5: 라이브 대조**

크롬 확장 새로고침 후, 롯데온 실제 상품 URL 로 `crawl.one` 을 돌려 표면가가
「나의 혜택가」가 아닌 판매가인지, 오너스율이 역산값이 아닌지 확인한다.

- [ ] **Step 6: Commit**

```bash
git add "프로그램/_시스템/extension/moum-crawler/"
git commit -m "fix(롯데온): 표면가를 혜택가로 대체한 경우 오너스율 역산 금지 — 이중차감 차단"
```

---

### Task 8: SSF 기프트포인트를 실제 금액으로

**Files:**
- Modify: `프로그램/_시스템/webapp/routes/api_benefits.py:741-747`
- Test: `프로그램/_시스템/tests/pricing/test_ssf_gift_point_amount.py`

문제: 크롤한 기프트포인트 **금액을 버리고** 10% 정률로 계산한다. 게다가 정률은
직전 잔액에 곱해지므로 표면가의 10% 도 아니다.

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# -*- coding: utf-8 -*-
"""SSF 기프트포인트는 크롤한 실제 금액으로 빠져야 한다.

종전: 금액을 버리고 10% 정률 → 직전 잔액에 곱해져 실제와 다른 값이 됐다.
"""
import json
import pytest

from shared.db import SessionLocal
from lemouton.sourcing.models_pricing import OptionSourceUrl
from lemouton.sources.models import SourceProduct
from webapp.routes.api_benefits import compute_breakdown

PFX, SSF = 'SSFGP-', 4


@pytest.fixture
def sku():
    s = SessionLocal()
    k, url = PFX + '1', 'https://example.test/ssfgp/1'
    try:
        s.add(OptionSourceUrl(canonical_sku=k, source_id=SSF, product_url=url))
        s.add(SourceProduct(site='ssf', url=url, dynamic_benefits_json=json.dumps(
            {'gift_point_amount': 7000})))
        s.commit()
    finally:
        s.close()
    yield k
    s = SessionLocal()
    try:
        s.query(OptionSourceUrl).filter(
            OptionSourceUrl.canonical_sku.like(PFX + '%')).delete(synchronize_session=False)
        s.query(SourceProduct).filter(
            SourceProduct.url.like('https://example.test/ssfgp/%')).delete(
            synchronize_session=False)
        s.commit()
    finally:
        s.close()


def test_gift_point_uses_crawled_amount(sku):
    s = SessionLocal()
    try:
        r = compute_breakdown(s, sku=sku, source_id=SSF, sale_price=100000.0)
        gp = [st for st in r['steps'] if '기프트포인트' in st['name']]
        assert gp, '기프트포인트 단계가 있어야 한다'
        assert gp[0]['deduct'] == 7000       # 10%(=10,000)가 아니라 실제 7,000
    finally:
        s.rollback(); s.close()
```

- [ ] **Step 2: 실패 확인**

Run: `cd "프로그램/_시스템" && python -m pytest tests/pricing/test_ssf_gift_point_amount.py -q`
Expected: FAIL — `assert 10000 == 7000`

- [ ] **Step 3: 실금액 사용으로 교체**

```python
    if _site_for == 'ssf':
        # [2026-07-20] 크롤한 실제 금액으로 뺀다. 종전엔 금액을 버리고 10% 정률이라
        #   직전 잔액에 곱해져 표면가의 10% 도, 실제 적립액도 아닌 값이 됐다.
        #   금액을 못 읽었을 때만 비활성 placeholder 로 남겨 화면에서 사라지지 않게 한다.
        _gp_amt = (_dynamic_benefits or {}).get('gift_point_amount')
        try:
            _gp_amt = int(_gp_amt) if _gp_amt else 0
        except (TypeError, ValueError):
            _gp_amt = 0
        effective.append(('dyn', _DynBenefit(
            name='기프트포인트 (멤버십 한정)',
            btype='amount' if _gp_amt else 'rate',
            value=_gp_amt if _gp_amt else 0.10,
            enabled=bool(_gp_amt),
        )))
```

- [ ] **Step 4: 통과 + 박제 확인**

Run: `cd "프로그램/_시스템" && python -m pytest tests/pricing/ -q`
Expected: 새 테스트 통과. 박제의 `test_ssf_gift_point_active_when_crawled` 기대값
(85,500)이 바뀐다 → **의도한 금액 변경**이므로 실측값으로 갱신하고 Task 12 대조표에 싣는다.

- [ ] **Step 5: Commit**

```bash
git add "프로그램/_시스템/webapp/routes/api_benefits.py" "프로그램/_시스템/tests/pricing/"
git commit -m "fix(SSF): 기프트포인트를 크롤 실금액으로 차감 (종전 10% 정률 추정 제거)"
```

---

### Task 9: 현대H몰 혜택이 빈값으로 덮이지 않게

**Files:**
- Modify: `프로그램/_시스템/webapp/routes/api_sources_parse.py:142-167`
- Test: `프로그램/_시스템/tests/sourcing/test_navgrab_benefit_preserve.py`

문제: 창 없이 빠르게 긁는 경로는 혜택을 못 읽는데, 결과가 비어도 그대로 저장해
**전에 제대로 긁어둔 혜택까지 지운다.**

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# -*- coding: utf-8 -*-
"""혜택을 못 읽은 크롤이 기존 혜택을 지우면 안 된다.

현대H몰은 창 없이 긁으면 혜택이 안 나온다(렌더 DOM 필요). 그 빈 결과로 덮으면
전에 제대로 긁어둔 포인트·카드할인이 사라져 매입가가 비싸진다.
"""
import json
import pytest

from shared.db import SessionLocal
from lemouton.sources.models import SourceProduct
from webapp.routes.api_sources_parse import _save_navgrab_dynamic_benefits

URL = 'https://example.test/hmallpreserve/1'


@pytest.fixture
def sp():
    s = SessionLocal()
    try:
        s.query(SourceProduct).filter_by(url=URL).delete(synchronize_session=False)
        s.add(SourceProduct(site='hmall', url=URL, dynamic_benefits_json=json.dumps(
            {'hmall_point_amount': 3000, 'hmall_card_discount': 5000})))
        s.commit()
    finally:
        s.close()
    yield URL
    s = SessionLocal()
    try:
        s.query(SourceProduct).filter_by(url=URL).delete(synchronize_session=False)
        s.commit()
    finally:
        s.close()


def test_empty_benefits_do_not_wipe_existing(sp):
    """★ 빈 혜택으로 덮지 않는다."""
    s = SessionLocal()
    try:
        _save_navgrab_dynamic_benefits(s, url=sp, site='hmall', options=[])
        s.commit()
        row = s.query(SourceProduct).filter_by(url=sp).first()
        kept = json.loads(row.dynamic_benefits_json or '{}')
        assert kept.get('hmall_point_amount') == 3000
        assert kept.get('hmall_card_discount') == 5000
    finally:
        s.rollback(); s.close()


def test_new_benefits_still_overwrite(sp):
    """값이 있으면 정상적으로 갱신된다(보존이 갱신을 막으면 안 된다)."""
    s = SessionLocal()
    try:
        _save_navgrab_dynamic_benefits(
            s, url=sp, site='hmall',
            options=[{'hmall_point_amount': 4000}])
        s.commit()
        row = s.query(SourceProduct).filter_by(url=sp).first()
        assert json.loads(row.dynamic_benefits_json)['hmall_point_amount'] == 4000
    finally:
        s.rollback(); s.close()
```

- [ ] **Step 2: 실패 확인**

Run: `cd "프로그램/_시스템" && python -m pytest tests/sourcing/test_navgrab_benefit_preserve.py -q`
Expected: FAIL — 기존 혜택이 지워짐

- [ ] **Step 3: 빈 결과일 때 보존**

`api_sources_parse.py` 의 `_save_navgrab_dynamic_benefits` 안, 저장 직전:

```python
    # [2026-07-20] 혜택을 하나도 못 읽었으면 **기존 값을 유지**한다.
    #   현대H몰은 창 없이 긁으면 혜택이 안 나온다(렌더 DOM 필요). 그 빈 결과로 덮으면
    #   전에 제대로 긁어둔 포인트·카드할인이 사라져 매입가가 비싸진다.
    #   '못 읽음'과 '혜택 없음'은 다르다 — 못 읽었으면 아무 말도 하지 않는다.
    if not dyn:
        return 0
```

(함수의 기존 시그니처·반환 규약에 맞춰 삽입한다. 시그니처가 다르면
`sp.dynamic_benefits_json = None` 로 쓰는 지점을 찾아 그 앞에 같은 가드를 둔다.)

- [ ] **Step 4: 통과 확인**

Run: `cd "프로그램/_시스템" && python -m pytest tests/sourcing/ -q`
Expected: 전건 통과

- [ ] **Step 5: Commit**

```bash
git add "프로그램/_시스템/webapp/routes/api_sources_parse.py" "프로그램/_시스템/tests/sourcing/test_navgrab_benefit_preserve.py"
git commit -m "fix(현대H몰): 혜택 못 읽은 크롤이 기존 혜택을 지우던 것 차단"
```

---

## Phase 4 — S5 금액 공용화

### Task 10: 조립부를 순수 함수로 추출

**Files:**
- Create: `프로그램/_시스템/lemouton/pricing/benefit_assembly.py`
- Modify: `프로그램/_시스템/webapp/routes/api_benefits.py:684-1112`

- [ ] **Step 1: 박제 기준선 확인**

Run: `cd "프로그램/_시스템" && python -m pytest tests/pricing/test_breakdown_characterization.py -q`
Expected: `12 passed`

- [ ] **Step 2: 추출 — 시그니처 고정**

`benefit_assembly.py` 에 `assemble_effective` 를 만들고, `api_benefits.py:684-1112` 본문을
**그대로** 옮긴다. 옮기면서 아래만 바꾼다:
- `session.query(...)` 하던 세 곳(구매카드·가이드 2회)은 인자로 받는다
- `_turn_off` 지역집합(Task 5)과 `_norm_benefit_name`(Task 6)은 같이 옮긴다

```python
def assemble_effective(*, sale_price, source_id, sku, site,
                       tpl_items, ovr_items, dynamic_benefits,
                       purchase_cards, guide):
    """혜택 조립 — 최종가 계산에 넘길 effective 목록을 만든다(순수 함수).

    ⚠ 이 함수는 최종 매입가의 단일 원천이다. 화면(매트릭스 영수증)과 소싱처 지도
      「▶ 크롤」이 **같은 함수**를 써야 같은 금액이 나온다. 여기 규칙을 다른 곳에
      옮겨 적으면 같은 상품이 화면마다 다른 금액이 된다.

    returns: (effective, base_override, coupon_pick)
    """
```

- [ ] **Step 3: `compute_breakdown` 이 호출하도록 교체**

```python
    effective, _base_override, _coupon_pick = assemble_effective(
        sale_price=sale_price, source_id=source_id, sku=sku, site=_site_for,
        tpl_items=tpl_items, ovr_items=ovr_items,
        dynamic_benefits=_dynamic_benefits,
        purchase_cards=_load_purchase_cards(session, _cache),
        guide=_roster_guide,      # 984/1083 에서 쓰던 것을 취득부로 끌어올린다
    )
```

- [ ] **Step 4: 박제 무변동 확인 (가장 중요)**

Run: `cd "프로그램/_시스템" && python -m pytest tests/pricing/ tests/sourcing/ -q`
Expected: **전건 통과 · 금액 한 원도 안 바뀜.** 하나라도 깨지면 추출이 잘못된 것이니
되돌리고 원인을 찾는다.

- [ ] **Step 5: Commit**

```bash
git add "프로그램/_시스템/lemouton/pricing/benefit_assembly.py" "프로그램/_시스템/webapp/routes/api_benefits.py"
git commit -m "refactor(가격): 혜택 조립부를 공용 순수 함수로 추출 (박제 무변동)"
```

---

### Task 11: 지도 「▶ 크롤」이 같은 엔진을 쓰게

**Files:**
- Modify: `프로그램/_시스템/lemouton/sourcing/guide_url_result.py`
- Modify: `프로그램/_시스템/webapp/routes/sourcing_guide.py` (url-result 라우트)
- Test: `프로그램/_시스템/tests/sourcing/test_guide_url_result.py` (기존 20건 유지 + 추가)

- [ ] **Step 1: 실패하는 테스트 추가**

```python
def test_musinsa_uses_crawled_benefit_amounts():
    """★ 무신사 — 긁은 혜택 라인에서 금액을 뽑아 실제 최종 매입가를 낸다.

    2026-07-20 실측: 표면 119,900 에서 등급적립 4,460 + 무신사머니 4,050 이
    빠져야 하는데, 종전에는 고정 혜택 500원만 빠져 119,400 이 나왔다.
    """
    LINES = ['등급 적립(LV.9 블랙다이아몬드 · 4%)4,460원',
             '무신사머니 결제 시 4% 적립4,050원',
             '후기 적립2,500원', '등급 할인 불가', '상품 쿠폰사용가능 쿠폰 없음']
    g = _guide(benefits=[_crawled('등급적립', ['등급 적립']),
                         _crawled('무신사머니 결제 적립', ['무신사머니'])])
    r = compute_url_result(g, _ok(surface_price=119900, benefit_lines=LINES),
                           now_iso=NOW, site='musinsa')
    assert r['benefit_source'] == 'crawled'
    assert r['benefit_total'] >= 8510          # 등급적립 + 무신사머니
    assert r['final_price'] <= 111400
```

- [ ] **Step 2: 실패 확인**

Run: `cd "프로그램/_시스템" && python -m pytest tests/sourcing/test_guide_url_result.py -q`
Expected: FAIL — `compute_url_result() got an unexpected keyword argument 'site'`

- [ ] **Step 3: 크롤 결과 → 동적혜택 → 공용 조립 배선**

`guide_url_result.py` 에서 자체 혜택 조립을 걷어내고 아래로 바꾼다:

```python
from lemouton.pricing.benefit_assembly import assemble_effective
from lemouton.pricing.benefit_parse import (
    parse_musinsa_benefit_amounts, extract_dynamic_benefits_from_options,
)

def _dynamic_from_raw(site, raw, surface):
    """크롤 raw → dynamic_benefits dict. 저장 경로와 **같은 함수**를 쓴다."""
    if site == 'musinsa':
        d = parse_musinsa_benefit_amounts(raw.get('benefit_lines') or [],
                                          surface_price=surface)
        d['surface_price'] = surface
        return d
    return extract_dynamic_benefits_from_options(raw.get('options') or [])
```

`compute_url_result(guide, raw, *, now_iso, site=None, tpl_items=(), purchase_cards=())`
로 시그니처를 넓히고, 내부에서 `assemble_effective(...)` → `compute_final_price(...)` 를 태운다.

- [ ] **Step 4: 라우트가 소싱처 키·템플릿을 넘기게**

`sourcing_guide.py` 의 `api_url_result` 에서:

```python
        from lemouton.sourcing.source_ids import pricing_source_id
        _pid = pricing_source_id(src.source_key)
        _tpls = (s.query(SourceBenefitTemplate).filter_by(source_id=_pid)
                  .order_by(SourceBenefitTemplate.sort_order).all()
                 if isinstance(_pid, int) else [])
        result = compute_url_result(guide, raw, now_iso=_now_iso(),
                                    site=src.source_key, tpl_items=_tpls)
```

- [ ] **Step 5: 통과 확인**

Run: `cd "프로그램/_시스템" && python -m pytest tests/sourcing/ tests/pricing/ -q`
Expected: 전건 통과

- [ ] **Step 6: Commit**

```bash
git add "프로그램/_시스템/lemouton/sourcing/guide_url_result.py" "프로그램/_시스템/webapp/routes/sourcing_guide.py" "프로그램/_시스템/tests/sourcing/test_guide_url_result.py"
git commit -m "feat(지도): 「▶ 크롤」이 매트릭스와 같은 계산 엔진을 쓰게 — 금액 일치"
```

---

## Phase 5 — 검증·배포

### Task 12: 전/후 금액 대조표 (🛑 배포 게이트)

**Files:**
- Create: `프로그램/_시스템/scripts/price_diff_report.py`

- [ ] **Step 1: 대조 스크립트 작성**

수정 전(`origin/main`)과 후(현재 브랜치)를 각각 돌려 소싱처별 최종 매입가를 비교하고,
**바뀐 셀만** 표로 뽑는다. 박제 테스트의 12개 시나리오 + 소싱처별 실제 URL 1건씩.

- [ ] **Step 2: 실행**

Run: `cd "프로그램/_시스템" && python scripts/price_diff_report.py`
Expected: `소싱처 | 상품 | 전 | 후 | 차액 | 이유` 표

- [ ] **Step 3: 🛑 사장님 확인 게이트**

바뀐 금액을 전부 보고하고 확인받는다. **확인 전 배포 금지.**

- [ ] **Step 4: 라이브 전수 대조**

확장 새로고침 후 소싱처 8곳 실제 상품 URL 로 「▶ 크롤」을 돌려, 화면 최종 매입가가
매트릭스 영수증과 **같은 값**인지 눈으로 대조한다(정합성 원칙 1·2).

- [ ] **Step 5: 배포**

```bash
git rebase origin/main
git push origin feat/sourcing-detail-into-map:main
```

---

## Self-Review

**Spec coverage** — 조사에서 나온 6개 문제 + S5 금액 공용화 전부에 태스크가 있다:
번호(T1~T4) · 공유객체(T5) · 게이트 이름(T6) · 롯데온(T7) · SSF(T8) · 현대H몰(T9) ·
공용화(T10~T11) · 검증(T0·T12).

**미해결로 남기는 것 (별도 결정 필요 — 이번 범위 밖)**
- 스마트스토어 `review_point_max` 를 계산이 읽지 않는 건: 읽게 하면 **매입가가 내려간다**.
  값이 맞는지 라이브 확인이 먼저라 별도 건으로 둔다.
- SSG 상품쿠폰 **상한액**(`product_coupon_max_discount`) 미적용: 쿠폰 토글이 꺼져 있어야
  안 물린다. 토글 켜는 운영을 시작하기 전에 처리.
- 현대H몰·롯데아이몰이 혜택 템플릿을 못 갖는 구조(문자 번호): T4 에서 **경고는 뜨게** 했지만
  구조 자체는 안 고친다(설계문서 §8 기존 항목).
