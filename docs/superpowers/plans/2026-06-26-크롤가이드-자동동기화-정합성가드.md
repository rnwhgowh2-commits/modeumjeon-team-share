# 크롤가이드 자동 동기화 + 전체크롤 정합성 가드 — 구현 플랜

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 크롤 코드가 바뀌거나 신규 소싱처가 추가될 때, 데이터지도(원문 .md + 보기 map.html)와의 어긋남을 데이터지도 상단 배너로 자동 노출한다.

**Architecture:** 순수 검사 함수(`webapp/routes/guide_sync.py`)가 ① 빌트인 소싱처 ↔ 가이드 라벨(P1) ② §7 인용 핵심 심볼 존재(P3·C2)를 대조해 dict 반환. `data_code_map()` 라우트가 이를 템플릿에 전달, `map.html` 상단이 배너 렌더. 확장 버전 정합성(P3·C1)은 클라이언트 JS가 `MoumExt.ping()` vs 서버 주입 기준버전 비교. 모든 검사 best-effort(페이지 안 깨짐), 누락 0이면 배너 없음.

**Tech Stack:** Python 3 · Flask · pytest(in-repo `프로그램/_시스템/tests/`) · Vanilla JS(map.html).

**배포:** 구현은 origin/main 워크트리에서 진행 → `git push origin HEAD:main`(프로젝트 자동배포 패턴, `[[project_deploy_isolated_change_via_worktree]]`). 아래 커밋 스텝은 워크트리 로컬 커밋이며, 각 Task 묶음 후 main push.

**테스트 실행 기준 디렉토리:** 모든 pytest 명령은 `프로그램/_시스템` 에서 실행(`cd 프로그램/_시스템`). 그 디렉토리가 `webapp`·`lemouton` import 루트.

---

## File Structure

| 파일 | 책임 | 신규/수정 |
|---|---|---|
| `프로그램/_시스템/webapp/routes/guide_sync.py` | 순수 검사 함수 + 매니페스트 + 기준버전 상수 | **신규** |
| `프로그램/_시스템/tests/sourcing/test_guide_sync.py` | 검사 함수 단위 테스트 | **신규** |
| `프로그램/_시스템/webapp/routes/sourcing_guide.py` | `data_code_map()` 라우트가 검사 호출·템플릿 전달 | 수정 |
| `프로그램/_시스템/webapp/templates/sourcing_guide/map.html` | 상단 배너 렌더 + C1 클라이언트 JS | 수정 |
| `프로그램/_시스템/docs/크롤링-가이드.md` | §7 E-섹션 정정(P0) + §7 체크리스트 게이트(P2) + §8 ✅상태 갱신 | 수정 |
| `~/.claude/skills/build-source-adapter/SKILL.md`(또는 프로젝트 스킬 위치) | 마지막 단계 "가이드 갱신→배너 0" 추가(P2) | 수정 |

---

## Task 1: P0 — §7 E-섹션 정정 (원문 + 보기, 문서)

전체크롤 실제 흐름과 어긋난 §7 E-섹션을 정정. 코드 없음(문서), 검증은 텍스트 확인.

**Files:**
- Modify: `프로그램/_시스템/docs/크롤링-가이드.md` (§7 확장 배선 표 E1~E4 영역)
- Modify: `프로그램/_시스템/webapp/templates/sourcing_guide/map.html` (보기 단계7 확장 표)

- [ ] **Step 1: 원문 §7 E-섹션 도입문 정정**

`docs/크롤링-가이드.md` 의 `### 🔧💻 확장 배선 4지점` 표 **바로 위**에 다음 문단을 삽입(없으면 추가):

```markdown
> ⚠️ **전체크롤 실제 진입점**: `toss.js`의 `enqueueCrawl(code)` → `ext_bridge.js`가 `send("crawl.enqueue",{code})` → **확장 백그라운드 큐러너**가 수행(탭 닫아도 지속). 큐러너가 소싱처별로 비로그인 4=navGrab(→서버 `/api/sources/parse`)·로그인 2=navExtract(확장 추출기)로 분기.
> ⚠️ **실제 확장 코드 = 데스크톱 로드본(현재 v0.7.6)**. repo `extension/moum-crawler`(manifest v0.4.3)는 **구버전**이라 `crawl.enqueue` 큐러너가 없음 — repo 확장 라인 신뢰 금지, 심볼 기준으로만 인용.
```

- [ ] **Step 2: 원문 E4 행 정정**

같은 파일 E4 행을 아래로 교체(`crawlBundle`→실제 진입점):

```markdown
| E4 | 크롤 대상 목록·진입 | `webapp/static/ext_bridge.js` `enqueueCrawl`→`crawl.enqueue`(확장 큐러너) / `crawlBundle`은 구버전 스케줄러 경로 | 새 소싱처는 모음전 등록 URL에 포함되면 큐러너가 자동 순회 · 혜택은 `crawl-result` 페이로드 `benefit_lines` 전달 필요 |
```

- [ ] **Step 3: 보기(map.html) 단계7 확장 표 동일 정정**

`map.html` 의 보기 단계7 확장 4지점 표에서 E4 행을 동일 취지로 교체:

```html
          <tr><td class="el">E4</td><td>크롤 진입점</td><td class="mono">ext_bridge.js enqueueCrawl → crawl.enqueue(확장 큐러너)</td><td>crawlBundle은 구버전 · 새 소싱처는 등록URL 포함시 자동순회</td></tr>
```

그리고 보기 단계7 상단(확장 표 위)에 한 줄 경고 추가:

```html
        <div class="d" style="margin-bottom:4px;color:#c0392b"><b>⚠️ 실제 확장 = 데스크톱 로드본(v0.7.6)</b> · repo extension/(v0.4.3) 구버전 — 심볼 기준 인용</div>
```

- [ ] **Step 4: 텍스트 검증**

Run: `cd 프로그램/_시스템 && grep -c "crawl.enqueue" docs/크롤링-가이드.md webapp/templates/sourcing_guide/map.html`
Expected: 두 파일 모두 ≥1 (정정 반영). `crawlBundleAll` 단독 인용이 사라졌는지 육안 확인.

- [ ] **Step 5: Commit**

```bash
git add docs/크롤링-가이드.md webapp/templates/sourcing_guide/map.html
git commit -m "fix(guide): §7 E-섹션 전체크롤 실제 흐름으로 정정(enqueueCrawl·확장큐러너)"
```

---

## Task 2: guide_sync.missing_sources + 테스트

빌트인 소싱처가 원문/보기에 문서화돼 있나(키 또는 라벨 등장) 대조하는 순수 함수.

**Files:**
- Create: `프로그램/_시스템/webapp/routes/guide_sync.py`
- Test: `프로그램/_시스템/tests/sourcing/test_guide_sync.py`

- [ ] **Step 1: 실패 테스트 작성**

`tests/sourcing/test_guide_sync.py` 생성:

```python
"""크롤가이드 ↔ 코드 동기화 검사 (guide_sync). 2026-06-26."""
from webapp.routes.guide_sync import missing_sources


def test_flags_source_absent_in_html():
    md = "무신사 표면가, crawlers/ssf.py 파싱"   # musinsa·ssf 둘 다 md엔 있음
    html = "무신사"                                # ssf 는 html에 없음
    sources = [
        {"key": "musinsa", "label": "무신사"},
        {"key": "ssf", "label": "SSF샵"},
    ]
    out = missing_sources(md, html, sources)
    assert len(out) == 1
    assert out[0]["key"] == "ssf"
    assert out[0]["in_md"] is True
    assert out[0]["in_html"] is False


def test_empty_when_all_present_by_key_or_label():
    md = html = "무신사 crawlers/ssf.py SSG 롯데온"
    sources = [
        {"key": "musinsa", "label": "무신사"},
        {"key": "ssf", "label": "SSF샵"},   # key 'ssf' 가 'crawlers/ssf.py' 에 등장
    ]
    assert missing_sources(md, html, sources) == []
```

- [ ] **Step 2: 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sourcing/test_guide_sync.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'webapp.routes.guide_sync'`

- [ ] **Step 3: 최소 구현**

`webapp/routes/guide_sync.py` 생성:

```python
"""크롤 가이드 ↔ 코드 동기화 검사 (순수 함수). data_code_map 라우트에서 호출.

best-effort: 어떤 검사도 예외를 던지지 않고 빈 결과로 degrade.
"""
from __future__ import annotations


def missing_sources(md_text, html_text, sources):
    """빌트인 소싱처 중 원문/보기에서 키·라벨 어느 것도 안 보이는 것 목록.

    sources: [{'key':..,'label':..}, ...]
    반환: [{'key','label','in_md','in_html'}, ...] (둘 중 하나라도 빠진 것만)
    """
    out = []
    for s in sources or []:
        key = (s.get("key") or "").strip()
        label = (s.get("label") or key).strip()

        def _present(text):
            text = text or ""
            return bool((key and key in text) or (label and label in text))

        in_md = _present(md_text)
        in_html = _present(html_text)
        if not (in_md and in_html):
            out.append({"key": key, "label": label, "in_md": in_md, "in_html": in_html})
    return out
```

- [ ] **Step 4: 통과 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sourcing/test_guide_sync.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: Commit**

```bash
git add webapp/routes/guide_sync.py tests/sourcing/test_guide_sync.py
git commit -m "feat(guide-sync): missing_sources 소싱처 누락 검사(P1)"
```

---

## Task 3: guide_sync.missing_symbols + SYMBOL_MANIFEST + 테스트

§7 등이 인용한 핵심 코드 심볼이 해당 파일에 실재하나 grep하는 순수 함수.

**Files:**
- Modify: `프로그램/_시스템/webapp/routes/guide_sync.py`
- Test: `프로그램/_시스템/tests/sourcing/test_guide_sync.py`

- [ ] **Step 1: 실패 테스트 추가**

`test_guide_sync.py` 에 추가:

```python
import os
from webapp.routes.guide_sync import missing_symbols, SYMBOL_MANIFEST

# 앱 루트(_시스템) = 이 파일 기준 ../../
APP_ROOT = os.path.normpath(os.path.join(os.path.dirname(__file__), "..", ".."))


def test_missing_symbols_real_repo_is_clean():
    # 현재 매니페스트 심볼은 전부 실제 코드에 존재해야 함(0 누락)
    assert missing_symbols(APP_ROOT) == []


def test_missing_symbols_flags_renamed(tmp_path):
    # 가짜 앱루트 — 매니페스트 심볼이 없는 파일
    f = tmp_path / "a.py"
    f.write_text("def other(): pass", encoding="utf-8")
    manifest = [("build_crawlers", "a.py")]
    out = missing_symbols(str(tmp_path), manifest=manifest)
    assert out == [{"symbol": "build_crawlers", "file": "a.py"}]
```

- [ ] **Step 2: 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sourcing/test_guide_sync.py -v`
Expected: FAIL — `ImportError: cannot import name 'missing_symbols'`

- [ ] **Step 3: 구현 추가**

`guide_sync.py` 에 추가:

```python
import os

# C2 — §7 등이 인용하는 핵심 코드 심볼: (심볼, 앱루트(_시스템) 기준 상대경로)
SYMBOL_MANIFEST = [
    ("build_crawlers",        "lemouton/sourcing/crawlers/__init__.py"),
    ("_SITE_BY_SRC",          "webapp/routes/api_benefits.py"),
    ("compute_breakdown",     "webapp/routes/api_benefits.py"),
    ("_detect_site_from_url", "webapp/routes/api_pricing.py"),
    ("_resolve_stock",        "webapp/routes/api_pricing.py"),
    ("_persist_option_stocks","webapp/routes/api_pricing.py"),
    ("PRODUCT_DYNAMIC_KEYS",  "lemouton/sources/service.py"),
    ("SOURCES",               "lemouton/sourcing/source_registry.py"),
    ("SOURCE_ORDER",          "webapp/static/crawl_log.js"),
    ("EXTRACTORS",            "extension/moum-crawler/background.js"),
]


def missing_symbols(app_root, manifest=None):
    """매니페스트 심볼 중 해당 파일에서 안 보이는(또는 파일 없는) 것 목록.

    반환: [{'symbol','file'}, ...]
    """
    manifest = manifest if manifest is not None else SYMBOL_MANIFEST
    out = []
    for symbol, rel in manifest:
        path = os.path.join(app_root, *rel.split("/"))
        try:
            with open(path, encoding="utf-8") as f:
                text = f.read()
        except OSError:
            out.append({"symbol": symbol, "file": rel})
            continue
        if symbol not in text:
            out.append({"symbol": symbol, "file": rel})
    return out
```

- [ ] **Step 4: 통과 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sourcing/test_guide_sync.py -v`
Expected: PASS (4 passed). `test_missing_symbols_real_repo_is_clean` 가 통과 = 현재 모든 인용 심볼 실재.

- [ ] **Step 5: Commit**

```bash
git add webapp/routes/guide_sync.py tests/sourcing/test_guide_sync.py
git commit -m "feat(guide-sync): missing_symbols + SYMBOL_MANIFEST 심볼 정합성(P3-C2)"
```

---

## Task 4: compute_guide_drift 오케스트레이터 + 기준버전 상수 + 테스트

라우트가 한 번에 부를 조립 함수. 파일 읽기·SOURCES 필터·두 검사 호출·기준버전 포함.

**Files:**
- Modify: `프로그램/_시스템/webapp/routes/guide_sync.py`
- Test: `프로그램/_시스템/tests/sourcing/test_guide_sync.py`

- [ ] **Step 1: 실패 테스트 추가**

`test_guide_sync.py` 에 추가:

```python
from webapp.routes.guide_sync import compute_guide_drift, GUIDE_EXT_BASELINE


def test_compute_guide_drift_real_repo_clean():
    # 현재 6 빌트인 소싱처·심볼 모두 문서화/존재 → 누락 0
    d = compute_guide_drift(APP_ROOT)
    assert d["missing_sources"] == []
    assert d["missing_symbols"] == []
    assert d["ext_baseline"] == GUIDE_EXT_BASELINE
    assert isinstance(GUIDE_EXT_BASELINE, str) and GUIDE_EXT_BASELINE
```

- [ ] **Step 2: 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sourcing/test_guide_sync.py::test_compute_guide_drift_real_repo_clean -v`
Expected: FAIL — `ImportError: cannot import name 'compute_guide_drift'`

- [ ] **Step 3: 구현 추가**

`guide_sync.py` 에 추가:

```python
# C1 — 가이드가 기준 삼는 전체크롤 확장 버전. 확장 방식 갱신·§7 정정 시 함께 올림.
GUIDE_EXT_BASELINE = "0.7.6"

_MD_REL = ("docs", "크롤링-가이드.md")
_HTML_REL = ("webapp", "templates", "sourcing_guide", "map.html")


def _read(path):
    try:
        with open(path, encoding="utf-8") as f:
            return f.read()
    except OSError:
        return ""


def _builtin_sources():
    """source_registry.SOURCES 중 빌트인만 [{'key','label'}]. import 실패 시 빈 목록."""
    try:
        from lemouton.sourcing.source_registry import SOURCES
    except Exception:
        return []
    return [{"key": s.get("key"), "label": s.get("label")}
            for s in SOURCES if s.get("builtin")]


def compute_guide_drift(app_root):
    """라우트용 조립 — 원문/보기 읽고 두 검사 + 기준버전 반환. 절대 예외 안 던짐."""
    md = _read(os.path.join(app_root, *_MD_REL))
    html = _read(os.path.join(app_root, *_HTML_REL))
    try:
        ms = missing_sources(md, html, _builtin_sources())
    except Exception:
        ms = []
    try:
        msym = missing_symbols(app_root)
    except Exception:
        msym = []
    return {"missing_sources": ms, "missing_symbols": msym,
            "ext_baseline": GUIDE_EXT_BASELINE}
```

- [ ] **Step 4: 통과 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sourcing/test_guide_sync.py -v`
Expected: PASS (5 passed). real_repo_clean 통과 = 현재 라이브 상태 누락 0(배너 안 뜸).

- [ ] **Step 5: Commit**

```bash
git add webapp/routes/guide_sync.py tests/sourcing/test_guide_sync.py
git commit -m "feat(guide-sync): compute_guide_drift 조립 + GUIDE_EXT_BASELINE(C1 기준버전)"
```

---

## Task 5: data_code_map 라우트에 검사 배선

라우트가 `compute_guide_drift`를 호출해 템플릿에 전달.

**Files:**
- Modify: `프로그램/_시스템/webapp/routes/sourcing_guide.py` (`data_code_map` 함수, 약 104~117행)

- [ ] **Step 1: import 추가**

`sourcing_guide.py` 상단 import 블록에 추가:

```python
from webapp.routes.guide_sync import compute_guide_drift
```

- [ ] **Step 2: 앱 루트 계산 상수 추가**

`_GUIDE_MD` 정의(약 99행) 바로 아래에 추가:

```python
# 앱 루트(_시스템) = 이 파일 기준 ../.. (guide_sync 검사용)
_APP_ROOT = os.path.normpath(os.path.join(os.path.dirname(__file__), "..", ".."))
```

- [ ] **Step 3: data_code_map 에서 drift 계산·전달**

`data_code_map()` 함수(약 104~117행)를 아래로 수정 — `render_template` 호출에 `drift=...` 추가:

```python
@bp.route("/map")
def data_code_map():
    """데이터·코드 지도 (HTML 렌더 — 보기/원문 토글 + 동기화 배너)."""
    drift = compute_guide_drift(_APP_ROOT)
    bare = request.args.get("bare")
    if bare:
        return make_response(render_template(
            "sourcing_guide/map.html", active="sourcing_guide",
            layout="_bare.html", drift=drift))
    return render_template("sourcing_guide/map.html",
                           active="sourcing_guide", layout="base.html", drift=drift)
```

(주: 기존 함수의 `bare` 분기 구조를 따르되 `drift=drift` 만 두 `render_template` 호출에 추가. `request`·`make_response` 가 미import면 기존 import 라인에 추가.)

- [ ] **Step 4: 라우트 로드 검증(스모크)**

Run: `cd 프로그램/_시스템 && python -c "import webapp.routes.sourcing_guide as m; print('ok', hasattr(m, 'data_code_map'))"`
Expected: `ok True` (import 에러 없음 — drift 배선 후 모듈 로드 정상)

- [ ] **Step 5: Commit**

```bash
git add webapp/routes/sourcing_guide.py
git commit -m "feat(guide-sync): data_code_map 라우트에 drift 검사 배선"
```

---

## Task 6: map.html 상단 배너 렌더 (P1 소싱처 + C2 심볼)

서버가 넘긴 `drift` 로 보기 모드 상단 배너 표시. 누락 0이면 안 그림.

**Files:**
- Modify: `프로그램/_시스템/webapp/templates/sourcing_guide/map.html` (보기/원문 토글 `seg` 바로 위 또는 `.sgmap` 컨테이너 최상단)

- [ ] **Step 1: 배너 CSS 추가**

`map.html` `<style>` 블록 끝부분(`.sgmap .who.m{...}` 다음 줄)에 추가:

```css
  .sgmap .syncbar{background:#FFF4F4;border:1px solid #F3C2C2;border-radius:10px;padding:11px 14px;margin-bottom:16px;font-size:14px;line-height:1.6;color:#8a2a2a;}
  .sgmap .syncbar b{color:#c0392b;}
  .sgmap .syncbar .it{display:inline-block;margin-right:10px;}
```

- [ ] **Step 2: 배너 마크업 추가**

`map.html` 에서 보기/원문 토글(`<div class="seg">...`) **바로 위**에 Jinja 블록 삽입:

```html
  {% if drift and (drift.missing_sources or drift.missing_symbols) %}
  <div class="syncbar" id="syncbar">
    <b>⚠️ 가이드 ↔ 코드 동기화 필요</b><br>
    {% for s in drift.missing_sources %}
      <span class="it">소싱처 <b>{{ s.label }}</b> 누락 (원문 {{ '✓' if s.in_md else '✗' }} / 보기 {{ '✓' if s.in_html else '✗' }})</span>
    {% endfor %}
    {% for sym in drift.missing_symbols %}
      <span class="it">심볼 <b>{{ sym.symbol }}</b> 사라짐 ({{ sym.file }})</span>
    {% endfor %}
  </div>
  {% endif %}
```

- [ ] **Step 3: 렌더 검증(라이브, 누락 0 → 배너 없음)**

Run: 배포 후 `/sourcing-guide/map` 열기. Expected: 현재 누락 0이므로 **배너 안 보임**(정상). 임시로 `SYMBOL_MANIFEST` 에 가짜 심볼(`("__NOPE__","webapp/routes/api_pricing.py")`)을 넣고 새로고침하면 빨강 배너 노출 → 확인 후 가짜 항목 제거.

- [ ] **Step 4: Commit**

```bash
git add webapp/templates/sourcing_guide/map.html
git commit -m "feat(guide-sync): 데이터지도 상단 동기화 배너(P1 소싱처·C2 심볼)"
```

---

## Task 7: map.html C1 확장 버전 정합성 (클라이언트 JS) + §8 상태 갱신

확장이 설치돼 있으면 `MoumExt.ping()` 버전을 서버 기준버전과 비교해 배너 한 줄 추가.

**Files:**
- Modify: `프로그램/_시스템/webapp/templates/sourcing_guide/map.html` (`<script>` 블록 + 배너)
- Modify: `프로그램/_시스템/docs/크롤링-가이드.md` (§8 ✅(예정)→(적용))

- [ ] **Step 1: 기준버전을 JS로 노출**

`map.html` 배너 영역 근처(또는 script 시작부)에 서버 값 주입:

```html
  <script>window.__GUIDE_EXT_BASELINE = "{{ drift.ext_baseline if drift else '' }}";</script>
```

- [ ] **Step 2: C1 비교 JS 추가**

`map.html` 메인 `<script>` 블록 끝(IIFE 내부, 탭 핸들러 다음)에 추가:

```javascript
  // C1 — 확장 버전 정합성: 로드된 확장 vs 가이드 기준버전
  (function(){
    var base = window.__GUIDE_EXT_BASELINE || "";
    if (!base || !(window.MoumExt && window.MoumExt.ping)) return;  // 확장 없으면 skip
    Promise.resolve(window.MoumExt.ping()).then(function(p){
      var v = (p && p.version) || "";
      if (!v || v === base) return;
      var bar = root.querySelector('#syncbar');
      if (!bar) {
        bar = document.createElement('div');
        bar.className = 'syncbar'; bar.id = 'syncbar';
        bar.innerHTML = '<b>⚠️ 가이드 ↔ 코드 동기화 필요</b><br>';
        var seg = root.querySelector('.seg');
        if (seg && seg.parentNode) seg.parentNode.insertBefore(bar, seg);
      }
      var span = document.createElement('span');
      span.className = 'it';
      span.innerHTML = '전체크롤 확장 <b>v' + v + '</b> 로드됨 — 가이드 기준 v' + base + ', §7 전체크롤 설명 재검토';
      bar.appendChild(span);
    }).catch(function(){});
  })();
```

(주: `root` 는 기존 스크립트가 쓰는 `.sgmap` 루트 엘리먼트. 기존 IIFE 안에 넣어 같은 `root` 를 재사용. 별도 IIFE면 `var root=document.querySelector('.sgmap');` 추가.)

- [ ] **Step 3: §8 상태 표기 갱신(예정→적용)**

`docs/크롤링-가이드.md` §8 "무엇이 자동 / 무엇이 절차" 표에서 P1·P3 행의 `✅ (예정 — ...)` 를 `✅ (적용)` 로 바꾸고, 아래 "현재 상태" 인용문을 갱신:

```markdown
> ✅ **현재 상태**: 4·5의 자동 배너 **적용 완료** — 데이터지도 상단에 소싱처 누락(P1)·심볼 사라짐(C2)·확장 버전 불일치(C1) 자동 표시. 순수 로직 변경은 여전히 절차로.
```

보기(map.html) 8번 탭의 동일 "현재 상태" 문구도 같은 취지로 갱신(`구현 예정`→`적용 완료`, `예정` 배지 제거).

- [ ] **Step 4: 라이브 검증**

Run: 배포 후 확장 설치된 브라우저에서 `/sourcing-guide/map` 열기. Expected: 로드 확장 버전이 기준(0.7.6)과 같으면 배너 없음; `GUIDE_EXT_BASELINE` 를 임시로 `"0.0.1"` 로 바꿔 배포·새로고침하면 "확장 vX 로드됨 — 기준 v0.0.1" 배너 노출 → 확인 후 되돌림.

- [ ] **Step 5: Commit**

```bash
git add webapp/templates/sourcing_guide/map.html docs/크롤링-가이드.md
git commit -m "feat(guide-sync): C1 확장버전 정합성 클라이언트 검사 + §8 적용상태 갱신"
```

---

## Task 8: P2 — 갱신 절차 게이트화 (§7 체크리스트 + 스킬)

신규 소싱처 추가/크롤방식 변경 시 "원문+보기 갱신→배너 0" 을 절차로 못박음.

**Files:**
- Modify: `프로그램/_시스템/docs/크롤링-가이드.md` (§7 자가검증 끝)
- Modify: `build-source-adapter` 스킬 문서(프로젝트 스킬 위치 — 없으면 §7만)

- [ ] **Step 1: §7 자가검증에 게이트 줄 추가**

`docs/크롤링-가이드.md` §7 "라이브 자가검증" 리스트 끝에 추가:

```markdown
7. **동기화 게이트** — 변경 후 `/sourcing-guide/map` 상단 **배너가 0(조용함)** 인가? 빨강이면: 소싱처 누락→§1·§2·§6·§7 행 추가(원문+보기) / 심볼 사라짐→인용·SYMBOL_MANIFEST 갱신 / 확장버전→§7 설명 + GUIDE_EXT_BASELINE 갱신.
```

- [ ] **Step 2: build-source-adapter 스킬에 마지막 단계 추가**

`build-source-adapter` 스킬 문서 마지막에 단계 추가(스킬 파일을 찾아 SOP 끝에 삽입):

```markdown
## 마지막 — 가이드 동기화 (필수)
새 소싱처/크롤방식 반영 후:
1. 크롤링-가이드.md(원문) §1 재고·§2 가격·§6 형분류·§7 행 추가
2. map.html(보기) 해당 탭 행/카드 동일 추가
3. `/sourcing-guide/map` 배너가 0인지 확인 (P1/P3 통과). 빨강이면 §7 동기화 게이트 따라 해소.
```

(주: 스킬 파일 위치를 못 찾으면 이 Step은 §7 게이트 줄(Step 1)로 충분 — Step 1 만 적용하고 스킬 수정은 후속 과제로 남김.)

- [ ] **Step 3: 검증**

Run: `cd 프로그램/_시스템 && grep -c "동기화 게이트" docs/크롤링-가이드.md`
Expected: ≥1

- [ ] **Step 4: Commit**

```bash
git add docs/크롤링-가이드.md
git commit -m "docs(guide): P2 동기화 게이트 절차 — §7 자가검증 + 스킬"
```

---

## Task 9: 전체 테스트 + main 배포

- [ ] **Step 1: 전체 guide_sync 테스트**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sourcing/test_guide_sync.py -v`
Expected: PASS (5 passed)

- [ ] **Step 2: 회귀 — 인접 테스트 깨지지 않았나**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sourcing/ -q`
Expected: 기존 통과 테스트 유지(신규 실패 없음). 환경상 일부 사전 실패가 있으면 guide_sync 외 동일 상태인지만 확인.

- [ ] **Step 3: main 배포**

```bash
git push origin HEAD:main
```
배포 워크플로 success 확인 후 `/sourcing-guide/map` 라이브에서 배너 0(정상) 육안 검증.

---

## 검증 매트릭스 (스펙 ↔ 플랜)

| 스펙 | 구현 Task |
|---|---|
| P0 §7 E 정정 | Task 1 |
| P1 소싱처 누락 배너 | Task 2(함수)·5(라우트)·6(배너) |
| P2 갱신 절차 | Task 8 |
| P3-C1 확장버전 | Task 4(기준상수)·7(JS) |
| P3-C2 심볼 존재 | Task 3(함수)·6(배너) |
| 단일 진실 원천 SOURCES | Task 4 `_builtin_sources` |
| best-effort 안 깨짐 | Task 4 try/except, Task 6 `{% if drift %}` |
| 단위 테스트 | Task 2·3·4 |
