# 소싱처 업데이트 백엔드 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 구성(모음전)에 연동된 옵션들의 소싱처 URL을 모델 단위로 타깃 재크롤하는 백엔드 서비스 + 라우트 + 대시보드 버튼 배선을 만든다.

**Architecture:** 신규 `source_update_service.update_set_sources(session, set_id, ...)`가 ProductSet의 모든 SetProduct.model_code(중복 제거·정렬)를 모아, 각 모델마다 기존 `crawl_bundle_registered_urls(model_code=...)`(등록 소싱처 URL 재크롤·하드리셋·finalize 포함)를 호출하고 결과를 집계한다. HTTP 소싱처(ssf·ssg·ss_lemouton·smartstore 등)는 서버에서 즉시 크롤, 브라우저 소싱처(무신사·롯데온)는 `no_crawler`로 집계 → `need_extension=True`로 확장 크롤 안내. 라우트는 POST `/api/sets/<id>/recrawl-sources`. 대시보드 `📦 소싱처 업데이트` 버튼(`.sd-upd-src`, 현재 placeholder alert)을 이 라우트로 배선.

**Tech Stack:** Python 3.14, SQLAlchemy, Flask Blueprint, pytest(in-memory SQLite, DI 페이크), Vanilla JS.

---

### Task 1: source_update_service.update_set_sources (오케스트레이션)

**Files:**
- Create: `프로그램/_시스템/lemouton/sets/source_update_service.py`
- Test: `프로그램/_시스템/tests/sets/test_source_update_service.py`

테스트는 `프로그램/_시스템`에서 `python -m pytest tests/sets/...` 로 실행. 실제 크롤러를 부르지 않도록 `crawl_fn`(=crawl_bundle_registered_urls)을 DI로 주입한다.

- [ ] **Step 1: 실패 테스트 작성**

```python
import pytest
from sqlalchemy import create_engine
from sqlalchemy.orm import Session
from shared.db import Base
for _m in (
    "lemouton.sourcing.models", "lemouton.sourcing.models_pricing",
    "lemouton.sourcing.models_v2", "lemouton.pricing.settings",
    "lemouton.uploader.models", "lemouton.templates.models",
    "lemouton.inventory.models", "lemouton.sources.models",
    "lemouton.multitenancy.models", "lemouton.audit.models",
    "lemouton.mapping.models", "lemouton.sets.models",
):
    try:
        __import__(_m)
    except ImportError:
        pass
from lemouton.sets.models import ProductSet, SetProduct
from lemouton.sets import source_update_service as srv


@pytest.fixture
def db():
    eng = create_engine("sqlite://"); Base.metadata.create_all(eng)
    s = Session(eng); yield s; s.close()


def _seed_set(db, model_codes):
    ps = ProductSet(name="테스트셋", model_code=model_codes[0])
    db.add(ps); db.flush()
    for i, mc in enumerate(model_codes):
        db.add(SetProduct(set_id=ps.id, model_code=mc, quantity=1, sort_order=i))
    db.commit(); return ps


def _fake_crawl(calls, ret_by_model):
    def _fn(session, *, model_code, crawlers=None, progress_cb=None):
        calls.append(model_code)
        return ret_by_model.get(model_code, {"total": 0, "ok": 0, "error": 0, "no_crawler": 0, "per_source": {}})
    return _fn


def test_crawls_each_distinct_model(db):
    ps = _seed_set(db, ["MOD_A", "MOD_B"])
    calls = []
    fn = _fake_crawl(calls, {
        "MOD_A": {"total": 2, "ok": 2, "error": 0, "no_crawler": 0, "per_source": {"ssf": {"ok": 2, "error": 0, "no_crawler": 0}}},
        "MOD_B": {"total": 1, "ok": 1, "error": 0, "no_crawler": 0, "per_source": {"ssg": {"ok": 1, "error": 0, "no_crawler": 0}}},
    })
    r = srv.update_set_sources(db, set_id=ps.id, crawlers={}, crawl_fn=fn)
    assert calls == ["MOD_A", "MOD_B"]
    assert r["ok"] is True
    assert r["totals"] == {"total": 3, "ok": 3, "error": 0, "no_crawler": 0}
    assert r["per_source"]["ssf"]["ok"] == 2 and r["per_source"]["ssg"]["ok"] == 1
    assert r["need_extension"] is False
    assert [m["model_code"] for m in r["models"]] == ["MOD_A", "MOD_B"]


def test_dedupes_repeated_model(db):
    ps = _seed_set(db, ["MOD_A", "MOD_A"])
    calls = []
    fn = _fake_crawl(calls, {})
    srv.update_set_sources(db, set_id=ps.id, crawlers={}, crawl_fn=fn)
    assert calls == ["MOD_A"]


def test_need_extension_when_no_crawler(db):
    ps = _seed_set(db, ["MOD_A"])
    calls = []
    fn = _fake_crawl(calls, {
        "MOD_A": {"total": 3, "ok": 1, "error": 0, "no_crawler": 2, "per_source": {"musinsa": {"ok": 0, "error": 0, "no_crawler": 2}}},
    })
    r = srv.update_set_sources(db, set_id=ps.id, crawlers={}, crawl_fn=fn)
    assert r["need_extension"] is True
    assert r["totals"]["no_crawler"] == 2


def test_unknown_set_returns_error(db):
    fn = _fake_crawl([], {})
    r = srv.update_set_sources(db, set_id=99999, crawlers={}, crawl_fn=fn)
    assert r["ok"] is False and "구성" in r["error"]
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest tests/sets/test_source_update_service.py -v`
Expected: FAIL (ModuleNotFoundError: lemouton.sets.source_update_service)

- [ ] **Step 3: 최소 구현**

```python
"""[구성 레이어] 소싱처 업데이트 — 연동 옵션의 모델 단위로 등록 소싱처 URL 재크롤.

각 SetProduct.model_code(중복 제거)마다 crawl_bundle_registered_urls 를 호출.
HTTP 소싱처(ssf·ssg·ss_lemouton·smartstore)는 서버 즉시 크롤,
브라우저 소싱처(무신사·롯데온)는 no_crawler 로 집계 → need_extension 안내.
재크롤은 하드리셋+finalize 포함(기존 '전체 크롤'과 동일 경로) — 매트릭스 가격/재고가
이 한 번으로 갱신된다(폴백 금지 정책 유지).
"""
from __future__ import annotations


def update_set_sources(session, *, set_id, crawlers=None, crawl_fn=None):
    """구성에 연동된 옵션들의 소싱처 URL을 모델 단위로 재크롤한다.

    crawl_fn: DI용. 기본은 crawl_bundle_registered_urls. (테스트는 페이크 주입.)
    Returns: {ok, models:[{model_code,total,ok,error,no_crawler,per_source}],
              totals:{total,ok,error,no_crawler}, per_source:{key:{ok,error,no_crawler}},
              need_extension:bool}
    """
    from lemouton.sets.models import ProductSet
    if crawl_fn is None:
        from lemouton.sources.service import crawl_bundle_registered_urls
        crawl_fn = crawl_bundle_registered_urls
    if crawlers is None:
        from lemouton.sourcing.crawlers import build_crawlers
        crawlers = build_crawlers()

    ps = session.get(ProductSet, set_id)
    if ps is None:
        return {"ok": False, "error": "구성을 찾을 수 없어요."}

    # 다품: 각 SetProduct 의 model_code 합집합(정렬·중복 제거)
    seen = set()
    model_codes = []
    for sp in sorted(ps.products, key=lambda p: ((p.sort_order or 0), p.id)):
        mc = sp.model_code
        if mc and mc not in seen:
            seen.add(mc)
            model_codes.append(mc)

    totals = {"total": 0, "ok": 0, "error": 0, "no_crawler": 0}
    per_source: dict[str, dict] = {}
    models = []
    for mc in model_codes:
        r = crawl_fn(session, model_code=mc, crawlers=crawlers) or {}
        row = {"model_code": mc}
        for k in ("total", "ok", "error", "no_crawler"):
            v = int(r.get(k) or 0)
            row[k] = v
            totals[k] += v
        ps_map = r.get("per_source") or {}
        row["per_source"] = ps_map
        for sk, d in ps_map.items():
            agg = per_source.setdefault(sk, {"ok": 0, "error": 0, "no_crawler": 0})
            for kk in ("ok", "error", "no_crawler"):
                agg[kk] += int((d or {}).get(kk) or 0)
        models.append(row)

    return {
        "ok": True,
        "models": models,
        "totals": totals,
        "per_source": per_source,
        "need_extension": totals["no_crawler"] > 0,
    }
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest tests/sets/test_source_update_service.py -v`
Expected: PASS (4 passed)

- [ ] **Step 5: 커밋**

```bash
git add 프로그램/_시스템/lemouton/sets/source_update_service.py 프로그램/_시스템/tests/sets/test_source_update_service.py
git commit -m "feat(sets): 소싱처 업데이트 서비스 update_set_sources(모델 단위 재크롤·집계)"
```

---

### Task 2: POST /api/sets/<id>/recrawl-sources 라우트

**Files:**
- Modify: `프로그램/_시스템/webapp/routes/sets_api.py` (collect_set_route 아래에 추가)
- Test: `프로그램/_시스템/tests/sets/test_sets_api_routes.py` (라우트 등록·동작 — 기존 패턴 따름)

라우트는 기존 collect_set_route 패턴(SessionLocal→서비스→commit→jsonify) 그대로. 실제 크롤은 무거우므로 라우트 단위 테스트는 monkeypatch 로 update_set_sources 를 페이크로 바꿔 "라우트가 서비스를 부르고 JSON 그대로 반환"만 검증.

- [ ] **Step 1: 실패 테스트 작성** (test_sets_api_routes.py 에 추가)

```python
def test_recrawl_sources_route(monkeypatch, client):
    import lemouton.sets.source_update_service as srv
    monkeypatch.setattr(srv, "update_set_sources",
                        lambda s, *, set_id, **kw: {"ok": True, "models": [], "totals": {"total": 0, "ok": 0, "error": 0, "no_crawler": 0}, "per_source": {}, "need_extension": False})
    resp = client.post("/api/sets/1/recrawl-sources")
    assert resp.status_code == 200
    assert resp.get_json()["ok"] is True
```

(client fixture·import 스타일은 test_sets_api_routes.py 기존 것을 그대로 사용. 없으면 기존 collect 라우트 테스트를 복제해 맞춘다.)

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest tests/sets/test_sets_api_routes.py::test_recrawl_sources_route -v`
Expected: FAIL (404 — 라우트 미존재)

- [ ] **Step 3: 라우트 구현** (collect_set_route 함수 바로 아래 추가)

```python
@bp.post("/sets/<int:set_id>/recrawl-sources")
def recrawl_sources_route(set_id):
    """[작업3] 구성에 연동된 옵션들의 소싱처 URL을 모델 단위로 재크롤.
    HTTP 소싱처는 서버 즉시 크롤, 무신사·롯데온은 need_extension(확장 크롤 안내)."""
    from lemouton.sets import source_update_service as srv
    s = SessionLocal()
    try:
        r = srv.update_set_sources(s, set_id=set_id)
        if not r.get("ok"):
            return _err(r.get("error") or "소싱처 업데이트 실패", 404)
        s.commit()
        return jsonify(r)
    finally:
        s.close()
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest tests/sets/test_sets_api_routes.py -v`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add 프로그램/_시스템/webapp/routes/sets_api.py 프로그램/_시스템/tests/sets/test_sets_api_routes.py
git commit -m "feat(sets): POST /sets/<id>/recrawl-sources 라우트(소싱처 업데이트)"
```

---

### Task 3: 대시보드 `📦 소싱처 업데이트` 버튼 배선

**Files:**
- Modify: `프로그램/_시스템/webapp/templates/sets/dashboard.html`
  - 이벤트 위임의 `.sd-upd-src` 분기(현재 `alert('소싱처 업데이트는 곧 추가됩니다...')`)
  - `refreshSet` 옆에 `recrawlSources(btn)` 함수 추가

JS 변경이므로 `node --check`로 문법 검증.

- [ ] **Step 1: recrawlSources 함수 추가** (refreshSet 함수 아래)

```javascript
  function recrawlSources(btn){
    var sid=btn.getAttribute('data-sid'); var orig=btn.textContent;
    btn.disabled=true; btn.textContent='크롤 중…';
    fetch('/api/sets/'+sid+'/recrawl-sources',{method:'POST',headers:{'Accept':'application/json'}})
      .then(function(r){return r.json();})
      .then(function(d){
        btn.disabled=false; btn.textContent=orig;
        if(!(d&&d.ok)){ alert('소싱처 업데이트 실패: '+(d&&d.error||'')); return; }
        var t=d.totals||{};
        var msg='소싱처 재크롤 완료 · 성공 '+(t.ok||0)+' / 실패 '+(t.error||0)+' / 총 '+(t.total||0);
        if(d.need_extension){ msg+='\n\n무신사·롯데온 등 일부 소싱처는 로그인 브라우저(크롬 확장)로 크롤해야 합니다. 확장에서 전체 크롤을 한 번 실행해 주세요.'; }
        alert(msg);
        load();
      })
      .catch(function(){ btn.disabled=false; btn.textContent=orig; alert('소싱처 업데이트 호출 실패'); });
  }
```

- [ ] **Step 2: 이벤트 분기 교체**

기존:
```javascript
    var upSrc=e.target.closest('.sd-upd-src');
    if(upSrc){ e.stopPropagation(); alert('소싱처 업데이트는 곧 추가됩니다 (연동된 옵션만 재크롤).'); return; }
```
교체:
```javascript
    var upSrc=e.target.closest('.sd-upd-src');
    if(upSrc){ e.stopPropagation(); recrawlSources(upSrc); return; }
```

- [ ] **Step 3: JS 문법 검증**

`<script>` 블록 추출 → `node --check`. Expected: 통과.

- [ ] **Step 4: 커밋**

```bash
git add 프로그램/_시스템/webapp/templates/sets/dashboard.html
git commit -m "feat(sets): 대시보드 소싱처 업데이트 버튼 배선(recrawl-sources)"
```

---

## Self-Review

- **스펙 커버리지:** (3) 소싱처 업데이트 백엔드 = SetProduct(canonical_sku 연동) 소싱 URL 타깃 재크롤 + 다품 합집합 → Task 1(서비스·모델 합집합·dedup) + Task 2(라우트) + Task 3(버튼). ✅
- **플레이스홀더 스캔:** 모든 코드 블록 실코드. ✅
- **타입 일관성:** `update_set_sources(session, *, set_id, crawlers, crawl_fn)` / 반환 키(ok·models·totals·per_source·need_extension) Task 1↔2↔3 일치. crawl_fn 시그니처 `(session, *, model_code, crawlers, progress_cb)` = 실제 crawl_bundle_registered_urls 와 호환(progress_cb 옵션). ✅
- **주의:** crawl_bundle_registered_urls 는 하드리셋·finalize(crawl_blocked) 부수효과 포함 — 세트 재크롤이 곧 '전체 크롤'과 동일 경로라 매트릭스 가격/재고 갱신됨. 데이터 무결성(폴백 금지) 정책 유지.
