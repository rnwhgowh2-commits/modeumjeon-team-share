# 판매처 마켓연동 — 기존상품 연결 (P0 백엔드) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 모음전 상품에 마켓 상품번호를 붙여넣으면, 그 상품의 옵션을 API로 조회해 색상·사이즈로 자동 매칭하고 `MarketRegistration`에 (상품ID·옵션ID)를 저장한다 — 마켓에 **쓰지 않고** "연결"만.

**Architecture:** 순수 매칭 코어(`linker.py`, DB·네트워크 무관) → DB 서비스(`link_service.py`, 옵션 추출·매칭·upsert) → 마켓 조회 어댑터(`market_fetch.py`, smartstore 우선) → 얇은 Flask 라우트. 매칭은 기존 `mapping.matcher.normalize`(영한 색상·단위·공백 정규화)를 재사용. 검증은 DB에 이미 저장된 스마트스토어 상품ID로 **읽기 전용** 실호출(P0-2).

**Tech Stack:** Python 3 · SQLAlchemy · Flask · pytest · 기존 `shared/platforms/smartstore` (OAuth·옵션조회).

**Scope:** 본 플랜 = 스펙 `2026-06-26-판매처-마켓연동-기존상품연결-design.md`의 **P0(입력 0개·즉시 가능) 중 백엔드 + 읽기검증**. 매트릭스 "판매처 컬럼" UI(P0-3)와 쿠팡 키 입력(P1), 라이브 재고 push(P2)는 후속 플랜으로 분리. 쿠팡 조회 어댑터는 인터페이스만 열어두고 미구현(키 부재).

**작업 위치:** 모든 명령은 `cd "C:\dev\모음전 프로젝트\프로그램\_시스템"` 기준. pytest rootdir = `프로그램/_시스템` (여기서 `lemouton`·`shared` import 가능).

---

## File Structure

| 파일 | 책임 | 신규/수정 |
|---|---|---|
| `lemouton/uploader/linker.py` | 순수 매칭 코어 (`MarketOption`, `LinkRow`, `match_market_options_to_skus`) | 신규 |
| `lemouton/uploader/market_fetch.py` | 마켓 상품번호 → 공통 `MarketOption` 목록 (smartstore 어댑터) | 신규 |
| `lemouton/uploader/link_service.py` | 모음전 옵션 추출 → 매칭 → `MarketRegistration` upsert | 신규 |
| `webapp/routes/api.py` | 얇은 라우트 `POST /api/bundles/<code>/link/<market>` | 수정 |
| `tests/uploader/test_linker.py` | 매칭 코어 단위 테스트 (오프라인) | 신규 |
| `tests/uploader/test_link_service.py` | 서비스 통합 테스트 (in-memory SQLite) | 신규 |
| `scripts/verify_smartstore_read.py` | P0-2 읽기경로 실검증 스크립트 (쓰기 없음) | 신규 |

---

## Task 1: 매칭 코어 `linker.py`

**Files:**
- Create: `lemouton/uploader/linker.py`
- Test: `tests/uploader/test_linker.py`

- [ ] **Step 1: 실패하는 테스트 작성**

Create `tests/uploader/test_linker.py`:

```python
"""linker 매칭 코어 — 오프라인 단위 테스트 (DB·네트워크 불필요)."""
from lemouton.uploader.linker import MarketOption, match_market_options_to_skus


def _bundle():
    return [
        {"canonical_sku": "AF-블랙-260", "color_code": "블랙", "color_display": "블랙",
         "size_code": "260", "size_display": "260"},
        {"canonical_sku": "AF-블루-270", "color_code": "블루", "color_display": "블루",
         "size_code": "270", "size_display": "270"},
    ]


def test_exact_match_by_display():
    rows = match_market_options_to_skus(
        _bundle(), [MarketOption(option_id="opt1", color="블랙", size="260")])
    assert rows[0].status == "matched"
    assert rows[0].canonical_sku == "AF-블랙-260"
    assert rows[0].market_option_id == "opt1"


def test_english_korean_color_match():
    # 마켓이 'navy' 로 표기 → normalize 가 '블루' 로 매핑
    rows = match_market_options_to_skus(
        _bundle(), [MarketOption(option_id="o2", color="navy", size="270")])
    assert rows[0].status == "matched"
    assert rows[0].canonical_sku == "AF-블루-270"


def test_size_unit_normalized():
    # '270mm' 의 단위 mm 제거 후 '270' 매칭
    rows = match_market_options_to_skus(
        _bundle(), [MarketOption(option_id="o3", color="블루", size="270mm")])
    assert rows[0].status == "matched"
    assert rows[0].canonical_sku == "AF-블루-270"


def test_unmatched_when_no_pair():
    rows = match_market_options_to_skus(
        _bundle(), [MarketOption(option_id="o4", color="레드", size="999")])
    assert rows[0].status == "unmatched"
    assert rows[0].canonical_sku is None


def test_ambiguous_when_two_bundle_share_color_size():
    dupe = _bundle() + [
        {"canonical_sku": "AF-블랙-260-DUP", "color_code": "블랙", "color_display": "블랙",
         "size_code": "260", "size_display": "260"}]
    rows = match_market_options_to_skus(
        dupe, [MarketOption(option_id="o5", color="블랙", size="260")])
    assert rows[0].status == "ambiguous"
    assert rows[0].canonical_sku is None
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd "C:\dev\모음전 프로젝트\프로그램\_시스템" && python -m pytest tests/uploader/test_linker.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'lemouton.uploader.linker'`

- [ ] **Step 3: 매칭 코어 구현**

Create `lemouton/uploader/linker.py`:

```python
"""[연결] 마켓 옵션 ↔ 모음전 옵션(canonical_sku) 매칭 코어.

순수 함수: DB·네트워크 없음. 라우트가 추출한 모음전 옵션 + 마켓에서 가져온
옵션 목록을 받아 색상·사이즈 정규화 일치로 canonical_sku 를 매핑한다.
정규화는 mapping.matcher.normalize 재사용(영한 색상·단위·공백 처리).
"""
from __future__ import annotations

from dataclasses import dataclass
from typing import Optional

from lemouton.mapping.matcher import normalize


@dataclass
class MarketOption:
    """마켓에서 가져온 옵션 1개 (소싱처 무관 공통형)."""
    option_id: str
    color: Optional[str]
    size: Optional[str]
    stock: int = 0
    price: int = 0


@dataclass
class LinkRow:
    """매칭 결과 1행."""
    market_option_id: str
    market_color: Optional[str]
    market_size: Optional[str]
    canonical_sku: Optional[str]
    status: str  # 'matched' | 'unmatched' | 'ambiguous'


def _color_keys(opt: dict) -> set[str]:
    return {normalize(opt.get("color_display") or ""),
            normalize(opt.get("color_code") or "")} - {""}


def _size_keys(opt: dict) -> set[str]:
    return {normalize(opt.get("size_display") or ""),
            normalize(opt.get("size_code") or "")} - {""}


def match_market_options_to_skus(
    bundle_options: list[dict],
    market_options: list[MarketOption],
) -> list[LinkRow]:
    """마켓 옵션 각각을 모음전 옵션(canonical_sku)에 매칭.

    bundle_options: [{"canonical_sku","color_code","color_display",
                      "size_code","size_display"}, ...]
    market_options: [MarketOption(...), ...]

    매칭: 색상·사이즈 정규화가 모두 일치하는 모음전 옵션
          1개 → matched / 0개 → unmatched / 2개↑ → ambiguous(연결 보류).
    """
    rows: list[LinkRow] = []
    for mo in market_options:
        mc = normalize(mo.color or "")
        ms = normalize(mo.size or "")
        hits = [
            b for b in bundle_options
            if mc and ms and mc in _color_keys(b) and ms in _size_keys(b)
        ]
        if len(hits) == 1:
            rows.append(LinkRow(mo.option_id, mo.color, mo.size,
                                hits[0]["canonical_sku"], "matched"))
        elif not hits:
            rows.append(LinkRow(mo.option_id, mo.color, mo.size, None, "unmatched"))
        else:
            rows.append(LinkRow(mo.option_id, mo.color, mo.size, None, "ambiguous"))
    return rows
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd "C:\dev\모음전 프로젝트\프로그램\_시스템" && python -m pytest tests/uploader/test_linker.py -v`
Expected: PASS (5 passed)

- [ ] **Step 5: 커밋**

```bash
cd "C:\dev\모음전 프로젝트"
git add "프로그램/_시스템/lemouton/uploader/linker.py" "프로그램/_시스템/tests/uploader/test_linker.py"
git commit -m "feat(uploader): 판매처 연결 매칭 코어(linker) — 색상·사이즈 정규화 매핑

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: DB 서비스 `link_service.py`

모음전 옵션을 DB에서 추출하고, 주입된 fetcher로 마켓 옵션을 가져와 매칭하고, matched 만 `MarketRegistration`에 upsert 한다. fetcher 를 인자로 받아 네트워크 없이 테스트한다.

**Files:**
- Create: `lemouton/uploader/link_service.py`
- Test: `tests/uploader/test_link_service.py`

- [ ] **Step 1: 실패하는 테스트 작성**

Create `tests/uploader/test_link_service.py`:

```python
"""link_service — in-memory SQLite 통합 테스트 (가짜 fetcher 주입, 네트워크 없음)."""
import pytest
from sqlalchemy import create_engine
from sqlalchemy.orm import Session

from shared.db import Base

# create_all 이 FK 타겟까지 찾도록 전체 모델 등록 (app.py 와 동일)
for _m in (
    "lemouton.sourcing.models", "lemouton.sourcing.models_pricing",
    "lemouton.sourcing.models_v2", "lemouton.pricing.settings",
    "lemouton.uploader.models", "lemouton.templates.models",
    "lemouton.inventory.models", "lemouton.sources.models",
    "lemouton.multitenancy.models", "lemouton.audit.models",
    "lemouton.mapping.models",
):
    try:
        __import__(_m)
    except ImportError:
        pass

import lemouton.sourcing.models as M
from lemouton.uploader.linker import MarketOption
from lemouton.uploader.link_service import link_bundle_market
from lemouton.uploader.models import MarketRegistration


@pytest.fixture
def db():
    eng = create_engine("sqlite://")
    Base.metadata.create_all(eng)
    s = Session(eng)
    s.add(M.Model(model_code="AF", model_name_raw="에어포스"))
    s.add(M.Option(canonical_sku="AF-블랙-260", model_code="AF",
                   color_code="블랙", color_display="블랙",
                   size_code="260", size_display="260"))
    s.add(M.Option(canonical_sku="AF-블루-270", model_code="AF",
                   color_code="블루", color_display="블루",
                   size_code="270", size_display="270"))
    s.commit()
    yield s
    s.close()


def _fake_fetcher_ok(market, product_id):
    from lemouton.uploader.market_fetch import FetchResult
    return FetchResult(
        success=True, product_name="에어포스 상품", error=None,
        options=[
            MarketOption(option_id="11", color="블랙", size="260", stock=5),
            MarketOption(option_id="22", color="navy", size="270mm", stock=0),
            MarketOption(option_id="33", color="레드", size="999", stock=3),  # 미매칭
        ])


def test_link_persists_matched_only(db):
    result = link_bundle_market(
        db, model_code="AF", market="smartstore",
        market_product_id="555", fetcher=_fake_fetcher_ok)
    assert result["ok"] is True
    assert result["linked"] == 2
    assert result["unmatched"] == 1
    regs = {r.canonical_sku: r for r in db.query(MarketRegistration).all()}
    assert set(regs) == {"AF-블랙-260", "AF-블루-270"}
    assert regs["AF-블랙-260"].market_option_id == "11"
    assert regs["AF-블랙-260"].market_product_id == "555"
    assert regs["AF-블랙-260"].status == "linked"
    assert regs["AF-블루-270"].market_option_id == "22"


def test_link_fetch_failure_returns_error(db):
    def _bad(market, product_id):
        from lemouton.uploader.market_fetch import FetchResult
        return FetchResult(success=False, product_name=None, options=[],
                           error="상품을 찾을 수 없어요")
    result = link_bundle_market(
        db, model_code="AF", market="smartstore",
        market_product_id="999", fetcher=_bad)
    assert result["ok"] is False
    assert result["linked"] == 0
    assert db.query(MarketRegistration).count() == 0
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd "C:\dev\모음전 프로젝트\프로그램\_시스템" && python -m pytest tests/uploader/test_link_service.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'lemouton.uploader.market_fetch'` (또는 `link_service`)

- [ ] **Step 3: market_fetch.py 의 FetchResult 골격 먼저 작성**

Create `lemouton/uploader/market_fetch.py` (Task 3 에서 smartstore 본체 추가; 지금은 타입+분기만):

```python
"""[연결] 마켓 상품번호 → 공통 MarketOption 목록 (소싱처별 어댑터).

smartstore 는 Task 3 에서 실 API 연결. 본 파일의 FetchResult 는 link_service·
테스트가 공유하는 공통 반환형이다.
"""
from __future__ import annotations

from dataclasses import dataclass
from typing import Optional

from .linker import MarketOption


@dataclass
class FetchResult:
    success: bool
    product_name: Optional[str]
    options: list[MarketOption]
    error: Optional[str] = None


def fetch_market_options(market: str, product_id: str) -> FetchResult:
    """마켓 상품번호로 옵션 목록 조회. (smartstore 본체는 Task 3)"""
    if market == "smartstore":
        return _fetch_smartstore(product_id)
    return FetchResult(False, None, [], f"아직 지원하지 않는 마켓: {market}")


def _fetch_smartstore(product_id: str) -> FetchResult:
    return FetchResult(False, None, [], "smartstore fetch 미구현 (Task 3)")
```

- [ ] **Step 4: link_service.py 구현**

Create `lemouton/uploader/link_service.py`:

```python
"""[연결] 모음전 옵션 추출 → 마켓 옵션 매칭 → MarketRegistration upsert."""
from __future__ import annotations

from sqlalchemy.orm import Session

from lemouton.sourcing.models import Option
from lemouton.uploader.linker import match_market_options_to_skus
from lemouton.uploader.market_fetch import fetch_market_options
from lemouton.uploader.repository import upsert_registration


def link_bundle_market(
    session: Session,
    *,
    model_code: str,
    market: str,
    market_product_id: str,
    fetcher=fetch_market_options,
) -> dict:
    """모음전 상품(model_code)을 마켓 상품(product_id)에 연결.

    fetcher: (market, product_id) -> FetchResult. 테스트는 가짜 주입.
    matched 옵션만 MarketRegistration 에 status='linked' 로 저장. 마켓에 쓰지 않음.
    """
    options = session.query(Option).filter_by(model_code=model_code).all()
    bundle_options = [
        {
            "canonical_sku": o.canonical_sku,
            "color_code": o.color_code,
            "color_display": o.color_display,
            "size_code": o.size_code,
            "size_display": o.size_display,
        }
        for o in options
    ]

    fr = fetcher(market, market_product_id)
    if not fr.success:
        return {"ok": False, "error": fr.error, "product_name": None,
                "linked": 0, "unmatched": 0, "ambiguous": 0, "rows": []}

    rows = match_market_options_to_skus(bundle_options, fr.options)

    linked = unmatched = ambiguous = 0
    for r in rows:
        if r.status == "matched":
            upsert_registration(
                session,
                canonical_sku=r.canonical_sku, market=market,
                market_product_id=str(market_product_id),
                market_option_id=r.market_option_id,
                status="linked",
            )
            linked += 1
        elif r.status == "ambiguous":
            ambiguous += 1
        else:
            unmatched += 1
    session.commit()

    return {
        "ok": True, "error": None, "product_name": fr.product_name,
        "linked": linked, "unmatched": unmatched, "ambiguous": ambiguous,
        "rows": [r.__dict__ for r in rows],
    }
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `cd "C:\dev\모음전 프로젝트\프로그램\_시스템" && python -m pytest tests/uploader/test_link_service.py -v`
Expected: PASS (2 passed)

- [ ] **Step 6: 커밋**

```bash
cd "C:\dev\모음전 프로젝트"
git add "프로그램/_시스템/lemouton/uploader/market_fetch.py" "프로그램/_시스템/lemouton/uploader/link_service.py" "프로그램/_시스템/tests/uploader/test_link_service.py"
git commit -m "feat(uploader): 연결 서비스 — 옵션추출·매칭·MarketRegistration upsert

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: smartstore 조회 어댑터 + 연결 라우트

`market_fetch._fetch_smartstore` 에 기존 `fetch_product_options` 를 배선하고, 얇은 Flask 라우트를 추가한다.

**Files:**
- Modify: `lemouton/uploader/market_fetch.py` (`_fetch_smartstore` 본체)
- Modify: `webapp/routes/api.py` (라우트 추가)
- Test: `tests/uploader/test_link_service.py` (smartstore 매핑 단위 테스트 1개 추가)

- [ ] **Step 1: `_fetch_smartstore` 매핑 테스트 작성 (네트워크 없이 클라이언트 주입)**

`fetch_product_options(origin_product_no, client=...)` 는 client 를 주입할 수 있다. 가짜 client 의 `.request()` 가 네이버 v2 응답 형태 dict 를 반환하게 해서 매핑만 검증한다. `tests/uploader/test_link_service.py` 끝에 추가:

```python
def test_fetch_smartstore_maps_options(monkeypatch):
    from shared.platforms.smartstore import get_options as go
    from lemouton.uploader import market_fetch

    fake_payload = {
        "originProduct": {
            "name": "에어포스 1",
            "salePrice": 115900,
            "detailAttribute": {"optionInfo": {"optionCombinations": [
                {"id": 111, "optionName1": "블랙", "optionName2": "260", "stockQuantity": 4},
                {"id": 222, "optionName1": "블루", "optionName2": "270", "stockQuantity": 0},
            ]}},
        }
    }

    class FakeClient:
        def request(self, method, path, query="", body=None):
            return fake_payload

    # fetch_product_options 가 새 client 를 만들지 않도록 주입형으로 호출되게 패치
    orig = go.fetch_product_options
    monkeypatch.setattr(
        market_fetch, "_fetch_smartstore",
        lambda pid: _wrap(orig(int(pid), client=FakeClient())))

    def _wrap(r):
        from lemouton.uploader.market_fetch import FetchResult
        from lemouton.uploader.linker import MarketOption
        return FetchResult(
            success=r.success, product_name=r.product_name, error=r.error,
            options=[MarketOption(option_id=str(o.option_id), color=o.name1,
                                  size=o.name2, stock=o.stock, price=o.add_price)
                     for o in r.options])

    fr = market_fetch.fetch_market_options("smartstore", "999")
    assert fr.success is True
    assert len(fr.options) == 2
    assert fr.options[0].option_id == "111"
    assert fr.options[0].color == "블랙"
    assert fr.options[0].size == "260"
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd "C:\dev\모음전 프로젝트\프로그램\_시스템" && python -m pytest tests/uploader/test_link_service.py::test_fetch_smartstore_maps_options -v`
Expected: FAIL (현재 `_fetch_smartstore` 가 "미구현" 반환 → monkeypatch 가 대체하므로 통과처럼 보일 수 있음. 먼저 monkeypatch 없이 `fetch_market_options("smartstore","999")` 가 미구현임을 Step 3 전 수동 확인)

> 주: 이 테스트는 `_fetch_smartstore` 의 **매핑 형태**를 고정하는 회귀 테스트다. Step 3 에서 본체를 동일 형태로 구현한다.

- [ ] **Step 3: `_fetch_smartstore` 본체 구현**

Modify `lemouton/uploader/market_fetch.py` — `_fetch_smartstore` 교체:

```python
def _fetch_smartstore(product_id: str) -> FetchResult:
    from shared.platforms.smartstore.get_options import fetch_product_options
    try:
        pid = int(product_id)
    except (TypeError, ValueError):
        return FetchResult(False, None, [], f"상품번호가 숫자가 아니에요: {product_id!r}")
    r = fetch_product_options(pid)
    if not r.success:
        return FetchResult(False, None, [], r.error or "옵션 조회 실패")
    opts = [
        MarketOption(option_id=str(o.option_id), color=o.name1, size=o.name2,
                     stock=o.stock, price=o.add_price)
        for o in r.options
    ]
    return FetchResult(True, r.product_name, opts)
```

- [ ] **Step 4: 라우트 추가**

`webapp/routes/api.py` 상단 import 에 `jsonify` 가 없으면 추가(`from flask import ..., jsonify`). 파일 끝(기존 `register_to_market` 라우트 근처)에 추가:

```python
@bp.post('/bundles/<code>/link/<market>')
def link_to_market(code: str, market: str):
    """기존 마켓 상품을 모음전 옵션에 연결 (쓰기 없음, 매핑만 저장).

    Body: {"market_product_id": "12345678"}
    """
    if market not in ('smartstore', 'coupang'):
        return _err('market은 smartstore/coupang 중 하나여야 해요.', 400)
    payload = request.get_json(silent=True) or {}
    product_id = str(payload.get('market_product_id') or '').strip()
    if not product_id:
        return _err('market_product_id 가 필요해요.', 400)

    from lemouton.uploader.link_service import link_bundle_market
    s = SessionLocal()
    try:
        result = link_bundle_market(
            s, model_code=code, market=market, market_product_id=product_id)
    finally:
        s.close()

    if not result.get('ok'):
        return _err(result.get('error') or '연결 실패', 502)
    return jsonify(result)
```

- [ ] **Step 5: 전체 uploader 테스트 통과 확인**

Run: `cd "C:\dev\모음전 프로젝트\프로그램\_시스템" && python -m pytest tests/uploader/ -v`
Expected: PASS (linker 5 + link_service 3 + 기존 preview 테스트 모두 통과)

- [ ] **Step 6: 커밋**

```bash
cd "C:\dev\모음전 프로젝트"
git add "프로그램/_시스템/lemouton/uploader/market_fetch.py" "프로그램/_시스템/webapp/routes/api.py" "프로그램/_시스템/tests/uploader/test_link_service.py"
git commit -m "feat(uploader): smartstore 조회 어댑터 + 연결 라우트 POST /api/bundles/<code>/link/<market>

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: P0-2 읽기검증 스크립트 (쓰기 없음, 라이브 읽기)

DB에 이미 저장된 스마트스토어 상품ID로 옵션을 **읽기만** 호출해 (1) API가 실제로 작동하는지, (2) 모음전 옵션과 매칭률이 얼마인지 보고한다. 마켓에 **아무것도 쓰지 않는다**.

**Files:**
- Create: `scripts/verify_smartstore_read.py`

- [ ] **Step 1: 스크립트 작성**

Create `scripts/verify_smartstore_read.py`:

```python
"""[P0-2] 스마트스토어 읽기경로 실검증 — 쓰기 없음, 옵션조회만.

목적:
  - 스마트스토어 API 가 실제로 응답하는지(기존 ok 36건이 라이브인지) 확인
  - 가져온 옵션을 모음전 옵션과 매칭해 매칭률 보고

사용 (cd 프로그램/_시스템):
  python -m scripts.verify_smartstore_read              # DB의 기존 상품ID 자동 선택
  python -m scripts.verify_smartstore_read 12345678     # 특정 상품번호 지정
  python -m scripts.verify_smartstore_read 12345678 AF  # 상품번호 + 모음전 model_code
"""
import sys

from shared.db import SessionLocal
from lemouton.sourcing.models import Option
from lemouton.uploader.models import MarketRegistration
from lemouton.uploader.market_fetch import fetch_market_options
from lemouton.uploader.linker import match_market_options_to_skus


def _pick_existing_product_id(s) -> str | None:
    row = (s.query(MarketRegistration)
           .filter(MarketRegistration.market == "smartstore",
                   MarketRegistration.market_product_id.isnot(None))
           .first())
    return row.market_product_id if row else None


def main(argv):
    product_id = argv[1] if len(argv) > 1 else None
    model_code = argv[2] if len(argv) > 2 else None

    s = SessionLocal()
    try:
        if not product_id:
            product_id = _pick_existing_product_id(s)
            if not product_id:
                print("DB에 저장된 스마트스토어 상품ID가 없어요. 상품번호를 인자로 주세요.")
                return 2
            print(f"[자동선택] DB의 기존 상품ID: {product_id}")

        print(f"[읽기호출] smartstore 상품 {product_id} 옵션 조회 중...")
        fr = fetch_market_options("smartstore", product_id)
        if not fr.success:
            print(f"❌ 조회 실패: {fr.error}")
            return 1

        print(f"✅ 응답 OK — 상품명: {fr.product_name} / 옵션 {len(fr.options)}개")
        for o in fr.options[:30]:
            print(f"   - opt {o.option_id}: 색상={o.color} 사이즈={o.size} 재고={o.stock}")

        if model_code:
            opts = s.query(Option).filter_by(model_code=model_code).all()
            bundle = [{"canonical_sku": x.canonical_sku, "color_code": x.color_code,
                       "color_display": x.color_display, "size_code": x.size_code,
                       "size_display": x.size_display} for x in opts]
            rows = match_market_options_to_skus(bundle, fr.options)
            matched = sum(1 for r in rows if r.status == "matched")
            print(f"\n[매칭] model_code={model_code} 옵션 {len(bundle)}개 기준")
            print(f"   매칭 {matched} / 전체 {len(rows)} "
                  f"(unmatched {sum(1 for r in rows if r.status=='unmatched')}, "
                  f"ambiguous {sum(1 for r in rows if r.status=='ambiguous')})")
        return 0
    finally:
        s.close()


if __name__ == "__main__":
    raise SystemExit(main(sys.argv))
```

- [ ] **Step 2: import/구문 오프라인 확인 (네트워크 없이)**

Run: `cd "C:\dev\모음전 프로젝트\프로그램\_시스템" && python -c "import scripts.verify_smartstore_read as v; print('import ok', hasattr(v, 'main'))"`
Expected: `import ok True`

- [ ] **Step 3: 라이브 읽기 실행 (실 API 호출, 쓰기 없음)**

> ⚠️ `.env` 의 `SMARTSTORE_MAIN_CLIENT_ID/SECRET` 로 실제 네이버 커머스 API 를 호출한다. **읽기(GET)만** 한다.

Run: `cd "C:\dev\모음전 프로젝트\프로그램\_시스템" && python -m scripts.verify_smartstore_read`
Expected (성공 시): `✅ 응답 OK — 상품명: ... / 옵션 N개` + 옵션 목록 출력 → **스마트스토어 API 라이브 작동 확정**.
Expected (실패 시): `❌ 조회 실패: ...` → 에러 메시지(401/404 등)를 기록해 사용자에게 보고(키 만료·권한·상품ID stale 등 원인 구분).

- [ ] **Step 4: 커밋**

```bash
cd "C:\dev\모음전 프로젝트"
git add "프로그램/_시스템/scripts/verify_smartstore_read.py"
git commit -m "feat(uploader): P0-2 스마트스토어 읽기검증 스크립트(쓰기 없음)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

- [ ] **Step 5: 결과 보고**

Step 3 출력(성공/실패, 옵션 수, 매칭률)을 사용자에게 7섹션 형식으로 보고. 이 결과가 스펙 §2의 "스마트스토어 ok 36건이 라이브인지" 미해결 질문의 답이 된다.

---

## 완료 후 — 다음 단계 (본 플랜 범위 밖)

- **P0-3 (후속 플랜)**: 매트릭스 "판매처 컬럼" 읽기전용 UI 골격 — `MarketRegistration` 을 읽어 옵션별 연결/미연결 표시. 라이브 브라우저 검증 필요(`/ui-verify`).
- **P1**: 쿠팡 API 키 입력 → `market_fetch` 에 `_fetch_coupang` 추가(`get_product`+`extract_vendor_items`) → 연결 라우트 그대로 재사용.
- **P2**: 재고 push (미리보기→승인→라이브 1건), 기존 `orchestrator.run_uploader` 배선.

---

## Self-Review

- **Spec 커버리지**: §7 P0-1(연결 엔진)=Task1·2·3, P0-2(읽기검증)=Task4. P0-3(UI)은 범위 분리 명시. §4 데이터흐름 [1 연결]=Task1·2·3, [2 미리보기]·[3 업데이트]는 P2(범위 밖). §8 폴백가 금지=matched 만 저장(unmatched 폴백 없음). ✅
- **Placeholder 스캔**: TBD/TODO 없음. 모든 코드 step 에 실제 코드·명령·기대출력 포함. ✅
- **타입 일관성**: `MarketOption`(option_id,color,size,stock,price)·`LinkRow`(market_option_id,market_color,market_size,canonical_sku,status)·`FetchResult`(success,product_name,options,error)·`link_bundle_market(...)→dict{ok,error,product_name,linked,unmatched,ambiguous,rows}` 전 태스크 동일 사용. `upsert_registration(session,*,canonical_sku,market,**kwargs)` 실제 시그니처와 일치. ✅
