# 상품 가공 탭 ①「정책 생성」 화면 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 「마켓별 정책」을 「정책 생성」으로 바꾸고, 마켓 6곳을 각각 채워야 하는 83칸 문제를
「마켓 공통」 한 벌 + 넣기/불러오기로 없앤다. 정책 복사·브랜드 분류·마켓 켜고 끄기를 붙인다.

**Architecture:** 값 저장소(`market_policy_values`)에 `market='common'` 한 줄을 더 쓰는 방식.
스키마를 갈아엎지 않는다. 「공통에서 받았나 / 직접 고쳤나」는 값 비교로 판정하지 않고
`from_common_at` 시각으로 판정한다 — 값 비교는 공통이 나중에 바뀌면 틀린 답을 낸다.

**Tech Stack:** Python 3 · Flask · SQLAlchemy · pytest · Jinja2 (서버 렌더) · 바닐라 JS

**설계서:** `docs/superpowers/specs/2026-07-31-상품가공탭-1단계-design.md`

---

## File Structure

| 파일 | 책임 |
|---|---|
| `lemouton/policy/fields.py` (수정) | 마켓 목록 + 「마켓 공통」 키. 항목표 단일 원천 |
| `lemouton/policy/models.py` (수정) | `brand` · `enabled_markets` · `from_common_at` 칸 추가 |
| `lemouton/policy/service.py` (수정) | 만들기·저장은 그대로. 여기엔 손대지 않는다 |
| `lemouton/policy/common_sync.py` (신규) | **넣기·불러오기·출처 판정**만 담당 |
| `lemouton/policy/copy.py` (신규) | **정책 복사**만 담당 |
| `shared/db.py` (수정) | 새 칸 3개를 가벼운 마이그레이션에 등록 |
| `webapp/routes/policy.py` (수정) | 새 API 5개 + 목록·상세 화면 인자 |
| `webapp/templates/policy/index.html` (수정) | 이름 변경 · 브랜드 단추 · 복사 · 마켓 배지 |
| `webapp/templates/policy/detail.html` (수정) | 「마켓 공통」 탭 · 넣기 줄 · 불러오기 줄 |
| `tests/policy/test_common_sync.py` (신규) | 넣기·불러오기·출처 판정 |
| `tests/policy/test_policy_copy.py` (신규) | 복사 |
| `tests/policy/test_policy_meta.py` (신규) | 브랜드 · 마켓 켜고 끄기 |

**왜 `common_sync.py`·`copy.py` 를 따로 두나** — `service.py` 는 이미 만들기·저장·적용·현황을
다 갖고 있다. 여기에 넣기/불러오기/복사까지 넣으면 한 파일이 여섯 가지 일을 한다.

---

## Task 1: 「마켓 공통」 저장 자리 만들기

**Files:**
- Modify: `프로그램/_시스템/lemouton/policy/fields.py`
- Test: `프로그램/_시스템/tests/policy/test_common_sync.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`프로그램/_시스템/tests/policy/test_common_sync.py` 를 새로 만든다.

```python
# -*- coding: utf-8 -*-
"""「마켓 공통」 — 넣기 · 불러오기 · 값 출처 판정.

🔴 이 파일이 지키는 것:
  ① 공통은 **한 번 넣으면 끝**이다. 그 뒤 마켓에서 고치면 공통이 다시 덮지 않는다.
  ② 「공통에서 받았나 / 직접 고쳤나」는 **값 비교로 판정하지 않는다** —
     공통이 나중에 바뀌면 값 비교는 「직접 고침」이라는 틀린 답을 낸다.
"""
import pytest
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker

from shared.db import Base
from lemouton.policy import models as PM     # noqa: F401 — 테이블 등록
from lemouton.policy.fields import COMMON_KEY, MARKET_KEYS, item_keys_for
from lemouton.policy.service import create_policy, save_item, values_for


@pytest.fixture()
def db():
    eng = create_engine('sqlite:///:memory:')
    Base.metadata.create_all(eng)
    s = sessionmaker(bind=eng)()
    yield s
    s.close()


def test_공통은_마켓_목록에_섞이지_않는다():
    """「마켓 공통」은 진짜 마켓이 아니다 — 마켓 목록에 들어가면 전송 대상이 된다."""
    assert COMMON_KEY == 'common'
    assert COMMON_KEY not in MARKET_KEYS


def test_공통에도_항목을_저장할_수_있다(db):
    p = create_policy(db, name='르무통 기본')
    save_item(db, policy=p, market=COMMON_KEY, item_key='price',
              config={'margin_rate': 25})
    assert values_for(db, p.id, COMMON_KEY) == {'price': {'margin_rate': 25}}


def test_공통_항목표는_마켓_전용을_빼고_준다(db):
    """공통에는 「쿠팡만 있는 항목」을 둘 수 없다 — 어느 마켓에 넣을지 정해지지 않는다."""
    common_keys = item_keys_for(COMMON_KEY)
    coupang_only = set(item_keys_for('coupang')) - set(item_keys_for('smartstore'))
    assert coupang_only, '쿠팡 전용 항목이 하나도 없다면 이 테스트는 의미가 없다'
    assert not (coupang_only & set(common_keys))
```

- [ ] **Step 2: 실패하는지 돌려본다**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy/test_common_sync.py -v`
Expected: FAIL — `ImportError: cannot import name 'COMMON_KEY'`

- [ ] **Step 3: 최소한만 구현한다**

`lemouton/policy/fields.py` 의 `MARKET_KEYS` 정의 바로 아래에 넣는다:

```python
MARKET_KEYS = [k for k, _ in MARKETS]
MARKET_LABEL = dict(MARKETS)

# ── 「마켓 공통」 ────────────────────────────────────────────────────────
#  진짜 마켓이 아니라 **값을 담아두는 자리**다. 여기 채워 두고 마켓으로 넣거나,
#  마켓 쪽에서 불러온다. MARKET_KEYS 에 넣지 않는다 — 넣으면 전송 대상이 된다.
COMMON_KEY = 'common'
COMMON_LABEL = '마켓 공통'
```

같은 파일의 `items_for()` 를 고친다. 지금은 모르는 마켓이면 마켓 전용 항목이 하나도 안 붙는데,
공통은 **그 규칙이 그대로 맞다**(마켓 전용 항목은 공통에 둘 수 없다). 다만 뜻을 분명히 적는다:

```python
def items_for(market: str) -> list[dict]:
    """그 마켓에 해당하는 항목만. 13항목 + 그 마켓 전용 예외.

    market=COMMON_KEY 면 **13항목만** 준다 — 「쿠팡만 있는 항목」을 공통에 두면
    어느 마켓으로 넣을지 정해지지 않는다.
    """
    out = base_items()
    if market == COMMON_KEY:
        return out
    out += [dict(it) for it in EXTRA_ITEMS
            if not it.get('only') or market in it['only']]
    return out
```

`save_item()` 이 공통을 받아들이게 `lemouton/policy/service.py` 를 고친다:

```python
from lemouton.policy.fields import (
    COMMON_KEY, MARKET_KEYS, PRICE_REQUIRED_ITEMS, all_item_keys, item_keys_for,
)
```

```python
    if market not in MARKET_KEYS and market != COMMON_KEY:
        raise PolicyError(f'모르는 마켓이에요: {market}')
```

- [ ] **Step 4: 통과하는지 돌려본다**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy/test_common_sync.py -v`
Expected: PASS (3 passed)

- [ ] **Step 5: 커밋**

```bash
git add "프로그램/_시스템/lemouton/policy/fields.py" "프로그램/_시스템/lemouton/policy/service.py" "프로그램/_시스템/tests/policy/test_common_sync.py"
git commit -m "feat(정책): 「마켓 공통」 저장 자리 — 마켓 목록에 섞지 않는다"
```

---

## Task 2: 「공통에서 받은 시각」 칸 추가

**Files:**
- Modify: `프로그램/_시스템/lemouton/policy/models.py`
- Modify: `프로그램/_시스템/shared/db.py` (migrations 리스트)
- Test: `프로그램/_시스템/tests/policy/test_common_sync.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/policy/test_common_sync.py` 맨 아래에 붙인다:

```python
def test_받은_시각_칸이_있다(db):
    """값 비교로 출처를 판정하면 공통이 바뀔 때 틀린 답이 나온다 — 시각을 남긴다."""
    from lemouton.policy.models import MarketPolicyValue
    p = create_policy(db, name='르무통 기본')
    save_item(db, policy=p, market='smartstore', item_key='price',
              config={'margin_rate': 25})
    row = db.query(MarketPolicyValue).filter_by(
        policy_id=p.id, market='smartstore', field_key='price').one()
    assert row.from_common_at is None, '직접 저장한 값은 공통에서 온 게 아니다'
```

- [ ] **Step 2: 실패하는지 돌려본다**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy/test_common_sync.py::test_받은_시각_칸이_있다 -v`
Expected: FAIL — `AttributeError: 'MarketPolicyValue' object has no attribute 'from_common_at'`

- [ ] **Step 3: 최소한만 구현한다**

`lemouton/policy/models.py` 의 `MarketPolicyValue` 에 칸을 넣는다:

```python
    value = Column(Text)                       # 전부 문자열로 보관 — 화면 입력 그대로
    # 「마켓 공통」에서 받은 시각. 직접 저장하면 None 으로 돌아간다.
    #   🔴 값 비교로 「공통 따름」을 판정하면 안 된다 — 공통이 나중에 바뀌면
    #     받은 적 있는 마켓이 「직접 고침」으로 잘못 뜬다.
    from_common_at = Column(DateTime)
    updated_at = Column(DateTime, default=_utcnow, onupdate=_utcnow, nullable=False)
```

`shared/db.py` 의 `migrations` 리스트 맨 위에 넣는다 (기존 첫 줄 위):

```python
    migrations = [
        # [2026-07-31] 정책 값이 「마켓 공통」에서 온 것인지 표시 (NULL = 직접 저장)
        ("market_policy_values", "from_common_at", "TIMESTAMP"),
        # [2026-07-31] 디자인 모드 — 사람마다 따로. current=안전망(예전 디자인)
        ("users", "design_mode", "VARCHAR(16) DEFAULT 'current' NOT NULL"),
```

- [ ] **Step 4: 통과하는지 돌려본다**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy/test_common_sync.py -v`
Expected: PASS (4 passed)

- [ ] **Step 5: 커밋**

```bash
git add "프로그램/_시스템/lemouton/policy/models.py" "프로그램/_시스템/shared/db.py" "프로그램/_시스템/tests/policy/test_common_sync.py"
git commit -m "feat(정책): 「공통에서 받은 시각」 칸 — 값 비교로 출처를 판정하지 않는다"
```

---

## Task 3: 넣기 — 공통을 고른 마켓으로

**Files:**
- Create: `프로그램/_시스템/lemouton/policy/common_sync.py`
- Test: `프로그램/_시스템/tests/policy/test_common_sync.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/policy/test_common_sync.py` 맨 아래에 붙인다:

```python
def test_넣으면_고른_마켓에만_들어간다(db):
    from lemouton.policy.common_sync import push_to_markets
    p = create_policy(db, name='르무통 기본')
    save_item(db, policy=p, market=COMMON_KEY, item_key='price',
              config={'margin_rate': 25})

    n = push_to_markets(db, policy=p, markets=['smartstore', 'coupang'])

    assert n == 2
    assert values_for(db, p.id, 'smartstore') == {'price': {'margin_rate': 25}}
    assert values_for(db, p.id, 'coupang') == {'price': {'margin_rate': 25}}
    assert values_for(db, p.id, 'gmarket') == {}, '안 고른 마켓은 그대로여야 한다'


def test_넣은_뒤_마켓에서_고치면_공통이_다시_덮지_않는다(db):
    """사장님 확정 — 「한 번 넣으면 끝, 그 뒤 고치면 고친 대로」."""
    from lemouton.policy.common_sync import push_to_markets
    p = create_policy(db, name='르무통 기본')
    save_item(db, policy=p, market=COMMON_KEY, item_key='price',
              config={'margin_rate': 25})
    push_to_markets(db, policy=p, markets=['coupang'])

    save_item(db, policy=p, market='coupang', item_key='price',
              config={'margin_rate': 32})
    save_item(db, policy=p, market=COMMON_KEY, item_key='price',
              config={'margin_rate': 10})

    assert values_for(db, p.id, 'coupang') == {'price': {'margin_rate': 32}}


def test_항목을_골라_넣을_수_있다(db):
    from lemouton.policy.common_sync import push_to_markets
    p = create_policy(db, name='르무통 기본')
    save_item(db, policy=p, market=COMMON_KEY, item_key='price',
              config={'margin_rate': 25})
    save_item(db, policy=p, market=COMMON_KEY, item_key='name',
              config={'max_len': 100})

    push_to_markets(db, policy=p, markets=['coupang'], item_keys=['price'])

    assert values_for(db, p.id, 'coupang') == {'price': {'margin_rate': 25}}


def test_모르는_마켓에는_못_넣는다(db):
    from lemouton.policy.common_sync import push_to_markets
    from lemouton.policy.service import PolicyError
    p = create_policy(db, name='르무통 기본')
    with pytest.raises(PolicyError):
        push_to_markets(db, policy=p, markets=['gmarket', '없는마켓'])
```

- [ ] **Step 2: 실패하는지 돌려본다**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy/test_common_sync.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'lemouton.policy.common_sync'`

- [ ] **Step 3: 최소한만 구현한다**

`lemouton/policy/common_sync.py` 를 새로 만든다:

```python
# -*- coding: utf-8 -*-
"""「마켓 공통」 ↔ 마켓 — 넣기 · 불러오기 · 값 출처 판정.

사장님 확정 2026-07-31 —
  「마켓 공통에서 채우고, 적용할 마켓만 체크해서 적용. 이것은 1회성임.
   한번 설정하고 각 마켓에서 수정하면 수정한대로 적용되는 것.
   나중에 마켓 공통에서 다시 수정하고 반영하고 싶은 마켓 체크해서 하면 됨.」

🔴 **공통은 따라다니지 않는다.** 넣는 순간 값이 복사되고 끝이다. 그래서 공통 탭의
   값은 「마지막에 넣은 값」이지 지금 그 마켓의 값이 아니다 — 화면이 이걸 말해야 한다.
"""
from __future__ import annotations

import json
from datetime import datetime, timezone

from sqlalchemy import select

from lemouton.policy.fields import COMMON_KEY, MARKET_KEYS, item_keys_for
from lemouton.policy.models import MarketPolicyValue
from lemouton.policy.service import PolicyError, values_for


def _utcnow():
    return datetime.now(timezone.utc)


def _check_markets(markets: list[str]) -> list[str]:
    picked = [m for m in dict.fromkeys(markets or []) if m]
    if not picked:
        raise PolicyError('넣을 마켓을 하나도 고르지 않았어요.')
    unknown = [m for m in picked if m not in MARKET_KEYS]
    if unknown:
        raise PolicyError(f'모르는 마켓이에요: {", ".join(unknown)}')
    return picked


def _write(session, *, policy_id: int, market: str, item_key: str,
           config: dict, from_common: bool) -> None:
    """값 한 칸을 쓴다. 공통에서 온 것이면 시각을 남기고, 아니면 지운다."""
    row = session.scalar(select(MarketPolicyValue).where(
        MarketPolicyValue.policy_id == policy_id,
        MarketPolicyValue.market == market,
        MarketPolicyValue.field_key == item_key))
    body = json.dumps(config, ensure_ascii=False)
    stamp = _utcnow() if from_common else None
    if row is None:
        session.add(MarketPolicyValue(policy_id=policy_id, market=market,
                                      field_key=item_key, value=body,
                                      from_common_at=stamp))
    else:
        row.value = body
        row.from_common_at = stamp
    session.flush()


def push_to_markets(session, *, policy, markets: list[str],
                    item_keys: list[str] | None = None) -> int:
    """「마켓 공통」 값을 고른 마켓에 넣는다. 넣은 마켓 수를 돌려준다.

    🔴 **덮어쓴다.** 그 마켓이 따로 고쳐 둔 값이 있으면 사라진다 —
      화면이 넣기 전에 그 사실을 보여줘야 한다.
    """
    picked = _check_markets(markets)
    common = values_for(session, policy.id, COMMON_KEY)
    if item_keys is not None:
        want = set(item_keys)
        common = {k: v for k, v in common.items() if k in want}
    if not common:
        raise PolicyError('「마켓 공통」에 저장된 항목이 없어요 — 먼저 채워 주세요.')
    for mk in picked:
        allowed = set(item_keys_for(mk))
        for k, cfg in common.items():
            if k not in allowed:
                continue        # 그 마켓에 없는 항목은 건너뛴다(조용히 만들지 않는다)
            _write(session, policy_id=policy.id, market=mk, item_key=k,
                   config=cfg, from_common=True)
    session.flush()
    return len(picked)
```

`service.save_item()` 이 직접 저장할 때 시각을 지우게 고친다. `lemouton/policy/service.py` 의
`save_item` 에서 값을 쓰는 두 줄을 바꾼다:

```python
    body = json.dumps(config, ensure_ascii=False)
    if row is None:
        session.add(MarketPolicyValue(policy_id=policy.id, market=market,
                                      field_key=item_key, value=body))
    else:
        row.value = body
        # 화면에서 직접 저장한 값은 「공통에서 받은 값」이 아니다.
        row.from_common_at = None
    session.flush()
```

- [ ] **Step 4: 통과하는지 돌려본다**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy/test_common_sync.py -v`
Expected: PASS (8 passed)

- [ ] **Step 5: 커밋**

```bash
git add "프로그램/_시스템/lemouton/policy/common_sync.py" "프로그램/_시스템/lemouton/policy/service.py" "프로그램/_시스템/tests/policy/test_common_sync.py"
git commit -m "feat(정책): 「마켓 공통」 넣기 — 고른 마켓에만, 한 번으로 끝"
```

---

## Task 4: 불러오기 — 마켓이 공통을 당긴다

**Files:**
- Modify: `프로그램/_시스템/lemouton/policy/common_sync.py`
- Test: `프로그램/_시스템/tests/policy/test_common_sync.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/policy/test_common_sync.py` 맨 아래에 붙인다:

```python
def test_전체_불러오기(db):
    from lemouton.policy.common_sync import pull_from_common
    p = create_policy(db, name='르무통 기본')
    save_item(db, policy=p, market=COMMON_KEY, item_key='price',
              config={'margin_rate': 25})
    save_item(db, policy=p, market=COMMON_KEY, item_key='name',
              config={'max_len': 100})
    save_item(db, policy=p, market='coupang', item_key='price',
              config={'margin_rate': 32})

    n = pull_from_common(db, policy=p, market='coupang')

    assert n == 2
    assert values_for(db, p.id, 'coupang') == {
        'price': {'margin_rate': 25}, 'name': {'max_len': 100}}


def test_항목_하나만_불러오기(db):
    from lemouton.policy.common_sync import pull_from_common
    p = create_policy(db, name='르무통 기본')
    save_item(db, policy=p, market=COMMON_KEY, item_key='price',
              config={'margin_rate': 25})
    save_item(db, policy=p, market=COMMON_KEY, item_key='name',
              config={'max_len': 100})
    save_item(db, policy=p, market='coupang', item_key='name',
              config={'max_len': 50})

    n = pull_from_common(db, policy=p, market='coupang', item_keys=['price'])

    assert n == 1
    assert values_for(db, p.id, 'coupang') == {
        'price': {'margin_rate': 25}, 'name': {'max_len': 50}}


def test_공통이_비었으면_불러오기는_막는다(db):
    from lemouton.policy.common_sync import pull_from_common
    from lemouton.policy.service import PolicyError
    p = create_policy(db, name='르무통 기본')
    with pytest.raises(PolicyError):
        pull_from_common(db, policy=p, market='coupang')


def test_공통_자신은_불러올_수_없다(db):
    from lemouton.policy.common_sync import pull_from_common
    from lemouton.policy.service import PolicyError
    p = create_policy(db, name='르무통 기본')
    save_item(db, policy=p, market=COMMON_KEY, item_key='price',
              config={'margin_rate': 25})
    with pytest.raises(PolicyError):
        pull_from_common(db, policy=p, market=COMMON_KEY)
```

- [ ] **Step 2: 실패하는지 돌려본다**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy/test_common_sync.py -v`
Expected: FAIL — `ImportError: cannot import name 'pull_from_common'`

- [ ] **Step 3: 최소한만 구현한다**

`lemouton/policy/common_sync.py` 맨 아래에 붙인다:

```python
def pull_from_common(session, *, policy, market: str,
                     item_keys: list[str] | None = None) -> int:
    """그 마켓이 「마켓 공통」을 불러온다. 바뀐 항목 수를 돌려준다.

    넣기(push)와 방향만 다르고 결과는 같다 — 값이 복사되고 끝난다.
    """
    if market == COMMON_KEY:
        raise PolicyError('「마켓 공통」이 자기 자신을 불러올 수는 없어요.')
    if market not in MARKET_KEYS:
        raise PolicyError(f'모르는 마켓이에요: {market}')
    common = values_for(session, policy.id, COMMON_KEY)
    if item_keys is not None:
        want = set(item_keys)
        common = {k: v for k, v in common.items() if k in want}
    if not common:
        raise PolicyError('「마켓 공통」에 저장된 항목이 없어요 — 먼저 채워 주세요.')
    allowed = set(item_keys_for(market))
    n = 0
    for k, cfg in common.items():
        if k not in allowed:
            continue
        _write(session, policy_id=policy.id, market=market, item_key=k,
               config=cfg, from_common=True)
        n += 1
    session.flush()
    return n
```

- [ ] **Step 4: 통과하는지 돌려본다**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy/test_common_sync.py -v`
Expected: PASS (12 passed)

- [ ] **Step 5: 커밋**

```bash
git add "프로그램/_시스템/lemouton/policy/common_sync.py" "프로그램/_시스템/tests/policy/test_common_sync.py"
git commit -m "feat(정책): 마켓 탭에서 「공통 불러오기」 — 전체 · 항목별"
```

---

## Task 5: 값 출처 판정 — 공통 / 직접 / 없음

**Files:**
- Modify: `프로그램/_시스템/lemouton/policy/common_sync.py`
- Test: `프로그램/_시스템/tests/policy/test_common_sync.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/policy/test_common_sync.py` 맨 아래에 붙인다:

```python
def test_출처_판정_세_가지(db):
    from lemouton.policy.common_sync import origin_of, push_to_markets
    p = create_policy(db, name='르무통 기본')
    save_item(db, policy=p, market=COMMON_KEY, item_key='price',
              config={'margin_rate': 25})
    push_to_markets(db, policy=p, markets=['smartstore'])
    save_item(db, policy=p, market='coupang', item_key='price',
              config={'margin_rate': 32})

    assert origin_of(db, p.id, 'smartstore')['price'] == 'common'
    assert origin_of(db, p.id, 'coupang')['price'] == 'own'
    assert origin_of(db, p.id, 'gmarket').get('price', 'none') == 'none'


def test_공통이_바뀌어도_받은_마켓은_계속_공통이다(db):
    """값 비교로 판정했다면 여기서 「직접 고침」이라는 틀린 답이 나온다."""
    from lemouton.policy.common_sync import origin_of, push_to_markets
    p = create_policy(db, name='르무통 기본')
    save_item(db, policy=p, market=COMMON_KEY, item_key='price',
              config={'margin_rate': 25})
    push_to_markets(db, policy=p, markets=['smartstore'])

    save_item(db, policy=p, market=COMMON_KEY, item_key='price',
              config={'margin_rate': 10})

    assert origin_of(db, p.id, 'smartstore')['price'] == 'common'


def test_마켓_요약은_한_단어로_말한다(db):
    from lemouton.policy.common_sync import market_summary, push_to_markets
    p = create_policy(db, name='르무통 기본')
    save_item(db, policy=p, market=COMMON_KEY, item_key='price',
              config={'margin_rate': 25})
    push_to_markets(db, policy=p, markets=['smartstore'])
    save_item(db, policy=p, market='coupang', item_key='price',
              config={'margin_rate': 32})

    s = market_summary(db, p.id)
    assert s['smartstore']['state'] == 'common'
    assert s['coupang']['state'] == 'own'
    assert s['gmarket']['state'] == 'none'
    assert s['smartstore']['at'] is not None, '받은 날짜를 화면에 보여줘야 한다'
```

- [ ] **Step 2: 실패하는지 돌려본다**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy/test_common_sync.py -v`
Expected: FAIL — `ImportError: cannot import name 'origin_of'`

- [ ] **Step 3: 최소한만 구현한다**

`lemouton/policy/common_sync.py` 맨 아래에 붙인다:

```python
def origin_of(session, policy_id: int, market: str) -> dict:
    """{item_key: 'common' | 'own'}. 값이 없는 항목은 **키 자체가 없다**.

    화면에서는 키가 없으면 「없음」으로 그린다.
    """
    out = {}
    for v in session.scalars(select(MarketPolicyValue).where(
            MarketPolicyValue.policy_id == policy_id,
            MarketPolicyValue.market == market)):
        out[v.field_key] = 'common' if v.from_common_at else 'own'
    return out


def market_summary(session, policy_id: int) -> dict:
    """마켓마다 한 단어 + 날짜 — {market: {'state': ..., 'at': datetime|None}}.

    state: 'common'(공통에서 받음) · 'own'(직접 고침) · 'none'(아직 없음)
    한 마켓 안에 공통과 직접이 섞여 있으면 **'own'** 으로 본다 —
    「공통 따름」이라고 말했다가 실제로 다르면 그게 더 나쁘다.
    """
    out = {}
    for mk in MARKET_KEYS:
        rows = list(session.scalars(select(MarketPolicyValue).where(
            MarketPolicyValue.policy_id == policy_id,
            MarketPolicyValue.market == mk)))
        if not rows:
            out[mk] = {'state': 'none', 'at': None}
            continue
        stamps = [r.from_common_at for r in rows if r.from_common_at]
        state = 'common' if len(stamps) == len(rows) else 'own'
        out[mk] = {'state': state, 'at': max(stamps) if stamps else None}
    return out
```

- [ ] **Step 4: 통과하는지 돌려본다**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy/test_common_sync.py -v`
Expected: PASS (15 passed)

- [ ] **Step 5: 커밋**

```bash
git add "프로그램/_시스템/lemouton/policy/common_sync.py" "프로그램/_시스템/tests/policy/test_common_sync.py"
git commit -m "feat(정책): 값 출처 판정 — 공통 · 직접 · 없음"
```

---

## Task 6: 정책 복사

**Files:**
- Create: `프로그램/_시스템/lemouton/policy/copy.py`
- Test: `프로그램/_시스템/tests/policy/test_policy_copy.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`프로그램/_시스템/tests/policy/test_policy_copy.py` 를 새로 만든다:

```python
# -*- coding: utf-8 -*-
"""정책 복사 — 노션 「생성된 정책 복사기능」.

목적 = 「한 상품을 정책별로 다르게 가공해 여러 상품으로 올리기」.
🔴 붙은 상품은 복사하지 않는다 — 복사본이 같은 상품을 낚아채면 원본이 풀린다.
"""
import pytest
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker

from shared.db import Base
from lemouton.policy import models as PM     # noqa: F401 — 테이블 등록
from lemouton.policy.fields import COMMON_KEY
from lemouton.policy.service import (
    PolicyError, applied_count, apply_to, create_policy, save_item, values_for,
)
from lemouton.sourcing.models import Model


@pytest.fixture()
def db():
    eng = create_engine('sqlite:///:memory:')
    Base.metadata.create_all(eng)
    s = sessionmaker(bind=eng)()
    yield s
    s.close()


def test_값이_전부_복사된다(db):
    from lemouton.policy.copy import copy_policy
    p = create_policy(db, name='르무통 기본')
    save_item(db, policy=p, market=COMMON_KEY, item_key='price',
              config={'margin_rate': 25})
    save_item(db, policy=p, market='coupang', item_key='price',
              config={'margin_rate': 32})

    c = copy_policy(db, policy=p)

    assert c.id != p.id
    assert values_for(db, c.id, COMMON_KEY) == {'price': {'margin_rate': 25}}
    assert values_for(db, c.id, 'coupang') == {'price': {'margin_rate': 32}}


def test_이름은_겹치지_않게_붙는다(db):
    from lemouton.policy.copy import copy_policy
    p = create_policy(db, name='르무통 기본')
    c1 = copy_policy(db, policy=p)
    c2 = copy_policy(db, policy=p)
    assert c1.name == '르무통 기본 (복사)'
    assert c2.name == '르무통 기본 (복사 2)'


def test_붙은_상품은_따라오지_않는다(db):
    from lemouton.policy.copy import copy_policy
    db.add(Model(model_code='M1', model_name_raw='M1', brand='르무통'))
    db.flush()
    p = create_policy(db, name='르무통 기본')
    apply_to(db, policy=p, model_codes=['M1'])

    c = copy_policy(db, policy=p)

    assert applied_count(db, p.id) == 1
    assert applied_count(db, c.id) == 0


def test_기본_정책_표시는_따라오지_않는다(db):
    from lemouton.policy.copy import copy_policy
    from lemouton.policy.service import set_default
    p = create_policy(db, name='르무통 기본')
    set_default(db, policy=p)

    c = copy_policy(db, policy=p)

    assert p.is_default == 1
    assert c.is_default == 0


def test_이름을_직접_줄_수_있다(db):
    from lemouton.policy.copy import copy_policy
    p = create_policy(db, name='르무통 기본')
    c = copy_policy(db, policy=p, name='르무통 프리미엄')
    assert c.name == '르무통 프리미엄'


def test_이미_있는_이름으로는_복사하지_못한다(db):
    from lemouton.policy.copy import copy_policy
    p = create_policy(db, name='르무통 기본')
    create_policy(db, name='르무통 프리미엄')
    with pytest.raises(PolicyError):
        copy_policy(db, policy=p, name='르무통 프리미엄')
```

- [ ] **Step 2: 실패하는지 돌려본다**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy/test_policy_copy.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'lemouton.policy.copy'`

- [ ] **Step 3: 최소한만 구현한다**

`lemouton/policy/copy.py` 를 새로 만든다:

```python
# -*- coding: utf-8 -*-
"""정책 복사 — 노션 「생성된 정책 복사기능」.

「한가지 상품에도 여러 정책으로 정책별로 가공하여 마켓에 업로드 가능」이 목적이다.
🔴 **붙은 상품은 복사하지 않는다.** 상품 하나에 정책 하나라, 복사본이 같은 상품을
   가져가면 원본에서 조용히 떨어진다.
"""
from __future__ import annotations

from sqlalchemy import select

from lemouton.policy.models import MarketPolicy, MarketPolicyValue
from lemouton.policy.service import PolicyError


def _free_name(session, base: str) -> str:
    """「… (복사)」, 그것도 있으면 「… (복사 2)」."""
    taken = set(session.scalars(select(MarketPolicy.name).where(
        MarketPolicy.deleted_at.is_(None))))
    cand = f'{base} (복사)'
    if cand not in taken:
        return cand
    n = 2
    while f'{base} (복사 {n})' in taken:
        n += 1
    return f'{base} (복사 {n})'


def copy_policy(session, *, policy: MarketPolicy, name: str = '') -> MarketPolicy:
    """정책과 그 값 전부를 복사한다. 붙은 상품·기본 표시는 따라오지 않는다."""
    want = (name or '').strip()
    if want:
        dup = session.scalar(select(MarketPolicy).where(
            MarketPolicy.name == want, MarketPolicy.deleted_at.is_(None)))
        if dup is not None:
            raise PolicyError(f'「{want}」 이름의 정책이 이미 있어요.')
    else:
        want = _free_name(session, policy.name)

    new = MarketPolicy(name=want, memo=policy.memo, is_default=0,
                       brand=getattr(policy, 'brand', None),
                       enabled_markets=getattr(policy, 'enabled_markets', None))
    session.add(new)
    session.flush()

    for v in session.scalars(select(MarketPolicyValue).where(
            MarketPolicyValue.policy_id == policy.id)):
        session.add(MarketPolicyValue(
            policy_id=new.id, market=v.market, field_key=v.field_key,
            value=v.value, from_common_at=v.from_common_at))
    session.flush()
    return new
```

> `brand` · `enabled_markets` 는 Task 7·8 에서 만든다. `getattr(..., None)` 로 받아
> 이 순서로 실행해도 깨지지 않게 한다.

- [ ] **Step 4: 통과하는지 돌려본다**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy/test_policy_copy.py -v`
Expected: PASS (6 passed)

- [ ] **Step 5: 커밋**

```bash
git add "프로그램/_시스템/lemouton/policy/copy.py" "프로그램/_시스템/tests/policy/test_policy_copy.py"
git commit -m "feat(정책): 정책 복사 — 붙은 상품은 따라오지 않는다"
```

---

## Task 7: 정책에 브랜드 달기

**Files:**
- Modify: `프로그램/_시스템/lemouton/policy/models.py`
- Modify: `프로그램/_시스템/shared/db.py`
- Create: `프로그램/_시스템/tests/policy/test_policy_meta.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`프로그램/_시스템/tests/policy/test_policy_meta.py` 를 새로 만든다:

```python
# -*- coding: utf-8 -*-
"""정책의 브랜드 · 내보낼 마켓.

브랜드 = 노션 「브랜드별로 정책분류」. 정책이 늘면 목록에서 못 찾는다.
내보낼 마켓 = 노션 「상세 보기 전에 마켓별 토글 활성화(전체선택 포함)」.
"""
import pytest
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker

from shared.db import Base
from lemouton.policy import models as PM     # noqa: F401 — 테이블 등록
from lemouton.policy.service import PolicyError, create_policy


@pytest.fixture()
def db():
    eng = create_engine('sqlite:///:memory:')
    Base.metadata.create_all(eng)
    s = sessionmaker(bind=eng)()
    yield s
    s.close()


def test_브랜드는_안_정해도_된다(db):
    p = create_policy(db, name='르무통 기본')
    assert p.brand is None, '브랜드 없는 정책은 「브랜드 없음」으로 모인다'


def test_브랜드를_정할_수_있다(db):
    p = create_policy(db, name='르무통 기본', brand='르무통')
    assert p.brand == '르무통'


def test_브랜드_목록은_개수까지_준다(db):
    from lemouton.policy.service import brand_counts
    create_policy(db, name='르무통 기본', brand='르무통')
    create_policy(db, name='르무통 프리미엄', brand='르무통')
    create_policy(db, name='나이키 기본', brand='나이키')
    create_policy(db, name='TEST')

    assert brand_counts(db) == [('르무통', 2), ('나이키', 1), (None, 1)]
```

- [ ] **Step 2: 실패하는지 돌려본다**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy/test_policy_meta.py -v`
Expected: FAIL — `AttributeError: 'MarketPolicy' object has no attribute 'brand'`

- [ ] **Step 3: 최소한만 구현한다**

`lemouton/policy/models.py` 의 `MarketPolicy` 에 칸을 넣는다:

```python
    memo = Column(Text)
    # 브랜드별 분류(노션). 비어 있으면 목록에서 「브랜드 없음」으로 모인다.
    brand = Column(String(128))
```

`shared/db.py` 의 `migrations` 리스트에 넣는다 (Task 2 에서 넣은 줄 아래):

```python
        ("market_policy_values", "from_common_at", "TIMESTAMP"),
        # [2026-07-31] 정책 브랜드 분류 (NULL = 브랜드 없음)
        ("market_policies", "brand", "VARCHAR(128)"),
```

`lemouton/policy/service.py` 의 `create_policy` 가 브랜드를 받게 고친다:

```python
def create_policy(session, *, name: str, memo: str = '',
                  brand: str = '') -> MarketPolicy:
    name = (name or '').strip()
    if not name:
        raise PolicyError('정책 이름을 넣어 주세요.')
    dup = session.scalar(select(MarketPolicy).where(
        MarketPolicy.name == name, MarketPolicy.deleted_at.is_(None)))
    if dup is not None:
        raise PolicyError(f'「{name}」 이름의 정책이 이미 있어요.')
    p = MarketPolicy(name=name, memo=(memo or '').strip() or None,
                     brand=(brand or '').strip() or None)
    session.add(p)
    session.flush()
    return p
```

같은 파일 맨 아래에 붙인다:

```python
def brand_counts(session) -> list[tuple[str | None, int]]:
    """브랜드별 정책 수. 많은 순 → 이름 순. 브랜드 없는 것은 **맨 뒤**.

    맨 뒤에 두는 이유 — 「브랜드 없음」은 대개 만들다 만 정책이라
    목록 위쪽을 차지하면 진짜 정책이 밀린다.
    """
    from collections import Counter
    c = Counter(p.brand for p in session.scalars(select(MarketPolicy).where(
        MarketPolicy.deleted_at.is_(None))))
    named = sorted(((b, n) for b, n in c.items() if b),
                   key=lambda x: (-x[1], x[0]))
    if None in c:
        named.append((None, c[None]))
    return named
```

- [ ] **Step 4: 통과하는지 돌려본다**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy/test_policy_meta.py -v`
Expected: PASS (3 passed)

- [ ] **Step 5: 커밋**

```bash
git add "프로그램/_시스템/lemouton/policy/models.py" "프로그램/_시스템/lemouton/policy/service.py" "프로그램/_시스템/shared/db.py" "프로그램/_시스템/tests/policy/test_policy_meta.py"
git commit -m "feat(정책): 브랜드 분류 — 브랜드 없는 정책은 맨 뒤로"
```

---

## Task 8: 마켓 켜고 끄기

**Files:**
- Modify: `프로그램/_시스템/lemouton/policy/models.py`
- Modify: `프로그램/_시스템/lemouton/policy/service.py`
- Modify: `프로그램/_시스템/shared/db.py`
- Test: `프로그램/_시스템/tests/policy/test_policy_meta.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/policy/test_policy_meta.py` 맨 아래에 붙인다:

```python
def test_처음에는_모든_마켓이_켜져_있다(db):
    from lemouton.policy.fields import MARKET_KEYS
    from lemouton.policy.service import enabled_markets
    p = create_policy(db, name='르무통 기본')
    assert enabled_markets(db, p) == MARKET_KEYS


def test_마켓을_끌_수_있다(db):
    from lemouton.policy.service import enabled_markets, set_enabled_markets
    p = create_policy(db, name='르무통 기본')
    set_enabled_markets(db, policy=p, markets=['smartstore', 'coupang'])
    assert enabled_markets(db, p) == ['smartstore', 'coupang']


def test_모르는_마켓은_켤_수_없다(db):
    from lemouton.policy.service import set_enabled_markets
    p = create_policy(db, name='르무통 기본')
    with pytest.raises(PolicyError):
        set_enabled_markets(db, policy=p, markets=['없는마켓'])


def test_전부_끌_수_있다(db):
    """전부 끄면 아무 데도 안 나간다 — 화면이 그렇게 말해야 한다."""
    from lemouton.policy.service import enabled_markets, set_enabled_markets
    p = create_policy(db, name='르무통 기본')
    set_enabled_markets(db, policy=p, markets=[])
    assert enabled_markets(db, p) == []


def test_켜진_순서는_화면_순서를_따른다(db):
    """거꾸로 넣어도 화면 탭 순서(스스·쿠팡·G마켓…)로 돌아온다."""
    from lemouton.policy.service import enabled_markets, set_enabled_markets
    p = create_policy(db, name='르무통 기본')
    set_enabled_markets(db, policy=p, markets=['coupang', 'smartstore'])
    assert enabled_markets(db, p) == ['smartstore', 'coupang']
```

- [ ] **Step 2: 실패하는지 돌려본다**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy/test_policy_meta.py -v`
Expected: FAIL — `ImportError: cannot import name 'enabled_markets'`

- [ ] **Step 3: 최소한만 구현한다**

`lemouton/policy/models.py` 의 `MarketPolicy` 에 칸을 넣는다:

```python
    brand = Column(String(128))
    # 내보낼 마켓 (JSON 배열 문자열). NULL = 아직 안 정함 = **전부 켜짐**.
    #   🔴 빈 배열 '[]' 은 「전부 끔」이다 — NULL 과 다르다.
    enabled_markets = Column(Text)
```

`shared/db.py` 의 `migrations` 리스트에 넣는다:

```python
        ("market_policies", "brand", "VARCHAR(128)"),
        # [2026-07-31] 내보낼 마켓 (NULL = 전부 켜짐 · '[]' = 전부 꺼짐)
        ("market_policies", "enabled_markets", "TEXT"),
```

`lemouton/policy/service.py` 맨 아래에 붙인다:

```python
def enabled_markets(session, policy: MarketPolicy) -> list[str]:
    """내보낼 마켓. 아직 안 정했으면 **전부 켜진 것**으로 본다.

    🔴 안 정한 것을 「전부 꺼짐」으로 읽으면, 지금까지 잘 나가던 정책이
      이 기능을 붙이는 순간 조용히 멈춘다.
    """
    raw = getattr(policy, 'enabled_markets', None)
    if raw is None:
        return list(MARKET_KEYS)
    try:
        got = json.loads(raw)
    except (TypeError, ValueError):
        return list(MARKET_KEYS)        # 깨진 값은 안 정한 것으로 본다
    if not isinstance(got, list):
        return list(MARKET_KEYS)
    return [m for m in MARKET_KEYS if m in set(got)]


def set_enabled_markets(session, *, policy: MarketPolicy,
                        markets: list[str]) -> list[str]:
    """내보낼 마켓을 정한다. 빈 목록도 받는다(= 아무 데도 안 나감)."""
    picked = [m for m in dict.fromkeys(markets or []) if m]
    unknown = [m for m in picked if m not in MARKET_KEYS]
    if unknown:
        raise PolicyError(f'모르는 마켓이에요: {", ".join(unknown)}')
    ordered = [m for m in MARKET_KEYS if m in set(picked)]
    policy.enabled_markets = json.dumps(ordered, ensure_ascii=False)
    session.flush()
    return ordered
```

- [ ] **Step 4: 통과하는지 돌려본다**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy/test_policy_meta.py -v`
Expected: PASS (8 passed)

- [ ] **Step 5: 커밋**

```bash
git add "프로그램/_시스템/lemouton/policy/models.py" "프로그램/_시스템/lemouton/policy/service.py" "프로그램/_시스템/shared/db.py" "프로그램/_시스템/tests/policy/test_policy_meta.py"
git commit -m "feat(정책): 마켓 켜고 끄기 — 안 정한 것은 전부 켜짐"
```

---

## Task 9: 기존 테스트가 안 깨졌는지 확인

**Files:**
- Test: 기존 `프로그램/_시스템/tests/policy/`, `tests/registration/`

- [ ] **Step 1: 정책·대량등록 테스트를 다 돌린다**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy tests/registration -q`
Expected: 실패 0건. 실패가 나면 **이번 변경 탓인지 원래 그런지**를
`git stash` 후 같은 명령으로 대조해 가른다.

> ⚠️ `pytest` 를 인자 없이 돌리지 않는다 — 이 저장소는 테스트가 서로 오염돼
> 전체 실패 개수가 들쭉날쭉하다. 반드시 폴더를 지정한다.

- [ ] **Step 2: 커밋 (고칠 게 있었을 때만)**

```bash
git add -A
git commit -m "fix(정책): 새 칸 추가로 깨진 기존 테스트 정정"
```

---

## Task 10: 목록 화면 — 이름 변경 · 브랜드 단추 · 복사 · 마켓 배지

**Files:**
- Modify: `프로그램/_시스템/webapp/routes/policy.py:20-40` (`policy_index`)
- Modify: `프로그램/_시스템/webapp/templates/policy/index.html`
- Test: `프로그램/_시스템/tests/policy/test_policy_routes.py` (신규)

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`프로그램/_시스템/tests/policy/test_policy_routes.py` 를 새로 만든다:

```python
# -*- coding: utf-8 -*-
"""「정책 생성」 화면 API — 복사 · 브랜드 · 마켓 켜고 끄기 · 넣기 · 불러오기."""
import json

import pytest

from app import create_app
from shared.db import SessionLocal
from lemouton.policy.fields import COMMON_KEY
from lemouton.policy.service import create_policy, save_item


@pytest.fixture()
def client():
    app = create_app()
    app.config['TESTING'] = True
    with app.test_client() as c:
        yield c


def _new_policy(name):
    s = SessionLocal()
    try:
        p = create_policy(s, name=name)
        s.commit()
        return p.id
    finally:
        s.close()


def test_목록_제목이_정책_생성이다(client):
    r = client.get('/policies')
    assert r.status_code == 200
    assert '정책 생성' in r.get_data(as_text=True)


def test_복사_API(client):
    pid = _new_policy('복사원본')
    r = client.post(f'/api/policies/{pid}/copy',
                    json={}, content_type='application/json')
    assert r.status_code == 200
    j = r.get_json()
    assert j['ok'] is True
    assert j['name'] == '복사원본 (복사)'


def test_마켓_켜고_끄기_API(client):
    pid = _new_policy('마켓토글')
    r = client.post(f'/api/policies/{pid}/markets',
                    json={'markets': ['smartstore']},
                    content_type='application/json')
    assert r.status_code == 200
    assert r.get_json()['markets'] == ['smartstore']


def test_넣기_API(client):
    pid = _new_policy('넣기')
    s = SessionLocal()
    try:
        from lemouton.policy.models import MarketPolicy
        p = s.get(MarketPolicy, pid)
        save_item(s, policy=p, market=COMMON_KEY, item_key='price',
                  config={'margin_rate': 25})
        s.commit()
    finally:
        s.close()
    r = client.post(f'/api/policies/{pid}/push',
                    json={'markets': ['coupang']},
                    content_type='application/json')
    assert r.status_code == 200
    assert r.get_json()['count'] == 1


def test_불러오기_API(client):
    pid = _new_policy('불러오기')
    s = SessionLocal()
    try:
        from lemouton.policy.models import MarketPolicy
        p = s.get(MarketPolicy, pid)
        save_item(s, policy=p, market=COMMON_KEY, item_key='price',
                  config={'margin_rate': 25})
        s.commit()
    finally:
        s.close()
    r = client.post(f'/api/policies/{pid}/pull',
                    json={'market': 'coupang'},
                    content_type='application/json')
    assert r.status_code == 200
    assert r.get_json()['count'] == 1


def test_공통에_아무것도_없으면_넣기는_친절히_막는다(client):
    pid = _new_policy('빈공통')
    r = client.post(f'/api/policies/{pid}/push',
                    json={'markets': ['coupang']},
                    content_type='application/json')
    assert r.status_code == 400
    assert '먼저 채워' in r.get_json()['error']
```

- [ ] **Step 2: 실패하는지 돌려본다**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy/test_policy_routes.py -v`
Expected: FAIL — 404 (`/api/policies/<id>/copy` 없음)

- [ ] **Step 3: 최소한만 구현한다**

`webapp/routes/policy.py` 의 `policy_index` 를 고친다 (20~40행):

```python
@bp.route('/policies')
def policy_index():
    from lemouton.policy.common_sync import market_summary
    from lemouton.policy.models import MarketPolicy
    from lemouton.policy.service import (
        applied_count, brand_counts, enabled_markets, readiness,
    )
    from lemouton.policy.fields import MARKET_LABEL
    want_brand = (request.args.get('brand') or '').strip()
    s = SessionLocal()
    try:
        items = []
        for p in s.query(MarketPolicy).filter(MarketPolicy.deleted_at.is_(None)) \
                  .order_by(MarketPolicy.is_default.desc(), MarketPolicy.created_at.desc()):
            rd = readiness(s, p.id)
            items.append({
                'id': p.id, 'name': p.name, 'memo': p.memo, 'brand': p.brand,
                'is_default': bool(p.is_default),
                'applied': applied_count(s, p.id),
                'filled': sum(v['filled'] for v in rd.values()),
                'total': sum(v['total'] for v in rd.values()),
                'ready': [m for m in rd if rd[m]['price_ready']],
                'markets': enabled_markets(s, p),
                'summary': market_summary(s, p.id),
            })
        brands = brand_counts(s)
    finally:
        s.close()
    # 「브랜드 없음」은 brand=__none__ 으로 고른다 — 빈 문자열은 「전체」와 못 가른다.
    if want_brand == '__none__':
        items = [it for it in items if not it['brand']]
    elif want_brand:
        items = [it for it in items if it['brand'] == want_brand]
    return render_template('policy/index.html', active='policies', items=items,
                           brands=brands, want_brand=want_brand,
                           market_label=MARKET_LABEL, total=len(items))
```

같은 파일 맨 아래에 API 3개를 붙인다:

```python
@bp.post('/api/policies/<int:pid>/copy')
def api_copy(pid: int):
    from lemouton.policy.copy import copy_policy
    from lemouton.policy.models import MarketPolicy
    from lemouton.policy.service import PolicyError
    name = (request.get_json(silent=True) or {}).get('name') or ''
    s = SessionLocal()
    try:
        p = s.get(MarketPolicy, pid)
        if p is None or p.deleted_at:
            return jsonify({'ok': False, 'error': '없는 정책이에요.'}), 404
        try:
            c = copy_policy(s, policy=p, name=name)
        except PolicyError as e:
            return jsonify({'ok': False, 'error': str(e)}), 400
        s.commit()
        return jsonify({'ok': True, 'id': c.id, 'name': c.name})
    finally:
        s.close()


@bp.post('/api/policies/<int:pid>/markets')
def api_set_markets(pid: int):
    from lemouton.policy.models import MarketPolicy
    from lemouton.policy.service import PolicyError, set_enabled_markets
    body = request.get_json(silent=True) or {}
    s = SessionLocal()
    try:
        p = s.get(MarketPolicy, pid)
        if p is None or p.deleted_at:
            return jsonify({'ok': False, 'error': '없는 정책이에요.'}), 404
        try:
            got = set_enabled_markets(s, policy=p, markets=body.get('markets') or [])
        except PolicyError as e:
            return jsonify({'ok': False, 'error': str(e)}), 400
        s.commit()
        return jsonify({'ok': True, 'markets': got})
    finally:
        s.close()


@bp.post('/api/policies/<int:pid>/push')
def api_push(pid: int):
    """「마켓 공통」 값을 고른 마켓에 넣는다."""
    from lemouton.policy.common_sync import push_to_markets
    from lemouton.policy.models import MarketPolicy
    from lemouton.policy.service import PolicyError
    body = request.get_json(silent=True) or {}
    s = SessionLocal()
    try:
        p = s.get(MarketPolicy, pid)
        if p is None or p.deleted_at:
            return jsonify({'ok': False, 'error': '없는 정책이에요.'}), 404
        try:
            n = push_to_markets(s, policy=p, markets=body.get('markets') or [],
                                item_keys=body.get('item_keys'))
        except PolicyError as e:
            return jsonify({'ok': False, 'error': str(e)}), 400
        s.commit()
        return jsonify({'ok': True, 'count': n,
                        'message': f'{n}곳에 넣었습니다.'})
    finally:
        s.close()


@bp.post('/api/policies/<int:pid>/pull')
def api_pull(pid: int):
    """그 마켓이 「마켓 공통」을 불러온다."""
    from lemouton.policy.common_sync import pull_from_common
    from lemouton.policy.models import MarketPolicy
    from lemouton.policy.service import PolicyError
    body = request.get_json(silent=True) or {}
    s = SessionLocal()
    try:
        p = s.get(MarketPolicy, pid)
        if p is None or p.deleted_at:
            return jsonify({'ok': False, 'error': '없는 정책이에요.'}), 404
        try:
            n = pull_from_common(s, policy=p, market=body.get('market') or '',
                                 item_keys=body.get('item_keys'))
        except PolicyError as e:
            return jsonify({'ok': False, 'error': str(e)}), 400
        s.commit()
        return jsonify({'ok': True, 'count': n,
                        'message': f'{n}개 항목을 불러왔습니다.'})
    finally:
        s.close()
```

`webapp/templates/policy/index.html` 을 고친다.

① 제목·탭 이름 (38~45행):

```html
  <div class="pl-head">
    <h1>🔧 정책 생성</h1>
    <div class="pl-new">
      <input id="pname" placeholder="새 정책 이름 (예: 르무통 기본)">
      <input id="pbrand" placeholder="브랜드 (예: 르무통)" style="min-width:140px">
      <button class="btn2" id="add" type="button">＋ 정책 만들기</button>
    </div>
  </div>
  <div class="pl-desc">마켓마다 어떻게 올릴지 한 벌로 정해두고, 상품에 붙여서 씁니다.</div>
```

② 경고 박스 아래에 브랜드 단추 줄을 넣는다 (52행 `</div>` 다음):

```html
  <div class="pl-brands">
    <a class="chip2 {{ '' if want_brand else 'on' }}" href="/policies">전체
      <span class="n">{{ total }}</span></a>
    {% for b, n in brands %}
    <a class="chip2 {{ 'on' if want_brand == (b or '__none__') }}"
       href="/policies?brand={{ (b or '__none__') | urlencode }}"
       {% if not b %}style="color:var(--amber,#B45309)"{% endif %}>{{ b or '브랜드 없음' }}
      <span class="n">{{ n }}</span></a>
    {% endfor %}
  </div>
```

③ 정책 칸에 브랜드·마켓 배지·복사 단추를 넣는다 (57~80행 `pl-card` 통째로 교체):

```html
    <div class="pl-card">
      <div class="r1">
        <a class="nm" href="/policies/{{ it.id }}">{{ it.name }}</a>
        {% if it.is_default %}<span class="tag def">기본</span>{% endif %}
        {% if it.brand %}<span class="tag br">{{ it.brand }}</span>{% endif %}
      </div>
      {% if it.memo %}<div class="memo">{{ it.memo }}</div>{% endif %}
      <div class="bar"><i style="width:{{ (it.filled / it.total * 100) if it.total else 0 }}%"></i></div>
      <div class="pl-meta">
        <span>채움 <b>{{ it.filled }}</b> / {{ it.total }}</span>
        <span>붙은 상품 <b>{{ it.applied }}</b>개</span>
        {% if it.ready %}<span style="color:var(--green,#15803D)">가격 쓸 수 있음 {{ it.ready|length }}마켓</span>
        {% else %}<span class="tag no">가격 아직 못 씀</span>{% endif %}
      </div>
      <div class="pl-mk" data-id="{{ it.id }}">
        {% if it.markets %}
          {% for m in it.markets %}<span class="mkbadge">{{ market_label[m] }}</span>{% endfor %}
        {% else %}
          <span class="mkbadge off">내보낼 마켓 없음 — 아무 데도 안 나갑니다</span>
        {% endif %}
        <button class="btn2 ghost mk-edit" type="button">고치기</button>
      </div>
      <div class="pl-act">
        <a class="btn2 ghost" href="/policies/{{ it.id }}" style="text-decoration:none">항목 채우기</a>
        <button class="btn2 ghost" type="button" onclick="copyPolicy({{ it.id }})">📄 복사</button>
        {% if not it.is_default %}
        <button class="btn2 ghost" type="button" onclick="setDef({{ it.id }})">기본으로</button>
        {% endif %}
        {% if not it.applied %}
        <button class="btn2 ghost" type="button" style="margin-left:auto;color:#B91C1C"
                onclick="del({{ it.id }}, '{{ it.name }}')">지우기</button>
        {% endif %}
      </div>
    </div>
```

④ `<style>` 블록 맨 아래(35행 `.msg.ok{...}` 다음)에 새 부품 스타일을 붙인다:

```css
    .pl-brands{display:flex;gap:6px;flex-wrap:wrap;margin-bottom:16px}
    .chip2{border:1.5px solid var(--line,#E5E8EB);background:var(--surface,#ffffff);border-radius:999px;
           padding:6px 14px;font-size:12px;font-weight:600;color:var(--글자-기본,#4E5968);
           text-decoration:none}
    .chip2.on{border-color:var(--primary,#3182F6);background:var(--연한-파랑,#E8F2FF);color:#1B64DA}
    .chip2 .n{font-variant-numeric:tabular-nums;color:var(--sub,#8B95A1);margin-left:4px}
    .chip2.on .n{color:#3F72B8}
    .tag.br{background:var(--n100,#F2F4F6);color:var(--글자-기본,#4E5968)}
    .pl-mk{display:flex;gap:4px;flex-wrap:wrap;align-items:center;margin-top:8px}
    .mkbadge{font-size:11px;font-weight:700;background:var(--연한-파랑,#E8F2FF);color:#1B64DA;
             border-radius:999px;padding:1px 8px}
    .mkbadge.off{background:var(--연한-빨강,#FEECEC);color:#B91C1C}
    .pl-mk .btn2{padding:2px 10px;font-size:11px}
    .mk-box{display:none;width:100%;margin-top:8px;padding-top:8px;border-top:1px dashed var(--line,#E5E8EB)}
    .pl-mk.open .mk-box{display:block}
    .mk-grid{display:grid;grid-template-columns:repeat(3,max-content);gap:6px 20px;justify-content:start}
    .mk-grid label{display:flex;align-items:center;gap:6px;font-size:12px;color:var(--글자-기본,#4E5968);
                   cursor:pointer}
```

⑤ `<script>` 안, `del()` 위에 복사·마켓 편집을 붙인다:

```javascript
async function copyPolicy(id){
  const j = await (await fetch('/api/policies/' + id + '/copy', {
    method:'POST', headers:{'Content-Type':'application/json'}, body:'{}'})).json();
  if (j.ok) location.href = '/policies/' + j.id;
  else { msg.className = 'msg err'; msg.textContent = j.error; }
}

const MK = {{ market_label | tojson }};
document.querySelectorAll('.pl-mk').forEach(function (box) {
  const btn = box.querySelector('.mk-edit');
  const on = [].map.call(box.querySelectorAll('.mkbadge:not(.off)'),
                         function (b) { return b.textContent.trim(); });
  const wrap = document.createElement('div');
  wrap.className = 'mk-box';
  wrap.innerHTML = '<div class="mk-grid">' + Object.keys(MK).map(function (k) {
    return '<label><input type="checkbox" value="' + k + '"' +
      (on.indexOf(MK[k]) >= 0 ? ' checked' : '') + '> ' + MK[k] + '</label>';
  }).join('') + '</div>' +
    '<div style="margin-top:8px;font-size:11px;color:var(--amber,#B45309)">' +
    '끄면 그 마켓으로 <b>더 이상 안 나갑니다</b> — 이미 올라간 상품은 그대로 남습니다</div>' +
    '<button class="btn2" type="button" style="margin-top:8px">저장</button>';
  box.appendChild(wrap);
  btn.addEventListener('click', function () { box.classList.toggle('open'); });
  wrap.querySelector('button').addEventListener('click', async function () {
    const picked = [].filter.call(wrap.querySelectorAll('input'),
                                  function (i) { return i.checked; })
                     .map(function (i) { return i.value; });
    const j = await (await fetch('/api/policies/' + box.dataset.id + '/markets', {
      method:'POST', headers:{'Content-Type':'application/json'},
      body: JSON.stringify({markets: picked})})).json();
    if (j.ok) location.reload();
    else { msg.className = 'msg err'; msg.textContent = j.error; }
  });
});
```

⑥ 정책 만들 때 브랜드도 같이 보내게 `add` 핸들러를 고친다:

```javascript
document.getElementById('add').addEventListener('click', async () => {
  const name = document.getElementById('pname').value;
  const brand = document.getElementById('pbrand').value;
  msg.className = 'msg'; msg.textContent = '만드는 중…';
  const r = await fetch('/api/policies', {method:'POST', headers:{'Content-Type':'application/json'},
                                          body: JSON.stringify({name, brand})});
  const j = await r.json();
  if (j.ok){ location.href = '/policies/' + j.id; }
  else { msg.className = 'msg err'; msg.textContent = j.error; }
});
```

`webapp/routes/policy.py` 의 `api_create` 가 브랜드를 넘기게 고친다:

```python
        p = create_policy(s, name=body.get('name') or '',
                          memo=body.get('memo') or '',
                          brand=body.get('brand') or '')
```

- [ ] **Step 4: 통과하는지 돌려본다**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy/test_policy_routes.py -v`
Expected: PASS (6 passed)

- [ ] **Step 5: 커밋**

```bash
git add "프로그램/_시스템/webapp/routes/policy.py" "프로그램/_시스템/webapp/templates/policy/index.html" "프로그램/_시스템/tests/policy/test_policy_routes.py"
git commit -m "feat(정책 생성): 이름 변경 · 브랜드 단추 · 복사 · 마켓 켜고 끄기"
```

---

## Task 11: 상세 화면 — 「마켓 공통」 탭 + 넣기 · 불러오기

**Files:**
- Modify: `프로그램/_시스템/webapp/routes/policy.py:43-66` (`policy_detail`)
- Modify: `프로그램/_시스템/webapp/templates/policy/detail.html`
- Test: `프로그램/_시스템/tests/policy/test_policy_routes.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/policy/test_policy_routes.py` 맨 아래에 붙인다:

```python
def test_상세에_마켓_공통_탭이_있다(client):
    pid = _new_policy('공통탭')
    r = client.get(f'/policies/{pid}')
    body = r.get_data(as_text=True)
    assert r.status_code == 200
    assert '마켓 공통' in body


def test_공통_탭에는_마켓_전용_항목이_안_나온다(client):
    """「위너일 때 가격」은 쿠팡 전용 — 공통에 두면 어디로 넣을지 알 수 없다."""
    pid = _new_policy('공통탭2')
    r = client.get(f'/policies/{pid}?m=common')
    assert '위너일 때 가격' not in r.get_data(as_text=True)


def test_마켓_탭에는_공통_불러오기_단추가_있다(client):
    pid = _new_policy('불러오기단추')
    r = client.get(f'/policies/{pid}?m=smartstore')
    body = r.get_data(as_text=True)
    assert '공통 불러오기' in body
    assert '전체 불러오기' in body


def test_공통_탭에는_넣을_마켓_고르는_줄이_있다(client):
    pid = _new_policy('넣기줄')
    r = client.get(f'/policies/{pid}?m=common')
    assert '저장하고 넣기' in r.get_data(as_text=True)
```

- [ ] **Step 2: 실패하는지 돌려본다**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy/test_policy_routes.py -v`
Expected: FAIL — `'마켓 공통' not in body`

- [ ] **Step 3: 최소한만 구현한다**

`webapp/routes/policy.py` 의 `policy_detail` 을 고친다 (43~66행). 지금 코드는
`markets` 를 6개로 넘기는데, 맨 앞에 「마켓 공통」을 끼우고 출처 요약을 함께 넘긴다:

```python
@bp.route('/policies/<int:pid>')
def policy_detail(pid: int):
    from lemouton.policy.common_sync import market_summary, origin_of
    from lemouton.policy.fields import (
        COMMON_KEY, COMMON_LABEL, MARKETS, items_for,
    )
    from lemouton.policy.models import MarketPolicy
    from lemouton.policy.service import applied_count, readiness, values_for
    market = (request.args.get('m') or COMMON_KEY).strip()
    valid = [COMMON_KEY] + [k for k, _ in MARKETS]
    if market not in valid:
        market = COMMON_KEY
    s = SessionLocal()
    try:
        p = s.get(MarketPolicy, pid)
        if p is None or p.deleted_at:
            return render_template('errors/option_not_found.html', active='policies',
                                   what=f'정책 {pid}'), 404
        ctx = {
            'policy': p,
            'market': market,
            'is_common': market == COMMON_KEY,
            'common_key': COMMON_KEY,
            'markets': [(COMMON_KEY, COMMON_LABEL)] + list(MARKETS),
            'items': items_for(market),
            'values': values_for(s, pid, market),
            'origin': origin_of(s, pid, market),
            'summary': market_summary(s, pid),
            'readiness': readiness(s, pid),
            'applied': applied_count(s, pid),
        }
    finally:
        s.close()
    return render_template('policy/detail.html', active='policies', **ctx)
```

> `readiness()` 는 6마켓만 센다(공통은 세지 않는다). 템플릿에서 공통 탭의
> `readiness[market]` 을 읽지 않도록 아래 ①에서 갈라 쓴다.

`webapp/templates/policy/detail.html` 을 고친다.

① 마켓 탭 줄 (96~106행 통째로 교체):

```html
  <div class="mtabs">
    {% for mk, label in markets %}
    <a class="mtab {{ 'on' if mk == market }}" href="/policies/{{ policy.id }}?m={{ mk }}">
      {{ label }}
      {% if mk == common_key %}
        <span class="st">여기서 채웁니다</span>
      {% else %}
        {% set sm = summary[mk] %}
        <span class="st {{ 'no' if sm.state == 'none' else '' }}">
          {% if sm.state == 'common' %}공통{% if sm.at %} · {{ sm.at.strftime('%m/%d') }}{% endif %}
          {% elif sm.state == 'own' %}직접
          {% else %}없음{% endif %}
          · {{ readiness[mk].filled }}/{{ readiness[mk].total }}
        </span>
      {% endif %}
    </a>
    {% endfor %}
  </div>
```

② 「가격 못 씀」 경고 (108~114행)를 공통 탭에서는 안 띄운다:

```html
  {% if not is_common and not readiness[market].price_ready %}
  <div class="warn">
    <b>이 마켓은 아직 가격 계산에 쓸 수 없습니다.</b>
    「판매가」 항목을 아직 정하지 않았습니다. 정하지 않은 항목을 임의로 채워 계산하면
    그 가격이 그대로 마켓에 나갑니다.
  </div>
  {% endif %}

  {% if is_common %}
  <div class="warn">
    <b>여기 값은 「마지막에 넣은 값」입니다.</b> 마켓에서 따로 고친 곳은 여기를 고쳐도
    <b>저절로 바뀌지 않습니다</b> — 아래에서 그 마켓을 체크해 다시 넣거나,
    그 마켓 탭에서 <b>「공통 불러오기」</b>를 눌러야 바뀝니다.
  </div>
  {% else %}
  <div class="pullbar">
    <span class="pb-t">공통 불러오기</span>
    <span class="pb-w">
      {% set sm = summary[market] %}
      {% if sm.state == 'common' and sm.at %}마지막으로 불러온 때 · {{ sm.at.strftime('%m월 %d일 %H:%M') }}
      {% elif sm.state == 'own' %}이 마켓에서 직접 고친 값입니다
      {% else %}아직 불러온 적이 없습니다{% endif %}
    </span>
    <button class="btn2 ghost" id="pullAll" type="button">전체 불러오기</button>
  </div>
  <div class="pb-hint">「전체 불러오기」는 <b>이 마켓의 항목을 한꺼번에</b> 공통 값으로 바꿉니다.</div>
  {% endif %}
```

③ 항목 머리줄 (140~145행)에 출처 배지와 항목별 불러오기 단추를 넣는다:

```html
    <div class="it-h" onclick="tg('{{ it.key }}')">
      <span class="nm">{{ it.label }}</span>
      <span class="ref">{{ it.spec_ref or '' }}</span>
      {% if not is_common %}
      <span class="src {{ origin.get(it.key) or 'none' }}">
        {% if origin.get(it.key) == 'common' %}공통
        {% elif origin.get(it.key) == 'own' %}직접
        {% else %}없음{% endif %}
      </span>
      <button class="btnsm pull-one" type="button" data-item="{{ it.key }}"
              onclick="event.stopPropagation()">공통 불러오기</button>
      {% endif %}
      <span class="set {{ 'y' if saved else 'n' }}">{{ '정함' if saved else '안 정함' }}</span>
      <span class="chev">›</span>
    </div>
```

④ 저장 줄 (206~210행)을 공통/마켓으로 가른다:

```html
  {% if is_common %}
  <div class="savebar" style="display:block;padding:12px 0 16px">
    <div class="pushrow"><input type="checkbox" id="mkAll"> 전체 고르기
      <span style="margin-left:12px;font-weight:400;color:var(--글자-보조,#6B7684)">— 넣을 마켓</span></div>
    <div class="mk-grid">
      {% for mk, label in markets %}{% if mk != common_key %}
      <label><input type="checkbox" class="mkPick" value="{{ mk }}"> {{ label }}
        <span class="src {{ summary[mk].state }}">
          {% if summary[mk].state == 'common' %}공통
          {% elif summary[mk].state == 'own' %}직접{% else %}없음{% endif %}</span></label>
      {% endif %}{% endfor %}
    </div>
    <div style="display:flex;align-items:center;gap:12px;margin-top:12px">
      <span class="n" style="flex:1">고른 마켓 <b id="mkN">0</b>곳 ·
        <b style="color:var(--amber,#B45309)">「직접」인 마켓을 고르면 그 마켓이 고쳐 둔 값이 사라집니다</b></span>
      <span class="msg" id="msg"></span>
      <button class="btn2 ghost" id="save">공통만 저장</button>
      <button class="btn2" id="savePush">저장하고 넣기</button>
    </div>
  </div>
  {% else %}
  <div class="savebar">
    <span class="n">저장한 항목만 <b>「정함」</b>이 됩니다. 안 저장한 항목은 계산에 쓰지 않습니다.</span>
    <span class="msg" id="msg"></span>
    <button class="btn2" id="save" type="button">이 마켓 저장</button>
  </div>
  {% endif %}
```

⑤ `<style>` 블록 맨 아래(84행 `.scrollbox{...}` 다음)에 새 부품을 붙인다:

```css
    .src{font-size:10.5px;font-weight:700;border-radius:999px;padding:0 7px;white-space:nowrap;
         margin-left:auto}
    .src.common{background:var(--n100,#F2F4F6);color:var(--글자-보조,#6B7684)}
    .src.own{background:var(--연한-주황,#FFF7ED);color:var(--amber,#B45309)}
    .src.none{background:var(--연한-빨강,#FEECEC);color:#B91C1C}
    .btnsm{background:var(--surface,#ffffff);color:var(--글자-기본,#4E5968);
           border:1px solid var(--line,#E5E8EB);border-radius:8px;padding:3px 10px;
           font-size:11px;font-weight:700;font-family:inherit;cursor:pointer;white-space:nowrap;
           margin-left:6px}
    .btnsm:hover{border-color:var(--primary,#3182F6);color:#1B64DA}
    .pullbar{background:var(--surface,#ffffff);border:1px solid var(--line,#E5E8EB);border-radius:12px;
             padding:12px 16px;margin-bottom:8px;display:flex;align-items:center;gap:12px;flex-wrap:wrap}
    .pullbar .pb-t{font-size:13px;font-weight:700}
    .pullbar .pb-w{font-size:11px;color:var(--sub,#8B95A1)}
    .pullbar .btn2{margin-left:auto}
    .pb-hint{font-size:11px;color:var(--글자-보조,#6B7684);margin:0 0 12px 4px}
    .pushrow{display:flex;align-items:center;gap:6px;font-size:12px;font-weight:700;
             color:var(--ink,#191F28);padding-bottom:8px;margin-bottom:8px;
             border-bottom:1px dashed var(--line,#E5E8EB)}
    .mk-grid{display:grid;grid-template-columns:repeat(3,max-content);gap:6px 20px;justify-content:start}
    .mk-grid label{display:flex;align-items:center;gap:6px;font-size:12px;
                   color:var(--글자-기본,#4E5968);cursor:pointer}
    .mk-grid .src{margin-left:2px}
```

⑥ `<script>` 맨 아래에 넣기·불러오기 동작을 붙인다:

```javascript
/* ── 「마켓 공통」 넣기 · 불러오기 ─────────────────────────────── */
const IS_COMMON = {{ is_common | tojson }};

function pickedMarkets() {
  return [].filter.call(document.querySelectorAll('.mkPick'), function (i) { return i.checked; })
           .map(function (i) { return i.value; });
}

if (IS_COMMON) {
  const all = document.getElementById('mkAll');
  const n = document.getElementById('mkN');
  function recount() { n.textContent = pickedMarkets().length; }
  document.querySelectorAll('.mkPick').forEach(function (c) {
    c.addEventListener('change', recount);
  });
  all.addEventListener('change', function () {
    document.querySelectorAll('.mkPick').forEach(function (c) { c.checked = all.checked; });
    recount();
  });

  document.getElementById('savePush').addEventListener('click', async function () {
    const picked = pickedMarkets();
    if (!picked.length) { msg.className = 'msg err'; msg.textContent = '넣을 마켓을 고르세요.'; return; }
    const own = [].filter.call(document.querySelectorAll('.mkPick'), function (i) {
      return i.checked && i.parentElement.querySelector('.src.own'); })
      .map(function (i) { return i.parentElement.textContent.trim().split(/\s+/)[0]; });
    if (own.length && !window.confirm(
        '「' + own.join(', ') + '」 은(는) 그 마켓에서 직접 고친 값이 있습니다.\n' +
        '넣으면 그 값이 사라집니다. 계속할까요?')) return;
    await saveAll();                     /* 기존 저장 로직 — 아래 주의 참조 */
    const j = await (await fetch('/api/policies/' + PID + '/push', {
      method:'POST', headers:{'Content-Type':'application/json'},
      body: JSON.stringify({markets: picked})})).json();
    msg.className = 'msg ' + (j.ok ? 'ok' : 'err');
    msg.textContent = j.ok ? j.message : j.error;
    if (j.ok) setTimeout(function () { location.reload(); }, 600);
  });
} else {
  document.getElementById('pullAll').addEventListener('click', async function () {
    if (!window.confirm('이 마켓의 항목을 「마켓 공통」 값으로 바꿉니다.\n' +
                        '여기서 고친 값이 있으면 사라집니다. 계속할까요?')) return;
    const j = await (await fetch('/api/policies/' + PID + '/pull', {
      method:'POST', headers:{'Content-Type':'application/json'},
      body: JSON.stringify({market: MARKET})})).json();
    msg.className = 'msg ' + (j.ok ? 'ok' : 'err');
    msg.textContent = j.ok ? j.message : j.error;
    if (j.ok) setTimeout(function () { location.reload(); }, 600);
  });
  document.querySelectorAll('.pull-one').forEach(function (b) {
    b.addEventListener('click', async function () {
      const j = await (await fetch('/api/policies/' + PID + '/pull', {
        method:'POST', headers:{'Content-Type':'application/json'},
        body: JSON.stringify({market: MARKET, item_keys: [b.dataset.item]})})).json();
      msg.className = 'msg ' + (j.ok ? 'ok' : 'err');
      msg.textContent = j.ok ? j.message : j.error;
      if (j.ok) setTimeout(function () { location.reload(); }, 600);
    });
  });
}
```

> **주의** — 위 `saveAll()` 은 이 파일에 **이미 있는 저장 로직**을 함수로 뽑아 쓴 것이다.
> 지금은 `document.getElementById('save').addEventListener('click', ...)` 안에 인라인으로
> 들어 있다. 그 본문을 `async function saveAll(){ ... }` 로 꺼내고, 기존 `save` 핸들러는
> `saveAll` 을 부르게 바꾼다. 저장 코드를 **복사하지 않는다** — 두 벌이 되면 갈린다.

- [ ] **Step 4: 통과하는지 돌려본다**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy/test_policy_routes.py -v`
Expected: PASS (10 passed)

- [ ] **Step 5: 커밋**

```bash
git add "프로그램/_시스템/webapp/routes/policy.py" "프로그램/_시스템/webapp/templates/policy/detail.html" "프로그램/_시스템/tests/policy/test_policy_routes.py"
git commit -m "feat(정책 생성): 「마켓 공통」 탭 — 넣기 · 불러오기 · 출처 표시"
```

---

## Task 12: 사이드바 이름 바꾸기

**Files:**
- Modify: `프로그램/_시스템/webapp/templates/partials/` 안의 사이드바 (아래 명령으로 찾는다)

- [ ] **Step 1: 어디에 적혀 있는지 찾는다**

Run: `cd "프로그램/_시스템" && grep -rn "마켓별 정책" webapp/ --include=*.html --include=*.py`
Expected: 사이드바 항목 정의 파일 1~2곳

- [ ] **Step 2: 「정책 생성」으로 바꾼다**

찾은 자리의 `마켓별 정책` 을 `정책 생성` 으로 바꾼다. 링크 주소(`/policies`)는 그대로 둔다.

- [ ] **Step 3: 안 남았는지 확인한다**

Run: `cd "프로그램/_시스템" && grep -rn "마켓별 정책" webapp/`
Expected: 결과 없음 (주석·설계 문서 제외)

- [ ] **Step 4: 커밋**

```bash
git add -A "프로그램/_시스템/webapp"
git commit -m "feat(정책 생성): 사이드바 이름 「마켓별 정책」 → 「정책 생성」"
```

---

## Task 13: 라이브 실검증

**Files:** 없음 (실행·확인만)

- [ ] **Step 1: 정책·대량등록 테스트 전부**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy tests/registration -q`
Expected: 실패 0건

- [ ] **Step 2: 로컬에서 서버를 띄우고 눈으로 본다**

`.claude/launch.json` 의 설정으로 미리보기를 띄우고 `/policies` 를 연다.
확인할 것:
1. 제목이 「정책 생성」인가
2. 브랜드 단추 줄이 나오는가 (`전체 2` · `브랜드 없음 2`)
3. `📄 복사` 를 누르면 「TEST (복사)」가 생기는가
4. `고치기` 로 마켓을 끄면 배지가 줄어드는가
5. 정책을 열면 맨 앞 탭이 「마켓 공통」인가
6. 공통에서 「판매가」를 저장하고 마켓 2곳을 골라 `저장하고 넣기` 가 되는가
7. 그 마켓 탭에 `공통` 배지와 날짜가 뜨는가
8. 그 마켓에서 값을 고쳐 저장하면 배지가 `직접` 으로 바뀌는가
9. `전체 불러오기` 를 누르면 다시 `공통` 이 되는가

- [ ] **Step 3: 배포 전 마지막 확인**

Run: `cd "프로그램/_시스템" && python -m pytest tests/policy tests/registration -q`
Expected: 실패 0건

- [ ] **Step 4: PR 올리기**

```bash
git push -u origin feature/process-tab-stage1
gh pr create --title "feat(상품 가공): 「정책 생성」 화면 — 마켓 공통 · 복사 · 브랜드 · 마켓 켜고 끄기" --body "..."
```

---

## 이 계획이 다루지 않는 것 (다음 계획)

| 계획 | 내용 |
|---|---|
| 2 | 판매가 항목 심화(칸 52개 신설·19개 확장) + 「상품 정책 적용」 하위탭 |
| 3 | 상품 상세 3탭 재편(F1) + 정책별 결과 보기(H1) |
| 4 | 가격 단일 원천 전환 + 전 모음전 전수 대조 |

---

## Self-Review

**설계서 대비 빠진 것** — 설계서 §3-1(넣기/불러오기) Task 1~5 · §3-2(브랜드) Task 7·10 ·
§3-3(마켓 토글) Task 8·10 · §3-4(복사) Task 6·10 · §2(이름 변경) Task 10·12.
§3-5~3-8(판매가 심화·상세 재편)은 계획 2·3 으로 넘긴다고 위에 적었다.

**빈칸 검사** — 「TBD」·「나중에」·「적절히 처리」 없음. 모든 단계에 실제 코드와 실행 명령이 있다.

**이름이 어긋나지 않는가** — `COMMON_KEY` · `push_to_markets` · `pull_from_common` ·
`origin_of` · `market_summary` · `copy_policy` · `brand_counts` · `enabled_markets` ·
`set_enabled_markets` 를 정의한 자리와 쓰는 자리가 같다.
`from_common_at` 은 Task 2 에서 만들고 Task 3·5 에서 쓴다.
