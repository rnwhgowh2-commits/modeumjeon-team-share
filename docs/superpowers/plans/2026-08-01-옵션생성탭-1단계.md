# 「옵션생성 & 상품생성」 1단계 (탭 재편) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 사이드바 「상품수집·생성」을 「옵션생성 & 상품생성」으로 바꾸고, 삭제 확정 3개를 없애고, 매트릭스·모음전 목록을 상품관리로 옮긴다. **기능 변화 0 — 자리만 바뀐다.**

**Architecture:** 사이드바는 `_STAGE_SPEC`(스펙) + 저장본 마이그레이션 두 곳을 **같이** 고쳐야 라이브에 반영된다(스펙만 고치면 안 나온다 — `api_sidebar.py:187` 실사고 기록). 새 허브 `/optgen` 은 A2 배치(가로탭 2개 + 카드 2장)의 **껍데기만** 만들고, 실제 화면은 지금 쓰는 곳으로 보낸다. 없는 기능을 있는 척하지 않는다.

**Tech Stack:** Flask Blueprint · Jinja2 · pytest

**설계서:** `docs/superpowers/specs/2026-08-01-옵션생성-상품생성-탭-design.md`

---

## 파일 구조

| 파일 | 책임 | 신규/수정 |
|---|---|---|
| `webapp/routes/api_sidebar.py` | 사이드바 스펙·저장본 마이그레이션 (단일 진실 원천) | 수정 |
| `webapp/routes/optgen.py` | `/optgen` 허브 라우트 + 하위탭 정의 | **신규** |
| `webapp/templates/optgen/index.html` | 허브 화면 (가로탭 2 + 카드 2) | **신규** |
| `webapp/routes/__init__.py` | 블루프린트 등록 | 수정 |
| `tests/catalog/test_optgen_tab.py` | 사이드바 재편 + 허브 화면 회귀 | **신규** |

---

## Task 1: 사이드바 스펙 재편

**Files:**
- Modify: `webapp/routes/api_sidebar.py:28-77`
- Test: `tests/catalog/test_optgen_tab.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/catalog/test_optgen_tab.py` 를 새로 만든다:

```python
# -*- coding: utf-8 -*-
"""「옵션생성 & 상품생성」 1단계 — 사이드바 재편이 라이브에 도달하는지 지킨다.

★ 스펙(_STAGE_SPEC)만 고치면 라이브에 안 나온다. 서버는 사장님이 드래그로
  저장한 레이아웃을 쓰기 때문(api_sidebar.py:187 실사고 기록).
  그래서 스펙·저장본 마이그레이션·렌더 필터 셋을 다 지킨다.
"""
from webapp.routes import api_sidebar as SB


def _stage(out, sid):
    for st in out.get('stages', []):
        if st.get('id') == sid:
            return st
    return None


def _ids(out, sid):
    st = _stage(out, sid)
    return [it.get('id') for it in (st or {}).get('items', [])]


def test_스펙의_수집단계는_옵션생성_상품생성_하나뿐이다():
    spec = {s[0]: s for s in SB._STAGE_SPEC}
    sid, emoji, name, color, ids = spec['s_collect']
    assert name == '옵션생성 & 상품생성'
    assert ids == ['i_optgen']


def test_스펙의_상품관리에_모음전_상품관리와_옵션관리가_들어간다():
    spec = {s[0]: s for s in SB._STAGE_SPEC}
    ids = spec['s_catalog'][4]
    assert ids == ['i_bundles', 'i_matrix', 'i_catalog']


def test_삭제확정_3개는_렌더에서_걸러진다():
    """신규 모음전 등록·기존 마켓 연동·판매처 연동 — 사장님 확정 삭제."""
    for iid in ('i_new', 'i_migrate', 'i_sets_dash'):
        assert iid in SB._REMOVED_IDS


def test_옮긴_항목은_이름이_강제로_바뀐다():
    """사장님이 예전에 고쳐둔 이름이 남아 있어도 새 이름으로 보여야 한다."""
    for iid in ('i_bundles', 'i_matrix', 'i_catalog'):
        assert iid in SB._FORCE_RENAME
    assert SB._ITEM_DEFS['i_bundles']['name'] == '모음전 상품관리'
    assert SB._ITEM_DEFS['i_matrix']['name'] == '모음전 옵션관리'
    assert SB._ITEM_DEFS['i_catalog']['name'] == '마켓 상품 현황'
    assert SB._ITEM_DEFS['i_optgen']['url'] == '/optgen'
```

- [ ] **Step 2: 실패하는지 돌려본다**

```bash
cd "C:/dev/_wt_optgen/프로그램/_시스템" && python -m pytest tests/catalog/test_optgen_tab.py -v
```

기대: 4개 전부 FAIL (`KeyError: 'i_optgen'` · `assert '상품수집·생성' == '옵션생성 & 상품생성'`)

- [ ] **Step 3: `_ITEM_DEFS` 를 고친다**

`webapp/routes/api_sidebar.py` 에서 — **삭제**할 3줄 (`i_new` · `i_migrate` · `i_sets_dash`):

```python
    'i_new':            {'emoji': '➕', 'name': '신규 모음전 등록', 'url': '/bundles/new',           'active_key': 'bundles_new',     'badge_key': None},
    'i_migrate':        {'emoji': '🔗', 'name': '기존 마켓 연동',   'url': '/bundles/migrate',       'active_key': 'bundles_migrate', 'badge_key': None},
    'i_sets_dash':      {'emoji': '🏬', 'name': '판매처 연동',      'url': '/api/sets/dashboard',    'active_key': 'sets_dashboard',  'badge_key': 'sets_alerts'},
```

**추가**할 1줄 (`i_bundles` 바로 위):

```python
    'i_optgen':         {'emoji': '📥', 'name': '옵션생성 & 상품생성', 'url': '/optgen',             'active_key': 'optgen',          'badge_key': None},
```

**이름을 바꿀** 3줄 — 아래처럼 교체:

```python
    'i_bundles':        {'emoji': '📋', 'name': '모음전 상품관리',   'url': '/bundles',               'active_key': 'bundles',         'badge_key': None},
    'i_matrix':         {'emoji': '🧱', 'name': '모음전 옵션관리',   'url': '/matrix',                'active_key': 'matrix',          'badge_key': None},
    'i_catalog':        {'emoji': '📦', 'name': '마켓 상품 현황',    'url': '/catalog/',              'active_key': 'catalog',         'badge_key': None},
```

- [ ] **Step 4: `_STAGE_SPEC` 두 줄을 고친다**

```python
    ('s_collect',   '📥', '옵션생성 & 상품생성', '#3182F6', ['i_optgen']),
```

```python
    ('s_catalog',   '📦', '상품 관리',     '#06B6D4', ['i_bundles', 'i_matrix', 'i_catalog']),
```

- [ ] **Step 5: `_REMOVED_IDS` · `_FORCE_RENAME` 을 고친다**

`_REMOVED_IDS` 에 3개 추가 — 아래 줄을 집합 안에 넣는다:

```python
    # [2026-08-01] 사장님 확정 — 신규 모음전 등록 / 기존 마켓 연동 / 판매처 연동
    'i_new', 'i_migrate', 'i_sets_dash',
```

`_FORCE_RENAME` 을 통째로 교체:

```python
# 이름·이모지를 **강제로** 바꿀 항목 — 사용자가 고친 값이 있어도 덮어씀(의도된 개명).
#   i_templates: 「템플릿」 → 「가격 정책」
#   [2026-08-01] i_bundles·i_matrix·i_catalog: 상품관리 3탭으로 이동하며 개명.
#     사장님이 예전에 고쳐둔 옛 이름이 남아 있으면 옮긴 뜻이 안 보인다.
_FORCE_RENAME: set[str] = {'i_templates', 'i_bundles', 'i_matrix', 'i_catalog'}
```

- [ ] **Step 6: 테스트가 통과하는지 돌려본다**

```bash
cd "C:/dev/_wt_optgen/프로그램/_시스템" && python -m pytest tests/catalog/test_optgen_tab.py -v
```

기대: 4 passed

- [ ] **Step 7: 커밋**

```bash
cd "C:/dev/_wt_optgen" && git add "프로그램/_시스템/webapp/routes/api_sidebar.py" "프로그램/_시스템/tests/catalog/test_optgen_tab.py" && git commit -m "feat(사이드바): 「상품수집·생성」 → 「옵션생성 & 상품생성」 스펙 재편"
```

---

## Task 2: 저장본 마이그레이션 (🔴 이걸 빼면 라이브에 안 나온다)

**Files:**
- Modify: `webapp/routes/api_sidebar.py` (`_migrate_to_8groups` 아래에 함수 추가 · `_load` 에 호출 추가)
- Test: `tests/catalog/test_optgen_tab.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/catalog/test_optgen_tab.py` 끝에 붙인다:

```python
def _old_saved():
    """1단계 전 라이브 저장본을 흉내낸다 — 사장님이 이름을 고쳐둔 상태까지."""
    return {
        'version': 1, 'schema': 8, 'standalone': [],
        'stages': [
            {'id': 's_collect', 'emoji': '📥', 'name': '상품수집·생성', 'items': [
                {'id': 'i_new', 'name': '신규 모음전 등록', 'url': '/bundles/new'},
                {'id': 'i_bundles', 'name': '내가 고친 이름', 'url': '/bundles'},
                {'id': 'i_matrix', 'name': '매트릭스 옵션', 'url': '/matrix'},
                {'id': 'i_migrate', 'name': '기존 마켓 연동', 'url': '/bundles/migrate'},
                {'id': 'i_sets_dash', 'name': '판매처 연동', 'url': '/api/sets/dashboard'},
            ]},
            {'id': 's_catalog', 'emoji': '📦', 'name': '상품 관리', 'items': [
                {'id': 'i_catalog', 'name': '상품관리', 'url': '/catalog/'},
            ]},
        ],
    }


def test_저장본이_재편된다():
    layout = _old_saved()
    assert SB._migrate_optgen(layout) is True
    assert _ids(layout, 's_collect') == ['i_optgen']
    assert _ids(layout, 's_catalog') == ['i_bundles', 'i_matrix', 'i_catalog']
    assert _stage(layout, 's_collect')['name'] == '옵션생성 & 상품생성'


def test_삭제확정_3개가_저장본에서도_사라진다():
    layout = _old_saved()
    SB._migrate_optgen(layout)
    left = {it.get('id') for st in layout['stages'] for it in st['items']}
    assert not ({'i_new', 'i_migrate', 'i_sets_dash'} & left)


def test_두_번_돌려도_같다():
    """마이그레이션은 매 요청마다 불린다 — 두 번째부터는 아무것도 안 해야 한다."""
    layout = _old_saved()
    SB._migrate_optgen(layout)
    snapshot = [(_stage(layout, s)['name'], _ids(layout, s))
                for s in ('s_collect', 's_catalog')]
    assert SB._migrate_optgen(layout) is False
    after = [(_stage(layout, s)['name'], _ids(layout, s))
             for s in ('s_collect', 's_catalog')]
    assert snapshot == after


def test_옮긴_뒤_화면에는_새_이름으로_뜬다(monkeypatch):
    """사장님이 고쳐둔 「내가 고친 이름」이 아니라 「모음전 상품관리」로 보여야 한다."""
    layout = _old_saved()
    SB._migrate_optgen(layout)
    monkeypatch.setattr(SB, '_load', lambda: layout)
    out = SB.get_layout_for_template()
    names = {it['id']: it['name']
             for st in out['stages'] for it in st['items']}
    assert names['i_bundles'] == '모음전 상품관리'
    assert names['i_matrix'] == '모음전 옵션관리'
    assert names['i_optgen'] == '옵션생성 & 상품생성'
```

- [ ] **Step 2: 실패하는지 돌려본다**

```bash
cd "C:/dev/_wt_optgen/프로그램/_시스템" && python -m pytest tests/catalog/test_optgen_tab.py -v
```

기대: 새 4개 FAIL (`AttributeError: module 'webapp.routes.api_sidebar' has no attribute '_migrate_optgen'`)

- [ ] **Step 3: 마이그레이션 함수를 쓴다**

`webapp/routes/api_sidebar.py` 의 `_migrate_to_8groups` 함수 **바로 아래**에 붙인다:

```python
def _migrate_optgen(layout: dict) -> bool:
    """[2026-08-01] 「상품수집·생성」 → 「옵션생성 & 상품생성」 재편(1회, idempotent).

    🔴 스펙(_STAGE_SPEC)만 고치면 라이브에 안 나온다 — 서버는 사장님이 드래그로
       저장한 레이아웃을 쓴다. 그래서 **저장본 자체를 갈아끼운다.**

    · 수집 스테이지 항목을 i_optgen 하나로
    · i_bundles(모음전 상품관리) · i_matrix(모음전 옵션관리) 는 상품관리로 이동
    · 삭제 확정분(i_new · i_migrate · i_sets_dash)은 _REMOVED_IDS 가 렌더에서 거르지만,
      저장본에서도 빼서 다음 저장 때 되살아나지 않게 한다.
    """
    if _has_item_id(layout, 'i_optgen'):
        return False                                   # 이미 재편됨

    stages = layout.get('stages') or []
    moved: dict[str, dict] = {}
    for st in stages:
        keep = []
        for it in st.get('items') or []:
            iid = it.get('id')
            if iid in ('i_bundles', 'i_matrix'):
                moved.setdefault(iid, it)              # 어느 스테이지에 있든 뽑아낸다
                continue
            if iid in _REMOVED_IDS:
                continue
            keep.append(it)
        st['items'] = keep

    for st in stages:
        if st.get('id') == 's_collect':
            st['emoji'], st['name'] = '📥', '옵션생성 & 상품생성'
            st['items'] = [_item('i_optgen')]
            break
    else:
        stages.append({'id': 's_collect', 'emoji': '📥', 'name': '옵션생성 & 상품생성',
                       'color': '#3182F6', 'collapsed': False,
                       'items': [_item('i_optgen')]})

    cat = next((st for st in stages if st.get('id') == 's_catalog'), None)
    if cat is None:
        cat = {'id': 's_catalog', 'emoji': '📦', 'name': '상품 관리',
               'color': '#06B6D4', 'collapsed': False, 'items': []}
        stages.append(cat)
    cat['items'] = ([_item(i, moved.get(i)) for i in ('i_bundles', 'i_matrix')]
                    + list(cat.get('items') or []))

    layout['stages'] = stages
    return True
```

- [ ] **Step 4: `_load` 에서 부른다**

`_load()` 안의 마이그레이션 호출 줄들 아래에 한 줄 추가:

```python
        _mig4 = _migrate_to_8groups(data)  # 노션 8분류 재편 + 삭제 확정분 제거(1회)
        _mig5 = _migrate_optgen(data)      # [2026-08-01] 옵션생성 & 상품생성 재편(1회)
```

그리고 바로 아래 if 문을 고친다:

```python
        if _mig1 or _mig2 or _mig3 or _mig4 or _mig5:
```

- [ ] **Step 5: 테스트가 통과하는지 돌려본다**

```bash
cd "C:/dev/_wt_optgen/프로그램/_시스템" && python -m pytest tests/catalog/test_optgen_tab.py -v
```

기대: 8 passed

- [ ] **Step 6: 기존 사이드바 테스트도 깨지지 않았는지 본다**

```bash
cd "C:/dev/_wt_optgen/프로그램/_시스템" && python -m pytest tests/catalog -q
```

기대: 실패 0 (`test_sidebar_inject.py` 포함)

- [ ] **Step 7: 커밋**

```bash
cd "C:/dev/_wt_optgen" && git add "프로그램/_시스템/webapp/routes/api_sidebar.py" "프로그램/_시스템/tests/catalog/test_optgen_tab.py" && git commit -m "feat(사이드바): 저장본 마이그레이션 — 라이브 저장본도 새 배치로 갈아끼움"
```

---

## Task 3: `/optgen` 허브 화면

**Files:**
- Create: `webapp/routes/optgen.py`
- Create: `webapp/templates/optgen/index.html`
- Modify: `webapp/routes/__init__.py` (import · register)
- Test: `tests/catalog/test_optgen_tab.py`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/catalog/test_optgen_tab.py` 끝에 붙인다:

```python
import pytest


@pytest.fixture
def client(monkeypatch):
    monkeypatch.setenv('DISABLE_AUTH', '1')
    monkeypatch.delenv('MOUM_LIVE_UPLOAD', raising=False)
    import app as appmod
    flask_app = appmod.create_app()
    flask_app.config['TESTING'] = True
    return flask_app.test_client()


def test_허브가_뜬다(client):
    r = client.get('/optgen')
    assert r.status_code == 200
    html = r.get_data(as_text=True)
    assert '옵션생성 &amp; 상품생성' in html or '옵션생성 & 상품생성' in html


def test_가로탭이_두_개다(client):
    html = client.get('/optgen').get_data(as_text=True)
    assert '모음전 옵션 생성' in html
    assert '모음전 상품 생성' in html


def test_옵션생성_탭에_카드가_두_장이다(client):
    html = client.get('/optgen?tab=option').get_data(as_text=True)
    assert '직접 만들기' in html
    assert '내마켓 불러오기' in html


def test_모르는_탭은_옵션생성으로_돌아온다(client):
    """조용히 빈 화면을 그리지 않는다."""
    html = client.get('/optgen?tab=__없는탭__').get_data(as_text=True)
    assert '직접 만들기' in html


def test_아직_안_만든_기능을_있는_척하지_않는다(client):
    """1단계는 자리만 만든다 — 카드는 지금 쓰는 화면으로 보낸다."""
    html = client.get('/optgen?tab=option').get_data(as_text=True)
    assert '지금은' in html
```

- [ ] **Step 2: 실패하는지 돌려본다**

```bash
cd "C:/dev/_wt_optgen/프로그램/_시스템" && python -m pytest tests/catalog/test_optgen_tab.py -v -k "허브 or 가로탭 or 카드 or 돌아온다 or 척하지"
```

기대: 404 (`assert 404 == 200`)

- [ ] **Step 3: 라우트를 만든다**

`webapp/routes/optgen.py` 를 새로 만든다:

```python
# -*- coding: utf-8 -*-
"""옵션생성 & 상품생성 — 허브.

설계서: docs/superpowers/specs/2026-08-01-옵션생성-상품생성-탭-design.md
배치 확정: A2 (가로탭 2개 + 옵션 생성 안에서 카드 2장) — 노션 원문 그대로.

⚠️ 1단계는 **자리만 만든다.** 실제 옵션 만들기 화면은 3단계에서 온다.
   그때까지 카드는 「지금은 어디서 하는지」를 알려주고 그 화면으로 보낸다.
   없는 기능을 있는 척하면 사장님이 눌렀을 때 빈 화면을 본다.
"""
from flask import Blueprint, render_template, request

bp = Blueprint('optgen', __name__, url_prefix='/optgen')

#: 상단 가로탭. ⚠️ 여기 없는 탭은 화면에 아예 안 뜬다(catalog·bulk 와 같은 함정).
SUBTABS = [
    {'key': 'option', 'label': '모음전 옵션 생성',
     'desc': '색상·사이즈를 정해 옵션을 만듭니다'},
    {'key': 'product', 'label': '모음전 상품 생성',
     'desc': '만들어 둔 옵션을 담아 파는 단위를 만듭니다'},
]


@bp.get('/')
def index():
    tab = request.args.get('tab', 'option')
    if tab not in {t['key'] for t in SUBTABS}:
        tab = 'option'                      # 모르는 값은 조용히 빈 화면 대신 기본 탭
    return render_template('optgen/index.html',
                           active_app='bundles', active='optgen',
                           subtabs=SUBTABS, tab=tab)
```

- [ ] **Step 4: 화면을 만든다**

`webapp/templates/optgen/index.html` 를 새로 만든다:

```html
{% extends 'base.html' %}
{% block title %}옵션생성 &amp; 상품생성{% endblock %}
{% block content %}
<div class="page">
  <div class="page-header">
    <h1 class="page-header__title">옵션생성 &amp; 상품생성</h1>
    <p class="page-sub">
      {{ subtabs | selectattr('key', 'equalto', tab) | map(attribute='desc') | first }}</p>
  </div>

  <div class="og-tabs">
    {% for t in subtabs %}
      <a class="og-tab {% if t.key == tab %}on{% endif %}"
         href="/optgen?tab={{ t.key }}">{{ t.label }}</a>
    {% endfor %}
  </div>

  {% if tab == 'option' %}
  <div class="og-cards">
    <div class="og-card">
      <div class="og-ico">✏️</div>
      <div class="og-t">직접 만들기</div>
      <div class="og-d">색상과 사이즈를 직접 적어 옵션을 만듭니다.
        새 상품을 처음 등록할 때 씁니다.</div>
      <div class="og-now">지금은 <b>상품 관리 &gt; 모음전 상품관리</b> 에서 상품을 고른 뒤
        <b>「옵션 조합 생성 및 수정」</b> 으로 합니다. 이 자리로 옮기는 것은 다음 작업입니다.</div>
      <a class="og-go" href="/bundles">모음전 상품관리로 가기 →</a>
    </div>
    <div class="og-card">
      <div class="og-ico">🏪</div>
      <div class="og-t">내마켓 불러오기</div>
      <div class="og-d">이미 마켓에서 팔고 있는 상품을 찾아 이름·브랜드를 가져옵니다.
        색상·사이즈는 직접 짭니다.</div>
      <div class="og-now">지금은 <b>상품 관리 &gt; 마켓 상품 현황</b> 의
        <b>「상품 담기」</b> 에서 합니다. 이 자리로 옮기는 것은 다음 작업입니다.</div>
      <a class="og-go" href="/catalog/?tab=pick">상품 담기로 가기 →</a>
    </div>
  </div>
  {% else %}
  <div class="og-cards">
    <div class="og-card">
      <div class="og-ico">🧱</div>
      <div class="og-t">옵션을 담아 상품 만들기</div>
      <div class="og-d">만들어 둔 옵션을 격자에서 골라 담으면
        파는 단위(모음전 상품)가 만들어집니다.</div>
      <div class="og-now">지금은 <b>상품 관리 &gt; 모음전 옵션관리</b> 에서 합니다.
        이 자리로 옮기는 것은 다음 작업입니다.</div>
      <a class="og-go" href="/matrix">모음전 옵션관리로 가기 →</a>
    </div>
  </div>
  {% endif %}
</div>

<style>
  .og-tabs{display:flex;gap:4px;border-bottom:1px solid var(--color-border);
    margin-bottom:var(--sp-5)}
  .og-tab{padding:8px 16px;font-size:14px;font-weight:600;
    color:var(--color-text-tertiary);border-bottom:2px solid transparent;
    text-decoration:none}
  .og-tab.on{color:var(--color-primary);border-bottom-color:var(--color-primary)}
  .og-cards{display:grid;grid-template-columns:repeat(auto-fit,minmax(320px,1fr));
    gap:var(--sp-4);max-width:1100px}
  .og-card{border:1px solid var(--color-border);border-radius:12px;
    padding:var(--sp-5);background:var(--surface,#fff)}
  .og-ico{font-size:32px;margin-bottom:var(--sp-2)}
  .og-t{font-size:18px;font-weight:700;margin-bottom:var(--sp-2)}
  .og-d{font-size:14px;color:var(--color-text-secondary);margin-bottom:var(--sp-3)}
  .og-now{font-size:13px;color:var(--color-text-tertiary);
    background:var(--color-bg-muted);border-radius:8px;padding:10px 12px;
    margin-bottom:var(--sp-3);line-height:1.6}
  .og-go{display:inline-block;font-size:14px;font-weight:600;
    color:var(--color-primary);text-decoration:none}
  .og-go:hover{text-decoration:underline}
</style>
{% endblock %}
```

- [ ] **Step 5: 블루프린트를 등록한다**

`webapp/routes/__init__.py` — `from webapp.routes.catalog import bp as catalog_bp` 줄 **바로 아래**에 추가:

```python
    from webapp.routes.optgen import bp as optgen_bp  # [2026-08-01] 옵션생성 & 상품생성 허브
```

`app.register_blueprint(catalog_bp)` 줄 **바로 아래**에 추가:

```python
    app.register_blueprint(optgen_bp)  # [2026-08-01] 옵션생성 & 상품생성 허브
```

- [ ] **Step 6: 테스트가 통과하는지 돌려본다**

```bash
cd "C:/dev/_wt_optgen/프로그램/_시스템" && python -m pytest tests/catalog/test_optgen_tab.py -v
```

기대: 13 passed

- [ ] **Step 7: 커밋**

```bash
cd "C:/dev/_wt_optgen" && git add "프로그램/_시스템/webapp/routes/optgen.py" "프로그램/_시스템/webapp/templates/optgen/index.html" "프로그램/_시스템/webapp/routes/__init__.py" "프로그램/_시스템/tests/catalog/test_optgen_tab.py" && git commit -m "feat(옵션생성): /optgen 허브 — 가로탭 2 + 카드 2 (자리만, 기능 변화 0)"
```

---

## Task 4: 회귀 확인 · 라이브 검증

**Files:** 없음 (확인만)

- [ ] **Step 1: 사이드바를 건드렸으니 대량등록 테스트도 돌린다**

🔴 앞 세션이 사이드바·모드를 고치고 이걸 안 돌려 `test_bulk_mode` 2건이 깨진 채 PR 5개가 나갔다(PR#558에서 수정).

```bash
cd "C:/dev/_wt_optgen/프로그램/_시스템" && python -m pytest tests/registration tests/catalog tests/matrix -q
```

기대: `tests/registration/test_send_tab.py` 2건 · `test_speed_settings.py` 3건만 실패
(**origin/main 기준선에서도 같이 실패하는 기존 건**이라 무관하다. 새로 깨진 게 있으면 멈춘다.)

- [ ] **Step 2: 기준선과 대조한다 (위에서 실패 수가 5를 넘으면)**

```bash
cd "C:/dev/_wt_optgen" && git stash && cd "프로그램/_시스템" && python -m pytest tests/registration tests/catalog tests/matrix -q; cd "C:/dev/_wt_optgen" && git stash pop
```

기대: 같은 5건. 다르면 내가 깨뜨린 것.

- [ ] **Step 3: 로컬에서 화면을 눈으로 본다**

이 저장소엔 `.claude/launch.json` 이 없다. 아래 내용으로 새로 만든 뒤 미리보기를 띄운다.

```json
{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "moum-dev",
      "runtimeExecutable": "python",
      "runtimeArgs": ["프로그램/_시스템/app.py"],
      "port": 5000
    }
  ]
}
```

확인할 것:

1. 사이드바 첫 그룹 이름이 **「옵션생성 & 상품생성」** 인가
2. 그 그룹 안에 항목이 **하나(옵션생성 & 상품생성)** 뿐인가
3. 「상품 관리」 그룹에 **모음전 상품관리 · 모음전 옵션관리 · 마켓 상품 현황** 셋이 있는가
4. 「신규 모음전 등록」 · 「기존 마켓 연동」 · 「판매처 연동」이 **안 보이는가**
5. `/optgen` 을 눌렀을 때 가로탭 2개와 카드가 뜨는가
6. 카드의 「…로 가기 →」 를 눌러 실제로 그 화면이 열리는가

- [ ] **Step 4: PR 을 만든다**

```bash
cd "C:/dev/_wt_optgen" && git push -u origin feature/option-gen-tab && gh pr create --title "feat(옵션생성): 「상품수집·생성」 → 「옵션생성 & 상품생성」 탭 재편 (1단계)" --body "$(cat <<'EOF'
## 무엇

노션 「무재고 모음전 솔루션 개발현황」 기준 탭 재편의 **1단계 — 자리만 옮깁니다. 기능 변화 0.**

- 사이드바 첫 그룹 이름: 「상품수집·생성」 → **「옵션생성 & 상품생성」**
- 삭제(사장님 확정): 신규 모음전 등록 · 기존 마켓 연동 · 판매처 연동
- 이동: 모음전 구성 → **상품관리 > 모음전 상품관리** / 매트릭스 옵션 → **상품관리 > 모음전 옵션관리**
- 신설: `/optgen` 허브 — 가로탭 2개(모음전 옵션 생성 · 모음전 상품 생성) + 카드 2장

## 안 하는 것

실제 옵션 만들기 화면은 아직 안 옮겼습니다. 카드는 **「지금은 어디서 하는지」를 알려주고 그 화면으로 보냅니다** — 없는 기능을 있는 척하지 않기 위해서입니다.

## 🔴 주의

`_STAGE_SPEC` 만 고치면 라이브에 안 나옵니다(서버는 저장본을 씀). 그래서 `_migrate_optgen` 으로 **저장본도 갈아끼웁니다**. 두 번 돌려도 같은 결과인지 테스트로 지킵니다.

## 검증

- `tests/catalog/test_optgen_tab.py` 13건 신규
- `tests/registration tests/catalog tests/matrix` — origin/main 기존 실패 5건 외 새로 깨진 것 없음

설계서: `docs/superpowers/specs/2026-08-01-옵션생성-상품생성-탭-design.md`

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **Step 5: 배포 뒤 라이브에서 확인한다**

🔴 「배포 = success」 로그만 믿지 않는다. `origin/main` 에 새 코드 표식이 실제로 있는지 본다:

```bash
cd "C:/dev/_wt_optgen" && git fetch origin && git show origin/main:프로그램/_시스템/webapp/routes/api_sidebar.py | grep -c "_migrate_optgen"
```

기대: `2` 이상 (정의 + `_load` 호출)

그다음 `https://mou-m.com` 에서 Step 3 의 6가지를 그대로 다시 본다.

---

## 이 계획이 다루지 않는 것

설계서의 2~6단계는 각각 별도 계획으로 쓴다. 특히 **2단계(옵션 주인 이관)** 는 이 계획과 성격이 완전히 다르다 — 데이터를 옮기는 작업이고, 전수 대조·되돌리기·실전송 차단이 전제다.

| 단계 | 내용 |
|---|---|
| 2 | 옵션 주인 이관 + 소싱처 주소 저장소 통합 + 표시번호 소급 |
| 3 | 하위탭① 옵션 만들기 화면 승격(E1) + 새로 만들 5가지 |
| 4 | 하위탭③ 상품 생성 — 카테고리 칸 + 복제→참조 전환 |
| 5 | 하위탭② 내마켓 불러오기 |
| 6 | 전수 품절 알림 |
