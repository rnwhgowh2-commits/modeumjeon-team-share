# P2b 소싱 변동기록 훅 + 자동 주기 수집 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans.

**Goal:** (6a) 소싱 크롤 값 변동을 ChannelChangeEvent(source='source')로 기록 → H2 변동이력 소싱열·source_changed 알림 점등. (6b) 연동된 전 구성의 판매처 현재값을 주기 수집(스케줄러).

**Architecture(안전 설계):** 머니-크리티컬 글로벌 핫패스(save_crawl_result)는 **건드리지 않는다**. 대신 소싱 변동을 **세트 단위 스냅샷 비교**로 기록 — `change_service.snapshot_source_values(session, set_id, value_map)`가 매트릭스(_option_matrix_data)의 현재 src 값을 직전 source 이벤트와 비교해 변동만 기록. 트리거 = ①소싱처 업데이트 라우트(재크롤 직후) ②스케줄러 주기 작업. 주기 수집 = `collect_service.collect_all_linked_sets`. 스케줄러 잡은 env 가드(LEMOUTON_SETS_COLLECT_HOURS, 0=비활성)로 운영 부하 통제.

**Tech Stack:** Python/SQLAlchemy/APScheduler, pytest(in-memory SQLite).

---

### Task 1: change_service.snapshot_source_values (TDD)

**Files:** Modify `lemouton/sets/change_service.py` · Test `tests/sets/test_change_service.py`(추가)

value_map = {canonical_sku: {"stock": int|None, "price": int|None}}. 각 채널(market)·매칭 옵션마다 직전 source 이벤트 next_value 와 비교 → record_change(source="source") (동일값 미기록).

- [ ] Step 1: 실패 테스트
```python
def test_snapshot_source_values_records_and_idempotent(db):
    from lemouton.sets import change_service as cs
    from lemouton.sets.models import SetChannel, SetChannelOption
    ch = SetChannel(set_id=5, market="smartstore", account_key="a", market_product_id="9", status="linked")
    db.add(ch); db.flush()
    db.add(SetChannelOption(channel_id=ch.id, canonical_sku="K", market_option_id="1", status="matched"))
    db.commit()
    n1 = cs.snapshot_source_values(db, set_id=5, value_map={"K": {"stock": 7, "price": 50000}}); db.commit()
    assert n1 == 2
    evs = db.query(ChannelChangeEvent).filter_by(source="source").all()
    assert {(e.field, e.next_value) for e in evs} == {("stock", 7), ("price", 50000)}
    # 같은 값 재스냅샷 → 미기록
    n2 = cs.snapshot_source_values(db, set_id=5, value_map={"K": {"stock": 7, "price": 50000}}); db.commit()
    assert n2 == 0
    # 변동 → 기록
    n3 = cs.snapshot_source_values(db, set_id=5, value_map={"K": {"stock": 0, "price": 50000}}); db.commit()
    assert n3 == 1
    last = (db.query(ChannelChangeEvent).filter_by(source="source", field="stock")
            .order_by(ChannelChangeEvent.at.desc()).first())
    assert last.prev_value == 7 and last.next_value == 0
```

- [ ] Step 2: 실패 확인
- [ ] Step 3: 구현 (change_service.py 추가)
```python
def snapshot_source_values(session, *, set_id, value_map):
    """구성 옵션의 현재 소싱 값(value_map={sku:{stock,price}})을 직전 source 이벤트와
    비교해 변동만 ChannelChangeEvent(source='source')로 기록. 기록 건수 반환(호출자 commit)."""
    from lemouton.sets.models import SetChannel, SetChannelOption
    n = 0
    chans = session.query(SetChannel).filter_by(set_id=set_id).all()
    for ch in chans:
        scos = (session.query(SetChannelOption)
                .filter_by(channel_id=ch.id, status="matched").all())
        for sco in scos:
            vals = value_map.get(sco.canonical_sku)
            if not vals:
                continue
            for field in ("stock", "price"):
                new = vals.get(field)
                if new is None:
                    continue
                last = (session.query(ChannelChangeEvent)
                        .filter_by(set_id=set_id, market=ch.market,
                                   canonical_sku=sco.canonical_sku, field=field,
                                   source="source")
                        .order_by(ChannelChangeEvent.at.desc()).first())
                prev = last.next_value if last else None
                if record_change(session, set_id=set_id, market=ch.market,
                                 canonical_sku=sco.canonical_sku, field=field,
                                 source="source", prev_value=prev, next_value=new):
                    n += 1
    return n
```

- [ ] Step 4: 통과 확인 / Step 5: 커밋 `feat(sets): 소싱 변동 스냅샷 snapshot_source_values(source 이벤트)`

---

### Task 2: collect_service.collect_all_linked_sets (TDD)

**Files:** Modify `lemouton/sets/collect_service.py` · Test `tests/sets/test_collect_service.py`(추가)

- [ ] Step 1: 실패 테스트
```python
def test_collect_all_linked_sets(db):
    from lemouton.sets.models import SetChannel, SetChannelOption
    for sid in (10, 11):
        ch = SetChannel(set_id=sid, market="smartstore", account_key="a", market_product_id="9", status="linked")
        db.add(ch); db.flush()
        db.add(SetChannelOption(channel_id=ch.id, canonical_sku="S", market_option_id="11", status="matched", mkt_stock=5, mkt_price=100))
    db.commit()
    fetcher = lambda market, pid, env_prefix=None: _FR([_MO("11", 3, 100)])
    r = col.collect_all_linked_sets(db, fetcher=fetcher); db.commit()
    assert r["ok"] and r["sets"] == 2
```

- [ ] Step 2: 실패 확인
- [ ] Step 3: 구현 (collect_service.py 추가)
```python
def collect_all_linked_sets(session, fetcher=fetch_market_options):
    """연동된 모든 구성의 판매처 현재값 일괄 수집(주기 작업용)."""
    from lemouton.sets.models import SetChannel
    set_ids = [r[0] for r in session.query(SetChannel.set_id).distinct().all()]
    done = 0
    for sid in set_ids:
        try:
            collect_set(session, sid, fetcher=fetcher)
            done += 1
        except Exception:
            session.rollback()
    return {"ok": True, "sets": done}
```

- [ ] Step 4: 통과 / Step 5: 커밋 `feat(sets): 전 구성 주기 수집 collect_all_linked_sets`

---

### Task 3: 소싱처 업데이트 라우트에 소싱 스냅샷 배선

**Files:** Modify `webapp/routes/sets_api.py` (recrawl_sources_route)

재크롤 직후 매트릭스 src 값으로 snapshot_source_values 호출 → H2 소싱열·source_changed 점등.

- [ ] Step 1: recrawl_sources_route 에 스냅샷 추가 — update_set_sources 성공 후:
```python
        # 재크롤 직후 소싱 변동 스냅샷(H2 소싱열·source_changed 알림)
        try:
            from webapp.routes.api_pricing import _option_matrix_data
            from lemouton.sets.change_service import snapshot_source_values
            detail = svc.get_set_detail(s, set_id)
            if detail:
                skus = set()
                for p in detail["products"]:
                    skus.update(p["options"])
                vmap = {}
                for mc in {p["model_code"] for p in detail["products"]}:
                    data = _option_matrix_data(mc)
                    if not data.get("ok"):
                        continue
                    for o in data.get("options", []):
                        if o.get("sku") in skus:
                            is_pur = o.get("purchase_priority_resolved") == "purchase"
                            vmap[o["sku"]] = {
                                "stock": o.get("purchase_stock") if is_pur else o.get("src_stock"),
                                "price": o.get("src_cost"),
                            }
                snapshot_source_values(s, set_id=set_id, value_map=vmap)
                s.commit()
        except Exception:
            s.rollback()
```
(update_set_sources 의 s.commit() 뒤, jsonify(r) 앞에 삽입)

- [ ] Step 2: tests/sets 전체 통과(회귀) — `python -m pytest tests/sets/ -q`
- [ ] Step 3: 커밋 `feat(sets): 소싱처 업데이트 직후 소싱 변동 스냅샷 배선`

---

### Task 4: 스케줄러 주기 수집 잡(env 가드)

**Files:** Create `scheduler/sets_collect.py` · Modify `scheduler/main.py` (start_scheduler 끝에 등록)

운영 부하 통제: `LEMOUTON_SETS_COLLECT_HOURS`(default 12, 0=비활성). 잡 = 전 구성 collect + 소싱 스냅샷.

- [ ] Step 1: `scheduler/sets_collect.py`
```python
"""[E] 연동 구성 주기 수집 — 판매처 현재값 + 소싱 변동 스냅샷.

스케줄러(BackgroundScheduler) 잡. 마켓에 쓰지 않음(읽기+로컬). env 가드로 부하 통제.
"""
from __future__ import annotations
import logging

logger = logging.getLogger(__name__)


def collect_and_snapshot_all() -> dict:
    from shared.db import SessionLocal
    from lemouton.sets.collect_service import collect_all_linked_sets
    from lemouton.sets.set_service import list_linked_sets, get_set_detail
    from lemouton.sets.change_service import snapshot_source_values
    s = SessionLocal()
    collected = snapped = 0
    try:
        r = collect_all_linked_sets(s); s.commit()
        collected = r.get("sets", 0)
        # 소싱 변동 스냅샷(구성별)
        try:
            from webapp.routes.api_pricing import _option_matrix_data
        except Exception:
            _option_matrix_data = None
        if _option_matrix_data is not None:
            for row in list_linked_sets(s):
                sid = row["set_id"]
                try:
                    detail = get_set_detail(s, sid)
                    if not detail:
                        continue
                    skus = set()
                    for p in detail["products"]:
                        skus.update(p["options"])
                    vmap = {}
                    for mc in {p["model_code"] for p in detail["products"]}:
                        data = _option_matrix_data(mc)
                        if not data.get("ok"):
                            continue
                        for o in data.get("options", []):
                            if o.get("sku") in skus:
                                is_pur = o.get("purchase_priority_resolved") == "purchase"
                                vmap[o["sku"]] = {
                                    "stock": o.get("purchase_stock") if is_pur else o.get("src_stock"),
                                    "price": o.get("src_cost"),
                                }
                    snapped += snapshot_source_values(s, set_id=sid, value_map=vmap)
                    s.commit()
                except Exception:
                    s.rollback()
        return {"ok": True, "collected": collected, "snapped": snapped}
    except Exception:
        s.rollback()
        logger.exception("sets collect job failed")
        return {"ok": False, "collected": collected, "snapped": snapped}
    finally:
        s.close()
```

- [ ] Step 2: scheduler/main.py — `from scheduler.sets_collect import collect_and_snapshot_all` (상단), start_scheduler 의 `return sched` 직전에:
```python
    # 연동 구성 주기 수집(판매처 현재값 + 소싱 변동 스냅샷). env 가드(0=비활성).
    sets_hours = int(os.environ.get('LEMOUTON_SETS_COLLECT_HOURS', '12'))
    if sets_hours > 0 and sched.get_job('sets_collect') is None:
        sched.add_job(collect_and_snapshot_all, 'interval', hours=sets_hours,
                      id='sets_collect', max_instances=1, coalesce=True,
                      misfire_grace_time=60 * 30)
        logger.info('scheduler: sets_collect job every %dh', sets_hours)
```

- [ ] Step 3: import 스모크 — `python -c "import scheduler.main; import scheduler.sets_collect"` (앱 컨텍스트 없이 import만)
- [ ] Step 4: 커밋 `feat(scheduler): 연동 구성 주기 수집+소싱 스냅샷 잡(env 가드)`

---

## Self-Review
- 핫패스(save_crawl_result) 미변경 → 머니 무손상. 소싱 변동은 세트 단위 스냅샷(재크롤 직후·주기). ✅
- record_change 동일값 미기록 재사용 → 노이즈 0, 멱등. ✅
- 스케줄러 잡 env 가드 → 운영 부하 통제, 기본 12h. collect 는 마켓 read-only(쓰기 0). ✅
- source 이벤트 per (set, market) → H2 셀(set+market+field) 소싱열 정확 매핑. ✅
