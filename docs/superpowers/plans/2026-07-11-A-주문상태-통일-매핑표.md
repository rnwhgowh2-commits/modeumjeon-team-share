# 주문상태 통일 매핑표 (블랙스팟 선행 A) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 마켓 API 주문상태(`order_export._status_ko` 어휘)를 샵마인 정산 어휘로 번역하는 `status_to_shopmine()` 를 `sell_source.py` 에 추가해, 이식될 `classifier.py` 의 정산상태 판정이 API 경로에서도 정확하도록 한다. **블랙스팟(②)의 선행 블로커 해소.**

**Architecture:** 번역 지점은 **단 한 곳** — `sell_source._rows_to_df` 의 `"주문상태"` 대입부(line 268). 기존 `market_to_shopmine`(쇼핑몰 번역)과 대칭. 여기서 정규화하면 `matcher.match_for_classifier` 와 `classifier._determine_settlement_status` 는 **무수정**으로 올바른 값을 받는다. 샵마인 엑셀 경로(`from_shopmine_excel`)는 이미 진짜 샵마인 어휘라 **건드리지 않는다**.

**Tech Stack:** Python 3, pandas, pytest. (프런트·DB 변경 없음.)

**Worktree:** `C:\dev\_wt_margin_calc`, 브랜치 `feature/margin-calculator`. 모든 명령은 `C:\dev\_wt_margin_calc\프로그램\_시스템` 에서. **`export PYTHONIOENCODING=utf-8` 먼저.**

**근거(조사 완료):**
- 위험값 2개만 존재 — 롯데온 `철회`(odPrgsStepCd 22)·`회수확정`(26)이 현재 classifier 기본값(default→`O`)으로 **오분류**됨. 각각 취소·반품이어야 함.
- 모음전 프런트 `UNIFY_RULES`(`webapp/templates/orders/index.html:284-291`, PR#189 라이브검증)가 이미 `철회→취소완료`·`회수확정→반품완료`·`회수지시/진행→반품요청` 로 판정 — 교차확인 원천.
- 교환(`교환완료`/`교환요청`)은 원본 classifier 가 **정산O(판매 유지)** 로 취급 → "원본 그대로" 원칙대로 O 유지. 🔒 셀러화면 대조는 후속 항목으로 표기만.
- `SETTLEMENT_O_EXACT`/`SETTLEMENT_X_EXACT`/키워드셋은 `lemouton/margin/config.py:97-133` 에 이미 이식됨.

**이 플랜의 범위 밖:** classifier.py 이식(플랜 B) · 블랙스팟 UI(플랜 C). 여기서는 번역 함수 + 그 출력이 config 버킷에 올바로 떨어지는지까지만 검증한다(전 체인 통과 검증은 B 에서).

---

## 파일 구조

| 파일 | 책임 | 신규/수정 |
|---|---|---|
| `lemouton/margin/sell_source.py` | `status_to_shopmine()` 추가 + `_rows_to_df` 268행 배선 | 수정 |
| `tests/margin/test_status_to_shopmine.py` | 마켓별 어휘 → 정규화 + config 버킷 귀속 검증 | 신규 |

---

## Task 1: status_to_shopmine — 마켓별 주문상태 정규화

**Files:**
- Modify: `프로그램/_시스템/lemouton/margin/sell_source.py`
- Test: `프로그램/_시스템/tests/margin/test_status_to_shopmine.py`

`market_to_shopmine`(쇼핑몰 번역) 바로 아래에 `(판매처, API주문상태) → 샵마인 정규 문자열` 번역기를 추가한다. 위험값 2개는 remap, "우연히 맞던" 값은 명시 pin(견고성), 이미 정확한 값은 identity 통과.

- [ ] **Step 1: 실패하는 테스트 작성**

`프로그램/_시스템/tests/margin/test_status_to_shopmine.py`:
```python
# -*- coding: utf-8 -*-
"""API 주문상태 → 샵마인 정산 어휘 번역 + config 버킷 귀속 검증.

핵심: 롯데온 철회/회수확정이 정산O 로 새지 않고 취소/반품으로 떨어져야 한다.
"""
from lemouton.margin import sell_source as S
from lemouton.margin import config as C


def _bucket(shopmine_status: str) -> str:
    """config 규칙으로 샵마인 문자열이 어느 정산 버킷인지 (테스트용 축약 판정).

    classifier._determine_settlement_status 의 순서를 축약 재현:
    O_EXACT/EXCEPT_TO_O/EXCHANGE → O, 반품/회수/환불/수거 토큰 → 반품, 취소 토큰 → 취소.
    """
    s = shopmine_status
    if s in C.SETTLEMENT_O_EXACT or s in C.SETTLEMENT_X_EXCEPT_TO_O:
        return "O"
    if any(k in s for k in C.SETTLEMENT_EXCHANGE_KEYWORDS):
        return "O"
    if any(tok in s for tok in ("반품", "회수", "환불", "수거")):
        return "반품"
    if any(k in s for k in C.SETTLEMENT_CANCEL_KEYWORDS) or "취소" in s:
        return "취소"
    if s in C.SETTLEMENT_X_EXACT:
        return "반품" if any(t in s for t in ("반품", "회수", "환불", "수거", "교환")) else "취소"
    return "O"  # default fallthrough (classifier 와 동일)


# ── 위험값 2개 (이 플랜의 존재 이유) ──────────────────────────────
def test_lotteon_withdraw_is_cancel_not_settled():
    out = S.status_to_shopmine("롯데온", "철회")
    assert _bucket(out) == "취소", f"철회 → {out} (정산O 로 새면 안 됨)"


def test_lotteon_collect_confirmed_is_return_not_settled():
    out = S.status_to_shopmine("롯데온", "회수확정")
    assert _bucket(out) == "반품", f"회수확정 → {out} (정산O 로 새면 안 됨)"


# ── "우연히 맞던" 값 명시 pin (견고성) ─────────────────────────────
def test_pinned_values_land_in_settled():
    assert _bucket(S.status_to_shopmine("롯데온", "발송완료")) == "O"
    assert _bucket(S.status_to_shopmine("옥션", "구매결정")) == "O"
    assert _bucket(S.status_to_shopmine("쿠팡", "업체직접배송")) == "O"


# ── 이미 정확한 값 = identity 통과, 올바른 버킷 ───────────────────
def test_identity_values_keep_correct_bucket():
    assert _bucket(S.status_to_shopmine("스마트스토어", "배송완료")) == "O"
    assert _bucket(S.status_to_shopmine("쿠팡", "결제완료")) == "O"
    assert _bucket(S.status_to_shopmine("11번가", "취소완료")) == "취소"
    assert _bucket(S.status_to_shopmine("스마트스토어", "반품완료")) == "반품"
    assert _bucket(S.status_to_shopmine("롯데온", "회수지시")) == "반품"


# ── 교환 = 정산O 유지 (원본 그대로) ───────────────────────────────
def test_exchange_stays_settled_like_original():
    assert _bucket(S.status_to_shopmine("스마트스토어", "교환완료")) == "O"
    assert _bucket(S.status_to_shopmine("11번가", "교환요청")) == "O"


# ── 미지의 값·빈값 = 안전하게 원본 통과 ───────────────────────────
def test_unknown_passes_through():
    assert S.status_to_shopmine("스마트스토어", "듣도보도못한상태") == "듣도보도못한상태"
    assert S.status_to_shopmine("스마트스토어", "") == ""
    assert S.status_to_shopmine("", "배송완료") == "배송완료"


# ── 전 마켓 전 어휘 스모크: 예외 없이 문자열 반환 ─────────────────
def test_all_known_values_return_string():
    vocab = ["결제대기","결제완료","상품준비중","상품준비","출고지시","배송준비중",
             "배송지시","배송중","발송완료","배송완료","업체직접배송","구매확정",
             "구매결정","수취완료","취소요청","취소완료","철회","회수지시","회수진행",
             "회수완료","회수확정","반품요청","반품완료","교환요청","교환완료"]
    for mk in ("스마트스토어","쿠팡","롯데온","11번가","옥션","G마켓"):
        for v in vocab:
            out = S.status_to_shopmine(mk, v)
            assert isinstance(out, str) and out
```

- [ ] **Step 2: 실패 확인**

Run: `export PYTHONIOENCODING=utf-8; python -m pytest tests/margin/test_status_to_shopmine.py -v`
Expected: FAIL — `AttributeError: module 'lemouton.margin.sell_source' has no attribute 'status_to_shopmine'`

만약 `_bucket` 안에서 `C.SETTLEMENT_EXCHANGE_KEYWORDS` 등 상수가 없다고 나면, `config.py` 의 실제 상수명을 확인해 테스트의 참조를 **실제 이름으로 맞춘다**(상수 자체는 이미 존재 — 이름만 대조). 상수가 정말 없으면 STOP 하고 보고.

- [ ] **Step 3: `status_to_shopmine` 구현**

`sell_source.py` 의 `market_to_shopmine(...)` 정의 **바로 아래**에 추가:
```python
# order_export '판매처'별 API 주문상태 → 샵마인 정산 어휘 정규화.
# 위험값(정산O 로 오분류되는 값)만 remap + '우연히 맞던' 값 명시 pin.
# 이미 SETTLEMENT_* 에 정확히 있는 값은 여기 없으면 identity 통과.
_STATUS_TO_SHOPMINE = {
    "롯데온": {
        "철회": "취소완료",        # ★odPrgsStepCd 22 — 기본값 O 로 새던 것
        "회수확정": "반품완료",     # ★odPrgsStepCd 26 — 기본값 O 로 새던 것
        "발송완료": "발송완료(배송중)",  # pin: 정산O
    },
    "옥션":  {"구매결정": "구매확정"},   # pin: 정산O
    "G마켓": {"구매결정": "구매확정"},
    "쿠팡":  {"업체직접배송": "배송중"},  # pin: 정산O
}


def status_to_shopmine(panmaecheo, api_status):
    """(판매처, API 주문상태) → 샵마인 정산 정규 문자열. 미지 값은 원본 통과.

    번역 지점은 여기 한 곳뿐 — classifier/config 는 무수정. UNIFY_RULES(프런트)와
    철회→취소·회수확정→반품 판정이 일치한다. 교환은 원본대로 정산O 로 남긴다.
    """
    status = "" if api_status is None else str(api_status).strip()
    if not status:
        return status
    per = _STATUS_TO_SHOPMINE.get(str(panmaecheo).strip())
    if per and status in per:
        return per[status]
    return status
```

- [ ] **Step 4: `_rows_to_df` 268행 배선**

`sell_source.py` 의 `_rows_to_df` 안, 현재:
```python
            "주문상태": r.get("주문상태", ""),
```
를 다음으로 교체:
```python
            "주문상태": status_to_shopmine(r.get("판매처", ""), r.get("주문상태", "")),
```
(바로 위 `쇼핑몰` 줄의 `market_to_shopmine(r.get("판매처", ""))` 과 대칭.) **다른 줄은 건드리지 않는다.**

- [ ] **Step 5: 통과 확인 + 회귀**

```bash
export PYTHONIOENCODING=utf-8
python -m pytest tests/margin/test_status_to_shopmine.py -v      # 전부 pass
python -m pytest tests/margin/ -q                                # 기존 회귀 없음
```
`from_shopmine_excel` 경로 테스트가 깨지면 안 된다(그 경로는 status_to_shopmine 을 안 탐). 깨지면 `_rows_to_df`(API 경로) 외의 곳을 건드린 것 — 되돌린다.

- [ ] **Step 6: 커밋**

```bash
git add 프로그램/_시스템/lemouton/margin/sell_source.py 프로그램/_시스템/tests/margin/test_status_to_shopmine.py
git commit -m "feat(margin): 주문상태 통일 매핑표 — API 어휘→샵마인 정산어휘(철회→취소·회수확정→반품). 블랙스팟 선행"
```

---

## Task 2: 검증 메모 — UNSURE 항목을 스펙에 남긴다

**Files:**
- Modify: `프로그램/_시스템/docs/superpowers/specs/2026-07-10-대량등록-마진계산기-이식-design.md` (§13 의존 표 근처)

🔒 3대 원칙: 확정 못한 매핑은 셀러화면 대조 전까지 "확인 필요"로 표기한다(조용히 단정 금지).

- [ ] **Step 1: 스펙에 UNSURE 표 추가**

§13(후속 의존) 근처에 한 블록 추가:
```markdown
### 주문상태 매핑 — 셀러화면 대조 필요 항목 (2026-07-11, 플랜 A)
`status_to_shopmine`(sell_source.py) 구현 완료. 아래는 라이브 셀러화면으로 재확인 대상:
- 롯데온 `철회`→취소 / `회수확정`→반품 : UNIFY_RULES·메모리와 일치하나 실계정 1건 대조 권장.
- `교환완료`/`교환요청` = 정산O(판매 유지) : 원본 classifier 정책 그대로. 팀이 교환을 별도 집계 원하면 후속 변경.
- 스스 `결제대기`(PAYMENT_WAITING) : 매출시트 진입 드묾. 진입 시 정산O(대기)로 처리 중.
- 옥션·G마켓 클레임(취소/반품) : esm 빌더가 아직 클레임 행 미발행 → 라이브 연동 시 매핑 추가.
```

- [ ] **Step 2: 커밋**

```bash
git add 프로그램/_시스템/docs/superpowers/specs/2026-07-10-대량등록-마진계산기-이식-design.md
git commit -m "docs(spec): 주문상태 매핑 셀러화면 대조 필요 항목 표기(교환·철회·결제대기·esm클레임)"
```

---

## 완료 기준

- `test_status_to_shopmine.py` 전부 통과(위험값 2개 + pin + identity + 교환 + 미지 + 전마켓 스모크).
- `tests/margin/` 회귀 0.
- 번역 지점은 `_rows_to_df` 1곳뿐 — classifier·config·matcher·`from_shopmine_excel` 무수정.
- UNSURE 항목이 스펙에 문서화됨.

## 다음 (플랜 B)

`classifier.py` 무수정 이식 + 카드 우선순위 체인(`_getRowsByCardFilter_internal`)을 JS 로 이식. 이 매핑표가 붙었으므로 B 의 전 체인 테스트(API 행 → 매핑 → matcher → classifier → 정산상태)가 가능해진다.
