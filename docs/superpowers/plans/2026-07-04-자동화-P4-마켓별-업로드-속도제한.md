# 자동화 실행 엔진 P4 — 마켓별 업로드 속도 제한 (정책 + 계산기) 구현 플랜

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 판매처(마켓)마다 "분당 몇 개까지 보낼지"를 사용자가 정하고, 전송기가 그 속도를 넘지 않도록 하는 **정책 저장 + 속도제한 계산기**를 만든다. 너무 빨리 올려 마켓이 막는 걸 방지. 실제 전송 루프에 끼우는 배선(P4b)은 dry-run/실전송 경로와 함께 다음 단계.

**Architecture:** (1) 마켓별 정책 테이블 `MarketUploadPolicy(market, per_minute, enabled)` + get/set/검증(자동화 설정과 같은 패턴), (2) 순수 계산 `upload_allowance`(지금 몇 개 더 보내도 되나) + `throttle_take`(목록을 지금분/대기분으로 나눔), (3) 정책을 읽어 허용량을 주는 `market_send_allowance`. 실제 마켓 API 호출·전송 기록은 P4b.

**Tech Stack:** SQLAlchemy, pytest(in-memory SQLite, `test_automation_settings.py` 와 같은 self-contained 픽스처).

**대상 워크트리:** `C:\dev\모음전 프로젝트\_wt_auto_engine` (브랜치 `feature/automation-engine`)

**스펙:** [`docs/superpowers/specs/2026-07-04-자동화-연속배수큐-실행엔진-design.md`](../specs/2026-07-04-자동화-연속배수큐-실행엔진-design.md) §5.1(마켓 정책)·§5.5(업로드 속도 제한)

**경로 표기:** 이하 `프로그램/_시스템/...` 생략.

**전제:** 마켓 키 = `"coupang"`, `"smartstore"` (기존 `lemouton/uploader/runtime.py::select_adapters` 사용). `GlobalSettings`·`get_or_init` 은 `lemouton/pricing/settings.py`. 신규 테이블은 `create_all` 이 생성(모델을 `pricing/settings.py` 에 두어 부팅 시 등록).

**설계 규칙:**
- `per_minute` = 마켓별 분당 상한(정수 ≥ 0). 클수록 빠르게. 사용자가 겪어보며 조절.
- `enabled=False` = 그 마켓 자동 전송 잠금(허용량 0).
- 허용량 = `enabled` 이면 `max(0, per_minute − 최근1분전송수)`, 아니면 0. (최근1분전송수는 호출자가 주입 — 전송 기록은 P4b)
- 기본값: coupang·smartstore = `per_minute 10, enabled True` (안전한 시작값, 사용자 조절).

---

## File Structure

- **Modify** `lemouton/pricing/settings.py` — `MarketUploadPolicy` 모델 + `get_market_policies`·`set_market_policy`
- **Create** `lemouton/uploader/throttle.py` — 순수 `upload_allowance`·`throttle_take` + 정책 읽는 `market_send_allowance`
- **Create** `tests/pricing/test_market_upload_policy.py` — 정책 저장/검증
- **Create** `tests/uploader/test_upload_throttle.py` — 계산기 + 허용량

---

## Task 1: 마켓별 정책 저장 (MarketUploadPolicy)

**Files:**
- Modify: `lemouton/pricing/settings.py`
- Modify: `shared/db.py` (신규 테이블은 create_all 생성 — 컬럼 튜플 불필요, 단 주석 한 줄 추가)
- Test: `tests/pricing/test_market_upload_policy.py`

- [ ] **Step 1: 실패 테스트 작성**

`tests/pricing/test_market_upload_policy.py` 생성 (`test_automation_settings.py` 의 self-contained 픽스처 패턴):

```python
import pytest
from sqlalchemy import create_engine
from sqlalchemy.orm import Session

from shared.db import Base
from lemouton.pricing import settings as st
from lemouton.pricing.settings import MarketUploadPolicy  # noqa: F401 (등록용)


@pytest.fixture
def db():
    eng = create_engine("sqlite://")
    Base.metadata.create_all(eng)
    s = Session(eng)
    yield s
    s.close()


def test_defaults_seeded_for_known_markets(db):
    pol = st.get_market_policies(db)
    db.commit()
    assert pol["coupang"]["per_minute"] == 10
    assert pol["coupang"]["enabled"] is True
    assert pol["smartstore"]["per_minute"] == 10


def test_set_updates_only_given_fields(db):
    st.get_market_policies(db)  # seed
    st.set_market_policy(db, "coupang", per_minute=3)
    db.commit()
    pol = st.get_market_policies(db)
    assert pol["coupang"]["per_minute"] == 3
    assert pol["coupang"]["enabled"] is True   # 안 건드린 값 유지


def test_set_validates_non_negative(db):
    st.get_market_policies(db)
    st.set_market_policy(db, "smartstore", per_minute=-5)
    db.commit()
    pol = st.get_market_policies(db)
    assert pol["smartstore"]["per_minute"] == 0   # 음수 → 0 클램프


def test_set_enabled_toggle(db):
    st.get_market_policies(db)
    st.set_market_policy(db, "coupang", enabled=False)
    db.commit()
    assert st.get_market_policies(db)["coupang"]["enabled"] is False
```

- [ ] **Step 2: 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/pricing/test_market_upload_policy.py -v`
Expected: FAIL — `ImportError: cannot import name 'MarketUploadPolicy'`

- [ ] **Step 3: 구현** — `lemouton/pricing/settings.py`

파일 상단 import 에 `Boolean`, `String` 이 있는지 확인(없으면 추가). `GlobalSettings` 정의 아래에 추가:

```python
_MARKET_DEFAULTS = {
    "coupang": {"per_minute": 10, "enabled": True},
    "smartstore": {"per_minute": 10, "enabled": True},
}


class MarketUploadPolicy(Base):
    """마켓별 업로드 속도 정책. market = 마켓 키(coupang/smartstore …)."""
    __tablename__ = "market_upload_policies"

    market = Column(String(32), primary_key=True)
    per_minute = Column(Integer, default=10, nullable=False)  # 분당 상한
    enabled = Column(Boolean, default=True, nullable=False)


def get_market_policies(session) -> dict:
    """마켓별 정책 dict. 알려진 마켓(coupang/smartstore)은 없으면 기본값 시드."""
    for mk, d in _MARKET_DEFAULTS.items():
        row = session.get(MarketUploadPolicy, mk)
        if row is None:
            session.add(MarketUploadPolicy(market=mk, per_minute=d["per_minute"],
                                           enabled=d["enabled"]))
    session.flush()
    rows = session.query(MarketUploadPolicy).all()
    return {r.market: {"per_minute": r.per_minute, "enabled": r.enabled} for r in rows}


def set_market_policy(session, market: str, *, per_minute: int | None = None,
                      enabled: bool | None = None) -> dict:
    """전달된 항목만 갱신. per_minute 음수 → 0. 호출자가 commit."""
    row = session.get(MarketUploadPolicy, market)
    if row is None:
        d = _MARKET_DEFAULTS.get(market, {"per_minute": 10, "enabled": True})
        row = MarketUploadPolicy(market=market, per_minute=d["per_minute"],
                                 enabled=d["enabled"])
        session.add(row)
    if per_minute is not None:
        row.per_minute = max(0, int(per_minute))
    if enabled is not None:
        row.enabled = bool(enabled)
    session.flush()
    return {"per_minute": row.per_minute, "enabled": row.enabled}
```

- [ ] **Step 4: db.py 주석** — `shared/db.py` 마이그레이션 리스트 근처(라인 228 부근)에 한 줄 주석 추가(신규 테이블은 create_all 생성이라 튜플 불필요임을 명시):

```python
        # 2026-07-04: market_upload_policies 는 신규 테이블 → create_all 이 생성(컬럼 튜플 불필요)
```

- [ ] **Step 5: 통과 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/pricing/test_market_upload_policy.py -v`
Expected: PASS (4 passed)

- [ ] **Step 6: 커밋**

```bash
git add 프로그램/_시스템/lemouton/pricing/settings.py 프로그램/_시스템/shared/db.py 프로그램/_시스템/tests/pricing/test_market_upload_policy.py
git commit -m "feat(pricing): 마켓별 업로드 속도 정책 MarketUploadPolicy + get/set"
```

---

## Task 2: 순수 속도제한 계산기 + 허용량

**Files:**
- Create: `lemouton/uploader/throttle.py`
- Test: `tests/uploader/test_upload_throttle.py`

- [ ] **Step 1: 실패 테스트 작성**

`tests/uploader/` 에 `__init__.py` 가 필요한지 `tests/pricing/` 구조를 먼저 보고 맞춘다(있으면 생성). `tests/uploader/test_upload_throttle.py` 생성:

```python
import pytest
from sqlalchemy import create_engine
from sqlalchemy.orm import Session

from shared.db import Base
from lemouton.pricing import settings as st
from lemouton.pricing.settings import MarketUploadPolicy  # noqa: F401
from lemouton.uploader.throttle import (
    upload_allowance, throttle_take, market_send_allowance,
)


@pytest.fixture
def db():
    eng = create_engine("sqlite://")
    Base.metadata.create_all(eng)
    s = Session(eng)
    yield s
    s.close()


def test_allowance_basic():
    assert upload_allowance(10, 0) == 10
    assert upload_allowance(10, 7) == 3
    assert upload_allowance(10, 10) == 0
    assert upload_allowance(10, 15) == 0   # 이미 초과 → 0 (음수 아님)
    assert upload_allowance(0, 0) == 0      # 상한 0 = 안 보냄


def test_throttle_take_splits():
    items = [1, 2, 3, 4, 5]
    now, later = throttle_take(items, 2)
    assert now == [1, 2] and later == [3, 4, 5]
    now, later = throttle_take(items, 0)
    assert now == [] and later == [1, 2, 3, 4, 5]
    now, later = throttle_take(items, 99)
    assert now == [1, 2, 3, 4, 5] and later == []


def test_market_send_allowance_reads_policy(db):
    st.get_market_policies(db)                       # 기본 coupang per_minute 10
    st.set_market_policy(db, "coupang", per_minute=5)
    db.commit()
    assert market_send_allowance(db, "coupang", sent_last_minute=2) == 3


def test_disabled_market_allows_nothing(db):
    st.get_market_policies(db)
    st.set_market_policy(db, "coupang", enabled=False)
    db.commit()
    assert market_send_allowance(db, "coupang", sent_last_minute=0) == 0


def test_unknown_market_defaults_to_zero(db):
    # 정책 없는 낯선 마켓 → 안전하게 0 (함부로 보내지 않음)
    assert market_send_allowance(db, "elevenst", sent_last_minute=0) == 0
```

- [ ] **Step 2: 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/uploader/test_upload_throttle.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'lemouton.uploader.throttle'`

- [ ] **Step 3: 구현** — `lemouton/uploader/throttle.py`

```python
"""마켓별 업로드 속도 제한 — 순수 계산 + 정책 읽기.

너무 빨리 올려 판매처가 막는 걸 방지. 실제 전송·전송기록은 P4b.
"""


def upload_allowance(per_minute: int, sent_last_minute: int) -> int:
    """지금 이 마켓에 몇 개 더 보내도 되나. 음수 없음."""
    return max(0, int(per_minute) - int(sent_last_minute))


def throttle_take(items: list, allowance: int) -> tuple[list, list]:
    """목록을 (지금 보낼, 나중에) 로 나눔. allowance 개만 지금."""
    n = max(0, int(allowance))
    return items[:n], items[n:]


def market_send_allowance(session, market: str, sent_last_minute: int) -> int:
    """정책을 읽어 이 마켓의 현재 허용량. enabled 아니거나 정책 없으면 0."""
    from lemouton.pricing.settings import get_market_policies
    pol = get_market_policies(session).get(market)
    if not pol or not pol.get("enabled"):
        return 0
    return upload_allowance(pol["per_minute"], sent_last_minute)
```

- [ ] **Step 4: 통과 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/uploader/test_upload_throttle.py -v`
Expected: PASS (5 passed)

- [ ] **Step 5: 회귀 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/pricing tests/uploader tests/sources -q`
Expected: 전부 PASS

- [ ] **Step 6: 커밋**

```bash
git add 프로그램/_시스템/lemouton/uploader/throttle.py 프로그램/_시스템/tests/uploader/test_upload_throttle.py
git commit -m "feat(uploader): 업로드 속도제한 계산기 + 마켓 허용량 market_send_allowance"
```

---

## 완료 기준 (P4)

- [ ] `MarketUploadPolicy` 저장·조회, coupang·smartstore 기본값 시드, 음수 클램프, enabled 토글
- [ ] `upload_allowance`·`throttle_take` 순수 계산 정확(초과 시 0, 분할)
- [ ] `market_send_allowance` 가 정책 읽어 허용량, 미등록/비활성 마켓 = 0(안전)
- [ ] 회귀 통과(pricing·uploader·sources)

## 다음(P4b — 실제 전송 배선, dry-run 게이트)

- 전송 사이클: `changed_product_ids_since`(P1) → 변동 URL이 걸린 마켓별 대상 옵션 수집 → 오른쪽 카드 규칙(`autosend_mode` preview/real·`autosend_on_stock/price`·`autosend_stock_threshold`) 적용 → 마켓별 `market_send_allowance` 만큼 `throttle_take` 로 지금분만 전송(`uploader/runtime.py` 어댑터, `MOUM_LIVE_UPLOAD` 기본 OFF=dry-run) → 전송 기록(최근1분 카운트용).
- "최근 1분 전송 수" 소스(전송 로그/ChannelChangeEvent)는 P4b 에서 확정.
