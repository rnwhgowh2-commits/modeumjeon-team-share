# 자동화 실행 엔진 P3a — 크롤 큐 API + 실행/정지 상태 (서버측) 구현 플랜

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 로컬 크롤러(확장, P3b)가 "지금 긁을 URL 목록"과 "실행/정지 상태"를 물어볼 **서버측 창구**를 만든다. 서버는 **목록만 알려줄 뿐 크롤하지 않는다**(크롤=로컬 원칙 유지). 실제 크롤 루프는 P3b(확장, 라이브 검증).

**Architecture:** P2의 `select_due_products` 위에 (1) 실행/정지 게이트(`crawl_auto_enabled`)와 직렬화를 얹은 서비스 함수 `due_crawl_payload`, (2) 그걸 JSON 으로 내주는 읽기전용 라우트 `GET /api/crawl/queue`. 크롤·쓰기 없음(SELECT only). 시각은 라우트가 주입.

**Tech Stack:** Flask(blueprint `api`, `/api` prefix, `SessionLocal`), SQLAlchemy, pytest(서비스=in-memory `db` 픽스처 / 라우트=test_client + `Base.metadata.create_all`).

**대상 워크트리:** `C:\dev\모음전 프로젝트\_wt_auto_engine` (브랜치 `feature/automation-engine`)

**스펙:** [`docs/superpowers/specs/2026-07-04-자동화-연속배수큐-실행엔진-design.md`](../specs/2026-07-04-자동화-연속배수큐-실행엔진-design.md) §5.2, §5.6 (실행/정지)

**경로 표기:** 이하 `프로그램/_시스템/...` 생략.

**전제(P1·P2 완료)**: `lemouton/sources/crawl_schedule.py` 에 `select_due_products(session, *, now)`·`base_crawl_interval_seconds(session)`·`due_products(...)`. `GlobalSettings.crawl_auto_enabled`(실행/정지, 기본 False), `get_or_init(session)`. `SourceProduct` 필드: `id·site·url·crawl_weight·no_change_streak·last_fetched_at`.

**원칙 못박기:** 이 라우트는 **읽기 전용**이며 소싱처 사이트에 절대 접속하지 않는다. 서버 크롤 부활 아님.

---

## File Structure

- **Modify** `lemouton/sources/crawl_schedule.py` — `due_crawl_payload(session, *, now)` 추가(실행/정지 게이트 + 직렬화)
- **Modify** `webapp/routes/api.py` — 읽기전용 라우트 `GET /api/crawl/queue`
- **Create** `tests/sources/test_crawl_queue_payload.py` — 서비스 함수 테스트(in-memory db 픽스처)
- **Create** `tests/sourcing/test_crawl_queue_endpoint.py` — 라우트 테스트(test_client)

---

## Task 1: 서비스 — due_crawl_payload (실행/정지 게이트 + 직렬화)

**Files:**
- Modify: `lemouton/sources/crawl_schedule.py`
- Test: `tests/sources/test_crawl_queue_payload.py` (신규, `tests/sources/conftest.py` 의 `db` 픽스처 자동 사용)

- [ ] **Step 1: 실패 테스트 작성**

`tests/sources/test_crawl_queue_payload.py` 생성:

```python
from datetime import datetime

from lemouton.sources.crawl_schedule import due_crawl_payload
from lemouton.sources.models import SourceProduct
from lemouton.pricing.settings import get_or_init

NOW = datetime(2026, 7, 4, 12, 0, 0)


def _enable(db, on=True, hours=6, minutes=0):
    s = get_or_init(db)
    s.crawl_auto_enabled = on
    s.crawl_interval_hours = hours
    s.crawl_interval_minutes = minutes
    db.flush()


def _sp(db, url, *, last=None, weight=1):
    sp = SourceProduct(site="musinsa", url=url, crawl_weight=weight,
                       no_change_streak=0, last_fetched_at=last)
    db.add(sp); db.flush()
    return sp


def test_disabled_returns_empty_and_flag(db):
    _enable(db, on=False)
    _sp(db, "u/never", last=None)  # due 지만 정지 상태
    out = due_crawl_payload(db, now=NOW)
    assert out["enabled"] is False
    assert out["items"] == [] and out["count"] == 0


def test_enabled_returns_due_items_with_fields(db):
    _enable(db, on=True)
    sp = _sp(db, "u/never", last=None, weight=3)  # 미크롤 → due
    out = due_crawl_payload(db, now=NOW)
    assert out["enabled"] is True
    assert out["count"] == 1
    item = out["items"][0]
    assert item["source_product_id"] == sp.id
    assert item["url"] == "u/never"
    assert item["site"] == "musinsa"
    assert item["crawl_weight"] == 3
    assert item["last_fetched_at"] is None
    assert out["base_interval_seconds"] == 6 * 3600


def test_last_fetched_at_serialized_iso(db):
    _enable(db, on=True)
    from datetime import timedelta
    _sp(db, "u/old", last=NOW - timedelta(hours=20))  # 크게 연체 → due
    out = due_crawl_payload(db, now=NOW)
    assert isinstance(out["items"][0]["last_fetched_at"], str)  # iso 문자열
```

- [ ] **Step 2: 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sources/test_crawl_queue_payload.py -v`
Expected: FAIL — `ImportError: cannot import name 'due_crawl_payload'`

- [ ] **Step 3: 구현** — `crawl_schedule.py` 에 추가

```python
def due_crawl_payload(session, *, now) -> dict:
    """로컬 크롤러(확장)가 폴링할 페이로드. 서버는 목록만 알려줄 뿐 크롤 안 함.

    실행/정지(crawl_auto_enabled)가 꺼져 있으면 빈 목록 + enabled=False.
    """
    from lemouton.pricing.settings import get_or_init
    s = get_or_init(session)
    base = base_crawl_interval_seconds(session)
    if not bool(s.crawl_auto_enabled):
        return {"enabled": False, "base_interval_seconds": base,
                "count": 0, "items": []}
    products = due_products(session, base_interval_seconds=base, now=now)
    items = [{
        "source_product_id": p.id,
        "site": p.site,
        "url": p.url,
        "crawl_weight": p.crawl_weight,
        "no_change_streak": p.no_change_streak,
        "last_fetched_at": p.last_fetched_at.isoformat() if p.last_fetched_at else None,
    } for p in products]
    return {"enabled": True, "base_interval_seconds": base,
            "count": len(items), "items": items}
```

- [ ] **Step 4: 통과 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sources/test_crawl_queue_payload.py -v`
Expected: PASS (3 passed)

- [ ] **Step 5: 커밋**

```bash
git add 프로그램/_시스템/lemouton/sources/crawl_schedule.py 프로그램/_시스템/tests/sources/test_crawl_queue_payload.py
git commit -m "feat(sources): 크롤 큐 페이로드 due_crawl_payload (실행/정지 게이트)"
```

---

## Task 2: 라우트 — GET /api/crawl/queue (읽기 전용)

**Files:**
- Modify: `webapp/routes/api.py` (blueprint `bp = Blueprint('api', __name__, url_prefix='/api')`)
- Test: `tests/sourcing/test_crawl_queue_endpoint.py` (신규)

- [ ] **Step 1: 실패 테스트 작성**

`tests/sourcing/test_crawl_queue_endpoint.py` 생성 (기존 `tests/pricing/test_integrity_endpoint.py` 의 client 픽스처 패턴 그대로):

```python
# -*- coding: utf-8 -*-
"""[TEST] GET /api/crawl/queue — 로컬 크롤러가 폴링하는 due 목록(읽기 전용)."""
import os
from datetime import datetime, timezone

import pytest

os.environ.setdefault("ENVIRONMENT", "test")


@pytest.fixture
def client():
    for _m in (
        "lemouton.sourcing.models", "lemouton.sourcing.models_pricing",
        "lemouton.sources.models", "lemouton.templates.models",
        "lemouton.inventory.models", "lemouton.pricing.settings",
    ):
        try:
            __import__(_m)
        except ImportError:
            pass
    from shared.db import Base, engine
    Base.metadata.create_all(engine)

    from flask import Flask
    from webapp.routes.api import bp
    app = Flask(__name__)
    app.register_blueprint(bp)
    app.config.update(TESTING=True)
    return app.test_client()


def _seed(enabled: bool):
    """SessionLocal 로 실행/정지 상태 + due 한 SourceProduct 1건 시드."""
    from shared.db import SessionLocal
    from lemouton.pricing.settings import get_or_init
    from lemouton.sources.models import SourceProduct
    s = SessionLocal()
    try:
        gs = get_or_init(s)
        gs.crawl_auto_enabled = enabled
        gs.crawl_interval_hours = 6
        gs.crawl_interval_minutes = 0
        # 미크롤(=반드시 due) URL 1건 (유니크 url 로 중복 시드 방지)
        url = "https://example.test/crawl-queue-seed"
        if not s.query(SourceProduct).filter_by(url=url).first():
            s.add(SourceProduct(site="musinsa", url=url, crawl_weight=1,
                                no_change_streak=0, last_fetched_at=None))
        s.commit()
    finally:
        s.close()


def test_enabled_returns_items(client):
    _seed(enabled=True)
    r = client.get("/api/crawl/queue")
    assert r.status_code == 200
    d = r.get_json()
    assert d["enabled"] is True
    assert d["count"] >= 1
    assert any(it["url"].endswith("crawl-queue-seed") for it in d["items"])


def test_disabled_returns_empty(client):
    _seed(enabled=False)
    r = client.get("/api/crawl/queue")
    assert r.status_code == 200
    d = r.get_json()
    assert d["enabled"] is False
    assert d["items"] == []
```

- [ ] **Step 2: 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sourcing/test_crawl_queue_endpoint.py -v`
Expected: FAIL — 404 (route not defined) → assertion error on status_code.

- [ ] **Step 3: 구현** — `webapp/routes/api.py` 에 라우트 추가 (파일 안 아무 라우트 정의 구역, 다른 `@bp.route` 들 근처)

```python
@bp.route('/crawl/queue')
def crawl_queue():
    """[읽기전용] 로컬 크롤러(확장)가 폴링하는 '지금 긁을 URL' 목록 + 실행/정지.

    서버는 목록만 알려줄 뿐 소싱처에 접속하지 않는다(크롤=로컬 원칙).
    """
    from datetime import datetime, timezone
    from lemouton.sources.crawl_schedule import due_crawl_payload
    s = SessionLocal()
    try:
        now = datetime.now(timezone.utc).replace(tzinfo=None)  # naive UTC
        return jsonify(due_crawl_payload(s, now=now))
    finally:
        s.close()
```

(`jsonify`·`SessionLocal` 은 파일 상단에 이미 import 돼 있음 — 확인만.)

- [ ] **Step 4: 통과 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sourcing/test_crawl_queue_endpoint.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: 회귀 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sources tests/sourcing tests/pricing -q`
Expected: 전부 PASS

- [ ] **Step 6: 커밋**

```bash
git add 프로그램/_시스템/webapp/routes/api.py 프로그램/_시스템/tests/sourcing/test_crawl_queue_endpoint.py
git commit -m "feat(api): GET /api/crawl/queue — 로컬 크롤러 폴링용 due 목록(읽기전용)"
```

---

## 완료 기준 (P3a)

- [ ] `due_crawl_payload` 가 실행(enabled)일 때만 due 목록, 정지면 빈 목록 + enabled=False
- [ ] 항목에 source_product_id·site·url·crawl_weight·no_change_streak·last_fetched_at(iso|null)
- [ ] `GET /api/crawl/queue` 200 · JSON · 읽기 전용(쓰기·크롤 없음)
- [ ] 회귀 통과(tests/sources·sourcing·pricing)

## 다음(P3b — 별도, 라이브 검증 필수)

- 크롬 확장(moum-crawler) 배경에 워커 루프: `GET /api/crawl/queue` 폴링 → enabled면 items 를 **소싱처별 동시 상한**으로 로컬 크롤 → 기존 `POST /api/sources/crawl-result` 로 결과 업로드(→ P1이 last_fetched_at·CrawlDelta 갱신 → 다음 폴링 때 자동 순환).
- ⚠️ P2 유효간격 0(기준0)이면 항상 due → **확장의 소싱처별 동시 상한이 사이트 보호의 핵심**(load-bearing). P3b에서 반드시 구현.
- P3b 는 확장 JS 라 pytest 불가 → `/live-browser-verify` 로 실제 크롤 실시간 감시 검증.
