# P1 · 소싱처 추가·업데이트 카드 + 큐 (웹/데이터) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 크롤링 가이드에 "소싱처 추가·업데이트" 4번째 카드를 신설하여, 사용자가 소싱처명 + URL(다중)만 입력하면 SourceRegistry 행이 생성되어 전체보기에 즉시 등장하고 "분석 대기" 큐에 식별되도록 한다. (Claude 실행·검증은 P2, 강제 가드는 P3.)

**Architecture:** 기존 `SourceRegistry`(전체보기·지도·카드의 단일 표) + `crawl_guide` JSON(sample_urls 재사용)에 얹는다. 새 테이블 없음 — 큐 = "sample_urls는 있는데 가이드 카드가 빈(또는 `update_requested` 플래그가 선) SourceRegistry 행". 과거이력 패널은 기존 `webapp/static/error_catalog.json`을 읽기 전용으로 표시.

**Tech Stack:** Flask(Blueprint `sourcing_guide`, url_prefix `/sourcing-guide`) · Jinja 템플릿 · 순수 JS(no framework) · pytest(`프로그램/_시스템`에서 실행) · `lemouton.sourcing.crawl_guide`(JSON 스키마 검증).

**작업 디렉토리:** `C:\dev\모음전 프로젝트\.worktrees\source-add-card` (origin/main 기반). 모든 경로는 이 워크트리 루트 기준. 파이썬 테스트는 `프로그램/_시스템` 에서 `python -m pytest` 로 실행.

---

## File Structure

| 파일 | 책임 | 변경 |
|---|---|---|
| `프로그램/_시스템/lemouton/sourcing/crawl_guide.py` | 카드 JSON 스키마 — `update_requested` 필드 추가 | Modify |
| `프로그램/_시스템/webapp/routes/sourcing_guide.py` | `/add` 페이지 + 생성/업데이트요청/큐 API | Modify |
| `프로그램/_시스템/webapp/templates/sourcing_guide/add.html` | 2탭 패널(신규/기존) + URL 에디터 + 과거이력 패널 | Create |
| `프로그램/_시스템/webapp/templates/sourcing_guide/overview.html` | 4번째 카드 + 전체보기 "분석 대기" 배지 | Modify |
| `프로그램/_시스템/tests/sourcing/test_source_add_card.py` | 스키마·생성·업데이트요청·큐 단위 테스트 | Create |
| `docs/신규-소싱처-추가-가이드.md` | 옛 SOP 문서(라이브 정독으로 대체) | Delete |

---

## Task 1: crawl_guide 스키마에 `update_requested` 필드 추가

**Files:**
- Modify: `프로그램/_시스템/lemouton/sourcing/crawl_guide.py` (empty_skeleton ~190, validate_guide ~218, dumps 끝부분)
- Test: `프로그램/_시스템/tests/sourcing/test_source_add_card.py`

- [ ] **Step 1: 실패 테스트 작성**

`프로그램/_시스템/tests/sourcing/test_source_add_card.py` 생성:

```python
from lemouton.sourcing import crawl_guide as cg


def test_skeleton_has_update_requested_none():
    sk = cg.empty_skeleton()
    assert sk["update_requested"] is None


def test_validate_keeps_update_requested():
    g = cg.empty_skeleton()
    g["update_requested"] = {"at": "2026-06-29T00:00:00+00:00", "note": "재고 둔갑"}
    out = cg.validate_guide(g)
    assert out["update_requested"]["note"] == "재고 둔갑"
    assert out["update_requested"]["at"] == "2026-06-29T00:00:00+00:00"


def test_validate_rejects_bad_update_requested():
    g = cg.empty_skeleton()
    g["update_requested"] = "nope"          # dict 아님 → None 으로 정제
    out = cg.validate_guide(g)
    assert out["update_requested"] is None
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sourcing/test_source_add_card.py -v`
Expected: FAIL — `KeyError: 'update_requested'` (empty_skeleton 에 키 없음)

- [ ] **Step 3: empty_skeleton 에 필드 추가**

`crawl_guide.py` `empty_skeleton()` 의 반환 dict 에서 `"updated_at": None,` **위에** 추가:

```python
        "exclude_keywords": [],
        "verification": {"lead_cache": None, "last_new_check": None, "examples": [],
                         "saved_checks": [], "checklist": default_checklist()},
        "update_requested": None,
        "updated_at": None,
    }
```

- [ ] **Step 4: validate_guide 에 정제 로직 추가**

`crawl_guide.py` `validate_guide()` 의 `out["updated_at"] = data.get("updated_at")` **바로 위에** 추가:

```python
    ur = data.get("update_requested")
    if isinstance(ur, dict):
        out["update_requested"] = {
            "at": str(ur.get("at", "")) or None,
            "note": str(ur.get("note", ""))[:200],
        }
    else:
        out["update_requested"] = None
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sourcing/test_source_add_card.py -v`
Expected: PASS (3 passed)

- [ ] **Step 6: 회귀 — 기존 크롤가이드 테스트**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sourcing/test_crawl_guide.py -v`
Expected: PASS (기존 테스트 전부 통과 — 필드 추가는 하위호환)

- [ ] **Step 7: 커밋**

```bash
git add 프로그램/_시스템/lemouton/sourcing/crawl_guide.py 프로그램/_시스템/tests/sourcing/test_source_add_card.py
git commit -m "feat(guide): crawl_guide 카드에 update_requested 필드 추가"
```

---

## Task 2: 신규 소싱처 생성 API (소싱처명 + URL 다중)

소싱처명 + URL 목록을 받아 `SourceRegistry` 행을 만들고 `crawl_guide.sample_urls` 에 URL을 저장한다. 첫 URL은 `is_lead=true`. 저장 즉시 전체보기에 "미정의·N개"로 등장한다.

**Files:**
- Modify: `프로그램/_시스템/webapp/routes/sourcing_guide.py` (헬퍼 `_save_guide`, 라우트 추가)
- Test: `프로그램/_시스템/tests/sourcing/test_source_add_card.py`

- [ ] **Step 1: 실패 테스트 작성**

`test_source_add_card.py` 에 추가:

```python
import json
from shared.db import SessionLocal
from lemouton.sourcing.models_pricing import SourceRegistry


def _cleanup(name):
    s = SessionLocal()
    try:
        for r in s.query(SourceRegistry).filter_by(name=name).all():
            s.delete(r)
        s.commit()
    finally:
        s.close()


def test_add_source_creates_registry_and_urls(client):
    _cleanup("테스트29")
    resp = client.post("/sourcing-guide/api/add-source", json={
        "name": "테스트29",
        "urls": ["https://example.com/p/1", "https://example.com/p/2"],
    })
    assert resp.status_code == 200
    data = resp.get_json()
    assert data["ok"] is True
    s = SessionLocal()
    try:
        row = s.query(SourceRegistry).filter_by(name="테스트29").first()
        assert row is not None
        guide = json.loads(row.crawl_guide)
        assert [u["url"] for u in guide["sample_urls"]] == \
            ["https://example.com/p/1", "https://example.com/p/2"]
        assert guide["sample_urls"][0]["is_lead"] is True
    finally:
        s.close()
    _cleanup("테스트29")


def test_add_source_rejects_empty_name(client):
    resp = client.post("/sourcing-guide/api/add-source", json={"name": "", "urls": []})
    assert resp.status_code == 400
```

> `client` 픽스처는 `tests/sourcing/conftest.py` 에 있다고 가정. 없으면 Step 1b 로 추가.

- [ ] **Step 1b: client 픽스처 확인/추가**

Run: `cd 프로그램/_시스템 && grep -n "def client" tests/sourcing/conftest.py`
없으면 `conftest.py` 에 추가:

```python
import pytest
from webapp import create_app   # 실제 앱 팩토리 경로 확인 후 사용


@pytest.fixture
def client():
    app = create_app()
    app.config.update(TESTING=True)
    with app.test_client() as c:
        yield c
```

> 앱 팩토리 이름이 다르면(`webapp.app:app` 등) 실제 import 경로로 교체. `cd 프로그램/_시스템 && grep -rn "Flask(__name__)\|create_app" webapp/__init__.py webapp/app.py` 로 확인.

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sourcing/test_source_add_card.py::test_add_source_creates_registry_and_urls -v`
Expected: FAIL — 404 (라우트 없음)

- [ ] **Step 3: 헬퍼 + 라우트 구현**

`sourcing_guide.py` 의 `_source()` 함수 **아래에** 헬퍼 추가:

```python
def _save_guide(s, src, guide: dict) -> None:
    """검증 후 SourceRegistry.crawl_guide 에 직렬화 저장."""
    src.crawl_guide = cg.dumps(guide)
    s.commit()
```

`overview()` 라우트 **아래에** 추가:

```python
@bp.post("/api/add-source")
def api_add_source():
    """신규 소싱처: 이름 + URL 다중 → SourceRegistry 생성 + sample_urls 저장.
    저장 즉시 전체보기에 '미정의'로 등장하고 '분석 대기' 큐에 잡힌다."""
    data = request.get_json(silent=True) or {}
    name = (data.get("name") or "").strip()
    urls = [u.strip() for u in (data.get("urls") or []) if str(u).strip()]
    if not name:
        return jsonify(ok=False, error="소싱처 이름을 입력하세요."), 400
    if len(name) > 64:
        return jsonify(ok=False, error="이름은 64자 이내."), 400
    s = SessionLocal()
    try:
        if s.query(SourceRegistry).filter_by(name=name).first():
            return jsonify(ok=False, error=f"'{name}' 은 이미 등록된 소싱처에요."), 400
        src = SourceRegistry(name=name, sort_order=s.query(SourceRegistry).count())
        s.add(src)
        s.flush()                       # src.id 확보
        guide = cg.empty_skeleton()
        guide["sample_urls"] = [
            {"url": u, "is_lead": (i == 0)} for i, u in enumerate(urls)
        ]
        guide["updated_at"] = _now_iso()
        _save_guide(s, src, guide)
        return jsonify(ok=True, id=src.id, name=src.name,
                       url_count=len(guide["sample_urls"]))
    finally:
        s.close()
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sourcing/test_source_add_card.py -v`
Expected: PASS (신규 2개 포함 전부 통과)

- [ ] **Step 5: 커밋**

```bash
git add 프로그램/_시스템/webapp/routes/sourcing_guide.py 프로그램/_시스템/tests/sourcing/test_source_add_card.py
git commit -m "feat(guide): 신규 소싱처 생성 API (이름+URL다중 → SourceRegistry+sample_urls)"
```

---

## Task 3: URL 편집 + 기존 업데이트 요청 API

기존 소싱처의 URL 추가/수정/삭제 + "크롤 업데이트 요청"(update_requested 플래그)을 한 엔드포인트로 처리한다.

**Files:**
- Modify: `프로그램/_시스템/webapp/routes/sourcing_guide.py`
- Test: `프로그램/_시스템/tests/sourcing/test_source_add_card.py`

- [ ] **Step 1: 실패 테스트 작성**

`test_source_add_card.py` 에 추가:

```python
def _make_source(name, urls):
    s = SessionLocal()
    try:
        src = SourceRegistry(name=name, sort_order=999)
        s.add(src); s.flush()
        g = cg.empty_skeleton()
        g["sample_urls"] = [{"url": u, "is_lead": i == 0} for i, u in enumerate(urls)]
        src.crawl_guide = cg.dumps(g)
        s.commit()
        return src.id
    finally:
        s.close()


def test_save_urls_replaces_list(client):
    _cleanup("테스트URL")
    sid = _make_source("테스트URL", ["https://a.com/1"])
    resp = client.post(f"/sourcing-guide/api/{sid}/save-urls", json={
        "urls": ["https://a.com/2", "https://a.com/3"]})
    assert resp.status_code == 200
    s = SessionLocal()
    try:
        g = json.loads(s.query(SourceRegistry).get(sid).crawl_guide)
        assert [u["url"] for u in g["sample_urls"]] == ["https://a.com/2", "https://a.com/3"]
    finally:
        s.close()
    _cleanup("테스트URL")


def test_request_update_sets_flag(client):
    _cleanup("테스트UPD")
    sid = _make_source("테스트UPD", ["https://a.com/1"])
    resp = client.post(f"/sourcing-guide/api/{sid}/request-update", json={"note": "재고 둔갑"})
    assert resp.status_code == 200
    s = SessionLocal()
    try:
        g = json.loads(s.query(SourceRegistry).get(sid).crawl_guide)
        assert g["update_requested"]["note"] == "재고 둔갑"
    finally:
        s.close()
    _cleanup("테스트UPD")
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sourcing/test_source_add_card.py::test_save_urls_replaces_list tests/sourcing/test_source_add_card.py::test_request_update_sets_flag -v`
Expected: FAIL — 404

- [ ] **Step 3: 두 라우트 구현**

`sourcing_guide.py` `api_add_source` **아래에** 추가:

```python
@bp.post("/api/<int:sid>/save-urls")
def api_save_urls(sid: int):
    """기존 소싱처 sample_urls 전체 교체(추가/수정/삭제 결과 리스트). 첫 URL=대표."""
    data = request.get_json(silent=True) or {}
    urls = [u.strip() for u in (data.get("urls") or []) if str(u).strip()]
    s = SessionLocal()
    try:
        src = s.query(SourceRegistry).get(sid)
        if not src:
            return jsonify(ok=False, error="소싱처를 찾을 수 없어요."), 404
        guide = cg.loads(src.crawl_guide)
        guide["sample_urls"] = [{"url": u, "is_lead": (i == 0)} for i, u in enumerate(urls)]
        guide["updated_at"] = _now_iso()
        _save_guide(s, src, guide)
        return jsonify(ok=True, url_count=len(urls))
    finally:
        s.close()


@bp.post("/api/<int:sid>/request-update")
def api_request_update(sid: int):
    """기존 소싱처 크롤 업데이트 요청 — update_requested 플래그 설정(=업데이트 대기 큐)."""
    data = request.get_json(silent=True) or {}
    note = (data.get("note") or "").strip()
    s = SessionLocal()
    try:
        src = s.query(SourceRegistry).get(sid)
        if not src:
            return jsonify(ok=False, error="소싱처를 찾을 수 없어요."), 404
        guide = cg.loads(src.crawl_guide)
        guide["update_requested"] = {"at": _now_iso(), "note": note}
        _save_guide(s, src, guide)
        return jsonify(ok=True)
    finally:
        s.close()
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sourcing/test_source_add_card.py -v`
Expected: PASS (전부)

- [ ] **Step 5: 커밋**

```bash
git add 프로그램/_시스템/webapp/routes/sourcing_guide.py 프로그램/_시스템/tests/sourcing/test_source_add_card.py
git commit -m "feat(guide): URL 저장 + 기존 업데이트 요청(update_requested) API"
```

---

## Task 4: 분석 대기 큐 식별 헬퍼 + API

큐 = ⓐ sample_urls≥1 이면서 모든 fields.status 가 ok 가 아닌(=미작성) 신규 대기 + ⓑ update_requested 가 선 업데이트 대기. 별도 테이블 없음.

**Files:**
- Modify: `프로그램/_시스템/webapp/routes/sourcing_guide.py`
- Test: `프로그램/_시스템/tests/sourcing/test_source_add_card.py`

- [ ] **Step 1: 실패 테스트 작성**

`test_source_add_card.py` 에 추가:

```python
def test_queue_lists_pending(client):
    _cleanup("테스트큐")
    sid = _make_source("테스트큐", ["https://a.com/1"])   # 카드 빈 + URL 있음 = 신규 대기
    resp = client.get("/sourcing-guide/api/queue")
    assert resp.status_code == 200
    items = resp.get_json()["items"]
    hit = [it for it in items if it["id"] == sid]
    assert hit and hit[0]["kind"] == "new"
    _cleanup("테스트큐")
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sourcing/test_source_add_card.py::test_queue_lists_pending -v`
Expected: FAIL — 404

- [ ] **Step 3: 헬퍼 + 라우트 구현**

`sourcing_guide.py` 에 헬퍼(파일 함수 영역) + 라우트 추가:

```python
def _guide_is_blank(guide: dict) -> bool:
    """카드가 미작성인가 — 6항목 중 status=ok 가 하나도 없으면 미작성."""
    return not any(f.get("status") == "ok" for f in guide.get("fields", {}).values())


def _queue_items():
    """분석/업데이트 대기 목록. 신규(빈 카드+URL) + 업데이트(update_requested)."""
    out = []
    for src in _sources():
        guide = cg.loads(src.crawl_guide)
        has_url = len(guide.get("sample_urls", [])) > 0
        if guide.get("update_requested"):
            out.append({"id": src.id, "name": src.name, "kind": "update",
                        "note": guide["update_requested"].get("note", ""),
                        "url_count": len(guide.get("sample_urls", []))})
        elif has_url and _guide_is_blank(guide):
            out.append({"id": src.id, "name": src.name, "kind": "new",
                        "note": "", "url_count": len(guide["sample_urls"])})
    return out


@bp.get("/api/queue")
def api_queue():
    return jsonify(ok=True, items=_queue_items())
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sourcing/test_source_add_card.py -v`
Expected: PASS (전부)

- [ ] **Step 5: 커밋**

```bash
git add 프로그램/_시스템/webapp/routes/sourcing_guide.py 프로그램/_시스템/tests/sourcing/test_source_add_card.py
git commit -m "feat(guide): 분석 대기 큐 식별 헬퍼+API (빈카드+URL / update_requested)"
```

---

## Task 5: `/add` 페이지 라우트 + add.html 템플릿 (2탭 + URL 에디터 + 과거이력 패널)

**Files:**
- Modify: `프로그램/_시스템/webapp/routes/sourcing_guide.py` (라우트)
- Create: `프로그램/_시스템/webapp/templates/sourcing_guide/add.html`

- [ ] **Step 1: `/add` 라우트 추가**

`sourcing_guide.py` `overview()` **아래에**(api_add_source 위 아무 곳) 추가:

```python
@bp.route("/add")
def add_page():
    """소싱처 추가·업데이트 카드 — 2탭(신규/기존) 페이지."""
    sources = [{"id": x.id, "name": x.name} for x in _sources()]
    return render_template("sourcing_guide/add.html",
                           active="sourcing_guide", sources=sources)
```

- [ ] **Step 2: add.html 생성**

`프로그램/_시스템/webapp/templates/sourcing_guide/add.html`:

```html
{% extends "base.html" %}
{% block title %}소싱처 추가·업데이트{% endblock %}
{% block content %}
<style>
  .sa{padding:48px 60px;max-width:980px;}
  .sa h2{font-size:28px;font-weight:800;margin-bottom:4px;}
  .sa .sub{color:#8B95A1;font-size:14px;margin-bottom:26px;}
  .sa .tabs{display:flex;border-bottom:2px solid #E5E8EB;margin-bottom:24px;}
  .sa .tab{padding:12px 20px;font-size:16px;font-weight:700;color:#8B95A1;cursor:pointer;background:none;border:none;}
  .sa .tab.on{color:#191F28;border-bottom:3px solid #191F28;margin-bottom:-2px;}
  .sa .pane{display:none;} .sa .pane.on{display:block;}
  .sa label{display:block;font-size:13px;font-weight:700;margin:16px 0 6px;}
  .sa input[type=text],.sa select{border:1px solid #d1d6db;border-radius:8px;padding:10px 12px;font-size:14px;width:100%;max-width:420px;}
  .sa .urlrow{display:flex;gap:6px;align-items:center;margin-bottom:8px;max-width:760px;}
  .sa .urlrow input{flex:1;max-width:none;}
  .sa .btn{border:1px solid #d1d6db;background:#fff;border-radius:7px;padding:9px 12px;font-size:13px;cursor:pointer;}
  .sa .btn.del{border-color:#ffc9c9;color:#e03131;}
  .sa .btn.add{border:1.5px dashed #adb5bd;color:#495057;font-weight:600;max-width:760px;width:100%;}
  .sa .btn.primary{background:#191F28;color:#fff;border:none;padding:12px 24px;font-weight:700;font-size:15px;margin-top:18px;}
  .sa .lead{font-size:11px;background:#FFE8CC;color:#D9480F;border-radius:6px;padding:3px 7px;font-weight:700;}
  .sa .hist{margin-top:30px;border:1px solid #ffe3b0;background:#FFFBF2;border-radius:12px;padding:16px 18px;}
  .sa .hist h3{font-size:14px;font-weight:800;margin-bottom:4px;}
  .sa .hist .hd{font-size:12px;color:#8B95A1;margin-bottom:10px;}
  .sa .ec{border:1px solid #E5E8EB;border-radius:8px;padding:10px;background:#fff;margin-bottom:8px;font-size:12.5px;line-height:1.6;}
  .sa .ec b{font-weight:800;}
  .sa .toast{margin-top:14px;font-size:13px;font-weight:700;display:none;}
</style>
<div class="sa">
  <h2>➕ 소싱처 추가·업데이트</h2>
  <div class="sub">소싱처명과 URL만 넣고 저장하면 전체보기에 바로 등장합니다. 이후 Claude Code에 "○○ 추가/업데이트해줘"라고 하면 됩니다.</div>

  <div class="tabs">
    <button class="tab on" data-pane="new">신규 소싱처 추가</button>
    <button class="tab" data-pane="upd">기존 소싱처 크롤 업데이트</button>
  </div>

  <!-- 신규 -->
  <div class="pane on" id="pane-new">
    <label>소싱처명</label>
    <input type="text" id="newName" placeholder="예: 29CM">
    <label>샘플 URL <span style="color:#8B95A1;font-weight:500;">(여러 개 · 첫 줄 = 대표)</span></label>
    <div id="newUrls"></div>
    <button class="btn add" type="button" onclick="addUrlRow('newUrls')">+ URL 추가</button>
    <br><button class="btn primary" type="button" onclick="submitNew()">📥 추가 + 분석 요청 담기</button>
    <div class="toast" id="newToast"></div>
    <div class="hist" id="histNew"></div>
  </div>

  <!-- 기존 -->
  <div class="pane" id="pane-upd">
    <label>소싱처 선택</label>
    <select id="updSel" onchange="loadUpd()">
      <option value="">— 선택 —</option>
      {% for s in sources %}<option value="{{ s.id }}">{{ s.name }}</option>{% endfor %}
    </select>
    <label>샘플 URL <span style="color:#8B95A1;font-weight:500;">(추가/수정/삭제)</span></label>
    <div id="updUrls"></div>
    <button class="btn add" type="button" onclick="addUrlRow('updUrls')">+ URL 추가</button>
    <label>업데이트 사유(선택)</label>
    <input type="text" id="updNote" placeholder="예: 가격이 0으로 나옴 / 재고가 다 '있음'으로 둔갑">
    <br><button class="btn primary" type="button" onclick="submitUpd()">🔄 URL 저장 + 크롤 업데이트 요청</button>
    <div class="toast" id="updToast"></div>
    <div class="hist" id="histUpd"></div>
  </div>
</div>

<script>
function addUrlRow(boxId, val){
  var box=document.getElementById(boxId);
  var n=box.querySelectorAll('.urlrow').length;
  var row=document.createElement('div'); row.className='urlrow';
  row.innerHTML =
    (n===0?'<span class="lead">대표</span>':'<span style="width:34px"></span>')
    + '<input type="text" placeholder="https://..." value="'+(val||'')+'">'
    + '<button class="btn" type="button" onclick="openUrl(this)">↗ 바로가기</button>'
    + '<button class="btn del" type="button" onclick="this.closest(\'.urlrow\').remove()">🗑</button>';
  box.appendChild(row);
}
function openUrl(btn){
  var v=btn.closest('.urlrow').querySelector('input').value.trim();
  if(v) window.open(v,'_blank','noopener');
}
function collectUrls(boxId){
  return Array.from(document.querySelectorAll('#'+boxId+' input'))
    .map(function(i){return i.value.trim();}).filter(Boolean);
}
function toast(id,msg,ok){
  var t=document.getElementById(id); t.style.display='block';
  t.style.color = ok?'#0F9D58':'#e03131'; t.textContent=msg;
}
async function postJSON(url,body){
  var r=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});
  return {ok:r.ok, data:await r.json().catch(function(){return {};})};
}
async function submitNew(){
  var name=document.getElementById('newName').value.trim();
  var urls=collectUrls('newUrls');
  if(!name){toast('newToast','소싱처명을 입력하세요.',false);return;}
  var res=await postJSON('/sourcing-guide/api/add-source',{name:name,urls:urls});
  if(res.ok&&res.data.ok){
    toast('newToast','✓ "'+name+'" 추가됨 — 전체보기에 등장. 이제 Claude Code에 "'+name+' 추가해줘".',true);
    document.getElementById('newName').value=''; document.getElementById('newUrls').innerHTML=''; addUrlRow('newUrls');
  } else { toast('newToast', res.data.error||'실패', false); }
}
async function loadUpd(){
  var box=document.getElementById('updUrls'); box.innerHTML='';
  var sid=document.getElementById('updSel').value; if(!sid){return;}
  var r=await fetch('/sourcing-guide/'+sid+'/guide.json').then(function(x){return x.json();}).catch(function(){return null;});
  var urls=(r&&r.guide&&r.guide.sample_urls)||[];
  if(!urls.length){ addUrlRow('updUrls'); } else { urls.forEach(function(u){addUrlRow('updUrls',u.url);}); }
}
async function submitUpd(){
  var sid=document.getElementById('updSel').value;
  if(!sid){toast('updToast','소싱처를 선택하세요.',false);return;}
  var urls=collectUrls('updUrls');
  var note=document.getElementById('updNote').value.trim();
  await postJSON('/sourcing-guide/api/'+sid+'/save-urls',{urls:urls});
  var res=await postJSON('/sourcing-guide/api/'+sid+'/request-update',{note:note});
  if(res.ok&&res.data.ok){ toast('updToast','✓ 저장+업데이트 요청됨. 이제 Claude Code에 "이 소싱처 크롤 업데이트해줘".',true); }
  else { toast('updToast','실패',false); }
}
// 과거이력 읽기전용 패널 — 4철칙(rule) 항상 + 재고/가격 대표 재발 패턴
async function renderHistory(){
  var items=await fetch('/static/error_catalog.json').then(function(r){return r.json();}).then(function(j){return j.items||[];}).catch(function(){return [];});
  var rules=items.filter(function(it){return it.rule;});
  var top=items.filter(function(it){return it.id && it.st==='ok';}).slice(0,4);
  var html='<h3>⚠️ 과거 이력 참고 (읽기 전용 · 🤖 Claude가 자동 판정)</h3>'
    +'<div class="hd">소싱처 이름과 무관하게 같은 수집방식·공유코드면 Claude가 자동 참고합니다. 아래는 미리보기.</div>';
  rules.forEach(function(r){html+='<div class="ec"><b>철칙 · '+r.sy+'</b><br>'+(r.cz||'')+'</div>';});
  top.forEach(function(t){html+='<div class="ec"><b>'+t.id+' · '+t.g+' · '+(t.src||'')+'</b> ✅<br>'+(t.sy||'')+'<br><span style="color:#11A36B">✔ '+(t.pv||'')+'</span></div>';});
  document.getElementById('histNew').innerHTML=html;
  document.getElementById('histUpd').innerHTML=html;
}
// 탭 전환 + 초기화
document.querySelectorAll('.sa .tab').forEach(function(t){
  t.addEventListener('click',function(){
    document.querySelectorAll('.sa .tab').forEach(function(x){x.classList.remove('on');});
    document.querySelectorAll('.sa .pane').forEach(function(x){x.classList.remove('on');});
    t.classList.add('on');
    document.getElementById('pane-'+t.dataset.pane).classList.add('on');
  });
});
addUrlRow('newUrls'); renderHistory();
</script>
{% endblock %}
```

> **참고**: `/sourcing-guide/<id>/guide.json`(기존 카드 JSON GET)은 origin/main 에 이미 존재(가이드 GET). 없으면 `grep -n "guide.json\|def.*guide" sourcing_guide.py` 로 실제 경로 확인 후 `loadUpd()` fetch URL 교체.

- [ ] **Step 3: 라이브 수동 검증 (live-browser-verify)**

배포 후 `mou-m.com/sourcing-guide/add` 접속 →
- 신규 탭: 소싱처명 "테스트삭제용" + URL 2개 입력 → "추가" → 토스트 성공 → `/sourcing-guide/` 전체보기에 행 등장(미정의·2개) 확인 → 행 삭제 정리.
- 기존 탭: 소싱처 선택 → URL 로드 → 저장+업데이트요청 → 토스트 성공.
- 과거이력 패널에 4철칙 + 에러카드 보임.

- [ ] **Step 4: 커밋**

```bash
git add 프로그램/_시스템/webapp/routes/sourcing_guide.py 프로그램/_시스템/webapp/templates/sourcing_guide/add.html
git commit -m "feat(guide): 소싱처 추가·업데이트 /add 2탭 페이지 + URL에디터 + 과거이력 패널"
```

---

## Task 6: 4번째 카드 + 전체보기 "분석 대기" 배지

**Files:**
- Modify: `프로그램/_시스템/webapp/templates/sourcing_guide/overview.html`

- [ ] **Step 1: 런처 그리드 4칸으로 + 4번째 카드 추가**

`overview.html` line 146 `.sg-launch{grid-template-columns:repeat(3,1fr);...}` 를 `repeat(4,1fr)` 로 변경. line 157 의 미디어쿼리도 동일 파일 내라 유지.

`.sg-launch` 블록(line 173~176 크롤러설치 카드) **다음에** 4번째 카드 추가:

```html
    <a class="lc c-purple" href="/sourcing-guide/add">
      <div class="lc-ic">➕</div><div class="lc-t">소싱처 추가·업데이트</div>
      <div class="lc-d">신규 소싱처 추가 · 기존 소싱처 크롤 업데이트 — 이름·URL만 넣으면 Claude가 가이드대로 구현</div>
      <span class="lc-tag p">🛠 팀원 · 등록</span></a>
```

- [ ] **Step 2: 전체보기 표에 "분석 대기" 배지**

`overview()` 라우트(`sourcing_guide.py`)가 대기 여부를 같이 넘기도록 수정 — `rows.append(...)` 를:

```python
    for src in _sources():
        guide = cg.loads(src.crawl_guide)
        pending = bool(guide.get("update_requested")) or \
            (len(guide.get("sample_urls", [])) > 0 and _guide_is_blank(guide))
        rows.append({"id": src.id, "name": src.name, "guide": guide, "pending": pending})
```

`overview.html` 의 소싱처명 셀(line 185)을 수정:

```html
          <td class="sg-name">{{ r.name }}{% if r.pending %} <span class="sg-chip" style="background:#FFF3BF;color:#B5870A;">분석 대기</span>{% endif %}</td>
```

- [ ] **Step 3: 라이브 수동 검증**

`mou-m.com/sourcing-guide/` → 카드 4개 나란히(➕ 소싱처 추가·업데이트 포함) · Task5에서 만든 대기 소싱처에 "분석 대기" 배지 보임.

- [ ] **Step 4: 커밋**

```bash
git add 프로그램/_시스템/webapp/templates/sourcing_guide/overview.html 프로그램/_시스템/webapp/routes/sourcing_guide.py
git commit -m "feat(guide): 전체보기 4번째 카드(소싱처 추가·업데이트) + 분석 대기 배지"
```

---

## Task 7: 옛 지침서 삭제 + 끊긴 참조 정리

**Files:**
- Delete: `docs/신규-소싱처-추가-가이드.md`
- Modify: `프로그램/_시스템/webapp/templates/sourcing_guide/overview.html` (참조 문구)

- [ ] **Step 1: 루트 중복 사본 없음 확인**

Run: `ls docs/크롤링-가이드.md 2>/dev/null && echo "있음(삭제대상)" || echo "없음(정상 — 정본은 _시스템/docs)"`
Expected: "없음" (origin/main 기준 루트에 중복 없음). 만약 "있음"이면 `git rm "docs/크롤링-가이드.md"`.

- [ ] **Step 2: 끊길 참조 위치 확인**

Run: `grep -rn "신규-소싱처-추가-가이드.md" 프로그램/_시스템/webapp/templates/ 프로그램/_시스템/webapp/routes/`
→ 나오는 각 위치의 문구를 라이브 정본 안내로 교체. overview.html 의 `정식 절차 문서: docs/신규-소싱처-추가-가이드.md` 류 → `정식 절차: 라이브 크롤링 가이드(/sourcing-guide/map 보기 · /sourcing-guide/map.md 원문)` 로 변경.

- [ ] **Step 3: 옛 SOP 문서 삭제**

```bash
git rm "docs/신규-소싱처-추가-가이드.md"
```

- [ ] **Step 4: 전체 회귀 테스트**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sourcing/ -q`
Expected: PASS (삭제·참조변경이 테스트 깨지 않음)

- [ ] **Step 5: 커밋**

```bash
git add -A
git commit -m "chore(guide): 옛 SOP 문서 삭제 + 참조를 라이브 정본(/sourcing-guide/map)으로 정리"
```

---

## Self-Review 결과 (작성자 점검)

- **스펙 커버리지**: §4.1 카드·URL에디터·바로가기(T5,T6) · §4.2 큐(T4) · §4.3 SourceRegistry 통일(T2) · §4.4 과거이력 읽기전용 패널(T5) · §6 삭제(T7). **P1 범위 내 전부 태스크 존재.** (스테이지 SOP=§4.6·강제가드=§5.1·완료게이트=§5 는 **P2·P3 범위** — 의도적 제외.)
- **타입 일관성**: `_save_guide`·`_guide_is_blank`·`_queue_items` 시그니처 태스크 간 일치. `update_requested` 스키마(T1) ↔ 사용(T3·T4·T6) 일치. API 경로 `/sourcing-guide/api/...` 전부 일치.
- **플레이스홀더**: 없음. (단 2곳 "실제 경로 확인 후 교체" 안내 = client 픽스처 import·guide.json GET 경로 — origin/main 실제값 확인 스텝 포함.)

## 미해결·다음 플랜
- **P2**: "○○ 추가/업데이트해줘" → Claude가 큐(`/api/queue`) 읽어 스테이지(S0~S6/U0~U4)·라이브 가이드 정독·자동 OK/고쳐·에러이력 축적. (실행 스킬)
- **P3**: 에러이력 미기재 차단(pre-commit/CI) + 완료 게이트(drift).
