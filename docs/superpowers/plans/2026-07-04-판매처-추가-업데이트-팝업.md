# 판매처 추가·업데이트 팝업 + 데이터 코드 지도 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 판매처 관리(`/accounts/upload`)에 소싱처와 동일한 매뉴얼 2개(➕판매처 추가·업데이트 위저드 팝업 · 📖데이터 코드 지도)를 얹는다.

**Architecture:** 소싱처 가이드(`sourcing_guide`)를 미러링한 새 블루프린트 `marketplace_guide`. `/add`·`/map` 을 `?bare=1` 로 열면 same-origin iframe 팝업용 최소 레이아웃 + `X-Frame-Options: SAMEORIGIN`. 진입은 `accounts/upload.html` 상단 카드 버튼 + 기존 `_page_modal.html`(iframe 모달) 재사용. 위저드는 DB 무기록 — 순수 클라이언트 프롬프트 생성.

**Tech Stack:** Flask Blueprint · Jinja2(`{% extends layout|default("base.html") %}`) · 바닐라 JS · pytest(Flask test client)

**Spec:** `docs/superpowers/specs/2026-07-04-판매처-추가-업데이트-팝업-design.md`
**Design mockup(승인):** 바탕화면 `모음전 판매처추가팝업 미니멀 시안 v2.html` 시안 #4(좌측 세로 레일) — 위저드 CSS·마크업 원본.

---

## File Structure

| 유닛 | 파일 | 책임 |
|---|---|---|
| 라우트 | `webapp/routes/marketplace_guide.py` (신규) | `/add`·`/map` (bare→SAMEORIGIN), MARKET_METADATA 목록 주입 |
| 등록 | `webapp/routes/__init__.py` (수정) | 블루프린트 import+register |
| 추가 팝업 | `webapp/templates/marketplace_guide/add.html` (신규) | 좌측 레일 4단계 위저드 + 2탭 + 프롬프트 생성 JS |
| 지도 | `webapp/templates/marketplace_guide/map.html` (신규) | 판매처 데이터 코드 지도 |
| 진입 버튼 | `webapp/templates/accounts/upload.html` (수정) | 카드 버튼 2개 + `_page_modal.html` include |
| 테스트 | `tests/marketplace_guide/test_routes.py` 외 (신규) | 라우트·헤더·템플릿 내용 검증 |

기준 디렉터리: `프로그램/_시스템/`. 모든 경로는 이 아래 상대경로.

---

### Task 1: `marketplace_guide` 블루프린트 + `/add`·`/map` 라우트

**Files:**
- Create: `webapp/routes/marketplace_guide.py`
- Create: `webapp/templates/marketplace_guide/add.html` (Task 1은 최소 스텁 — 라우트가 렌더되게만)
- Create: `webapp/templates/marketplace_guide/map.html` (최소 스텁)
- Modify: `webapp/routes/__init__.py` (import 블록 ~line 95, register 블록 ~line 121)
- Test: `tests/marketplace_guide/test_routes.py`

- [ ] **Step 1: 실패 테스트 작성**

Create `tests/marketplace_guide/test_routes.py`:
```python
# -*- coding: utf-8 -*-
"""판매처 가이드 라우트 — 200 + bare 시 X-Frame-Options SAMEORIGIN."""
import pytest
from flask import Flask
from webapp.routes import marketplace_guide as mg


@pytest.fixture
def client(monkeypatch):
    monkeypatch.setenv("ENVIRONMENT", "test")   # _admin_only 우회
    app = Flask(__name__,
                template_folder="webapp/templates",  # 실제 템플릿 폴더
                root_path=__import__("pathlib").Path(mg.__file__).parents[2].as_posix())
    app.register_blueprint(mg.bp)
    return app.test_client()


def test_add_bare_sets_sameorigin(client):
    r = client.get("/marketplace-guide/add?bare=1")
    assert r.status_code == 200
    assert r.headers.get("X-Frame-Options") == "SAMEORIGIN"


def test_map_bare_sets_sameorigin(client):
    r = client.get("/marketplace-guide/map?bare=1")
    assert r.status_code == 200
    assert r.headers.get("X-Frame-Options") == "SAMEORIGIN"


def test_add_non_bare_renders(client):
    assert client.get("/marketplace-guide/add").status_code == 200
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/marketplace_guide/test_routes.py -v`
Expected: FAIL — `ModuleNotFoundError: webapp.routes.marketplace_guide`

- [ ] **Step 3: 라우트 구현**

Create `webapp/routes/marketplace_guide.py`:
```python
"""판매처 추가·업데이트 + 데이터 코드 지도 (판매처판).

소싱처 가이드(sourcing_guide)의 판매처 짝. /add·/map 을 ?bare=1 로 열면
same-origin iframe 팝업용 최소 레이아웃 + X-Frame-Options: SAMEORIGIN.
(전역 기본 X-Frame-Options: DENY 가 same-origin iframe 까지 막으므로 라우트에서 예외.)
"""
from __future__ import annotations

import os

from flask import Blueprint, render_template, request, make_response

bp = Blueprint("marketplace_guide", __name__, url_prefix="/marketplace-guide")


@bp.before_request
def _admin_only():
    if os.environ.get("ENVIRONMENT") != "team-share-dev":
        return None
    from webapp.auth.permissions import enforce_admin
    return enforce_admin()


def _markets():
    """MARKET_METADATA 단일 출처 — 신규 추가용(coming_soon) / 연동됨(ready) 분리."""
    from webapp.routes.accounts import MARKET_METADATA
    coming, ready = [], []
    for key, meta in sorted(MARKET_METADATA.items(),
                            key=lambda kv: kv[1].get("sort_order", 999)):
        row = {"key": key, "label": meta["label"], "icon": meta.get("icon", "🔧"),
               "api_type": meta.get("api_type", ""), "status": meta.get("status", "")}
        (ready if meta.get("status") == "ready" else coming).append(row)
    return coming, ready


@bp.route("/add")
def add_page():
    """판매처 추가·업데이트 — 2탭(신규/기존). ?bare=1 → 사이드바 없는 팝업 iframe용."""
    coming, ready = _markets()
    if request.args.get("bare"):
        resp = make_response(render_template(
            "marketplace_guide/add.html", layout="_bare.html", coming=coming, ready=ready))
        resp.headers["X-Frame-Options"] = "SAMEORIGIN"
        return resp
    return render_template("marketplace_guide/add.html",
                           layout="base.html", coming=coming, ready=ready)


@bp.route("/map")
def data_code_map():
    """데이터 코드 지도(판매처판). ?bare=1 → 팝업 iframe용 최소 레이아웃."""
    if request.args.get("bare"):
        resp = make_response(render_template("marketplace_guide/map.html", layout="_bare.html"))
        resp.headers["X-Frame-Options"] = "SAMEORIGIN"
        return resp
    return render_template("marketplace_guide/map.html", layout="base.html")
```

Create `webapp/templates/marketplace_guide/add.html` (Task 2에서 완성 — 지금은 스텁):
```html
{% extends layout|default("base.html") %}
{% block title %}판매처 추가·업데이트{% endblock %}
{% block content %}
<div class="ma"><h2>➕ 판매처 추가·업데이트</h2></div>
{% endblock %}
```

Create `webapp/templates/marketplace_guide/map.html` (Task 3에서 완성 — 스텁):
```html
{% extends layout|default("base.html") %}
{% block title %}데이터 코드 지도{% endblock %}
{% block content %}
<div class="mm"><h2>📖 데이터 코드 지도</h2></div>
{% endblock %}
```

Modify `webapp/routes/__init__.py` — import 블록(소싱처 줄 아래)에 추가:
```python
    from webapp.routes.sourcing_guide import bp as sourcing_guide_bp  # 소싱처 크롤링 가이드
    from webapp.routes.marketplace_guide import bp as marketplace_guide_bp  # 판매처 추가·데이터지도
```
register 블록(소싱처 줄 아래)에 추가:
```python
    app.register_blueprint(sourcing_guide_bp)  # 소싱처 크롤링 가이드
    app.register_blueprint(marketplace_guide_bp)  # 판매처 추가·데이터지도
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/marketplace_guide/test_routes.py -v`
Expected: PASS (3 passed)

- [ ] **Step 5: 커밋**

```bash
git add webapp/routes/marketplace_guide.py webapp/templates/marketplace_guide/ webapp/routes/__init__.py tests/marketplace_guide/test_routes.py
git commit -m "feat(marketplace-guide): /add·/map 라우트 + bare SAMEORIGIN + 블루프린트 등록"
```

---

### Task 2: `add.html` 위저드 (좌측 레일 4단계 + 2탭 + 프롬프트 생성)

**Files:**
- Modify: `webapp/templates/marketplace_guide/add.html` (스텁 → 완성)
- Test: `tests/marketplace_guide/test_add_template.py`

CSS·마크업은 승인 시안 #4(좌측 세로 레일)에서 이식. 아래 테스트가 요구하는 **훅**을 반드시 포함한다.

- [ ] **Step 1: 실패 테스트 작성**

Create `tests/marketplace_guide/test_add_template.py`:
```python
# -*- coding: utf-8 -*-
"""add.html — 2탭·4단계·마켓목록·프롬프트 생성 훅 검증."""
import pathlib
from webapp.routes import marketplace_guide as mg

TPL = pathlib.Path(mg.__file__).parents[1] / "templates" / "marketplace_guide"


def _add():
    return (TPL / "add.html").read_text(encoding="utf-8")


def test_two_tabs():
    html = _add()
    assert "신규 판매처 추가" in html
    assert "기존 마켓 연동 업데이트" in html


def test_four_steps_left_rail():
    html = _add()
    for label in ["마켓", "인증·계정", "지원 동작", "프롬프트"]:
        assert label in html
    assert "ma-rail" in html          # 좌측 세로 레일 클래스


def test_support_actions_three():
    html = _add()
    for a in ["신규 상품 등록", "기존 상품 연동", "가격·재고 업데이트"]:
        assert a in html


def test_market_list_injected():
    html = _add()
    assert "{% for m in coming %}" in html   # coming_soon 마켓 목록 루프


def test_prompt_generator_present():
    html = _add()
    assert "genPrompt" in html               # 프롬프트 생성 함수
    assert "복사" in html                     # 복사 버튼
    assert "판매처 추가 가이드 참고해서" in html  # 프롬프트 본문 템플릿
    assert "100% 검증 전" in html             # 무결성 규칙 문구


def test_design_tokens():
    html = _add()
    assert "Pretendard" in html
    assert "#191F28" in html
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/marketplace_guide/test_add_template.py -v`
Expected: FAIL (스텁이라 훅 없음)

- [ ] **Step 3: `add.html` 완성**

`webapp/templates/marketplace_guide/add.html` 전체를 아래 구조로 작성(시안 #4 CSS 이식 + 아래 훅 포함):
```html
{% extends layout|default("base.html") %}
{% block title %}판매처 추가·업데이트{% endblock %}
{% block content %}
<style>
  /* 시안 #4(좌측 세로 레일) CSS 이식 — 미니멀 팔레트: 먹색+회색+네이비 포인트 */
  .ma{font-family:'Pretendard','Malgun Gothic',sans-serif;color:#191F28;padding:26px 28px;max-width:720px;}
  .ma h2{font-size:20px;font-weight:800;margin:0;}
  .ma .lead{font-size:12.5px;color:#8B95A1;margin-top:5px;}
  .ma .tabs{display:flex;border-bottom:2px solid #E9ECEF;margin:16px 0 0;}
  .ma .tab{padding:11px 18px;font-size:14.5px;font-weight:700;color:#8B95A1;background:none;border:none;cursor:pointer;}
  .ma .tab.on{color:#191F28;border-bottom:3px solid #191F28;margin-bottom:-2px;}
  .ma .pane{display:none;} .ma .pane.on{display:block;}
  .ma-rail-wrap{display:grid;grid-template-columns:150px 1fr;gap:22px;margin-top:16px;}
  .ma-rail .r{display:flex;gap:10px;align-items:center;padding:8px 0;font-size:13px;color:#8B95A1;font-weight:600;}
  .ma-rail .r .n{width:22px;height:22px;border-radius:50%;background:#F6F8FA;border:1px solid #E9ECEF;font-size:11px;font-weight:800;text-align:center;line-height:22px;}
  .ma-rail .r.on{color:#191F28;} .ma-rail .r.on .n{background:#0A2540;color:#fff;border-color:#0A2540;}
  .ma-rail .r.done .n{background:#191F28;color:#fff;}
  .ma label{display:block;font-size:12.5px;font-weight:700;color:#4E5968;margin:15px 0 7px;}
  .ma input,.ma select{width:100%;border:1px solid #E9ECEF;border-radius:9px;padding:11px 13px;font-size:14px;}
  .ma .seg{display:inline-flex;border:1px solid #E9ECEF;border-radius:8px;overflow:hidden;}
  .ma .seg button{border:none;background:#fff;padding:7px 13px;font-size:12.5px;cursor:pointer;color:#4E5968;border-right:1px solid #E9ECEF;}
  .ma .seg button.on{background:#0A2540;color:#fff;font-weight:700;}
  .ma .chk{display:inline-flex;align-items:center;gap:8px;font-size:13.5px;font-weight:600;margin-right:18px;}
  .ma .genbtn{margin-top:18px;background:#0A2540;color:#fff;border:none;border-radius:10px;padding:12px 20px;font-size:14px;font-weight:800;cursor:pointer;}
  .ma .prm{margin-top:16px;background:#0F172A;border-radius:12px;padding:16px 18px;position:relative;display:none;}
  .ma .prm.on{display:block;}
  .ma .prm pre{margin:0;color:#E6EDF6;font-family:ui-monospace,monospace;font-size:12.5px;white-space:pre-wrap;}
  .ma .prm .cp{position:absolute;top:14px;right:14px;background:#1E293B;color:#CBD5E1;border:none;border-radius:8px;padding:6px 12px;font-size:12px;font-weight:700;cursor:pointer;}
</style>
<div class="ma">
  <h2>➕ 판매처 추가·업데이트</h2>
  <div class="lead">마켓명·인증방식·셀러 API 정보를 넣고 <b>프롬프트 생성</b> → Claude Code에 붙여넣으면 업로드 연동이 추가/수정됩니다.</div>
  <div class="tabs">
    <button class="tab on" data-pane="new">신규 판매처 추가</button>
    <button class="tab" data-pane="upd">기존 마켓 연동 업데이트</button>
  </div>

  <!-- 신규 -->
  <div class="pane on" id="pane-new">
    <div class="ma-rail-wrap">
      <div class="ma-rail">
        <div class="r on" data-step="1"><span class="n">1</span>마켓</div>
        <div class="r" data-step="2"><span class="n">2</span>인증·계정</div>
        <div class="r" data-step="3"><span class="n">3</span>지원 동작</div>
        <div class="r" data-step="4"><span class="n">4</span>프롬프트</div>
      </div>
      <div>
        <label>마켓(판매처)명</label>
        <input list="mk-list" id="mkName" placeholder="예: 롯데온, 11번가 — 목록에서 고르거나 직접 입력">
        <datalist id="mk-list">
          {% for m in coming %}<option value="{{ m.label }}">{{ m.api_type }}</option>{% endfor %}
        </datalist>
        <label>인증 · API 방식</label>
        <span class="seg" id="mkAuth">
          <button type="button" class="on" data-k="OAuth">OAuth</button>
          <button type="button" data-k="HMAC">HMAC</button>
          <button type="button" data-k="셀러센터API">셀러센터API</button>
          <button type="button" data-k="기타">기타</button>
        </span>
        <label>셀러 API 문서 URL <span style="color:#b6bcc4;font-weight:500">(키발급·상품등록·재고가격)</span></label>
        <input id="mkDocs" placeholder="예: https://seller.lotteon.com/ , /api/products , /api/stock-price">
        <label>필요 키 <span style="color:#b6bcc4;font-weight:500">(아는 경우)</span></label>
        <input id="mkKeys" placeholder="예: CLIENT_ID, CLIENT_SECRET">
        <label>지원 동작</label>
        <div>
          <label class="chk"><input type="checkbox" class="mkAct" value="신규 상품 등록" checked> 신규 상품 등록</label>
          <label class="chk"><input type="checkbox" class="mkAct" value="기존 상품 연동" checked> 기존 상품 연동</label>
          <label class="chk"><input type="checkbox" class="mkAct" value="가격·재고 업데이트" checked> 가격·재고 업데이트</label>
        </div>
        <button class="genbtn" type="button" onclick="genPrompt()">📋 추가 + Claude Code 프롬프트 생성</button>
        <div class="prm" id="mkPrm"><button class="cp" type="button" onclick="copyPrm()">복사</button><pre id="mkPrmText"></pre></div>
      </div>
    </div>
  </div>

  <!-- 기존 -->
  <div class="pane" id="pane-upd">
    <label>연동된 마켓</label>
    <select id="updMk">{% for m in ready %}<option>{{ m.label }}</option>{% endfor %}</select>
    <label>증상 · 요청</label>
    <input id="updNote" placeholder="예: 재고가 0으로 안 올라감 / 옵션 매칭 틀림">
    <button class="genbtn" type="button" onclick="genUpd()">📋 저장 + Claude Code 프롬프트 생성</button>
    <div class="prm" id="updPrm"><button class="cp" type="button" onclick="copyPrm('updPrmText')">복사</button><pre id="updPrmText"></pre></div>
  </div>
</div>

<script>
document.querySelectorAll('.ma .tab').forEach(function(t){
  t.addEventListener('click', function(){
    document.querySelectorAll('.ma .tab').forEach(x=>x.classList.remove('on'));
    document.querySelectorAll('.ma .pane').forEach(x=>x.classList.remove('on'));
    t.classList.add('on');
    document.getElementById('pane-'+t.dataset.pane).classList.add('on');
  });
});
document.querySelectorAll('#mkAuth button').forEach(function(b){
  b.addEventListener('click', function(){
    document.querySelectorAll('#mkAuth button').forEach(x=>x.classList.remove('on'));
    b.classList.add('on');
  });
});
function genPrompt(){
  var name=(document.getElementById('mkName').value||'').trim();
  if(!name){ alert('마켓명을 입력하세요.'); return; }
  var auth=(document.querySelector('#mkAuth button.on')||{}).dataset ? document.querySelector('#mkAuth button.on').dataset.k : 'OAuth';
  var docs=(document.getElementById('mkDocs').value||'').trim() || '(문서 링크 미입력 — 조사 필요)';
  var keys=(document.getElementById('mkKeys').value||'').trim() || '(조사 필요)';
  var acts=[].slice.call(document.querySelectorAll('.mkAct:checked')).map(c=>c.value).join(' · ');
  var t='판매처 추가 가이드 참고해서 "'+name+'" 업로드 연동 추가해줘.\n'
    +'· 인증/API : '+auth+'\n'
    +'· 문서 : '+docs+'\n'
    +'· 필요 키 : '+keys+'\n'
    +'· 지원 동작 : '+acts+'\n'
    +'※ 실계정 100% 검증 전 "완료" 선언 금지 (가격·재고 오류 = 금전 손실).';
  document.getElementById('mkPrmText').textContent=t;
  document.getElementById('mkPrm').classList.add('on');
}
function genUpd(){
  var mk=document.getElementById('updMk').value;
  var note=(document.getElementById('updNote').value||'').trim();
  var t='판매처 업로드 로직 점검해줘 — 마켓: "'+mk+'".\n· 증상/요청 : '+(note||'(미입력)')+'\n※ 실계정 100% 검증 전 "완료" 선언 금지.';
  document.getElementById('updPrmText').textContent=t;
  document.getElementById('updPrm').classList.add('on');
}
function copyPrm(id){
  var el=document.getElementById(id||'mkPrmText');
  navigator.clipboard && navigator.clipboard.writeText(el.textContent);
}
</script>
{% endblock %}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/marketplace_guide/test_add_template.py -v`
Expected: PASS (6 passed)

- [ ] **Step 5: 커밋**

```bash
git add webapp/templates/marketplace_guide/add.html tests/marketplace_guide/test_add_template.py
git commit -m "feat(marketplace-guide): 판매처 추가 위저드(좌측 레일 4단계 2탭 + 프롬프트 생성)"
```

---

### Task 3: `map.html` 데이터 코드 지도 (판매처판)

**Files:**
- Modify: `webapp/templates/marketplace_guide/map.html` (스텁 → 완성)
- Test: `tests/marketplace_guide/test_map_template.py`

- [ ] **Step 1: 실패 테스트 작성**

Create `tests/marketplace_guide/test_map_template.py`:
```python
# -*- coding: utf-8 -*-
"""map.html — 흐름 4층·두 갈래·SOP·코드위치·리스크 검증."""
import pathlib
from webapp.routes import marketplace_guide as mg

TPL = pathlib.Path(mg.__file__).parents[1] / "templates" / "marketplace_guide"


def _map():
    return (TPL / "map.html").read_text(encoding="utf-8")


def test_flow_four_layers():
    html = _map()
    for node in ["프로그램", "전송 대기", "판매처 API", "마켓 반영"]:
        assert node in html


def test_two_branches_explicit():
    html = _map()
    assert "신규 상품" in html and "등록" in html
    assert "기존" in html and "연동" in html


def test_code_map_files():
    html = _map()
    for f in ["uploader/runtime.py", "smartstore", "coupang", "MARKET_METADATA"]:
        assert f in html


def test_risk_integrity():
    html = _map()
    assert "LEMOUTON_LIVE_UPLOAD" in html
    assert "0원" in html or "재고" in html


def test_design_tokens():
    html = _map()
    assert "Pretendard" in html and "#191F28" in html
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/marketplace_guide/test_map_template.py -v`
Expected: FAIL

- [ ] **Step 3: `map.html` 완성**

`webapp/templates/marketplace_guide/map.html` 을 아래로 작성(소싱처 map.html 톤 이식 + 아래 훅):
```html
{% extends layout|default("base.html") %}
{% block title %}데이터 코드 지도{% endblock %}
{% block content %}
<style>
  .mm{font-family:'Pretendard','Malgun Gothic',sans-serif;color:#191F28;padding:26px 30px;max-width:1000px;}
  .mm h2{font-size:22px;font-weight:800;margin:0 0 6px;}
  .mm .sub{font-size:13px;color:#8B95A1;margin-bottom:20px;}
  .mm .flow{display:flex;align-items:center;gap:10px;flex-wrap:wrap;margin:14px 0;}
  .mm .node{background:#F6F8FA;border:1px solid #E9ECEF;border-radius:10px;padding:11px 14px;font-weight:700;text-align:center;font-size:13px;}
  .mm .node small{display:block;color:#8B95A1;font-weight:400;font-size:11px;margin-top:2px;}
  .mm .ar{color:#8B95A1;font-weight:800;}
  .mm .branch{display:grid;grid-template-columns:1fr 1fr;gap:14px;margin:14px 0;}
  .mm .b{border:1px solid #E9ECEF;border-radius:12px;padding:16px 18px;}
  .mm .b h4{margin:0 0 6px;font-size:14.5px;font-weight:800;}
  .mm .b p{margin:0;font-size:12.5px;color:#4E5968;}
  .mm h3{font-size:16px;font-weight:800;margin:26px 0 10px;padding-left:11px;border-left:4px solid #0A2540;}
  .mm .cm{display:grid;grid-template-columns:1fr auto;gap:8px;padding:8px 0;border-bottom:1px solid #F1F3F5;font-size:13px;}
  .mm .cm .f{font-family:ui-monospace,monospace;font-size:11.5px;color:#4E5968;}
  .mm .risk{margin-top:14px;background:#FFF4E6;border:1px solid #FFD8A8;border-radius:11px;padding:13px 16px;font-size:12.5px;color:#4E5968;line-height:1.75;}
  .mm .risk b{color:#E8590C;}
  .mm .sop{display:flex;gap:8px;flex-wrap:wrap;margin:8px 0;}
  .mm .sop .s{background:#EDF1F5;border:1px solid #E9ECEF;border-radius:8px;padding:7px 12px;font-size:12.5px;font-weight:600;}
</style>
<div class="mm">
  <h2>📖 데이터 코드 지도 — 판매처</h2>
  <div class="sub">프로그램 가격·재고가 마켓까지 가는 길 + 마켓별 정밀 세팅 절차 + 코드 위치.</div>

  <h3>1 · 업로드 흐름</h3>
  <div class="flow">
    <div class="node">프로그램<small>가격·재고</small></div><span class="ar">→</span>
    <div class="node">전송 대기<small>스냅샷</small></div><span class="ar">→</span>
    <div class="node">판매처 API<small>스스 OAuth · 쿠팡 HMAC</small></div><span class="ar">→</span>
    <div class="node">마켓 반영<small>상품·재고 갱신</small></div>
  </div>
  <div class="branch">
    <div class="b"><h4>갈래 A · 신규 상품 마켓 등록</h4><p>모음전 상품을 마켓에 처음 올림 → 상품·옵션 생성 API. 이후 가격·재고 반복 동기화.</p></div>
    <div class="b"><h4>갈래 B · 기 등록 상품 연동</h4><p>이미 마켓에 있는 상품과 매칭·연동 → 가격·재고만 반복 동기화.</p></div>
  </div>

  <h3>2 · 마켓별 정밀 세팅 SOP (Claude Code 실행)</h3>
  <div class="sop">
    <span class="s">① 인증 방식 분류</span><span class="s">② 엔드포인트 확정</span>
    <span class="s">③ 필드 매핑</span><span class="s">④ 실계정 검증 100% 일치</span>
  </div>

  <h3>3 · 코드 위치</h3>
  <div class="cm"><div>업로드 실행·전송 플래그</div><div class="f">uploader/runtime.py</div></div>
  <div class="cm"><div>스마트스토어 인증·호출</div><div class="f">platforms/smartstore/client.py</div></div>
  <div class="cm"><div>쿠팡 HMAC 서명·테스트</div><div class="f">accounts.py · _test_coupang</div></div>
  <div class="cm"><div>마켓 정의·키 스키마</div><div class="f">accounts.py · MARKET_METADATA</div></div>

  <div class="risk"><b>⚠️ 정직성</b> — 실전송 기본 OFF(<code>LEMOUTON_LIVE_UPLOAD</code>) · 0원·재고없음 전송 금지 · 크롤 실패 값 폴백 금지("없음" 표기).</div>
</div>
{% endblock %}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/marketplace_guide/test_map_template.py -v`
Expected: PASS (5 passed)

- [ ] **Step 5: 커밋**

```bash
git add webapp/templates/marketplace_guide/map.html tests/marketplace_guide/test_map_template.py
git commit -m "feat(marketplace-guide): 데이터 코드 지도(판매처판) — 흐름·두갈래·SOP·코드위치·리스크"
```

---

### Task 4: 진입 버튼 2개 + `_page_modal.html` include (`accounts/upload.html`)

**Files:**
- Modify: `webapp/templates/accounts/upload.html`
- Test: `tests/marketplace_guide/test_upload_entry.py`

- [ ] **Step 1: 실패 테스트 작성**

Create `tests/marketplace_guide/test_upload_entry.py`:
```python
# -*- coding: utf-8 -*-
"""accounts/upload.html — 판매처 매뉴얼 진입 버튼 + 모달 기구 include."""
import pathlib
from webapp.routes import marketplace_guide as mg

UPLOAD = pathlib.Path(mg.__file__).parents[1] / "templates" / "accounts" / "upload.html"


def test_add_button():
    html = UPLOAD.read_text(encoding="utf-8")
    assert 'data-guide-modal="/marketplace-guide/add?bare=1"' in html
    assert "판매처 추가·업데이트" in html


def test_map_button():
    html = UPLOAD.read_text(encoding="utf-8")
    assert 'data-guide-modal="/marketplace-guide/map?bare=1"' in html
    assert "데이터 코드 지도" in html


def test_page_modal_included():
    html = UPLOAD.read_text(encoding="utf-8")
    assert "sourcing_guide/_page_modal.html" in html
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/marketplace_guide/test_upload_entry.py -v`
Expected: FAIL

- [ ] **Step 3: `upload.html` 수정**

`webapp/templates/accounts/upload.html` 의 페이지 제목(「판매처 계정」) 영역 바로 아래에 카드 버튼 블록을 삽입한다. `{% block content %}` 안, 목록 위. 삽입할 마크업:
```html
  <!-- 판매처 매뉴얼 진입 (소싱처 가이드 미러) -->
  <div class="mkg-cards" style="display:flex;gap:14px;margin:0 0 20px;">
    <button type="button" class="mkg-card" data-guide-modal="/marketplace-guide/add?bare=1"
      style="flex:1;display:flex;align-items:center;gap:12px;background:#fff;border:1px solid #E5E8EB;border-radius:14px;padding:16px 18px;cursor:pointer;text-align:left;">
      <span style="font-size:20px;">➕</span>
      <span><b style="display:block;font-size:15px;">판매처 추가·업데이트</b>
      <span style="font-size:12.5px;color:#8B95A1;">마켓 선택 → 인증 → 프롬프트 생성</span></span>
    </button>
    <button type="button" class="mkg-card" data-guide-modal="/marketplace-guide/map?bare=1"
      style="flex:1;display:flex;align-items:center;gap:12px;background:#0A2540;border:1px solid #0A2540;border-radius:14px;padding:16px 18px;cursor:pointer;text-align:left;">
      <span style="font-size:20px;">📖</span>
      <span><b style="display:block;font-size:15px;color:#fff;">데이터 코드 지도</b>
      <span style="font-size:12.5px;color:#C9D1D9;">가격·재고가 마켓까지 가는 길</span></span>
    </button>
  </div>
```
그리고 `{% block content %}` 맨 끝(닫는 `{% endblock %}` 직전)에 모달 기구 include 추가:
```html
{% include "sourcing_guide/_page_modal.html" %}
```

> ⚠️ `upload.html` 은 utf-8 · 기존 구조 유지. 삽입 위치는 제목 아래·목록 위. 한글 깨짐 방지 위해 편집은 UTF-8 유지.

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/marketplace_guide/test_upload_entry.py -v`
Expected: PASS (3 passed)

- [ ] **Step 5: 커밋**

```bash
git add webapp/templates/accounts/upload.html tests/marketplace_guide/test_upload_entry.py
git commit -m "feat(marketplace-guide): 판매처 계정 상단에 매뉴얼 카드 버튼 2개 + iframe 모달 include"
```

---

### Task 5: 전체 회귀 + 라이브 검증 + PR

**Files:** 없음(검증)

- [ ] **Step 1: 마켓플레이스 가이드 테스트 전체 통과**

Run: `cd 프로그램/_시스템 && python -m pytest tests/marketplace_guide/ -v`
Expected: PASS (모든 테스트)

- [ ] **Step 2: 인접 회귀 없음 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/sourcing/test_guide_tabs.py tests/uploader/ -q`
Expected: 기존 통과 유지

- [ ] **Step 3: 라이브 브라우저 검증 (배포 후)**

`/accounts/upload` 접속 → 「➕ 판매처 추가·업데이트」·「📖 데이터 코드 지도」 버튼 클릭 →
iframe 모달이 **빈 상자·깨진 아이콘 없이** 렌더되는지 확인. `/marketplace-guide/add?bare=1` 응답 헤더
`X-Frame-Options: SAMEORIGIN` 확인(개발자도구 Network). 위저드에서 마켓·인증·동작 채우고 「프롬프트 생성」 → 프롬프트 문구 확인.

- [ ] **Step 4: PR 생성 (배포는 사용자 merge)**

```bash
git push -u origin feature/marketplace-add-card
gh pr create --base main --head feature/marketplace-add-card \
  --title "feat: 판매처 추가·업데이트 팝업 + 데이터 코드 지도(판매처판)" \
  --body "소싱처 가이드의 판매처 짝. 좌측 레일 4단계 위저드 + 데이터 코드 지도. bare 라우트 SAMEORIGIN. 스펙·플랜: docs/superpowers/"
```

---

## Self-Review

**Spec coverage:**
- §2 배치·진입 → Task 4 ✅
- §3 팝업 내용(4단계·2탭·프롬프트) → Task 2 ✅
- §4 데이터 코드 지도 → Task 3 ✅
- §5 아키텍처(블루프린트·라우트·SAMEORIGIN) → Task 1 ✅
- §6 DB 무기록(프롬프트만) → Task 2 JS(genPrompt, DB 호출 없음) ✅
- §8 테스트·검증 → Task 1~5 ✅

**Placeholder scan:** 실제 코드·테스트·명령 모두 포함. 프롬프트 문구·훅 명시. 통과.

**Type/name consistency:** `genPrompt`/`genUpd`/`copyPrm`(add.html JS ↔ test), `ma-rail`·`data-guide-modal="/marketplace-guide/add?bare=1"`(template ↔ test), `_markets()`→coming/ready(route ↔ template loop) 일치. 통과.
