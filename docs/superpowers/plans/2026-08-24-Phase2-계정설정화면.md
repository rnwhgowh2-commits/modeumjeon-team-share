# Phase 2 — 판매처관리 계정 설정 화면 (구현 계획)

> **선행 문서**
> - 설계: `docs/superpowers/specs/2026-08-24-모음전-3계층-재설계-design.md`
> - 칸 목록(이 계획의 입력): `docs/superpowers/specs/2026-08-24-2층-계정설정-칸목록.md`
> - Phase 1 (완료·배포됨): `MarketAccountSetting` · `account_settings.py` · `DEFAULT_FEES`
>
> **목표**: 「판매처 관리」에서 계정을 펼치면 등록 설정을 넣을 수 있고, 한 번 넣으면
> 정책을 새로 만들어도 **다시 안 넣어도 된다**.

**Architecture**: 저장은 Phase 1 의 `MarketAccountSetting`(공통 10칸 컬럼 + `extra` JSON).
읽기는 `account_settings.value_of()` / `is_set()` 단일 창구. 화면은 기존
`accounts/upload.html` 의 계정 표에 「설정」 펼침을 더한다 — **새 페이지를 만들지 않는다**.

**Tech Stack**: Flask · SQLAlchemy · Jinja2 · pytest

---

## 🔴 이 Phase 의 절대 규칙

1. **자격증명은 안 담는다.** API 키는 `.env` 가 단일 원천(`lemouton/auth/secrets.py`).
   이 화면은 **등록 설정만** 다룬다. 기존 「API 키」 폼은 건드리지 않는다.
2. **쿠팡 출고지·반품지는 새로 만들지 않는다.** `coupang_vendor_settings` 를 읽는다
   (읽는 곳이 이미 5군데 — 두 벌이 갈리면 어느 출고지로 나갔는지 못 쫓는다).
3. **재고 기본값(999)은 만들지 않는다.** 「재고는 소싱처 실제 재고로만」이 절대 규칙이다.
   `stock_default` 컬럼은 Phase 1 에 있으나 **화면에 노출하지 않는다.**
4. **「안 정함」과 「0원」을 가른다.** 빈 칸으로 두면 NULL(안 정함)로 저장한다.
   0 을 입력하면 0 으로 저장한다. 저장할 때 빈 칸을 0 으로 바꾸지 않는다.
5. `DEFAULT_FEES`(반품 5,000 · 교환 10,000)는 **새 계정을 처음 만들 때만** 넣는다.
   기존 계정의 NULL 을 채우지 않는다.

---

### Task 1: 마켓별 허용 키 화이트리스트

**Files**
- Modify: `lemouton/policy/account_settings.py`
- Test: `tests/policy/test_account_settings.py` (기존 파일에 덧붙임)

오타 난 키가 조용히 저장되면 「왜 안 먹지」로 한참 헤맨다. 저장 창구에서 막는다.

- [ ] **Step 1: 실패하는 시험**

```python
def test_그_마켓에_없는_칸은_저장을_거부한다(db):
    acc = _account(db)
    with pytest.raises(ValueError, match='쿠팡'):
        set_extra(db, acc.id, 'coupang', {'owhpNo': '123'})   # 롯데ON 칸이다


def test_그_마켓_칸이면_저장된다(db):
    acc = _account(db)
    set_extra(db, acc.id, 'coupang', {'discountRate': 10})
    assert value_of(db, acc.id, 'discountRate') == 10


def test_허용키_목록이_6마켓_전부_있다():
    for m in ('smartstore', 'coupang', 'auction', 'gmarket', 'eleven11', 'lotteon'):
        assert MARKET_EXTRA_KEYS[m], f'{m} 허용키가 비었다'


def test_옥션과_G마켓은_같은_칸을_쓴다():
    """ESM 공용 — 다르면 한쪽만 저장되는 사고가 난다."""
    assert MARKET_EXTRA_KEYS['auction'] == MARKET_EXTRA_KEYS['gmarket']
```

- [ ] **Step 2: 실패 확인** — `ImportError: cannot import name 'MARKET_EXTRA_KEYS'`
- [ ] **Step 3: 구현** — 칸 목록 문서 §2 를 그대로 옮긴다. `set_extra()` 는 화이트리스트에
      없는 키를 만나면 `ValueError` 로 마켓명과 함께 거부.
- [ ] **Step 4: 통과 확인**

---

### Task 2: 저장 API

**Files**
- Modify: `webapp/routes/accounts.py`
- Test: `tests/policy/test_account_settings_api.py` (신규)

기존 `save_secrets` 와 같은 모양으로 붙인다 — `POST /accounts/api/settings/<account_id>`.

- [ ] **Step 1: 실패하는 시험** — 저장 → 다시 읽기 왕복, 없는 계정 404,
      빈 문자열이 NULL 로 저장되는지, 0 이 0 으로 저장되는지
- [ ] **Step 2: 실패 확인**
- [ ] **Step 3: 구현**
  - Body: `{"columns": {...}, "extra": {...}}`
  - 🔴 빈 문자열(`''`)은 **NULL 로** 저장 — 「안 정함」으로 되돌리는 유일한 방법
  - 🔴 `0` 은 `0` 으로 저장 — 빈 값으로 바꾸지 않는다
  - 화이트리스트 위반은 400 + 어느 키가 문제인지 명시
- [ ] **Step 4: 통과 확인**

---

### Task 3: 화면 — 계정 행에 「설정」 펼침

**Files**
- Modify: `webapp/templates/accounts/upload.html`
- Test: `tests/policy/test_account_settings_view.py` (신규 — 렌더 확인)

결정 1번안(삼바 동일 · 긴 폼). 탭 없이 **구분선**으로 나눈다.

- [ ] **Step 1: 실패하는 시험** — `/accounts/upload` 응답에 「등록 설정」 구분선과
      그 마켓 칸의 라벨이 실제로 들어 있나(마켓마다 다른 칸이 나오는지)
- [ ] **Step 2: 실패 확인**
- [ ] **Step 3: 구현**
  - 구분선 4개: 「인증」(기존) / 「배송」 / 「등록 기본값」 / 「혜택」
  - 🔴 **원본 스타일 그대로 쓴다** — 이 화면엔 이미 `v2-section`·`v2-label-section`·
    `form-row`·`v2-form-body` 클래스가 있다. 새 CSS 를 짓지 말고 그걸 재사용한다.
  - 쿠팡 출고지·반품지는 `coupang_vendor_settings` 값을 **읽기 전용으로** 보여주고
    「쿠팡 계정정보에서 관리합니다」 안내 + 그쪽 링크
  - 재고 기본값 칸은 **안 그린다**
  - 빈 칸에는 placeholder 로 「안 정함」을 띄워 0 과 구분되게 한다
- [ ] **Step 4: 통과 확인**

---

### Task 4: 새 계정 만들 때 기본 배송비 넣기

**Files**
- Modify: `webapp/routes/accounts.py` (`create_upload_account`)
- Test: `tests/policy/test_account_settings_api.py` 에 덧붙임

- [ ] **Step 1: 실패하는 시험**

```python
def test_새_계정을_만들면_기본_배송비가_들어간다(client, db):
    # 계정 생성 → MarketAccountSetting 이 함께 생기고 return_fee=5000, exchange_fee=10000


def test_기존_계정의_안_정한_칸은_안_채운다(db):
    """🔴 이미 있는 계정의 NULL 을 나중에 5,000 으로 채우면 「안 정함」이 사라진다."""
```

- [ ] **Step 2~4**: 실패 확인 → 구현 → 통과 확인

---

### Task 5: 회귀 확인

- [ ] `python -m pytest tests/policy tests/registration -q`
- [ ] `python -c "import app; print('boot ok')"`
- [ ] 🔴 **실화면 확인** — 이번 Phase 는 화면이 생기므로 `ui-verify` 대상이다.
      로컬 서버를 띄워 「판매처 관리」에서 계정을 펼치고, 값을 넣고 저장한 뒤
      새로고침해서 **실제로 남아 있는지** 눈으로 확인한다. 코드만 보고 PASS 금지.

---

## 안 하는 것 (Phase 3+ 로 미룸)

- 이 설정을 **실제 마켓 전송에 쓰는 배선** — Phase 4. 이번엔 저장·표시까지.
- 기존 정책값 되채움 **실행** — 화면이 생긴 뒤 사장님 확인 후.
- 옥션/G마켓·롯데ON 의 출고지 **번호 목록을 마켓 API 로 불러오는 기능** — 지금은 직접 입력.
  (삼바는 불러오기 버튼이 있다. 편의 기능이라 별도 항목으로 뺀다.)
