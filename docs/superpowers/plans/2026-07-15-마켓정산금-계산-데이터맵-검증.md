# 마켓 정산금 계산 — 데이터맵 신설 + 롯데온 실검증 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 롯데온 정산예정금액을 주문+혜택+수수료율로 계산해 실제 정산 엑셀 117행과 오차 0으로 맞추고, 그 API→필드 지도를 map.html '정산금 계산' 영역탭에 기록한다.

**Architecture:** 순수 계산 함수(단위테스트)로 공식을 고정하고, 롯데온 혜택조회(245)·수수료(SettleCommission) 어댑터를 붙인 뒤, 서버 프로브로 실주문을 불러와 엑셀과 건별 대조한다. 검증 통과 후 UI(map.html)에 데이터맵을 렌더한다.

**Tech Stack:** Python(Flask, pytest), 롯데온 OpenAPI(Bearer), Jinja/JS(map.html)

---

## 파일 구조

- Create: `프로그램/_시스템/shared/platforms/lotteon/benefits.py` — 셀러/롯데부담 할인 조회(245)
- Create: `프로그램/_시스템/lemouton/margin/lotteon_settlement.py` — 순수 계산 함수
- Modify: `프로그램/_시스템/shared/platforms/lotteon/claims.py` — commission_map 멀티계정 trNo 수정
- Modify: `프로그램/_시스템/webapp/routes/api_margin.py` — 검증 프로브(임시)
- Modify: `프로그램/_시스템/webapp/templates/marketplace_guide/map.html` — 정산금 계산 영역탭
- Test: `프로그램/_시스템/tests/margin/test_lotteon_settlement.py`, `tests/platforms/test_lotteon_benefits.py`

로컬 테스트 실행 기준: cwd `프로그램/_시스템`, `ENVIRONMENT=team-share-dev DISABLE_AUTH=1`.

---

## Phase 1 — 롯데온 계산 배선 + 실검증

### Task 1: 정산 계산 순수 함수

**Files:**
- Create: `프로그램/_시스템/lemouton/margin/lotteon_settlement.py`
- Test: `프로그램/_시스템/tests/margin/test_lotteon_settlement.py`

- [ ] **Step 1: 실패 테스트 작성** (엑셀 주문 2026070814649375 실측값)

```python
from lemouton.margin.lotteon_settlement import compute_settlement

def test_matches_excel_row_1():
    # 상품가 32200, 배송비 0, 셀러부담 966, 롯데부담 3864 → 엑셀 정산예정금액 26404
    got = compute_settlement(product_price=32200, shipping=0,
                             seller_discount=966, platform_discount=3864)
    assert got == 26404

def test_shipping_included():
    # 배송비 3000, 배송비수수료율 3.3% → 배송비 정산분 3000-99=2901 가산
    got = compute_settlement(product_price=10000, shipping=3000,
                             seller_discount=0, platform_discount=0)
    # 정산대상 13000 - (1300 + 99 + 200 - 0) = 11401
    assert got == 11401
```

- [ ] **Step 2: 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/margin/test_lotteon_settlement.py -v`
Expected: FAIL (ModuleNotFoundError: lotteon_settlement)

- [ ] **Step 3: 최소 구현**

```python
# -*- coding: utf-8 -*-
"""롯데온 정산예정금액 순수 계산 — 실정산 엑셀 117행 오차0 검증된 공식.

정산예정금액 = (상품가 − 셀러부담할인 + 배송비) − 중개수수료총합계
  중개수수료 = round(상품가×상품율) + round(배송비×배송율) + round(상품가×제휴율) − 롯데부담할인
율 기본값은 이 셀러 계약율(13%/3.3%/2%). 정산완료분에서 역산한 율을 주입하면 그 값을 쓴다.
"""

def compute_settlement(product_price: int, shipping: int,
                       seller_discount: int, platform_discount: int,
                       rate_product: float = 0.13,
                       rate_shipping: float = 0.033,
                       rate_affiliate: float = 0.02) -> int:
    settle_target = product_price - platform_discount - seller_discount + shipping
    commission = (round(product_price * rate_product)
                  + round(shipping * rate_shipping)
                  + round(product_price * rate_affiliate)
                  - platform_discount)
    return int(round(settle_target - commission))
```

- [ ] **Step 4: 통과 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/margin/test_lotteon_settlement.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: 커밋**

```bash
git add 프로그램/_시스템/lemouton/margin/lotteon_settlement.py 프로그램/_시스템/tests/margin/test_lotteon_settlement.py
git commit -m "feat(margin): 롯데온 정산예정금액 순수 계산 함수 (엑셀 오차0 검증)"
```

### Task 2: 셀러/롯데부담 할인 조회 (getSROrderList 245)

**Files:**
- Create: `프로그램/_시스템/shared/platforms/lotteon/benefits.py`
- Test: `프로그램/_시스템/tests/platforms/test_lotteon_benefits.py`

- [ ] **Step 1: 실패 테스트 작성** (응답 파싱만 검증 — 네트워크 없이 fake client)

```python
from shared.platforms.lotteon.benefits import parse_benefit_map

def test_parse_seller_and_platform_discount():
    resp = {"data": {"orderItems": [
        {"odNo": "A1", "fvrList": [
            {"odFvrDvsCd": "DC", "prTypCd": "PRD_DC", "prEntpShrAmt": 966, "prSfcoShrAmt": 3864},
            {"odFvrDvsCd": "GF", "prTypCd": "GIV_PR", "prEntpShrAmt": 500, "prSfcoShrAmt": 0},
        ]},
    ]}}
    m = parse_benefit_map(resp)
    # 할인(DC)만 합산, 증정(GF) 제외
    assert m["A1"] == {"seller_discount": 966, "platform_discount": 3864}
```

- [ ] **Step 2: 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/platforms/test_lotteon_benefits.py -v`
Expected: FAIL (ModuleNotFoundError)

- [ ] **Step 3: 최소 구현**

```python
# -*- coding: utf-8 -*-
"""롯데온 셀러 주문혜택 조회(245) — 주문별 셀러/롯데부담 할인.

GET /v1/openapi/order/v1/getSROrderList (srchStrtDttm/srchEndDttm yyyymmddhh24miss).
응답 data.orderItems[].fvrList[]: odFvrDvsCd(DC 할인/GF 증정)·prTypCd·
  prEntpShrAmt(업체=셀러 분담)·prSfcoShrAmt(자사=롯데 분담).
정산 계산엔 '할인(DC)'만 필요 → 셀러부담=Σ prEntpShrAmt(DC), 롯데부담=Σ prSfcoShrAmt(DC).
"""
from datetime import datetime, timedelta
from typing import Optional

_PATH = "/v1/openapi/order/v1/getSROrderList"


def parse_benefit_map(resp: dict) -> dict:
    out: dict = {}
    data = (resp or {}).get("data") or {}
    for od in (data.get("orderItems") or []):
        od_no = str(od.get("odNo") or "")
        if not od_no:
            continue
        s = p = 0
        for f in (od.get("fvrList") or []):
            if f.get("odFvrDvsCd") != "DC":   # 할인만(증정 제외)
                continue
            s += int(float(f.get("prEntpShrAmt") or 0))
            p += int(float(f.get("prSfcoShrAmt") or 0))
        cur = out.setdefault(od_no, {"seller_discount": 0, "platform_discount": 0})
        cur["seller_discount"] += s
        cur["platform_discount"] += p
    return out


def benefit_map(since: datetime, until: datetime, *, client) -> dict:
    """[since, until] 주문의 할인맵. 1일 초과 조회 불가 → 24h 윈도우."""
    out: dict = {}
    win = since
    while win < until:
        end = min(win + timedelta(days=1), until)
        body = {"srchStrtDttm": win.strftime("%Y%m%d%H%M%S"),
                "srchEndDttm": (end - timedelta(seconds=1)).strftime("%Y%m%d%H%M%S")}
        resp = client.request(method="GET", path=_PATH, body=body)
        for k, v in parse_benefit_map(resp).items():
            cur = out.setdefault(k, {"seller_discount": 0, "platform_discount": 0})
            cur["seller_discount"] += v["seller_discount"]
            cur["platform_discount"] += v["platform_discount"]
        win = end
    return out
```

- [ ] **Step 4: 통과 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/platforms/test_lotteon_benefits.py -v`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add 프로그램/_시스템/shared/platforms/lotteon/benefits.py 프로그램/_시스템/tests/platforms/test_lotteon_benefits.py
git commit -m "feat(lotteon): 셀러 주문혜택 조회(245) 셀러/롯데부담 할인맵"
```

### Task 3: commission_map 멀티계정 trNo 버그 수정

**Files:**
- Modify: `프로그램/_시스템/shared/platforms/lotteon/claims.py` (commission_map, line ~102-104)
- Test: `프로그램/_시스템/tests/platforms/test_lotteon_commission_trno.py`

- [ ] **Step 1: 실패 테스트 작성** (client._cfg 의 trNo 를 body 에 쓰는지)

```python
from shared.platforms.lotteon.claims import commission_map
from datetime import datetime

class _FakeClient:
    def __init__(self):
        self._cfg = {"tr_grp_cd": "SR", "tr_no": "LO_ACCT_X", "lrtr_no": ""}
        self.sent = []
    def request(self, method, path, body=None):
        self.sent.append(body)
        return {"data": []}

def test_uses_client_trno_not_module_cfg():
    cli = _FakeClient()
    commission_map(datetime(2026,7,1), datetime(2026,7,2), client=cli)
    assert cli.sent and cli.sent[0]["trNo"] == "LO_ACCT_X"
```

- [ ] **Step 2: 실패 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/platforms/test_lotteon_commission_trno.py -v`
Expected: FAIL (trNo == "" 모듈 _CFG 기본값)

- [ ] **Step 3: 구현** — claims.py commission_map 의 body 를 client._cfg 에서 읽도록 수정

```python
    client = client or LotteonClient()
    cfg = getattr(client, "_cfg", None) or _CFG
    path = "/v1/openapi/settle/v1/se/SettleCommission"
    out: dict = {}
    for w_from, w_to in _windows(since, until):
        body = {"trGrpCd": cfg.get("tr_grp_cd", "SR"), "trNo": cfg.get("tr_no", ""),
                "lrtrNo": cfg.get("lrtr_no", ""),
                "startDate": w_from.strftime("%Y%m%d"), "endDate": w_to.strftime("%Y%m%d")}
```

- [ ] **Step 4: 통과 확인**

Run: `cd 프로그램/_시스템 && python -m pytest tests/platforms/test_lotteon_commission_trno.py -v`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add 프로그램/_시스템/shared/platforms/lotteon/claims.py 프로그램/_시스템/tests/platforms/test_lotteon_commission_trno.py
git commit -m "fix(lotteon): commission_map 계정별 trNo(client._cfg) 사용 — 멀티계정 정산 누락 수정"
```

### Task 4: 검증 프로브 라우트 (엑셀 주문 → 계산값)

**Files:**
- Modify: `프로그램/_시스템/webapp/routes/api_margin.py` (임시 라우트 추가)

- [ ] **Step 1: 라우트 추가** — POST `/api/margin/_probe_lo_calc` {since, until, orders:[...]}: 각 주문번호에 209(상품가·배송비)+benefit_map(245 할인)+SettleCommission(정산완료 실수수료) 조합해 compute_settlement 로 계산, per-order {계산값, 실수수료여부} 반환.

```python
@bp.route("/_probe_lo_calc", methods=["POST"])
def _probe_lo_calc():
    body = request.get_json(silent=True) or {}
    since = _dt.datetime.fromisoformat(body["since"]); until = _dt.datetime.fromisoformat(body["until"])
    want = set(str(o) for o in (body.get("orders") or []))
    from lemouton.markets.order_export import _account_client, _active_accounts, _ENV_PREFIX
    from shared.platforms.lotteon.orders import iter_delivery_orders
    from shared.platforms.lotteon.benefits import benefit_map
    from shared.platforms.lotteon.claims import commission_map
    from lemouton.margin.lotteon_settlement import compute_settlement
    accts = _active_accounts("lotteon") or [(_ENV_PREFIX.get("lotteon"), "대표")]
    rows = []
    for prefix, name in accts:
        cli = _account_client("lotteon", prefix)
        if cli is None: continue
        bm = benefit_map(since, _dt.datetime.now(), client=cli)
        cm = commission_map(since, _dt.datetime.now(), client=cli)  # {odNo: 실수수료합}
        for od in iter_delivery_orders(since, until, if_cpl_yn="", client=cli):
            odno = str(od.get("odNo") or "")
            if want and odno not in want: continue
            pp = int(float(od.get("slPrc") or 0)) * int(float(od.get("odQty") or 0))
            ship = int(float(od.get("dvCst") or 0))
            b = bm.get(odno, {"seller_discount": 0, "platform_discount": 0})
            calc = compute_settlement(pp, ship, b["seller_discount"], b["platform_discount"])
            rows.append({"acct": name, "odNo": odno, "상품가": pp, "배송비": ship,
                         "셀러부담": b["seller_discount"], "롯데부담": b["platform_discount"],
                         "계산": calc, "실수수료": cm.get(odno)})
    return jsonify({"rows": rows})
```

- [ ] **Step 2: 문법 확인**

Run: `cd 프로그램/_시스템 && python -c "import ast; ast.parse(open('webapp/routes/api_margin.py',encoding='utf-8').read()); print('OK')"`
Expected: OK

- [ ] **Step 3: 커밋 + 배포**

```bash
git add 프로그램/_시스템/webapp/routes/api_margin.py
git commit -m "diag(margin): 롯데온 정산 계산 검증 프로브(임시)"
```
그 후 origin/main 기준 브랜치 → PR → (사용자 머지) → aws-lightsail-deploy → 서버 200 확인.

### Task 5: 전수 대조 + 잔차 규명 (오차 0)

- [ ] **Step 1: 프로브 실행** — 정산 엑셀 117행의 주문번호로 `_probe_lo_calc` 호출, 결과 JSON 저장.

```bash
curl -s -X POST http://54.116.196.90/api/margin/_probe_lo_calc -H "Content-Type: application/json" \
  -d '{"since":"2026-07-08","until":"2026-07-15","orders":[...117개...]}' -o lo_calc.json
```

- [ ] **Step 2: 엑셀과 건별 대조** — 로컬 스크립트로 `계산` vs 엑셀 `정산예정금액(AV)` diff. 오차>0 행 목록화.

- [ ] **Step 3: 잔차 규명** — 불일치 원인(변동수수료·특별승인·다상품 안분·역산율 필요)을 파고들어 계산식/율 보정. `compute_settlement` 에 per-order 역산율 주입 등. 오차 0 될 때까지 반복(부분검증 불가).

- [ ] **Step 4: 결과 기록** — 오차 0 확인 후 대조표(엑셀) 산출, 스펙 §7 충족.

---

## Phase 2 — 정산금 계산 영역탭 (map.html)

### Task 6: 정산 데이터맵 데이터 정의

**Files:**
- Modify: `프로그램/_시스템/webapp/templates/marketplace_guide/map.html`

- [ ] **Step 1:** 기존 마켓 데이터 구조 옆에 마켓별 `settleCalc` 배열 정의 — 각 항목 {input, api(method·path), field, st}. 4마켓(롯데온·11번가·쿠팡·스스) 스펙 §4 그대로. 상단 계산식·§5 획득방법·§3 충분성 텍스트 상수.
- [ ] **Step 2:** 로컬 렌더 확인(라이브 서버 §라우트 진입) — 콘솔 에러 없음.
- [ ] **Step 3: 커밋**

```bash
git add 프로그램/_시스템/webapp/templates/marketplace_guide/map.html
git commit -m "feat(datamap): 정산금 계산 데이터 정의(4마켓 API→필드)"
```

### Task 7: 영역탭 렌더 (시안대로)

- [ ] **Step 1:** 좌측 영역탭 목록에 '정산금 계산' 추가, 선택 시 우측에 계산식 헤더 + 표(필요입력/호출API/응답필드/상태) 렌더. 시안(2026-07-15 승인)과 동일 컬럼·배지.
- [ ] **Step 2:** `/live-browser-verify` — 실제 라우트 접속해 탭 전환·표 렌더·마켓 전환 육안 확인.
- [ ] **Step 3: 커밋**

```bash
git commit -am "feat(datamap): 정산금 계산 영역탭 렌더(시안 확정)"
```

---

## Phase 3 — 기존 코드 재점검 + 정리

### Task 8: 기존 map.html API 코드 감사

- [ ] **Step 1:** map.html 기존 items 의 `code` 값을 `shared/platforms/*` 실제 경로/코드 및 공식 문서와 대조(마켓별). 표로 불일치 목록화.
- [ ] **Step 2:** 불일치 정정 + 상태 배지 갱신. 근거 주석.
- [ ] **Step 3: 커밋**

```bash
git commit -am "fix(datamap): 기존 API 코드 재점검·정정"
```

### Task 9: 프로브 제거 + 배포

- [ ] **Step 1:** 임시 라우트 3종(_probe_lo_settle, _probe_lo_order, _probe_lo_calc) 제거.
- [ ] **Step 2:** 전체 테스트 `python -m pytest tests/margin tests/platforms -q` 통과.
- [ ] **Step 3: 커밋 + 배포** — origin/main 브랜치 → PR → 머지 → 배포 → 라이브 200·탭 확인.

---

## 남은 마켓 (후속 사이클)

롯데온 오차 0 확인 후 동일 방식으로 11번가 → 쿠팡 → 스스 순차 검증. 각 마켓 데이터맵 상태를 '검증완료'로 갱신.
