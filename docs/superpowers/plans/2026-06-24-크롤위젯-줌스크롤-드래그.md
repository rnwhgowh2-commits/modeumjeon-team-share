# 크롤 위젯 줌·스크롤·폭조절 입력 재정의 — 구현 플랜

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 크롤 위젯의 마우스 입력을 재정의한다 — 폭=드래그 전용, Ctrl+휠=위젯만 1.0×~2.0× 줌, 그냥 휠=내부 패널(모음전 목록·소싱처 카드·소싱처 로그표) 세로 스크롤.

**Architecture:** 단일 파일 `crawl_log.js`(IIFE) 안에서 ①`scale` 고정상수를 `--mcl-scale` CSS변수 + `panelScale` JS변수로 동적화하고 ②`applyZoom()` 신설 ③`onPanelWheel()` 재정의(휠→폭 제거, Ctrl+휠→줌, 그냥 휠→네이티브 스크롤 통과) ④죽은 `clampPanelW()` 제거. 폭 드래그(`onGripDown`)는 `DOCK_SCALE`→`panelScale` 참조만 교체.

**Tech Stack:** Vanilla JS(ES5 스타일), 브라우저 DOM, CSS custom property. JS 단위테스트 인프라 없음 → 자동 게이트는 `node --check`(구문), 동작 검증은 라이브 실브라우저(프로젝트 필수 규칙).

---

## ⚠️ 선행 조건 (반드시 먼저)

- **이 작업은 origin/main 기준 워크트리에서 한다.** 현재 체크아웃된 `feature/crawl-worker-queue` 는 origin/main 보다 **305 커밋 뒤처져** 있고, 그 브랜치의 `crawl_log.js`(1140줄)는 라이브(origin/main, 1338줄)와 다르다. 라이브에 반영되려면 origin/main 에서 분기해야 한다.
- 라이브 로딩 위치: `origin/main:프로그램/_시스템/webapp/templates/base.html:166` →
  `<script src="{{ url_for('static', filename='crawl_log.js') }}?v=20260622_wheel">`
- 아래 모든 `Edit` 의 `old_string` 은 origin/main 의 `crawl_log.js` 원문과 **byte 단위로 일치**함을 확인했다(텍스트 동일, 줄번호만 다름).

## 파일 구조

| 파일 | 역할 | 변경 |
|---|---|---|
| `프로그램/_시스템/webapp/static/crawl_log.js` | 크롤 위젯 전체 로직(IIFE) | 수정 — CSS 3줄, panelScale/applyZoom, onPanelWheel 재정의, clampPanelW 제거, 패널 생성 시 초기 스케일 |
| `프로그램/_시스템/webapp/templates/base.html` | 정적 스크립트 로딩 | 수정 — `crawl_log.js?v=` 캐시버스터 + 주석 |

---

## Task 0: origin/main 워크트리 생성

**Files:** (없음 — 환경 준비)

- [ ] **Step 1: 최신 origin/main 받기**

Run:
```bash
cd "C:/dev/모음전 프로젝트"
git fetch origin
```
Expected: 에러 없이 완료.

- [ ] **Step 2: origin/main 분기 워크트리 생성**

Run:
```bash
git worktree add -b feature/widget-zoom-scroll "C:/dev/_wt_widget_zoom" origin/main
```
Expected: `Preparing worktree ... HEAD is now at <origin/main 최신 커밋>`.

- [ ] **Step 3: 대상 파일이 1338줄(라이브 버전)인지 확인**

Run:
```bash
wc -l < "C:/dev/_wt_widget_zoom/프로그램/_시스템/webapp/static/crawl_log.js"
```
Expected: `1338`

> 이후 모든 Edit/Bash 의 작업 경로는 `C:/dev/_wt_widget_zoom/...` 이다.

---

## Task 1: scale 동적화 (CSS변수 + panelScale + DOCK_SCALE 교체)

**Files:**
- Modify: `C:/dev/_wt_widget_zoom/프로그램/_시스템/webapp/static/crawl_log.js`

- [ ] **Step 1: 패널 CSS `transform`·`height` 를 `--mcl-scale` 변수로**

Edit `crawl_log.js` — `old_string`:
```
      '  position:fixed; top:0; right:0; width:520px; height:calc(100vh / 1.5);',
      '  transform:scale(1.5); transform-origin:top right;',
```
`new_string`:
```
      '  position:fixed; top:0; right:0; width:520px; height:calc(100vh / var(--mcl-scale, 1.5));',
      '  transform:scale(var(--mcl-scale, 1.5)); transform-origin:top right;',
```

- [ ] **Step 2: 숨김 상태 transform 도 변수로**

Edit `crawl_log.js` — `old_string`:
```
      '#mcl-panel.mcl-hidden { transform:scale(1.5) translateX(100%); pointer-events:none; }',
```
`new_string`:
```
      '#mcl-panel.mcl-hidden { transform:scale(var(--mcl-scale, 1.5)) translateX(100%); pointer-events:none; }',
```

- [ ] **Step 3: 패널 생성 시 초기 스케일 변수 주입**

Edit `crawl_log.js` — `old_string`:
```
    var p = document.createElement('div');
    p.id = PANEL_ID;
    p.classList.add('mcl-hidden');
```
`new_string`:
```
    var p = document.createElement('div');
    p.id = PANEL_ID;
    p.classList.add('mcl-hidden');
    p.style.setProperty('--mcl-scale', String(panelScale));   // 현재 줌 배율(기본 1.5×) — Ctrl+휠로 변경
```

- [ ] **Step 4: 고정 상수 `DOCK_SCALE` → 가변 변수 `panelScale`**

Edit `crawl_log.js` — `old_string`:
```
  var DOCK_SCALE = 1.5;   // #mcl-panel transform:scale(1.5) — 드래그 좌표(뷰포트) → 미변환 폭 변환용
```
`new_string`:
```
  var panelScale = 1.5;   // 현재 위젯 줌 배율(--mcl-scale 와 동기). 드래그 좌표(뷰포트)→미변환 폭 변환·줌에 공용. Ctrl+휠로 applyZoom 이 변경.
```

- [ ] **Step 5: `onGripDown` 의 DOCK_SCALE 참조 2곳 교체**

Edit `crawl_log.js` — `old_string`:
```
      var dx = (startX - ev.clientX) / DOCK_SCALE;      // 왼쪽으로 끌면 넓어짐
      var w = startW + dx;
      var maxUnscaled = (window.innerWidth - 24) / DOCK_SCALE;  // 화면 밖으로 못 나가게(안전선)
```
`new_string`:
```
      var dx = (startX - ev.clientX) / panelScale;      // 왼쪽으로 끌면 넓어짐(현재 줌 반영)
      var w = startW + dx;
      var maxUnscaled = (window.innerWidth - 24) / panelScale;  // 화면 밖으로 못 나가게(안전선)
```

- [ ] **Step 6: 구문 검사**

Run:
```bash
node --check "C:/dev/_wt_widget_zoom/프로그램/_시스템/webapp/static/crawl_log.js"
```
Expected: 출력 없음(성공). 만약 `DOCK_SCALE is not defined` 같은 런타임 오류는 node --check 로는 안 잡히므로, Task 2 에서 clampPanelW 의 마지막 DOCK_SCALE 참조까지 제거한 뒤 grep 으로 잔존 확인한다.

- [ ] **Step 7: 커밋**

```bash
cd "C:/dev/_wt_widget_zoom"
git add 프로그램/_시스템/webapp/static/crawl_log.js
git commit -m "feat(crawl-widget): scale 고정상수→--mcl-scale 변수+panelScale 동적화"
```

---

## Task 2: applyZoom 신설 + onPanelWheel 재정의 + clampPanelW 제거

**Files:**
- Modify: `C:/dev/_wt_widget_zoom/프로그램/_시스템/webapp/static/crawl_log.js`

> `clampPanelW` 와 `onPanelWheel` 은 파일에서 연속으로 붙어 있다(아래 `old_string`). 이 블록을 통째로 교체하면서 `clampPanelW`(휠→폭 제거로 더 이상 안 쓰임, 마지막 DOCK_SCALE 참조 포함)를 없애고 `applyZoom` + 새 `onPanelWheel` 을 넣는다.

- [ ] **Step 1: clampPanelW 제거 + applyZoom 추가 + onPanelWheel 재정의**

Edit `crawl_log.js` — `old_string`:
```
  // 폭 클램프(붕괴 방지 하한 + 화면이탈 방지 상한, UX 제한은 없음)
  function clampPanelW(w) {
    var maxUnscaled = (window.innerWidth - 24) / DOCK_SCALE;
    return Math.max(80, Math.min(w, maxUnscaled));
  }
  // [2026-06-22] 위젯 위에서 마우스 휠 = 폭 조절(위로 넓게/아래로 좁게). 페이지·내용 스크롤 대신 크기만 변경.
  function onPanelWheel(e) {
    var panel = document.getElementById(PANEL_ID);
    if (!panel || panel.classList.contains('mcl-hidden')) return;
    if (e.ctrlKey) return;                              // 브라우저 줌(ctrl+휠)은 건드리지 않음
    e.preventDefault();                                 // 페이지/내용 스크롤 막고 크기만 변경
    var step = (e.deltaY < 0 ? 1 : -1) * 40;            // 한 노치 = 40px(미변환) ≈ 60px(화면)
    panel.style.width = clampPanelW(panel.offsetWidth + step) + 'px';
    applyDock();
  }
```
`new_string`:
```
  // [2026-06-24] 위젯 줌 — Ctrl+휠로만 위젯 자체를 1.0×~2.0×, 0.25 단계로 확대/축소.
  //   --mcl-scale 만 바꾸면 transform·height(=100vh/scale) 가 함께 따라가 화면 세로를 항상 꽉 채운다.
  //   panelScale 은 드래그 좌표 변환·도킹 계산에도 쓰이므로 같이 갱신한다. applyDock 으로 본문 밀어내기 폭 재계산.
  var ZOOM_MIN = 1.0, ZOOM_MAX = 2.0, ZOOM_STEP = 0.25;
  function applyZoom(next) {
    next = Math.round(next / ZOOM_STEP) * ZOOM_STEP;          // 0.25 격자 스냅
    next = Math.max(ZOOM_MIN, Math.min(ZOOM_MAX, next));      // 범위 클램프
    if (Math.abs(next - panelScale) < 1e-9) return;           // 변화 없음
    panelScale = next;
    var panel = document.getElementById(PANEL_ID);
    if (panel) panel.style.setProperty('--mcl-scale', String(next));
    applyDock();                                              // 화면상 폭 변동 → 본문 max-width 재계산
  }
  // [2026-06-24] 휠 입력 분기:
  //   · Ctrl(또는 ⌘)+휠 → 위젯 줌(브라우저 페이지 줌 차단). 위로 +0.25 / 아래로 −0.25.
  //   · 그냥 휠       → 아무것도 안 함(preventDefault 제거) → #mcl-rail·#mcl-cards-wrap·.mcl-card-logs
  //                     의 네이티브 세로 스크롤이 마우스 위치에 따라 살아난다(폭 조절은 드래그 전용으로 이관).
  function onPanelWheel(e) {
    var panel = document.getElementById(PANEL_ID);
    if (!panel || panel.classList.contains('mcl-hidden')) return;
    if (e.ctrlKey || e.metaKey) {
      e.preventDefault();                                     // 브라우저 페이지 줌 차단
      applyZoom(panelScale + (e.deltaY < 0 ? ZOOM_STEP : -ZOOM_STEP));
    }
    // else: 그냥 휠 — 막지 않고 통과시켜 내부 스크롤 영역이 세로 스크롤되게 둔다.
  }
```

- [ ] **Step 2: 휠 리스너 등록 주석 갱신(동작 의미 변경 반영)**

Edit `crawl_log.js` — `old_string`:
```
    p.addEventListener('wheel', onPanelWheel, { passive: false });   // 위젯 위 휠 = 폭 조절
```
`new_string`:
```
    p.addEventListener('wheel', onPanelWheel, { passive: false });   // Ctrl+휠=위젯 줌(그냥 휠은 통과→내부 스크롤)
```

- [ ] **Step 3: DOCK_SCALE / clampPanelW 잔존 참조 0 확인**

Run:
```bash
grep -n "DOCK_SCALE\|clampPanelW" "C:/dev/_wt_widget_zoom/프로그램/_시스템/webapp/static/crawl_log.js" || echo "OK-none-left"
```
Expected: `OK-none-left` (한 줄도 남으면 안 됨 — 남으면 ReferenceError 발생).

- [ ] **Step 4: 구문 검사**

Run:
```bash
node --check "C:/dev/_wt_widget_zoom/프로그램/_시스템/webapp/static/crawl_log.js"
```
Expected: 출력 없음(성공).

- [ ] **Step 5: applyZoom·panelScale·--mcl-scale 정의/사용 정합 확인**

Run:
```bash
grep -n "panelScale\|applyZoom\|--mcl-scale" "C:/dev/_wt_widget_zoom/프로그램/_시스템/webapp/static/crawl_log.js"
```
Expected: `panelScale` 선언 1 + 사용 다수, `applyZoom` 정의 1 + 호출 1(onPanelWheel), `--mcl-scale` 는 CSS 3곳 + setProperty 2곳(buildPanelDOM 초기화, applyZoom).

- [ ] **Step 6: 커밋**

```bash
cd "C:/dev/_wt_widget_zoom"
git add 프로그램/_시스템/webapp/static/crawl_log.js
git commit -m "feat(crawl-widget): Ctrl+휠 줌(1.0~2.0×) + 그냥 휠 내부 스크롤 통과, 휠→폭조절 제거"
```

---

## Task 3: base.html 캐시버스터 + 주석 갱신

**Files:**
- Modify: `C:/dev/_wt_widget_zoom/프로그램/_시스템/webapp/templates/base.html`

- [ ] **Step 1: 캐시버스터 버전 + 설명 주석 교체**

Edit `base.html` — `old_string`:
```
<script src="{{ url_for('static', filename='crawl_log.js') }}?v=20260622_wheel"></script><!-- 실시간 크롤 대시보드 (… + 도킹/폭드래그 시안B + 폭 축소 시 위젯 가로 스크롤 + 휠로 폭 조절) -->
```
`new_string`:
```
<script src="{{ url_for('static', filename='crawl_log.js') }}?v=20260624_zoomscroll"></script><!-- 실시간 크롤 대시보드 (… + 도킹/폭드래그 시안B + 폭=드래그전용 · Ctrl+휠=위젯 줌1.0~2.0× · 그냥 휠=내부 세로 스크롤) -->
```

- [ ] **Step 2: 커밋**

```bash
cd "C:/dev/_wt_widget_zoom"
git add 프로그램/_시스템/webapp/templates/base.html
git commit -m "chore(crawl-widget): crawl_log.js 캐시버스터 20260624_zoomscroll"
```

---

## Task 4: 배포 (origin/main push)

**Files:** (없음 — 배포)

> 프로젝트 배포 파이프라인: push → AWS Lightsail 자동 배포([[project_aws_deploy]]). origin/main 분기 워크트리이므로 그대로 main 에 올린다(force 금지).

- [ ] **Step 1: origin/main 최신과 충돌 없는지 rebase 확인**

Run:
```bash
cd "C:/dev/_wt_widget_zoom"
git fetch origin && git rebase origin/main
```
Expected: `Successfully rebased` 또는 이미 최신. 충돌 시 멈추고 사용자에게 보고.

- [ ] **Step 2: push**

Run:
```bash
git push origin feature/widget-zoom-scroll:main
```
Expected: main 에 3커밋 반영. (저장소 정책이 PR 필수면 대신 `git push origin feature/widget-zoom-scroll` 후 PR 생성.)

- [ ] **Step 3: 배포 완료 대기 확인**

AWS 자동 배포가 끝날 때까지 대기(수 분). 배포 success ≠ 코드 라이브일 수 있으므로([[project_musinsa_dan_stock_and_deploy_fix]]) Task 5 라이브 검증에서 실제 적용을 확인한다.

---

## Task 5: 라이브 실브라우저 검증 (live-browser-verify)

**Files:** (없음 — 검증)

> 로컬 `file://` 프리뷰는 CSS 충돌을 못 잡으므로 라이브(mou-m.com)에서만 유효([[project_sg_guide_font_and_preview_gotcha]]). 크롤 위젯이 떠 있어야 검증 가능 — 모음전 편집 화면(`/bundles/<코드>#tab=opt`)에서 크롤을 1건 돌리거나 진행 중 위젯을 띄운다.

- [ ] **Step 1: 캐시버스터 적용 확인**

브라우저 DevTools Network 또는 페이지 소스에서 `crawl_log.js?v=20260624_zoomscroll` 가 로드됐는지 확인(구버전 `20260622_wheel` 이면 강력 새로고침/배포 미완).

- [ ] **Step 2: 폭 = 드래그 전용**

- 위젯 왼쪽 모서리 핸들을 드래그 → 폭이 변한다. ✅
- 위젯 빈 곳/헤더 위에서 **그냥 휠**을 굴린다 → **폭이 안 변한다.** ✅ (구버전이면 폭이 변함)

- [ ] **Step 3: Ctrl+휠 = 위젯만 줌**

- 위젯 위에서 Ctrl+휠 ↑ → 위젯이 커진다(글자·카드 확대). 계속 올려도 **2.0× 에서 멈춤.**
- Ctrl+휠 ↓ → 작아진다. **1.0× 에서 멈춤.** 단계는 0.25씩.
- 줌 동안 **브라우저 페이지(매트릭스 본문)는 줌되지 않는다.** ✅
- 위젯 **바깥**에서 Ctrl+휠 → 브라우저 페이지 줌(정상, 그대로). 확인 후 100%로 복원.

- [ ] **Step 4: 그냥 휠 = 내부 세로 스크롤 (3영역)**

- 좌측 **모음전 목록**(`#mcl-rail`) 위 휠 → 목록 세로 스크롤. (항목이 많아야 보임)
- 우측 **소싱처 카드 전체**(`#mcl-cards-wrap`) 위 휠(카드 사이 여백/헤더) → 카드 목록 세로 스크롤.
- 한 소싱처 카드 안 **로그표**(`.mcl-card-logs`, 로그 펼친 상태) 위 휠 → 그 로그표 스크롤, 끝까지 가면 바깥 카드 목록으로 이어서 스크롤(체이닝).

- [ ] **Step 5: 줌·드래그 상호작용**

- 1.0×/1.5×/2.0× 각 배율에서 위젯 **높이가 화면 세로를 꽉 채우고** 오른쪽 가장자리에 붙어 있다.
- 줌을 바꾼 뒤 핸들 드래그로 폭을 조절 → **어긋남 없이** 동작하고 본문(매트릭스)이 그에 맞게 밀려난다(도킹).

- [ ] **Step 6: 회귀 확인(기존 기능 유지)**

- 일시중지/재개·중지 버튼, 최소화(─)/복원, 모음전 행 클릭 전환, 소싱처 카드 로그 펼침이 모두 종전대로 동작.
- DevTools Console 에 `panelScale`/`applyZoom`/`--mcl-scale` 관련 에러(ReferenceError 등) 없음.

- [ ] **Step 7: 7섹션 보고서 작성 (/ui-verify 형식)**

검증 결과를 7섹션 보고서로 정리(작업 요약·변경 파일·검증 항목/결과·발견 이슈·정직성 점검·잔여·다음 단계). 거짓 PASS 금지 — 실패 항목은 그대로 보고.

---

## Task 6: 마무리 — 워크트리 정리 + 메모리 기록

**Files:** (없음)

- [ ] **Step 1: 워크트리 제거(머지·배포·검증 완료 후)**

Run:
```bash
cd "C:/dev/모음전 프로젝트"
git worktree remove "C:/dev/_wt_widget_zoom"
```
Expected: 깨끗이 제거(미커밋 변경 없을 때).

- [ ] **Step 2: 메모리 갱신**

`crawl_log.js` 위젯 입력 관련 메모리([[project_crawl_widget_dock_resize]])에 "휠→폭조절 폐기, Ctrl+휠=줌1.0~2.0×, 그냥 휠=내부 스크롤(v=20260624_zoomscroll)" 한 줄 추가/갱신.

---

## 자기점검(작성자 확인 완료)

- **스펙 커버리지:** 요구1(폭=드래그) Task1 Step5 + Task2(휠→폭 제거) / 요구2(Ctrl+휠 줌) Task2 applyZoom·onPanelWheel / 요구3(그냥 휠 내부 스크롤 3영역) Task2(preventDefault 제거) + Task5 Step4. 결정값(1.0~2.0·0.25·기본1.5·리셋없음·체이닝 기본) 모두 반영.
- **플레이스홀더:** 없음 — 모든 Edit 에 실제 old/new 문자열, 모든 명령에 기대 출력 명시.
- **타입/식별자 정합:** `panelScale`(선언 Task1S4 → 사용 Task1S5·Task2), `applyZoom`(정의·호출 Task2), `--mcl-scale`(CSS Task1S1·S2 + setProperty Task1S3·Task2), `ZOOM_MIN/MAX/STEP`(Task2 내 정의·사용). `DOCK_SCALE`·`clampPanelW` 는 Task2S3 에서 잔존 0 확인.
