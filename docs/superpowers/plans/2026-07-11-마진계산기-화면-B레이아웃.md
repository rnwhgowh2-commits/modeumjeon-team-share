# 대량등록 마진계산기 화면 (B 레이아웃) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `/orders?tab=margin` 에 B 레이아웃(왼쪽 세로 서브탭 · 좌우 분할) 화면을 붙여, 이미 완성된 `/api/margin/*` 백엔드가 계산한 마진 분석을 화면으로 보게 한다.

**Architecture:** 백엔드(`aggregator`)가 요약·6종 집계·필터를 **서버에서 미리 계산**해 JSON 으로 주므로, 화면은 **재집계 없이 렌더만** 한다. 옛 프로그램의 9,800줄 JS 를 그대로 옮기지 않는다 — 서버가 준 값을 그린다. 행별 분류(정상·고마진·의심손실·계산불가)만 `margin_rules.js`(원본 그대로 이식)로 판정한다. 파이차트는 외부 라이브러리 없이 손으로 그린 인라인 SVG(원본 방식).

**Tech Stack:** Flask/Jinja2, 순수 바닐라 JS(프레임워크 없음), 인라인 SVG 차트, pytest(라우트·렌더), 실브라우저 검증

**디자인 확정 (2026-07-11, design-mockup 게이트):** B 레이아웃 — 왼쪽 세로 서브탭. 탭 11개 자리(만든 9개 + 블랙스팟·설정 '준비중' 흐리게). 정산 추정/확인불가 배지 없음(값 그대로). 단계 안내 = 세로 카드(①엑셀 ②기간 ③불러오기 ④분석). 시안 파일 = `바탕화면/모음전 마진계산기 시안 v1.html`.

**Worktree:** `C:\dev\_wt_margin_calc`, 브랜치 `feature/margin-calculator`. 모든 명령은 `C:\dev\_wt_margin_calc\프로그램\_시스템` 에서. **`export PYTHONIOENCODING=utf-8` 먼저** (한글 출력 시 `cp949` 오류 방지).

**베이스라인:** `origin/main`(cba43c34)에 원래 실패하는 테스트 4건 존재(`test_eleven11_orders::test_merges_preparing_shipping_claims` / `test_lotteon_orders::test_order_rows_merges_claims` / `test_order_export::test_coupang_claims_merge` / `test_sidebar_layout::test_default_contains_all_visible_items`). 우리 것 아님. 손대지 말 것. `tests/margin/` 는 이미 120+ 통과.

**전제:** 백엔드(`lemouton/margin/*`, `webapp/routes/api_margin.py`)는 이미 완성돼 있다. 이 플랜은 **화면만** 만든다. 백엔드 라우트를 수정하지 않는다 (필요하면 멈추고 보고).

---

## API 응답 계약 (화면이 의존하는 값 — 이미 구현됨)

`POST /api/margin/analyze` 와 `GET /api/margin/analyses/<id>` 가 주는 JSON:

```
{
  "analysis_id": 12,
  "counts": {buy, sell, matched, unmatched_buy, unmatched_sell, settle_estimated},
  "markets_failed": ["[coupang] 키 없음", ...],
  "matched": [ {주문일,마켓,상품명,브랜드,옵션_매출,옵션_매입,단가,판매가,실결제금액,
                정산예상금액,구매가격,순마진,마진율,수수료율,수량_매출,상품코드,매칭타입,
                일자,월,금액대,동일인연속,수량2이상,이상가,_주문미이행,_매입흔적,간단메모}, ... ],
  "unmatched_buy": [ {주문일,마켓주문번호,마켓명,상품명,옵션,구매가격,수령인,비고}, ... ],
  "unmatched_sell": [ {주문일,마켓주문번호,쇼핑몰,상품명,옵션,단가,정산예상금액,수령인}, ... ],
  "summary": {총매출,총실결제,총정산,총매입,총순마진,평균마진율,매칭건수,고유상품수,
              정상매출,정상매입,정상순마진,정상마진율,이상가건수, card_*},
  "market":[{마켓,매출,순마진,...}], "daily":[{일자,...}], "monthly":[{월,...}],
  "brand":[{브랜드,...}], "priceRange":[{금액대,...}], "product":[{상품코드,상품명,...}],
  "filters": {"brands":[...], "markets":[...], "priceRange":[...]}
}
```

`POST /api/margin/upload` → `{rows, markets, period_from, period_to}` (분석 안 함, 기간 자동 추론).
`GET /api/margin/analyses` → `[{id, created_at, created_by, period_from, period_to, buy_filename, counts, markets_failed}]` (최근 20건, bare list).
`POST /api/margin/export` → xlsx 파일 다운로드.

---

## 파일 구조

| 파일 | 책임 | 신규/수정 |
|---|---|---|
| `webapp/static/margin_rules.js` | 행별 분류(정상·고마진·의심손실·계산불가) 단일 원천 | 이식(원본 그대로) |
| `webapp/static/margin_app.js` | 화면 로직 — 업로드·분석·탭 전환·렌더·필터·차트 | 신규 |
| `webapp/static/margin.css` | B 레이아웃 스타일(좌우 분할·세로 서브탭·표·차트) | 신규 |
| `webapp/templates/orders/_margin.html` | margin 탭 마크업(정적 뼈대 — 데이터는 JS 채움) | 신규 |
| `webapp/templates/orders/index.html` | `tab == 'margin'` 분기에서 `_margin.html` include | 수정 |
| `tests/margin/test_margin_ui_routes.py` | 화면 라우트 렌더 + 정적파일 로드 검증 | 신규 |
| `tests/margin/test_margin_rules_js.py` | margin_rules.js 원본 동치 + 분류 규칙 | 신규 |

**옛 원본 (읽기 전용):** `C:\dev\대량등록 마진계산기\static\js\margin_rules.js`, `templates/index.html`.

---

## Task 1: margin_rules.js 원본 그대로 이식

**Files:**
- Create: `프로그램/_시스템/webapp/static/margin_rules.js`
- Test: `프로그램/_시스템/tests/margin/test_margin_rules_js.py`

행별 분류 규칙(정산0/매입0)은 원본 `margin_rules.js` 에만 정의돼 있고, "index.html 인라인에 재작성 금지"라고 파일 주석이 못박고 있다. 그대로 옮긴다.

- [ ] **Step 1: 실패하는 테스트 작성**

`프로그램/_시스템/tests/margin/test_margin_rules_js.py`:
```python
# -*- coding: utf-8 -*-
"""margin_rules.js — 원본 그대로 이식됐는지 + 분류 규칙(Node 실행)."""
import pathlib
import shutil
import subprocess

import pytest

PORTED = pathlib.Path(__file__).resolve().parents[2] / "webapp" / "static" / "margin_rules.js"
ORIGINAL = pathlib.Path(r"C:\dev\대량등록 마진계산기\static\js\margin_rules.js")


def test_file_exists():
    assert PORTED.exists()


def test_verbatim_from_original():
    if not ORIGINAL.exists():
        pytest.skip(f"원본 없음: {ORIGINAL}")
    assert PORTED.read_text(encoding="utf-8") == ORIGINAL.read_text(encoding="utf-8"), \
        "margin_rules.js 가 원본과 다릅니다 — 규칙은 원본 그대로 이식"


@pytest.mark.skipif(shutil.which("node") is None, reason="node 없음")
def test_classify_rules_via_node():
    script = r"""
    const MR = require(process.argv[1]);
    const loss = {정산예상금액:0, 구매가격:50000};
    const high = {정산예상금액:70000, 구매가격:0};
    const normal = {정산예상금액:70000, 구매가격:50000};
    const uncomp = {정산예상금액:0, 구매가격:0};
    const out = [MR.classify(loss), MR.classify(high), MR.classify(normal), MR.classify(uncomp)];
    console.log(JSON.stringify(out));
    """
    r = subprocess.run(["node", "-e", script, str(PORTED)],
                       capture_output=True, text=True)
    assert r.returncode == 0, r.stderr
    assert r.stdout.strip().endswith('["loss","highmargin","normal","uncomputable"]')
```

- [ ] **Step 2: 실패 확인**

Run: `export PYTHONIOENCODING=utf-8; python -m pytest tests/margin/test_margin_rules_js.py -v`
Expected: FAIL — `test_file_exists` 실패 (파일 없음)

- [ ] **Step 3: 원본 복사**

```bash
python - <<'PY'
import io, shutil, pathlib
src = pathlib.Path(r"C:\dev\대량등록 마진계산기\static\js\margin_rules.js")
dst = pathlib.Path("webapp/static/margin_rules.js")
dst.write_bytes(src.read_bytes())
print("copied", dst, dst.stat().st_size, "bytes")
PY
```

내용을 손대지 않는다. `test_verbatim_from_original` 이 바이트 동치를 요구한다.

- [ ] **Step 4: 통과 확인**

Run: `export PYTHONIOENCODING=utf-8; python -m pytest tests/margin/test_margin_rules_js.py -v`
Expected: `test_file_exists`·`test_verbatim_from_original` PASS. node 있으면 `test_classify_rules_via_node` 도 PASS, 없으면 skip.

- [ ] **Step 5: 커밋**

```bash
git add 프로그램/_시스템/webapp/static/margin_rules.js 프로그램/_시스템/tests/margin/test_margin_rules_js.py
git commit -m "feat(margin-ui): margin_rules.js 원본 그대로 이식 (행별 분류 단일원천)"
```

---

## Task 2: margin 탭 뼈대 마크업 + 라우트 include

**Files:**
- Create: `프로그램/_시스템/webapp/templates/orders/_margin.html`
- Modify: `프로그램/_시스템/webapp/templates/orders/index.html`
- Create: `프로그램/_시스템/webapp/static/margin.css`
- Test: `프로그램/_시스템/tests/margin/test_margin_ui_routes.py`

정적 뼈대만 만든다. 데이터는 Task 4+ 에서 JS 로 채운다. B 레이아웃 = 좌우 분할(왼쪽 세로 서브탭 · 오른쪽 콘텐츠).

- [ ] **Step 1: 실패하는 테스트 작성**

`프로그램/_시스템/tests/margin/test_margin_ui_routes.py`:
```python
# -*- coding: utf-8 -*-
"""margin 탭 화면 — 렌더 + 정적파일 로드 + 서브탭 자리."""
import pytest


@pytest.fixture
def client(monkeypatch):
    monkeypatch.setenv("DISABLE_AUTH", "1")
    import app as appmod
    appmod.app.config["TESTING"] = True
    return appmod.app.test_client()


def test_margin_tab_renders(client):
    r = client.get("/orders/?tab=margin")
    assert r.status_code == 200
    html = r.get_data(as_text=True)
    assert 'id="margin-app"' in html


def test_left_subnav_has_11_slots(client):
    html = client.get("/orders/?tab=margin").get_data(as_text=True)
    for label in ("요약", "전체내역", "일별", "월별", "브랜드별", "금액대별",
                  "상품별", "마켓별", "소싱처별"):
        assert label in html, label
    # 준비중 2개 자리
    assert "블랙스팟" in html and "설정" in html
    assert html.count('data-mtab=') >= 9


def test_static_assets_referenced(client):
    html = client.get("/orders/?tab=margin").get_data(as_text=True)
    assert "margin_rules.js" in html
    assert "margin_app.js" in html
    assert "margin.css" in html


def test_upload_steps_present(client):
    html = client.get("/orders/?tab=margin").get_data(as_text=True)
    assert "더망고" in html      # ① 엑셀
    assert "분석" in html          # ④ 분석 버튼
```

- [ ] **Step 2: 실패 확인**

Run: `export PYTHONIOENCODING=utf-8; python -m pytest tests/margin/test_margin_ui_routes.py -v`
Expected: FAIL — `id="margin-app"` 없음

- [ ] **Step 3: `_margin.html` 생성**

`프로그램/_시스템/webapp/templates/orders/_margin.html`:
```html
<link rel="stylesheet" href="{{ url_for('static', filename='margin.css') }}?v=20260711a">
<div id="margin-app">

  <div class="mg-upload">
    <div class="mg-ucard">
      <div class="mg-urow" data-step="buy">
        <span class="mg-ubadge">1</span>
        <div class="mg-umain"><div class="mg-ut">더망고 엑셀 올리기</div>
          <div class="mg-us" id="mg-buy-status">파일을 선택하거나 끌어다 놓으세요</div></div>
        <label class="mg-uact p">파일 선택<input type="file" id="mg-buy-file" accept=".xls,.xlsx,.htm,.html" hidden></label>
      </div>
      <div class="mg-urow" data-step="period">
        <span class="mg-ubadge">2</span>
        <div class="mg-umain"><div class="mg-ut">조회 기간</div>
          <div class="mg-us">
            <input type="date" id="mg-since"> ~ <input type="date" id="mg-until">
            <span id="mg-period-auto" class="mg-hint"></span></div></div>
      </div>
      <div class="mg-urow" data-step="fetch">
        <span class="mg-ubadge">3</span>
        <div class="mg-umain"><div class="mg-ut">주문 불러오기 + 분석</div>
          <div class="mg-us" id="mg-analyze-status">더망고 엑셀을 먼저 올리세요</div></div>
        <button class="mg-uact p" id="mg-analyze-btn" disabled>분석 시작</button>
      </div>
      <details class="mg-optional">
        <summary>옥션·G마켓 주문이 있으면 — 샵마인 엑셀 추가 (선택)</summary>
        <label class="mg-uact g">샵마인 엑셀<input type="file" id="mg-sm-file" accept=".xls,.xlsx" hidden></label>
        <span id="mg-sm-status" class="mg-hint"></span>
      </details>
    </div>
  </div>

  <div id="mg-warn" class="mg-warn" hidden></div>

  <div class="mg-body">
    <nav class="mg-subnav" id="mg-subnav">
      <a data-mtab="summary" class="on">요약</a>
      <a data-mtab="all">전체내역</a>
      <a data-mtab="daily">일별</a>
      <a data-mtab="monthly">월별</a>
      <a data-mtab="brand">브랜드별</a>
      <a data-mtab="pricerange">금액대별</a>
      <a data-mtab="product">상품별</a>
      <a data-mtab="market">마켓별</a>
      <a data-mtab="source">소싱처별</a>
      <a class="soon" title="다음 업데이트에서 제공">⚫ 블랙스팟 · 준비중</a>
      <a class="soon" title="다음 업데이트에서 제공">⚙️ 설정 · 준비중</a>
    </nav>
    <section class="mg-content" id="mg-content">
      <div class="mg-empty" id="mg-empty">더망고 엑셀을 올리고 <b>분석 시작</b>을 누르면 결과가 여기 표시됩니다.</div>
      <div id="mg-panes" hidden></div>
    </section>
  </div>

  <div class="mg-history">
    <div class="mg-hh">최근 분석</div>
    <div id="mg-history-list" class="mg-hlist"></div>
  </div>
</div>
<script src="{{ url_for('static', filename='margin_rules.js') }}?v=20260711a"></script>
<script src="{{ url_for('static', filename='margin_app.js') }}?v=20260711a"></script>
```

- [ ] **Step 4: `orders/index.html` 의 margin 분기에 include**

`orders/index.html` 에서 `tab == 'margin'` 을 처리하는 `{% elif tab == 'margin' %}` (또는 해당 분기)를 찾아 그 블록 내용을 다음으로 교체:
```jinja
  {% elif tab == 'margin' %}
    {% include 'orders/_margin.html' %}
```
기존 margin 분기가 없고 다른 탭들이 `{% if %}/{% elif %}` 로 나뉘어 있으면, `list`·`sales` 등과 같은 층에 `{% elif tab == 'margin' %}` 를 추가한다. **다른 탭 분기는 건드리지 않는다.**

- [ ] **Step 5: `margin.css` 최소 뼈대 생성**

`프로그램/_시스템/webapp/static/margin.css`:
```css
#margin-app{--mg-line:#E5E8EB;--mg-ink:#191F28;--mg-sub:#6B7684;--mg-blue:#3182F6;
  --mg-blue-bg:#E8F1FE;--mg-green:#12B886;--mg-red:#F03E3E;--mg-amber:#F59F00;font-size:13px;color:var(--mg-ink)}
#margin-app .mg-ucard{background:#fff;border:1px solid var(--mg-line);border-radius:12px;padding:16px;margin-bottom:16px}
#margin-app .mg-urow{display:flex;gap:12px;align-items:center;padding:10px 0;border-bottom:1px dashed var(--mg-line)}
#margin-app .mg-urow:last-of-type{border-bottom:0}
#margin-app .mg-ubadge{width:24px;height:24px;border-radius:50%;background:#F1F3F5;color:#868E96;
  display:flex;align-items:center;justify-content:center;font-weight:700;flex:0 0 auto}
#margin-app .mg-urow.done .mg-ubadge{background:#E6F7F1;color:#0C7A5A}
#margin-app .mg-umain{flex:1}
#margin-app .mg-ut{font-weight:700}
#margin-app .mg-us{font-size:12px;color:var(--mg-sub);margin-top:2px}
#margin-app .mg-hint{font-size:11px;color:#8B95A1;margin-left:6px}
#margin-app .mg-uact{font-weight:700;padding:7px 14px;border-radius:7px;cursor:pointer;border:0;font-size:12.5px}
#margin-app .mg-uact.p{background:var(--mg-blue);color:#fff}
#margin-app .mg-uact.p:disabled{background:#C1C8CE;cursor:default}
#margin-app .mg-uact.g{background:#F1F3F5;color:#4E5968}
#margin-app .mg-optional{margin-top:10px;font-size:12px;color:var(--mg-sub)}
#margin-app .mg-body{display:grid;grid-template-columns:160px 1fr;gap:16px}
#margin-app .mg-subnav{background:#fff;border:1px solid var(--mg-line);border-radius:12px;padding:7px;height:max-content}
#margin-app .mg-subnav a{display:block;padding:9px 12px;border-radius:7px;color:#4E5968;cursor:pointer;margin-bottom:1px}
#margin-app .mg-subnav a.on{background:var(--mg-blue-bg);color:#1B64DA;font-weight:700}
#margin-app .mg-subnav a.soon{color:#B0B8C1;cursor:default;font-size:12px}
#margin-app .mg-content{min-height:300px}
#margin-app .mg-empty{padding:60px 20px;text-align:center;color:var(--mg-sub);background:#fff;
  border:1px solid var(--mg-line);border-radius:12px}
#margin-app .mg-warn{background:#FEF2F2;border:1px solid #FCA5A5;color:#991B1B;border-radius:10px;padding:10px 14px;margin-bottom:14px;font-size:12.5px}
#margin-app .mg-history{margin-top:20px}
#margin-app .mg-hh{font-weight:700;margin-bottom:8px}
#margin-app .mg-hlist{display:flex;flex-direction:column;gap:6px}
```

- [ ] **Step 6: `margin_app.js` 빈 파일 생성 (Task 3 에서 채움)**

```bash
printf '/* margin_app.js — Task 3+ 에서 구현 */\n' > 프로그램/_시스템/webapp/static/margin_app.js
```

- [ ] **Step 7: 통과 확인**

Run: `export PYTHONIOENCODING=utf-8; python -m pytest tests/margin/test_margin_ui_routes.py -v`
Expected: 4 passed

문제 시: `DISABLE_AUTH` 로도 라우트가 인증을 요구하면, 기존 다른 라우트 테스트(`tests/orders/test_orders_tab.py`)가 client 를 어떻게 만드는지 보고 그 패턴을 따른다.

- [ ] **Step 8: 커밋**

```bash
git add 프로그램/_시스템/webapp/templates/orders/_margin.html \
        프로그램/_시스템/webapp/templates/orders/index.html \
        프로그램/_시스템/webapp/static/margin.css \
        프로그램/_시스템/webapp/static/margin_app.js \
        프로그램/_시스템/tests/margin/test_margin_ui_routes.py
git commit -m "feat(margin-ui): B 레이아웃 뼈대 — 좌우 분할 + 세로 서브탭 11자리 + 단계 카드"
```

---

## Task 3: 업로드 → 기간 자동 → 분석 배선

**Files:**
- Modify: `프로그램/_시스템/webapp/static/margin_app.js`

JS 는 pytest 로 직접 못 돌리므로, 이 태스크는 **실브라우저 검증**으로 확인한다(Step 4). 순수 함수(숫자 포맷·기간 계산)만 별도 테스트.

- [ ] **Step 1: 순수 함수 테스트 (Node)**

`프로그램/_시스템/tests/margin/test_margin_app_js.py`:
```python
# -*- coding: utf-8 -*-
"""margin_app.js 순수 함수 — Node 로 직접 실행."""
import pathlib
import shutil
import subprocess

import pytest

APP = pathlib.Path(__file__).resolve().parents[2] / "webapp" / "static" / "margin_app.js"


@pytest.mark.skipif(shutil.which("node") is None, reason="node 없음")
def test_won_formats_thousands():
    r = subprocess.run(["node", "-e",
        f"const M=require('{APP.as_posix()}');"
        "console.log(M.__test.won(1325722)+'|'+M.__test.won('')+'|'+M.__test.won(-98000))"],
        capture_output=True, text=True)
    assert r.returncode == 0, r.stderr
    assert r.stdout.strip() == "1,325,722|0|-98,000"


@pytest.mark.skipif(shutil.which("node") is None, reason="node 없음")
def test_margin_app_exports_testhook():
    r = subprocess.run(["node", "-e",
        f"const M=require('{APP.as_posix()}');console.log(typeof M.__test.won)"],
        capture_output=True, text=True)
    assert r.stdout.strip() == "function"
```

- [ ] **Step 2: 실패 확인**

Run: `export PYTHONIOENCODING=utf-8; python -m pytest tests/margin/test_margin_app_js.py -v`
Expected: node 있으면 FAIL(`__test` 없음), 없으면 skip

- [ ] **Step 3: `margin_app.js` 구현 (업로드·기간·분석 + Node export)**

`프로그램/_시스템/webapp/static/margin_app.js` 전체:
```javascript
/* margin_app.js — 마진계산기 화면. 서버가 계산한 결과를 렌더만 한다. */
(function (root) {
  'use strict';
  var state = { data: null };

  function won(v) {
    var n = Number(String(v == null ? 0 : v).replace(/,/g, ''));
    if (!isFinite(n)) n = 0;
    return n.toLocaleString('en-US');
  }
  function $(id) { return document.getElementById(id); }
  function post(url, body, isForm) {
    return fetch(url, isForm ? { method: 'POST', body: body }
      : { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(body) })
      .then(function (r) { return r.json().then(function (j) { return { ok: r.ok, status: r.status, j: j }; }); });
  }

  function init() {
    if (!document.getElementById('margin-app')) return;
    var buyFile = $('mg-buy-file'), smFile = $('mg-sm-file');
    buyFile.addEventListener('change', function () { uploadBuy(buyFile.files[0]); });
    smFile.addEventListener('change', function () { uploadShopmine(smFile.files[0]); });
    $('mg-analyze-btn').addEventListener('click', analyze);
    bindSubnav();
    loadHistory();
  }

  function uploadBuy(file) {
    if (!file) return;
    var fd = new FormData(); fd.append('file', file);
    $('mg-buy-status').textContent = '읽는 중…';
    post('/api/margin/upload', fd, true).then(function (res) {
      if (!res.ok) { $('mg-buy-status').textContent = '오류: ' + (res.j.error || res.status); return; }
      var j = res.j;
      $('mg-buy-status').textContent = '매입 ' + j.rows + '건 · ' + (j.markets || []).join(', ');
      $('mg-since').value = j.period_from; $('mg-until').value = j.period_to;
      $('mg-period-auto').textContent = '(엑셀에서 자동 추론 — 바꿀 수 있어요)';
      document.querySelector('.mg-urow[data-step=buy]').classList.add('done');
      $('mg-analyze-btn').disabled = false;
      $('mg-analyze-status').textContent = '기간 확인 후 분석 시작을 누르세요';
    });
  }

  function uploadShopmine(file) {
    if (!file) return;
    var fd = new FormData(); fd.append('file', file);
    $('mg-sm-status').textContent = '읽는 중…';
    post('/api/margin/upload-shopmine', fd, true).then(function (res) {
      $('mg-sm-status').textContent = res.ok
        ? ('옥션·G마켓 ' + res.j.rows + '건 추가됨') : ('오류: ' + (res.j.error || res.status));
    });
  }

  function analyze() {
    var since = $('mg-since').value, until = $('mg-until').value;
    if (!since || !until) { $('mg-analyze-status').textContent = '조회 기간을 정하세요'; return; }
    $('mg-analyze-btn').disabled = true;
    $('mg-analyze-status').textContent = '마켓 주문 불러오는 중… (수십 초 걸릴 수 있어요)';
    post('/api/margin/analyze', { since: since, until: until }).then(function (res) {
      $('mg-analyze-btn').disabled = false;
      if (!res.ok) {
        $('mg-analyze-status').textContent = '실패: ' + (res.j.error || res.status);
        showWarn([res.j.error || ('분석 실패 (' + res.status + ')')]);
        return;
      }
      $('mg-analyze-status').textContent = '분석 완료 · ' + res.j.counts.matched + '건 매칭';
      render(res.j);
      loadHistory();
    });
  }

  function showWarn(lines) {
    var w = $('mg-warn');
    if (!lines || !lines.length) { w.hidden = true; return; }
    w.hidden = false;
    w.innerHTML = lines.map(function (l) { return '⚠ ' + l; }).join('<br>');
  }

  function render(data) {
    state.data = data;
    $('mg-empty').hidden = true; $('mg-panes').hidden = false;
    showWarn(data.markets_failed || []);
    if (root.MG_RENDER) root.MG_RENDER(data);  // Task 4+ 가 채우는 렌더러
  }

  function bindSubnav() {
    var nav = $('mg-subnav');
    nav.addEventListener('click', function (e) {
      var a = e.target.closest('a[data-mtab]'); if (!a) return;
      nav.querySelectorAll('a').forEach(function (x) { x.classList.remove('on'); });
      a.classList.add('on');
      if (state.data && root.MG_SHOW_TAB) root.MG_SHOW_TAB(a.dataset.mtab, state.data);
    });
  }

  function loadHistory() {
    fetch('/api/margin/analyses').then(function (r) { return r.json(); }).then(function (list) {
      var box = $('mg-history-list'); if (!box) return;
      if (!list || !list.length) { box.innerHTML = '<div class="mg-hint">아직 분석 기록이 없어요.</div>'; return; }
      box.innerHTML = list.map(function (a) {
        return '<a class="mg-hitem" data-id="' + a.id + '">'
          + a.created_at.slice(0, 16).replace('T', ' ') + ' · '
          + a.buy_filename + ' · 매칭 ' + (a.counts && a.counts.matched || 0) + '건</a>';
      }).join('');
      box.querySelectorAll('.mg-hitem').forEach(function (el) {
        el.addEventListener('click', function () { openAnalysis(el.dataset.id); });
      });
    });
  }
  function openAnalysis(id) {
    fetch('/api/margin/analyses/' + id).then(function (r) { return r.json(); }).then(function (j) {
      if (j && j.matched) { render(j); $('mg-analyze-status').textContent = '과거 분석 불러옴 · ' + id; }
    });
  }

  if (typeof document !== 'undefined') {
    if (document.readyState !== 'loading') init();
    else document.addEventListener('DOMContentLoaded', init);
  }
  var api = { won: won };
  if (typeof module !== 'undefined' && module.exports) module.exports = { __test: api };
  root.MG_APP = api;
})(typeof window !== 'undefined' ? window : globalThis);
```

- [ ] **Step 4: 통과 확인 + 실브라우저 사전 확인**

```bash
export PYTHONIOENCODING=utf-8
python -m pytest tests/margin/test_margin_app_js.py -v      # node 있으면 2 passed
python -m pytest tests/margin/ -q                            # 회귀 없음
```
브라우저 검증은 Task 8 에서 한꺼번에. 여기서는 `margin_app.js` 가 문법 오류 없이 로드되는지만: `node --check webapp/static/margin_app.js` → 오류 없음.

- [ ] **Step 5: 커밋**

```bash
git add 프로그램/_시스템/webapp/static/margin_app.js 프로그램/_시스템/tests/margin/test_margin_app_js.py
git commit -m "feat(margin-ui): 업로드→기간 자동추론→분석 배선 + 최근분석 목록"
```

---

## Task 4: 요약 탭 렌더 (KPI + 미니카드 + 파이차트)

**Files:**
- Create: `프로그램/_시스템/webapp/static/margin_render.js`
- Modify: `프로그램/_시스템/webapp/templates/orders/_margin.html` (스크립트 추가)

렌더러를 별도 파일로 분리(관심사 분리 — `margin_app.js` 는 배선, `margin_render.js` 는 그리기).

- [ ] **Step 1: 순수 함수 테스트 (파이차트 각도 계산)**

`프로그램/_시스템/tests/margin/test_margin_render_js.py`:
```python
# -*- coding: utf-8 -*-
"""margin_render.js — 파이 각도 계산 등 순수 함수."""
import pathlib
import shutil
import subprocess

import pytest

R = pathlib.Path(__file__).resolve().parents[2] / "webapp" / "static" / "margin_render.js"


@pytest.mark.skipif(shutil.which("node") is None, reason="node 없음")
def test_pie_slices_sum_full_circle():
    r = subprocess.run(["node", "-e",
        f"const R=require('{R.as_posix()}');"
        "const s=R.__test.pieSlices([{v:30},{v:70}],'v');"
        "console.log(JSON.stringify([s.length, Math.round(s[1].end)]))"],
        capture_output=True, text=True)
    assert r.returncode == 0, r.stderr
    assert r.stdout.strip() == "[2,360]"


@pytest.mark.skipif(shutil.which("node") is None, reason="node 없음")
def test_pie_empty_is_safe():
    r = subprocess.run(["node", "-e",
        f"const R=require('{R.as_posix()}');console.log(R.__test.pieSlices([],'v').length)"],
        capture_output=True, text=True)
    assert r.stdout.strip() == "0"
```

- [ ] **Step 2: 실패 확인**

Run: `export PYTHONIOENCODING=utf-8; python -m pytest tests/margin/test_margin_render_js.py -v`
Expected: node 있으면 FAIL(파일 없음), 없으면 skip

- [ ] **Step 3: `margin_render.js` 생성 (요약 탭 + 공통 유틸)**

`프로그램/_시스템/webapp/static/margin_render.js`:
```javascript
/* margin_render.js — 서버가 준 분석 JSON 을 탭별로 그린다. 재집계 없음. */
(function (root) {
  'use strict';
  function won(v){ var n=Number(String(v==null?0:v).replace(/,/g,'')); return (isFinite(n)?n:0).toLocaleString('en-US'); }
  function esc(s){ return String(s==null?'':s).replace(/[&<>]/g,function(c){return {'&':'&amp;','<':'&lt;','>':'&gt;'}[c];}); }

  var PIE = ['#3182F6','#12B886','#F59F00','#F03E3E','#7048E8','#1098AD','#E8590C','#868E96'];
  function pieSlices(rows, key){
    var tot = (rows||[]).reduce(function(a,r){return a+Math.max(0,Number(r[key])||0);},0);
    if (tot<=0) return [];
    var acc=0, out=[];
    (rows||[]).forEach(function(r){
      var v=Math.max(0,Number(r[key])||0); var start=acc/tot*360; acc+=v; var end=acc/tot*360;
      out.push({start:start,end:end,v:v});
    });
    return out;
  }
  function pieSvg(rows, key, labelKey){
    var s=pieSlices(rows,key); if(!s.length) return '<div class="mg-hint">데이터 없음</div>';
    var cx=70,cy=70,r=60, paths=s.map(function(sl,i){
      var a0=(sl.start-90)*Math.PI/180, a1=(sl.end-90)*Math.PI/180;
      var x0=cx+r*Math.cos(a0), y0=cy+r*Math.sin(a0), x1=cx+r*Math.cos(a1), y1=cy+r*Math.sin(a1);
      var large=(sl.end-sl.start)>180?1:0;
      if (sl.end-sl.start>=359.999) return '<circle cx="'+cx+'" cy="'+cy+'" r="'+r+'" fill="'+PIE[i%PIE.length]+'"/>';
      return '<path d="M'+cx+','+cy+' L'+x0+','+y0+' A'+r+','+r+' 0 '+large+' 1 '+x1+','+y1+' Z" fill="'+PIE[i%PIE.length]+'"/>';
    }).join('');
    var legend=(rows||[]).map(function(rw,i){
      return '<div class="mg-leg"><span class="mg-dot" style="background:'+PIE[i%PIE.length]+'"></span>'
        + esc(rw[labelKey]) + ' <b>'+won(rw[key])+'</b></div>';
    }).join('');
    return '<div class="mg-pie"><svg viewBox="0 0 140 140" width="140" height="140">'+paths+'</svg><div class="mg-legs">'+legend+'</div></div>';
  }

  function renderSummary(d){
    var s=d.summary||{}, c=d.counts||{};
    var kpis=[['총매출',s.총매출],['총정산',s.총정산],['총매입',s.총매입],['총순마진',s.총순마진,'hl'],['평균 마진율',(s.평균마진율||0),'pct']];
    var kpiHtml=kpis.map(function(k){
      var val=k[2]==='pct'?((Number(k[1])||0).toFixed(1)+'<span class="u"> %</span>'):(won(k[1])+'<span class="u"> 원</span>');
      return '<div class="mg-kpi '+(k[2]==='hl'?'hl':'')+'"><div class="k">'+k[0]+'</div><div class="v">'+val+'</div></div>';
    }).join('');
    var minis=[['정상',s.정상,''],['고마진',s.고마진,'g'],['의심손실',s.의심손실,'r'],['계산불가',s.계산불가,'a']];
    var miniHtml=minis.map(function(m){
      return '<div class="mg-mini '+m[2]+'"><span class="k">'+m[0]+'</span><span class="v">'+(m[1]||0)+'</span></div>';
    }).join('');
    var estNote = c.settle_estimated ? '<div class="mg-hint" style="margin-top:6px">※ 정산 미확정(추정) '+c.settle_estimated+'건 포함</div>' : '';
    return '<div class="mg-kpis">'+kpiHtml+'</div><div class="mg-minis">'+miniHtml+'</div>'+estNote
      + '<div class="mg-charts"><div class="mg-chart"><div class="mg-ct">마켓별 매출</div>'+pieSvg(d.market,'매출','마켓')+'</div>'
      + '<div class="mg-chart"><div class="mg-ct">마켓별 순마진</div>'+pieSvg(d.market,'순마진','마켓')+'</div></div>';
  }

  root.MG_RENDERERS = { summary: renderSummary, __won: won, __esc: esc, __pieSvg: pieSvg };
  var api = { pieSlices: pieSlices, won: won };
  if (typeof module!=='undefined'&&module.exports) module.exports={__test:api};
})(typeof window!=='undefined'?window:globalThis);
```

- [ ] **Step 4: `_margin.html` 에 렌더러·show-tab 배선 스크립트 추가**

`_margin.html` 의 `margin_app.js` 로드 줄 **위에** `margin_render.js` 를 추가하고, `margin_app.js` 로드 줄 **아래**에 탭 라우팅 스크립트 추가:
```html
<script src="{{ url_for('static', filename='margin_render.js') }}?v=20260711a"></script>
<script src="{{ url_for('static', filename='margin_app.js') }}?v=20260711a"></script>
<script>
  window.MG_RENDER = function(d){ window.MG_SHOW_TAB('summary', d); };
  window.MG_SHOW_TAB = function(tab, d){
    var r = (window.MG_RENDERERS || {})[tab];
    document.getElementById('mg-panes').innerHTML = r ? r(d)
      : '<div class="mg-empty">이 탭은 다음 태스크에서 구현됩니다.</div>';
  };
</script>
```
(기존 `margin_app.js` 로드 줄 하나를 이 3-스크립트 블록으로 교체.)

- [ ] **Step 5: 요약 스타일 추가 (`margin.css` 말미)**

```css
#margin-app .mg-kpis{display:grid;grid-template-columns:repeat(5,1fr);gap:10px;margin-bottom:12px}
#margin-app .mg-kpi{background:#fff;border:1px solid var(--mg-line);border-radius:12px;padding:13px 14px}
#margin-app .mg-kpi .k{font-size:11.5px;color:var(--mg-sub)}
#margin-app .mg-kpi .v{font-size:19px;font-weight:700;margin-top:4px;font-variant-numeric:tabular-nums}
#margin-app .mg-kpi .u{font-size:12px;color:var(--mg-sub);font-weight:400}
#margin-app .mg-kpi.hl{background:var(--mg-blue-bg);border-color:#cfe0fd}
#margin-app .mg-minis{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-bottom:16px}
#margin-app .mg-mini{background:#fff;border:1px solid var(--mg-line);border-radius:10px;padding:10px 14px;display:flex;justify-content:space-between;align-items:center}
#margin-app .mg-mini .v{font-weight:700;font-variant-numeric:tabular-nums}
#margin-app .mg-mini.g .v{color:var(--mg-green)}#margin-app .mg-mini.r .v{color:var(--mg-red)}#margin-app .mg-mini.a .v{color:var(--mg-amber)}
#margin-app .mg-charts{display:grid;grid-template-columns:1fr 1fr;gap:12px}
#margin-app .mg-chart{background:#fff;border:1px solid var(--mg-line);border-radius:12px;padding:14px}
#margin-app .mg-ct{font-weight:700;margin-bottom:10px}
#margin-app .mg-pie{display:flex;gap:14px;align-items:center}
#margin-app .mg-legs{font-size:12px}#margin-app .mg-leg{padding:2px 0}
#margin-app .mg-dot{display:inline-block;width:9px;height:9px;border-radius:2px;margin-right:5px;vertical-align:middle}
```

- [ ] **Step 6: 통과 확인**

```bash
export PYTHONIOENCODING=utf-8
python -m pytest tests/margin/test_margin_render_js.py -v   # node 있으면 2 passed
node --check webapp/static/margin_render.js                 # 문법 OK
python -m pytest tests/margin/ -q                           # 회귀 없음
```

- [ ] **Step 7: 커밋**

```bash
git add 프로그램/_시스템/webapp/static/margin_render.js \
        프로그램/_시스템/webapp/static/margin.css \
        프로그램/_시스템/webapp/templates/orders/_margin.html \
        프로그램/_시스템/tests/margin/test_margin_render_js.py
git commit -m "feat(margin-ui): 요약 탭 — KPI·미니카드·마켓 파이차트(인라인 SVG)"
```

---

## Task 5: 전체내역 탭 (표 + 필터 + 행별 분류색)

**Files:**
- Modify: `프로그램/_시스템/webapp/static/margin_render.js`
- Modify: `프로그램/_시스템/webapp/static/margin.css`

`margin_rules.js`(Task 1)의 `MR.classify(row)` 로 행별 색을 정한다. 필터 = 마켓·브랜드·금액대 드롭다운(서버가 준 `filters`).

- [ ] **Step 1: 분류→행클래스 매핑 테스트 (Node)**

`test_margin_render_js.py` 에 추가:
```python
@pytest.mark.skipif(shutil.which("node") is None, reason="node 없음")
def test_row_class_from_classify():
    import pathlib
    RULES = (pathlib.Path(__file__).resolve().parents[2] / "webapp" / "static" / "margin_rules.js").as_posix()
    r = subprocess.run(["node", "-e",
        f"global.window=global; require('{RULES}');"
        f"const R=require('{R.as_posix()}');"
        "console.log(R.__test.rowClass({정산예상금액:0,구매가격:50000})+'|'+R.__test.rowClass({정산예상금액:70000,구매가격:0}))"],
        capture_output=True, text=True)
    assert r.returncode == 0, r.stderr
    assert r.stdout.strip() == "mg-loss|mg-high"
```

- [ ] **Step 2: 실패 확인** → `export PYTHONIOENCODING=utf-8; python -m pytest tests/margin/test_margin_render_js.py::test_row_class_from_classify -v` → FAIL

- [ ] **Step 3: `margin_render.js` 에 전체내역 렌더 + rowClass 추가**

`root.MG_RENDERERS = {...}` 줄 **직전에** 추가:
```javascript
  function rowClass(r){
    var MR = root.MR; if (!MR) return '';
    var c = MR.classify(r);
    return c==='loss'?'mg-loss':c==='highmargin'?'mg-high':c==='uncomputable'?'mg-uncomp':'';
  }
  function renderAll(d){
    var f = d.filters||{};
    function opts(arr){ return ['<option value="">전체</option>'].concat((arr||[]).map(function(x){return '<option>'+esc(x)+'</option>';})).join(''); }
    var bar = '<div class="mg-filter">'
      + '<select data-f="마켓">'+opts(f.markets)+'</select>'
      + '<select data-f="브랜드">'+opts(f.brands)+'</select>'
      + '<select data-f="금액대">'+opts(f.priceRange)+'</select>'
      + '<button class="mg-xlsx" data-export="detail_filtered">필터 결과 엑셀</button></div>';
    var head = '<tr><th>주문일</th><th>마켓</th><th>상품 · 옵션</th><th class="num">판매가</th>'
      + '<th class="num">정산예정</th><th class="num">매입</th><th class="num">순마진</th>'
      + '<th class="num">마진율</th><th class="ctr">매칭</th></tr>';
    var body = (d.matched||[]).map(function(r){
      return '<tr class="'+rowClass(r)+'" data-mk="'+esc(r.마켓)+'" data-br="'+esc(r.브랜드)+'" data-pr="'+esc(r.금액대)+'">'
        + '<td>'+esc(r.주문일).slice(0,10)+'</td><td class="ctr">'+esc(r.마켓)+'</td>'
        + '<td>'+esc(r.상품명)+' · '+esc(r.옵션_매출)+'</td>'
        + '<td class="num">'+won(r.판매가)+'</td><td class="num">'+won(r.정산예상금액)+'</td>'
        + '<td class="num">'+won(r.구매가격)+'</td><td class="num">'+won(r.순마진)+'</td>'
        + '<td class="num">'+(Number(r.마진율)||0).toFixed(1)+'</td><td class="ctr">'+esc(r.매칭타입)+'</td></tr>';
    }).join('');
    return bar + '<div class="mg-tblwrap"><table class="mg-tbl"><thead>'+head+'</thead><tbody>'+body+'</tbody></table></div>';
  }
```
그리고 `MG_RENDERERS` 에 `all: renderAll` 추가, `__test` 에 `rowClass: rowClass` 추가.

- [ ] **Step 4: 필터·엑셀 배선 (`_margin.html` 의 인라인 스크립트 확장)**

Task 4 에서 넣은 `MG_SHOW_TAB` 아래에 추가:
```html
<script>
  document.getElementById('mg-panes').addEventListener('change', function(e){
    var sel = e.target.closest('select[data-f]'); if(!sel) return;
    function q(f){ var s=document.querySelector('select[data-f="'+f+'"]'); return s?s.value:''; }
    var mk=q('마켓'), br=q('브랜드'), pr=q('금액대');
    document.querySelectorAll('.mg-tbl tbody tr').forEach(function(tr){
      var show=(!mk||tr.dataset.mk===mk)&&(!br||tr.dataset.br===br)&&(!pr||tr.dataset.pr===pr);
      tr.style.display = show?'':'none';
    });
  });
  document.getElementById('mg-panes').addEventListener('click', function(e){
    var b=e.target.closest('[data-export]'); if(!b||!window.__mgData) return;
    var visible=[...document.querySelectorAll('.mg-tbl tbody tr')].filter(function(tr){return tr.style.display!=='none';});
    // 화면 필터 결과를 서버로 보내 엑셀 생성 (analysis_id + tab)
    fetch('/api/margin/export',{method:'POST',headers:{'Content-Type':'application/json'},
      body:JSON.stringify({analysis_id:window.__mgData.analysis_id, tab:'all'})})
      .then(function(r){return r.blob();}).then(function(bl){
        var u=URL.createObjectURL(bl),a=document.createElement('a');a.href=u;a.download='마진분석.xlsx';a.click();URL.revokeObjectURL(u);
      });
  });
</script>
```
그리고 `MG_RENDER` 안에 `window.__mgData = d;` 한 줄 추가(export 가 analysis_id 를 알도록).

- [ ] **Step 5: 표·필터·분류색 스타일 (`margin.css` 말미)**

```css
#margin-app .mg-filter{display:flex;gap:8px;margin-bottom:12px;align-items:center}
#margin-app .mg-filter select{padding:6px 10px;border:1px solid var(--mg-line);border-radius:7px;font-size:12.5px}
#margin-app .mg-xlsx{margin-left:auto;padding:7px 14px;border:1px solid var(--mg-line);background:#fff;border-radius:7px;font-weight:700;cursor:pointer}
#margin-app .mg-tblwrap{overflow-x:auto;background:#fff;border:1px solid var(--mg-line);border-radius:12px}
#margin-app .mg-tbl{width:100%;border-collapse:collapse;font-size:12.5px}
#margin-app .mg-tbl th,.mg-tbl td{padding:9px 12px;border-bottom:1px solid var(--mg-line);text-align:left;white-space:nowrap}
#margin-app .mg-tbl th{background:#fafbfc;font-size:11.5px;color:var(--mg-sub);font-weight:700}
#margin-app .mg-tbl td.num,.mg-tbl th.num{text-align:right;font-variant-numeric:tabular-nums}
#margin-app .mg-tbl td.ctr,.mg-tbl th.ctr{text-align:center}
#margin-app .mg-tbl tr.mg-loss td{background:#FEF6F6}
#margin-app .mg-tbl tr.mg-high td{background:#F2FBF7}
#margin-app .mg-tbl tr.mg-uncomp td{background:#FAFAFA;color:#868E96}
```

- [ ] **Step 6: 통과 확인**

```bash
export PYTHONIOENCODING=utf-8
python -m pytest tests/margin/test_margin_render_js.py -v
node --check webapp/static/margin_render.js
python -m pytest tests/margin/ -q
```

- [ ] **Step 7: 커밋**

```bash
git add 프로그램/_시스템/webapp/static/margin_render.js 프로그램/_시스템/webapp/static/margin.css 프로그램/_시스템/webapp/templates/orders/_margin.html 프로그램/_시스템/tests/margin/test_margin_render_js.py
git commit -m "feat(margin-ui): 전체내역 탭 — 표+마켓/브랜드/금액대 필터+행별 분류색+엑셀"
```

---

## Task 6: 집계 6탭 (일별·월별·브랜드·금액대·상품·마켓) — 공통 표 렌더

**Files:**
- Modify: `프로그램/_시스템/webapp/static/margin_render.js`

6개 탭은 구조가 같다(그룹명 + 매출·순마진·건수 + 파이차트). 하나의 `renderGroup(rows, labelKey)` 로 처리 → DRY.

- [ ] **Step 1: 집계표 헤더 매핑 테스트 (Node)**

`test_margin_render_js.py` 에 추가:
```python
@pytest.mark.skipif(shutil.which("node") is None, reason="node 없음")
def test_group_label_keys():
    r = subprocess.run(["node", "-e",
        f"const R=require('{R.as_posix()}');console.log(R.__test.groupLabelKey('daily')+'|'+R.__test.groupLabelKey('brand'))"],
        capture_output=True, text=True)
    assert r.returncode == 0, r.stderr
    assert r.stdout.strip() == "일자|브랜드"
```

- [ ] **Step 2: 실패 확인** → FAIL (`groupLabelKey` 없음)

- [ ] **Step 3: `renderGroup` + 6탭 등록**

`MG_RENDERERS` 줄 직전에 추가:
```javascript
  var GROUP = { daily:['daily','일자'], monthly:['monthly','월'], brand:['brand','브랜드'],
                pricerange:['priceRange','금액대'], product:['product','상품명'], market:['market','마켓'] };
  function groupLabelKey(tab){ return (GROUP[tab]||[null,''])[1]; }
  function renderGroup(tab, d){
    var conf=GROUP[tab]; if(!conf) return '<div class="mg-empty">준비 중</div>';
    var rows=d[conf[0]]||[], labelKey=conf[1];
    var head='<tr><th>'+labelKey+'</th><th class="num">매출</th><th class="num">순마진</th><th class="num">건수</th></tr>';
    var body=rows.map(function(r){
      return '<tr><td>'+esc(r[labelKey])+'</td><td class="num">'+won(r.매출)+'</td>'
        +'<td class="num">'+won(r.순마진)+'</td><td class="num">'+(r.건수!=null?r.건수:(r.매입건수!=null?r.매입건수:''))+'</td></tr>';
    }).join('');
    return '<div class="mg-charts"><div class="mg-chart"><div class="mg-ct">'+labelKey+' 매출 비중</div>'
      + pieSvg(rows,'매출',labelKey)+'</div></div>'
      + '<div class="mg-tblwrap"><table class="mg-tbl"><thead>'+head+'</thead><tbody>'+body+'</tbody></table></div>';
  }
```
`MG_RENDERERS` 를 다음으로 교체:
```javascript
  root.MG_RENDERERS = {
    summary: renderSummary, all: renderAll,
    daily: function(d){return renderGroup('daily',d);},
    monthly: function(d){return renderGroup('monthly',d);},
    brand: function(d){return renderGroup('brand',d);},
    pricerange: function(d){return renderGroup('pricerange',d);},
    product: function(d){return renderGroup('product',d);},
    market: function(d){return renderGroup('market',d);},
    source: function(){ return '<div class="mg-empty">소싱처별 — 간단메모의 소싱처 URL 기준 집계(다음 태스크).</div>'; },
    __won: won, __esc: esc, __pieSvg: pieSvg
  };
```
`__test` 에 `groupLabelKey: groupLabelKey` 추가.

- [ ] **Step 4: 통과 확인**

```bash
export PYTHONIOENCODING=utf-8
python -m pytest tests/margin/test_margin_render_js.py -v
node --check webapp/static/margin_render.js
python -m pytest tests/margin/ -q
```

- [ ] **Step 5: 커밋**

```bash
git add 프로그램/_시스템/webapp/static/margin_render.js 프로그램/_시스템/tests/margin/test_margin_render_js.py
git commit -m "feat(margin-ui): 집계 6탭(일별·월별·브랜드·금액대·상품·마켓) 공통 렌더"
```

---

## Task 7: 소싱처별 탭 + 미매칭·경고 표면화

**Files:**
- Modify: `프로그램/_시스템/webapp/static/margin_render.js`

소싱처별 = `matched` 행의 `간단메모` 에서 소싱처를 뽑아 집계(서버가 별도 그룹을 안 주므로 화면에서 그룹핑 — 단순 카운트만, 재집계 아님). 미매칭 매입/매출 건수도 표면화(조용한 실패 금지).

- [ ] **Step 1: 소싱처 추출 테스트 (Node)**

`test_margin_render_js.py` 에 추가:
```python
@pytest.mark.skipif(shutil.which("node") is None, reason="node 없음")
def test_source_from_memo():
    r = subprocess.run(["node", "-e",
        f"const R=require('{R.as_posix()}');"
        "console.log(R.__test.sourceOf({간단메모:'무신사 https://musinsa.com/app/goods/123'})+'|'+R.__test.sourceOf({간단메모:'메모없음'}))"],
        capture_output=True, text=True)
    assert r.returncode == 0, r.stderr
    assert r.stdout.strip() == "무신사|미상"
```

- [ ] **Step 2: 실패 확인** → FAIL

- [ ] **Step 3: 소싱처 렌더 구현**

`renderGroup` 아래에 추가:
```javascript
  var SRC = ['무신사','SSF','SSG','롯데','ABC마트','그랜드스테이지','GS샵','폴더','나이키','올리브영','지마켓','패션플러스','르무통'];
  function sourceOf(r){
    var memo=String(r.간단메모||''); for(var i=0;i<SRC.length;i++) if(memo.indexOf(SRC[i])>=0) return SRC[i];
    return '미상';
  }
  function renderSource(d){
    var g={}; (d.matched||[]).forEach(function(r){ var s=sourceOf(r); (g[s]=g[s]||{매출:0,순마진:0,건수:0});
      g[s].매출+=Number(r.판매가)||0; g[s].순마진+=Number(r.순마진)||0; g[s].건수++; });
    var rows=Object.keys(g).map(function(k){return {소싱처:k,매출:g[k].매출,순마진:g[k].순마진,건수:g[k].건수};})
      .sort(function(a,b){return b.매출-a.매출;});
    var head='<tr><th>소싱처</th><th class="num">매출</th><th class="num">순마진</th><th class="num">건수</th></tr>';
    var body=rows.map(function(r){return '<tr><td>'+esc(r.소싱처)+'</td><td class="num">'+won(r.매출)
      +'</td><td class="num">'+won(r.순마진)+'</td><td class="num">'+r.건수+'</td></tr>';}).join('');
    var c=d.counts||{};
    var note='<div class="mg-hint" style="margin-bottom:10px">매입 미매칭 '+(c.unmatched_buy||0)+'건 · 매출 미매칭 '+(c.unmatched_sell||0)+'건</div>';
    return note+'<div class="mg-tblwrap"><table class="mg-tbl"><thead>'+head+'</thead><tbody>'+body+'</tbody></table></div>';
  }
```
`MG_RENDERERS.source` 를 `function(d){return renderSource(d);}` 로 교체. `__test` 에 `sourceOf: sourceOf` 추가.

- [ ] **Step 4: 통과 확인**

```bash
export PYTHONIOENCODING=utf-8
python -m pytest tests/margin/test_margin_render_js.py -v
node --check webapp/static/margin_render.js
python -m pytest tests/margin/ -q
```

- [ ] **Step 5: 커밋**

```bash
git add 프로그램/_시스템/webapp/static/margin_render.js 프로그램/_시스템/tests/margin/test_margin_render_js.py
git commit -m "feat(margin-ui): 소싱처별 탭(간단메모 기준) + 미매칭 건수 표면화"
```

---

## Task 8: 실브라우저 전수 검증 + 회귀

**Files:**
- Create: `프로그램/_시스템/scripts/margin_ui_smoke.py`

JS 는 단위 테스트가 한계다. 실제 화면을 띄워 눈으로 확인한다. 마켓 API 는 로컬에서 막혀 있으므로, `from_api` 를 모킹해 분석까지 돌리는 스모크 라우트/스크립트로 확인한다.

- [ ] **Step 1: 스모크 스크립트 (모킹 분석 → 렌더용 JSON 저장)**

`프로그램/_시스템/scripts/margin_ui_smoke.py`:
```python
# -*- coding: utf-8 -*-
"""실브라우저 검증용 — 260704 엑셀을 샵마인 경로로 분석해 결과 JSON 을 콘솔에 덤프.
마켓 API(서버 IP 전용) 없이 화면 렌더를 확인하기 위한 고정 데이터."""
import io, json, os, pathlib, sys
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from lemouton.margin import aggregator as A, buy_parser as B, pipeline as P, sell_source as S
from lemouton.margin.config import DEFAULT_PRICE_RANGES

d = pathlib.Path(r"C:\dev\대량등록 마진계산기\데이터\260704")
b = next(p for p in d.iterdir() if "더망고" in p.name)
s = next(p for p in d.iterdir() if "샵마인" in p.name)
out = P.run(B.parse_buy(b.read_bytes(), b.name), S.from_shopmine_excel(s.read_bytes(), s.name))
agg = A.aggregate(out["matched"], DEFAULT_PRICE_RANGES)
payload = {"analysis_id": 0, "counts": {"matched": len(out["matched"]),
           "unmatched_buy": len(out["unmatched_buy"]), "unmatched_sell": len(out["unmatched_sell"]),
           "settle_estimated": 0}, "markets_failed": [], **out, **agg}
pathlib.Path("webapp/static/_margin_sample.json").write_text(
    json.dumps(payload, ensure_ascii=False), encoding="utf-8")
print("wrote webapp/static/_margin_sample.json · matched", len(out["matched"]))
```

- [ ] **Step 2: 샘플 생성 + 앱 실행**

```bash
export PYTHONIOENCODING=utf-8
python scripts/margin_ui_smoke.py          # _margin_sample.json 생성 (matched 166)
```
`.claude/launch.json` 에 dev 서버가 있으면 그걸로, 없으면 `python app.py` 로 띄운다.
브라우저에서 `/orders/?tab=margin` 접속.

- [ ] **Step 3: 브라우저 콘솔로 샘플 렌더 (API 없이 화면 확인)**

브라우저 콘솔에서:
```javascript
fetch('/static/_margin_sample.json').then(r=>r.json()).then(d=>window.MG_RENDER(d));
```
그리고 왼쪽 세로 탭 9개를 하나씩 눌러 **육안 확인**:

- [ ] 요약: KPI 5개(총매출 10,715,000 / 총순마진 1,325,722) + 미니 4개 + 파이 2개
- [ ] 전체내역: 표 뜨고, 마켓·브랜드·금액대 드롭다운으로 필터되고, 의심손실 행이 붉게
- [ ] 일별·월별·브랜드·금액대·상품·마켓: 각 표 + 파이차트
- [ ] 소싱처별: 소싱처 그룹 + 미매칭 건수 문구
- [ ] 숫자가 오른쪽으로 자릿수 맞게 정렬됐나 (판매가·정산예정·순마진)
- [ ] 위 5개 탭(주문내역…마진계산기)과 왼쪽 9개 탭이 다른 층으로 보이나
- [ ] 블랙스팟·설정이 흐리게 '준비중'으로 있나

어긋난 항목은 해당 Task 로 돌아가 고치고 재확인.

- [ ] **Step 4: 임시 샘플 파일 정리 + 전체 회귀**

```bash
rm -f 프로그램/_시스템/webapp/static/_margin_sample.json   # 커밋하지 않는다
export PYTHONIOENCODING=utf-8
python -m pytest tests/ -q     # 베이스라인 4건 외 회귀 없음
python -c "import app; print('import OK')"
```
`.gitignore` 에 `webapp/static/_margin_sample.json` 추가.

- [ ] **Step 5: 커밋**

```bash
git add 프로그램/_시스템/scripts/margin_ui_smoke.py 프로그램/_시스템/.gitignore
git commit -m "test(margin-ui): 실브라우저 스모크 스크립트 + 9탭 육안 검증 완료"
```

---

## Task 9: PR 갱신 + 마무리

- [ ] **Step 1: 전체 테스트 + import 확인**

```bash
export PYTHONIOENCODING=utf-8
python -m pytest tests/ -q     # 4 failed(베이스라인), 나머지 통과
python -c "import app; print('OK')"
```

- [ ] **Step 2: 푸시 (PR #228 에 이어붙음)**

```bash
git push
```

- [ ] **Step 3: PR 본문에 UI 추가 반영**

`gh pr edit 228 --body` 로 "화면(B 레이아웃) 추가 — 왼쪽 세로 9탭 + 단계 카드 + 파이차트, 블랙스팟·설정 자리만" 을 본문에 덧붙인다. **머지는 사용자 확인 후.**

- [ ] **Step 4: 스펙에 UI 완료 반영**

`docs/superpowers/specs/2026-07-10-대량등록-마진계산기-이식-design.md` §8(UI) 에 "B 레이아웃으로 구현 완료 (2026-07-11), margin_app.js/margin_render.js/margin.css/_margin.html" 한 줄 추가하고 커밋.

---

## 후속 (이 플랜 밖)

1. **서버에서 골든테스트 2단계 실행** (`scripts/margin_api_parity.py`) — API 정산값이 샵마인과 같은지, 실계정으로.
2. **② 블랙스팟 탭** (49개 코드) — 선행: 주문상태 통일 매핑표.
3. **④ 설정 탭** (키워드·소싱처 계정).
4. **③ 소싱처 주문상태 자동확인** — 서버 Playwright 금지(3대 원칙 3), 모음전 크롬확장 방식으로 재구현.
5. 필터 결과 엑셀(`detail_filtered`)을 화면에 보이는 정확한 행·컬럼순서로 전송(지금은 tab='all' 전량).
