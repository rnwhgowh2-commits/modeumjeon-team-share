# 판매처 지도 단일원천화 + 전수정독 게이트 — 구현 플랜

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 판매처 데이터 코드 지도의 진실원천을 `marketplace_api_map.json` 하나로 통합(1부)하고, 마켓 API 개발 전 지도를 전수정독하는 게이트(스킬 + 마켓 1장 브리핑 라우트)를 만든다(2부).

**Architecture:** 팝업(map.html)은 렌더러일 뿐 — 죽은 인라인 카탈로그(446개)를 삭제하고, 살아있는 인라인 2곳(주문상태 전환·정산 계산)을 JSON 신규 키(`autoConfirm`·`settleCalc`)로 이관. 그 위에 `GET /marketplace-guide/map-brief?market=<id>`(마켓 1개=마크다운 1장) + 프로젝트 스킬 `consult-market-map`(6단계 게이트)을 얹는다. **실제 마켓 호출 코드(shared/platforms 등)는 불변** — 표시·문서 레이어만.

**Tech Stack:** Flask(blueprint marketplace_guide) · 순수 Python(무의존 검증기) · Jinja 템플릿 내 vanilla JS · pytest.

**스펙:** `docs/superpowers/specs/2026-07-21-판매처지도-단일원천화-전수정독-design.md`

---

## ⚠️ 절대 규칙 (실행자 필독)

1. **회귀 금지가 최우선.** 탭 하나 바꿀 때마다 전/후 브라우저 대조. 화면이 달라지면 **중단하고 정직 보고**(가짜 완료 금지).
2. **`pytest` 인자 없이 실행 금지** — tests/scripts 일부가 라이브 접속함. 항상 `python -m pytest tests/marketplace_guide -v` 처럼 경로 지정.
3. **본 플랜의 라인 번호는 `_wt_reg`(feat/4market-register) 기준 참고값.** 새 워크트리(origin/main)에서는 다를 수 있음 → **반드시 문자열 앵커로 위치를 찾을 것.**
4. 로컬 서버 검증 시 `DISABLE_AUTH=1` 환경변수로 기동(로그인 우회). `.env` 없으면 SQLite 폴백 — 지도 화면은 파일 기반이라 무관.
5. 커밋은 원자 단위(태스크당 1커밋). `git checkout --` 로 미커밋 날리기 금지.

## 사전 조사 결과 (2026-07-21 실측 — 재조사 불필요)

| map.html 인라인 | 라인(참고) | 소비자 | 판정 |
|---|---|---|---|
| `ESM_CATS`(81)·`MKT_CATS`(365) | 303~876 | `dmFind`(933)만 — **`dmFind` 호출처 0** | ☠️ 죽은 코드 → 삭제 |
| `dmFind`·`dmRow`·`dmSec` | 933~935 | 호출처 0 | ☠️ 삭제 |
| `API_STEPS`(879)·`API_HOW`(886) | 879~892 | `dmApiSteps`(1037)만 — **호출처 0** | ☠️ 삭제 |
| `dmApiSteps` | 1037~1046 | 호출처 0 | ☠️ 삭제 |
| `DM_MARKETS` | 295~302 | `dmTransRail`(955)·`dmTransMarket`(1059) — `.nm` 라벨만 | 🔄 MK_META로 대체 후 삭제 |
| `TRANS`·`TRANS_MK`·`API_CALLS`·`dmMkb` | 893~929 | 주문상태 탭(`dmTransRail/Summary/Market`, dmRender 1308) | 🔄 JSON `autoConfirm` 이관 |
| `dmSettle` 내부 `MK/ROWS/TOT/FML`+노트 | 1070~1104 | 정산 탭 | 🔄 JSON `settleCalc` 이관 |
| `INC_MKMETA`(1195)·색상값 | — | 뱃지 색(표현) | ✅ 인라인 유지(데이터 아님) |
| 속도탭 `dmSpeed`·문서수집법 `dmHowto`·과거이력 `dmIncidents` | — | **이미 JSON**(markets.upload_limit / ingest-paths.json / DATA.incidents) | ✅ 손대지 않음 |

JSON(SOT) 최상위 키: `schema_version, markets, unifiedStatuses, transitions, codes, apis(610), incidents`. markets[i] 키: `id,label,base_url,auth,env_prefixes,concurrency,rate,upload_limit`.

## 파일 구조

- Modify: `프로그램/_시스템/webapp/templates/marketplace_guide/map.html` — 인라인 삭제·JSON 렌더 전환
- Modify: `프로그램/_시스템/webapp/data/marketplace_api_map.json` — `autoConfirm`·`settleCalc` 키 추가
- Modify: `프로그램/_시스템/webapp/marketplace_api_map.py` — 검증기 확장
- Create: `프로그램/_시스템/webapp/market_brief.py` — 브리핑 조립(순수 함수)
- Modify: `프로그램/_시스템/webapp/routes/marketplace_guide.py` — `/map-brief` 라우트
- Modify: `프로그램/_시스템/tests/marketplace_guide/test_map_template.py` — 인라인→SOT 검증 전환
- Modify: `프로그램/_시스템/tests/marketplace_guide/test_api_map_schema.py` — 신규 키 스키마 테스트
- Create: `프로그램/_시스템/tests/marketplace_guide/test_map_brief.py` — 브리핑 테스트
- Create: `.claude/skills/consult-market-map/SKILL.md` — 전수정독 게이트 스킬
- Modify: `CLAUDE.md`(프로젝트) — 게이트 필독 섹션 1개 추가

---

### Task 0: 워크트리 준비 + 기준선 기록

**Files:** 없음(환경 준비)

- [ ] **Step 1: origin/main 워크트리 생성** (⚠️ `_wt_map` 이미 존재 — 이름 겹치지 않게 `_wt_map_sot`)

```bash
cd "C:/dev/모음전 프로젝트"
git fetch origin
git worktree add _wt_map_sot -b feat/marketmap-sot-brief origin/main
```

- [ ] **Step 2: 스펙·플랜을 워크트리에 복사 + 문서 커밋**

```bash
cp "docs/superpowers/specs/2026-07-21-판매처지도-단일원천화-전수정독-design.md" "_wt_map_sot/docs/superpowers/specs/"
cp "docs/superpowers/plans/2026-07-21-판매처지도-단일원천화-전수정독.md" "_wt_map_sot/docs/superpowers/plans/"
cd _wt_map_sot
git add docs/superpowers && git commit -m "docs(spec+plan): 판매처지도 단일원천화+전수정독 게이트"
```

- [ ] **Step 3: 기준선 — 기존 테스트 전부 통과 확인**

Run: `cd "_wt_map_sot/프로그램/_시스템" && python -m pytest tests/marketplace_guide -v`
Expected: 전부 PASS. (실패가 있으면 **중단·보고** — 우리가 만든 게 아니어도 기준선 오염이므로 먼저 확인.)

- [ ] **Step 4: 기준선 — 검증기 오류 0 확인 + API 개수 기록**

Run: `python -c "from webapp.marketplace_api_map import load_map, validate_map; d=load_map(); print('errors:', validate_map(d)); print('apis:', len(d['apis']))"`
Expected: `errors: []` · `apis: 610` 근처(±: main이 더 최신일 수 있음 — 개수를 기록해 이후 태스크에서 불변 확인).

- [ ] **Step 5: 기준선 — 팝업 화면 스냅샷**

로컬 서버 기동(`DISABLE_AUTH=1` 로 `python app.py` 또는 preview_start) → `/marketplace-guide/map` 접속 → **9개 상위탭 전부 클릭**(마켓별 전체 API(6마켓 각각)·문서수집법·상품조회·상품관리·정산(매트릭스+계산식)·배송송장·클레임CS·주문상태(요약+4마켓)·과거이력·업로드속도) → 각 화면 스크린샷 저장 + **콘솔 에러 0** 확인. 이것이 전/후 대조의 "전".

---

### Task 1: 죽은 인라인 카탈로그 삭제 + 템플릿 테스트를 SOT 기준으로 전환

**Files:**
- Modify: `프로그램/_시스템/tests/marketplace_guide/test_map_template.py`
- Modify: `프로그램/_시스템/webapp/templates/marketplace_guide/map.html`

- [ ] **Step 1: 인라인 문자열에 의존하는 기존 테스트 3개를 SOT(JSON) 검증으로 교체 + 죽은코드 부재 테스트 추가(현재는 FAIL이 정상)**

`test_map_template.py`에서 `test_official_category_names_present` · `test_settlement_official_apis_catalogued` · `test_official_categories_per_market` 3개 함수를 삭제하고, `test_status_four_states`를 아래 `test_status_labels`로 교체하고, 파일 끝에 아래를 추가:

```python
# ── 단일 진실 원천(JSON) 검증 — 인라인 카탈로그 삭제 후에도 카탈로그가 SOT에 온전함을 보장 ──

def _sot():
    from webapp.marketplace_api_map import load_map
    return load_map()


def test_status_labels():
    html = _map()
    for s in ["씀", "검증대기", "안 씀"]:   # DM_LBL — 렌더 상태 라벨
        assert s in html


def test_no_dead_inline_catalog():
    html = _map()
    for token in ["const MKT_CATS", "const ESM_CATS", "const API_STEPS", "const API_HOW",
                  "function dmApiSteps", "function dmFind", "function dmRow(", "function dmSec"]:
        assert token not in html, f"죽은 인라인 재등장 금지: {token}"


def test_sot_catalog_covers_all_markets():
    data = _sot()
    by = {}
    for a in data["apis"]:
        by.setdefault(a["market"], []).append(a)
    for mk in ["coupang", "smartstore", "lotteon", "eleven11", "auction", "gmarket"]:
        assert len(by.get(mk, [])) >= 20, f"{mk} 카탈로그 빈약"
        assert {a["category"] for a in by[mk]}, f"{mk} 카테고리 없음"


def test_sot_settlement_apis_per_market():
    data = _sot()
    for mk in ["coupang", "smartstore", "lotteon", "eleven11"]:
        assert any(a["market"] == mk and ("정산" in (a.get("category") or "")
                   or any("정산" in t for t in (a.get("tabs") or [])))
                   for a in data["apis"]), f"{mk} 정산 API 없음"
```

- [ ] **Step 2: 실패 확인**

Run: `python -m pytest tests/marketplace_guide/test_map_template.py -v`
Expected: `test_no_dead_inline_catalog` FAIL(인라인 아직 존재) · 나머지 PASS.

- [ ] **Step 3: map.html에서 죽은 블록 4곳 삭제** (문자열 앵커로 찾기)

1. **카탈로그 블록**: 주석 `// 마켓별 = 그 마켓 공식 문서의 카테고리 그대로.` 줄부터 ~ `const DM_LBL=` **바로 앞의** `};` 줄까지 통째 삭제(= 주석 5줄 + `const ESM_CATS=[...]` + `const MKT_CATS={...};`). ⚠️ 바로 위 `const DM_MARKETS=[...]`(8줄)은 **남긴다**(Task 2에서 처리).
2. **확보절차 상수**: `const API_STEPS=[` 줄부터 `const API_CALLS={` **바로 앞** 줄까지 삭제(= API_STEPS + API_HOW). ⚠️ `API_CALLS`는 남긴다.
3. **죽은 헬퍼 3줄**: `function dmFind(`, `function dmRow(`, `function dmSec(` 각 한 줄씩 삭제.
4. **죽은 렌더 함수**: `function dmApiSteps(){` 부터 다음 `function ` 선언 직전 닫는 `}` 까지 삭제. 위의 주석 `// API 정보 확보 절차 — 「마켓별 전체 API」 탭으로 이동`도 함께 삭제.

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest tests/marketplace_guide -v`
Expected: 전부 PASS.

- [ ] **Step 5: 브라우저 전/후 대조** — 서버 리로드 → 9개 탭 전수 클릭 → Task 0 Step 5 스냅샷과 **완전 동일**(죽은 코드라 화면 불변이 정상) + 콘솔 에러 0.

- [ ] **Step 6: Commit**

```bash
git add -A && git commit -m "refactor(판매처지도): 죽은 인라인 카탈로그 삭제(MKT_CATS·ESM_CATS 등 호출처0) — 렌더 불변, 카탈로그 SOT=JSON 단일화"
```

---

### Task 2: 주문상태 전환 탭 → JSON `autoConfirm` 이관 (DM_MARKETS·TRANS·API_CALLS 제거)

**Files:**
- Modify: `프로그램/_시스템/webapp/data/marketplace_api_map.json`
- Modify: `프로그램/_시스템/webapp/marketplace_api_map.py`
- Modify: `프로그램/_시스템/tests/marketplace_guide/test_api_map_schema.py`
- Modify: `프로그램/_시스템/webapp/templates/marketplace_guide/map.html`

- [ ] **Step 1: 스키마 테스트 추가(FAIL 확인용)** — `test_api_map_schema.py` 끝에:

```python
def test_autoconfirm_sot():
    from webapp.marketplace_api_map import load_map, validate_map
    d = load_map()
    ac = d.get("autoConfirm")
    assert isinstance(ac, dict), "autoConfirm 누락"
    assert len(ac.get("markets", [])) == 4
    assert set(ac.get("calls", {})) == {"coupang", "smartstore", "lotteon", "eleven11"}
    assert validate_map(d) == []
```

Run: `python -m pytest tests/marketplace_guide/test_api_map_schema.py -v` → 신규 테스트 FAIL 확인.

- [ ] **Step 2: JSON에 `autoConfirm` 추가** — `marketplace_api_map.json` 최상위(예: `"incidents"` 앞)에 아래를 그대로(인라인 TRANS/API_CALLS/경고문 전사 — 값 변경 금지):

```json
"autoConfirm": {
  "markets": [
    {"id": "coupang", "api": "PUT …/ordersheets/acknowledgement", "ids": "shipmentBoxId", "v": "done"},
    {"id": "smartstore", "api": "POST …/product-orders/confirm", "ids": "productOrderId (=오픈마켓주문번호)", "v": "done"},
    {"id": "lotteon", "api": "POST …/SellerDeliveryProgressStateInform · odPrgsStepCd=12", "ids": "odNo·odSeq·procSeq·spdNo·sitmNo·slQty", "v": "wait"},
    {"id": "eleven11", "api": "GET …/reqpackaging/{ordNo}/{ordPrdSeq}/N/null/{dlvNo}", "ids": "ordNo·ordPrdSeq·dlvNo", "v": "done"}
  ],
  "calls": {
    "coupang": {"auth": "HMAC-SHA256", "rows": [
      ["주문조회", "GET /v2/providers/openapi/apis/api/v5/vendors/{vendorId}/ordersheets?status=ACCEPT..", "발주서 목록"],
      ["★발주확인(상품준비중)", "PUT /v2/providers/openapi/apis/api/v4/vendors/{vendorId}/ordersheets/acknowledgement · body {vendorId, shipmentBoxIds:[정수…]}", "ACCEPT→INSTRUCT"],
      ["송장전송", "POST /…/orders/{shipmentBoxId}/ordersheets/invoices", "code 200"]
    ]},
    "smartstore": {"auth": "OAuth2 Bearer", "rows": [
      ["주문조회", "GET /external/v1/pay-order/seller/product-orders/last-changed-statuses", "productOrderId[]"],
      ["주문상세", "POST /external/v1/pay-order/seller/product-orders/query · {productOrderIds}", "placeOrderStatus 등"],
      ["★발주확인", "POST /external/v1/pay-order/seller/product-orders/confirm · {productOrderIds}", "successProductOrderInfos · 이미확인=104443(성공취급)"],
      ["송장전송", "POST /…/product-orders/dispatch", "—"]
    ], "note": "발주확인해도 주문상태 「결제완료(PAYED)」 그대로 → placeOrderStatus=OK 로 배송준비 판정(우리 앱이 이 필드 읽음)"},
    "lotteon": {"auth": "Bearer + IP등록", "rows": [
      ["★상품준비(발주확인)", "POST /v1/openapi/delivery/v1/SellerDeliveryProgressStateInform · body {…, odPrgsStepCd:\"12\", spdNo, sitmNo, slQty, dvRtrvDvsCd:\"DV\"}", "returnCode \"0000\" (apiNo 137)"],
      ["송장전송(발송완료)", "같은 URL · odPrgsStepCd:\"13\" + 송장필드", "returnCode \"0000\""]
    ]},
    "eleven11": {"auth": "openapikey 헤더 + IP등록 · XML", "rows": [
      ["결제완료(발송대기) 조회", "GET /rest/ordservices/complete/{시작}/{종료}", "발송대기"],
      ["★발주처리(→배송준비중)", "GET /rest/ordservices/reqpackaging/{ordNo}/{ordPrdSeq}/{addPrdYn}/{addPrdNo}/{dlvNo}", "result_code \"0\" · 추가구성 없으면 N/null"],
      ["배송준비중 조회", "GET /rest/ordservices/packaging/{시작}/{종료}", "발주확인 완료"],
      ["송장전송(발송처리)", "GET /rest/ordservices/reqdelivery/{발송일}/{배송방식}/{택배사}/{송장}/{dlvNo}", "result_code \"0\""]
    ]}
  },
  "cautions": [
    {"id": "smartstore", "text": "발주확인해도 주문상태 그대로(<b>placeOrderStatus=OK</b> 로 배송준비 판정)"},
    {"id": "lotteon", "text": "결제완료 주문 생기면 1건만 확인하면 종료"}
  ]
}
```

- [ ] **Step 3: 검증기 확장** — `marketplace_api_map.py`의 `validate_map`에서:
  ① `market_ids = {m.get("id") for m in data.get("markets", [])}` 계산을 함수 상단(apis 루프 앞)으로 이동(과거이력 검증부의 중복 계산 줄은 삭제).
  ② `return errors` 직전에 추가:

```python
    # autoConfirm — 주문상태 전환(V1) SOT (구 인라인 TRANS/API_CALLS 이관분)
    ac = data.get("autoConfirm")
    if not isinstance(ac, dict):
        errors.append("autoConfirm 누락(주문상태 전환 SOT)")
    else:
        for m in ac.get("markets", []):
            mid = m.get("id", "?")
            if market_ids and mid not in market_ids:
                errors.append(f"autoConfirm.markets 참조 없음: {mid}")
            for k in ("id", "api", "ids", "v"):
                if not m.get(k):
                    errors.append(f"autoConfirm.markets[{mid}] {k} 비어있음")
        if not ac.get("markets"):
            errors.append("autoConfirm.markets 비어있음")
        for k, call in (ac.get("calls") or {}).items():
            if market_ids and k not in market_ids:
                errors.append(f"autoConfirm.calls 참조 없음: {k}")
            if not call.get("auth") or not call.get("rows"):
                errors.append(f"autoConfirm.calls[{k}] auth/rows 비어있음")
        for c in ac.get("cautions", []):
            if market_ids and c.get("id") not in market_ids:
                errors.append(f"autoConfirm.cautions 참조 없음: {c.get('id')}")
```

Run: `python -m pytest tests/marketplace_guide/test_api_map_schema.py -v` → 전부 PASS.

- [ ] **Step 4: map.html 주문상태 렌더를 JSON으로 전환**

① `const DM_MARKETS=[...]` 블록(8줄), `const TRANS=[...]`+`const TRANS_MK=[...]`(주석 포함), `const API_CALLS={...}` 블록, `const dmMkb=...` 한 줄 — 모두 삭제.
② `dmMkb` 자리에 id 기반 뱃지 추가(표현색은 기존 `INC_MKMETA` 재사용):

```js
const dmMkbId=id=>{const lb=(MK_META[id]||{}).label||id;const cls=(INC_MKMETA[id]||{}).cls||'';return `<span class="mkbadge ${cls}">${lb}</span>`;};
```

③ `dmTransRail`·`dmTransSummary`·`dmTransMarket` 세 함수를 아래로 교체:

```js
function dmTransRail(){
  const AC=DATA.autoConfirm||{markets:[]};
  let h=`<div class="rt">주문상태 전환</div>`;
  h+=`<button class="railbtn${DM_TSUB==='summary'?' on':''}" data-ts="summary">📋 실배선 현황</button>`;
  AC.markets.forEach(m=>{const lb=(MK_META[m.id]||{}).label||m.id;const d=m.v==='done'?'done':'wait';
    h+=`<button class="railbtn${DM_TSUB===m.id?' on':''}" data-ts="${m.id}"><span>${lb}</span><span class="rdot ${d}"></span></button>`;});
  return h;
}
function dmTransSummary(){
  const AC=DATA.autoConfirm||{markets:[],cautions:[]};
  let h=`<div class="favhead"><span class="ft">⚡ 주문상태 전환</span><span class="fs">결제완료 → 배송준비중 · 마켓별 실배선/검증 현황</span></div>`;
  h+=`<div class="apih3" style="margin-top:0">📋 실배선 현황</div>`;
  h+=`<div class="wrapbox"><table class="cmp"><tr><th style="width:100px">마켓</th><th>전환 API</th><th style="width:172px">필요 식별자</th><th class="c" style="width:44px">배선</th><th class="c" style="width:86px">검증</th></tr>`;
  AC.markets.forEach(m=>{h+=`<tr><td class="el">${dmMkbId(m.id)}</td><td class="url">${esc(m.api)}</td><td class="idcell">${dmIdChips(m.ids)}</td><td class="c">✅</td><td class="c">${dmVb(m.v)}</td></tr>`;});
  h+=`</table></div>`;
  if((AC.cautions||[]).length){
    h+=`<div class="warnbox"><div class="wt">⚠️ 마켓별 주의</div>`;
    AC.cautions.forEach(c=>{h+=`<div class="wr">${dmMkbId(c.id)}<span>${c.text}</span></div>`;});
    h+=`</div>`;
  }
  return h;
}
function dmTransMarket(id){
  const AC=DATA.autoConfirm||{markets:[],calls:{}};
  const lb=(MK_META[id]||{}).label||id, c=(AC.calls||{})[id], t=(AC.markets||[]).filter(x=>x.id===id)[0];
  if(!c)return `<div class="empty"><div class="big">📄</div>${lb} 전환 API 정보가 아직 없어요.</div>`;
  let h=`<div class="favhead"><span class="ft">${lb} · 주문상태 전환</span><span class="fs">${esc(c.auth)}</span></div>`;
  if(t)h+=`<div style="font-size:11.5px;margin-bottom:10px">배선 <b style="color:var(--ok)">✅</b> · 검증 ${dmVb(t.v)} · 식별자 <span class="ids2">${esc(t.ids)}</span></div>`;
  h+=`<table class="cmp"><tr><th style="width:120px">용도</th><th>메서드 · URL</th><th style="width:120px">성공·비고</th></tr>`;
  c.rows.forEach(r=>{const st=r[0][0]==='★';h+=`<tr><td class="el${st?' star':''}">${esc(r[0])}</td><td class="url">${esc(r[1])}</td><td class="g">${esc(r[2])}</td></tr>`;});
  h+=`</table>`;
  if(c.note)h+=`<div class="apinote">⚠️ ${esc(c.note)}</div>`;
  return h;
}
```

④ `dmRender` 안의 `TRANS_MK.indexOf(DM_TSUB)<0` 를 다음으로 교체:

```js
if(DM_TOP==='trans'&&DM_TSUB!=='summary'&&!(DATA.autoConfirm&&DATA.autoConfirm.markets.some(m=>m.id===DM_TSUB)))DM_TSUB='summary';
```

⚠️ `esc()`는 map.html에 이미 있음(`function esc(v)`). ⚠️ `dmMkb(` 잔여 호출이 없는지 `grep dmMkb` 로 확인(전부 dmMkbId여야 함).

- [ ] **Step 5: 죽은코드 부재 테스트 갱신** — `test_map_template.py`의 `test_no_dead_inline_catalog` 토큰 목록에 `"const DM_MARKETS"`, `"const TRANS="`, `"const TRANS_MK"`, `"const API_CALLS"` 4개 추가.

- [ ] **Step 6: 테스트 + 브라우저 대조**

Run: `python -m pytest tests/marketplace_guide -v` → 전부 PASS.
브라우저: 주문상태 탭 → 요약 + 쿠팡·스스·롯데온·11번가 4개 화면이 Task 0 스냅샷과 **동일**(행 수·문구·검증 뱃지) + 콘솔 에러 0.

- [ ] **Step 7: Commit**

```bash
git add -A && git commit -m "refactor(판매처지도): 주문상태 전환 탭 SOT 이관 — 인라인 TRANS·API_CALLS·DM_MARKETS → autoConfirm(JSON)+검증기"
```

---

### Task 3: 정산 계산 탭 → JSON `settleCalc` 이관

**Files:**
- Modify: `프로그램/_시스템/webapp/data/marketplace_api_map.json`
- Modify: `프로그램/_시스템/webapp/marketplace_api_map.py`
- Modify: `프로그램/_시스템/tests/marketplace_guide/test_api_map_schema.py`
- Modify: `프로그램/_시스템/webapp/templates/marketplace_guide/map.html`

- [ ] **Step 1: 스키마 테스트 추가(FAIL 확인)** — `test_api_map_schema.py` 끝에:

```python
def test_settlecalc_sot():
    from webapp.marketplace_api_map import load_map, validate_map
    d = load_map()
    sc = d.get("settleCalc")
    assert isinstance(sc, dict), "settleCalc 누락"
    n = len(sc["markets"])
    assert n == 4
    for r in sc["rows"]:
        assert len(r["cells"]) == n, r["item"]
    assert len(sc["total"]) == n
    assert len(sc["formulas"]) == n
    assert validate_map(d) == []
```

Run → FAIL 확인.

- [ ] **Step 2: JSON에 `settleCalc` 추가** (인라인 dmSettle의 MK/ROWS/TOT/FML/노트 전사 — 값 변경 금지):

```json
"settleCalc": {
  "markets": [
    {"id": "coupang", "g": "쿠", "api": "revenue-history"},
    {"id": "smartstore", "g": "N", "api": "settle/case"},
    {"id": "lotteon", "g": "롯", "api": "soapi"},
    {"id": "eleven11", "g": "11", "api": "settlementList"}
  ],
  "rows": [
    {"item": "판매가", "op": "기준", "cells": [{"c": "saleAmount"}, {"c": "paySettleAmount"}, {"c": "slAmt"}, {"c": "selPrcAmt"}]},
    {"item": "수수료", "op": "(−)", "cells": [{"c": "serviceFee"}, {"c": "totalPayCommissionAmount"}, {"c": "cmsnSumAmt"}, {"c": "selFee"}]},
    {"item": "판매자할인", "op": "(−)", "cells": [{"inc": "settlementAmount"}, {"inc": "paySettleAmount"}, {"c": "pdDcSlrBdnAmt"}, {"inc": "stlAmt"}]},
    {"item": "결제(페이)비", "op": "(−)", "cells": [{"inc": "serviceFee"}, {"c": "totalPayCommissionAmount"}, {"inc": "cmsnSumAmt"}, {"inc": "stlAmt"}]},
    {"item": "배송비", "op": "(+)", "cells": [{"c": "deliveryFee.settlementAmount"}, {"c": "productOrderType=DELIVERY"}, {"c": "dvCst", "n": "수수료적용"}, {"c": "선불배송비 별도라인"}]},
    {"item": "수량", "op": "참고", "cells": [{"c": "quantity"}, {"c": "quantity"}, {"c": "slQty"}, {"c": "ordQty"}]},
    {"item": "매칭 키", "op": "라인", "cells": [{"c": "orderId+vendorItemId"}, {"c": "productOrderId"}, {"c": "odNo+odseq"}, {"c": "ordNo+ordPrdSeq"}]}
  ],
  "total": ["settlementAmount + deliveryFee", "settleExpectAmount", "pymtTgtAmt", "selPrcAmt − deductAmt"],
  "noteMtx": "<b>쿠팡·스스</b> = 상품·배송비를 별도 필드로 수집(각각 정확). <b>롯데온</b> = <span class=\"cc\">pymtTgtAmt</span> 지급총액(할인·결제비 포함). <b>11번가</b> = 라인별 <span class=\"cc\">selPrcAmt − deductAmt</span>(상품·선불배송비 각각 별도 라인, deductAmt=수수료·할인·서비스이용료 공제). ★11번가 <span class=\"cc\">stlAmt</span> 는 배송비 라인서 서비스이용료 미반영 총액이라 <span class=\"cc\">selPrcAmt − deductAmt</span> 로 계산해야 실정산과 오차0(라이브 검증).",
  "formulas": [
    {"id": "coupang", "tm": [{"t": "판매가", "c": "saleAmount"}, {"o": "−", "t": "수수료", "c": "serviceFee"}, {"o": "+", "t": "배송비", "c": "deliveryFee"}, {"o": "=", "t": "정산액", "c": "settlementAmount", "res": 1}, {"o": "·", "t": "할인·결제비", "mi": "serviceFee 흡수"}]},
    {"id": "smartstore", "tm": [{"t": "기준가", "c": "paySettleAmount"}, {"o": "−", "t": "수수료", "c": "totalPayCommissionAmount"}, {"o": "+", "t": "배송비", "c": "DELIVERY"}, {"o": "=", "t": "정산액", "c": "settleExpectAmount", "res": 1}, {"o": "·", "t": "판매자할인", "mi": "기준가에 반영"}]},
    {"id": "lotteon", "tm": [{"t": "상품가", "c": "slAmt"}, {"o": "−", "t": "수수료", "c": "cmsnSumAmt"}, {"o": "−", "t": "셀러할인", "c": "pdDcSlrBdnAmt"}, {"o": "+", "t": "롯데부담", "c": "pdDcOcoBdnAmt"}, {"o": "=", "t": "정산액", "c": "pymtTgtAmt", "res": 1}]},
    {"id": "eleven11", "tm": [{"t": "판매/배송", "c": "selPrcAmt"}, {"o": "−", "t": "공제", "c": "deductAmt"}, {"o": "=", "t": "정산액", "c": "selPrcAmt−deductAmt", "res": 1}, {"o": "·", "t": "배송비", "mi": "선불배송비 별도 라인"}]}
  ],
  "noteFml": "라인 매칭 키 = 마켓별 주문번호+순번: 쿠팡 <span class=\"cc\">orderId+vendorItemId</span> · 스스 <span class=\"cc\">productOrderId</span> · 롯데온 <span class=\"cc\">odNo+odseq</span> · 11번가 <span class=\"cc\">ordNo+ordPrdSeq</span>. 점선 항 = 별도 필드 없음(총액 흡수)."
}
```

- [ ] **Step 3: 검증기 확장** — `validate_map`의 `return errors` 직전(autoConfirm 검증 뒤)에:

```python
    # settleCalc — 정산 계산 매트릭스 SOT (구 인라인 dmSettle 데이터 이관분)
    sc = data.get("settleCalc")
    if not isinstance(sc, dict):
        errors.append("settleCalc 누락(정산 계산 SOT)")
    else:
        n = len(sc.get("markets", []))
        if n == 0:
            errors.append("settleCalc.markets 비어있음")
        for m in sc.get("markets", []):
            if market_ids and m.get("id") not in market_ids:
                errors.append(f"settleCalc.markets 참조 없음: {m.get('id')}")
        for r in sc.get("rows", []):
            if len(r.get("cells", [])) != n:
                errors.append(f"settleCalc.rows[{r.get('item','?')}] cells {len(r.get('cells', []))}개 ≠ markets {n}개")
        if len(sc.get("total", [])) != n:
            errors.append("settleCalc.total 수 ≠ markets 수")
        if len(sc.get("formulas", [])) != n:
            errors.append("settleCalc.formulas 수 ≠ markets 수")
```

Run: `python -m pytest tests/marketplace_guide/test_api_map_schema.py -v` → 전부 PASS.

- [ ] **Step 4: `dmSettle` 을 JSON 렌더로 교체** (색상만 인라인 유지 — 표현):

```js
function dmSettle(){
  const SC=DATA.settleCalc;
  if(!SC)return `<div class="empty"><div class="big">⚠️</div>정산 계산 데이터를 불러오지 못했어요.</div>`;
  const STL_C={coupang:'#AE3FE0',smartstore:'#03C75A',lotteon:'#DA2128',eleven11:'#FF4800'};
  const mks=SC.markets.map(m=>({id:m.id,g:m.g,api:m.api,nm:(MK_META[m.id]||{}).label||m.id,c:STL_C[m.id]||'#888'}));
  const cell=(v)=>v.inc?`<span class="inc"><span class="cc">${v.inc}</span><span class="t">에 포함${v.n?' ('+v.n+')':''}</span></span>`:`<span class="cc">${v.c}</span>`;
  const sub=`<div class="stlsub"><button class="${DM_STL==='mtx'?'on':''}" data-stl="mtx">항목×마켓 매트릭스</button>`
           +`<button class="${DM_STL==='fml'?'on':''}" data-stl="fml">계산식 대입</button></div>`;
  let bd;
  if(DM_STL==='mtx'){
    const head=`<tr><th style="text-align:left">항목</th>`+mks.map(m=>`<th><span class="mkc"><span class="lg" style="background:${m.c}">${m.g}</span>${m.nm}</span><br><span style="font-size:9px;color:var(--faint);font-weight:600;font-family:var(--mono)">${m.api}</span></th>`).join('')+`</tr>`;
    const rows=SC.rows.map(r=>`<tr><th>${r.item}<span class="op">${r.op}</span></th>`+r.cells.map(v=>`<td>${cell(v)}</td>`).join('')+`</tr>`).join('');
    const tot=`<tr class="tot"><th>= 최종 정산액</th>`+SC.total.map(c=>`<td><span class="cc res">${c}</span></td>`).join('')+`</tr>`;
    bd=`<table class="stlmtx"><thead>${head}</thead><tbody>${rows}${tot}</tbody></table>`
      +`<div class="stlnote">${SC.noteMtx}</div>`;
  }else{
    bd=`<div class="fml">`+SC.formulas.map(f=>{const mk=mks.filter(m=>m.id===f.id)[0]||{};
      return `<div class="fr"><div class="frh"><span class="lg" style="background:${mk.c}">${mk.g}</span> ${mk.nm}</div><div class="fx">`
      +f.tm.map(t=>(t.o?`<span class="fo"${t.o==='·'?' style="padding-top:16px"':''}>${t.o}</span>`:'')
        +`<div class="tm${t.res?' res':''}${t.mi?' mi':''}"><span class="t">${t.t}</span>`
        +(t.mi?`<span class="inc"><span class="t">→ ${t.mi}</span></span>`:`<span class="cc${t.res?' res':''}">${t.c}</span>`)+`</div>`).join('')
      +`</div></div>`;}).join('')+`</div>`
      +`<div class="stlnote">${SC.noteFml}</div>`;
  }
  return `<div class="favhead"><span class="ft">💰 정산 계산 항목 — API 필드 코드</span><span class="fs">각 항목이 어느 API 필드에서 오는지. 별도 필드 없는 항목은 「~에 포함」으로 흡수처 표시.</span></div>`+sub+bd;
}
```

- [ ] **Step 5: 테스트 + 브라우저 대조**

Run: `python -m pytest tests/marketplace_guide -v` → 전부 PASS.
브라우저: 정산 탭 → 「항목×마켓 매트릭스」와 「계산식 대입」 두 화면이 Task 0 스냅샷과 **완전 동일**(셀 값·노트 문구까지) + 콘솔 에러 0.

- [ ] **Step 6: Commit**

```bash
git add -A && git commit -m "refactor(판매처지도): 정산 계산 탭 SOT 이관 — 인라인 매트릭스·계산식 → settleCalc(JSON)+검증기"
```

---

### Task 4: 마켓 1장 브리핑 — `market_brief.py` + `/map-brief` 라우트

**Files:**
- Create: `프로그램/_시스템/webapp/market_brief.py`
- Modify: `프로그램/_시스템/webapp/routes/marketplace_guide.py`
- Create: `프로그램/_시스템/tests/marketplace_guide/test_map_brief.py`

- [ ] **Step 1: 테스트 작성(FAIL 확인)** — `test_map_brief.py` 신규:

```python
# -*- coding: utf-8 -*-
"""map-brief — 마켓 1개=마크다운 1장(전수정독 브리핑). SOT 조립·날조 금지 검증."""
import pathlib

import pytest
from flask import Flask

from webapp.routes import marketplace_guide as mg
from webapp.marketplace_api_map import load_map

MARKETS = [m["id"] for m in load_map()["markets"]]
REQUIRED = ["## 1. 개발환경", "## 2. API 카탈로그", "## 3. 정산 계산",
            "## 4. 주문상태 전환", "## 5. 통일 주문상태 전이",
            "## 6. 공식 문서 수집법", "## 7. 과거이력", "## 9. 요약"]


@pytest.mark.parametrize("mid", MARKETS)
def test_brief_has_all_sections(mid):
    from webapp.market_brief import build_brief
    md = build_brief(mid)
    assert md, mid
    for s in REQUIRED:
        assert s in md, f"{mid}: {s} 누락"


def test_brief_unknown_market_is_none():
    from webapp.market_brief import build_brief
    assert build_brief("nope") is None


def test_brief_api_count_matches_sot():
    from webapp.market_brief import build_brief
    data = load_map()
    for mid in MARKETS:
        n = sum(1 for a in data["apis"] if a["market"] == mid)
        assert f"{n}개" in build_brief(mid), mid


@pytest.fixture
def client(monkeypatch):
    monkeypatch.setenv("ENVIRONMENT", "test")
    app = Flask(__name__, template_folder="webapp/templates",
                root_path=pathlib.Path(mg.__file__).parents[2].as_posix())
    app.register_blueprint(mg.bp)
    return app.test_client()


def test_brief_route(client):
    r = client.get("/marketplace-guide/map-brief?market=coupang")
    assert r.status_code == 200
    assert "text/markdown" in r.headers.get("Content-Type", "")
    assert r.status_code == 200 and "## 2. API" in r.get_data(as_text=True)
    assert client.get("/marketplace-guide/map-brief?market=nope").status_code == 404
```

Run: `python -m pytest tests/marketplace_guide/test_map_brief.py -v` → FAIL(`market_brief` 없음).

- [ ] **Step 2: `market_brief.py` 작성**

```python
# -*- coding: utf-8 -*-
"""마켓 1개 = 마크다운 1장 브리핑(전수정독용).

출처 = 화면(/marketplace-guide/map)과 동일: marketplace_api_map.json(SOT)
+ api_ingest_paths.json(문서 수집법) + docs/markets/<id>.yaml(어댑터 근거, 있으면).
원칙: 날조 금지 — 없는 정보는 '확인불가'로 표기. 폴백 금지.
"""
from __future__ import annotations

import json
import os

_WEBAPP = os.path.dirname(__file__)
_DATA = os.path.join(_WEBAPP, "data")


def _load(name: str) -> dict:
    with open(os.path.join(_DATA, name), encoding="utf-8") as f:
        return json.load(f)


def _repo_root() -> str:
    # webapp → _시스템 → 프로그램 → 저장소 루트
    return os.path.dirname(os.path.dirname(os.path.dirname(_WEBAPP)))


def _yaml_profile(mid: str) -> str | None:
    p = os.path.join(_repo_root(), "docs", "markets", f"{mid}.yaml")
    if os.path.exists(p):
        with open(p, encoding="utf-8") as f:
            return f.read()
    return None


def _j(v) -> str:
    return json.dumps(v, ensure_ascii=False, indent=1)


def build_brief(mid: str) -> str | None:
    data = _load("marketplace_api_map.json")
    mk = next((m for m in data.get("markets", []) if m.get("id") == mid), None)
    if mk is None:
        return None
    out: list[str] = []
    add = out.append
    label = mk.get("label", mid)
    add(f"# {label} 개발 브리핑 (자동생성 — 데이터 코드 지도 SOT)")
    add("")
    add("> 출처: marketplace_api_map.json · api_ingest_paths.json · docs/markets/*.yaml — 화면(/marketplace-guide/map)과 같은 원천.")
    add("> 원칙: 날조 금지 — 없는 정보는 '확인불가'.")
    add("")
    add("## 1. 개발환경")
    for k in ("auth", "base_url", "env_prefixes", "concurrency", "rate"):
        v = mk.get(k)
        add(f"- {k}: {v if v not in (None, '', []) else '확인불가'}")
    u = mk.get("upload_limit") or {}
    if u:
        add(f"- 업로드 한도(실측): {u.get('measured', '확인불가')} · 단위={u.get('scope', '?')}"
            f" · 동시={u.get('concurrent', '?')} · 문서한도={u.get('limit_doc', '?')}")
        if u.get("note"):
            add(f"  - {u['note']}")
    add("")
    apis = [a for a in data.get("apis", []) if a.get("market") == mid]
    add(f"## 2. API 카탈로그 — {len(apis)}개 (st: ok=라이브검증 · code=코드있음(검증대기) · off=문서만 · todo=미확인)")
    by_cat: dict[str, list] = {}
    for a in apis:
        by_cat.setdefault(a.get("category") or "기타", []).append(a)
    for cat, items in by_cat.items():
        add(f"### {cat} ({len(items)})")
        for a in items:
            add(f"- [{a.get('st', '?')}] ({a.get('dir', '?')}) **{a.get('nm', '')}** — `{a.get('endpoint', '')}`"
                f"  (id={a.get('id', '')}, tabs={','.join(a.get('tabs') or [])})")
            if a.get("codeRef"):
                add(f"  - 코드: `{a['codeRef']}`")
            if a.get("st") in ("ok", "code"):
                for key in ("req", "res", "fields", "success"):
                    if a.get(key):
                        add(f"  - {key}: {_j(a[key])}")
            for key in ("idTraps", "persistIds"):
                if a.get(key):
                    add(f"  - {key}: {_j(a[key])}")
        add("")
    sc = data.get("settleCalc") or {}
    idx = next((i for i, m in enumerate(sc.get("markets", [])) if m.get("id") == mid), None)
    add("## 3. 정산 계산 (settleCalc)")
    if idx is None:
        add("- 확인불가(이 마켓의 정산 계산 데이터 미접수)")
    else:
        add(f"- 정산 API: {sc['markets'][idx].get('api', '?')}")
        for r in sc.get("rows", []):
            cells = r.get("cells") or []
            c = cells[idx] if idx < len(cells) else {}
            val = c.get("c") or (f"{c.get('inc')} 에 포함" + (f" ({c['n']})" if c.get("n") else "")) if c else "확인불가"
            add(f"- {r.get('item')} ({r.get('op')}): `{val}`")
        tot = sc.get("total") or []
        if idx < len(tot):
            add(f"- 최종 정산액: `{tot[idx]}`")
    add("")
    ac = data.get("autoConfirm") or {}
    ent = next((m for m in ac.get("markets", []) if m.get("id") == mid), None)
    call = (ac.get("calls") or {}).get(mid)
    add("## 4. 주문상태 전환 (autoConfirm)")
    if not ent and not call:
        add("- 확인불가(미배선 또는 미접수)")
    if ent:
        add(f"- 전환 API: `{ent.get('api')}` · 식별자: {ent.get('ids')} · 검증: {ent.get('v')}")
    if call:
        add(f"- 인증: {call.get('auth')}")
        for r in call.get("rows", []):
            add(f"- {r[0]}: `{r[1]}` — {r[2]}")
        if call.get("note"):
            add(f"- ⚠️ {call['note']}")
    for c in ac.get("cautions", []):
        if c.get("id") == mid:
            add(f"- ⚠️ {c.get('text')}")
    add("")
    add("## 5. 통일 주문상태 전이 (transitions)")
    hit = False
    for t in data.get("transitions", []):
        ref = (t.get("perMarket") or {}).get(mid)
        if ref:
            hit = True
            add(f"- {t.get('from')} → {t.get('to')}: {ref}")
    if not hit:
        add("- 확인불가(이 마켓 참조 없음)")
    add("")
    add("## 6. 공식 문서 수집법 (api_ingest_paths.json)")
    try:
        ing = _load("api_ingest_paths.json")
        row = next((r for r in ing.get("matrix", [])
                    if isinstance(r, list) and r and label in str(r[0])), None)
        if row:
            add(f"- 매트릭스(경로별 뚫림): {row}")
        h = next((x for x in ing.get("hier", []) if label in str(x.get("mk", ""))), None)
        if h:
            add(f"- 채택 경로: {h.get('route', '?')}")
            add(f"- 상위탭: {h.get('top', '?')}")
            add(f"- 카테고리: {h.get('cats', '?')}")
            if h.get("detail"):
                add(f"- 상세: {h['detail']}")
        if not row and not h:
            add("- 확인불가(수집법 데이터에 이 마켓 없음)")
    except OSError:
        add("- 확인불가(api_ingest_paths.json 읽기 실패)")
    add("")
    incs = [i for i in data.get("incidents", []) if mid in (i.get("markets") or [])]
    add(f"## 7. 과거이력 ({len(incs)}건)")
    for i in incs:
        add(f"- [{i.get('date')}·{i.get('severity')}·{i.get('status')}] {i.get('title')}"
            f" — 증상: {i.get('symptom')} / 원인: {i.get('cause')} / 해결: {i.get('fix')} / 교훈: {i.get('lesson')}")
    add("")
    add("## 8. 어댑터 프로파일 (docs/markets/*.yaml — 라이브 코드 근거)")
    y = _yaml_profile(mid)
    if y:
        add("```yaml")
        add(y.rstrip())
        add("```")
    else:
        add("- 확인불가(yaml 없음 — 서버 배포본엔 docs/ 미포함일 수 있음. 로컬 저장소에서 확인)")
    add("")
    wired = sum(1 for a in apis if a.get("st") in ("ok", "code"))
    off = sum(1 for a in apis if a.get("st") == "off")
    todo = sum(1 for a in apis if a.get("st") == "todo")
    add("## 9. 요약 — 배선 현황")
    add(f"- 우리 코드 연결(ok+code): {wired} · 문서만(off): {off} · 미확인(todo): {todo}")
    add("- 빈칸 채우기: 인앱 「📘 API 문서 수집법」 탭(=docs/markets/_API문서수집법.md) 플레이북 순서로 확보 → 이 JSON에 되채움(validate_map 통과).")
    return "\n".join(out)
```

- [ ] **Step 3: 라우트 추가** — `marketplace_guide.py`의 `map_data()` 아래에:

```python
@bp.route("/map-brief")
def map_brief():
    """마켓 1개 = 마크다운 1장 브리핑(전수정독 게이트용). ?market=coupang"""
    from webapp.market_brief import build_brief
    mid = request.args.get("market", "")
    md = build_brief(mid)
    if md is None:
        return make_response((f"unknown market: {mid}", 404))
    resp = make_response(md)
    resp.headers["Content-Type"] = "text/markdown; charset=utf-8"
    return resp
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest tests/marketplace_guide -v`
Expected: 전부 PASS.

- [ ] **Step 5: 실서빙 확인** — 로컬 서버에서 `curl -s "http://localhost:<port>/marketplace-guide/map-brief?market=coupang" | head -40` → 마크다운 헤더·섹션 확인. 6개 마켓 전부 200.

- [ ] **Step 6: Commit**

```bash
git add -A && git commit -m "feat(판매처지도): /map-brief — 마켓 1개=마크다운 1장 브리핑(SOT 조립·확인불가 정직표기)"
```

---

### Task 5: 스킬 `consult-market-map` + CLAUDE.md 게이트 선언

**Files:**
- Create: `.claude/skills/consult-market-map/SKILL.md`
- Modify: `CLAUDE.md` (저장소 루트 — 프로젝트 CLAUDE.md)

- [ ] **Step 1: SKILL.md 작성**

```markdown
---
name: consult-market-map
description: 판매처(마켓) API 관련 개발·수정 시작 시 강제 발동 — 데이터 코드 지도 전수정독 게이트. Triggers — "쿠팡/스마트스토어/롯데온/11번가/옥션/G마켓" + "가격/재고/주문/정산/클레임/송장/등록/전환/연동/API" 조합의 개발 발화, "마켓 API 붙여줘", "판매처 기능 만들어줘". 이 관문 통과 전 마켓 API 코드 작성 금지.
---

# 판매처 지도 전수정독 게이트 (consult-market-map)

판매처(마켓) API 기능을 개발·수정할 때, 코드를 만지기 **전에** 데이터 코드 지도를 전수정독한다.
목적: 지도에 이미 있는 정보를 F12로 재발견하는 노가다와, 탭 몇 개만 보고 헤매는 것(삥삥)의 종식.

<HARD-GATE>
아래 0~4단계를 통과하기 전에는 마켓 API 코드를 작성·수정하지 않는다.
"간단한 수정이라 괜찮다"는 예외 사유가 아니다 — 과거이력·idTraps 미확인이 곧 사고 원인이었다.
</HARD-GATE>

## 6단계

0. **멈춤** — 발화에서 대상 마켓 id(coupang/smartstore/lotteon/eleven11/auction/gmarket)와 기능을 확정. TodoWrite로 1~5단계 체크리스트 생성.
1. **전수 정독** — 마켓 브리핑 한 장을 Read:
   - 라이브: `https://mou-m.com/marketplace-guide/map-brief?market=<id>` (WebFetch)
   - 로컬 저장소: `프로그램/_시스템/webapp/market_brief.py` 의 `build_brief("<id>")` 실행 결과
   섹션 1~9를 **전부** 읽었는지 체크: ①개발환경 ②API 카탈로그 ③정산 ④주문상태 전환 ⑤상태 전이 ⑥문서 수집법 ⑦과거이력 ⑧어댑터 yaml ⑨요약.
   특히 **⑦과거이력**(같은 함정 재발 방지)과 API 항목의 **idTraps** 는 건너뛰기 금지.
2. **브리핑 작성** — 이번 기능에 필요한 API만 추려 표로 정리: method·URL·요청필드·응답필드·에러코드·ID모델·인증·상태(st).
3. **갭 선순환** — 필요한 칸이 비어 있으면(st=off/todo·필드 불명):
   ① `docs/markets/_API문서수집법.md`(인앱 📘 탭과 동일) 플레이북 순서(A→A-2→robots→H→F→C→I)로 스스로 확보.
   ② 확보한 내용을 `프로그램/_시스템/webapp/data/marketplace_api_map.json` 에 되채움 — `validate_map` 통과 필수. 다음번엔 지도에 이미 있게 된다.
   ③ 그래도 못 구한 칸은 **"확인불가"** 로 명시(날조·추정 절대 금지. 폴백 금지).
4. **라이브 대조** — `/marketplace-guide/map` 팝업의 해당 마켓 탭을 열어 브리핑과 화면 일치(항목 수·상태색) 확인.
5. **개발 착수** — 이제 코드. TDD(superpowers:test-driven-development) 등 해당 작업 스킬로 진행.

## 완성 게이트

- 필요한 각 API가 "채워짐" 또는 "확인불가" 둘 중 하나로 명시되기 전에는 5단계 진입 금지.
- 3-②로 JSON을 고쳤으면 그 변경도 같은 브랜치에 커밋한다(지도 선순환 — 이것이 이 스킬의 존재 이유).
```

- [ ] **Step 2: 프로젝트 CLAUDE.md에 게이트 선언 추가** — `## ✅ 검증` 섹션 앞에:

```markdown
## 🗺️ 판매처(마켓) API 작업 시 (필독 — 전수정독 게이트)

> 판매처 **API 연동·가격·재고·주문·정산·클레임·송장** 작업을 시작하기 전,
> **`consult-market-map` 스킬(판매처 지도 전수정독 게이트)** 을 먼저 발동한다.
> 마켓 브리핑 = `GET /marketplace-guide/map-brief?market=<id>` (지도 SOT에서 자동생성 · 1마켓 1장).
> 지도에 없는 정보는 `docs/markets/_API문서수집법.md` 플레이북으로 확보해 **지도(JSON)에 되채운 뒤** 개발한다.
```

- [ ] **Step 3: 스킬 발동 확인(수동 베이스라인)** — 새 대화 가정 시나리오 3개를 SKILL.md description과 대조: "쿠팡 가격 자동으로 올려줘"(발동○) · "롯데온 클레임 조회 붙여줘"(발동○) · "무신사 크롤 고쳐줘"(발동× — 소싱처는 add-source 영역). description 트리거 문구가 세 케이스를 올바르게 가르는지 확인, 아니면 description 수정.

- [ ] **Step 4: Commit**

```bash
git add .claude/skills/consult-market-map CLAUDE.md
git commit -m "feat(스킬): consult-market-map — 판매처 지도 전수정독 게이트(6단계·갭 선순환·확인불가 정직표기)"
```

---

### Task 6: 전수 검증 → 머지 → 배포 → 라이브 대조

**Files:** 없음(검증·배포)

- [ ] **Step 1: 전체 관련 테스트**

Run: `cd "프로그램/_시스템" && python -m pytest tests/marketplace_guide tests/markets -v`
Expected: 전부 PASS. (⚠️ 인자 없는 pytest 금지.)

- [ ] **Step 2: 로컬 브라우저 최종 전수** — 9개 상위탭 + 마켓별 전체 API 6마켓 + 정산 2하위 + 주문상태 5하위 전부 클릭. Task 0 스냅샷과 대조: **모든 화면 동일** + 콘솔 에러 0 + `map-data.json` 의 `validation_errors: []`.

- [ ] **Step 3: 머지·배포**

```bash
git push -u origin feat/marketmap-sot-brief
gh pr create --base main --title "판매처지도 단일원천화(autoConfirm·settleCalc) + map-brief + consult-market-map 게이트" --body "스펙: docs/superpowers/specs/2026-07-21-판매처지도-단일원천화-전수정독-design.md — 렌더 불변 검증 완료(전/후 탭 전수 대조)"
gh pr merge --merge
gh run watch $(gh run list --branch main --limit 1 --json databaseId -q ".[0].databaseId")
```

Expected: GitHub Actions 성공 → Fly.io 배포.

- [ ] **Step 4: 라이브 대조(Q1-C 약속)** — `https://mou-m.com/marketplace-guide/map` 팝업을 실제로 열어 탭 전수 클릭(특히 주문상태·정산) → 배포 전과 동일 + `https://mou-m.com/marketplace-guide/map-brief?market=coupang` 200·섹션 9개 확인. ⚠️ Ctrl+F5(캐시).

- [ ] **Step 5: 마무리** — 메모리 갱신(`project_marketplace_api_map_sot` 에 autoConfirm·settleCalc·map-brief·스킬 추가 기록) + `/conversation-wrapup` 형식 보고. 워크트리 정리는 사용자 확인 후.

---

## Self-Review 체크 결과

- **스펙 커버리지**: 1부(§4 죽은코드 삭제=Task1, 살아있는 인라인 이관=Task2·3, 검증게이트=각 태스크 Step) ✅ · 2부(§5.1 덤프=Task4, §5.2 스킬=Task5) ✅ · 배포·라이브대조(§6, Q1-C)=Task6 ✅.
- **의도적 제외**: 스펙 §4.2의 "문서수집법 이관"은 이미 JSON(`api_ingest_paths.json`)으로 되어 있음이 실측 확인되어 태스크 불필요(사전 조사 표 참조).
- **타입 일관성**: `autoConfirm.markets[].{id,api,ids,v}` · `settleCalc.rows[].cells[].{c|inc,n}` — Task2·3 JSON ↔ JS 렌더 ↔ Task4 브리핑 ↔ 검증기 모두 동일 키 사용 확인.
- **플레이스홀더**: 없음(모든 코드·데이터 전문 수록).
