# 주문내역 #3 CS 「고객문의」 탭(조회 우선) + 완료건 정리 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** CS 탭 「고객문의」 구역(인박스: 좌 목록+우 답변)을 쿠팡·스스 실조회로 채우고(전송 OFF), 완료 건(#2 대응완료·#3 답변완료)을 7일 자동 숨김 + 수기 삭제로 정리한다.

**Architecture:** 신규 `lemouton/cs_inquiries`(models `InquiryHandling`·service: 마켓 어댑터 정규화+list_inquiries+dismiss+reply-preview). #2 `lemouton/claims` 확장(`dismissed_at`+대응완료 7일 필터+dismiss). 라우트는 orders 블루프린트에 `/orders/cs/inquiries*`·`/claims/dismiss`. UI는 index.html cs 탭 「고객문의」 구역(시안 3). 답변 전송은 `LEMOUTON_LIVE_INQUIRY_REPLY` OFF.

**Tech Stack:** Python 3, Flask, SQLAlchemy(`shared.db.Base`), pytest. cwd=`프로그램/_시스템`.

**스펙:** `docs/superpowers/specs/2026-07-16-CS-고객문의탭-조회우선-design.md` **시안:** `바탕화면/모음전_CS_고객문의_시안_v3.html`(3번 인박스)
**분기:** `main` 기준 새 워크트리.

**★필드명 주의:** 쿠팡 `onlineInquiries`·스스 `pay-user/inquiries` 응답 JSON 필드는 미검증. 정규화 어댑터는 방어적으로(여러 후보 키 폴백) 쓰고, **Task 8 라이브에서 실제 필드 확인·보정**한다(거짓 완료 금지 — 프로젝트 규율).

---

## File Structure
- **Modify** `lemouton/claims/models.py` — `ClaimHandling.dismissed_at` 추가.
- **Modify** `lemouton/claims/service.py` — list_claims 대응완료 7일+dismissed 필터, `dismiss_claim`.
- **Create** `lemouton/cs_inquiries/__init__.py`·`models.py`(`InquiryHandling`)·`service.py`.
- **Modify** `app.py` — `import lemouton.cs_inquiries.models`.
- **Modify** `webapp/routes/orders.py` — `/cs/inquiries.json`·`/cs/inquiries/reply-preview`·`/cs/inquiries/dismiss`·`/cs/claims/dismiss`.
- **Modify** `webapp/templates/orders/index.html` — 「고객문의」 구역(인박스) + 대응완료·답변완료 「삭제」.
- **Tests**: `tests/claims/test_claim_service.py`(추가), `tests/cs_inquiries/__init__.py`+`test_inquiry_service.py`, `tests/orders/test_cs_routes.py`(추가).

---

## Task 1: #2 대응완료 7일 자동 숨김 + 수기 삭제 (데이터·서비스)

**Files:** Modify `lemouton/claims/models.py`, `lemouton/claims/service.py` · Test `tests/claims/test_claim_service.py`

- [ ] **Step 1: 실패 테스트** — `test_claim_service.py`에 추가:

```python
def test_done_retention_7days_and_dismiss(session, monkeypatch):
    from lemouton.claims import service as sv
    import datetime as dt
    KST = dt.timezone(dt.timedelta(hours=9))
    now = dt.datetime(2026, 7, 16, 12, tzinfo=KST)
    rows = [
        {"판매처": "롯데온", "오픈마켓주문번호": "D1", "주문상태": "취소완료", "주문상태원본": "21", "_change_date": "20260715120000"},  # 1일전=노출
        {"판매처": "롯데온", "오픈마켓주문번호": "D8", "주문상태": "취소완료", "주문상태원본": "21", "_change_date": "20260706120000"},  # 10일전=숨김
        {"판매처": "쿠팡", "오픈마켓주문번호": "D2", "주문상태": "취소완료", "주문상태원본": "RETURNS_COMPLETED", "_change_date": "20260716010000"},  # 오늘=노출→삭제할것
        {"판매처": "11번가", "오픈마켓주문번호": "N1", "주문상태": "취소요청", "주문상태원본": "02", "_change_date": "20260701000000"},  # 요청=항상노출(오래돼도)
    ]
    monkeypatch.setattr(sv, "status_change_rows", lambda markets, **kw: rows)
    sv.dismiss_claim("쿠팡:D2:취소", market="쿠팡", order_no="D2", claim_type="취소", session=session)
    res = sv.list_claims(["lotteon","coupang","eleven11"], since=now, until=now, now=now, session=session)
    done = {c["오픈마켓주문번호"] for c in res["groups"]["대응완료"]}
    assert done == {"D1"}                          # D8=7일초과 숨김, D2=삭제됨 숨김
    assert {c["오픈마켓주문번호"] for c in res["groups"]["신규요청"]} == {"N1"}  # 요청은 오래돼도 노출
```

- [ ] **Step 2: 실패 확인** — `python -m pytest tests/claims/test_claim_service.py::test_done_retention_7days_and_dismiss -q` → FAIL (`dismiss_claim` 없음 / 필터 없음).

- [ ] **Step 3: 구현**
`models.py` `ClaimHandling`에 컬럼 추가(다른 컬럼 옆):
```python
    dismissed_at = Column(DateTime)   # 수기 삭제(있으면 목록서 숨김)
```
`service.py` — 날짜 헬퍼 + dismiss + list_claims 필터. 상단에 추가:
```python
import re as _re_cs


def _ymd(s):
    m = _re_cs.search(r"(\d{4})[-./]?(\d{2})[-./]?(\d{2})", str(s or ""))
    if not m:
        return None
    try:
        return _dt.date(int(m.group(1)), int(m.group(2)), int(m.group(3)))
    except ValueError:
        return None


def dismiss_claim(claim_key, *, market="", order_no="", claim_type="", session=None):
    own = session is None
    session = session or SessionLocal()
    try:
        row = _get_or_create(session, claim_key, market=market, order_no=order_no, claim_type=claim_type)
        row.dismissed_at = _dt.datetime.now(_dt.timezone.utc)
        session.commit()
    finally:
        if own:
            session.close()
```
`list_claims` 시그니처에 `now=None` 추가하고, 루프에서 대응완료만 7일+dismissed 필터. 기존 루프의 `groups[v["단계"]].append(v)` 부분을 다음으로 교체:
```python
        today = (now or _dt.datetime.now(_dt.timezone.utc)).date()
        ...
        for r in rows:
            ack = handled.get(claim_key_of(r))
            v = _claim_view(r, ack)
            if v["단계"] == "대응완료":
                d = _ymd(r.get("_change_date"))
                if (ack and ack.dismissed_at) or (d is not None and (today - d).days > 7):
                    continue   # 삭제됨 or 완료 7일 초과 → 숨김
            groups[v["단계"]].append(v)
            counts["전체"] += 1
            counts[v["판매처"]] = counts.get(v["판매처"], 0) + 1
```
(`today` 계산은 루프 앞에 1회. `import datetime as _dt` 는 service.py에 이미 있음.)

- [ ] **Step 4: 통과** — `python -m pytest tests/claims/ -q` → all pass.
- [ ] **Step 5: 커밋**
```bash
git add "프로그램/_시스템/lemouton/claims/models.py" "프로그램/_시스템/lemouton/claims/service.py" "프로그램/_시스템/tests/claims/test_claim_service.py"
git commit -m "feat(cs): 대응완료 7일 자동숨김 + dismiss_claim(수기 삭제)"
```

---

## Task 2: #2 dismiss 라우트 + UI 삭제 버튼

**Files:** Modify `webapp/routes/orders.py`, `webapp/templates/orders/index.html` · Test `tests/orders/test_cs_routes.py`

- [ ] **Step 1: 실패 테스트** — `test_cs_routes.py`에 추가(기존 `_make_client()` 재사용):
```python
def test_claim_dismiss_post(monkeypatch):
    calls = {}
    monkeypatch.setattr("webapp.routes.orders._claim_svc.dismiss_claim", lambda ck, **kw: calls.__setitem__("d", ck))
    c = _make_client()
    r = c.post("/orders/cs/claims/dismiss", json={"claim_key": "쿠팡:D2:취소", "market":"쿠팡","order_no":"D2","claim_type":"취소"})
    assert r.status_code == 200 and calls["d"] == "쿠팡:D2:취소"
```
- [ ] **Step 2: 실패 확인** — FAIL(404).
- [ ] **Step 3: 구현** — `orders.py`에 라우트 추가:
```python
@bp.route('/cs/claims/dismiss', methods=['POST'])
def cs_claim_dismiss():
    d = request.get_json(silent=True) or {}
    ck = (d.get('claim_key') or '').strip()
    if not ck:
        return jsonify(ok=False, error='claim_key 필요'), 400
    try:
        _claim_svc.dismiss_claim(ck, market=d.get('market',''), order_no=d.get('order_no',''), claim_type=d.get('claim_type',''))
    except Exception as e:   # noqa: BLE001
        import logging; logging.getLogger(__name__).exception("cs claim dismiss failed")
        return jsonify(ok=False, error=str(e)), 200
    return jsonify(ok=True)
```
UI(index.html cs 대응완료 카드): 「삭제」 링크 추가 → `POST /orders/cs/claims/dismiss {claim_key,market,order_no,claim_type}` → 성공 시 재조회. (기존 대응완료 카드 렌더에 작은 삭제 버튼.)
- [ ] **Step 4: 통과** — `python -m pytest tests/orders/test_cs_routes.py -q`.
- [ ] **Step 5: 커밋**
```bash
git add "프로그램/_시스템/webapp/routes/orders.py" "프로그램/_시스템/webapp/templates/orders/index.html" "프로그램/_시스템/tests/orders/test_cs_routes.py"
git commit -m "feat(cs): 대응완료 삭제 라우트+UI(수기 정리)"
```

---

## Task 3: `InquiryHandling` 모델 + 등록

**Files:** Create `lemouton/cs_inquiries/__init__.py`·`models.py` · Modify `app.py` · Test `tests/cs_inquiries/__init__.py`+`test_inquiry_service.py`

- [ ] **Step 1: 실패 테스트** — Create `tests/cs_inquiries/__init__.py`(빈) + `tests/cs_inquiries/test_inquiry_service.py`:
```python
# -*- coding: utf-8 -*-
import datetime as dt
import pytest
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from shared.db import Base


@pytest.fixture()
def session():
    import lemouton.cs_inquiries.models
    eng = create_engine("sqlite:///:memory:")
    Base.metadata.create_all(eng)
    s = sessionmaker(bind=eng)()
    yield s
    s.close()


def test_inquiry_handling_table(session):
    from lemouton.cs_inquiries.models import InquiryHandling
    row = InquiryHandling(inquiry_key="쿠팡:Q1", market="쿠팡")
    session.add(row); session.commit()
    assert session.query(InquiryHandling).filter_by(inquiry_key="쿠팡:Q1").one().dismissed_at is None
```
- [ ] **Step 2: 실패 확인** — FAIL(`No module named 'lemouton.cs_inquiries'`).
- [ ] **Step 3: 구현** — Create `lemouton/cs_inquiries/__init__.py`(빈). `lemouton/cs_inquiries/models.py`:
```python
"""CS 고객문의 처리상태(팀 공유). create_all 자동생성(Alembic 불요). 삭제(정리) 플래그만 저장."""
from datetime import datetime, timezone
from sqlalchemy import Column, Integer, String, DateTime
from shared.db import Base


class InquiryHandling(Base):
    __tablename__ = "inquiry_handling"
    id = Column(Integer, primary_key=True, autoincrement=True)
    inquiry_key = Column(String(128), unique=True, nullable=False, index=True)  # market:inquiry_id
    market = Column(String(32))
    dismissed_at = Column(DateTime)
    created_at = Column(DateTime, default=lambda: datetime.now(timezone.utc))
    updated_at = Column(DateTime, default=lambda: datetime.now(timezone.utc),
                        onupdate=lambda: datetime.now(timezone.utc))
```
`app.py`에 등록(다른 모델 import 옆):
```python
    import lemouton.cs_inquiries.models  # noqa: F401  # CS 고객문의 처리상태
```
- [ ] **Step 4: 통과** — `python -m pytest tests/cs_inquiries/test_inquiry_service.py::test_inquiry_handling_table -q`.
- [ ] **Step 5: 커밋**
```bash
git add "프로그램/_시스템/lemouton/cs_inquiries/__init__.py" "프로그램/_시스템/lemouton/cs_inquiries/models.py" "프로그램/_시스템/app.py" "프로그램/_시스템/tests/cs_inquiries/__init__.py" "프로그램/_시스템/tests/cs_inquiries/test_inquiry_service.py"
git commit -m "feat(cs): InquiryHandling 모델 + 등록(create_all)"
```

---

## Task 4: 문의 정규화 어댑터 + `list_inquiries`

**Files:** Modify `lemouton/cs_inquiries/service.py`(create) · Test `tests/cs_inquiries/test_inquiry_service.py`

- [ ] **Step 1: 실패 테스트** — 추가:
```python
def test_normalize_and_list(session, monkeypatch):
    from lemouton.cs_inquiries import service as isv
    import datetime as dt
    KST = dt.timezone(dt.timedelta(hours=9))
    now = dt.datetime(2026, 7, 16, 12, tzinfo=KST)
    # 어댑터를 정규화된 행으로 대체(원 API 필드 대신 통일 dict 직접 반환)
    def fake_fetch(market, since, until, status):
        if market == "coupang":
            return [{"마켓":"쿠팡","문의형태":"온라인문의","문의ID":"CQ1","고객":"박지훈","상품":"키트",
                     "문의내용":"교환되나요","일시":"2026-07-16 08:05","상태":"미답변","답변내용":""}]
        if market == "smartstore":
            return [{"마켓":"스마트스토어","문의형태":"상품문의","문의ID":"SQ9","고객":"오세훈","상품":"티셔츠",
                     "문의내용":"남녀공용?","일시":"2026-07-05 11:05","상태":"답변완료","답변내용":"네 공용입니다","답변일":"20260705"}]
        raise RuntimeError("미지원")
    monkeypatch.setattr(isv, "_fetch_market", fake_fetch)
    res = isv.list_inquiries(["coupang","smartstore","lotteon"], since=now, until=now, now=now, session=session)
    assert [q["문의ID"] for q in res["groups"]["미답변"]] == ["CQ1"]
    # 답변완료 SQ9 는 답변일 2026-07-05 = 11일전 → 7일 필터로 숨김
    assert res["groups"]["답변완료"] == []
    assert any("연동 준비 중" in w for w in res["warnings"])   # lotteon 미지원
```
- [ ] **Step 2: 실패 확인** — FAIL(`no attribute 'list_inquiries'`).
- [ ] **Step 3: 구현** — Create `lemouton/cs_inquiries/service.py`:
```python
"""CS 고객문의 서비스 — 마켓별 조회 정규화 + 그룹핑 + 완료 7일/삭제 필터.

★쿠팡/스스 응답 필드는 미검증 → _normalize_* 에서 방어적 폴백. Task8 라이브 보정.
전송(reply)은 LEMOUTON_LIVE_INQUIRY_REPLY OFF(기본) — 미리보기만.
"""
import datetime as _dt
import os as _os
import re as _re

from shared.db import SessionLocal
from lemouton.cs_inquiries.models import InquiryHandling

_SUPPORTED = {"coupang", "smartstore"}   # 실조회 코드 있음. 롯데온·11번가=준비중
_MK_KO = {"coupang": "쿠팡", "smartstore": "스마트스토어", "lotteon": "롯데온", "eleven11": "11번가"}


def _ymd(s):
    m = _re.search(r"(\d{4})[-./]?(\d{2})[-./]?(\d{2})", str(s or ""))
    if not m:
        return None
    try:
        return _dt.date(int(m.group(1)), int(m.group(2)), int(m.group(3)))
    except ValueError:
        return None


def _fetch_market(market, since, until, status):
    """마켓 어댑터 → 정규화 dict 리스트. ★필드명 라이브 보정 대상.

    쿠팡: fetch_online_inquiries(answered_type ALL) → data[].(inquiryId·content·...)
    스스: fetch_inquiries(inquiry_status ALL/WAIT) → contents[].(inquiryNo·inquiryContent·answered)
    """
    if market == "coupang":
        from shared.platforms.coupang.inquiries import fetch_online_inquiries
        raw = fetch_online_inquiries(since, until, answered_type="ALL")
        return [_normalize_coupang(it) for it in (raw.get("data") or [])]
    if market == "smartstore":
        from shared.platforms.smartstore.orders import fetch_inquiries
        raw = fetch_inquiries(since, inquiry_status="ALL")
        items = raw.get("contents") or raw.get("data") or []
        return [_normalize_smartstore(it) for it in items]
    raise RuntimeError(f"{_MK_KO.get(market, market)} 문의 연동 준비 중")


def _g(d, *keys, default=""):
    for k in keys:
        v = d.get(k)
        if v not in (None, ""):
            return v
    return default


def _normalize_coupang(it):
    answered = bool(_g(it, "answered", "answeredAt", default="")) or _g(it, "answeredType") == "ANSWERED"
    return {"마켓": "쿠팡", "문의형태": "온라인문의", "문의ID": str(_g(it, "inquiryId", "onlineInquiryId")),
            "고객": _g(it, "buyerName", "memberId"), "상품": _g(it, "sellerProductName", "productName"),
            "문의내용": _g(it, "content", "inquiryContent"), "일시": _g(it, "inquiryAt", "createdAt"),
            "상태": "답변완료" if answered else "미답변", "답변내용": _g(it, "replyContent", "answerContent"),
            "답변일": _g(it, "answeredAt", "replyAt")}


def _normalize_smartstore(it):
    answered = _g(it, "inquiryStatus") == "ANSWERED" or bool(_g(it, "answered", "answerContent", default=""))
    return {"마켓": "스마트스토어", "문의형태": "상품문의", "문의ID": str(_g(it, "inquiryNo", "inquiryId")),
            "고객": _g(it, "customerName", "buyerName", "writerName"), "상품": _g(it, "productName", "productOrderName"),
            "문의내용": _g(it, "inquiryContent", "content", "question"), "일시": _g(it, "inquiryRegistrationDateTime", "createdAt"),
            "상태": "답변완료" if answered else "미답변", "답변내용": _g(it, "answerContent", "replyContent"),
            "답변일": _g(it, "answerDateTime", "answeredAt")}


def inquiry_key_of(row):
    return f'{row.get("마켓","")}:{row.get("문의ID","")}'


def list_inquiries(markets, *, since, until, now=None, session=None):
    own = session is None
    session = session or SessionLocal()
    try:
        today = (now or _dt.datetime.now(_dt.timezone.utc)).date()
        all_rows, warnings = [], []
        for mk in markets:
            if mk not in _SUPPORTED:
                warnings.append(f"[{_MK_KO.get(mk, mk)}] 문의 연동 준비 중")
                continue
            try:
                all_rows.extend(_fetch_market(mk, since, until, "ALL"))
            except Exception as e:   # noqa: BLE001
                warnings.append(f"[{_MK_KO.get(mk, mk)}] 문의 조회 실패: {e}")
        keys = [inquiry_key_of(r) for r in all_rows]
        dismissed = {h.inquiry_key for h in
                     session.query(InquiryHandling).filter(InquiryHandling.inquiry_key.in_(keys or [""])).all()
                     if h.dismissed_at is not None}
        groups = {"미답변": [], "답변완료": []}
        counts = {"전체": 0}
        for r in rows_iter(all_rows):
            r = dict(r); r["inquiry_key"] = inquiry_key_of(r)
            if r["상태"] == "답변완료":
                d = _ymd(r.get("답변일") or r.get("일시"))
                if r["inquiry_key"] in dismissed or (d is not None and (today - d).days > 7):
                    continue
            groups[r["상태"]].append(r)
            counts["전체"] += 1
            counts[r["마켓"]] = counts.get(r["마켓"], 0) + 1
        return {"groups": groups, "market_counts": counts, "warnings": warnings}
    finally:
        if own:
            session.close()


def rows_iter(rows):
    return rows
```
(주: `rows_iter` 는 테스트 monkeypatch 지점 분리용 얇은 래퍼 — 없어도 되면 제거 가능. 핵심은 `_fetch_market` 를 monkeypatch 한다.)

- [ ] **Step 4: 통과** — `python -m pytest tests/cs_inquiries/test_inquiry_service.py -q` → all pass.
- [ ] **Step 5: 커밋**
```bash
git add "프로그램/_시스템/lemouton/cs_inquiries/service.py" "프로그램/_시스템/tests/cs_inquiries/test_inquiry_service.py"
git commit -m "feat(cs): 문의 정규화 어댑터(쿠팡·스스)+list_inquiries(미지원 warnings·답변완료 7일필터)"
```

---

## Task 5: 문의 dismiss + reply-preview(OFF)

**Files:** Modify `lemouton/cs_inquiries/service.py` · Test `tests/cs_inquiries/test_inquiry_service.py`

- [ ] **Step 1: 실패 테스트** — 추가:
```python
def test_dismiss_and_reply_preview(session, monkeypatch):
    from lemouton.cs_inquiries import service as isv
    from lemouton.cs_inquiries.models import InquiryHandling
    isv.dismiss_inquiry("쿠팡:CQ1", market="쿠팡", session=session)
    assert session.query(InquiryHandling).filter_by(inquiry_key="쿠팡:CQ1").one().dismissed_at is not None
    # LIVE OFF → 실제 reply 함수 호출 안 됨(미리보기만)
    called = {"sent": False}
    monkeypatch.setenv("LEMOUTON_LIVE_INQUIRY_REPLY", "")
    res = isv.reply_preview("coupang", "CQ1", "안녕하세요 답변드립니다")
    assert res["sent"] is False and "안녕하세요" in res["preview"]
```
- [ ] **Step 2: 실패 확인** — FAIL(`no attribute 'dismiss_inquiry'`).
- [ ] **Step 3: 구현** — `service.py`에 추가:
```python
def dismiss_inquiry(inquiry_key, *, market="", session=None):
    own = session is None
    session = session or SessionLocal()
    try:
        row = session.query(InquiryHandling).filter_by(inquiry_key=inquiry_key).one_or_none()
        if row is None:
            row = InquiryHandling(inquiry_key=inquiry_key, market=market)
            session.add(row)
        row.dismissed_at = _dt.datetime.now(_dt.timezone.utc)
        session.commit()
    finally:
        if own:
            session.close()


def _live_reply_on():
    return _os.getenv("LEMOUTON_LIVE_INQUIRY_REPLY", "").strip().lower() in ("1", "true", "on", "yes")


def reply_preview(market, inquiry_id, content):
    """답변 미리보기. LIVE OFF(기본)면 실전송 안 함(거짓 전송 금지)."""
    if not _live_reply_on():
        return {"sent": False, "preview": content, "note": "전송 준비 중(검증 후 열림)"}
    # LIVE ON 경로(마켓별 실전송) — 검증 후에만 도달
    if market == "coupang":
        from shared.platforms.coupang.inquiries import reply_online_inquiry
        reply_online_inquiry(inquiry_id, content)
    elif market == "smartstore":
        from shared.platforms.smartstore.orders import reply_inquiry
        reply_inquiry(inquiry_id, content)
    else:
        raise RuntimeError(f"{market} 답변 전송 미지원")
    return {"sent": True, "preview": content}
```
- [ ] **Step 4: 통과** — `python -m pytest tests/cs_inquiries/ -q`.
- [ ] **Step 5: 커밋**
```bash
git add "프로그램/_시스템/lemouton/cs_inquiries/service.py" "프로그램/_시스템/tests/cs_inquiries/test_inquiry_service.py"
git commit -m "feat(cs): 문의 dismiss + reply_preview(LIVE OFF 기본·거짓전송 금지)"
```

---

## Task 6: 문의 라우트

**Files:** Modify `webapp/routes/orders.py` · Test `tests/orders/test_cs_routes.py`

- [ ] **Step 1: 실패 테스트** — 추가(기존 `_make_client()`):
```python
def test_inquiries_json_and_actions(monkeypatch):
    monkeypatch.setattr("webapp.routes.orders._inq_svc.list_inquiries",
                        lambda markets, **kw: {"groups": {"미답변":[{"문의ID":"Q","inquiry_key":"쿠팡:Q"}],"답변완료":[]},
                                               "market_counts": {"전체":1}, "warnings": ["[롯데온] 문의 연동 준비 중"]})
    calls = {}
    monkeypatch.setattr("webapp.routes.orders._inq_svc.dismiss_inquiry", lambda ik, **kw: calls.__setitem__("d", ik))
    monkeypatch.setattr("webapp.routes.orders._inq_svc.reply_preview", lambda m, i, c: {"sent": False, "preview": c})
    c = _make_client()
    r = c.get("/orders/cs/inquiries.json?markets=coupang&status=WAIT")
    assert r.status_code == 200 and r.get_json()["groups"]["미답변"][0]["문의ID"] == "Q"
    r2 = c.post("/orders/cs/inquiries/dismiss", json={"inquiry_key":"쿠팡:Q","market":"쿠팡"})
    r3 = c.post("/orders/cs/inquiries/reply-preview", json={"market":"coupang","inquiry_id":"Q","content":"답변"})
    assert r2.status_code == 200 and calls["d"] == "쿠팡:Q"
    assert r3.status_code == 200 and r3.get_json()["sent"] is False
```
- [ ] **Step 2: 실패 확인** — FAIL.
- [ ] **Step 3: 구현** — `orders.py` import 추가:
```python
from lemouton.cs_inquiries import service as _inq_svc
```
라우트 추가:
```python
@bp.route('/cs/inquiries.json')
def cs_inquiries():
    markets = _parse_markets(request.args)
    since, until = _parse_range(request.args)
    try:
        res = _inq_svc.list_inquiries(markets, since=since, until=until)
        return jsonify(ok=True, **res)
    except Exception as e:   # noqa: BLE001
        import logging; logging.getLogger(__name__).exception("cs inquiries failed")
        return jsonify(ok=False, error=str(e), groups={"미답변":[],"답변완료":[]}, market_counts={"전체":0}, warnings=[])


@bp.route('/cs/inquiries/dismiss', methods=['POST'])
def cs_inquiry_dismiss():
    d = request.get_json(silent=True) or {}
    ik = (d.get('inquiry_key') or '').strip()
    if not ik:
        return jsonify(ok=False, error='inquiry_key 필요'), 400
    try:
        _inq_svc.dismiss_inquiry(ik, market=d.get('market',''))
    except Exception as e:   # noqa: BLE001
        import logging; logging.getLogger(__name__).exception("cs inquiry dismiss failed")
        return jsonify(ok=False, error=str(e)), 200
    return jsonify(ok=True)


@bp.route('/cs/inquiries/reply-preview', methods=['POST'])
def cs_inquiry_reply_preview():
    d = request.get_json(silent=True) or {}
    try:
        res = _inq_svc.reply_preview(d.get('market',''), d.get('inquiry_id',''), d.get('content',''))
        return jsonify(ok=True, **res)
    except Exception as e:   # noqa: BLE001
        import logging; logging.getLogger(__name__).exception("cs reply preview failed")
        return jsonify(ok=False, error=str(e)), 200
```
- [ ] **Step 4: 통과** — `python -m pytest tests/orders/test_cs_routes.py -q`.
- [ ] **Step 5: 커밋**
```bash
git add "프로그램/_시스템/webapp/routes/orders.py" "프로그램/_시스템/tests/orders/test_cs_routes.py"
git commit -m "feat(cs): 문의 라우트 inquiries.json·dismiss·reply-preview"
```

---

## Task 7: UI — 「고객문의」 인박스(시안 3)

**Files:** Modify `webapp/templates/orders/index.html`

TDD 어려운 프론트. 확정 시안 `바탕화면/모음전_CS_고객문의_시안_v3.html`(3번 인박스) 구조를 옮긴다.

- [ ] **Step 1** — cs 탭 「고객문의」 토글 구역(#2에서 placeholder였던 자리)을 활성화. 상단 마켓 로고탭(#2 재사용) + [미답변][답변완료] 상태 탭 + 인박스(좌 목록·우 상세/답변).
- [ ] **Step 2** — `fetch('/orders/cs/inquiries.json?markets=<전체>&status=<선택>')`(항상 전 마켓, 클라 필터 — #2 교훈). 좌 목록 렌더(형태 배지·고객·일시·미리보기), 행 클릭 → 우 상세(문의 원문 + 답변칸 + 「미리보기」 + 「전송 🔒 준비 중」).
- [ ] **Step 3** — 답변완료 문의: 우 패널 내 답변 표시(읽기전용) + 「삭제」 → `POST /cs/inquiries/dismiss`. 「미리보기」 → `POST /cs/inquiries/reply-preview` → 결과 표시(전송 안 함).
- [ ] **Step 4** — warnings 배너(롯데온·11번가 "연동 준비 중"). 4상태(로딩/빈/에러/정상). esc() XSS 방어(문의내용·고객·상품). CSS 변수만, 색 의미 불변(형태 배지=시안 색).
- [ ] **Step 5** — Jinja 렌더 sanity: `python -c "from jinja2 import Environment, FileSystemLoader; Environment(loader=FileSystemLoader('webapp/templates')).get_template('orders/index.html')"` → 예외 없음. `python -m pytest tests/orders/ -q`(auto_confirm 기존 실패 제외 그린).
- [ ] **Step 6** — 커밋
```bash
git add "프로그램/_시스템/webapp/templates/orders/index.html"
git commit -m "feat(cs): 고객문의 인박스 UI(시안3·미답변/답변완료·답변 미리보기·삭제)"
```

---

## Task 8: 회귀·라이브 검증·머지·배포

- [ ] **Step 1: 회귀** — `cd 프로그램/_시스템 && python -m pytest tests/claims/ tests/cs_inquiries/ tests/orders/ tests/platforms/test_order_export.py -q` (auto_confirm 7건 기존 실패 제외 그린).
- [ ] **Step 2: main 머지+배포** — 최신 main pull→rebase(order_export/orders.py 충돌 주의)→ff-merge→push→`gh run watch` success.
- [ ] **Step 3: 라이브 검증**(`mou-m.com/orders/?tab=cs` 「고객문의」, 인증 세션)
  - `/orders/cs/inquiries.json?markets=coupang,smartstore&status=WAIT` 직접 조회 → 실제 미답변 문의 뜨나. **★쿠팡·스스 응답 필드 실제 확인** — `_normalize_*` 필드명 틀리면 보정 커밋.
  - 답변 작성→미리보기(전송 OFF 확인·실전송 없음).
  - 대응완료·답변완료 7일 필터·「삭제」 동작.
  - 롯데온·11번가 "연동 준비 중" 배너.
- [ ] **Step 4: 7섹션 보고서 + 메모리 갱신.**

---

## Self-Review (작성자 체크)
- **스펙 커버리지:** §3 화면=Task7 / §4.1 수집=Task4 / §4.2 전송OFF=Task5 / §4.3 모델=Task3 / §5 완료정리(#2)=Task1·2, (#3)=Task4(필터)·Task5(dismiss) / §6 라우트=Task2·6 / §7 엣지=Task4(warnings)·Task5(OFF) / §8 테스트=Task1·4·5·6. ✅
- **플레이스홀더:** 코드·명령·기대출력 포함. ★필드명 미검증은 Task8 라이브 보정으로 명시(추측 아님, 정직). UI·기존픽스처 재사용 주의 노트. ✅
- **타입/이름 일관성:** `dismiss_claim`·`dismiss_inquiry`·`list_inquiries`·`_fetch_market`·`inquiry_key_of`·`reply_preview`·`InquiryHandling.dismissed_at`·`ClaimHandling.dismissed_at`·`_inq_svc`·`_claim_svc` 전 태스크 동일. ✅
