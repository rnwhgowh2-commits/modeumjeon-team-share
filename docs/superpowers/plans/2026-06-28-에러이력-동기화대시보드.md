# 에러이력 동기화 대시보드 + 기재 거버넌스 — 구현 플랜

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** §5 에러 이력 카탈로그에 "전체크롤(코드) 기준·원문/보기 후행" 3중일치 동기화 대시보드 + rev 이력 + 공유 코드 교차회귀 방지 + 기재 거버넌스를 추가한다.

**Architecture:** 현재 `map.html`에 인라인된 `EC[]` 데이터를 `webapp/static/error_catalog.json` 단일 원천으로 분리한다. 보기(map.html)는 그 JSON을 fetch 해 동기화 대시보드(상태별 섹션 컴팩트 표)로 렌더하고, 백엔드 `guide_sync.py`(순수함수)는 같은 JSON을 읽어 심볼 존재·공유 코드 클러스터·ID 유일·원문↔보기 일치를 검사해 드리프트 배너로 표면화한다. 의미 판단(중복·모순·회귀)은 `크롤링-가이드.md` §5 거버넌스 박스가 사람에게 강제한다.

**Tech Stack:** Flask(Jinja) · 정적 JSON · Vanilla JS(map.html) · pytest(guide_sync) · Pretendard CSS.

**⚠️ 작업 위치:** 현재 브랜치 `feature/crawl-worker-queue`는 stale(정본 `_시스템/docs/크롤링-가이드.md` 없음). **반드시 origin/main 워크트리에서 작업**한다(spec §8·[[project_deploy_isolated_change_via_worktree]]). 모든 경로는 `_시스템` 루트 기준이며 실제 경로는 `프로그램/_시스템/...`.

---

## 파일 구조

| 파일 | 책임 | 변경 |
|---|---|---|
| `webapp/static/error_catalog.json` | 에러 카탈로그 **단일 원천**(55건 + 신규 필드) | **신규** |
| `webapp/templates/sourcing_guide/map.html` | EC 인라인 제거 → JSON fetch + 동기화 대시보드 렌더 | 수정 |
| `webapp/routes/guide_sync.py` | 카탈로그 로드·심볼/공유/ID/원문 검사 (순수함수) | 수정 |
| `tests/sourcing/test_error_catalog.py` | 위 순수함수 단위 테스트 | **신규** |
| `webapp/routes/sourcing_guide.py` | drift 에 카탈로그 검사 결과 합류 | 수정 |
| `docs/크롤링-가이드.md` | §5 머리에 기재 거버넌스 박스 + 동기화 개념 | 수정 |

**데이터 계약** — `error_catalog.json` = `{ "version": 1, "items": [ ITEM ] }`, `ITEM`:

```json
{
  "id": "S1",
  "g": "재고가격", "l": "be", "l2": "fe", "src": "르무통",
  "st": "ok", "cm": "d1fbe8fb 2d63a9e1",
  "sy": "사이즈별 재고가 상품합계로 균일 둔갑(전사이즈 53, 품절 265도 '있음')",
  "cz": "ⓐ _persist_option_stocks가 기존행만 갱신 ⓑ ext_bridge가 options[] 미전송→상품 합계 균일 폴백",
  "pv": "옵션 영속 단일 루틴 생성+갱신, parse·save 양쪽 호출 + ext_bridge options[] 전송",
  "code": { "file": "lemouton/sourcing/crawlers/ss_lemouton.py", "func": "persist_crawled_options", "desc": "옵션 영속 단일 루틴(생성+갱신)" },
  "crawl": "ok", "doc": "sync", "view": "sync",
  "rev": [
    {"d":"2026-06-26","a":"add","txt":"사이즈별 재고 합계 둔갑 최초 기재","src":"르무통","cm":"d1fbe8fb"},
    {"d":"2026-06-28","a":"mod","txt":"ext options[] 전송분 보강","src":"르무통","cm":"2d63a9e1"}
  ]
}
```

- `crawl`: `ok`(반영)·`warn`(심볼만, 라이브검증)·`no`(없음) — 기재자가 입력, guide_sync 가 `code` 심볼 존재로 교차검증.
- `doc`/`view`: `sync`·`lag`. `rule:true` 항목(철칙)은 `code`·`crawl` 없이 `g`+`sy`+`cz`+`pv`만.
- **섹션 분류(파생)**: `코드 미반영·검증필요 = crawl!=='ok'` / `문서 뒤처짐 = crawl==='ok' && (doc==='lag'||view==='lag')` / `완전 동기화 = 그 외`.
- **공유 코드(파생)**: `code.file+'::'+code.func` 가 같은 항목이 2+ 소싱처면 `shared`.

---

## Task 1: 카탈로그 JSON 생성 (현재 EC 이관 + 신규 필드 1건)

**Files:**
- Create: `webapp/static/error_catalog.json`

- [ ] **Step 1: 빈 골격 + 르무통 S1 1건만 작성**

`webapp/static/error_catalog.json` 에 위 데이터 계약대로 `version:1`, `items:[ {S1 전체} ]` 작성(위 ITEM 예시 그대로 1건). UTF-8, 끝에 개행.

- [ ] **Step 2: JSON 유효성 확인**

Run: `python -c "import json;d=json.load(open('webapp/static/error_catalog.json',encoding='utf-8'));print(len(d['items']),d['items'][0]['id'])"`
Expected: `1 S1`

- [ ] **Step 3: Commit**

```bash
git add webapp/static/error_catalog.json
git commit -m "feat(guide): 에러 카탈로그 JSON 단일원천 골격 + S1"
```

---

## Task 2: guide_sync — 카탈로그 로드 (TDD)

**Files:**
- Modify: `webapp/routes/guide_sync.py`
- Create: `tests/sourcing/test_error_catalog.py`

- [ ] **Step 1: 실패 테스트 작성**

`tests/sourcing/test_error_catalog.py`:

```python
import os
from webapp.routes.guide_sync import load_catalog

APP_ROOT = os.path.normpath(os.path.join(os.path.dirname(__file__), "..", ".."))

def test_load_catalog_returns_items():
    items = load_catalog(APP_ROOT)
    assert isinstance(items, list)
    assert any(it.get("id") == "S1" for it in items)

def test_load_catalog_missing_file_returns_empty(tmp_path):
    assert load_catalog(str(tmp_path)) == []
```

- [ ] **Step 2: 실패 확인**

Run: `pytest tests/sourcing/test_error_catalog.py::test_load_catalog_returns_items -v`
Expected: FAIL — `ImportError: cannot import name 'load_catalog'`

- [ ] **Step 3: 구현**

`guide_sync.py` 상단 `import os` 아래에 `import json` 추가, 파일 끝에:

```python
_CATALOG_REL = ("webapp", "static", "error_catalog.json")

def load_catalog(app_root):
    """error_catalog.json 의 items 리스트. 파일 없음/깨짐 → []. 절대 예외 안 던짐."""
    path = os.path.join(app_root, *_CATALOG_REL)
    try:
        with open(path, encoding="utf-8") as f:
            data = json.load(f)
    except (OSError, ValueError):
        return []
    items = data.get("items") if isinstance(data, dict) else None
    return items if isinstance(items, list) else []
```

- [ ] **Step 4: 통과 확인**

Run: `pytest tests/sourcing/test_error_catalog.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: Commit**

```bash
git add webapp/routes/guide_sync.py tests/sourcing/test_error_catalog.py
git commit -m "feat(guide_sync): load_catalog 순수함수 + 테스트"
```

---

## Task 3: guide_sync — 심볼 존재 교차검사 (TDD)

크롤 판정 근거: `code.func` 가 `code.file` 에 실재하나. `st=='ok'`/`crawl=='ok'` 인데 심볼 없으면 거짓(mismatch).

**Files:**
- Modify: `webapp/routes/guide_sync.py`, `tests/sourcing/test_error_catalog.py`

- [ ] **Step 1: 실패 테스트 작성** (append)

```python
from webapp.routes.guide_sync import catalog_symbol_drift

def test_symbol_drift_flags_missing_func(tmp_path):
    import json
    (tmp_path / "webapp" / "static").mkdir(parents=True)
    (tmp_path / "a.py").write_text("def other(): pass", encoding="utf-8")
    cat = {"version":1,"items":[
        {"id":"X1","src":"테스트","crawl":"ok","code":{"file":"a.py","func":"do_real"}},
    ]}
    (tmp_path / "webapp" / "static" / "error_catalog.json").write_text(
        json.dumps(cat), encoding="utf-8")
    out = catalog_symbol_drift(str(tmp_path))
    assert out == [{"id":"X1","func":"do_real","file":"a.py","crawl":"ok"}]

def test_symbol_drift_clean_when_present(tmp_path):
    import json
    (tmp_path / "webapp" / "static").mkdir(parents=True)
    (tmp_path / "a.py").write_text("def do_real(): pass", encoding="utf-8")
    cat = {"version":1,"items":[
        {"id":"X1","src":"t","crawl":"ok","code":{"file":"a.py","func":"do_real"}},
    ]}
    (tmp_path / "webapp" / "static" / "error_catalog.json").write_text(
        json.dumps(cat), encoding="utf-8")
    assert catalog_symbol_drift(str(tmp_path)) == []
```

- [ ] **Step 2: 실패 확인**

Run: `pytest tests/sourcing/test_error_catalog.py::test_symbol_drift_flags_missing_func -v`
Expected: FAIL — ImportError

- [ ] **Step 3: 구현** (append to guide_sync.py)

```python
def catalog_symbol_drift(app_root, catalog=None):
    """crawl=='ok' 인데 code.func 가 code.file 에 없는 항목(거짓 ok). rule 항목·code 없음은 skip."""
    items = catalog if catalog is not None else load_catalog(app_root)
    out = []
    for it in items:
        if it.get("rule"):
            continue
        code = it.get("code") or {}
        func, rel = code.get("func"), code.get("file")
        if not func or not rel:
            continue
        if it.get("crawl") != "ok":
            continue  # no/warn 은 이미 표면화됨 — ok 거짓만 검사
        path = os.path.join(app_root, *rel.split("/"))
        try:
            with open(path, encoding="utf-8") as f:
                present = func in f.read()
        except OSError:
            present = False
        if not present:
            out.append({"id": it.get("id"), "func": func, "file": rel, "crawl": "ok"})
    return out
```

- [ ] **Step 4: 통과 확인**

Run: `pytest tests/sourcing/test_error_catalog.py -v`
Expected: PASS (4 passed)

- [ ] **Step 5: Commit**

```bash
git add webapp/routes/guide_sync.py tests/sourcing/test_error_catalog.py
git commit -m "feat(guide_sync): catalog_symbol_drift — crawl ok 거짓 검사"
```

---

## Task 4: guide_sync — 공유 코드 영향맵 + ID 유일 (TDD)

**Files:**
- Modify: `webapp/routes/guide_sync.py`, `tests/sourcing/test_error_catalog.py`

- [ ] **Step 1: 실패 테스트 작성** (append)

```python
from webapp.routes.guide_sync import shared_code_map, duplicate_ids

def test_shared_code_map_groups_by_symbol():
    items = [
        {"id":"S1","src":"르무통","code":{"file":"a.py","func":"persist"}},
        {"id":"S2","src":"스마트스토어","code":{"file":"a.py","func":"persist"}},
        {"id":"S9","src":"롯데온","code":{"file":"b.py","func":"only"}},
    ]
    out = shared_code_map(items)
    assert out == {"a.py::persist": ["르무통", "스마트스토어"]}  # 2+ 소싱처만

def test_duplicate_ids():
    items = [{"id":"S1"},{"id":"S1"},{"id":"P3"}]
    assert duplicate_ids(items) == ["S1"]
```

- [ ] **Step 2: 실패 확인**

Run: `pytest tests/sourcing/test_error_catalog.py::test_shared_code_map_groups_by_symbol -v`
Expected: FAIL — ImportError

- [ ] **Step 3: 구현** (append)

```python
def shared_code_map(items):
    """code.file::func 별 소싱처 집합. 2+ 서로 다른 소싱처가 쓰는 심볼만 반환."""
    groups = {}
    for it in items or []:
        code = it.get("code") or {}
        func, rel = code.get("func"), code.get("file")
        if not func or not rel:
            continue
        key = rel + "::" + func
        src = it.get("src")
        bucket = groups.setdefault(key, [])
        if src and src not in bucket:
            bucket.append(src)
    return {k: v for k, v in groups.items() if len(v) >= 2}

def duplicate_ids(items):
    """중복된 id 목록(정렬, 유일)."""
    seen, dups = set(), set()
    for it in items or []:
        i = it.get("id")
        if not i:
            continue
        (dups if i in seen else seen).add(i)
    return sorted(dups)
```

- [ ] **Step 4: 통과 확인**

Run: `pytest tests/sourcing/test_error_catalog.py -v`
Expected: PASS (6 passed)

- [ ] **Step 5: Commit**

```bash
git add webapp/routes/guide_sync.py tests/sourcing/test_error_catalog.py
git commit -m "feat(guide_sync): shared_code_map + duplicate_ids"
```

---

## Task 5: guide_sync — 원문↔카탈로그 일치 + drift 통합 (TDD)

**Files:**
- Modify: `webapp/routes/guide_sync.py`, `tests/sourcing/test_error_catalog.py`

- [ ] **Step 1: 실패 테스트 작성** (append)

```python
from webapp.routes.guide_sync import catalog_doc_drift

def test_doc_drift_flags_id_absent_in_md():
    md = "표: S1 르무통 ... P3 SSG ..."
    items = [{"id":"S1","rule":False},{"id":"S2","rule":False},{"id":"P3"}]
    assert catalog_doc_drift(md, items) == ["S2"]  # md 에 없는 사건 id

def test_doc_drift_skips_rule_rows():
    items = [{"id":None,"rule":True,"sy":"폴백가 금지"}]
    assert catalog_doc_drift("아무 내용", items) == []
```

- [ ] **Step 2: 실패 확인**

Run: `pytest tests/sourcing/test_error_catalog.py::test_doc_drift_flags_id_absent_in_md -v`
Expected: FAIL — ImportError

- [ ] **Step 3: 구현** (append) + compute_guide_drift 통합

```python
def catalog_doc_drift(md_text, items):
    """사건 id 가 원문(.md)에 안 보이는 것. rule/ id없음 skip. 보기↔원문 드리프트 검출."""
    md = md_text or ""
    out = []
    for it in items or []:
        if it.get("rule"):
            continue
        i = it.get("id")
        if i and i not in md:
            out.append(i)
    return out
```

`compute_guide_drift` 끝부분 `return {...}` 직전에 카탈로그 검사 합류:

```python
    try:
        cat = load_catalog(app_root)
    except Exception:
        cat = []
    try:
        cat_sym = catalog_symbol_drift(app_root, cat)
    except Exception:
        cat_sym = []
    try:
        cat_dup = duplicate_ids(cat)
    except Exception:
        cat_dup = []
    try:
        cat_doc = catalog_doc_drift(md, cat)
    except Exception:
        cat_doc = []
    try:
        cat_shared = shared_code_map(cat)
    except Exception:
        cat_shared = {}
```

그리고 반환 dict 에 키 추가:

```python
    return {"missing_sources": ms, "missing_symbols": msym,
            "ext_baseline": GUIDE_EXT_BASELINE,
            "catalog_symbol_drift": cat_sym, "catalog_duplicate_ids": cat_dup,
            "catalog_doc_drift": cat_doc, "catalog_shared_code": cat_shared}
```

- [ ] **Step 4: 통과 확인 (회귀 포함)**

Run: `pytest tests/sourcing/test_error_catalog.py tests/sourcing/test_guide_sync.py -v`
Expected: PASS (기존 7 + 신규 8 모두 통과). `test_compute_guide_drift_real_repo_clean` 가 새 키로 안 깨지는지 확인 — 깨지면 그 테스트에 새 키 존재 assert 추가.

- [ ] **Step 5: Commit**

```bash
git add webapp/routes/guide_sync.py tests/sourcing/test_error_catalog.py
git commit -m "feat(guide_sync): catalog_doc_drift + compute_guide_drift 카탈로그 검사 통합"
```

---

## Task 6: 카탈로그 55건 백필

현재 `map.html` 의 `EC[]` 전 항목을 JSON `items` 로 옮기고 신규 필드를 채운다.

**Files:**
- Modify: `webapp/static/error_catalog.json`

- [ ] **Step 1: 전 항목 이관**

`map.html` EC 배열(철칙 5 + 에러 50)의 각 객체를 JSON item 으로 옮긴다. 규칙:
- `g,l,l2,src,st,cm,sy,cz,pv,rule` 그대로 복사.
- `id` 부여: 재고=S, 가격=P, 기타=O + 등장순 일련번호(원문 §5 표의 기존 번호와 일치). 철칙 항목은 `id:null,rule:true`.
- `code`: `cz`/`pv` 문장에서 핵심 함수·파일을 뽑아 `{file(상대경로),func,desc(한 줄)}`. 불명확하면 `func` 만 두고 `file` 은 best-guess(검사에서 no/ warn 으로 표면화되면 추후 교정).
- `crawl`: `st=='todo'` → `no`. `st=='ok'` 이고 코드가 라이브 확장(v0.7.x) 소관(예: 무신사 invMap·SSG 혜택)이면 `warn`, 그 외 `ok`.
- `doc`: 원문 §5 표에 그 사건이 있으면 `sync`(현재 전부 sync) — 없으면 `lag`.
- `view`: 전부 `sync`(JSON 이 곧 보기 원천이므로 기본 sync).
- `rev`: 알려진 최초 기재일·커밋으로 `add` 1건 이상. `cm` 의 날짜/해시 활용.

- [ ] **Step 2: 검사로 자기점검**

Run: `python -c "from webapp.routes.guide_sync import *; r='.'; import os; ar=os.path.abspath('.'); print('dup',duplicate_ids(load_catalog(ar))); print('symdrift',catalog_symbol_drift(ar)); print('shared',list(shared_code_map(load_catalog(ar)))[:5])"`
Expected: `dup []` · `symdrift []`(또는 교정 대상 소수 — 있으면 `crawl` 을 `no/warn` 로 정정하거나 `code.file` 교정) · `shared` 에 `persist_crawled_options`·`_match_option_so` 등 공유 심볼 노출.

- [ ] **Step 3: 항목 수 확인**

Run: `python -c "import json;print(len(json.load(open('webapp/static/error_catalog.json',encoding='utf-8'))['items']))"`
Expected: `55` (철칙 5 + 에러 50).

- [ ] **Step 4: Commit**

```bash
git add webapp/static/error_catalog.json
git commit -m "data(guide): 에러 카탈로그 55건 이관 + 신규 필드(code/crawl/doc/view/scope/rev)"
```

---

## Task 7: map.html — 인라인 EC 제거 → JSON fetch

**Files:**
- Modify: `webapp/templates/sourcing_guide/map.html` (현재 `var EC=[...]` ~ 렌더 IIFE: 약 548~660행)

- [ ] **Step 1: EC 배열 리터럴 삭제, fetch 로 교체**

`var EC=[ ... ];` 전체를 삭제하고, 기존 렌더 IIFE 를 fetch 로 감싼다. 기존 `GORD/GIC/GLABEL/...` 렌더 로직은 유지하되 진입을 비동기로:

```js
fetch('/static/error_catalog.json').then(r=>r.json()).then(function(data){
  var EC = data.items || [];
  // …기존 GORD.forEach 렌더 로직 그대로… (host=#errcards)
}).catch(function(){ /* 조용히 빈 렌더 */ });
```

- [ ] **Step 2: 라이브 렌더 확인 (브라우저)**

origin/main 로 로컬 앱 실행 후 `/sourcing-guide/map` §5 탭 → 기존 사건표가 **동일하게** 보이는지 육안 확인(이관 전후 동치). 콘솔 에러 0.

- [ ] **Step 3: Commit**

```bash
git add webapp/templates/sourcing_guide/map.html
git commit -m "refactor(guide): map.html EC 인라인 → error_catalog.json fetch"
```

---

## Task 8: map.html — 동기화 대시보드 렌더 (v7)

§5 탭 상단에 동기화 대시보드(요약 + 상태별 섹션 컴팩트 표 + 펼침)를 추가한다. 기존 사건표는 그 아래 유지.

**Files:**
- Modify: `webapp/templates/sourcing_guide/map.html`

- [ ] **Step 1: 마운트 지점 + 스타일**

§5 탭 콘텐츠 최상단, `#errcards` 위에 `<div id="syncdash"></div>` 추가. `바탕화면/모음전 에러이력 미니멀 확정본 v7.html` 의 `<style>` 중 대시보드 클래스(`.sum/.sech/table/.gl/.area/.sym/.det/.rev1/.foot` 등)를 map.html `<style>` 블록에 이식(기존 클래스명 충돌 시 `sd-` 프리픽스).

- [ ] **Step 2: 렌더 함수 작성**

Task 7 의 fetch 콜백 안, EC 확보 직후 호출:

```js
renderSyncDash(EC);

function renderSyncDash(items){
  var host=document.getElementById('syncdash'); if(!host) return;
  var errs=items.filter(function(e){return !e.rule;});
  var shared=buildShared(errs);            // file::func -> [srcs]
  function bucket(e){
    if(e.crawl!=='ok') return 'bad';        // 코드 미반영·검증필요
    if(e.doc==='lag'||e.view==='lag') return 'warn'; // 문서 뒤처짐
    return 'ok';                            // 완전 동기화
  }
  var groups={bad:[],warn:[],ok:[]};
  errs.forEach(function(e){groups[bucket(e)].push(e);});
  var meta={bad:{t:'코드 미반영·검증필요',h:'전체크롤 ✕/⚠ — 진짜 손볼 곳'},
            warn:{t:'문서 뒤처짐',h:'코드는 ✓, 가이드만 갱신하면 끝'},
            ok:{t:'완전 동기화',h:''}};
  var H='<div class="sum">'
    +stat(errs.length,'총 에러','')+stat(groups.ok.length,'완전 동기화','ok')
    +stat(groups.warn.length,'문서 뒤처짐','warn')+stat(groups.bad.length,'코드 미반영','bad')+'</div>';
  ['bad','warn','ok'].forEach(function(k){
    if(!groups[k].length) return;
    H+='<div class="sech '+k+'"><span class="d"></span>'+meta[k].t
       +' <span class="c">'+groups[k].length+'건</span>'
       +(meta[k].h?'<span class="hint">— '+meta[k].h+'</span>':'')+'</div>';
    H+='<table><thead><tr><th>영역</th><th>수정</th><th>소싱처</th><th>내용 · 코드</th>'
       +'<th class="ct">크롤</th><th class="ct">원문</th><th class="ct">보기</th><th></th></tr></thead><tbody>';
    groups[k].forEach(function(e){ H+=rowHtml(e,shared); });
    H+='</tbody></table>';
  });
  host.innerHTML=H;
  host.querySelectorAll('tr.r').forEach(function(r){
    r.addEventListener('click',function(){r.classList.toggle('open');});
  });
}
function stat(n,t,c){return '<div class="s '+(c||'')+'"><span class="n">'+n+'</span><span class="t">'+t+'</span></div>';}
function buildShared(errs){var g={};errs.forEach(function(e){var c=e.code||{};if(!c.func||!c.file)return;
  var k=c.file+'::'+c.func;(g[k]=g[k]||[]);if(e.src&&g[k].indexOf(e.src)<0)g[k].push(e.src);});
  var o={};Object.keys(g).forEach(function(k){if(g[k].length>=2)o[k]=g[k];});return o;}
function gl(s){var m={ok:['ok','✓'],no:['no','✕'],warn:['warn','⚠'],sync:['ok','✓'],lag:['lag','△']};
  var v=m[s]||['',s];return '<span class="gl '+v[0]+'">'+v[1]+'</span>';}
function esc(s){return String(s==null?'':s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');}
function rowHtml(e,shared){
  var c=e.code||{}; var key=(c.file&&c.func)?c.file+'::'+c.func:null;
  var sh=key&&shared[key]?shared[key]:null;
  var symTxt=(c.file?c.file.split('/').pop():'')+(c.func?' : '+c.func:'');
  var area=({재고가격:'📦+💰',재고:'📦 재고',가격:'💰 가격',기타:'🛠️ 기타'})[e.g]||e.g;
  var layer=({be:'백엔드',fe:'프론트',etc:'기타'});
  var lay=layer[e.l]||e.l||''; if(e.l2)lay+=' · '+(layer[e.l2]||e.l2);
  var autoCls=e.crawl==='ok'?'ok':(e.crawl==='no'?'no':'warn');
  var autoTxt=e.crawl==='ok'?'⚙ 심볼 확인':(e.crawl==='no'?'⚙ 심볼 미발견':'⚙ 검증필요');
  var shTag=sh?'<span class="shtag">🔗 공유 · '+sh.join('·')+'</span>':'';
  var revH=(e.rev||[]).map(function(r){return '<div class="rv"><span class="d">'+esc(r.d)+'</span>'
    +'<span class="a '+esc(r.a)+'">'+({add:'추가',mod:'수정',del:'삭제'}[r.a]||r.a)+'</span>'
    +'<span>'+esc(r.txt)+'</span><span class="src2">'+esc(r.src)+'</span>'
    +(r.cm?'<span class="cm">'+esc(r.cm)+'</span>':'')+'</div>';}).join('');
  return '<tr class="r"><td class="area">'+area+'</td><td class="layer">'+esc(lay)+'</td>'
    +'<td class="src">'+esc(e.src)+'</td>'
    +'<td>'+esc(e.sy)+'<div class="sym"><span class="auto '+autoCls+'">'+autoTxt+'</span>'
      +esc(symTxt)+' '+shTag+'</div></td>'
    +'<td class="ct">'+gl(e.crawl)+'</td><td class="ct">'+gl(e.doc)+'</td><td class="ct">'+gl(e.view)+'</td>'
    +'<td class="ar">▸</td></tr>'
    +'<tr class="det"><td colspan="8"><div class="inner">'
    +'<div class="dl"><span class="dk">원인</span><span>'+esc(e.cz)+'</span></div>'
    +'<div class="dl"><span class="dk">재발방지</span><span>'+esc(e.pv)+'</span></div>'
    +(sh?'<div class="note warn">🔗 공유 코드 — '+esc(sh.join('·'))+' 함께 영향. 수정 시 이 소싱처들 회귀 재크롤 필수.</div>':'')
    +'<div class="rev"><div class="rev-t">rev</div>'+revH+'</div>'
    +'</div></td></tr>';
}
```

`.shtag` 스타일 1줄 추가: `.shtag{font-size:11px;color:#7C3AED;margin-left:6px}`.

- [ ] **Step 3: 라이브 확인 (브라우저)**

`/sourcing-guide/map` §5 → 상단에 요약 + 3섹션(코드 미반영/문서 뒤처짐/완전 동기화) 컴팩트 표. 공유 심볼 행에 `🔗 공유` 태그. 행 클릭 → 원인·재발방지·공유 경고·rev 펼침. 콘솔 에러 0.

- [ ] **Step 4: Commit**

```bash
git add webapp/templates/sourcing_guide/map.html
git commit -m "feat(guide): §5 동기화 대시보드 렌더(상태별 섹션·공유태그·rev 펼침)"
```

---

## Task 9: 드리프트 배너에 카탈로그 검사 노출

**Files:**
- Modify: `webapp/templates/sourcing_guide/map.html` (drift 배너 영역)

- [ ] **Step 1: 배너 조건 확장**

기존 drift 배너(`{{ drift }}` 사용처) 옆에 카탈로그 위반 표시 추가:

```html
{% if drift.catalog_duplicate_ids %}<div class="syncbanner bad">중복 ID: {{ drift.catalog_duplicate_ids|join(', ') }}</div>{% endif %}
{% if drift.catalog_symbol_drift %}<div class="syncbanner bad">코드 심볼 미발견(거짓 ✓): {% for x in drift.catalog_symbol_drift %}{{ x.id }}({{ x.func }}) {% endfor %}</div>{% endif %}
{% if drift.catalog_doc_drift %}<div class="syncbanner warn">원문(.md) 누락: {{ drift.catalog_doc_drift|join(', ') }}</div>{% endif %}
```

(`.syncbanner` 스타일 없으면 기존 배너 클래스 재사용.)

- [ ] **Step 2: 라이브 확인**

배너가 위반 0 일 때 안 뜨고(침묵), 일부러 `error_catalog.json` 한 항목 `id` 중복 넣으면 "중복 ID" 배너 뜸 확인 후 되돌림.

- [ ] **Step 3: Commit**

```bash
git add webapp/templates/sourcing_guide/map.html
git commit -m "feat(guide): 카탈로그 검사(중복ID·심볼·원문누락) 드리프트 배너 노출"
```

---

## Task 10: 원문 §5 기재 거버넌스 박스 + 동기화 개념

**Files:**
- Modify: `docs/크롤링-가이드.md` (§5 머리)

- [ ] **Step 1: §5 머리에 박스 추가**

`## §5. 에러 이력 — 신규 추가 전 필독` 직후, 기존 분류 설명 위에 삽입:

```markdown
> 🧭 **판정 기준 = 전체크롤(코드).** 재발방지가 실제 코드에 들어갔나가 진실, 원문·보기는 그 코드를 따라왔나(동기화/뒤처짐)만 본다. 동기화 대시보드(`/sourcing-guide/map` §5)가 ❌ 코드 미반영 / 🔄 문서 뒤처짐 / ✅ 완전 동기화로 분류.

> 📝 **기재 거버넌스 — 추가/수정/삭제 판정 (중복·모순·교차회귀 방지)**
> 1. **근본원인 먼저** — 증상 아닌 "왜"를 확정.
> 2. **추가 vs 수정** — `같은 소싱처+같은 근본+같은 증상`이 이미 있으면 **수정**(기존 행에 커밋·rev 보강), 새 행 금지. 같은 근본 다른 소싱처면 둘 다 유지(예: S1·S2·S8).
> 3. **모순 검사** — 재발방지가 §1~§4와 어긋나면 (a)§1~4 정정 / (b)코드 위반 중이면 "§X 위반 중" 명시 / (c)그 외 모순은 사용자 보고.
> 4. **교차 회귀 방지 ⭐** — 소싱처 전용 코드에서 고칠 수 있으면 거기서(타 소싱처 무영향). 공유 코드(`_match_option_so`·`persist_crawled_options`·`compute_breakdown` 등) 수정이 불가피하면 **같은 코드 쓰는 다른 소싱처 전부 재크롤 회귀 검증** + 회귀테스트 PASS + 각 항목 rev. (94466889 회귀 재발 차단)
> 5. **삭제** — 버그 아님·진짜 중복일 때만. 묶을 수 있으면 유지(무결성).
> 6. **ID·rev·양쪽 반영** — prefix(S/P/O)+다음 번호(재사용 금지). 추가/수정/삭제마다 rev(날짜·액션·내용·소싱처·커밋). 원문(.md)+보기(error_catalog.json) **둘 다** 갱신.
```

- [ ] **Step 2: 원문↔보기 단일원천 주석**

§5 기존 "구조" 문단 끝에 한 줄: `> 보기 데이터 원천 = webapp/static/error_catalog.json(대시보드 fetch). 원문 표는 이 JSON 과 동기화 유지(자동점검이 누락 ID 배너로 표면화).`

- [ ] **Step 3: Commit**

```bash
git add docs/크롤링-가이드.md
git commit -m "docs(guide): §5 기재 거버넌스 박스 + 전체크롤 기준 동기화 개념"
```

---

## Task 11: 회귀 점검 + 배포 + 라이브 검증

**Files:** (없음 — 검증·배포)

- [ ] **Step 1: 전체 테스트**

Run: `pytest tests/sourcing/test_error_catalog.py tests/sourcing/test_guide_sync.py -v`
Expected: 전부 PASS(신규 8 + 기존 7).

- [ ] **Step 2: 배포 (origin/main)**

origin/main 워크트리에서 위 커밋들을 `push HEAD:main` → AWS Lightsail 자동배포. ([[project_deploy_isolated_change_via_worktree]])

- [ ] **Step 3: 라이브 검증**

`https://mou-m.com/sourcing-guide/map` §5:
- 상단 동기화 대시보드 = 요약 4숫자 + 상태별 섹션 컴팩트 표(✓/✕/△).
- 공유 심볼(예: persist_crawled_options) 행에 `🔗 공유 · 르무통·스마트스토어·무신사` 태그, 펼침에 회귀 경고.
- 행 클릭 → 원인·재발방지·rev 펼침.
- 드리프트 배너 = 위반 0이면 침묵.
- 기존 사건표·다른 6탭 정상(회귀 0).

- [ ] **Step 4: 7섹션 보고서** — `/ui-verify` 형식.

---

## 자가 점검 결과

- **스펙 커버리지**: §2 개념(Task 8 섹션 분류·Task 10) · §3 데이터모델(Task 1·6, scope=shared_code_map) · §4 화면(Task 8, v7) · §5 자동점검(Task 3·4·5·9) · §6 거버넌스(Task 10) · 교차회귀(Task 4 shared + Task 10 STEP4) · 비목표(F계열 미도입 — JSON 에 계열 필드 없음). ✅ 전 항목 매핑됨.
- **플레이스홀더**: 없음(전 코드 블록 실코드). Task 6 의 "best-guess file" 은 검사가 표면화하므로 의도된 반복 보정 루프.
- **타입 일관성**: `load_catalog→items[]`, `code:{file,func,desc}`, `crawl∈{ok,warn,no}`, `doc/view∈{sync,lag}`, `shared_code_map` 키 `file::func` — 백엔드(guide_sync)·프론트(buildShared) 동일 규칙. ✅
