# 완성도 백로그 — 자율 실행 진행 보고 (2026-07-03)

> 회원님 부재 중 자율 실행. **최종 라이브 배포(main 머지)는 안 함 — 돌아오시면 논의.**
> 모든 작업은 격리 워크트리 `C:/dev/moumjeon-backlog-wt` (origin/main 기준 `feature/completeness-backlog-20260703`).
> **회원님의 미커밋 작업 63개(feature/crawl-worker-queue)는 손대지 않음.**

---

## ✅ 완료 (검증·커밋·PR까지)

### PR #82 — 판매처 조용한 실패 4건 표면화
https://github.com/rnwhgowh2-commits/modeumjeon-team-share/pull/82 (**OPEN, 머지 안 함**)

| 항목 | 내용 | 방식 |
|---|---|---|
| #11 | `collect_all_linked_sets` 세트별 예외 삼킴 → `failed`/`failed_ids`+로그, 호출부까지 표면화 | TDD, sets 89 pass |
| #12 | 스스 `fetch_product_options` 파싱실패 success 위장 → `parse_failed`/`partial_failure` | TDD, uploader 49 pass |
| #10 | 센티넬 999가 전송값으로 샘 → `src_stock_qty`(실수량, 미상→None)로 통일 | TDD |
| #14 | 최종매입가 계산실패=정상 위장 → `final_error` 구분+로그, 카드 '상세 ⚠' | TDD |

- 대책 준수: throw 금지, **부분 성공 + 실패 표면화**(R2). 전부 origin/main 기준.
- 커밋: 1653d63b · daba1503 · 2634918b · 83d5a47b

---

## ⏳ 남은 항목 + 각 계획 (안전 경계 안에서 이어감)

### 안전 — 이어서 자율 진행
- **표면가 staleness 방어게이트**(판매처 #5) — `crawled_price`를 `last_status`로 게이팅. 소형·TDD 가능.
- **MED 조사(읽기 전용→문서화)**: 전체크롤 저장 원인(#4, CDP 자동화 한정 vs 제품버그 판별)·모음전 요재확인 5건 origin/main 대조·SSG 단품 14 매칭실패·데이터흐름 미확인 4건.
- **크롤가이드 §5**: origin/main 코드(`api_sources_parse.py:75`)는 이미 혜택 저장 중 → **stale 서술은 origin/main엔 없음**. stale 사본은 회원님 working 트리(feature/crawl-worker-queue)에 있을 가능성 → 그쪽 문서만 갱신 필요(배포와 무관, 돌아와서 확인).

### 🔴 위험 — 준비·문서화만 (블라인드 실행 안 함)
- **크롤 위젯 서버저장 검증(#6)** — 확장 JS(`background.js`). 서버 DB 저장 검증 로직 추가 필요. JS 테스트 하니스 확인 후 신중 진행 or 설계 문서화.
- **서버 크롤 제거 / 로컬 워커큐 이관(1-2)** — `scheduled_dispatch`(구현됨)가 스케줄러 미배선(죽은코드) + UI 버튼 3종이 서버 크롤 호출 + 로컬 워커 실가동 필요. **628커밋 분기 얽힘**이라 스크래치 검토·계획만. 실제 제거는 로컬 워커 실가동 확인 + 회원님 배포 논의 후.

### 정리 (회원님 액션/문서)
- 데스크톱 확장 임시 SSG 진단코드 회수(회원님 PC 폴더 — 저장소 밖).
- 확장 버전 스큐(0.7.6 vs 0.7.5) 실로드 폴더 확정(회원님 확인 필요).
- 작업 브랜치 stale(628 뒤) 결정 — 이 브랜치 계속 vs origin/main 재정렬.

---

## 🚦 돌아오시면 논의할 배포 목록 (제가 안 누른 것)
1. **PR #82** 머지=배포 — 판매처 4건 (검증 완료, 저위험)
2. **PR #81** 업로더 commit — 전송 OFF 확인 후 (금전위험 차단)
3. **PR #80** 롯데온 롯데오너스 — base 겹침(화면할인×롯데오너스) 실상품 1건 대조 후
4. 서버 크롤 제거 — 로컬 워커 실가동 확인 후 (맨 마지막)

## 🔬 라이브 검증 결과 (브라우저 연결 후, 2026-07-03)
- **#4 전체크롤 저장 = 버그 아님(확정).** 사용자 직접 전체크롤(65/65 성공) 후 서버 대조: pending 18→1, ok 47→64, 가격합 5,681,500→7,710,970. **정상 저장 확인.** 세션이 본 '0 저장'은 CDP 자동화 조종 탭 아티팩트(가설 #5 확정), 제품 버그 아님.
- **#6 위젯 vs 저장 = 미세 갭 1건.** 위젯 성공 65 vs DB ok 64(+pending 1). 치명적 아님이나 "위젯 성공=서버저장" 완전 일치는 아님 → 소형 개선 후보(위젯이 서버 ok 반영 확인).
- **판매처 정직성 수정(cfee3665) 라이브 반영 확인**: 쿠팡 현재재고 '미상'·스스 현재가 정상(0원 아님).

## 🔴 롯데온 이중차감 — 라이브 실측으로 확정 (PR#80 머지 보류)
- 전체크롤 후 저장된 롯데온 가격 = **118,720**(9건). 표면가 119,910은 저장 안 됨.
- 118,720 = 로그인 시 '나의 혜택가'(롯데오너스 1% 이미 반영). 즉 **크롤러가 로그인 상태로 표면가 아닌 회원혜택가를 저장**.
- → PR#80이 롯데오너스를 또 빼면 **이중차감**. **PR#80 머지 금지.**
- **진짜 수정 방향**: 롯데온 크롤러가 로그아웃 표면가(119,910)를 저장하거나, 롯데오너스를 표면가에서 분리해 혜택으로만 취급. (백로그 §4-3 정정: '해소' 아님 → 라이브서 이중차감 위험 확정)

## 🟡 롯데온 색상모음전 블랙 재고 불일치 — 라이브 확인 (신규 크롤 후에도 지속)
- 라이브 롯데온 블랙: 220~260 전 사이즈 재고(5~10개 남음). 프로그램은 일부 사이즈 품절 표기.
- 방금 전체크롤 직후에도 불일치 지속 → 재고 세션의 '크롤 타이밍' 가설 반증. 롯데온 색상모음전 per-size 재고 크롤(pbf/API vs 실재고) 실제 이슈 의심 → 크롤러 조사 필요.

## 모음전 요재확인 5건 — origin/main 대조 (진행 중)
- **① SSF 정규식** — origin/main `ssf.py` 에 공백 대응 `품절임박\s*\(\s*(\d+)\s*\)` 존재 → **해소로 보임**(stale 트리 우려 무효).
- ② 르무통 int 0폴백 / ③ 무신사 base_override — grep 미검출 → origin/main 에선 해당 패턴 없거나 리팩터됨(추가 확인 필요).
- ④ SSG MONEY rate=1 경계 / ⑤ unified fee=1.0 분모 — 공식 라인 확인 필요(다음 계속).

## 위치 요약
- 워크트리: `C:/dev/moumjeon-backlog-wt` (브랜치 feature/completeness-backlog-20260703)
- 원장: scratchpad/session_intake.md · 백로그: docs/검증세션_통합_완성도백로그_20260703.md
